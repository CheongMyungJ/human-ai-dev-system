# 작업 수준 판단과 의도 검토 UX — v0.8 2차 반영

갱신일: 2026-09-23. D-68~90의 UI 논의 확정분과 개발 인계를 2차 반영한다. 화면·대화 동작의 정본은 [대화 중심 UI 설계](ui-conversation-design.md)다. 초안 복구·검색 범위·활성 업무의 유형 변경·종료 후 설명·시간 한도·외부 열기의 제품 동작을 확정했으며 구체 저장 기술·API·실증은 후속 구현 대상이다. 현재 구현 기준은 DEVELOPMENT.md의 P4-03 완료·schema v15·Profile v2다. 아래 새 UI와 요청 제어를 구현 완료한 것으로 읽지 않는다. WorkDepth는 간소·표준·심층, Autonomy는 기본 ask-on-decision과 선택 controlled로 구분한다. Fast Lane은 실행 경로이며 전체 예산은 기본 무제한·설정 한도 강제다. 요청 정합성 확인은 항상 수행하고 독립 AI 의미 검토는 위험·불확실성·정책에 따라 선택한다. 선택된 독립 검토는 작성과 별도 세션에서 수행한다. 최초 품질 실패 후 repair 기본 최대 2회는 전체 예산과 별도로 유지한다. 상세 계약은 [자동진행·위임·예산 정책](autonomy-budget-policy.md)을 따른다.

## 1. 작업 수준은 크기보다 필요한 준비와 검증의 깊이를 표현

사람에게는 “작은/큰 작업”보다 “이번 업무에 이 정도 준비가 필요한 이유”가 보여야 한다. 변경 줄 수·파일 수·예상 토큰은 보조 자료이며 단독 판정 기준으로 사용하지 않는다. AI가 근거로 간소·표준·심층 수준을 구성하고 적용 이유를 표시하며 사용자가 조정할 수 있다.

분류는 실행 절차를 고정하지 않고 필요한 준비·검증을 구성하는 역할이다. AI는 [Case Profile](case-profiles.md)에 따라 목표·범위·종료조건을 정리하고 중요한 미정만 질문한다. 기능 개발 전용 문서·질문지를 모든 업무에 적용하지 않는다. 분석·연구에는 기존 권한·예산 안의 복구 가능한 로컬 재현·임시 계측·테스트를 포함하며 실험을 명시하지 않았다는 이유만으로 확인받지 않는다. 제품 수정으로 목적이 확대되거나 권한·외부 영향이 달라지면 해당 변경을 확인한다.

| 판단 축 | AI가 확인·제시할 근거 | 준비 또는 검증이 깊어지는 사례 |
|---|---|---|
| 사용자 의도와 성공 기준의 명확성 | 확인된 요구·AI 제안·미정 질문, 기존 화면/문서 근거 | 제품 동작 선택에 따라 결과가 크게 달라짐 |
| 변경 범위·구성 요소 의존성 | 영향받는 모듈·API·클라이언트·외부 시스템과 연결 | 여러 구성 요소를 함께 바꾸어야 동작함 |
| 호환성과 데이터 생명주기 | 공개 계약, 기존 데이터와 변환·보관 요구 | API 호환성, 스키마 변경, 데이터 마이그레이션 |
| 권한·보안·민감 데이터 영향 | 현재 권한 모델, 보호 대상과 새 접근 경로 | 단 한 줄이어도 접근 권한이나 민감 정보 노출 경로 변경 |
| 가역성과 실패 비용 | 복원 가능 여부, 되돌릴 데이터·환경·외부 반영 | 파괴적 데이터 변환, 운영 영향, 원상 복구 난도 |
| 해법·환경의 불확실성 | 확인한 사실, 상충 가설, 미검증 전제 | 동작 원인을 모르거나 실험 없이 해법 타당성 판단 불가 |
| 검증 난도 | 관찰 가능성, 재현성, 테스트 환경·기대값 확보 | 간헐성·동시성·성능·실환경 의존으로 단순 테스트가 불충분 |

각 축에는 숫자 점수보다 `관찰 근거 / 현재 판단 / 아직 확인할 것 / 준비·검증에 주는 영향`을 남긴다. 낮은 항목들의 평균으로 권한·마이그레이션 같은 중요한 영향을 상쇄하지 않는다. AI 권고와 실제 적용값·적용 정책을 구분하고 사람이 조정할 수 있게 한다. 깊이 조정으로 합의한 성공 기준·선택한 사람 검토·권한을 해제하지 않는다.

## 2. 확정된 세 수준에 대응하는 산출물 후보

모든 기능 개발에서 요청 정합성을 확인하되, 명확한 최초 요청을 정리했다는 이유로 같은 내용을 다시 승인받지 않는다. 기본 ask-on-decision은 실제 사람이 결정할 부분에서만 기다린다. controlled는 초기 목표·범위·기준·허용 행동과 변경 결과를 확인한다. 설계·계획의 사람 검토는 독립적으로 선택한다.

| 수준 | 적합한 조건 예시 | 설계·계획 표현 후보 | 검증 구성 후보 |
|---|---|---|---|
| 간소 | 의도가 명확하고 변경이 국소적이며 쉽게 복원·확인 가능 | 요청·범위·기준·변경 이유·구현/검증 계획의 최소 논리 기록. 별도 설계·계획 파일이나 실행을 강제하지 않음 | 해당 동작과 관련 회귀 확인. 관련 없는 대규모 검사는 강제하지 않음 |
| 표준 | 사용자 흐름·API·데이터 영향 중 설계 선택이 필요하고 영향 범위를 조사할 수 있음 | 요구별 설계 대응, 인터페이스/데이터/예외 처리, 의존 작업과 통합·검증 순서 | 설계 적합성, 계획 검증 가능성, 구현 검증과 완료 근거 평가 |
| 심층 | 영향이 넓거나 되돌리기 어렵거나 불확실성·검증 난도가 높음 | 대안과 선택 이유, 실패·복구 경로, 단계별 실험, 이행·통합 전략 등 해당 주제만 확장 | 필요한 전문 검토, 실험·성능·호환성·복구 검증 등 위험에 맞는 항목 추가 |

깊이를 높인다는 것이 항목과 문서를 모두 늘린다는 뜻은 아니다. 권한 변경은 권한 모델과 부정 사례를, 마이그레이션은 데이터 보존과 복구를 깊게 다룬다. 국소적·가역적이고 기대값이 명확하면 간소, 여러 인터페이스·데이터 동작의 설계가 필요하면 표준, 넓은 영향·어려운 복구·높은 불확실성에 중요한 근거가 있으면 심층을 제안한다. 불확실하면 근거 부족과 조사 작업을 표시한다. Fast Lane은 필요한 의미 기록·검증을 유지하면서 별도 절차를 합칠 수 있다. 간소라는 이름만으로 위험 검증을 줄이지 않는다.

WorkDepth·Autonomy·Budget을 독립 표시한다. 예를 들어 `심층 · ask-on-decision · 전체 예산 무제한`은 깊게 조사하면서 자동 진행한다. 기본값을 매번 선택하게 하지 않고 적용값과 조정 경로를 제공한다. Fast Lane 여부와 독립 검토 선택 이유도 확인할 수 있게 한다. Fast Lane에서 벗어나면 일반 자동 진행으로 전환하며 전환 자체가 사람 승인 요청은 아니다.

## 3. 예시로 분류 기준을 설명

아래는 확정 판정이 아니라 분류 근거가 어떻게 달라지는지 보이는 가상 사례다.

| 사례 | 초기 수준 제안 예시 | 달라질 수 있는 근거 | 핵심 검증 예시 |
|---|---|---|---|
| 버튼 문구 변경 | 간소 후보 | 다국어 키·접근성 이름·자동화 계약에 영향이 있으면 범위 재평가 | 의도한 화면/상태의 문구, 관련 접근성·다국어 동작 |
| CSV 표시/내보내기 추가 | 표준 후보 또는 정보 부족 | 행·열·권한·인코딩·데이터 규모가 미정이면 먼저 의도 구체화. 이미 정의된 작은 포맷 수정이면 간소 가능 | 합의한 데이터 집합, 호환성, 권한·성능 중 해당 기준 |
| 한 줄의 접근 권한 조건 변경 | 표준 이상의 준비 검토 후보 | 변경량보다 역할·리소스 경계와 권한 누수 가능성이 중요 | 허용/거부/경계 조합, 우회 경로, 기존 사용자 영향 |
| 간헐적 버그 원인 분석 | 심층 조사 후보 | 재현·관찰·실험이 핵심. 기능 개발의 의도→설계→계획을 그대로 강제하지 않음 | 가설을 구분하는 실험, 수정 전후 증거, 미재현의 한계 |
| 여러 모듈을 바꾸는 큰 기능 | 심층 후보 | 독립 분해 가능성·API 호환성·데이터 이행 여부에 따라 깊이 조정 | 요구별 대응, 통합 순서, 계약·이행·회귀 검증 |
| 새로운 기술 도입 연구 | 연구용 탐색 깊이 제안 | 질문·실험 범위·결정에 필요한 증거와 종료 조건이 중요 | 비교 조건의 일관성, 출처, 실험 재현성, 불확실성 |

중간에 정보가 바뀌면 영향받는 깊이·검사·작업을 재평가하고 근거를 표시한다. AI가 진행을 위해 사용자 명시 검토나 성공 기준을 조용히 낮추지 않는다. 자동 조건에 따른 검사 추가·생략과 사용자 OFF는 별도 출처로 남긴다.

## 4. 대화 중심 화면의 확정된 정보 구조

PC에서 한 프로젝트에 집중하는 사용을 기본으로, 대표 대화 하나를 Case 하나에 연결한다. 내부 Task·Run·작성/검토 세션은 활동 상세로 묶으며 별도 사용자 대화를 강제하지 않는다. 대화가 하나여도 독립 검토 세션·입력·판정의 출처와 증거는 분리하여 보존한다.

| 영역 | 구성 |
|---|---|
| 왼쪽 | 현재 프로젝트 선택, 새 대화, 검색, 답변 필요, 고정·최근 대화, 보관·설정. 다른 프로젝트 상황은 선택부에서 확인하며 자동 전환하지 않는다. |
| 가운데 | 대화, 실제 상태에 근거한 진행 요약, 질문·결정 카드, 공통 입력창. 도구·에이전트의 세부 활동은 펼쳐 본다. |
| 오른쪽 | 필요할 때 여는 결정 사항·의도/설계/계획 문서·코드 변경·검증 근거. Fast Lane에 존재하지 않는 문서의 빈 탭을 강제하지 않는다. |

프로젝트 전환 시 마지막 대화와 읽던 위치를 복원한다. 패널은 접기·크기 조절을 지원하며 설정한 크기를 기억한다. 오른쪽을 닫으면 가운데 대화를 넓게 쓴다. 라이트 테마가 기본이고 다크·시스템 따르기는 선택이다. 중립적인 색과 절제된 구분선을 사용하고 상태 색에는 의미를 부여한다. 일반 AI 답변을 모두 카드로 만들지 않으며 질문·결과처럼 조작과 판단이 필요한 내용에 카드를 쓴다. PC 창 크기 변화는 지원하고 휴대폰·태블릿 최적화는 후속 검토다.

Project·Profile, WorkDepth·Autonomy·Budget, 완료 지점, 위임·질문·Runner·GitHub 게시 상태는 간결한 요약과 상세 보기로 제공한다. 입력창 주변에는 프로젝트 기본 설정·작업 기준·자료 참조·전송/중단을 둔다. 매 업무마다 도구·모델·깊이를 다시 고르게 하지 않으며 업무별 조정값과 프로젝트 기본값으로 되돌리는 경로를 제공한다. 설정의 출처와 적용 시점을 보존한다.

진행률을 임의 백분율로 꾸미지 않고 현재 활동과 남은 작업을 요약한다. 답변·결과 확인 필요, 중요한 차단·오류·예산 문제, 완료에는 PC 알림을 제공하며 일상 진행에는 알리지 않는다. 현재 대화를 보고 있을 때 중복 팝업을 억제한다. PR 리뷰는 피드백 출처로 표시하고 기록용 이슈 댓글은 입력·승인으로 처리하지 않는다.

검색은 현재 프로젝트의 보관된 대화까지 포함하여 제목·요약·대화 본문·결정 사항을 찾는다. 결과에는 대화 제목과 일치 부분을 보여주고 해당 메시지·결정으로 이동한다. 본문과 본문 검색 인덱스는 원문 PC에 두며, PC 미연결 시 서버에 있는 제목·요약·결정 요약만 검색하고 `PC 미연결로 일부 내용 검색 제외`처럼 검색 범위를 명시한다. 범용 코드·파일 검색을 의미하지 않는다.

대화 상단의 `결과물`에서 실제 산출물 목록을 열고 선택한 내용을 오른쪽에서 본다. 일반 진입은 최신 버전을, 과거 메시지·참조 링크는 해당 버전을 연다. 읽는 v2 뒤에 v3가 생겨도 v2를 유지하고 새 버전 알림·변경 보기를 제공한다. 사용자가 선택해야 전환하며 v2 피드백은 v2에 고정하고 최신이 아님을 표시한다.

화면의 “검토 가능한 초안”은 실제 사용자 정보를 채웠다는 뜻이 아니라, 무엇이 알려졌고 무엇을 결정해야 하는지 검토할 수 있다는 뜻이다. 의도가 미정이어도 초안을 보여주며 품질 게이트 때문에 초안 열람·필요한 질문 카드 답변을 막지 않는다. 일반 메시지의 전송 시점은 아래 요청 처리 규칙을 따른다.

상단 표시 예시는 다음과 같다. 수치·상태는 가상의 화면 설명일 뿐 실제 프로젝트 판정이 아니다.

> 기능 개발 / 의도 정리 v2 / 행 범위 결정 대기
>
> 이번 변경: 내보낼 행의 범위를 수정하고 권한 질문을 추가했습니다.
>
> 필요한 판단: 권한 정책과 데이터 규모
>
> 요청 정합성 확인: 권한 근거와 행 범위에 쟁점 2개 — 전체 의도의 의례적 재승인이 아님

각 의도 항목에는 `요청·명시 결정 근거 / 정책 근거 / AI 제안 / 조사로 확인 / 미정`을 구분한다. 미정 질문에는 해결 시점·필요한 선택·의존 작업을 표시한다. “제외사항 없음”과 “제외사항 미정”을 구분하며 필수 정보의 빈칸을 임의 답으로 채우지 않는다. 업무에 관계없는 항목까지 질문으로 만들지는 않는다.

## 5. 대화·동의·변경의 UX 동작

기본 상호작용은 `요청 정리·정합성 확인 → 위임 범위에서 자동 진행 → 필요한 선택이 있을 때 해당 변경 질문 → 검증·결과 제시`다. controlled에서는 시작 확인과 변경 결과 확인을 추가하고 설계·계획 확인은 선택한다. 이미 실제 확인한 동일 내용은 반복 질문하지 않는다.

명확한 업무가 없어도 `논의 중/준비 단계` Case에서 대화를 시작한다. 목표·Profile이 미정인 논의를 억지로 기능 개발 업무로 만들지 않는다. 명확한 작업 요청이 생기면 해석한 목표·범위·제약을 표시하고 같은 Case에서 업무로 전환한다. 제안에 대한 단순 동의는 해당 결정이며 구현 지시가 아니다. 논의만 끝난 대화는 보관하고 복원하여 이어갈 수 있다. 별도 완료 판정이나 결과 게이트를 강제하지 않으며, 미해결 질문 없이 쉬고 있는 대화를 답변 필요로 표시하지 않는다. 논의 단계도 권한·예산 경계 안에서 수행하고 업무 전환으로 기록·누적 사용량을 초기화하지 않는다.

미완료 상태에서 같은 문제의 목적을 확장하거나 유형을 바꾸면 같은 Case에서 이어간다. 예를 들어 조사 중 수정까지 명확히 요청하면 기존 기준·의도 항목의 `relates_to` 연결·결정·검증·소비·repair 이력을 보존하고 새 목적의 의무와 필요한 권한·확인을 적용한다. 목적 추가나 Profile 명칭 변경만으로 기존 쓰기 제한을 우회하지 않는다. 이 정책은 새 요청의 실제 범위를 확인하는 것이며 단순 동의를 구현 요청으로 해석하는 예외가 아니다. 완료·예외 종료 뒤 실제 수정은 계속 새 Case다.

현재 요청을 처리하는 동안 일반 메시지의 전송을 막되 입력 초안은 편집할 수 있게 한다. 잠금은 Case 전체 수명이 아닌 요청 처리 단위다. 내부 Run 사이의 짧은 대기에는 풀지 않고 현재 요청이 끝나거나 실제 중단이 확인되면 푼다. 다른 Case의 열람·새 요청을 막지 않는다. 해당 질문·확인 카드의 범위가 정해진 답변과 중단 요청은 별도 경로로 처리한다. 일반 후속 요청의 대기열이나 초안 자동 전송은 만들지 않는다. 서버도 중복·충돌 요청을 차단하며 Runner의 원문 수신·제어 처리가 긴 CLI 실행에 막히지 않도록 해야 한다. 이는 이전의 수행 도중 자유로운 일반 피드백 요구를 변경한 결정이다.

입력 초안의 텍스트와 붙인 자료 참조는 현재 브라우저에 대화별로 자동 저장한다. 대화 전환·새로고침·브라우저 종료 후에도 복구하고 전송 실패 시 유지한다. PC 원문 저장과 서버 참조 등록을 모두 확인한 정상 접수 뒤에만 해당 전송분의 초안을 정리한다. 브라우저 초안 저장과 시스템 접수를 구분하며 복구·재연결이 자동 전송을 만들지 않는다. 기기 간 동기화는 지원 범위가 아니고 저장 기술은 후속 구현에서 정한다.

오른쪽 산출물에서 구절·코드 위치를 선택해 공통 입력창에 참조를 붙이고 의견을 보낸다. 참조는 대상 ID·버전·해시·위치와 연결하며 검토 중 내용이 바뀌어도 대상을 조용히 바꾸지 않는다. 실행 중 참조와 의견을 초안으로 준비할 수 있지만 일반 전송 조건은 동일하다. 수신·원문 저장·반영·미해결·대상 변경에 따른 재검토 상태를 구분한다. 첫 버전은 텍스트와 프로젝트 파일·코드·산출물 참조를 지원하며 이미지 첨부·분석은 후속 검토다.

보낸 메시지의 `수정해서 다시 요청`은 기존 내용을 입력창에 가져와 편집하고 원문에 연결된 새 정정 요청으로 전송한다. 과거 메시지·결정·수행 결과를 덮어쓰거나 없애지 않는다. 기존 결과의 영향을 확인하고 완료된 업무의 실제 변경이면 연결된 새 Case로 진행한다.

질문 카드는 선택지와 직접 입력을 제공하고 답변 뒤 기록된 결정을 표시한다. controlled 시작·결과 확인, 권한 범위 허용, 예외 수용은 대상과 효과를 담은 카드·의미 있는 버튼으로 수행한다. 의례적인 동의 문구 입력을 강제하지 않되 필요한 이유·범위는 수집한다. 행위자·대상·버전/해시·범위·시각을 기록하며 카드 답변을 일반 요청의 포괄 승인으로 확대하지 않는다.

- 질문에 대한 답변은 요구 변경과 구별한다. “왜 이런 제약이 필요한가?”라는 질문만으로 제약을 삭제하지 않는다.
- “현재 페이지로 해줘”는 해당 질문의 답변 또는 수정 요청이다. 전체 의도에 대한 동의로 확대하지 않는다.
- “이 의도에 동의”처럼 대상이 명확한 자연어 또는 UI 동작은 그 대상의 확인으로 기록한다. 명확한 최초 요청에 근거한 위임은 별도로 표시하며 사용자가 AI 초안을 검토한 것으로 기록하지 않는다.
- 동의할 때 산출물 버전이 바뀌었다면 사용자가 실제 본 대상과 현재 버전의 차이를 먼저 보여준다. 오래된 탭의 버튼 클릭으로 새 버전에 동의한 것으로 기록하지 않는다.
- 기존 유효 위임과 동일한 내용에는 다시 묻지 않는다. 최초 요청·후속 명시 결정·정책과 현재 범위를 연결하고 의미가 바뀐 부분만 강조해 확인한다.
- 버전만 바뀌었다고 전부 재승인하지 않는다. 표현 정리·같은 동작의 기술 해법 변경과 범위·제품 동작·중요 기준·권한의 변경을 구분한다. AI의 최근 초안끼리 비교하지 않고 마지막 유효 위임 대비 누적 차이를 확인한다.
- 의도 단계에서 필요한 사람 결정은 필수로 확인한다. 설계·계획에서 판단하는 것이 적절한 질문은 단계와 의존 관계를 표시해 이월할 수 있으며, 해당 결정에 의존하는 작업 전에 사람에게 확인한다. 이월 상태를 AI가 결정 완료로 바꾸지 않는다.
- 필요한 설계·계획에는 선택한 `사람 검토 / 자동 진행`과 해당 논리 기록 링크를 표시한다. 자동 진행은 `정책에 따른 자동 채택`으로 표현하며 `사람 승인됨`으로 표시하지 않는다. 선택한 사람 검토는 Fast Lane이나 예산 절감으로 해제하지 않는다.
- 요청이 명확하거나 사용자가 결정을 확인했어도 필요한 품질 검증이 남아 있으면 `위임 확인 · 검증 준비 중`처럼 두 상태를 분리한다. AI 게이트 통과가 새 권한을 만들지는 않는다.
- 사용자가 합의 내용을 바꾸면 변경 영향 목록을 보여준다. 예: 기존 설계의 권한 항목, 구현 작업, 이미 통과한 테스트 결과 중 재검토가 필요한 것. 영향받지 않은 승인·증거는 보존한다.

## 6. 품질 게이트 결과를 화면에 표현하는 기준

요청 정합성 확인은 필수로 표시하되 가벼운 확인과 독립 AI 의미 검토를 구분한다. 독립 검토가 선택된 경우에만 별도 세션이 필요하다. 나머지 게이트는 규모·위험·Profile에 따른 추천과 실제 정책 적용을 구분한다. 필수 실패는 해당 의존 작업을 보류하며 독립 작업까지 일괄 중지하지 않는다. 기본 repair 2회의 현재 차수·남은 한도를 보여주고 환경 복구·전체 예산과 혼동하지 않는다.

전체 예산은 기본 무제한이다. 사용자가 한도를 설정하면 실제 사용량·진행 중 예약·잔여량·측정 방식·강제 가능 범위를 표시한다. `exact / estimated / unavailable`을 구분하고 미제공을 0으로 표시하지 않는다. soft 경고와 hard 일시 중지를 구분하며 hard 한도 뒤 자동 continue를 허용하지 않는다. 예산 소진 화면에는 부분 결과·미검증·남은 작업을 보존하고 새 실행의 재개 조건을 보여준다.

시간 한도를 선택할 때는 AI 실행시간 합계를 기본 지표로 제안한다. 논의·업무 실행·검토·재시도·종료 후 설명의 실제 실행시간을 같은 Case에 누적하고 사람 답변 대기는 제외한다. 병렬 실행은 각 실행시간을 합산하므로 벽시계 경과시간과 다르다. 경과시간 제한은 별도 옵션으로 구분하며 기존 Case 한도의 의미를 소급 변경하지 않는다. 무제한 기본 예산과 계측·강제 가능 범위를 정직하게 표시하는 원칙은 유지한다.

| 표시 | 사용자가 알아야 할 내용 |
|---|---|
| 게이트 목적 | 이번 단계 뒤 무엇을 확인하는지 |
| 대상 버전·범위 | 어떤 의도·설계·코드·환경을 대상으로 한 판단인지 |
| 기준별 결과 | 충족 / 미충족 / 확인 불가 / 미실행 / 변경으로 재검토 필요 등 구분 |
| 근거 | AI 평가 의견, 정적 검사 결과, 실행 로그, 사람 판단 등 증거 종류와 원문 |
| 다음 행동 | 자동 보완 중인지, 사람 결정이 필요한지, 환경 문제인지 |
| 진행 영향 | 전체 업무를 막는지, 특정 후속 작업만 기다리는지 |

업무 화면에는 게이트별 관찰·추천·실제 적용·적용 출처를 보여준다. 개별 작업은 해당 항목의 상속·명시 조정과 유효 정책을 조회할 수 있게 한다. 요청 정합성 확인의 필수성은 유지한다. `사용자 명시 OFF`, `자동 조건에 따른 생략`, `업무상 미적용`, `통과`를 구분하고 추천만 있는 상태를 적용 완료로 표시하지 않는다.

예를 들어 업무에서 구현 품질 게이트가 ON이고 특정 Task에서 OFF를 선택했다면 그 Task의 게이트는 `사용자 설정으로 꺼짐`으로 표시한다. 다른 Task의 검사나 Case 전체의 통합 게이트까지 함께 꺼지는 것으로 표현하지 않는다. 이는 설정 구조를 설명하는 가상 사례이며 특정 위험의 기본값을 확정한 예시는 아니다.

OFF 상태는 검증 통과가 아니며 이전 실패 결과도 조회할 수 있다. 게이트를 끄면 해당 게이트의 실행·차단을 사용하지 않지만, 합의한 성공 기준의 검증 작업과 선택한 설계·계획 사람 검토는 별도라는 영향을 함께 보여준다. 진행 중 토글은 예약된다. 현재 검증 1회 종료 후 적용하고 이후 자동 수정·재검증부터 새 설정을 사용한다. 화면에는 예를 들어 `현재 ON / OFF 예약 / 이번 검증 종료 후 적용`을 표시한다. 예약과 실제 반영을 구분하고 설정 변경만으로 진행 중 검사를 중단하지 않는다.

총점 하나와 녹색 표시만 보여주지 않는다. 기준별 결과·미검증·질문을 드러내고 AI 판정의 근거 보완·이의 제기를 지원한다. 권한 부족·controlled 확인 대기·예산 소진·품질 실패를 서로 구분한다.

예를 들어 의도 초안의 품질 검토에서 “대상 사용자 미정”이 나오면 “AI가 해결할 결함”이 아니라 “사람에게 확인할 질문”일 수 있다. 초안 검토 품질과 구현 준비 품질에 같은 통과 조건을 적용하지 않는 것이 중요하다.

## 7. PC 연결과 원문 열람

서버+개인 PC Runner 구성에서는 상세 의도·설계·계획·대화·코드·diff·로그가 PC에 있다. 서버는 상태·요약·원문 ID·버전·해시 참조만 보관하며 상세 열람을 일시 중계한다. 화면은 `요약 조회 가능`과 `PC 연결 필요: 원문 열람 불가`를 구분한다. 원문을 볼 수 없는 상태에서 요약만으로 상세 의도에 동의한 것처럼 처리하지 않는다.

PC가 연결되면 원문 버전과 해시를 확인하여 문서·대화·차이를 표시하고 대상 버전에 대한 사람 결정을 기록한다. 서버 장애 복구용으로 상세 문서를 영구 복제하지 않는다. 단일 호스트에서도 원문과 상태·요약의 논리적 구분을 유지한다. 첫 버전은 한 사용자용이며 공동 열람·공동 편집·다중 역할 UI가 이미 구현된 것처럼 표시하지 않는다.

원문이 필요한 새 입력은 PC 영속 저장과 서버 참조 등록이 모두 확인돼야 저장 완료로 표시한다. PC 오프라인에서는 이미 불러온 내용과 작성 초안을 유지하고 초안 편집을 허용하되 일반 메시지·질문/확인 카드의 전송을 비활성화한다. 목록·상태·요약에는 마지막 확인 시각을 표시하고 추가 원문 열람에는 연결이 필요함을 알린다. 초안을 접수 완료로 표현하거나 서버 원문 대기열에 영구 보관해 우회하지 않는다. 현재 브라우저의 대화별 초안은 새로고침·종료 후에도 복구하며 그 저장 기술은 후속 구현 대상이다. 이미 유효한 동의는 연결 단절만으로 취소하지 않는다.

첫 버전 원격 접속은 LAN 또는 사용자 VPN 안에서 인증과 암호화를 적용한다. 인터넷 공개 운영은 이후 확장이다. 각 호스트는 지정 경로에 하루 한 번 백업하고 최근 7개를 보존한다. 화면에는 호스트별 최근 백업 결과·경로·실패 상태를 표시하는 안을 두며 PC 원문과 서버 상태의 복구 여부를 구분한다. 백업 회전이 원본·승인·증거를 자동 삭제하지 않는다.

Runner가 연결을 잃으면 `연결 끊김 · 실행 상태 확인 필요`로 표시한다. 안전한 도구 경계에서 멈추는 정책과 실제 멈췄다는 관찰 결과를 구분하며 재연결 후 실제 실행·원본·정책 상태를 대조한다. 초안을 자동 전송하지 않고 사용자가 직접 전송한다. CLI별 경계 제어 가능성은 호환성 검증 대상으로 표시하고, 단절만으로 같은 쓰기 작업을 다른 Runner에 중복 배정하지 않는다.

`중단`은 현재 요청의 후속 실행 시작을 막고 `중단 요청 중`으로 표시한다. 실제 정지를 확인한 뒤 일반 전송을 다시 허용하며 불명확한 상태를 완료로 가장하지 않는다. 완료한 부분·남은 변경·미완료/미검증을 요약하고 문서·코드·기록을 보존한다. 같은 대화에서 계속하거나 방향을 바꿀 수 있으며 재개 전 실제 파일·실행 상태를 재확인한다. 일반 CLI 중단과 잔여 프로세스 확인은 추가 구현·실증 대상이다.

일시적 오류는 기존 권한·예산 안에서 횟수를 제한해 복구한다. 로그인·설정·반복 실패·사람 판단은 원인, 완료한 부분, 필요한 조치를 담은 카드로 안내하고 로그는 펼쳐 보게 한다. `다시 시도`는 같은 Case에서 유효한 결과를 재사용하며 기록·비용·품질 수정 한도를 유지한다. 실행·게시 결과가 불명확하면 실제 결과를 먼저 대조한다. GitHub만 실패한 경우 내부 업무는 계속한다.

프로젝트 기본 CLI·모델과 업무·역할별 실제 배정을 조회할 수 있게 한다. 장애로 사전 허용되지 않은 대체 CLI가 필요하면 변경 전에 사람에게 확인한다. 검토 세션이 작성 세션과 분리됐는지 표시하되 같은 모델의 별도 세션을 독립적인 정답 보장으로 설명하지 않는다. 로그인 정보는 Runner에 두고 외부 AI 전송은 기존 CLI 설정을 따른다. 로컬 Runner 실행을 오프라인 AI 처리로 표현하지 않는다.

## 8. 최종 확인·예외 종료·후속 업무

새 코드 변경 업무는 현재 브랜치의 마지막 커밋을 작업 기준의 기본값으로 한다. 미커밋 변경이 있으면 목록을 보여주고 포함 여부를 묻는다. 포함 시 원래 폴더를 보존하여 전용 작업공간에 반영한다. 후속 업무는 이전 결과를 기준으로 제안하고 `작업 기준`에서 브랜치·커밋을 조정할 수 있으며 실행 전 버전을 고정한다. 미커밋 포함 경로는 추가 구현 대상이다.

기본 ask-on-decision은 성공 기준·검증·요청한 완료 지점을 충족하면 결과를 제시하고 자동 완료한다. controlled에서는 변경 결과를 먼저 확인받고 허용된 push·PR과 필요한 외부 CI를 수행한 뒤 완료한다. 화면에서 `결과 확인 대기`, `확인됨 · 게시/외부 검증 대기`, `완료`를 구분한다. 게시 후에만 가능한 검증을 확인 전에 이미 통과한 것처럼 표시하지 않는다. 결과 확인은 코드 push·PR에 앞서며 별도 허용된 기록 이슈·요약 게시는 작업 중 계속할 수 있다.

코드 변경 업무의 프로젝트 기본 완료 지점은 로컬 구현·검증이다. push·PR·필수 외부 CI까지 요청한 업무는 그 범위로 확장한다. 결과에는 변경 요약·검증·남은 문제와 변경 보기, 작업 PC 이름·경로 복사를 제공한다. 작업 폴더 열기는 해당 작업 PC에서 실제 지원·검증된 수단이 있을 때 제공하며 현재 웹 브라우저가 작업 PC에 있다고 가정하지 않는다. 앱 안에서는 읽기·참조 피드백을 제공하고 편집은 외부 도구에서 한다. 외부 변경은 대조·보존하여 검토 버전과 현재 파일을 구분하며 전체 문서·코드 편집기는 후속 검토다. 전용 작업공간에 있는 변경을 원래 폴더·기본 브랜치에 반영한 것으로 표시하지 않는다.

확인한 코드나 내용이 달라지면 새 후보를 재확인한다. 동일 내용의 재전송·상태 조회·예정된 외부 증거 추가만으로 재확인하지 않는다. CI 수정으로 내용이 달라지면 그 변경을 보여주고 다시 확인받는다. 자동 완료와 실제 사람 확인 기록은 구분한다.

사람이 남은 문제·미검증 범위와 영향을 확인하면 `예외 수용 종료`를 선택할 수 있다. 화면에는 충족·미충족·미검증·예외 대상과 근거를 함께 표시하며, 예외 수용으로 원래 게이트 판정을 통과로 바꾸지 않는다. 기준을 바꾸는 결정과 현 기준의 예외를 수용하는 결정을 구분한다. 예외 종료가 코드 push나 PR의 실행 권한을 만들지는 않는다.

완료·예외 종료 뒤 명확한 수정 요청을 받으면 반복 생성 확인 없이 연결된 새 업무를 만들고 해당 대화로 이동한다. 이전 결과·종료 기록을 보존하고 관련 결과·결정·제약을 출처와 함께 잇되 새 권한을 자동 부여하지 않는다. 단순 설명 질문은 기존 결과를 근거로 같은 대화에서 답하며 종료 판정을 바꾸지 않는다. 설명 실행의 사용량·예산은 같은 Case에 누적하되 종료 당시와 이후 사용을 나누어 표시한다. 추가 실험·수정이 필요한 요청은 연결된 새 Case로 진행한다. 새 업무 화면은 이전 업무와 출처를 연결하고 새 범위에 필요한 의도·검토·검증 조건을 표시한다.

## 9. GitHub 기록과 외부 코드 반영

프로젝트 등록은 폴더·대표 저장소 선택, 실행 환경 확인, 기본 설정, 새 대화로 이어진다. Git 원격 정보와 기존 인증·접근 가능 여부를 확인해 GitHub 연결과 업무 이슈 위치를 기본 설정한다. 여러 원격·fork·권한 부족처럼 판단이 필요한 부분만 묻고 연동은 나중에 설정할 수 있다. 연결 확인 자체가 이슈 생성·게시·코드 수정 허용을 뜻하지 않는다.

UI 용어는 `업무 이슈를 만들 위치`로 한다. 대표 저장소가 코드와 업무 이슈를 겸하는 것을 기본으로 하고 별도 위치를 선택할 수 있다. 같은 저장소여도 이슈 게시와 코드 수정·push·PR 허용 범위는 구분한다. 기록 위치 지정만으로 코드 대상에 자동 포함하지 않으며 현재 구현의 겸용 거부는 변경 대상이다.

논의 준비 단계에는 이슈를 만들지 않는다. 명확한 업무로 전환할 때 지정 위치에 Case당 새 이슈 하나를 유효한 게시 허용 안에서 만들고 관련 선행 논의의 결정을 요약한다. 기존 이슈는 출처로 연결한다. 여러 참여 저장소의 PR·커밋은 같은 Case에 연결한다. GitHub에는 의도·설계·계획·검증의 요약과 참조만 누적하며 전체 원문·코드·diff·상세 로그는 자동 기록 댓글에 포함하지 않는다. 게시 전 내용 범위를 점검하되 자동 필터의 완벽성을 주장하지 않는다.

GitHub 게시 장애 중에도 내부 작업은 진행하고 Outbox에 게시 내용을 보존하여 재시도한다. 업무 진행과 게시 상태를 별도로 보여주며 누적 실패를 알린다. 응답 유실로 전달 여부가 불분명할 때 중복 생성 확인 상태를 표시할 수 있게 한다. `업무 완료 · 요약 게시 대기`는 가능한 상태다.

코드 push·PR 생성·수정은 Case 시작에 허용한 저장소·브랜치·대상·행동 범위 안에서 자동 실행한다. controlled에서는 변경 결과 확인 뒤 실행한다. 범위 밖 행동은 별도로 확인하며 기록용 이슈·요약 게시 허용과 구분한다. 병합·이슈 종료·CI 수동 재실행·배포는 이후 확장이다. 일부 저장소만 반영됐으면 각각의 상태를 표시하고 결과 대조 뒤 제한 재시도한다. 자동 롤백하지 않는다. GitHub PR 리뷰는 업무 피드백으로 모으고 이슈 댓글은 실행 명령이나 승인으로 해석하지 않는다.

첫 버전은 최소 프로젝트 지식 관리·후보 추출·관련 지식 주입을 제공한다. 대화의 `[결정 사항]`에서 이번 업무의 목표·제약·결정을, 프로젝트 설정의 `[프로젝트 규칙]`에서 여러 업무에 적용되는 규칙을 확인한다. 확정된 결정을 자동 정리하되 원래 발언으로 이동할 수 있게 하고 적용 범위·출처·대체 관계를 보존한다. AI 제안이나 이번 업무의 결정을 프로젝트 규칙으로 자동 승격하지 않는다. 모든 Case에 채택 승인 화면이나 별도 QG-08 AI 검토를 강제하지 않는다. 상세는 [프로젝트 지식](project-knowledge.md)을 따른다.

## 10. 구현 상세와 개발 인계

- 확정한 PC 3영역 배치로 프로젝트 등록→논의→실행→결과→후속 요청을 검토하고 구체 문구·배치·상태 전이를 상세화한다. 휴대폰·태블릿 최적화와 이미지 첨부·분석은 후속 검토다.
- 개발은 대화·요청 기반을 먼저 마련하고 P4-04와 연결되는 문맥·복구·제어, 새 UI 기본 흐름, 나머지 P4와 추가 UI, P5/P6 순서로 진행한다. UI 전체를 P4-04 한 작업에 넣지 않으며 하위 작업별 plan·검증·인계는 DEVELOPMENT.md를 따른다.
- 준비 단계 Case·요청 처리 상태·일반 메시지/카드 응답/중단 분리, 브라우저 초안 저장 기술, PC 본문 검색 인덱스 구현, 중단·재연결의 실제 능력과 기존 자료 이행은 후속 설계·검증으로 구체화한다. 초안 복구·검색 대상·버전 탐색·업무 유형 변경·종료 후 설명의 제품 동작은 D-83~89로 확정됐으며 기술 선택과 구분한다.
- 규모 판단의 자동 분류 경계와 사용자 하향 조정 시 표시할 영향·남은 불확실성을 구체화한다. 시스템이 사용자 확인 없이 모든 위험 권고를 끌 수 없는 게이트로 승격하지 않는다.
- material delta의 합의된 분류를 실제 사례에 적용해 누적 차이·영향 작업이 이해되게 표시한다. 이미 유효한 동일 내용에는 재동의를 요구하지 않는다.
- PC 연결 전후 원문 버전 일치, 예약된 게이트와 현재 설정, 게시 장애와 업무 상태, 예산 예약과 실제 소비를 화면에서 구분한다.

개발 기술은 Python/FastAPI·SQLite와 React/TypeScript 웹 UI로 확정됐다. 사용자는 Python·Git을 준비하고 설치·실행 스크립트와 포함된 빌드 UI로 일반 사용자 프로세스를 실행한다. CLI는 기존 설치·로그인을 사용한다. PostgreSQL·별도 대형 워크플로 엔진은 이후 확장이다. 이 구현 정보는 개발 문서에서 다루며 일상 의도 검토 화면의 필수 입력으로 노출하지 않는다.

초기 수용시험은 10개 프로젝트·3개 Runner 등록, 동시 AI 실행 2개, 같은 Runner의 쓰기 기본 1개를 대상으로 한다. 이는 등록 상한이나 측정 완료된 성능 보장이 아니다. 화면에서는 실제 대기 원인과 실행 수를 표시하며 시험 규모를 제품의 확정 처리량으로 홍보하지 않는다.
