# 작업 수준 판단과 의도 검토 UX — v0.6

기준: v0.6과 연속 설계 검토의 확정 결정. 확정한 제품 방향과 상세 설계 제안을 구분한다. **확인됨: 간소·표준·심층 3단계, 간소는 짧은 변경안·계획, AI가 근거를 제시하고 사람이 조정. 적용한 필수 품질 기준 실패는 AI가 수정·재검증하고 반복 실패 또는 사람 판단이 필요하면 요청하며 해결 전 후속 진행을 보류. 기능 개발의 의도 품질 게이트는 끌 수 없는 필수 조건이며 나머지는 규모·위험에 따라 조절하고 Case별 ON/OFF 및 필요한 Task별 조정을 지원.** 켜진 게이트의 AI 의미 검토는 작성과 별도 세션에서 수행한다. 최초 실패 후 자동 수정·재검증은 기본 최대 2회이며 업무별 한도 조정을 지원한다. 화면 배치는 아래 제안이며 확정하지 않았다.

## 1. 작업 수준은 크기보다 필요한 준비와 검증의 깊이를 표현

사람에게는 “작은/큰 작업”보다 “이번 업무에 이 정도 준비가 필요한 이유”가 보여야 한다. 변경 줄 수·파일 수·예상 토큰은 보조 자료이며 단독 판정 기준으로 사용하지 않는 안을 권한다. 사용자 답변에 따라 수준은 간소·표준·심층 3단계로 표현한다.

분류는 실행 절차를 고정하는 명령이 아니라 필요한 산출물과 게이트의 기본 구성을 제안하는 역할이다. 연구와 불량 분석에는 기능 개발 전용 단계를 일괄 적용하지 않는다. 버그 수정·원인 분석·연구는 목표·범위·종료조건이 명확하면 시작하고 중요한 미정만 질문한다. 화면에는 정리한 목표·종료조건을 표시한다. 혼합 업무에서 새 기능이나 중요한 제품 동작 변경이 필요하면 해당 기능 개발 작업에 의도 검토를 적용한다. 실험 범위가 없는 분석·연구는 읽기 조사 뒤 재현·계측·임시 실험 범위를 제안해 한 번 확인받으며 이미 허용된 활동은 반복 확인하지 않는다.

| 판단 축 | AI가 확인·제시할 근거 | 준비 또는 검증이 깊어지는 사례 |
|---|---|---|
| 사용자 의도와 성공 기준의 명확성 | 확인된 요구·AI 제안·미정 질문, 기존 화면/문서 근거 | 제품 동작 선택에 따라 결과가 크게 달라짐 |
| 변경 범위·구성 요소 의존성 | 영향받는 모듈·API·클라이언트·외부 시스템과 연결 | 여러 구성 요소를 함께 바꾸어야 동작함 |
| 호환성과 데이터 생명주기 | 공개 계약, 기존 데이터와 변환·보관 요구 | API 호환성, 스키마 변경, 데이터 마이그레이션 |
| 권한·보안·민감 데이터 영향 | 현재 권한 모델, 보호 대상과 새 접근 경로 | 단 한 줄이어도 접근 권한이나 민감 정보 노출 경로 변경 |
| 가역성과 실패 비용 | 복원 가능 여부, 되돌릴 데이터·환경·외부 반영 | 파괴적 데이터 변환, 운영 영향, 원상 복구 난도 |
| 해법·환경의 불확실성 | 확인한 사실, 상충 가설, 미검증 전제 | 동작 원인을 모르거나 실험 없이 해법 타당성 판단 불가 |
| 검증 난도 | 관찰 가능성, 재현성, 테스트 환경·기대값 확보 | 간헐성·동시성·성능·실환경 의존으로 단순 테스트가 불충분 |

각 축에는 숫자 점수보다 `근거 / 현재 판단 / 아직 확인할 것 / 준비·검증에 주는 영향`을 남기는 방안을 추천한다. 낮은 항목들의 평균으로 권한·마이그레이션 같은 중요한 영향을 상쇄하지 않는다. AI는 권장 수준을 제안하고 사람이 상향·하향 조정할 수 있다. 조정 이유와 남는 위험을 기록하며, 상세도 조정이 의도 동의·합의한 검증·권한을 해제하지 않게 한다.

## 2. 확정된 세 수준에 대응하는 산출물 후보

모든 기능 개발 수준에 여섯 항목의 의도 초안, 피드백, 최신 내용에 대한 명시적 동의는 필수다. 설계·개발계획의 사람 검토는 각기 독립적인 옵션이며 프로젝트 기본값은 둘 다 사람 검토다.

| 수준 | 적합한 조건 예시 | 설계·계획 표현 후보 | 검증 구성 후보 |
|---|---|---|---|
| 간소 | 의도가 명확하고 변경이 국소적이며 쉽게 복원·확인 가능 | 짧은 변경안·계획을 남김. 설계 요약과 구현/검증 계획을 논리적으로 구분해 조회하되 같은 화면이나 물리 파일 안에 함께 표현 가능 | 해당 동작 확인과 관련 회귀 확인. 관련 없는 대규모 검사는 강제하지 않음 |
| 표준 | 사용자 흐름·API·데이터 영향 중 설계 선택이 필요하고 영향 범위를 조사할 수 있음 | 요구별 설계 대응, 인터페이스/데이터/예외 처리, 의존 작업과 통합·검증 순서 | 설계 적합성, 계획 검증 가능성, 구현 검증과 완료 근거 평가 |
| 심층 | 영향이 넓거나 되돌리기 어렵거나 불확실성·검증 난도가 높음 | 대안과 선택 이유, 실패·복구 경로, 단계별 실험, 이행·통합 전략 등 해당 주제만 확장 | 필요한 전문 검토, 실험·성능·호환성·복구 검증 등 위험에 맞는 항목 추가 |

깊이를 높인다는 것이 항목과 문서를 모두 늘린다는 뜻은 아니다. 권한 변경은 권한 모델과 부정 사례를, 마이그레이션은 데이터 보존과 복구를 깊게 다룬다. 간소 작업도 선택된 옵션대로 짧은 변경안·계획을 남긴다. 초기 추천 규칙은 국소적·가역적이고 기대값이 명확하면 간소, 여러 인터페이스·데이터 동작의 설계가 필요하면 표준, 넓은 영향·어려운 복구·높은 불확실성 중 중요한 근거가 있으면 심층을 제안하는 방식이다. 불확실할 때는 근거가 부족함을 표시하고 조사 작업을 제시한다. 이 규칙은 구현 제안이며 줄 수나 점수로 사용자의 조정권을 막지 않는다.

수준과 검토 방식은 두 축으로 표시한다. `심층 + 설계 자동 진행 + 계획 사람 검토`도 선택 가능해야 한다. 단, 자동 진행이 필요한 검토·산출물·게이트의 생략이나 사람 승인 기록 생성으로 이어지면 안 된다.

## 3. 예시로 분류 기준을 설명

아래는 확정 판정이 아니라 분류 근거가 어떻게 달라지는지 보이는 가상 사례다.

| 사례 | 초기 수준 제안 예시 | 달라질 수 있는 근거 | 핵심 검증 예시 |
|---|---|---|---|
| 버튼 문구 변경 | 간소 후보 | 다국어 키·접근성 이름·자동화 계약에 영향이 있으면 범위 재평가 | 의도한 화면/상태의 문구, 관련 접근성·다국어 동작 |
| CSV 표시/내보내기 추가 | 표준 후보 또는 정보 부족 | 행·열·권한·인코딩·데이터 규모가 미정이면 먼저 의도 구체화. 이미 정의된 작은 포맷 수정이면 간소 가능 | 합의한 데이터 집합, 호환성, 권한·성능 중 해당 기준 |
| 한 줄의 접근 권한 조건 변경 | 표준 이상의 준비 검토 후보 | 변경량보다 역할·리소스 경계와 권한 누수 가능성이 중요 | 허용/거부/경계 조합, 우회 경로, 기존 사용자 영향 |
| 간헐적 버그 원인 분석 | 심층 조사 후보 | 재현·관찰·실험이 핵심. 기능 개발의 의도→설계→계획을 그대로 강제하지 않음 | 가설을 구분하는 실험, 수정 전후 증거, 미재현의 한계 |
| 여러 모듈을 바꾸는 큰 기능 | 심층 후보 | 독립 분해 가능성·API 호환성·데이터 이행 여부에 따라 깊이 조정 | 요구별 대응, 통합 순서, 계약·이행·회귀 검증 |
| 새로운 기술 도입 연구 | 연구용 탐색 깊이 제안 | 질문·실험 범위·결정에 필요한 증거와 종료 조건이 중요 | 비교 조건의 일관성, 출처, 실험 재현성, 불확실성 |

중간에 정보가 바뀌면 재평가 기록을 만든다. 상향·하향의 이유와 영향을 받는 산출물·검증을 보여준다. AI가 진행을 위해 사용자 검토 옵션을 끄거나 수행 수준을 묵시적으로 낮추는 것은 확정된 검토 정책상 허용하지 않는다.

## 4. 웹 의도 검토 화면의 정보 구조 제안

독립된 채팅과 문서 저장소를 사용자가 오가게 하기보다 업무 상세 안에서 세 영역을 함께 보여주는 안을 권한다. 실제 화면 레이아웃과 반응형 배치는 아직 확정하지 않는다.

1. 업무 요약: 프로젝트, 업무 이름, 현재 활동, 최신 의도 버전과 동의 상태, 사람에게 필요한 다음 판단, Runner 연결·실행 상태와 GitHub 게시 상태.
2. 검토 산출물: 최신 의도 여섯 항목, 연결된 성공 기준, AI 가정과 출처, 이전 버전과의 차이. 설계·개발계획·검증 결과 탭 또는 링크.
3. 대화와 피드백: 자유로운 대화, 특정 항목을 지목한 질문·수정 요청, 각 피드백의 수신/반영/미해결 표시. PR 리뷰는 피드백 출처로 표시하고 기록용 이슈 댓글은 입력·승인으로 처리하지 않는다.

화면의 “검토 가능한 초안”은 실제 사용자 정보를 채웠다는 뜻이 아니라, 무엇이 알려졌고 무엇을 결정해야 하는지 검토할 수 있다는 뜻이다. 의도가 미정이어도 초안을 보여줄 수 있어야 하며 품질 게이트 때문에 사용자 피드백 접점까지 막으면 안 된다.

상단 표시 예시는 다음과 같다. 수치·상태는 가상의 화면 설명일 뿐 실제 프로젝트 판정이 아니다.

> 기능 개발 / 의도 초안 v2 / 동의 대기  
> 이번 변경: 내보낼 행의 범위를 수정하고 권한 질문을 추가했습니다.  
> 필요한 판단: 권한 정책과 데이터 규모  
> AI 품질 검토: 검토할 쟁점 2개 — 사람의 의도 동의와 별개

각 의도 항목에는 `사용자 확인 / AI 제안 / 조사로 확인 / 미정`을 구분한다. 미정 질문에는 `의도에서 결정 / 설계로 이월 / 계획으로 이월`, 사람에게 받을 결정, 의존하는 작업, 해결 시점을 표시한다. “제외사항 없음”과 “제외사항 미정”도 다르다. 데이터가 없으면 비워 두지 말고 미정과 질문을 연결하되 임의로 답을 채우지 않는다.

## 5. 대화·동의·변경의 UX 동작

권장 상호작용은 `초안 제시 → 사람이 자유롭게 질문/수정/답변 → 반영 결과와 차이 제시 → 최신 내용에 명시 동의 → 필요한 설계·계획`이다.

- 질문에 대한 답변은 요구 변경과 구별한다. “왜 이런 제약이 필요한가?”라는 질문만으로 제약을 삭제하지 않는다.
- “현재 페이지로 해줘”는 해당 질문의 답변 또는 수정 요청이다. 전체 의도에 대한 동의로 확대하지 않는다.
- “이 의도에 동의”처럼 대상이 명확한 행동으로 동의를 기록한다. 기존 FR-03의 수용 기준에 맞춰 대상이 명확한 자연어 또는 UI 동작을 수용한다. 모호한 문구를 전체 의도 동의로 확대하지 않는다.
- 동의할 때 산출물 버전이 바뀌었다면 사용자가 실제 본 대상과 현재 버전의 차이를 먼저 보여준다. 오래된 탭의 버튼 클릭으로 새 버전에 동의한 것으로 기록하지 않는다.
- 이미 동의한 동일 내용에는 다시 묻지 않는다. 원문 동의와 현재 적용 범위를 유지하며, 변경된 내용만 강조해서 확인한다. 새 의미를 추가·삭제한 변경은 기존 동의로 덮지 않는다.
- 버전이 바뀌었다는 이유만으로 전부 재승인하지 않되, AI의 “의미가 같음” 판단만으로 변경된 요구를 승인하지 않는다. 동의 재사용 가능한 변경의 정확한 분류는 명시적으로 정의할 필요가 있다.
- 의도 단계에서 필요한 사람 결정은 필수로 확인한다. 설계·계획에서 판단하는 것이 적절한 질문은 단계와 의존 관계를 표시해 이월할 수 있으며, 해당 결정에 의존하는 작업 전에 사람에게 확인한다. 이월 상태를 AI가 결정 완료로 바꾸지 않는다.
- 설계·계획은 각기 `사람 검토 / 자동 진행` 표시와 현재 산출물 링크를 가진다. 자동 진행의 결과는 `자동 조건 충족` 등으로 표현하고 `사람 승인됨`으로 표시하지 않는다. 자동 진행 옵션도 필요한 사람 결정을 대신하지 않는다.
- 사용자가 명시 동의했어도 필요한 품질 검증이 남아 있으면 “의도 동의됨 · 설계 준비 중”처럼 두 상태를 분리한다. AI 게이트 통과도 사람의 의도 동의를 대체하지 않는다.
- 사용자가 합의 내용을 바꾸면 변경 영향 목록을 보여준다. 예: 기존 설계의 권한 항목, 구현 작업, 이미 통과한 테스트 결과 중 재검토가 필요한 것. 영향받지 않은 승인·증거는 보존한다.

## 6. 품질 게이트 결과를 화면에 표현하는 기준

의도 품질 게이트는 필수 잠금으로 표시하고 나머지는 규모·위험에 따른 추천과 실제 사용자 설정을 구분한다. 켜진 게이트의 필수 실패는 후속 진행을 보류한다. 반복 실패나 사람의 판단이 필요할 때 요청한다. 기본 자동 수정·재검증 2회 중 현재 차수와 남은 한도를 표시한다. 환경 장애 복구는 별도로 표시하며 품질 수정 횟수와 실행 시간·비용 예산을 혼동하지 않는다. UI에서 최소한 다음 정보를 구분하는 안을 제안한다.

자동 실행 시간은 기본 한도 없이 운영하고 사용량과 경과 시간을 표시한다. 시간 한도가 없더라도 품질 수정·재검증 한도, 사람 결정 대기, 권한·의존 조건은 유지한다. 시간·사용량과 품질 수정 차수는 서로 다른 정보로 보여준다.

| 표시 | 사용자가 알아야 할 내용 |
|---|---|
| 게이트 목적 | 이번 단계 뒤 무엇을 확인하는지 |
| 대상 버전·범위 | 어떤 의도·설계·코드·환경을 대상으로 한 판단인지 |
| 기준별 결과 | 충족 / 미충족 / 확인 불가 / 미실행 / 변경으로 재검토 필요 등 구분 |
| 근거 | AI 평가 의견, 정적 검사 결과, 실행 로그, 사람 판단 등 증거 종류와 원문 |
| 다음 행동 | 자동 보완 중인지, 사람 결정이 필요한지, 환경 문제인지 |
| 진행 영향 | 전체 업무를 막는지, 특정 후속 작업만 기다리는지 |

업무 화면에는 게이트별 추천 ON/OFF·추천 이유·현재 설정·설정 출처를 보여준다. 개별 작업 화면에서는 `업무 설정 따름 / 켜기 / 끄기`를 선택할 수 있게 하는 안을 제안한다. 의도 품질 게이트에는 `필수 · 끄기 불가`를 표시한다. 업무 설정과 개별 작업 설정이 다르면 실제로 무엇이 적용되는지 바로 확인할 수 있어야 한다.

예를 들어 업무에서 구현 품질 게이트가 ON이고 특정 Task에서 OFF를 선택했다면 그 Task의 게이트는 `사용자 설정으로 꺼짐`으로 표시한다. 다른 Task의 검사나 Case 전체의 통합 게이트까지 함께 꺼지는 것으로 표현하지 않는다. 이는 설정 구조를 설명하는 가상 사례이며 특정 위험의 기본값을 확정한 예시는 아니다.

OFF 상태는 검증 통과가 아니며 이전 실패 결과도 조회할 수 있다. 게이트를 끄면 해당 게이트의 실행·차단을 사용하지 않지만, 합의한 성공 기준의 검증 작업과 선택한 설계·계획 사람 검토는 별도라는 영향을 함께 보여준다. 진행 중 토글은 예약된다. 현재 검증 1회 종료 후 적용하고 이후 자동 수정·재검증부터 새 설정을 사용한다. 화면에는 예를 들어 `현재 ON / OFF 예약 / 이번 검증 종료 후 적용`을 표시한다. 예약과 실제 반영을 구분하고 설정 변경만으로 진행 중 검사를 중단하지 않는다.

총점 하나와 녹색 표시만 보여주지 않는다. 기준별 결과·미검증 범위·남은 질문을 드러낸다. AI의 잘못된 판정에 이의를 제기하거나 근거를 보완할 수 있어야 한다. 기능 의도 미동의와 필수 동의/권한 조건은 일반 품질 경고와 구분한다.

예를 들어 의도 초안의 품질 검토에서 “대상 사용자 미정”이 나오면 “AI가 해결할 결함”이 아니라 “사람에게 확인할 질문”일 수 있다. 초안 검토 품질과 구현 준비 품질에 같은 통과 조건을 적용하지 않는 것이 중요하다.

## 7. PC 연결과 원문 열람

서버+개인 PC Runner 구성에서는 상세 의도·설계·계획·대화·코드·diff·로그가 PC에 있다. 서버는 상태·요약·원문 ID·버전·해시 참조만 보관하며 상세 열람을 일시 중계한다. 화면은 `요약 조회 가능`과 `PC 연결 필요: 원문 열람 불가`를 구분한다. 원문을 볼 수 없는 상태에서 요약만으로 상세 의도에 동의한 것처럼 처리하지 않는다.

PC가 연결되면 원문 버전과 해시를 확인하여 문서·대화·차이를 표시하고 대상 버전에 대한 사람 결정을 기록한다. 서버 장애 복구용으로 상세 문서를 영구 복제하지 않는다. 단일 호스트에서도 원문과 상태·요약의 논리적 구분을 유지한다. 첫 버전은 한 사용자용이며 공동 열람·공동 편집·다중 역할 UI가 이미 구현된 것처럼 표시하지 않는다.

원문이 필요한 새 입력은 PC 영속 저장과 서버 참조 등록이 모두 확인돼야 저장 완료로 표시한다. PC 오프라인에서는 상세 입력에 연결이 필요함을 알리고, 저장되지 않은 입력을 접수 완료로 표현하거나 서버에 영구 보관해 우회하지 않는다. 이미 유효한 동의는 연결 단절만으로 취소하지 않는다.

첫 버전 원격 접속은 LAN 또는 사용자 VPN 안에서 인증과 암호화를 적용한다. 인터넷 공개 운영은 이후 확장이다. 각 호스트는 지정 경로에 하루 한 번 백업하고 최근 7개를 보존한다. 화면에는 호스트별 최근 백업 결과·경로·실패 상태를 표시하는 안을 두며 PC 원문과 서버 상태의 복구 여부를 구분한다. 백업 회전이 원본·승인·증거를 자동 삭제하지 않는다.

Runner가 연결을 잃으면 `연결 끊김 · 현재 호출 종료 후 정지`와 실제 `정지됨`을 구분한다. 안전한 도구 경계에서 멈춘 뒤 재연결 후 재개한다. CLI별 경계 제어 가능성은 호환성 검증 대상으로 표시하고, 단절만으로 같은 쓰기 작업을 다른 Runner에 중복 배정하지 않는다.

프로젝트 기본 CLI·모델과 업무·역할별 실제 배정을 조회할 수 있게 한다. 장애로 대체 CLI가 필요하면 변경 전에 사람에게 확인한다. 검토 세션이 작성 세션과 분리됐는지 표시하되 같은 모델의 별도 세션을 독립적인 정답 보장으로 설명하지 않는다. 로그인 정보는 Runner에 두고 외부 AI 전송은 기존 CLI 설정을 따른다. 로컬 Runner 실행을 오프라인 AI 처리로 표현하지 않는다.

## 8. 최종 확인·예외 종료·후속 업무

성공 기준과 필요한 검증을 충족하면 기본적으로 `사람 최종 확인 대기`로 전환하고, 사람이 결과를 확인한 뒤 완료한다. 업무별로 자동 완료를 선택할 수 있으며 `자동 완료`와 `사람 확인 완료`를 구분한다. 처음의 의도 동의나 설계·계획 승인으로 최종 확인을 대체하지 않는다.

사람이 남은 문제·미검증 범위와 영향을 확인하면 `예외 수용 종료`를 선택할 수 있다. 화면에는 충족·미충족·미검증·예외 대상과 근거를 함께 표시하며, 예외 수용으로 원래 게이트 판정을 통과로 바꾸지 않는다. 기준을 바꾸는 결정과 현 기준의 예외를 수용하는 결정을 구분한다. 예외 종료가 코드 push나 PR의 실행 권한을 만들지는 않는다.

완료 뒤 수정 요청을 받으면 연결된 새 업무를 만들고 이전 결과·종료 기록을 보존한다. 설명 질문에는 기존 결과를 근거로 답하며 새 수정 업무를 자동 생성하지 않는다. 새 업무 화면은 이전 업무와 출처를 연결하고 새 범위에 필요한 의도·검토·검증 조건을 표시한다.

## 9. GitHub 기록과 외부 코드 반영

업무마다 새 기록용 이슈 하나를 만들고 기존 이슈는 출처로 연결한다. GitHub에는 의도·설계·계획·검증의 요약과 참조만 누적하며 전체 원문·코드·diff·상세 로그는 자동 기록 댓글에 포함하지 않는다. 게시 전에 내용 범위를 점검하되 자동 필터의 완벽성을 주장하지 않는다.

GitHub 게시 장애 중에도 내부 작업은 진행하고 Outbox에 게시 내용을 보존하여 재시도한다. 업무 진행과 게시 상태를 별도로 보여주며 누적 실패를 알린다. 응답 유실로 전달 여부가 불분명할 때 중복 생성 확인 상태를 표시할 수 있게 한다. `업무 완료 · 요약 게시 대기`는 가능한 상태다.

코드 push·PR 생성·수정은 준비한 저장소·브랜치·대상·내용을 사람이 확인한 뒤 실행한다. 기록용 이슈의 자동 게시 허용과 구분한다. 병합·이슈 종료·CI 수동 재실행·배포는 이후 확장이며 첫 버전에서 사용 가능한 버튼처럼 제공하지 않는다. GitHub PR 리뷰는 업무 피드백으로 모으고 이슈 댓글은 실행 명령이나 승인으로 해석하지 않는다.

첫 버전 지식 기능은 결정과 근거의 보존만 제공한다. 출처·적용 범위·결정 이유를 조회할 수 있도록 보존하며, 후보 자동 추출·채택 화면·자동 재사용과 QG-08은 이후 확장이다.

## 10. 화면 상세 설계에서 남은 범위

- 의도 문서·대화의 실제 배치와 반응형 표현은 화면 시안에서 검토한다. 웹 UI가 주 화면이라는 결정은 유지한다.
- 규모 판단의 자동 분류 경계와 사용자 하향 조정 시 표시할 영향·남은 불확실성을 구체화한다. 시스템이 사용자 확인 없이 모든 위험 권고를 끌 수 없는 게이트로 승격하지 않는다.
- 동의 재사용이 가능한 표현 수정과 새로운 의미 변경을 구분할 기준을 상세화한다. 이미 유효한 동일 내용에는 재동의를 요구하지 않는다.
- PC 연결 전후 원문 버전 일치, 예약된 게이트 설정과 현재 설정, 게시 장애와 업무 상태의 분리를 실제 화면에서 확인한다.

개발 기술은 Python/FastAPI·SQLite와 React/TypeScript 웹 UI로 확정됐다. 사용자는 Python·Git을 준비하고 설치·실행 스크립트와 포함된 빌드 UI로 일반 사용자 프로세스를 실행한다. CLI는 기존 설치·로그인을 사용한다. PostgreSQL·별도 대형 워크플로 엔진은 이후 확장이다. 이 구현 정보는 개발 문서에서 다루며 일상 의도 검토 화면의 필수 입력으로 노출하지 않는다.

초기 수용시험은 10개 프로젝트·3개 Runner 등록, 동시 AI 실행 2개, 같은 Runner의 쓰기 기본 1개를 대상으로 한다. 이는 등록 상한이나 측정 완료된 성능 보장이 아니다. 화면에서는 실제 대기 원인과 실행 수를 표시하며 시험 규모를 제품의 확정 처리량으로 홍보하지 않는다.
