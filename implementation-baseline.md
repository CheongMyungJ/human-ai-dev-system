# 구현·운영 기준안 v0.8

2차 개정일: 2026-09-23. 사용자 결정 D-01~90 중 관련 결정을 구체화한다. 현재 구현은 **P4-03 완료·스키마 v15·Profile 정의 v2**이며 [DEVELOPMENT.md](DEVELOPMENT.md)와 [P4-03 결과](p4/evidence/P4-03-results.md)가 근거다. 주요 UI 제품 정책·개발 순서는 확정했으나 새 대화 UI·요청 제어 및 전체 첫 버전 범위는 목표 설계이며 현재 지원 능력을 뜻하지 않는다. 화면 폭·간격·단축키·기술 상세는 구현 시 구체화하고 화면 시안·사용성·상세 수용시험 수치는 아직 검증하지 않았다.

## 1. 기술 구성과 프로그램 배치

사용자가 채택한 구성은 Python/FastAPI 제어부·Python Runner·SQLite·React/TypeScript 웹 UI다. 제어부는 모듈형 단일 서비스로 시작하고 브라우저용 정적 빌드도 제공한다. 일반 설치 사용자가 Node.js로 화면을 직접 빌드하게 하지 않는다. UI를 개발하는 사람에게는 별도 프런트엔드 개발 도구가 필요하다.

제어부의 업무 상태·배정·게시 Outbox는 로컬 SQLite에 저장한다. 각 Runner는 로컬 실행 기록·미전송 이벤트·원문 저장소를 가진다. 원격 PC가 서버 DB 파일을 공유해 직접 쓰지 않는다. API 요청이 끝났다고 장기 작업을 잃지 않도록 영속 배정과 별도 실행 루프를 사용한다. FastAPI BackgroundTasks만으로 전체 업무 복구를 구현하지 않는다. [FastAPI 공식 안내](https://fastapi.tiangolo.com/tutorial/background-tasks/), [SQLite 사용 지침](https://www.sqlite.org/whentouse.html)

현재 상태 테이블과 변경 이력·이벤트를 트랜잭션으로 연결하는 안을 제안한다. 모든 상태를 이벤트 재생으로만 만드는 완전한 이벤트 소싱이나 별도 메시지 브로커를 첫 구현의 필수 조건으로 두지 않는다. DB 접근·아티팩트 저장·CLI·GitHub 연동 경계를 분리해 PostgreSQL 또는 다른 실행 엔진을 나중에 연결할 수 있게 한다.

[대화 중심 UI](ui-conversation-design.md)는 대표 대화를 Case에 연결하고 목표·Profile 미정의 준비 단계부터 같은 기록을 유지한다. 현재 요청의 처리 상태는 Case·Task·Run과 별도로 관리하고, 일반 전송 잠금·범위가 정해진 카드 답변·중단 제어를 서버에서도 검사해야 한다. Runner의 원문 수신·저장·heartbeat·제어 처리가 긴 CLI 실행에 막히지 않도록 실행 루프와 분리하는 보완이 필요하다. 중단 요청만으로 실제 종료를 선언하지 않으며 잔류 활동·재연결 대조·재개는 도구별 실증 대상이다. 이 항목들은 추가 구현 과제이고, 기술 구성 채택이나 P4-03 완료가 구현을 증명하지 않는다.

권장 코드 책임 구분은 다음과 같다. 디렉터리명은 구현 상세안이다.

```text
controller/  API, Case·정책·승인·배정·복구, 요약과 참조, GitHub Outbox
runner/      연결·실행·로컬 원문·컨텍스트·도구 경계·프로세스 관리
adapters/    CLI별 프로토콜 및 능력, GitHub·CI 읽기/반영
domain/      업무·기준·버전·게이트·결정의 공통 모델과 규칙
web/         React/TypeScript 화면과 검토·제어 상태
scripts/     사전 점검, 설치, 실행, 백업, 복원, 진단
tests/       상태/정책, 어댑터 계약, 장애 주입, 사용자 시나리오
```

## 2. 설치·접속·자격증명

첫 버전은 사용자가 Python·Git과 코딩 CLI를 준비하고 설치/실행 스크립트로 구동한다. 시스템은 별도 가상환경과 데이터 경로를 사용하고 기존 프로젝트·사용자의 CLI 설정을 조용히 수정하지 않는다. 설치된 도구의 경로·버전·인증 가능 여부를 사전 점검하며 로그인 비밀정보 원문을 진단 로그로 가져오지 않는다.

로컬 완결형, Windows 서버 완결형, Windows 서버 제어부 + Windows PC Runner를 지원 목표로 둔다. 일반 사용자 프로세스로 실행하므로 종료·로그아웃·재부팅으로 프로세스가 사라질 수 있다. 재실행 후 영속 기록에서 복구하며 로그인 없이 계속 실행되는 Windows 서비스 운영은 첫 버전 필수가 아니다.

로컬 기본 접점은 루프백 주소다. 원격은 같은 네트워크 또는 사용자가 마련한 VPN 안에서 소유자 인증과 암호화 연결을 사용한다. 소유자 초기 등록·로그인, 세션 만료·재인증, Runner별 등록·폐기 가능한 연결 자격증명을 구분하는 상세안을 둔다. Runner가 서버에 연결을 시작하고 PC 수신 포트를 필수로 열지 않는 방식을 우선한다. 인증서·신뢰 설정과 접속 주소는 설치 안내 및 원격 수용시험에 포함한다. 인터넷 직접 공개·공개 터널은 지원 범위에 넣지 않는다.

첫 사용자 한 명이어도 Owner·Project·Runner 소유 관계와 결정 주체를 기록한다. 향후 다중 사용자 권한을 적용할 API 경계를 두되 첫 버전에 초대·공동 편집·역할 관리 화면을 제공할 필요는 없다. 토큰·비밀번호·CLI 로그인 파일은 일반 업무 DB·대화·백업 매니페스트·GitHub 요약에 넣지 않는다. 필요한 비밀정보는 해당 호스트의 별도 보호 저장과 참조로 연결하며 구체 공급자 인증 방식은 연결 시험에서 검증한다.

GitHub 기록 게시·PR API·push의 자격증명 사용은 승인된 행동의 실행 경로에 연결한다. CLI가 기존 개인 계정의 권한에 접근할 수 있는 신뢰 모드에서 OS 수준의 우회 차단을 보장한다고 주장하지 않는다. 프로그램의 배정·승인 검사와 PC 전체 격리의 한계를 분리해 표시한다.

## 3. 동시 실행·시간·비용

초기 검증 규모는 프로젝트 10개·Runner 3대·동시 AI 실행 2개다. 초기 스케줄러의 동시 AI 실행 설정도 2개, 같은 Runner의 쓰기 실행은 기본 1개로 두는 상세안을 제안한다. 대기 Case 수나 등록 프로젝트 수를 10개로 제한하는 정책은 아니다. 한 Case의 여러 Repository는 같은 Runner에서 실행한다. Case×Repository 작업공간과 공유 자원·외부 ref/PR의 경합을 조정하고 읽기 검토는 고정 입력을 사용한다. 초기 같은 Runner 쓰기 기본1을 유지한다. 작업공간 분리가 원격 ref 충돌을 없애지는 않는다.

자동 실행 시간의 기본 한도는 없다. AI는 합의 범위와 품질 수정 횟수 제한 안에서 계속 진행하고 사람 결정·권한·미충족 조건이 있으면 대기한다. 경과 시간, 실행 시간, 대기 시간, CLI가 제공하는 사용량을 구분해 보여준다. 제공되지 않는 토큰·금액은 ‘알 수 없음’, 추정값은 ‘추정’으로 표시한다. 관측·중단 능력이 없는 정확한 청구 금액 상한을 보장하지 않는다. 설정한 한도는 능력에 맞게 검사·강제하며 미제공을 0으로 취급하지 않는다.

시간 한도를 선택할 때는 실행시간 합계를 기본으로 하며 논의·업무·재시도·종료 후 설명 실행을 누적하고 사람 대기는 제외한다. 병렬 실행은 각각의 시간을 합산하고 경과시간 한도는 별도 옵션으로 둔다. 기존 Case의 설정·관측값은 보존한다. 현재 `assigned_at → finished_at` 관측과 요청/질문 대기 구간을 대조하여 집계를 구현하며 잔류 활동 불명과 실제 초과 노출을 숨기지 않는다. 종료 후 설명 소비도 원래 Case에 누적하되 종료 시점의 소비 기록과 이후 누적을 구분해야 한다. D-87·88은 즉시 취소 능력의 실증이 아니다.

예산은 [공통 정책](autonomy-budget-policy.md)에 따라 기본 무제한, Case별 선택 제한이다. 모든 역할·repair·재시도·선택적 지식 추출에 예약/사용량을 연결하고 병렬 배정 전에 잔여량을 검사한다. hard 도달 시 새 비용 실행을 중지하고 결과 수신·기록·정리를 유지한다. 추정 한도로 설정한 지표만 추정 기준을 사용한다. 정확한 hard 한도가 필요한데 in-flight 제어·측정 능력이 없으면 설정/배정을 보류하고 자동으로 추정 한도로 낮추지 않는다. 정리 비용을 예산 밖의 새 AI 실행으로 숨기지 않는다. 모델 절약은 기존 허용 범위·필수 품질 기준을 유지한다.

품질 수정은 최초 실패 뒤 기본 최대 2회이며 Case별 조정 가능하다. 이는 환경 재시도와 별개다. 환경 오류의 초기 상세안은 같은 요청에 최대 3회 재시도와 지수형 대기를 적용하고, 일시적 오류만 재시도하는 방식이다. 인증·권한 오류와 결과가 불명확한 쓰기는 무조건 재실행하지 않는다. 이 3회는 사용자 제품 결정이 아니라 조정 가능한 구현 기본값 제안이다.

GitHub 게시가 누적 실패하면 게시 항목을 보존하고 알린다. 예를 들어 연속 3회 실패 후 배지와 알림을 남기고, 연결 회복·사용자의 재시도 요청에 따라 재개하는 상세안을 둔다. 내부 개발 업무는 계속할 수 있다. API가 재시도 지연을 지정하면 이를 존중한다. 반복 알림은 같은 장애 건으로 묶는다.

한 CLI 호출이 오래 실행되거나 응답을 멈춘 상태와 Case 전체 자동 실행 시간은 다르다. 도구별 timeout·진행 신호·프로세스 생존을 기록하고 상황에 맞게 상태 확인·중지 요청을 제공한다. 활동이 없다는 이유만으로 쓰기 프로세스가 끝났다고 판정하거나 다른 Runner에 같은 작업을 배정하지 않는다.

## 4. 백업·보존·복원

사용자 결정은 **각 호스트의 지정 경로에 하루 한 번 자동 백업, 최근 7개 보관, 원본·승인·증거 자동 삭제 없음**이다. PC 원문을 제어 서버로 복제하지 않는다. 처음 설정할 때 제어부와 Runner 각각의 백업 경로를 지정하고 마지막 성공·다음 예정·실패·누락 상태를 표시한다.

제어부 백업에는 상태 DB·요약·정책·승인·원문 참조·게시 대기를 포함한다. PC 백업에는 원문 문서·대화·지식 버전·증거·실행 기록과 복구에 필요한 Repository별 로컬 Git 상태를 포함한다. 원격 Git에 없는 커밋과 미커밋·미추적 변경을 원격 저장소에 이미 있는 것으로 가정하지 않는다. 자격증명은 이 일반 업무 백업에 자동 포함하지 않으며 복원 후 재인증 경로를 제공한다.

DB의 일관된 스냅샷과 파일 매니페스트를 작성하고 ID·버전·해시를 연결한다. 원문 아티팩트는 버전별로 보존하고, 변경 중 worktree의 백업은 안전한 실행 경계에서 시스템 쓰기를 잠시 보류하거나 불일치를 탐지해 다시 캡처한다. 외부 편집까지 원자적으로 정지시킬 수 있다고 가정하지 않는다. 불완전한 백업은 성공 표시하거나 정상 백업 7개 중 하나로 계산하지 않는 안을 둔다. 새 정상 백업의 완료를 확인한 뒤 오래된 백업 사본을 순환 정리한다.

호스트나 프로그램이 꺼져 있으면 예정 시각에 백업할 수 없다. 다시 실행됐을 때 누락된 백업을 감지해 한 번 수행하는 안을 제안한다. 같은 디스크에 있는 백업은 그 디스크의 고장까지 복구할 수 있는 사본이 아니므로 경로와 보호 범위를 화면에 명시한다. 용량 부족 시 원본을 자동 삭제하는 대신 백업/작업 상태와 필요한 조치를 알린다.

복원은 서버 상태와 PC 원문을 각각 가져온 뒤 매니페스트를 대조한다. 한쪽만 복구한 경우 원문 누락·버전 불일치·실행 결과 불명을 표시한다. 과거 승인 이력을 보존하되 근거가 없는 현재 실행에 재사용하지 않는다. PC가 유실되면 서버 요약이나 GitHub 댓글을 원문으로 승격하지 않는다. 복원 절차는 성공한 백업과 실제 원문으로 수용시험한다.

정상 실행 중 일일 백업이 계속 성공한다면 백업 시점 사이의 변경은 매체 장애 시 손실될 수 있다. 프로세스 재시작은 이와 달리 저장 완료 응답한 기록을 영속 저장에서 복원해야 한다. 백업 빈도와 ‘저장 완료 후 재시작에 보존’ 요구를 혼동하지 않는다.

## 5. 초기 비기능 수용시험 목표

아래의 시간값은 구현 검증을 시작하기 위한 **설계 제안**이다. 실제 측정 전 달성했다고 표시하지 않으며 사용자 제품 정책을 바꾸는 한도는 아니다.

| 항목 | 초기 시험 목표·방법 |
|---|---|
| 기준 환경 | 현재 확인한 Windows 11 Home 64비트 PC를 첫 호환성 환경으로 기록. CPU·메모리·디스크·도구 버전을 시험 때 함께 기록 |
| 규모 | 프로젝트 10개·Runner 3대·동시 AI 2개. 100개의 누적 Case와 긴 대화·다수 산출물 자료를 준비하는 시험안 |
| 화면·상태 응답 | 외부 모델/도구 대기와 대용량 상세 전송을 제외한 목록·상태 조회 p95 2초 이내를 초기 목표로 측정 |
| 저장 | 저장 완료 응답한 승인·결정·원문이 프로세스 강제 종료 후 보존. PC와 서버 중 한쪽 저장 실패 시 완료 응답하지 않음 |
| 단절 | 하트비트 누락을 설정된 탐지 기한 내 표시. 감지 후 다음 호출 차단과 현재 호출 결과 보존을 CLI/모드별 검증 |
| 재개 | 100 Case 시험 데이터에서 서비스 준비 뒤 1분 안에 복구 상태·미확인 실행 목록 표시를 초기 목표로 측정. 실제 장기 프로세스 종료 시간을 포함하지 않음 |
| 원문 비보관 | 상세 의도·대화·코드·diff·로그 열람 후 서버 DB·파일·큐·프록시/앱 로그·캐시에 원문이 남지 않는지 검사 |
| 복원 | DB/PC 원문을 함께, 또는 한쪽만 복원하여 참조 일치·누락 표시·재검증·승인 유효성 확인 |
| 운영 정직성 | 실제 CLI 비용 미제공, OpenCode 실증 미완료, 원문 오프라인, 외부 응답 유실을 성공으로 표시하지 않음 |

사용자는 기능·게이트·승인 기준을 설정할 수 있지만 AI가 성공하기 쉽도록 기준을 낮추어 시험을 통과시키지는 않는다. 기술 목표를 수정해야 한다면 측정 결과와 이유를 보존하고 제품에 미치는 영향을 설명한다.

## 6. 구현 순서와 인수 조건

현재 상태와 하위 작업·plan·검증·인계는 [DEVELOPMENT.md](DEVELOPMENT.md)가 정본이다. **현재는 P4-03까지 완료**했고 다음 하위 작업은 **UI-01 대화·요청 기반**이다. P1 제한, P3의 남은 예산 라이브 스킵 등 기존 미검증 범위도 유지한다. P1~P3-03의 v0.6 증거와 이후 v0.7 구현·증거는 당시 사실로 보존하고 v0.8 UI 개편의 통과 증거로 재명명하지 않는다. 아래 P1~P6는 기존 단계의 책임이며 실제 후속 수행 순서는 D-90에 따라 그 아래에 구분한다.

1. **P1 CLI 계약·실증:** 완료/제한 결과를 보존하고 새 실행 요구에 필요한 capability만 추가 검증한다.
2. **P2 최소 업무 흐름:** 완료된 영속 상태·원문·CLI·진입 검사 기반을 재사용한다. 기존 정책값·승인·종료 기록을 보존하며 새 기본값을 소급해 권한을 만들지 않는다.
3. **P3 실행 기반과 v0.7 적용:** P3-01~03 이후 **R1 정책·Profile·마이그레이션 → R2 복수 저장소 → R3 예산 → R4 자동 실행/완료 → P3-04 수직 통합**의 수행 순서와 완료 근거를 보존한다. P3-04의 경로 C 라이브는 사용자 지시로 스킵됐으며 구현·자동 시험과 라이브 검증의 범위를 구분한다.
4. **P4 품질·다양한 업무·지식:** P4-01 게이트/repair, P4-02 예약변경·재검증, P4-03 Profile 정의 v2 완료 계약은 완료됐다. 남은 P4-04 문맥/재개, P4-05 완료·예외·후속, 최소 지식 관리·주입(P4-06)·선택 추출(P4-07)은 각 범위의 plan과 검증으로 이어간다. 지식 자동화를 후속 확장으로 일괄 제외하지 않는다.
5. **P5 GitHub:** Project 지정 기록 저장소, 범위 사전 허용 push/PR, 동일 ref 경합·부분 효과, controlled 결과 확인→게시→필요 CI→종료를 검증한다. 실제 쓰기 시험의 대상·범위 허용은 제품 정책과 별도로 확인한다.
6. **P6 원격·운영·전체 수용:** 세 배포 형태·원문/지식 비보관·단절·예산/효과 복구·백업·설치와 목표 운영 규모의 FR/NFR를 검증한다. Case 분산 실행·범용 검색은 범위 밖이다.

개발 세션은 **현재 하위 작업 하나**를 plan에 따라 수행하고 완료/미완료·증거·다음 작업을 기록한다. 이 개발 규칙을 제품 사용자에게 강제하는 단계별 승인 절차로 전용하지 않는다. 미지원 능력이 확인되면 지원 범위를 정직하게 표시하고 실제 제품 정책 선택이 필요한 경우만 근거·대안과 함께 질문한다.

D-90으로 확정한 후속 개발 순서는 다음과 같다. 각 항목은 구현 예정이며 이번 문서 반영으로 착수하지 않는다.

| 순서 | 작업과 plan 경계 |
|---|---|
| 1. UI-01 대화·요청 기반 | 새 `UI-PLAN-01` 작성·공유. 준비 Case, 메시지·정정·참조, 요청 처리 상태/API 잠금, 접수 멱등성, 최초 업무화. 실제 중단 제어는 제외 |
| 2. P4-04 + UI-02 | 문맥·재개와 수신/실행 분리·질문·중단·복구를 연결하되 **별도 plan**으로 관리. 실제 종료·잔류 활동·재연결 대조를 검증한 후 중단 UI에 연결 |
| 3. UI-03 기본 화면 | PC 라이트 구성에서 프로젝트/대화→요청→질문→결과 검토를 연결하고 초안 복구·읽던 결과물 버전 유지를 구현 |
| 4. P4-05~07 + UI-04 | 완료 후 설명·후속, 활성 Profile 이행, 지식, 검색, 미커밋 포함, 외부 편집 연결 등 남은 흐름. UI-04 착수 시 세분 plan으로 나눔 |
| 5. P5/P6 | GitHub 외부 반영과 원격·운영·전체 수용을 기존 책임에 따라 수행 |

전체 UI를 P4-04에 자동 편입하지 않는다. 완료된 P4-PLAN-01~03은 다시 열지 않으며 기존 Profile 정의판·완료 의미·원문·판정·누적 소비를 보존한다. UI 제품 정책이 확정됐다는 사실과 실제 시안·사용성·CLI 제어 검증의 완료는 구분한다.
