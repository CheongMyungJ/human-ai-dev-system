# 사용자 결정 목록 — v0.8 2차 반영

확정 근거는 사용자와의 대화에서 받은 답변이다. 구현 방법·필드명·시험의 세부 수치는 사용자 결정과 구분한다. 2026-09-23 현재 D-01~90을 반영한다. D-59~67은 2026-09-21 통합 결정, D-68~82는 대화 중심 UI 논의의 1차 확정분, D-83~90은 후속 논의의 2차 확정분이다. **주요 UI·제품 정책과 개발 순서를 확정했으며 세부 화면·기술 선택은 구현 과정에서 검토한다.** 현재 구현은 P4-03 완료·스키마 v15다. 이전 표현과 대체 관계는 [검토 이력](review-status.md), 화면·동작의 상세는 [대화 중심 UI](ui-conversation-design.md)를 따른다. 설계 개정을 구현·검증 완료로 소급하지 않는다.

## D-01~30: 업무·의도·검토·품질 정책

| ID | 확인한 내용 | 설계에 미치는 영향 |
|---|---|---|
| D-01 | 개인용으로 시작하되 처음부터 공동 작업을 고려 | 사용자·작성자·승인자·프로젝트 접근을 식별할 수 있는 모델 필요 |
| D-02 | 개인이 로컬 또는 서버에 설치 | 특정 SaaS 운영 환경에 종속되지 않는 설치·운영 구조 |
| D-03 | 서버에서 개인 PC의 실행기를 연결하는 형태도 지원 | 제어부와 Runner의 분리, 인증·단절 복구 필요 |
| D-04 | Windows 우선 | Windows 경로·프로세스·셸·CLI 호환성 검증 우선 |
| D-05 | 시스템 자체는 Python 중심 선호 | 제어부와 Runner를 Python 중심으로 구성하는 기술안 제시 |
| D-06 | 관리 대상 프로젝트 언어는 제한하지 않음 | 언어별 빌드·테스트 명령과 도구를 프로젝트 설정으로 관리 |
| D-07 | 첫 버전은 프로젝트별 독립 업무 | 한 업무의 대상은 한 프로젝트. 복수 프로젝트 업무는 확장 지점 |
| D-08 | Codex·Claude Code·OpenCode 연결 | 공통 계약과 도구별 능력·차이를 표현하는 어댑터 필요 |
| D-09 | OpenCode는 미설치이며 공식 가이드 기준으로 우선 구현 | 문서 기반 계약과 실제 실행 검증의 상태를 분리 |
| D-10 | 첫 Git 서비스는 GitHub, 이슈·PR·CI 연동 포함 | GitHub 어댑터가 초기 대상. GitLab은 후속 확장 |
| D-11 | 유효한 요청·위임과 실행 조건 안에서 자동 진행 | 기본 ask-on-decision. 위임하지 않은 판단·material delta·권한 확대·예외에 질문. controlled 체크포인트는 별도 유지 |
| D-12 | 웹 UI를 주 화면으로 채택 | 의도·설계·계획 검토와 실행 제어는 웹에서 수행. IDE·CLI 접점은 이후 확장 |
| D-13 | 저장 기술은 무료로 설치해 사용할 수 있으면 됨 | 무료 설치 가능한 DB·파일 저장 방식을 후보로 검토 |
| D-14 | AI가 Profile에 맞는 의도 초안을 작성하고 항목별 피드백 반영 | 명확한 요청 자체가 실행 위임 근거. 별도 초안 동의는 일괄 필수가 아니며 controlled 시작 확인·중대한 의미 변경에는 명시 결정 필요 |
| D-15 | 필요한 설계·계획의 깊이와 검토는 AI가 규모·영향·불확실성·정책으로 선택 | 판단·작업·검증 방법을 기록하고 필요한 제품 선택에만 질문 |
| D-16 | 설계·계획의 별도 사람 검토는 선택 사항 | Fast Lane은 별도 단계·파일·AI 실행 없이 필요한 논리 내용을 결합할 수 있음. 근거·성공 기준·필수 검증은 유지 |
| D-17 | 업무화된 Case당 기록 이슈 하나를 Project의 지정 저장소에 생성(D-74) | 여러 저장소의 PR을 연결. 논의 단계에는 생성하지 않으며 기록 지정만으로 코드 권한을 만들지 않음. 별도 허용이 있으면 겸용 가능(D-73) |
| D-18 | 원본 산출물·업무 상태·승인은 시스템이 관리 | GitHub는 누적 게시 대상. 기존 본문·댓글을 편집하거나 양방향 편집 동기화하지 않음 |
| D-19 | 논의가 구체 업무로 전환될 때 기록 이슈·요약 게시 범위를 연결·확인 | 동일 범위 반복 확인 없음. 연결·허용 전 실제 게시하지 않으며 push·PR은 별도 범위(D-64·74) |
| D-20 | 시스템이 만든 이슈·댓글 상단에 식별 표시 필수 | 사람 작성 글과 구분하는 `[AI 협업 시스템 기록]` 헤더 제안 |
| D-21 | 기본 ask-on-decision, 설계·계획의 별도 사람 검토는 기본 필수 아님 | controlled는 시작과 최종 결과 확인. 선택한 추가 검토와 위임하지 않은 판단은 유지 |
| D-22 | 허용된 읽기·분석을 진행하고 작업별 실행 조건 평가 | 자동 모드에 일괄 의도 동의 장벽 없음. controlled·미결 의존·권한은 유지. 로컬 실험은 D-66 |
| D-23 | WorkDepth는 simple / standard / deep | 깊이와 Autonomy·Budget을 분리. fast는 자율성 등급이 아니라 실행 경로 |
| D-24 | 필수 품질 기준 실패 시 AI가 수정·재검증하고 해결 전 후속 진행 보류 | 반복 실패나 사람 판단이 필요한 경우 요청. AI 검토 분리는 D-28, 자동 수정 기본 한도는 D-29로 구체화 |
| D-25 | 모든 Profile의 요청 정합성과 작업 준비 조건은 필수 확인 | 명확·저위험이면 가벼운 확인, 불확실성·영향·정책상 필요하면 독립 의미 검토. 미실행을 통과로 표시하지 않음 |
| D-26 | 추가 게이트는 규모·위험·Profile·Project 정책으로 선택 | AI·시스템이 근거와 적용 설정을 기록하고 사람이 조정 가능. OFF/미적용/통과 구별, 성공 기준·필수 검증 유지 |
| D-27 | Case 정책을 기본으로 필요한 Task에 게이트 구체화 | 작업 분할·명칭 변경으로 정합성·권한·성공 기준을 우회하지 않음 |
| D-28 | 독립 AI 의미 검토가 선택되면 작성과 별도 세션에서 수행 | 같은 CLI·모델 사용 가능. 결정적 검사·테스트·작성자 자체 점검과 구별하며 매 단계 별도 AI 호출을 의무화하지 않음 |
| D-29 | 최초 품질 실패 후 자동 수정·재검증 기본 최대 2회, 업무별 한도 조정 가능 | 한도 후 사람에게 요청. 제품 판단이 필요하면 즉시 질문. 환경 장애 재시도와 결함 수정 횟수 구분. **2026-09-24 보충:** P4-05 의 재작성 2회·작업 재시도 1회는 기본값이며, 시스템 기본값(환경 변수) + Case 별 이력 조정의 설정으로 바꾼다(P4-05b, DEVELOPMENT.md 1.8절). 기준 판정의 제품 기록 규칙과 자동 모드의 사람 동의는 그대로 확인 |
| D-30 | 실행 중 품질 게이트 설정 변경은 예약하고 현재 검증 1회 종료 직후 반영 | 이후 자동 수정·재검증부터 새 설정 사용. 현재 결과는 보존하며 예약만으로 진행 중 검사를 중단하지 않음 |

## D-31~58: 완료·실행·저장·운영 정책

### D-31 최종 결과 확인과 완료

기본 ask-on-decision은 성공 기준·필수 검증·요청한 외부 완료조건을 충족하면 자동 완료한다. controlled는 시작과 결과 후보를 사람이 확인하고 사전 허용된 게시·필요한 외부 CI 후 시스템이 종료를 확정한다(D-65). 자동 완료에 사람 인수 기록을 만들지 않으며 예외 수용은 D-32를 따른다.

### D-32 예외 수용 종료

사람이 남은 문제·미검증 범위와 영향을 확인한 경우 `예외 수용 종료`를 허용한다. 미충족·미검증 및 게이트 판정은 통과로 변경하지 않는다. 범위·기준을 수정하는 결정과 현 기준의 예외를 수용하는 결정은 구분한다. 외부 행동 권한을 자동 생성하지 않는다.

### D-33 완료 후 수정은 연결된 새 업무

완료한 업무의 수정 요청은 항상 연결된 새 업무로 생성한다. 기존 업무의 종료 기록과 결과는 보존한다. 원래 결과에 대한 설명 질문만으로 새 수정 업무를 자동 생성하지 않는다. 새 업무에서 필요한 의도·검토·검증 조건을 적용하며 과거 승인을 새 범위의 승인으로 묵시적으로 확대하지 않는다.

### D-34 Case당 새 기록 이슈

기존 이슈는 출처로 연결하고 Project의 지정 기록 저장소에 업무화된 Case당 새 이슈 하나를 만든다. 논의 단계에는 만들지 않고 업무화 뒤 연결·게시 허용 안에서 생성한다(D-74). 참여 저장소마다 이슈를 복제하지 않고 여러 PR을 Case에 연결한다. 기록 역할과 코드 쓰기 허용은 독립이며 같은 저장소의 겸용은 가능하다(D-73).

### D-35 GitHub 게시 장애와 내부 진행 분리

GitHub 이슈 생성·산출물 댓글 게시에 실패해도 내부 작업은 계속한다. 게시할 내용과 전송 상태를 보존하고 재시도하며 누적 실패를 사용자에게 알린다. 응답 유실 시 기존 결과를 대조하여 중복 게시를 피한다. 업무 진행·결과 확인과 게시 전달 상태를 구분한다.

### D-36 첫 버전 GitHub 쓰기 범위

push·PR 생성/수정을 지원한다. Case 시작 때 허용한 저장소·ref·행동·범위·유효기간·선행조건 안에서 자동 수행한다(D-64). 실행 직전 정확한 payload·대상·버전과 유효 조건을 검사·기록한다. 새 권한이나 material delta는 질문하며 controlled는 D-65를 따른다. 강제 push·병합·이슈 종료·CI 수동 재실행·배포를 묵시적으로 포함하지 않는다. 기록 이슈·댓글 허용은 별도이다.

### D-37 GitHub 입력 범위

PR 리뷰만 업무 피드백으로 수집한다. 이슈 댓글은 기록 열람용으로 유지하고 실행 명령이나 승인으로 해석하지 않는다. 산출물의 양방향 편집 동기화는 하지 않는다.

### D-38 Project 안의 복수 저장소

Project는 여러 Repository를 포함하고 Case는 필요한 일부를 선택한다. 기존 허용 쓰기 범위와 목표 안의 추가 저장소는 자동 선택한다. 명시적 제외·새 권한·제품/데이터 영향은 재판단한다. 한 Case의 여러 저장소 작업은 같은 Runner에서 수행한다. 여러 Project 또는 여러 Runner에 걸친 Case는 이후 확장이다(D-63).

### D-39 Case × Repository 작업공간

코드 변경 Case는 Repository별 별도 브랜치·worktree와 시작 기준을 가진다. 저장소별 스냅샷 및 전체 조합을 근거에 연결한다. 사용자 원래 미커밋 변경을 자동 폐기·stash·커밋하지 않는다. worktree는 OS 권한·외부 자원의 격리가 아니다.

### D-40 Runner 단절 시 안전 경계에서 정지

서버와 개인 PC Runner 연결이 끊기면 현재 도구 호출이 끝나는 안전한 경계에서 멈추고 재연결 후 재개한다. 연결 단절만으로 같은 쓰기 작업을 다른 Runner에 중복 배정하지 않는다. CLI별 내부 도구 경계 제어 가능 여부는 실제 호환성 검증 대상으로 남기고 지원 여부를 허위로 표시하지 않는다.


### D-41 Windows 네이티브 우선

첫 버전은 Windows에서 직접 실행하는 로컬 설치·서버 설치·개인 PC Runner를 지원한다. Linux/WSL은 이후 확장으로 둔다.

### D-42 첫 버전 실제 사용자는 한 명

첫 버전은 한 사용자용으로 제공한다. 사용자·소유자·프로젝트별 권한을 확장할 수 있는 데이터 구조와 행위자 기록을 초기부터 둔다. 다중 계정·공동 편집·역할 관리 UI는 첫 버전 필수 범위가 아니다.

### D-43 개인 PC 원문 보관

서버+개인 PC Runner 구성에서는 상태와 요약만 서버에 보관한다. 코드·diff·상세 로그는 PC에 유지하며 PC가 연결돼야 상세 열람할 수 있다. 필요한 원문이 있는 Runner에서 AI 분석·작성·검토를 실행하는 구조로 구체화한다. 단일 호스트 구성에서는 동일한 논리 경계가 같은 PC에 배치될 수 있다.


### D-44 신뢰하는 개인 프로젝트 실행

첫 버전 Windows 네이티브 Runner는 개인 소유의 신뢰하는 프로젝트를 대상으로 한다. CLI 권한 설정과 시스템 승인으로 통제하되 OS 수준의 완전 격리는 보장하지 않는다. worktree 분리와 실제 권한 격리를 혼동하지 않는다. 기능 구현 배정 조건과 승인 기록은 시스템에서 계속 검사한다.

### D-45 역할별 CLI·모델 배정

Project 기본 CLI·모델 및 Case·역할별 설정을 사용한다. 형식·규칙은 가능한 한 결정적 코드, 의미 판단은 역할·위험·예산에 맞는 허용 모델로 배정한다. 독립 검토에 다른 공급자를 의무화하지 않는다. 사전 허용되지 않은 다른 CLI로 장애 대체하기 전 확인하며 저비용 모델 전환으로 필수 검증을 없애지 않는다.

### D-46 GitHub 요약 게시

GitHub에는 의도·설계·계획·검증 요약과 참조만 누적 게시한다. 코드·diff·상세 로그는 제외한다. 전체 산출물은 시스템에서 관리하고 GitHub는 요약 기록을 제공한다. 게시 직전 내용 점검은 수행하되 자동 요약·비밀정보 필터의 완벽성을 주장하지 않는다.


### D-47 업무별 시작·종료 의미

feature, defect-fix, root-cause-analysis, research, refactoring, maintenance Profile은 의미 계약이며 고정 pipeline이 아니다(D-62). 목표·범위·종료조건과 위임 안에서 진행한다. 대표 Profile 하나로 다른 요청을 지우지 않는다. 충분한 증거로 이미 목표 상태임을 확인하면 무변경 완료도 가능하지만 미재현·CLI 정상 종료만으로 해결을 주장하지 않는다.

### D-48 분석·연구의 로컬 실험

기존 Project 권한·예산·허용 작업공간·테스트 데이터 안의 복구 가능한 로컬 재현·임시 계측·테스트는 자동 수행한다. controlled 체크포인트, 권한 확대·운영 데이터·외부 영향 판단은 유지한다. 원인 분석만 요청한 경우 제품 수정으로 목적을 확대하지 않는다(D-66).

### D-49 첫 버전 최소 지식 관리·추출·활용

첫 버전에 근거·버전·적용 범위가 있는 지식 관리, 의미 있는 작업 결과에서 선택적 추출, 작업 범위·활동에 맞는 자동 주입을 포함한다(D-67). 매 Case의 지식 산출물·추출 실행·사람 검토는 의무가 아니다. 범용 검색·외부 지식 시스템은 구성 요소나 의존 조건에 넣지 않는다.

### D-50 의도 질문의 결정 시점

사람의 결정이 필요한 부분은 필수로 확인한다. 의도 단계에 필요한 결정을 받지 않고 AI가 임의 확정하지 않는다. 설계·계획 단계로 미루는 것이 적절한 질문은 미룰 단계와 의존 관계를 표시하고 진행할 수 있다. 해당 질문에 의존하는 작업 전에 필요한 사람 결정을 받으며, 설계·계획 자동 진행 옵션이 이 확인을 대신하지 않는다.

### D-51 상세 문서·대화 원문도 PC 보관

서버+개인 PC Runner 구성에서 상세 의도·설계·계획·대화 원문도 PC에 보관한다. 서버는 영구 저장하지 않고 일시중계하며 PC 연결 시 열람·검토한다. 서버 상태·요약 및 원문 ID·버전·해시 참조와 PC 원문을 함께 논리적 시스템 기록으로 관리한다.

### D-52 외부 AI 전송은 기존 CLI 설정 사용

첫 버전은 코드·문서의 외부 AI 전송에 대해 기존 CLI의 사용자 설정을 그대로 따른다. 별도 프로젝트별 전송 정책은 이후 확장이다. Runner에서 CLI를 실행하는 것을 오프라인 모델 처리라고 설명하지 않는다. 로그인 자격증명은 Runner에 유지한다.


### D-53 기술 구성 채택

Python/FastAPI 제어부·Runner, SQLite, React/TypeScript 웹 UI를 채택한다. PostgreSQL과 별도 대형 워크플로 서버는 필요해질 때 확장한다. 브라우저 화면 외 주요 제어·실행 로직은 Python 중심이다.

### D-54 첫 배포 설치 방식

사용자가 Python·Git을 준비한 뒤 설치/실행 스크립트로 구동한다. 빌드된 웹 화면을 포함하고 일반 사용자 프로세스로 실행한다. 코딩 CLI 설치·로그인은 각 도구의 기존 방식을 사용한다. Python 런타임 포함 설치 프로그램과 Windows 서비스 운영은 첫 버전 필수가 아니다.

### D-55 초기 수용시험 운영 규모

프로젝트 10개·Runner 3대·동시 AI 실행 2개를 성능·복구 수용시험 기준으로 둔다. 같은 Runner의 쓰기 실행은 기본 1개다. 이 수치는 제품의 프로젝트·Runner 등록 개수 제한이나 실측 성능 보장이 아니다.


### D-56 기본 무제한과 선택적 예산 통제

전체 자동 실행 예산은 기본 무제한이다. 설정한 Case 한도는 작성·검토·재시도·선택적 지식 추출을 합산하며 강제 가능한 범위를 명시한다(D-61). 실행/대기/경과 시간과 정확/추정/미제공 사용량을 구별한다. 소진은 대기·중단이며 성공이나 기준 완화가 아니다. 품질 수정 한도와 권한은 별도 유지한다.

### D-57 백업과 보존

하루 한 번 각 호스트의 지정 경로에 자동 백업하고 최근 7개를 보관한다. 원본·승인·증거는 자동 삭제하지 않는다. 서버 상태와 PC 원문은 각 저장 위치에서 따로 백업하고 PC 원문을 서버로 복제하지 않는다.

### D-58 원격 접속 지원 범위

같은 네트워크 또는 사용자가 마련한 VPN 안에서 서버 웹 UI·Runner 연결을 지원한다. 원격 접속은 소유자 인증과 암호화 연결을 적용한다. 인터넷 직접 공개 구성은 이후 확장이다.



## D-59~67: 자동화·복수 저장소·지식 통합 (2026-09-21)

### D-59 WorkDepth와 Autonomy 분리

WorkDepth는 simple / standard / deep, Autonomy는 기본 ask-on-decision과 controlled다. deep 분석도 매 단계 사람 확인 없이 진행할 수 있다. fast는 조건에 맞는 실행 경로다. 사람은 위임하지 않은 제품/UX 선택, 권한 확대·되돌리기 어려운 외부 행동, 증거로 해소하지 못한 중요한 모호성, 정책 한도 변경, 예외 수용에서 호출한다. 검토 필요성·피드백 반영·stale 판정·증거 대응·게이트 선택·결과 후보는 AI와 시스템이 처리한다.

### D-60 Fast Lane과 material-delta 판단

최초 요청+유효한 후속 결정+Project 정책이 실행 위임 근거다. AI 초안을 사람이 승인한 것으로 기록하지 않는다. 정규화→가벼운 확인→필요한 계획/구현/검증→결과를 별도 단계·파일·실행 없이 연결할 수 있다. 독립 의미 검토는 불확실성·영향·정책상 필요할 때만 실행한다. material delta는 이전 AI 초안이 아니라 마지막 유효 위임과 비교하고 누적 변경도 본다. 범위 추가/제외, 새 제품 선택, 보안/권한/데이터 영향, 중요한 성공 기준 변경을 재판단한다. 허용 내 기술 선택·조사·깊이 증가는 자동이며 Fast Lane 이탈만으로 controlled로 바꾸지 않는다.

### D-61 강제 가능한 예산

한도 없는 기본값과 제한 Case를 지원한다. 실행/검토 횟수, 입출력/문맥 크기, 시간, 제공 토큰·추정 비용은 측정 방식과 강제 범위를 함께 기록한다. CLI Run과 내부 모델 호출은 동일하지 않다. 병렬 배정 전 예약·실행 후 대조로 전체 역할·재시도·추출을 누적하며 세션·Task 분할로 초기화하지 않는다. 경고선은 알림·허용된 절약, hard 한도는 새 비용 발생 실행 금지와 기존 결과 수신·기록·정리다. continue로 hard 한도를 우회하지 않고 계속하려면 한도 변경이 필요하다. 추정 한도로 명시 설정한 지표만 추정값 기준으로 강제한다. 정확한 hard 한도를 요구했는데 in-flight·미제공 지표 등으로 보장할 수 없다면 해당 설정/실행을 허용하지 않으며 배정을 보류한다. 표시만 추정으로 바꾸어 계속 실행하지 않는다. 상세는 [자동화·예산 정책](autonomy-budget-policy.md).

### D-62 Case Profile과 intake 초안

초기 여섯 Profile은 feature / defect-fix / root-cause-analysis / research / refactoring / maintenance다. AI intake 초안의 질문·성공 기준·미정 내용을 목적별로 구성한다. 공통 출처·범위·제약 위에 모든 양식을 중복시키거나 사용자에게 빈칸을 채우게 하지 않는다. 대표 Profile과 Task 활동을 구분하고 Profile 버전을 고정한다. 분류 변경만으로 재승인·예산 초기화·기준 삭제를 하지 않는다. 초안 공개와 특정 작업 실행 가능을 분리한다. 상세는 [Case Profile](case-profiles.md).

### D-63 복수 저장소의 범위·배치·기록

등록 저장소, 쓰기/게시 허용 집합, Case 선택 집합을 구분한다. 동일 목표·허용 안의 저장소는 자동 추가하고 명시 제외·새 권한·material delta는 질문한다. Case × Repository 작업공간을 동일 Runner에 준비하고 증거는 저장소별 버전과 통합 조합에 연결한다. 업무화된 Case의 기록 이슈는 D-74를 따른다. 기록 지정만으로 자동 체크아웃·코드 쓰기 대상으로 삼지 않되 별도 코드 허용이 있으면 겸용한다(D-73). 부분 완료는 전체 완료가 아니며 원격 결과 불명은 대조 후 재시도한다.

### D-64 Case 범위의 push·PR 사전 허용

Case 시작 때 실제 대상과 허용 경계를 지정하면 범위 내 push·PR 생성/수정을 자동 수행한다. 준비된 payload는 실행 직전 재검사하며 허용이 의무 게시나 완료를 뜻하지 않는다. 완료 지점은 요청·설정에 따라 로컬 결과/PR 생성/필수 CI 통과 등으로 정한다. 쓰기 허용 저장소 추가는 게시 허용 확대가 아니다. 동시 Case의 같은 원격 ref/PR 수정을 조정하고 대상 변화의 영향 검증을 갱신한다. 알려진 실패만 재시도하며 성공한 효과를 일괄 삭제·되돌리지 않는다.

### D-65 controlled 확인 순서

시작 때 목표·범위·기준·허용 행동을 확인하고 결과 후보를 공개 전에 확인한다. 설계/계획 사람 검토는 별도 선택이다. 로컬 구현·검증→diff/근거/정확한 대상/준비된 PR 내용 확인→사전 허용 push·PR→필요 외부 CI→시스템 종료 순서다. 결과 확인 당시 외부 검증 대기를 표시한다. CI 수정으로 확인한 내용이 바뀌면 새 후보를 재확인한 뒤 게시한다. 동일 내용 재전송에는 재확인하지 않는다.

### D-66 로컬 실험의 실행 위임

D-48 자동 실험은 원래 목적·허용 작업공간·테스트 데이터·예산·선행 결정 안에서 진행한다. 임시 코드가 있다는 이유만으로 기능 개발 전체 절차를 요구하지 않는다. 실험은 제품 수정·운영 데이터 변경·새 외부 자원·권한 확대를 포괄하지 않는다. 증거와 임시 변경을 구분하고 관찰을 프로젝트 전체 규칙으로 자동 일반화하지 않는다.

### D-67 최소 프로젝트 지식

재사용할 결정 이유·제약·알려진 문제·비자명한 운영 사실을 원본 참조·짧은 적용 내용·조건·버전·필수/참고·상태로 관리한다. 확정 규칙의 등록은 권위를 계승해 재승인하지 않는다. AI의 새 규칙 제안은 후보이며 의무를 만들지 않는다. 의미 있는 발견을 기존 작업 결과에서 선택적으로 추출하고 중복은 근거 추가/버전 대체로 정리한다. 활동+범위에 따라 주입하고 범위/지식 변경 시 영향 부분을 재평가한다. 필수 내용은 조건·예외까지 제공하며 주입 기록과 준수 검증을 구분한다. 선택적 추출은 완료를 막지 않는다. 상세 원문은 기존 Runner 경계를 따른다. 외부 검색 체계는 추가하지 않는다. 상세는 [프로젝트 지식](project-knowledge.md).

**2026-09-24 보충(P4-06b 로 구현 예정):** 사용자 결정으로 **지식은 원문 PC 경계의 예외**다. 지식의 적용 내용(짧은 본문·조건·예외)과 대화에서 자동 등록된 규칙의 **권위 원문(그 사용자 메시지 한 건)**을 서버 DB 에 저장해, 다른 PC·소유 PC 미연결에서도 주입할 수 있게 한다. 대화·의도·설계·코드 등 다른 원문의 경계는 그대로다. Runner 의 해시 대조·영수증(실제로 읽은 것만 읽음)은 유지하고, 서버 로그에는 본문을 남기지 않으며, 등록 화면·카드에 "서버에 저장됨, 비밀값을 적지 말 것"을 알린다. 이 결정으로 data-boundary-review 5절·project-knowledge 5절·AC-35·36 의 "원문 자동 서버 복제 없음"이 지식에 한해 바뀐다.

## D-68~82: 대화 중심 UI 1차 결정 (2026-09-23)

### D-68 PC·현재 프로젝트 중심의 대화 화면

Codex 앱과 유사한 왼쪽 프로젝트/대화 목록, 가운데 대화, 필요할 때 여는 오른쪽 검토 패널을 사용한다. 한 프로젝트 집중 사용을 기본으로 프로젝트 전환 시 마지막 대화·위치를 복원한다. 패널 접기·크기 조절·크기 기억을 제공한다. 라이트 테마가 기본이고 다크/시스템 따르기를 선택할 수 있다. 휴대폰·태블릿 전용 최적화는 후속 검토다.

### D-69 논의 준비 단계와 같은 Case의 업무화

대표 사용자 대화 하나가 Case 하나다. 목표·Profile이 미정인 논의 준비 단계부터 같은 Case에 원문·결정·제한·소비를 보존한다. 명확한 분석/실행 요청으로 업무화하며 일괄 시작 버튼을 강제하지 않는다. 단순 동의는 해당 선택에만 적용한다. 준비 단계는 Autonomy·권한과 별개다. 논의만 끝낸 대화는 보관·복원하고 완료/취소로 바꾸지 않는다. 질문 없는 휴식은 답변 필요가 아니다. 최초 Profile 배정과 기존 업무의 재분류는 구분하며 후자는 D-86으로 구체화한다.

### D-70 요청 처리 단위의 입력과 정정

현재 요청 처리 중에는 일반 전송을 막되 초안 편집을 허용한다. 질문·확인 카드의 대상 한정 답변과 중단은 별도 접점이다. 일반 후속 전송 대기열은 만들지 않는다. 서버가 중복·충돌을 검사하며 내부 Run 사이에 잠금을 풀거나 Case 전체 종료까지 잠그지 않는다. 다른 Case 사용은 가능하다. 보낸 메시지 수정은 원문을 유지한 새 정정 요청으로 남기며 이전 실행·효과를 지우지 않는다. 기존 FR-11/SC-07의 실행 중 일반 피드백 접수 범위를 이 결정으로 개정한다.

### D-71 진행 요약과 산출물 검토

대화에는 현재 활동·필요한 판단·결과 요약을 우선하고 세부 실행·로그는 펼쳐 본다. 실제 근거 없는 진행률·검토 완료를 표시하지 않는다. 오른쪽에서 실제 존재하는 문서·diff·검증을 보고 선택 위치를 버전 참조로 입력창에 붙여 피드백한다. 질문은 선택지·직접 입력이 있는 카드로 받고 답변 뒤 결정을 보여준다. 대화 통합과 실제 독립 reviewer 세션·증거 분리는 양립한다.

### D-72 프로젝트 기본값과 명확한 확인 동작

프로젝트 기본 도구·모델·깊이·자율성·예산을 주로 사용하고 입력창에는 간결한 적용 요약을 둔다. Case 조정은 펼쳐서 변경·출처·적용 시점과 기본값 복귀를 보여준다. controlled 시작/결과·권한 확대·예외는 대상과 범위를 제시하는 카드·버튼으로 확인한다. 의례적 동의 문구 타이핑을 강제하지 않으며 필요한 이유·범위와 행위자·시각·버전을 보존한다. 확인이 다른 행동의 허용이 되지 않는다.

### D-73 최소 프로젝트 등록과 저장소 겸용

폴더/대표 저장소, 실행 환경, 기본값을 확인하고 대화를 시작한다. Git remote와 기존 인증·접근 상태로 GitHub 연결을 설정하며 모호한 대상·부족한 인증에만 추가 입력을 받는다. GitHub 설정은 미룰 수 있다. 대표 저장소의 Issues를 업무 이슈 위치로 기본 제안하고 별도 저장소는 선택 사항이다. 같은 저장소가 코드와 기록을 겸할 수 있지만 각 허용은 별도다. 현재 구현의 기록 repo 코드 대상 일괄 거부는 변경 대상이며 이미 해소된 것으로 표시하지 않는다.

### D-74 업무화 이후의 기록 이슈

논의 준비 단계에서는 기록 이슈를 만들지 않는다. 구체 업무 전환 뒤 연결·대상·게시 허용 안에서 Case당 하나를 생성하며 필요한 선행 논의를 요약한다. GitHub 연결은 게시 허용이 아니다. 미연결·미허용·게시 대기와 내부 진행을 구분하고 기존 이슈·허용·전송 결과를 보존한다. 상세 원문은 PC에 남긴다.

### D-75 PC 단절 시 읽기·초안과 전송 구분

상태·요약의 마지막 확인 시각과 이미 불러온 내용·작성 중 입력을 유지한다. 추가 원문 열람에 연결 필요를 표시하고 일반 메시지·카드 답변 전송을 막는다. 실행은 확인 전까지 상태 불명으로 표시한다. 재연결 시 실제 상태·입력 저장 여부를 대조하고 초안은 사용자가 직접 전송한다. GitHub만 실패하면 내부 업무는 계속한다. 초안의 보관·복구 범위는 D-83으로 구체화하며 저장 기술은 구현 시 정한다.

### D-76 현재 요청의 중단과 보존 재개

중단 요청 후 관련 후속 실행 시작을 막고 실제 종료·잔류 활동을 확인한 뒤 전송을 다시 허용한다. 코드·문서·대화·소비를 보존하고 완료 부분·남은 변경·미완료/미검증을 요약한다. 같은 Case에서 현재 상태를 다시 확인하여 후속 요청을 진행할 수 있다. 중단 요청을 성공·취소 완료·롤백으로 표시하지 않으며 불명 상태에서 충돌 실행을 시작하지 않는다. CLI별 실증이 필요하다.

### D-77 작업 기준과 미커밋 변경

새 코드 업무는 등록 폴더의 현재 브랜치 마지막 커밋을 기본으로 하고 기준 변경 경로를 둔다. 미커밋 변경이 있으면 목록과 함께 포함/커밋된 코드만 선택하게 한다. 포함 시에도 원래 폴더를 보존하고 별도 작업공간에 정확한 시작 상태를 기록한다. 후속 수정은 이전 업무 결과를 기준으로 제안한다. 현재 커밋 기반 구현에 미커밋 포함 지원이 이미 있다는 뜻은 아니다.

### D-78 로컬 완료 기본값과 종료 후 대화

코드 변경 업무의 기본 완료 지점은 전용 작업공간의 로컬 구현·검증이다. 원래 폴더 반영·기본 브랜치 병합을 뜻하지 않는다. 요청에 PR/CI가 포함되면 해당 지점과 허용을 적용한다. 결과에는 변경·검증·남은 문제·작업공간·반영 상태를 표시한다. D-33을 유지하여 종료 후 명확한 수정은 새 Case/대화를 연결하고 이동하며 설명 질문은 기존 대화에 남긴다. 기존 Profile 완료 계약·기준을 완화하지 않는다.

### D-79 제한 자동 복구와 같은 업무 재시도

일시 오류는 기존 권한·예산·횟수 한도 안에서 자동 복구하고 로그인·설정·새 판단 등이 필요하면 조치 카드로 안내한다. 코드 보완 한도는 환경 재시도와 별도다. 실행/게시 결과 불명은 먼저 대조한다. 재시도는 같은 Case에서 유효한 결과를 재사용하고 실패·소비·repair 누적을 보존한다.

### D-80 Case 결정과 프로젝트 공통 규칙

대화의 결정은 출처와 적용 범위를 연결해 자동 정리한다. 현재 업무의 결정은 대화의 결정 사항에서, 공통 규칙은 프로젝트 설정에서 확인·변경한다. 원문과 이전 버전·대체 관계를 보존한다. 기존 권위의 충실한 등록은 재승인하지 않되 AI 제안을 새 필수 규칙으로 승격하지 않는다. 중요한 범위 모호성·충돌은 질문한다. D-67의 적용·추출·원문·예산 경계를 유지한다.

**2026-09-24 보충(P4-06):** 사용자가 대화에서 "이 프로젝트에서는 앞으로 …"처럼 프로젝트 공통 규칙을 말하면 논의 응답의 AI가 옮겨 적은 짧은 적용 내용을 **그 사용자 메시지를 권위로 바로 활성 등록**한다(재승인 없음). 옮겨 적기의 충실성은 시스템이 판정하지 못하므로 필수 규칙을 주입할 때 원래 사용자 메시지를 함께 넣고, 대화에 등록 카드와 무효화·개정 경로를 둔다. "이번 업무에서는"의 선택과 범위가 모호한 말은 등록하지 않고 묻는다. 구현·근거는 [P4-06 결과](p4/evidence/P4-06-results.md).

### D-81 텍스트와 프로젝트 자료 참조

첫 버전 입력은 텍스트, 프로젝트 파일·코드·기존 산출물의 버전/위치 참조다. 이미지 첨부·분석은 후속 검토로 미룬다. 범용 외부 파일 변환/업로드 기능까지 포함한 결정이 아니다. 접수는 PC 원문 저장·서버 참조 등록과 연결한다.

### D-82 필요한 상황의 PC 알림

답변·결과 확인 필요, 의미 있는 차단·오류·예산 문제, 완료를 알리고 일상 진행은 알리지 않는다. 현재 대화를 보고 있으면 중복 팝업을 억제한다. 다른 프로젝트의 상태는 선택기에 표시하고 자동 이동하지 않는다.

## D-83~90: 대화 중심 UI 2차 결정 (2026-09-23)

### D-83 현재 브라우저의 대화별 초안 복구

텍스트와 첨부한 프로젝트 참조를 현재 브라우저에 대화별 자동 저장하고 대화 전환·새로고침·브라우저 종료 후 복구한다. 전송 실패·접수 불명에는 유지하고 PC 원문 저장·서버 참조 등록으로 접수가 확인된 전송분만 비운다. 복구한 초안은 수동 전송하며 기기 간 자동 동기화하지 않는다. 초안 저장과 메시지 접수는 다른 상태이고 서버 원문 대기열을 만들지 않는다. 저장 기술은 구현 상세다.

### D-84 보관 포함 프로젝트 대화 검색

현재 프로젝트의 제목·요약·본문·결정을 보관된 대화까지 검색한다. 원문 PC 연결 시 본문을 검색하고 미연결이면 서버의 제목·요약·결정 요약만 검색하며 일부 내용 제외를 명확하게 표시한다. 결과의 대화 제목·일치 부분에서 해당 메시지나 결정으로 이동한다. 본문·본문 인덱스는 PC에 남긴다. 프로젝트 코드·파일 전체 검색을 추가하는 결정이 아니다.

### D-85 결과물 목록과 검토 버전 유지

대화 상단 결과물 목록에서 실제 산출물을 오른쪽 패널로 연다. 일반 진입은 최신 버전, 과거 메시지 링크는 그때 버전이다. 읽고 있는 버전보다 새 버전이 생겨도 본문을 자동 교체하지 않고 새 버전과 변경 보기 진입을 안내한다. 비교·전환은 사용자가 선택한다. 기존 피드백은 원래 버전에 남고 최신 버전이 아님을 표시한다.

### D-86 미완료 업무의 목적 확장·유형 변경

같은 문제의 미완료 업무에서 목적이 추가되거나 유형이 바뀌면 같은 대화·Case의 의도/Profile을 개정한다. 이전 기준·결정·증거·참조와 새 항목의 대응을 보존하고 누적 소비·실패·repair를 초기화하지 않는다. 변경 내용을 간결히 알리며 명확한 요청은 기존 정책 안에서 처리한다. controlled·새 권한·미결 판단은 유지하고 목적 선언만으로 조사 Profile의 제품 쓰기 제한을 우회하지 않는다. 종료 후 실제 수정은 새 Case다. P4-03 혼합 목적 완료 계약과 별개로 이행·실행 경로는 추가 구현한다.

### D-87 완료 후 설명도 같은 Case의 사용량에 누적

기존 결과·근거에 대한 설명은 기존 대화에 남기고 같은 Case 예산·사용량에 합산한다. 종료 당시 소비와 이후 설명 소비를 구분하고 완료 결과·판정은 그대로 보존한다. 예산이 부족하면 한도 조정이 필요하며 무제한 기본값은 유지한다. 추가 실험·코드 수정은 새 연결 Case다. 닫힌 업무를 재개하는 대신 설명 전용 진입과 종료 기록을 보존하는 예산 변경 이력을 구현한다.

### D-88 시간 한도 선택 시 실행시간 합계를 기본으로

기본 예산은 계속 무제한이다. 시간 한도를 설정할 때는 논의·업무·재시도·종료 후 설명의 실제 실행시간 합계를 기본 지표로 사용한다. 사용자 생각·답변 대기는 제외하며 병렬 실행은 각각 합산한다(10분 두 실행은 20분). 대화 생성 후 경과시간은 별도 표시·선택 한도다. 기존 한도·소비의 의미를 소급 변경하지 않고 지원 능력 이상의 즉시 강제 종료를 약속하지 않는다.

### D-89 직접 파일 편집은 외부 편집기

앱에서는 문서·diff·버전·근거 열람과 참조 피드백을 제공하고 직접 파일 편집은 외부 편집기를 사용한다. 작업 PC 이름·작업공간 경로·경로 복사를 제공하며 폴더·편집기 열기는 지원을 확인한 환경에서 작업 PC를 대상으로 한다. 다음 AI 실행 전에 외부 변경을 확인·보존한다. 앱 내부 전체 파일 편집기는 후속 검토다.

### D-90 대화 기반과 기본 UI를 앞당기는 개발 순서

대화·요청 기반 → P4-04와 연결한 문맥·복구·실행 제어 → 새 UI의 기본 흐름 → 남은 P4 업무 기능·추가 UI → P5 GitHub·P6 운영 수용 순서로 진행한다. 중단의 실제 실행·잔류 활동 확인 경로는 버튼 연결 전에 검증한다. 필요한 검증 일부를 앞당겨도 전체 P6 수용을 생략하지 않는다. P4-03 기반을 재사용하고 새 작업별 plan을 작성하며 전체 UI를 P4-04에 넣지 않는다. 구체 작업 식별자·분할은 [개발 안내](DEVELOPMENT.md)의 실행 관리 사항이다.

## 개정 적용 경계

D-01~82 중 위 본문은 후속 결정의 현재 의미를 반영하며 변경 관계는 [검토 이력](review-status.md)에 남긴다. P1~P4-03의 완료 plan·증거와 기존 데이터의 정책·Profile 버전은 당시 사실로 보존한다. UI 개편은 미구현이며 정확한 모델·이행·작업별 검증은 D-90 순서에 따른 후속 plan에서 정한다. [개발 안내](DEVELOPMENT.md)에 다른 세션의 시작점과 기술 검토 사항을 기록한다. 이번 허용은 문서 반영과 인계 정리이며 코드 구현·커밋·push는 수행하지 않는다. 실제 CLI 제어·비용 강제·복구·성능은 버전/모드별 검증이 필요하다.
