# 품질 게이트 기준안 v0.6

작성일: 2026-09-20. 상태: D-01~58의 관련 결정을 통합한 설계 기준안. 제품 구현·CLI 호환성·운영 검증을 완료했다는 의미는 아니다. 의도 동의·선택한 사람 검토·품질 판정·최종 인수·외부 반영 권한을 구분한다.

확인된 정책: 규모는 간소·표준·심층 3단계. 기능 개발의 의도 품질 게이트는 필수이며 끌 수 없다. 나머지는 규모·위험에 따라 조절하고 사용자가 켜거나 끌 수 있다. 업무 Case마다 설정하고 필요하면 내부 Task별로 조정한다. 켜진 게이트에서 AI 의미 검토가 필요하면 작성과 별도 세션으로 수행하며 같은 CLI·모델을 사용할 수 있다. 최초 품질 실패 후 자동 수정·재검증은 기본 최대 2회이고 업무별 한도를 조정할 수 있다. 반복 실패 또는 사람 판단이 필요하면 요청하고 해결 전 후속 진행을 보류한다. 실행 중 설정 변경은 예약하고 현재 검증 1회가 끝나면 반영한다. 이후 자동 수정·재검증부터 새 설정을 사용한다.

## 권고 요지

게이트는 모든 활동 뒤의 평가 보고서가 아니라, **다음 작업이 의존하는 결과를 받아들여도 되는지 확인하는 조건**으로 둔다. 의도 초안, 설계, 계획, 구현 묶음, 통합 결과, 업무 종료, 연구 결론, 지식 채택에 8개 유형을 정의하되 해당 업무에 필요한 유형만 만든다. 모든 업무가 8개를 순서대로 통과하는 구조가 아니다.

가장 효과적인 위치는 사람이 기능 의도를 검토하기 직전, 구현에 들어가기 전, 구현 결과를 통합하기 전, 최종 완료를 주장하기 전이다. 연구 게이트는 결론을 채택할 때 적용한다. QG-08 지식 채택은 첫 버전 구현 범위 밖의 확장 설계이며 첫 버전은 결정·근거 보존만 지원한다. 작은 변경은 구현·통합 검사를 같은 실행에서 수행하고 검사 결과를 재사용할 수 있다.

사람의 의도 동의, 선택된 설계·계획의 사람 검토, 최종 인수와 외부 반영 승인은 품질 게이트와 별도다. 품질 검사를 통과해도 의도 동의나 실행 권한을 얻지 않는다. 기본은 최종 결과를 사람이 확인한 뒤 완료하며 Case별 자동 완료를 선택할 수 있다. 자동 완료도 미충족·미검증의 예외를 스스로 수용하지 않는다. 사람이 문서를 승인하거나 예외 종료를 수용해도 원래 검증 결과를 통과로 바꾸지 않는다.

초안의 열람·질문·피드백은 게이트 실패 중에도 항상 가능하다. 의도 초안 게이트는 검토 준비 상태와 후속 채택 조건을 평가하며 사람이 문제를 확인하고 답할 접점까지 막지 않는다. 필수 여부와 별개로 지적된 내용은 UI에 근거·현재 상태와 함께 표시한다.

## 1. 후보 게이트 8개

QG-01은 기능 개발의 필수 게이트다. QG-02~07은 조건에 따라 사용하는 선택 가능 게이트이며 아래 검사·통과·복구 설명은 해당 게이트가 켜져 있을 때의 동작이다. QG-08은 이후 지식 기능 확장 시 사용하는 후보로 첫 버전 실행·설정 대상이 아니다. 버그 수정·원인 분석·연구는 원래 요청의 목표·범위·종료조건이 명확하면 시작하고 중요한 미정만 질문하며 정리한 내용을 화면에 표시한다. 새 기능이나 중요한 제품 동작 변경이 필요할 때 해당 기능 개발에 의도 검토를 적용한다.

비용 수준은 확정 요금이나 자동 실행 정책이 아니다. L=저비용 규칙 검사·좁은 범위 검토, M=AI 검토·관련 테스트, H=통합 환경·반복 실험·전문 검토. 크기와 비용을 기계적으로 동일시하지 않는다. 표의 AI 의미 검토는 작성과 별도 세션에서 수행한다. 규칙·실제 테스트만으로 판정할 수 있는 항목에는 AI 실행을 의무적으로 추가하지 않는다.

| 게이트 | 언제·무엇을 입력하는가 | 검사 항목과 실행 방식 | 통과 조건 제안 | 실패·미확정 시 복구 | 비용·적용 제안 |
|---|---|---|---|---|---|
| QG-01 의도 초안 품질 | 의도 초안 작성·피드백 반영 후. 최신 원문 요청, 피드백, 6개 항목, 성공 기준 후보, 출처 | 규칙: 필수 항목과 출처·미정 표기의 존재, 최신 버전. AI: 요청 누락·모순, AI 가정의 확정 요구 둔갑, 확인 불가능한 성공 기준, 중요한 미정 질문 식별 | **사람에게 검토를 요청할 준비**가 됨. 미정 질문은 있어도 되며 질문을 숨기거나 임의로 답하지 않아야 함. 사용자 의도에 부합한다는 최종 판정은 사람의 명시적 동의로 별도 충족 | 서술 누락은 자동 보완. 제품 정책이나 범위의 미정은 질문으로 돌림. AI 검토 반복으로 질문을 지워서는 안 됨 | L~M. 모든 기능 개발에 적용하되 작은 기능은 짧게. 질문 응답마다 전체 게이트를 반복하지 않고 최신 검토본을 대상으로 수행 |
| QG-02 설계 적합성 | 필요한 설계 완료 후, 개발계획에 채택하기 전. 기능 개발의 동의된 의도 또는 해당 업무의 목표·범위·종료조건, 요구·제약, 설계, 영향 코드·인터페이스 | 규칙: 기준 목표 유효성, 요구별 설계 대응·미대응 표시. AI: 합의 범위 충족, 경계·실패 흐름, 데이터·권한·호환성 영향, 검증 가능성, 대안 선택 근거. 필요 시 허용받은 실험 결과 | 해당 설계 채택에 필요한 결함·모순·사람 결정이 해결되고 필수 요구의 실현 경로가 있음. 이후 단계로 미룬 질문은 단계·의존 작업을 명시. 문서 길이·평점은 통과 근거가 아님 | 기술 해법은 수정 후 영향 구간 재검토. 의도 변경이면 의도 확인으로 돌아가고, 위임하지 않은 제품 결정은 필요한 단계에서 사람에게 확인. 허용되지 않은 실험은 범위를 제안해 확인 | M, 위험 큰 부분만 H. 설계가 필요한 업무에 적용. 사람 검토 옵션은 별도 유지 |
| QG-03 개발계획 실행 가능성 | 계획 완료 후 구현 배정 전. 업무 유형에 맞는 목표·종료조건과 해당 의도·설계, 작업 그래프, 환경, 기준별 검증 계획 | 규칙: 작업·의존 관계 유효성, 순환·누락, 선행 조건, 검증 작업 존재. AI: 요구·설계 대비 누락, 복구·통합 순서, 불확실성 해소 시점, 검증 기대값의 적절성 | 필수 기준별 검증 방법을 연결하고 실행 가능한 작업을 구성. 이월한 사람 질문은 단계·의존 작업을 명시하고 그 작업 전 결정. 모든 질문을 먼저 해결할 필요는 없음 | 작업 추가·분해·순서 변경. 범위 변경은 사람에게 확인. 확인받은 계획의 의미가 바뀌면 관련 검토의 유효성 재판단 | L~M. 계획이 필요한 업무에 적용. 작은 계획도 필수 검증 생략의 근거는 아님 |
| QG-04 구현 묶음 품질 | 검토 가능한 변경 묶음 완료 후. 기준 코드와 diff, 관련 요구·계획, 테스트·빌드·정적검사 결과 | 규칙/실행: 언어·프로젝트에 맞는 빌드·타입·정적검사·단위/회귀 테스트. AI: 요구 대비 동작, 누락 경계·오류 처리, 테스트의 독립적 기대값, 의도 밖 변경·테스트 약화·민감정보 혼입 여부. 원인 수정이면 수정 전 실패와 수정 후 성공을 가능한 범위에서 비교 | 적용하기로 한 필수 검사가 유효한 코드·환경에서 통과하고 해당 범위의 중요 결함이 해소됨. CLI 정상 종료나 작성자의 완료 보고만으로는 불가 | 결함에 연결된 수정 작업 생성 후 관련 검사 재실행. 기존 결함·불안정 테스트·환경 장애·새 회귀를 구별. 필수 기준·테스트 삭제나 기대값 완화로 해결하지 않음 | L~M, 위험에 따라 H. 파일 저장·커밋마다 별도 게이트를 만들지 않고 의미 있는 묶음마다 적용 |
| QG-05 통합·행동 검증 | 변경을 실제 통합할 기준 상태가 준비된 후. 통합 코드, 대상 환경, 사용자 시나리오, API·데이터 계약, 기존 기준 결과 | 실행: 관련 통합/E2E·계약·권한·회귀 테스트. 조건부 성능·데이터 변환·취소/재시도 검증. AI는 누락 시나리오와 증거 적합성을 점검. 미확정 UX는 사람 평가가 필요할 수 있음 | 해당 업무에 합의된 필수 행동과 제약을 실제 통합 상태에서 충족. 하위 작업이 각각 통과해도 통합 통과로 자동 승계하지 않음 | 인터페이스·순서·데이터 문제를 관련 작업으로 되돌리고 영향 구간 재검증. 환경 문제면 실행 불가로 분리하고 환경 복구 작업으로 전환 | M~H. 통합 영향이 없거나 QG-04와 동일 상태·검사를 사용한 작은 업무는 증거 재사용. 별도 테스트 실행을 형식적으로 중복하지 않음 |
| QG-06 업무 종료 근거 | 결과 후보·완료 판정 전. 최신 기준, 게이트 결과, 전체 변경, 증거, 미검증·미충족 사항과 실행 상태 | 규칙: 기준 커버리지, 후보 버전, 미해결 항목. AI: 결론의 과장·요구 누락·실제 상태 불일치. 사람 인수·예외 결정은 별도 참조 | 정상 완료의 근거가 있고 필수 조건이 충족됨. 합의한 연구 종료조건을 충족한 판단 불가 결론도 정상 완료 가능. 예외 수용은 게이트 통과로 바꾸지 않음 | 누락 검증·결함 수정 또는 남은 범위·영향을 제시해 사람의 예외 종료 수용 요청. 원래 기준과 판정 보존. 예외는 추가 실행 권한이 아님 | L~M. 게이트 ON/OFF와 별개로 기본 최종 사람 확인, Case별 자동 완료 지원. 자동 모드의 예외도 사람 수용 필요 |
| QG-07 연구·원인 판단의 근거 | 조사·실험 결과를 결론 또는 후속 설계의 근거로 채택할 때. 질문, 가설, 출처, 원시 결과, 환경·방법 | 규칙: 출처·실험 조건·결과 보존. AI: 관찰/가설/결론 분리, 반례·대안·한계, 비교 조건, 주장과 증거의 범위 일치. 필요 시 독립 재현·대조 실험 | 합의한 질문·종료조건에 대응하는 근거·한계가 있음. ‘증거 부족’도 결론이 될 수 있으나 ‘재현 안 됨’을 ‘해결됨’으로 바꾸지 않음 | 추가 조사·실험, 결론 범위 축소, 판단 불가 표시. 범위·종료조건·기존 허용을 바꿔야 하면 확인. 총시간 한도가 없어도 긍정적 결론을 얻기 위한 무의미한 반복을 완료 근거로 삼지 않음 | L~H. 매 검색이 아니라 결론 채택 경계에 적용. 연구에 구현 QG-04/05를 강제하지 않음 |
| QG-08 지식 채택 — 이후 확장 | 지식 후보를 재사용 규칙으로 승격할 때. 후보·출처·적용 범위·예외·관련 규칙 | 규칙: 출처·범위·상태. AI: 과잉 일반화, 규칙 충돌, 폐기 조건, 검사 규칙으로 표현 가능성 | 근거·조건·충돌 처리를 갖춘 후보만 지정 범위에서 채택. 업무 완료와 독립 | 후보 유지, 범위 축소, 충돌 검토, 근거 확보. 과거 통과를 영구 유효로 취급하지 않음 | 첫 버전에는 실행하지 않음. 결정·근거·출처 보존만 제공. 자동 추출·채택 화면·자동 재사용은 이후 확장 |

## 2. 업무·개별 작업별 설정

사용자는 업무 Case의 품질 게이트를 설정하고 필요한 개별 Task에서 조정한다. 의도 품질 게이트의 필수 잠금은 두 수준 모두에서 유지한다. 내부 Task마다 의도 동의를 새로 받는다는 의미는 아니며, 같은 기능의 유효한 의도와 동의를 공유하되 우회 구현을 허용하지 않는다.

| 설정 항목 | 동작 |
|---|---|
| 의도 품질 게이트 | 필수 ON, 끄기 불가. 사람의 명시적 의도 동의와 별도 조건 |
| 기타 게이트의 추천 | AI가 규모·위험·변경 특성에 맞는 적용안과 근거 제시 |
| 업무 Case 설정 | 해당 업무에 사용할 게이트를 사용자가 ON/OFF |
| 개별 Task 설정 | 해당 Task의 게이트는 업무 설정을 따르거나 ON/OFF로 조정 |
| 게이트 OFF | 해당 게이트의 실행과 진행 차단을 사용하지 않음. 통과 판정으로 취급하지 않음 |
| 독립적으로 유지할 조건 | 기능 개발의 사람 의도 동의, 선택한 설계·계획 사람 검토, 필요한 산출물·사람 결정, 실행 권한, 합의한 성공 기준·검증 작업, 최종 인수 정책 |

설정의 우선순위는 **기능 의도 필수 잠금 → 해당 Task의 명시 설정 → 업무의 명시 설정 → 규모·위험에 따른 기본 제안**으로 해석하는 안을 둔다. Task별 설정은 그 Task의 게이트에만 적용하며 다른 Task나 Case 전체의 통합·종료 게이트를 바꾸지 않는다. AI는 사용자 설정을 조용히 변경하지 않고 새 위험의 추천과 현재 설정 차이를 보여준다. 초기 추천을 실제 설정으로 만드는 시점과 화면 표현은 이 원칙 안에서 구현·검증한다.

OFF는 별도 AI 검토만을 끄는 것으로 제한하지 않는다. 해당 게이트만을 위해 수행하는 도구 검사도 그 게이트와 함께 생략할 수 있다. 반면 합의한 성공 기준을 확인하기 위해 이미 계획한 테스트는 별도 검증 작업이므로 게이트 토글만으로 삭제하지 않는다. 예를 들어 QG-04를 꺼도 합의된 ‘필터에 맞는 전체 결과를 내보낸다’는 기준과 검증이 자동 면제되지는 않는다. 검증 자체의 면제·기준 완화는 기존 FR-06의 중요한 변경 절차로 구분한다.

게이트를 끌 때 UI에는 `사용자 설정으로 꺼짐`을 표시하고 기존 실패·증거·설정 변경 주체와 버전을 보존하는 안을 제안한다. 업무상 해당하지 않는 `미적용`이나 검사를 완료한 `통과`와 구별한다. 다시 켤 때는 유효한 증거를 재사용할 수 있다. 실행 중 변경은 예약하고 해당 작업이 끝난 뒤 반영한다. 현재 설정·예약 설정·실제 반영 시각을 분리하며 설정 변경만으로 현재 검사를 중단하지 않는다. 예약 정책만 바뀌었다는 이유로 진행 중 검증을 무효화하지 않고, 그 결과를 원래 입력·정책에 귀속시킨 뒤 다음 배정 전에 새 설정을 적용한다. 다른 게이트나 합의한 성공 기준의 실패는 설정 변경으로 지워지지 않는다.

## 3. 점수 대신 근거와 판정의 분리

게이트의 적용 여부와 그 안의 개별 기준의 필수성은 분리한다. 예를 들어 켜진 설계 게이트에서도 합의한 권한 요구 위반과 표현 개선 권고는 진행에 주는 영향이 다르다. 각 발견은 연결된 기준·필수/권고 구분·차단 효과를 기록한다. AI의 막연한 의견이 새 필수 요구를 만들지는 않는다. 중요한 위반 가능성이 있으나 근거가 부족하면 확인 작업이나 사람 판단을 요청한다. 구체적인 검사 항목·필수/권고 분류는 업무에 맞춰 제안한다.

QG-03에서 질문을 분리해도 기능 의도 동의 전 구현할 수 있다는 뜻은 아니다. 의도 단계에 필요한 사람 결정은 그 단계에서 받고, 설계·계획으로 미루는 것이 적절한 질문은 미룰 단계·의존 관계를 표시한다. 해당 질문에 의존하는 작업 전에는 사람 결정을 받는다. 설계·계획의 자동 진행이나 게이트 OFF가 그 결정을 대신하지 않는다. QG-06은 유효한 QG-04/05/07 결과를 재사용하며 연구 종료조건과 결과 보고의 일치를 확인한다.

`quality score >= 80`처럼 종합 점수로 통과를 정하면 중요한 결함이 사소한 항목 점수로 상쇄될 수 있다. 검사 단위를 기준·산출물·증거에 연결하고 다음 정보를 반환하는 형태를 권한다.

- 판정: 통과 / 실패 / 판단 보류 / 실행 불가 / 변경으로 재검토 필요 / 미적용.
- 개별 발견 사항: 대상, 관찰·근거, 영향, 어떤 기준을 위반하는지, 확실성, 제안 수정, 재검증 방법.
- 검사 실행 정보: 규칙·도구·검토자, 입력과 환경 버전, 시작·종료, 적용 범위, 실제 실행 여부.
- 후속 효과: 어떤 작업을 계속할 수 있는지, 무엇을 보류해야 하는지, 어떤 질문 또는 복구 작업이 필요한지.

AI의 ‘문제가 있을 수도 있다’는 의견과 증거가 있는 필수 기준 위반을 구분한다. 중요 여부가 모호한 발견은 통과로 감추거나 확정 실패로 과장하지 않고 판단 보류 상태에서 좁은 조사·검증 작업으로 전환한다. 사용자에게 갈 때는 현재 근거와 결정이 필요한 선택을 제시한다.

사람이 남은 문제·미검증 범위와 영향을 확인하면 미충족·미검증 모두에 대해 예외 수용 종료를 할 수 있다. `FinalAcceptance`와 `ExceptionDecision`을 후보 버전에 각각 연결하고 원래 실패·미검증 상태를 보존한다. 한 화면에서 함께 결정할 수 있지만 기록은 분리한다. 예외 종료를 요구 변경이나 추가 구현·실험·외부 쓰기 권한으로 확대하지 않는다. 완료·예외 종료 후 수정은 연결된 새 Case로 처리하며 이전 의도 동의를 새 범위에 묵시적으로 적용하지 않는다.

## 4. 게이트 실행 방식의 비용·효과 비교

다음 네 방식은 서로 완전히 대체되는 선택지가 아니다. 문서·설계의 의미 검토에는 AI 판단이 도움이 되고, 실제 기능 동작은 실행 증거가 필요하며, 사람의 의도는 사람이 확인한다. 비용은 실제 CLI·모델·검사 환경에 따라 달라지므로 상대적으로만 표현했다.

| 방식 | 동작 | 상대 비용·시간 | 잘 확인하는 것 | 놓치기 쉬운 것 / 한계 |
|---|---|---|---|---|
| A. 작성 프롬프트 안의 점검 지시 | 작성 중 ‘6개 항목과 기준을 확인하라’는 체크리스트를 주고 점검 결과도 출력 | 추가 지연이 가장 작을 수 있음. 응답 길이·추론 증가 비용은 있음 | 빠진 항목, 명백한 서술 불일치, 작성 습관 개선 | 작성자가 자기 해석을 그대로 정당화할 수 있음. 검사를 실제 수행했다는 증거가 되지 않음 |
| B. 같은 작성 세션에서 재검토 | 초안·구현 뒤 별도 검토 차례를 실행하되 기존 대화와 판단을 유지 | A보다 보통 추가 실행·토큰이 필요. 맥락 재구성 비용은 상대적으로 낮음 | 이미 조사한 사실을 활용한 누락·모순·구현 실수 점검 | 앞선 판단에 끌릴 수 있고 컨텍스트가 이미 커졌을 수 있음. 자신이 놓친 가정을 다시 놓칠 수 있음 |
| C. 별도 세션에서 검토 | 새 세션에 합의 의도·기준·대상 산출물·원시 증거를 제공해 검토 | 검토 실행과 필요한 정보 재구성 비용이 추가. 큰 문서·코드는 더 비쌈 | 작성 과정의 결론에 덜 묶인 요구 대조, 반례 제안, 테스트 기대값 검토 | 맥락을 덜 줘서 생기는 오탐·누락. 같은 모델이면 오판 상관성이 남음. 다른 모델이어도 정확성을 보장하지 않음 |
| D. 규칙·실제 도구로 검증 | Python 등으로 항목·버전을 검사하고 빌드·테스트·측정·재현을 실행 | 필드·버전 검사는 낮음. 통합 환경·부하 실험은 높음. 모델 토큰 외 CPU·환경 준비 비용 발생 | 기계적인 조건, 실제 실행 결과, 수치·계약·회귀 여부 | 잘못된 테스트 기대값, 누락된 시나리오, 사용자의 진짜 목적은 자체적으로 판정 못 함 |

프롬프트에 ‘필수 항목을 검사하라’고 쓰는 A와 실제 프로그램이 누락·버전을 검사하는 D는 다르다. A/B/C는 의미 검토를 보조하며 D는 실행한 조건에 대한 근거를 제공한다. 어떤 방식도 의도 초안을 사람에게 보여주고 명시적 동의를 받는 절차를 대신하지 않는다.

기능 의도 동의 전에는 기존에 허용된 읽기·분석 범위를 지킨다. 이때 D는 문서 필드·버전 등 시스템 내부 검사일 수 있으며 프로젝트 실험·코드 수정을 자동 허용하지 않는다. 버그 수정·분석·연구에는 기능 의도 동의를 일괄 요구하지 않는다. 분석·연구의 실험 범위가 불명확하면 읽기 조사 후 재현·계측·임시 실험 범위를 제안해 한 번 확인받는다. 이미 구체적으로 요청·허용한 활동은 반복 확인하지 않는다.

### 예시 1: 의도 초안에 AI가 임의로 넣은 결정

요청은 ‘관리 화면 데이터를 CSV로 내보내기’다. AI가 6개 항목을 모두 작성하면서 ‘현재 페이지 50행만, 관리자만 사용’이라고 사용자 확정 요구처럼 적었다고 하자.

- A: 항목이 모두 있으므로 통과했다고 보고할 수 있다.
- B: 원문과 초안을 다시 비교하면 근거 없는 결정을 찾을 수 있지만 자기 가정을 유지할 수도 있다.
- C: 원문·프로젝트 규칙·확인된 답변을 별도로 제공하면 ‘행 범위와 권한 정책은 미정’이라는 발견을 기대할 수 있다. 검토 AI도 틀릴 수 있다.
- D: 항목과 출처 링크의 존재는 검사할 수 있다. 출처가 그 결정을 뒷받침하는지, 사용자가 무엇을 원했는지는 형식 검사만으로 해결되지 않는다.
- 필요한 결과: 초안의 해당 내용을 제안/질문으로 정정하고 사람이 선택한 최신 의도에 동의한다.

이 예시는 문서의 빈칸을 채운 사실과 사람의 의도를 확인한 사실을 구분해야 함을 보여준다.

### 예시 2: 필터 전체 내보내기의 잘못된 구현

사람이 ‘현재 필터에 맞는 전체 결과’를 확인했다. 구현은 현재 페이지 50행만 내보내고, 작성한 테스트도 50행을 정답으로 삼았다.

- A/B: 코드와 테스트가 서로 일치하므로 결함을 놓칠 수 있다.
- C: 합의된 기준을 기준으로 코드를 읽으면 페이지 제한과 잘못된 테스트 기대값을 찾아낼 수 있다.
- D: 기존 50행 테스트 통과는 충분하지 않다. 필터 일치 120행과 불일치 30행을 둔 독립 검증 사례에서 정확히 120행인지 실행하면 결함을 재현할 수 있다. 합의된 행 범위에 따라 기대값을 만든 것이 핵심이다.
- 필요한 결과: 구현과 잘못된 테스트를 수정하고 합의 기준에서 도출한 사례를 다시 실행한다. 필수 기준을 ‘50행 내보내기’로 낮추지 않는다.

### 예시 3: 계획은 완성됐지만 검증이 빠진 경우

설계에는 관리자 권한 정책이 있는데 계획은 버튼·API·CSV 생성만 포함한다. 개발계획 게이트는 ‘권한 구현·허용/비허용 사용자 검증 작업이 없다’는 누락을 찾아야 한다. D는 기준과 작업 연결의 빈칸을 잡을 수 있고, AI 검토는 단순 연결만 있고 실제 권한 검사가 없는 의미적 누락을 찾는 데 쓰인다. 작은 계획이라도 이런 검사는 가치가 있다.

### 확정한 조합

| 활동 | 선택한 방식 | 비용·효과를 조절할 부분 |
|---|---|---|
| 작성 중 자체 점검 | A/B를 보조적으로 사용 가능 | 작성 부담과 명백한 누락 감소. 게이트의 별도 검토를 대신하지 않음 |
| 켜진 게이트의 AI 의미 검토 | C, 작성과 별도 세션 | 관련 입력·검토 범위를 조절하고 프로젝트 기본 CLI·모델 및 업무·역할별 설정 사용 |
| 규칙·실제 실행 검증 | 필요한 D 수행, 유효한 증거 재사용 | 단계 이름만 바꾼 중복 실행을 줄이고 실제 성공 기준 검증 유지 |

선택 결과는 **켜진 게이트에서 AI 의미 검토가 필요하면 C(별도 세션)로 수행하고 필요한 D(규칙·실제 실행)를 조합**하는 방식이다. A/B의 작성자 자체 점검을 추가할 수 있지만 게이트의 별도 의미 검토를 대신하지 않는다. 게이트 자체는 의도 외에는 규모·위험 및 사용자 ON/OFF 설정에 따른다. D는 필요한 검사만을 뜻하고 모든 단계에서 전체 테스트를 실행한다는 의미가 아니다. 검토 실행 수·입력 크기·소요시간·수정 횟수와 실제 발견한 결함을 대표 업무에서 측정해 비용·효과를 검증한다.

간소 업무에서 보안·권한 변경이 생기면 깊은 검증이 필요할 수 있으므로 ‘간소=약한 검증’으로 고정하지 않는다. 같은 CLI·모델 사용은 허용하며 다른 공급자 사용을 필수로 하지 않는다.

별도 검토에는 업무 유형에 맞는 목표·종료조건과 해당 의도·제약·기준·산출물·원시 증거를 주고 작성자의 ‘모두 완료’ 결론만 전달하지 않는다. 정책상 읽기·검토 역할이 발견을 반환하고 별도 작성 작업이 반영한다. 실제 검증 명령은 현재 허용 범위의 Runner 작업으로 실행한다. 입력이 크면 관련 자료를 조회·분할하며 읽지 못한 범위를 숨기지 않는다. 이 역할 분리가 완전한 OS 격리를 뜻하지 않는다.

이 분리는 같은 모델의 상관된 오판을 없애지 않는다. 다른 CLI·모델 사용은 선택 수단이고 무조건 더 정확함을 보장하지 않는다. 핵심은 **사용자 합의 기준, 실제 실행·측정, 작성 코드에서 독립적으로 도출한 기대 동작, 원문 근거**를 조합하는 데 있다. 복수 AI의 찬성표만으로 사람의 의도 동의를 대체하지 않는다.

프로젝트 기본 CLI·모델을 사용하고 업무·역할별로 변경할 수 있다. 장애 시 다른 CLI로 변경하기 전에는 사람에게 확인한다. 의미 검토는 계속 작성과 별도 세션이며 다른 제품을 강제하지 않는다. 지적에 이견이 있으면 작성 측이 기준·코드·실행 증거로 반론을 남기고 별도 검토가 재판정하는 절차를 제안한다. 근거 보완 후에도 판정이 불일치하거나 제품 판단이 필요하면 사람에게 요청한다. 검토자가 동의할 때까지 같은 논쟁을 무한 반복하거나 작성자가 지적을 자체 삭제하지 않는다.

## 5. 실패 복구와 자동 반복

확인된 흐름은 `필수 검사 불충족 → 의존 작업 보류 → 허용 범위의 AI 수정·재검증 → 유효 조건 충족 후 진행`이다. 반복 실패 또는 사람 판단이 필요하면 요청한다. 범위·기준 변경과 권한 확대는 자동 수정 범위 밖이다. 독립 작업은 의존 관계·기존 권한을 따로 평가한다. 사람의 예외 종료는 현재 결과를 알고 업무를 끝내는 경로이며, 실패한 결과를 임의의 다음 실행에 채택할 권한은 만들지 않는다.

최초 품질 실패 후 자동 수정·재검증은 기본 최대 2회이며 업무별로 한도를 조정할 수 있다. 최초 실패를 찾은 검사는 수정 1회에 포함하지 않는다. 같은 결과를 고치는 수정 주기의 누적 한도는 새 세션·재시작·작업 재분할로 초기화하지 않는 안을 제안한다. 수정 도중 새 발견이 생겨도 같은 주기의 한도를 유지한다. 한도 이후에는 시도·근거·남은 선택을 제시하고 사람에게 요청한다. 제품 판단·범위 또는 기준 변경·추가 권한이 필요하면 한도를 기다리지 않고 질문한다.

환경 장애 재시도와 제품 결함 수정 횟수는 구분한다. 이미 수행한 수정은 기록하고 그 뒤 환경 문제로 못 마친 검증만 다시 실행하는 것은 새 코드 수정과 별개로 추적한다. 환경 복구의 구체 재시도·도구 응답 한도는 기술 제안과 검증 계획으로 정하며 전체 자동 실행 총시간 한도와 혼동하지 않는다. 비용 정보 미제공은 비용 0으로 간주하지 않는다.

차단은 해당 조건을 의존하는 작업에 적용한다. 승인 대기인 사용자 정책과 무관한 읽기 조사 또는 통과한 다른 작업까지 무조건 중지하지 않는다. 다만 작업이 독립적이라고 AI가 말한 것만으로 코드·계약·공유 자원 충돌 검사를 생략하지 않는다.

## 6. 회귀, 불안정 테스트와 환경 실패

- 변경 전 기준 결과를 가능한 범위에서 확보하고 코드·환경 버전과 함께 기록한다. 기존 실패라는 이유만으로 자동 허용하지 않으며, 해당 실패가 성공 기준·변경 영역에 영향을 주는지 판단한다.
- 새 실패가 생겨도 곧바로 새 코드 결함으로 확정하지 않는다. 같은 환경의 기준 코드 비교, 실패 로그, 환경 차이를 사용해 원인을 좁힌다.
- 불안정 테스트는 여러 번 돌려 한 번 통과한 결과만 남기지 않는다. 시도 전체, 통과·실패 조건과 분류 근거를 보존한다. 재실행 횟수와 분류 규칙은 프로젝트의 검증 기준에 맞춰 구현·확인한다.
- 서비스 미실행, 자격증명 만료, 네트워크 실패, 도구 미설치는 `실행 불가`로 분리한다. 업무 성공의 증거가 되지 않으며 환경 복구 후 검증한다.
- 검사기 경고가 통과·실패를 직접 의미하는지 프로젝트 정책으로 정한다. 린트 경고·코드 커버리지 수치를 모든 프로젝트에 동일하게 강제하지 않는다.
- 합의된 테스트를 삭제하거나 예상 결과를 변경한 diff는 성공 기준 변경인지 검토한다. 테스트 유지보수 자체를 금지하는 것이 아니라 실패 은폐를 방지하는 것이다.

## 7. 문서와 운영 모델 연결

`GateDefinition`에는 목적·적용 조건·입력 계약·검사·필수 여부·실행 방식·실패 시 의존 작업 효과를 둔다. `GateRun`에는 실제 입력 버전과 검사 결과·증거·발견·재검증 연결을 둔다. `Approval`은 별도 기록으로 유지한다. `ExceptionDecision`도 원래 검사 상태를 덮어쓰지 않는다.

`GatePolicy / GateOverride`는 규모·위험 추천과 Case/Task별 설정을 보존하는 모델 제안이다. 설정 주체·대상·시점·정책 버전을 기록하고 실제 판정과 분리한다. 설계·계획의 사람 검토를 정하는 `ReviewPolicy`와도 구분한다.

스케줄러는 의도 동의/선택된 사람 검토/권한 같은 기존 조건과 필요한 게이트 결과를 함께 검사한다. 게이트 실행 중 요구·코드가 변경되면 해당 결과가 어떤 상태에 대한 것인지 검사하여 오래된 통과 결과로 진행하지 않는다. 영향 없는 결과는 입력·의존 관계가 동일하다는 근거를 통해 재사용한다. 산출물 해시, 커밋, 실행 환경·도구 버전은 증거 유효성을 판단하는 자료다.

서버+개인 PC Runner 구성에서 코드·diff·상세 로그뿐 아니라 상세 의도·설계·계획·대화·검토 원문도 PC에 보관한다. 서버는 상태·요약·원문 ID/버전/해시 참조를 보존하고 상세 원문은 영구 저장 없이 일시중계한다. 원문이 필요한 검토는 해당 Runner에서 실행하고 PC 연결 시 상세를 열람한다. 요약만으로 원문 검토 완료를 만들지 않는다. 외부 AI 전송은 기존 CLI의 사용자 설정을 따르며 Runner 실행이 오프라인 처리를 뜻하지 않는다.

GitHub에는 매 Case의 새 기록용 이슈에 주요 게이트·산출물의 요약과 참조만 append한다. 코드·diff·상세 로그를 게시하지 않으며 게시 전 내용 점검의 완벽성을 주장하지 않는다. `통과`, `사람 확인 대기`, `검증 실패`, `예외 수용`을 분리한다. 게시 장애에도 내부 진행은 계속하고 대기·재시도·누적 실패 알림을 관리한다. PR 리뷰만 외부 피드백으로 수집하며 이슈 댓글을 승인·명령으로 해석하지 않는다. 코드 push·PR 생성·수정에는 준비된 대상·내용의 별도 사람 확인이 필요하다.

기존 FR-06/09/10/12/17/20/29의 내용을 확장하면 되며, 별도 기능 요구로 추가한다면 `업무별 품질 게이트 구성·판정·수정·재검증`이 핵심이다. 과도한 요구 ID 증설보다 기능 구현 진입과 작업 결과 채택·종료 경계에 공통 게이트 계약을 붙이는 편이 일관적이다.

## 8. 과잉 절차를 피하기 위한 경계

넣지 않을 것을 검토할 대상: 모든 대화 답변의 승인, 매 검색의 출처 보고서 승인, 파일마다 별도 AI 리뷰, 모든 업무의 성능·보안·부하 테스트, 같은 코드·환경의 테스트를 단계 이름만 바꿔 반복, 모든 문서에 정해진 분량·다이어그램·대안 개수 강제, 한 업무의 모든 미정 질문 해결 전 아무 활동도 금지.

소규모라도 권한·돈·데이터 변경은 깊은 검증이 필요할 수 있다. 반대로 대량의 기계적인 수정은 반복 가능한 검사와 표본의 독립적 확인이 더 효율적일 수 있다. 따라서 **문서 상세도, 사람 검토 여부, 기술 검증 범위를 별도 축으로 관리**하는 것을 권한다.

## 9. 남은 구현·검증 사항

1. 규모·위험별 초기 추천의 세부 기준, 이견 처리와 검토 범위·증거 재사용 판단의 정확도.
2. 환경 재시도·개별 도구의 장시간 미응답 처리·게시 실패 알림의 구체 수치는 구현 제안과 검증 계획으로 정한다. **기본 자동 실행 총시간 한도는 없으며**, 합의 범위·필수 결정·품질 수정 횟수 안에서 계속하고 사용량·경과 시간을 표시한다. 시간 무제한이 무한 결함 수정이나 환경 장애 반복을 뜻하지 않는다.
3. Windows native의 CLI 버전별 권한·도구 경계·재개 능력 실증. 첫 버전은 신뢰하는 개인 프로젝트용이며 CLI 설정·시스템 승인으로 통제하고 완전한 OS 격리는 보장하지 않는다.
4. 원문 일시중계가 서버 로그·캐시·추적 데이터에 상세 내용을 영구 보관하지 않는지 검증. 각 호스트 지정 경로에 하루 1회 자동 백업하고 최근 7개 백업을 유지한다. 백업 순환을 원본·승인·증거 자동 삭제로 해석하지 않으며 PC 원문을 서버 백업으로 복제하지 않는다.
5. 첫 버전은 한 사용자용이다. 다중 계정·공동 편집·역할 관리 UI와 지식 후보 자동 추출·채택·자동 재사용은 이후 확장이다.

실행 중 게이트 설정 변경은 **현재 검증 1회 종료 후 적용**하며 이후 자동 수정·재검증은 새 설정을 사용한다. Runner 연결 단절에 따른 **현재 도구 호출 종료 후 안전 정지**와는 별개의 경계다. 어느 기능도 실제 호환성을 검증하기 전 지원 완료로 표시하지 않는다.

원격 열람·검토는 LAN 또는 사용자 VPN에서 소유자 인증과 암호화 연결을 사용한다. 인터넷 직접 공개는 이후 범위다. 첫 버전 기술은 Python/FastAPI·SQLite와 React/TypeScript이며 일반 사용자 프로세스로 제공한다. 초기 시험 규모인 프로젝트 10개·Runner 3개·동시 AI 2개와 같은 Runner 쓰기 기본 1개는 등록 상한이나 성능 측정 결과가 아니다.
