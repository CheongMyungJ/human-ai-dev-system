# 품질 게이트 기준안 v0.7

작성일: 2026-09-21. 상태: D-01~67의 관련 결정을 통합한 설계 기준안. 제품 구현·CLI 호환성·운영 검증 완료를 의미하지 않는다. 요청에 따른 위임·선택한 사람 검토·품질 판정·결과 인수·외부 반영 권한을 구분한다.

확인된 정책: WorkDepth는 간소·표준·심층이며 기본 ask-on-decision과 선택 controlled, 전체 예산과 독립이다. 요청 정합성 확인은 항상 수행하고 기능 개발의 QG-01은 끌 수 없다. 명확한 저위험 요청에는 가벼운 확인을 적용하며 독립 AI 의미 검토는 위험·불확실성·정책에 따라 선택한다. 선택된 독립 검토는 작성과 별도 세션에서 수행하고 같은 CLI·모델도 가능하다. 나머지 게이트는 Profile·규모·위험과 Case/Task의 유효 정책으로 적용한다. 최초 품질 실패 후 repair는 기본 최대 2회이며 Case별 조정한다. 전체 예산은 기본 무제한이고 설정 한도는 별도로 강제한다. 게이트 설정 변경은 현재 검증 1회가 끝난 뒤 적용한다. 공통 계약은 [자동진행·위임·예산 정책](autonomy-budget-policy.md)을 따른다.

## 권고 요지

게이트는 모든 활동 뒤의 평가 보고서가 아니라, **다음 작업이 의존하는 결과를 받아들여도 되는지 확인하는 조건**으로 둔다. 의도 초안, 설계, 계획, 구현 묶음, 통합 결과, 업무 종료, 연구 결론, 지식 채택에 8개 유형을 정의하되 해당 업무에 필요한 유형만 만든다. 모든 업무가 8개를 순서대로 통과하는 구조가 아니다.

효과적인 위치는 요청 해석을 채택할 때, 구현에 들어갈 때, 변경을 통합할 때, 완료를 주장할 때다. 연구 게이트는 결론 채택에 적용한다. QG-08은 최소 지식 채택 계약과 연결하되 매 Case에 별도 AI 검토를 강제하지 않고 위험·충돌·영향에 따라 적용한다. 작은 변경은 별도 설계·계획 파일이나 실행 없이 최소 논리 기록을 남기고 필요한 검사를 합치거나 유효 증거를 재사용할 수 있다.

요청의 실행 위임, controlled 확인과 선택된 사람 검토, 결과 인수, 외부 반영 허용은 품질 게이트와 별도다. 품질 검사를 통과해도 새 권한을 얻지 않는다. 기본 ask-on-decision은 성공 기준·요청한 완료 지점 충족 시 자동 완료하며 controlled는 변경 결과 확인 뒤 허용된 push·PR과 필요한 외부 CI를 거쳐 완료한다. 어느 모드에서도 AI가 미충족·미검증을 예외 수용하지 않는다. 사람 확인이나 예외 수용이 원래 검증 결과를 통과로 바꾸지는 않는다.

초안의 열람·질문·피드백은 게이트 실패 중에도 항상 가능하다. 의도 초안 게이트는 검토 준비 상태와 후속 채택 조건을 평가하며 사람이 문제를 확인하고 답할 접점까지 막지 않는다. 필수 여부와 별개로 지적된 내용은 UI에 근거·현재 상태와 함께 표시한다.

## 1. 후보 게이트 8개

QG-01은 기능 개발의 필수 요청 정합성 게이트다. 다른 업무도 [Case Profile](case-profiles.md)의 목표·범위·완료 의미에 맞게 요청 정합성을 확인하며 기능 개발의 고정 문서·질문지를 강제하지 않는다. QG-02~07은 해당 업무에 필요한 유형을 선택한다. QG-08은 [프로젝트 지식](project-knowledge.md)의 채택 조건을 다루며 업무 완료와 분리한다. 목표가 명확하면 자동 진행하고 새 기능·중요한 제품 동작으로 목적이 바뀌면 해당 material delta를 확인한다.

비용 수준은 확정 요금이나 자동 실행 정책이 아니다. L=저비용 규칙 검사·좁은 범위 검토, M=AI 검토·관련 테스트, H=통합 환경·반복 실험·전문 검토. 크기와 비용을 기계적으로 동일시하지 않는다. 아래 표의 AI 검사는 필요할 때 선택하는 방식이며 모든 게이트에 AI 호출을 추가한다는 뜻이 아니다. 독립 의미 검토가 선택되면 별도 세션에서 수행한다. 실제 성공 기준에 필요한 검증은 비용을 이유로 삭제하지 않는다.

| 게이트 | 언제·무엇을 입력하는가 | 검사 항목과 실행 방식 | 통과 조건 제안 | 실패·미확정 시 복구 | 비용·적용 제안 |
|---|---|---|---|---|---|
| QG-01 요청·의도 정합성 | 최초 요청·후속 명시 결정·정책, 정규화한 목표·범위·기준, 출처·누적 변경 | 항상: 요청 대응·누락·가정·미정·범위·버전 확인. 명확한 저위험 요청은 가볍게 확인. 위험·불확실성·정책에 따라 독립 AI 의미 검토 선택 | 원래 위임과 정리한 의미가 부합하고 중요한 미정·변경이 드러남. 미정이 있는 기록의 검토 가능 상태와 그 질문에 의존하는 구현 가능 상태는 구분. 명확한 요청에 의례적 초안 승인 불필요 | 서술 누락은 자동 보완. 실제 제품 선택은 해당 의존 작업 전에 질문. AI가 질문을 지우거나 최신 자기 초안을 위임 근거로 삼지 않음 | L~M. 정합성 확인은 필수이고 독립 AI 검토는 조건부. Fast Lane 탈락은 일반 자동 진행으로 전환하며 사람 확인을 자동 추가하지 않음 |
| QG-02 설계 적합성 | 필요한 설계의 채택 전. 유효 요청·위임·기준, 해당 설계 기록, 영향 코드·인터페이스 | 규칙: 목표·출처 유효성, 요구별 대응·미대응. 필요 시 독립 AI: 범위 충족, 경계·실패 흐름, 데이터·권한·호환성, 검증 가능성·대안 근거. 허용된 실험 증거 | 필요한 결함·모순·미위임 결정이 해소되고 필수 요구의 실현 경로가 있음. 이월 질문은 시점·의존 작업 명시. 문서 길이·평점은 근거가 아님 | 기술 해법 수정과 영향 구간 재검토. material delta는 변경된 선택만 확인. 기존 권한을 넘는 실험은 허용 범위 확인 | M, 위험 큰 부분만 H. 별도 설계 파일·Run을 강제하지 않고 논리 기록으로 충족 가능. 선택한 사람 검토는 별도 |
| QG-03 개발계획 실행 가능성 | 실행 배정 전 필요한 작업 구성. 목표·종료조건·해당 설계, 작업 그래프·환경·검증 계획 | 규칙: 의존 관계·순환·누락·선행 조건·검증 연결·허용 범위·예산. 필요 시 독립 AI: 요구 누락·복구·통합 순서·불확실성 해소 시점·기대값 적절성 | 필수 기준에 검증 방법이 연결되고 실행 가능한 작업을 구성. 이월 질문은 의존 작업 전 해결. 모든 질문을 먼저 해결할 필요는 없음 | 작업 추가·분해·순서 조정은 위임 안에서 자동 수행. 범위·기준·권한이 바뀌면 해당 변경 확인 | L~M. 별도 계획 파일·Run을 강제하지 않음. 짧은 계획도 필수 검증 생략의 근거는 아님 |
| QG-04 구현 묶음 품질 | 검토 가능한 변경 묶음 완료 후. 기준 코드와 diff, 관련 요구·계획, 테스트·빌드·정적검사 결과 | 규칙/실행: 언어·프로젝트에 맞는 빌드·타입·정적검사·단위/회귀 테스트. AI: 요구 대비 동작, 누락 경계·오류 처리, 테스트의 독립적 기대값, 의도 밖 변경·테스트 약화·민감정보 혼입 여부. 원인 수정이면 수정 전 실패와 수정 후 성공을 가능한 범위에서 비교 | 적용하기로 한 필수 검사가 유효한 코드·환경에서 통과하고 해당 범위의 중요 결함이 해소됨. CLI 정상 종료나 작성자의 완료 보고만으로는 불가 | 결함에 연결된 수정 작업 생성 후 관련 검사 재실행. 기존 결함·불안정 테스트·환경 장애·새 회귀를 구별. 필수 기준·테스트 삭제나 기대값 완화로 해결하지 않음 | L~M, 위험에 따라 H. 파일 저장·커밋마다 별도 게이트를 만들지 않고 의미 있는 묶음마다 적용 |
| QG-05 통합·행동 검증 | 변경을 실제 통합할 기준 상태가 준비된 후. 통합 코드, 대상 환경, 사용자 시나리오, API·데이터 계약, 기존 기준 결과 | 실행: 관련 통합/E2E·계약·권한·회귀 테스트. 조건부 성능·데이터 변환·취소/재시도 검증. AI는 누락 시나리오와 증거 적합성을 점검. 미확정 UX는 사람 평가가 필요할 수 있음 | 해당 업무에 합의된 필수 행동과 제약을 실제 통합 상태에서 충족. 하위 작업이 각각 통과해도 통합 통과로 자동 승계하지 않음 | 인터페이스·순서·데이터 문제를 관련 작업으로 되돌리고 영향 구간 재검증. 환경 문제면 실행 불가로 분리하고 환경 복구 작업으로 전환 | M~H. 통합 영향이 없거나 QG-04와 동일 상태·검사를 사용한 작은 업무는 증거 재사용. 별도 테스트 실행을 형식적으로 중복하지 않음 |
| QG-06 업무 종료 근거 | 결과 후보 준비와 최종 종료 판정. 현재 기준·완료 지점·변경·증거·게이트·미충족·실행 상태 | 규칙: 기준 커버리지·후보 버전·미해결·외부 조건. 필요 시 독립 AI: 과장·요구 누락·상태 불일치. controlled 인수·예외는 별도 | 후보 준비와 최종 완료를 구분. 게시 후 CI는 후보 확인 시 예정 조건이며 실제 충족 전 종료하지 않음. 합의한 연구 종료조건을 충족한 판단 불가도 정상 완료 가능 | 누락 검증·결함 수정 또는 남은 범위·영향을 제시해 사람의 예외 수용 요청. 원 판정 보존. 예외는 추가 실행 권한이 아님 | L~M. 기본 자동 완료, controlled 결과 확인 뒤 게시·필요 CI·완료. 기존 증거 재사용, 이름만 바꾼 중복 리뷰 금지 |
| QG-07 연구·원인 판단의 근거 | 조사·실험 결과를 결론 또는 후속 설계의 근거로 채택할 때. 질문, 가설, 출처, 원시 결과, 환경·방법 | 규칙: 출처·실험 조건·결과 보존. AI: 관찰/가설/결론 분리, 반례·대안·한계, 비교 조건, 주장과 증거의 범위 일치. 필요 시 독립 재현·대조 실험 | 합의한 질문·종료조건에 대응하는 근거·한계가 있음. ‘증거 부족’도 결론이 될 수 있으나 ‘재현 안 됨’을 ‘해결됨’으로 바꾸지 않음 | 추가 조사·실험, 결론 범위 축소, 판단 불가 표시. 범위·종료조건·기존 허용을 바꿔야 하면 확인. 총시간 한도가 없어도 긍정적 결론을 얻기 위한 무의미한 반복을 완료 근거로 삼지 않음 | L~H. 매 검색이 아니라 결론 채택 경계에 적용. 연구에 구현 QG-04/05를 강제하지 않음 |
| QG-08 지식 채택 | 재사용 가치가 있는 후보의 채택 경계. 후보·출처·적용 범위·유효성·예외·관련 규칙 | 기본: 근거·범위·상태·버전 확인. 위험·충돌·영향에 따라 독립 AI 검토 선택. 과잉 일반화·규칙 충돌·폐기 조건 점검 | 근거·조건·충돌 처리를 갖춘 후보만 지정 범위·효력으로 채택. 후보 자체가 권한·필수 제약을 만들지 않음 | 후보 유지·범위 축소·충돌 검토·근거 확보. 과거 통과를 영구 유효로 취급하지 않음 | 매 Case의 별도 AI review를 강제하지 않음. 채택은 Case 완료와 독립, 추출·검토 비용은 예산에 포함. **구현(P4-07, 2026-09-24):** 활성화 때의 채택 확인(`adoption_check`) — 원문 가용성·열린 충돌·반증 대상 활성·범위 확대·대상 상태가 막고, 근거 없음·관측보다 넓은 범위·필수 승격·관계 대상 버전 변경·독립 검토 없음은 경고로 새 버전에 기록. 독립 AI 검토 실행은 없다. **P4-07b(2026-09-24, 사용자 결정: 반자동):** 참고 후보의 자동 활성 조건 여덟(후보·효력 참고·종류 운영 사실/알려진 문제·서로 다른 실행 둘 이상의 근거·그중 종료 코드 0 명령의 검증/분석 실행·관측한 저장소 범위·반증/충돌 없음·채택 확인 막음 없음)의 충족을 계산해 표시하고 사람이 한 번에 활성화한다 — 클릭 없는 활성화 없음 |

## 2. 업무·개별 작업별 설정

AI와 시스템은 업무 의미·위험·유효 정책에 따라 Case의 게이트 적용안을 구성하고 사용자가 Case와 필요한 Task에서 조정할 수 있게 한다. 모든 업무에서 게이트 선택을 사람에게 맡기지 않는다. 요청 정합성 확인의 필수성은 유지하며 내부 Task마다 같은 요청을 다시 승인받지 않는다.

| 설정 항목 | 동작 |
|---|---|
| 요청 정합성 게이트 | 기능 개발 QG-01 필수, OFF 불가. 확인 강도와 독립 AI 검토 여부는 조건부이며 실제 방식·근거를 기록 |
| 기타 게이트의 추천 | Profile·규모·위험·변경 특성의 관찰 근거와 적용안·이유를 제시 |
| 업무 Case 설정 | 유효 기본 정책에 따른 자동 적용·생략 또는 사용자의 명시 ON/OFF를 출처와 함께 기록 |
| 개별 Task 설정 | 해당 Task의 게이트는 업무 설정을 따르거나 ON/OFF로 조정 |
| 게이트 OFF | 해당 게이트의 실행과 진행 차단을 사용하지 않음. 통과 판정으로 취급하지 않음 |
| 독립적으로 유지할 조건 | 유효 실행 위임, controlled와 선택한 사람 검토, 필요한 논리 기록·사람 결정, 실행 권한·예산, 합의한 성공 기준·검증·완료 지점 |

필수 위임·권한·예산·요청 정합성 조건을 먼저 검사하고, 조정 가능한 값은 `Task 명시 설정 → Case 명시 설정 → Project 기본값 → Profile 기본값 → 시스템 기본값`으로 적용한다. Task 설정은 다른 Task나 Case 전체 기준을 해제하지 못한다. AI 추천과 실제 정책 적용을 구분하며 유효한 사용자 명시 설정을 조용히 덮어쓰지 않는다. 새 위험은 관찰·권고·적용 출처로 남긴다. 상세 우선순위는 [자동진행·위임·예산 정책](autonomy-budget-policy.md)을 따른다.

OFF는 별도 AI 검토만을 끄는 것으로 제한하지 않는다. 해당 게이트만을 위해 수행하는 도구 검사도 그 게이트와 함께 생략할 수 있다. 반면 합의한 성공 기준을 확인하기 위해 이미 계획한 테스트는 별도 검증 작업이므로 게이트 토글만으로 삭제하지 않는다. 예를 들어 QG-04를 꺼도 합의된 ‘필터에 맞는 전체 결과를 내보낸다’는 기준과 검증이 자동 면제되지는 않는다. 검증 자체의 면제·기준 완화는 기존 FR-06의 중요한 변경 절차로 구분한다.

게이트를 끌 때 `사용자 명시 OFF`와 `자동 조건에 따라 생략`을 구분하고 이유·주체·정책 버전·기존 실패·증거를 보존한다. 업무상 미적용과 검사 통과도 별도다. 다시 적용할 때 유효한 증거를 재사용할 수 있다. 실행 중 게이트 설정 변경은 현재 검증 1회 종료 뒤 반영한다. 현재 설정·예약 설정·실제 반영 시각을 구분하고 예약만으로 진행 중 검사를 중단하거나 원래 정책의 결과를 무효화하지 않는다. 권한 철회·중지 요청·hard budget은 별도 실행 제어다. 다른 게이트나 성공 기준의 실패는 설정 변경으로 지워지지 않는다.

## 3. 점수 대신 근거와 판정의 분리

게이트의 적용 여부와 그 안의 개별 기준의 필수성은 분리한다. 예를 들어 켜진 설계 게이트에서도 합의한 권한 요구 위반과 표현 개선 권고는 진행에 주는 영향이 다르다. 각 발견은 연결된 기준·필수/권고 구분·차단 효과를 기록한다. AI의 막연한 의견이 새 필수 요구를 만들지는 않는다. 중요한 위반 가능성이 있으나 근거가 부족하면 확인 작업이나 사람 판단을 요청한다. 구체적인 검사 항목·필수/권고 분류는 업무에 맞춰 제안한다.

QG-03의 질문 분리는 미위임 제품 선택을 AI가 대신 확정할 권한이 아니다. 필요한 사람 결정은 해당 작업 전 해결하며 다른 독립 작업은 계속할 수 있다. 명확한 요청의 정규화에 별도 전체 동의를 반복 요구하지 않는다. controlled·선택한 검토·게이트 OFF와 이 질문을 혼동하지 않는다. QG-06은 유효한 QG-04/05/07 증거와 외부 결과를 재사용하고 요청한 완료 의미와 보고를 대조한다.

`quality score >= 80`처럼 종합 점수로 통과를 정하면 중요한 결함이 사소한 항목 점수로 상쇄될 수 있다. 검사 단위를 기준·산출물·증거에 연결하고 다음 정보를 반환하는 형태를 권한다.

- 판정: 통과 / 실패 / 판단 보류 / 실행 불가 / 변경으로 재검토 필요 / 미적용.
- 개별 발견 사항: 대상, 관찰·근거, 영향, 어떤 기준을 위반하는지, 확실성, 제안 수정, 재검증 방법.
- 검사 실행 정보: 규칙·도구·검토자, 입력과 환경 버전, 시작·종료, 적용 범위, 실제 실행 여부.
- 후속 효과: 어떤 작업을 계속할 수 있는지, 무엇을 보류해야 하는지, 어떤 질문 또는 복구 작업이 필요한지.

AI의 ‘문제가 있을 수도 있다’는 의견과 증거가 있는 필수 기준 위반을 구분한다. 중요 여부가 모호한 발견은 통과로 감추거나 확정 실패로 과장하지 않고 판단 보류 상태에서 좁은 조사·검증 작업으로 전환한다. 사용자에게 갈 때는 현재 근거와 결정이 필요한 선택을 제시한다.

사람이 남은 문제·미검증 범위와 영향을 확인하면 미충족·미검증 모두에 대해 예외 수용 종료를 할 수 있다. `FinalAcceptance`와 `ExceptionDecision`을 후보 버전에 각각 연결하고 원래 실패·미검증 상태를 보존한다. 한 화면에서 함께 결정할 수 있지만 기록은 분리한다. 예외 종료를 요구 변경이나 추가 구현·실험·외부 쓰기 권한으로 확대하지 않는다. 완료·예외 종료 후 수정은 연결된 새 Case로 처리하며 이전 위임·인수를 새 범위에 묵시적으로 적용하지 않는다.

## 4. 게이트 실행 방식의 비용·효과 비교

다음 네 방식은 서로 완전히 대체되지 않는다. 의미 검토에는 AI 판단이 도움이 되고 실제 기능 동작에는 실행 증거가 필요하다. 사람은 미위임 제품 선택·중요한 변경·예외와 선택한 검토를 결정한다. 비용은 실제 CLI·모델·환경에 따라 달라 상대적으로 표현했다.

| 방식 | 동작 | 상대 비용·시간 | 잘 확인하는 것 | 놓치기 쉬운 것 / 한계 |
|---|---|---|---|---|
| A. 작성 프롬프트 안의 점검 지시 | 작성 중 ‘6개 항목과 기준을 확인하라’는 체크리스트를 주고 점검 결과도 출력 | 추가 지연이 가장 작을 수 있음. 응답 길이·추론 증가 비용은 있음 | 빠진 항목, 명백한 서술 불일치, 작성 습관 개선 | 작성자가 자기 해석을 그대로 정당화할 수 있음. 검사를 실제 수행했다는 증거가 되지 않음 |
| B. 같은 작성 세션에서 재검토 | 초안·구현 뒤 별도 검토 차례를 실행하되 기존 대화와 판단을 유지 | A보다 보통 추가 실행·토큰이 필요. 맥락 재구성 비용은 상대적으로 낮음 | 이미 조사한 사실을 활용한 누락·모순·구현 실수 점검 | 앞선 판단에 끌릴 수 있고 컨텍스트가 이미 커졌을 수 있음. 자신이 놓친 가정을 다시 놓칠 수 있음 |
| C. 별도 세션에서 검토 | 새 세션에 합의 의도·기준·대상 산출물·원시 증거를 제공해 검토 | 검토 실행과 필요한 정보 재구성 비용이 추가. 큰 문서·코드는 더 비쌈 | 작성 과정의 결론에 덜 묶인 요구 대조, 반례 제안, 테스트 기대값 검토 | 맥락을 덜 줘서 생기는 오탐·누락. 같은 모델이면 오판 상관성이 남음. 다른 모델이어도 정확성을 보장하지 않음 |
| D. 규칙·실제 도구로 검증 | Python 등으로 항목·버전을 검사하고 빌드·테스트·측정·재현을 실행 | 필드·버전 검사는 낮음. 통합 환경·부하 실험은 높음. 모델 토큰 외 CPU·환경 준비 비용 발생 | 기계적인 조건, 실제 실행 결과, 수치·계약·회귀 여부 | 잘못된 테스트 기대값, 누락된 시나리오, 사용자의 진짜 목적은 자체적으로 판정 못 함 |

프롬프트에 ‘필수 항목을 검사하라’고 쓰는 A와 실제 프로그램으로 검사하는 D는 다르다. A/B의 작성자 점검을 독립 AI 의미 검토 완료로 기록하지 않는다. 요청 정합성 확인은 항상 수행하되 명확한 저위험 요청에는 가벼운 확인을 적용할 수 있다. 독립 의미 검토가 선택됐을 때는 C가 필요하다. 어느 방식도 새 실행 권한이나 사람의 실제 확인 기록을 만들지 않는다.

활동은 최초 요청·후속 명시 결정·정책의 위임 범위를 따른다. 분석·연구의 복구 가능한 로컬 재현·임시 계측·테스트는 기존 권한·예산 안에서 자동 수행하며 실험을 명시하지 않았다는 이유만으로 별도 확인하지 않는다. 운영·외부 영향이나 권한 확대, 제품 수정으로의 목적 확대는 별도 판단 대상이다. 기능 개발의 미정 제품 선택을 임시 실험이라는 이름으로 확정 구현하지 않는다.

### 예시 1: 의도 초안에 AI가 임의로 넣은 결정

요청은 ‘관리 화면 데이터를 CSV로 내보내기’다. AI가 6개 항목을 모두 작성하면서 ‘현재 페이지 50행만, 관리자만 사용’이라고 사용자 확정 요구처럼 적었다고 하자.

- A: 항목이 모두 있으므로 통과했다고 보고할 수 있다.
- B: 원문과 초안을 다시 비교하면 근거 없는 결정을 찾을 수 있지만 자기 가정을 유지할 수도 있다.
- C: 원문·프로젝트 규칙·확인된 답변을 별도로 제공하면 ‘행 범위와 권한 정책은 미정’이라는 발견을 기대할 수 있다. 검토 AI도 틀릴 수 있다.
- D: 항목과 출처 링크의 존재는 검사할 수 있다. 출처가 그 결정을 뒷받침하는지, 사용자가 무엇을 원했는지는 형식 검사만으로 해결되지 않는다.
- 필요한 결과: 근거 없는 선택을 제안·질문으로 정정하고 필요한 사람 결정을 받는다. 해당 답변으로 위임이 명확해지면 기본 모드에서 전체 초안 승인을 다시 요구하지 않는다.

이 예시는 문서의 빈칸을 채운 사실과 사람의 의도를 확인한 사실을 구분해야 함을 보여준다.

### 예시 2: 필터 전체 내보내기의 잘못된 구현

사람이 ‘현재 필터에 맞는 전체 결과’를 확인했다. 구현은 현재 페이지 50행만 내보내고, 작성한 테스트도 50행을 정답으로 삼았다.

- A/B: 코드와 테스트가 서로 일치하므로 결함을 놓칠 수 있다.
- C: 합의된 기준을 기준으로 코드를 읽으면 페이지 제한과 잘못된 테스트 기대값을 찾아낼 수 있다.
- D: 기존 50행 테스트 통과는 충분하지 않다. 필터 일치 120행과 불일치 30행을 둔 독립 검증 사례에서 정확히 120행인지 실행하면 결함을 재현할 수 있다. 합의된 행 범위에 따라 기대값을 만든 것이 핵심이다.
- 필요한 결과: 구현과 잘못된 테스트를 수정하고 합의 기준에서 도출한 사례를 다시 실행한다. 필수 기준을 ‘50행 내보내기’로 낮추지 않는다.

### 예시 3: 계획은 완성됐지만 검증이 빠진 경우

설계에는 관리자 권한 정책이 있는데 계획은 버튼·API·CSV 생성만 포함한다. 개발계획 게이트는 ‘권한 구현·허용/비허용 사용자 검증 작업이 없다’는 누락을 찾아야 한다. D는 기준과 작업 연결의 빈칸을 잡을 수 있고, AI 검토는 단순 연결만 있고 실제 권한 검사가 없는 의미적 누락을 찾는 데 쓰인다. 작은 계획이라도 이런 검사는 가치가 있다.

### 확정한 조합

| 활동 | 선택한 방식 | 비용·효과를 조절할 부분 |
|---|---|---|
| 작성 중 자체 점검 | A/B를 보조적으로 사용 가능 | 명백한 누락 감소. 선택된 독립 AI 검토의 대체나 실제 실행 증거로 표시하지 않음 |
| 조건에 따라 선택된 독립 AI 의미 검토 | C, 작성과 별도 세션 | 위험·불확실성·정책에 따라 선택. 관련 범위·입력을 조절하고 역할별 CLI·모델 설정 사용 |
| 규칙·실제 실행 검증 | 필요한 D 수행, 유효한 증거 재사용 | 단계 이름만 바꾼 중복 실행을 줄이고 실제 성공 기준 검증 유지 |

선택 결과는 **요청 정합성을 항상 확인하고, 위험·불확실성·정책에 따라 필요한 독립 AI 의미 검토를 C로 수행하며, 실제 기준에 필요한 D를 조합**하는 방식이다. A/B를 보조로 쓰되 선택된 C를 대체하지 않는다. 게이트 적용·독립 AI 호출 여부·실제 검사 결과를 각각 기록한다. 성공 기준을 유지하면서 실행 병합과 유효 증거 재사용으로 비용을 줄인다. 대표 업무에서 실행 수·입력 크기·소요시간·수정 횟수·실제 발견을 관찰하여 효과를 검증한다.

간소 업무에서 보안·권한 변경이 생기면 깊은 검증이 필요할 수 있으므로 ‘간소=약한 검증’으로 고정하지 않는다. 같은 CLI·모델 사용은 허용하며 다른 공급자 사용을 필수로 하지 않는다.

별도 검토에는 업무 유형에 맞는 목표·종료조건과 해당 의도·제약·기준·산출물·원시 증거를 주고 작성자의 ‘모두 완료’ 결론만 전달하지 않는다. 정책상 읽기·검토 역할이 발견을 반환하고 별도 작성 작업이 반영한다. 실제 검증 명령은 현재 허용 범위의 Runner 작업으로 실행한다. 입력이 크면 관련 자료를 조회·분할하며 읽지 못한 범위를 숨기지 않는다. 이 역할 분리가 완전한 OS 격리를 뜻하지 않는다.

이 분리는 같은 모델의 상관된 오판을 없애지 않는다. 다른 CLI·모델도 정확성을 보장하지 않는다. 핵심은 유효 요청·기준, 실제 실행·측정, 작성 코드와 독립적으로 도출한 기대 동작, 원문 근거를 조합하는 데 있다. 복수 AI의 찬성표가 새 요구·권한을 만들거나 필요한 사람 결정을 대신하지 않는다.

프로젝트 기본 CLI·모델과 업무·역할별 배정을 사용한다. 규칙 검사는 결정적 코드로 처리하고 의미 작업의 모델은 필요한 능력·위험·입력·허용 예산에 맞춰 선택한다. 장애 시 사전 허용되지 않은 다른 CLI로 변경하기 전에는 D-45에 따라 사람에게 확인한다. 리뷰 이견은 기준·코드·실행 증거를 대조하고 필요한 좁은 검증·재판정을 수행한다. 반론·재검토도 실행 횟수·비용 예산에 포함하며 실제 수정은 같은 repair 주기로 연결한다. 작성자의 지적 삭제나 AI 표결로 해결하지 않고, 진전 없음·한도 소진·필요한 제품 선택이 남을 때 근거와 결정을 사람에게 제시한다.

## 5. 실패 복구와 자동 반복

확인된 흐름은 `필수 검사 불충족 → 의존 작업 보류 → 허용 범위의 AI 수정·재검증 → 유효 조건 충족 후 진행`이다. 반복 실패 또는 사람 판단이 필요하면 요청한다. 범위·기준 변경과 권한 확대는 자동 수정 범위 밖이다. 독립 작업은 의존 관계·기존 권한을 따로 평가한다. 사람의 예외 종료는 현재 결과를 알고 업무를 끝내는 경로이며, 실패한 결과를 임의의 다음 실행에 채택할 권한은 만들지 않는다.

최초 품질 실패 후 자동 수정·재검증은 기본 최대 2회이며 업무별로 한도를 조정할 수 있다. 최초 실패를 찾은 검사는 수정 1회에 포함하지 않는다. 같은 결과를 고치는 수정 주기의 누적 한도는 새 세션·재시작·작업 재분할로 초기화하지 않는 안을 제안한다. 수정 도중 새 발견이 생겨도 같은 주기의 한도를 유지한다. 한도 이후에는 시도·근거·남은 선택을 제시하고 사람에게 요청한다. 제품 판단·범위 또는 기준 변경·추가 권한이 필요하면 한도를 기다리지 않고 질문한다.

환경 장애 재시도와 제품 결함 수정 횟수는 구분한다. 이미 수행한 수정은 기록하고 같은 코드의 검증만 환경 복구 후 재실행하는 것을 새 코드 수정과 구분한다. 환경 복구·재시도·독립 리뷰·이견 해소·지식 추출의 실제 자원 소비는 전체 예산에 누적한다. 전체 예산 기본 무제한이 무한 환경 재시도를 뜻하지 않으며 선택한 hard 한도와 기술적 도구·복구 제한을 적용한다. 미제공 비용은 0이 아니다.

차단은 해당 조건을 의존하는 작업에 적용한다. 승인 대기인 사용자 정책과 무관한 읽기 조사 또는 통과한 다른 작업까지 무조건 중지하지 않는다. 다만 작업이 독립적이라고 AI가 말한 것만으로 코드·계약·공유 자원 충돌 검사를 생략하지 않는다.

## 6. 회귀, 불안정 테스트와 환경 실패

- 변경 전 기준 결과를 가능한 범위에서 확보하고 코드·환경 버전과 함께 기록한다. 기존 실패라는 이유만으로 자동 허용하지 않으며, 해당 실패가 성공 기준·변경 영역에 영향을 주는지 판단한다.
- 새 실패가 생겨도 곧바로 새 코드 결함으로 확정하지 않는다. 같은 환경의 기준 코드 비교, 실패 로그, 환경 차이를 사용해 원인을 좁힌다.
- 불안정 테스트는 여러 번 돌려 한 번 통과한 결과만 남기지 않는다. 시도 전체, 통과·실패 조건과 분류 근거를 보존한다. 재실행 횟수와 분류 규칙은 프로젝트의 검증 기준에 맞춰 구현·확인한다.
- 서비스 미실행, 자격증명 만료, 네트워크 실패, 도구 미설치는 `실행 불가`로 분리한다. 업무 성공의 증거가 되지 않으며 환경 복구 후 검증한다.
- 검사기 경고가 통과·실패를 직접 의미하는지 프로젝트 정책으로 정한다. 린트 경고·코드 커버리지 수치를 모든 프로젝트에 동일하게 강제하지 않는다.
- 합의된 테스트를 삭제하거나 예상 결과를 변경한 diff는 성공 기준 변경인지 검토한다. 테스트 유지보수 자체를 금지하는 것이 아니라 실패 은폐를 방지하는 것이다.

## 7. 문서와 운영 모델 연결

`GateDefinition`에는 목적·적용 조건·입력 계약·검사·필수 여부·실행 방식·실패 시 의존 작업 효과를 둔다. `GateRun`에는 실제 입력 버전과 검사 결과·증거·발견·재검증 연결을 둔다. `Approval`은 별도 기록으로 유지한다. `ExceptionDecision`도 원래 검사 상태를 덮어쓰지 않는다.

`GatePolicy / GateOverride`는 관찰·추천·Case/Task 설정·실제 적용을 분리해 보존한다. 적용 주체·대상·정책·시점과 명시 OFF·자동 조건부 생략을 기록한다. Autonomy·설계·계획의 선택한 사람 검토를 정하는 `ReviewPolicy`, 전체 `BudgetPolicy`와 구분한다.

스케줄러는 현재 위임·선택된 사람 검토·권한·의존 조건·예산 예약과 필요한 게이트 결과를 함께 검사한다. 요구·코드·정책이 바뀌면 원래 결과를 보존하고 영향받는 조건만 다시 평가한다. 유효한 증거의 재사용 근거를 남기며 해시·저장소별 커밋·환경·도구 버전을 연결한다. 게이트 통과 뒤 배정 직전의 변경도 다시 검사한다.

서버+개인 PC Runner 구성에서 코드·diff·상세 로그뿐 아니라 상세 의도·설계·계획·대화·검토 원문도 PC에 보관한다. 서버는 상태·요약·원문 ID/버전/해시 참조를 보존하고 상세 원문은 영구 저장 없이 일시중계한다. 원문이 필요한 검토는 해당 Runner에서 실행하고 PC 연결 시 상세를 열람한다. 요약만으로 원문 검토 완료를 만들지 않는다. 외부 AI 전송은 기존 CLI의 사용자 설정을 따르며 Runner 실행이 오프라인 처리를 뜻하지 않는다.

GitHub에는 Project 지정 기록 저장소의 Case별 새 이슈 하나에 주요 게이트·산출물의 요약과 참조만 누적한다. 코드·diff·상세 로그를 게시하지 않으며 게시 전 내용 점검의 완벽성을 주장하지 않는다. 품질 판정·사람 확인·예외·게시 상태를 분리한다. 요약 게시 장애 중 내부 진행은 계속하고 결과 대조·제한 재시도·누적 실패 알림을 관리한다. PR 리뷰만 외부 피드백으로 수집하며 이슈 댓글은 승인·명령이 아니다. 코드 push·PR은 Case 시작에 허용한 범위에서 자동 수행하고 controlled에서는 변경 결과를 먼저 확인한다.

기존 FR-06/09/10/12/17/20/29의 내용을 확장하면 되며, 별도 기능 요구로 추가한다면 `업무별 품질 게이트 구성·판정·수정·재검증`이 핵심이다. 과도한 요구 ID 증설보다 기능 구현 진입과 작업 결과 채택·종료 경계에 공통 게이트 계약을 붙이는 편이 일관적이다.

## 8. 과잉 절차를 피하기 위한 경계

넣지 않을 것을 검토할 대상: 모든 대화 답변의 승인, 매 검색의 출처 보고서 승인, 파일마다 별도 AI 리뷰, 모든 업무의 성능·보안·부하 테스트, 같은 코드·환경의 테스트를 단계 이름만 바꿔 반복, 모든 문서에 정해진 분량·다이어그램·대안 개수 강제, 한 업무의 모든 미정 질문 해결 전 아무 활동도 금지.

소규모라도 권한·돈·데이터 변경은 깊은 검증이 필요할 수 있다. 반대로 대량의 기계적인 수정은 반복 가능한 검사와 표본 확인이 더 효율적일 수 있다. WorkDepth, Autonomy, Profile, 게이트, 예산·권한을 구분한다. Fast Lane에서 별도 문서·단계·AI 실행을 줄이되 필수 의미 기록과 기준·증거는 보존한다.

## 9. 남은 구현·검증 사항

1. 규모·위험별 초기 추천의 세부 기준, 이견 처리와 검토 범위·증거 재사용 판단의 정확도.
2. 환경 재시도·개별 도구의 장시간 미응답·게시 알림 수치를 검증한다. 전체 예산은 기본 무제한이지만 설정된 제한은 배정 예약·실제 사용량 정산·중단 능력에 따라 강제한다. 정확값·추정값·미제공과 실제 강제 경계를 구분한다. repair·권한·필수 기준은 예산과 독립이다.
3. Windows native의 CLI 버전별 권한·도구 경계·재개 능력 실증. 첫 버전은 신뢰하는 개인 프로젝트용이며 CLI 설정·시스템 승인으로 통제하고 완전한 OS 격리는 보장하지 않는다.
4. 원문 일시중계가 서버 로그·캐시·추적 데이터에 상세 내용을 영구 보관하지 않는지 검증. 각 호스트 지정 경로에 하루 1회 자동 백업하고 최근 7개 백업을 유지한다. 백업 순환을 원본·승인·증거 자동 삭제로 해석하지 않으며 PC 원문을 서버 백업으로 복제하지 않는다.
5. 첫 버전은 한 사용자용이며 공동 계정·공동 편집·역할 UI는 후속이다. 지식은 최소 관리·추출·주입을 포함하고 모든 Case의 별도 검토 절차로 만들지 않는다. 채택·검색·증거 유효성은 프로젝트 지식 계약으로 검증한다.

실행 중 게이트 설정 변경은 **현재 검증 1회 종료 후 적용**하며 이후 자동 수정·재검증은 새 설정을 사용한다. Runner 연결 단절에 따른 **현재 도구 호출 종료 후 안전 정지**와는 별개의 경계다. 어느 기능도 실제 호환성을 검증하기 전 지원 완료로 표시하지 않는다.

원격 열람·검토는 LAN 또는 사용자 VPN에서 소유자 인증과 암호화 연결을 사용한다. 인터넷 직접 공개는 이후 범위다. 첫 버전 기술은 Python/FastAPI·SQLite와 React/TypeScript이며 일반 사용자 프로세스로 제공한다. 초기 시험 규모인 프로젝트 10개·Runner 3개·동시 AI 2개와 같은 Runner 쓰기 기본 1개는 등록 상한이나 성능 측정 결과가 아니다.
