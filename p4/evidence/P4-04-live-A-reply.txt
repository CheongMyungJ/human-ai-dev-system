run_id=run-p404-A-163738630
case_id=case-f0427a8a0bde4806
tool=codex/exec permission=read_only
observed_tool_version=codex-cli 0.154.0
session_ref=01a0cd32-f3af-7721-89bc-263f8c7d822f
exit_code=0
outcome=completed
normalized_events=12 unmapped=0
executed_at=2026-09-23T07:38:23.563678+00:00
--- final message ---
현재 `store.py`는 한 번 읽은 목록을 계속 캐시하고, `save()` 이후에도 무효화하지 않습니다. 따라서 저장 후 조회하면 이전 값이 나올 수 있습니다.

1. **캐시 제거 — 우선 추천합니다.** 매번 파일을 읽으면 구조가 단순해지고 저장 후 조회에도 변경이 반영됩니다. 파일 읽기와 JSON 파싱 비용은 늘지만, 성능 문제가 확인되지 않았다면 가장 간단한 선택입니다.

2. **`save()` 성공 후 캐시 무효화.** 저장이 성공하면 캐시를 비우고, 다음 조회에서 다시 읽습니다. 반복 조회 성능을 유지할 수 있지만, 다른 프로세스나 외부에서 파일을 변경하면 감지하지 못합니다. 모든 변경이 이 프로세스의 `save()`를 거친다면 적합합니다.

3. **파일 변경 여부를 확인해 캐시 무효화.** 조회할 때 수정 시각·크기 등을 비교하고 달라지면 다시 읽습니다. 외부 변경도 감지할 수 있지만 구현이 복잡해지고, 메타데이터만으로 모든 변경을 확실히 잡지는 못합니다.

코드와 문서는 수정하지 않았습니다. 선택 기준을 좁히려면, 현재 파일 크기와 조회 빈도는 어느 정도이고 `save()` 외부에서도 파일을 변경하나요?