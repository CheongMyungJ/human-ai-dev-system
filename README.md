# 사람–AI SW 개발 협업 시스템

설계 기준 **v0.8 2차 반영 · 2026-09-23 · D-01~95**(2026-09-24 보충: D-29·D-67·D-80, D-91~93(P4-09), D-94 명시로 켠 품질 게이트의 진행기 자동 검토·수정(UI-05a), D-95 실행 제한 시간 기본 1시간(P4-10 에서 구현됨)). GitHub: [CheongMyungJ/human-ai-dev-system](https://github.com/CheongMyungJ/human-ai-dev-system) (비공개).

정본 위치는 `C:\git\human-ai-dev-system-design`이다. **현재 구현은 P4-10 완료·스키마 v29**(UI-05a·P4-09·UI-04d·UI-04c·UI-04b·P4-07b·UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01·P4-03의 Profile 정의 v2 위)다. **S-037 이 P4-10(이슈 #8·#9 진행기 보강)을 마쳤다** — 실행 제한 시간이 업무·프로젝트 설정(기본 1시간, D-95)이 되고 실행마다 적용 값이 남으며, 제한에 걸린 실행은 실패가 아니라 시간 초과로 기록·표시되고 진행기는 같은 제한으로 되풀이하지 않고 "제한 시간 늘려 다시 시도" 카드에서 멈춘다; 검증이 근거와 함께 기준 미충족을 보고하면 진행기가 수정 구현 → 재검증 작업을 작업 그래프 새 리비전으로 더해 한도(기본 2) 안에서 돌리고 한도 뒤에만 사람 카드다(스키마 v29, 최종 전체 시험 807 통과·0 실패·2 건너뜀(16분 12초) — [P4-10 결과](p4/evidence/P4-10-results.md), 변경은 미커밋). **S-036 이 UI-05a(이슈 #7 관리 화면 정리 1단계)를 마쳤다** — 관리 화면 기능의 전수 대조(결정 기록), 새 화면 오른쪽 패널의 `작업` 탭(작업 그래프 보기·Task 추가·취소·질문↔Task 연결 고치기·실행 상세 — 고정 입력·영수증·이벤트·명령·작업공간 효과·출력), 그리고 **D-94**: 관리 화면도 QG-02~07 판정을 기록하지 못했다는 사실을 확인해 사용자가 고른 대로, 명시로 켠 QG-02·03·04 가 막으면 진행기가 별도 세션 검토 → 판정 기록 → 실패면 지적을 실은 수정(게이트 수정 한도) → 한도 뒤에만 `품질 게이트` 카드(한도 올림·끄기). 최종 전체 시험 795·0·2, 실제 codex 라이브(QG-04) 통과 — [UI-05a 결과](ui/evidence/UI-05a-results.md). 이 세션의 변경은 `2017fd3`로 사용자 지시에 따라 커밋·push했다. **S-035 가 P4-09(P5 전 정리 묶음, 사용자 결정 2026-09-24)를 마쳤다** — (a) 이슈 #2: 설계·계획으로 이월했다가 답한 질문의 답 원문이 그 질문이 막던 Task 의 구현·검증·실험·분석 실행에 핵심 고정 컨텍스트로 들어간다; (b) 이슈 #3(D-91): 읽기 전용 실행만 CLI 샌드박스를 유지하고 쓰기 실행은 codex `danger-full-access`·claude `bypassPermissions`(게시 규칙은 그대로); (c) 후속 대화의 시작 기준 카드에 `이전 업무 결과에서 시작`(이전 대화 브랜치의 끝 커밋이 커밋된 코드에 없을 때, 사람이 고름); (e) 이슈 #4: 끝나지 않은 실행이 없는 업무의 취소(사유 필수, 부분 결과·작업공간 보존, 되돌리지 않음); (f) 이슈 #5(D-92): AI 지식 후보는 범위가 틀려도 원래 값을 보존한 채 후보로 받고 주입하지 않으며 채택 확인에서 막는다, 거부된 항목의 내용 열람과 수동 등록 채우기; (g) 이슈 #6(D-93): 첫 응답의 자동 제목·업무화 때 한 번 갱신·사람의 이름 바꾸기(자동 제목이 덮지 않음); (d) 실제 codex 라이브(RCA·maintenance 의도 표본, 샌드박스 해제 뒤 진행 라이브 — [P4-09 결과](p4/evidence/P4-09-results.md)). 이 세션의 변경은 `5db52a9`로 사용자 지시에 따라 커밋·push했다. **S-034 가 P4-PLAN-08(여섯 Profile 대표 흐름의 진행기 경유 재현 + 혼합 목적 완료 — 새 시험 13건, 제품 코드 무변경)과 검증 환경 실험(원인은 codex 의 Windows 샌드박스가 사용자 프로필의 Python 을 읽지 못하는 것 — 인터프리터 폴더에 `CodexSandboxUsers` 그룹의 읽기·실행을 주면 제품 기본 샌드박스에서 실제 codex 가 시험을 돌린다)을 마치고 P4 를 제한 있는 완료로 닫았다**([P4-08 결과](p4/evidence/P4-08-results.md)·[P4-ENV 결과](p4/evidence/P4-ENV-results.md); `51a27e5`로 사용자 지시에 따라 커밋·push). **S-033 이 UI-04 완료 확인과 P4 수용 확인을 했다**(문서만 — [UI-04 결과](ui/evidence/UI-04-results.md): 네 하위 plan 의 확정 범위 전부 구현·검증됨, 남은 것 명시; [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md): 완료 조건 네 축 중 셋 완비, 여섯 Profile 의 진행기 경유 대표 흐름이 셋뿐이라 빠진 것을 [P4-PLAN-08](plans/P4-PLAN-08.md)로 기록 — 착수와 P4 종료 판정은 사용자 결정). **UI-04d 가 대화 검색(D-84)과 미커밋 포함 시작(D-77)을 더했다** — 왼쪽 목록의 검색이 현재 프로젝트의 대화(보관 포함)를 제목·요약·결정·규칙(서버)과 본문(원문을 가진 작업 PC 가 후보 원문만 읽어 올림, 제어부 메모리로만 중계 — 검색어·발췌·검색 표가 DB 에 없다)에서 찾고, PC 미연결이면 본문 N건 제외를 그대로 보이며, 결과에서 그 메시지·결정·규칙으로 이동한다. 코드 업무의 작업공간은 원래 폴더에 커밋하지 않은 변경이 있으면 만들기 전에 묻는다(`awaiting_basis`, 목록은 메모리 중계) — `포함해서 시작`(작업 PC 가 임시 인덱스로 스냅샷 커밋을 만들어 그 위에서 worktree 를 편다; 원래 트리·인덱스·브랜치는 그대로) / `커밋된 코드에서 시작`; `base_commit`·`committed_base`·포함 수·지문이 시작 상태의 기록이고 사람이 본 목록과 다른 것은 포함하지 않는다(지문 대조). 깨끗한 트리는 지금처럼 HEAD 에서 바로 만든다. 검색·선택은 권한이 아니다. **UI-04c 가 활성 목적·Profile 이행(D-86)·실행시간 합계(D-88)·외부 편집·작업 PC 열기(D-89)를 더했다** — 미완료 업무에서 목적을 더하거나 유형을 바꾸면 같은 대화의 Profile 을 개정하고(이력, 스키마 v26 `case_profile_revision`; 결정 사항 패널의 개정 폼 또는 업무 단계 응답의 AI 해석 `profile_change` — 근거는 사용자 메시지), 이전 Profile 의 의미 항목이 새 의도 문서에 유지 항목으로 남아 기준이 대상을 잃지 않으며 같은 키·같은 의무의 판정은 승계되고, 이전 목적은 유지 목적으로 선언 목적에 합쳐져 양쪽 충족을 요구한다. 진행기가 의도를 새 버전으로 다시 쓰고 그 버전은 검토·동의를 다시 지난다(개정은 권한이 아니다). 시간 한도의 기본 지표는 실행시간 합계(`execution_seconds` 의 뜻 그대로 — 실행별 배정~종료의 합, 사람 대기 제외)이고 상세 설정의 시간 절이 실행시간 합계·경과시간을 보이며, 재배정 뒤 옛 세대의 시간 행이 계속 자라던 계측 결함을 고쳤다. 결과물 패널의 작업공간 절이 작업 PC(작업공간을 만든 Runner)·경로·복사·열기(Runner 명령, 자기 worktree 만, 확인한 지원만 — Windows 폴더·PATH 의 `code`)를 보이고, 외부 변경은 다음 쓰기 실행 전에 트리 지문으로 확인·보존된다. **UI-04b 가 프로젝트 설정·상세 설정 화면과 PC 알림을 더했다**(D-72·D-82) — 프로젝트 설정 화면(기본값·프로젝트 규칙·저장소 탭)이 기본 도구·확인 경계·기본 예산·진행 상한·인라인 한도의 프로젝트 기본값을 이력으로 두고(값·출처·적용 시점·복귀, 스키마 v25 `project_setting`; 모델·깊이는 기본값이 없다는 사실을 표시), 그 값은 **그 뒤에 만든 대화·실행**에 적용된다(기존 기록 소급 없음). 대화의 상세 설정 탭이 적용 값·출처·적용 시각과 Autonomy·예산·진행 상한의 변경·복귀, 수준 조정, 저장소 선택, QG-02~07 설정을 맡는다. 브라우저 알림은 화면 조회의 전이(답변·확인 필요·막힘·실행 상태 확인 필요·예산 도달·완료·다른 프로젝트의 주의)를 띄우고 보고 있는 대화는 억제한다(일상 진행·첫 조회 제외, 켬/끔은 브라우저 설정). 설정은 실행 허용·동의·인수가 아니다. **P4-07b 가 참고 후보의 자동 활성 조건 표시(반자동, 사용자 결정 2026-09-24)를 더했다** — 시스템이 조건 여덟(후보·효력 참고·종류 운영 사실/알려진 문제·서로 다른 실행 둘 이상의 근거·그중 종료 코드 0 명령의 검증/분석 실행·관측한 저장소 범위·반증/충돌 없음·채택 확인 막음 없음)의 충족을 계산해 프로젝트 규칙 화면·후보 카드·관리 화면에 배지로 보이고, 사람이 규칙 화면에서 "한 번에 활성화"를 눌러야 참고 활성이 된다(클릭 없는 활성화 없음, 필수 승격 없음, 스키마 변경 없음). **UI-04a 가 결정 사항 패널과 프로젝트 규칙 화면을 더했다**(D-80) — 대화의 결정 사항 패널이 이 업무의 목표·기준·결정과 이 대화에서 정한 규칙·후보, 이 업무에 적용된 규칙(Manifest 집계)을 보고 원래 메시지로 이동하며, 프로젝트 규칙 화면이 공통 규칙의 확인·변경(채택 확인·활성화·개정·무효·충돌·수동 등록)과 원래 대화로 이동을 맡는다(스키마 변경 없음). **P4-07 이 선택적 지식 추출을 더했다** — 검증·분석·실험·구현 실행이 결과와 함께 남기는 후보(AI 제안, 후보로만), 논의 응답의 제안 표지, 근거 추가·중복 연결·대체 제안·반증 관계, QG-08 채택 확인과 사람의 활성화(범위 축소·다른 항목의 새 버전으로). 참고 후보의 자동 활성화는 사용자 결정(2026-09-24)으로 **반자동**(조건 충족 표시 + 사람이 한 번에 활성화)이며 P4-07b 로 구현했다. **P4-06b 가 사용자 결정(2026-09-24)으로 지식 원문(적용 내용과 자동 등록 규칙의 권위 사용자 메시지 한 건)을 서버 DB 에 저장해 어느 PC 의 실행에도 주입한다** — 원문 PC 경계의 유일한 예외이며 Runner 는 해시가 맞을 때만 쓴다. 대화 중심 UI의 주요 제품 정책과 개발 순서를 문서에 2차 반영했고, UI-01이 대화·요청의 영속 기반과 서버 전송 잠금을, P4-04가 고정 문맥의 등급·인라인 한도·실제 읽음 영수증·핵심 누락 보류·Runner 재시작의 사용량 복구를, UI-02가 Runner 제어/실행 분리·CLI 프로세스 트리의 중단과 종료 확인·요청 중단과 `unknown` 해제·재시작 대조·PC 미연결 전송 거부를, UI-03이 기본 대화 화면(`/`)·제어부 요청 처리기·AI 해석에 의한 같은 Case 업무화·브라우저 초안 복구·결과물 검토 버전 유지를, **P4-05가 업무 단계 진행기(업무화 뒤 의도 초안~완료를 제품이 잇고 사람 대기·완료·막힘에서만 요청을 끝냄)·확인 카드·기준 판정의 제품 기록·종료 Case 의 설명 전용 진입·후속 Case 이동을 구현했다.** **P4-05b가 재작성·재시도 상한을 설정(환경 변수 기본값 + Case 별 이력, 상한 카드의 "한도를 올리고 계속")으로 바꿨고, P4-06이 프로젝트 지식 등록부·대화에서 말한 규칙의 자동 활성 등록·활동+범위 자동 주입(필수 = 핵심 입력)·실행별 Manifest·적용 필수의 충돌 보류를 구현했다.** QG-02~07 자동 실행·파일별 포함 선택·후속 Case 의 이전 결과 기준 제안은 뒤 작업(별도 plan)이다. 관리 화면(`?view=admin`)은 그대로 있다. 화면 폭·간격·단축키와 기술 상세는 구현 과정에서 구체화한다. 화면 시안이나 실제 사용성 검증을 마쳤다는 뜻은 아니다.

## 다음 개발 세션

[DEVELOPMENT.md](DEVELOPMENT.md)와 [대화 중심 UI 설계](ui-conversation-design.md), [UI-05a 결과](ui/evidence/UI-05a-results.md)·[P4-09 결과](p4/evidence/P4-09-results.md)·[UI-04d 결과](ui/evidence/UI-04d-results.md)·[UI-04c 결과](ui/evidence/UI-04c-results.md)·[UI-04b 결과](ui/evidence/UI-04b-results.md)·[P4-07b 결과](p4/evidence/P4-07b-results.md)·[UI-04a 결과](ui/evidence/UI-04a-results.md)·[P4-07 결과](p4/evidence/P4-07-results.md)·[P4-06b 결과](p4/evidence/P4-06b-results.md)·[P4-06 결과](p4/evidence/P4-06-results.md)·[P4-05b 결과](p4/evidence/P4-05b-results.md)·[P4-05 결과](p4/evidence/P4-05-results.md)를 먼저 읽는다. **UI-04 완료 확인 + P4 수용 확인은 완료**했다(S-033 — 문서만, 코드 변경 없음, 기준선 pytest 740·0 실패·2 건너뜀 — [UI-04 결과](ui/evidence/UI-04-results.md)·[P4 수용 결과](p4/evidence/P4-ACCEPT-results.md)). 이 문서 변경은 `7c15df5`로 사용자 지시에 따라 커밋·push했고, 사용자 결정으로 **S-034 가 같은 대화에서 P4-PLAN-08 을 마치고 P4 를 닫았다**(`DONE_WITH_LIMITATIONS`; S-034 의 작업은 `51a27e5`로 사용자 지시에 따라 커밋·push했다). **UI-04d(대화 검색 + 미커밋 포함 시작)는 완료**했다(S-032 — 서버 필드 즉시·소유 PC 의 본문 검색(메모리 중계)·범위 표시·결과 이동, 더러운 트리의 시작 기준 카드·스냅샷 커밋 포함·지문 대조, 스키마 v27; 가짜 CLI·실제 Edge 시험, 실제 codex 라이브는 선택 사항이라 하지 않았다 — [UI-04d 결과](ui/evidence/UI-04d-results.md)). 미커밋 포함의 이진 선택은 사용자가 그대로 두기로 확인했다. 이 작업은 `5c52486`로 사용자 지시에 따라 커밋·push했다. **UI-04c(활성 목적·Profile 이행 + 실행시간 합계 + 외부 편집·작업 PC 열기)는 완료**했다(S-031 — 개정 이력·유지 항목·유지 목적·기준 승계·개정 폼·`profile_change` 해석, 시간 절·시간 한도 기본값·재배정 계측 수정, 작업공간 절·열기 명령·외부 변경 대조, 스키마 v26; 가짜 CLI·실제 Edge 시험, 실제 codex 라이브는 선택 사항이라 하지 않았다 — [UI-04c 결과](ui/evidence/UI-04c-results.md)). 이 작업은 `8461200`로 사용자 지시에 따라 커밋·push했다. **UI-04b(프로젝트 설정·상세 설정 화면 + 알림)는 완료**했다(S-030 — 프로젝트 기본값 층(스키마 v25)·설정 화면·상세 설정 탭·브라우저 알림; 가짜 CLI·실제 Edge 시험, 실제 codex 라이브는 선택 사항이라 하지 않았다). 이 작업은 `b388f1e`로 사용자 지시에 따라 커밋·push했다. **P4-07b(참고 후보의 자동 활성 조건 표시 — 반자동)는 완료**했다(S-029 — 조건 여덟의 충족을 계산해 표시하고 사람이 규칙 화면에서 한 번에 활성화; 가짜 CLI·실제 Edge 시험, 실제 codex 라이브는 선택 사항이라 하지 않았다). 이 작업은 `f57c036`로 사용자 지시에 따라 커밋·push했다. **UI-04a(결정 사항 패널·프로젝트 규칙 화면)는 완료**했다(S-028 — 이 대화에서 정한 규칙·후보·적용된 규칙을 보고 원래 메시지로 이동, 프로젝트 규칙 화면에서 채택 확인·활성화·개정·무효·수동 등록; 실제 codex·실제 Edge 라이브는 UI-04a 결과 3절). 이 작업은 `4c347a0`로 사용자 지시에 따라 커밋·push했다. **P4-07(선택적 지식 추출)은 완료**했다(S-027 — 작업 실행의 후보·근거·관계, QG-08 채택 확인과 활성화; 실제 codex 라이브는 P4-07 결과 3절). 이 작업은 `8defdfd`로 사용자 지시에 따라 커밋·push했다. **P4-06b(지식 원문 서버 저장)는 완료**했다(S-026 — 두 PC 시험: PC A 에서 등록한 규칙이 PC A 미연결에서도 PC B 의 실행에 서버 본문으로 주입되고 영수증 `read`; 실제 codex 두 Runner 라이브). 이 작업은 `d1dcc4e`로 사용자 지시에 따라 커밋·push했다. **P4-06(지식 관리·적용)과 P4-05b(진행 상한 설정)는 완료**했다(S-025, 실제 codex 라이브 21/21 — 대화에서 정한 규칙이 다른 대화의 업무 실행 여섯에 원문으로 주입됨). 이 두 작업은 `d74726a`로 사용자 지시에 따라 커밋·push했다. **P4-05(업무 단계 자동 진행과 완료·예외·후속)는 완료**했다 — 업무화 뒤 의도 초안→QG-01·재작성→질문·동의→controlled 시작→준비→작업 그래프·작업공간→구현·검증→기준 판정→자동 완료/controlled 결과/예외 수용을 제품이 잇고, 사람이 필요한 곳(확인 카드)에서만 전송을 연다. 실행 사이에는 요청 잠금을 유지하고 사람의 결정 뒤에는 진행 요청을 연다. 종료 Case 는 설명 전용으로 열리고 수정 요청은 후속 Case 로 옮긴다(실제 codex·실제 Edge 로 확인, 제품 규칙 16/16). **UI-03(기본 대화 화면)도 완료**했다 — 새 대화에서 메시지를 보내면 제어부가 읽기 전용 AI 응답을 만들어 붙이고 요청을 끝내며, 명확한 작업 요청은 AI 해석으로 같은 대화에서 업무로 전환된다(위임 근거는 사용자 메시지). 초안은 이 브라우저에 남고 접수가 확인된 것만 비워지며 자동으로 보내지지 않는다. 결과물은 읽던 버전이 고정된다(실제 codex·실제 Edge 로 확인). 구현 중 제어부 공유 연결의 동시 사용 결함(P2 부터)을 찾아 고쳤다. **UI-02(입력·실행 제어)도 완료**했다 — CLI 가 Windows job 안에서 돌고 중단하면 손자까지 끝낸 뒤 활성 0 을 확인하며, Runner 가 죽으면 OS 가 트리를 끝내고, 재기동한 Runner 가 CLI 를 다시 부르지 않고 원장으로 대조한다. 확인 근거가 있을 때만 요청이 `interrupted` 로 풀리고 PC 미연결이면 입력을 받지 않는다(실제 codex 로 실증). **P4-04(문맥·재개)도 완료**했다 — 핵심/보조 등급과 실행당 인라인 한도(보조만 드러내어 생략, 핵심 초과·핵심 원문 미확인은 보류), Runner가 CLI 호출 전에 해시를 대조해 보고하는 영수증, 시작하지 않은 실행의 소비 0, 결과 시점 최신성, 재시작 뒤 원시 출력의 사용량 복구. **P4-09(P5 전 정리 묶음)는 완료**했다(S-035 — 이슈 #2~#6·후속 Case 시작 기준·실제 codex 라이브, 스키마 v28, pytest 785·0 실패·2 건너뜀; [P4-09 결과](p4/evidence/P4-09-results.md)). 이 작업은 `5db52a9`로 사용자 지시에 따라 커밋·push했다. 다음 작업은 **UI-05a·UI-05b**(이슈 #7 관리 화면 정리 — 필수 기능을 새 화면으로 옮기고 관리 화면 삭제, 사용자 결정 2026-09-24), 그 뒤 **P5-01**이다. plan 을 기록·공유한 뒤 시작한다. 완료된 P4-01~07·07b·UI-01~03·UI-04a~04d 를 다시 열지 않는다. 여러 Runner 의 배정 경로 한계(S-026 발견)는 P6-01 에서 다루며 그때까지 한 제어부에 Runner 하나로 쓴다. UI-01은 `a509063`, P4-04는 `5580896`, UI-02는 `592eb39`, UI-03은 `d51fc02`로 각각 사용자 지시에 따라 커밋·push했다. P4-05는 `b475146`로 사용자 지시에 따라 커밋·push했다. 완료된 작업의 설명·시작점·이전 인계는 [보존본](development-history-v0.8.md)으로 옮겨 DEVELOPMENT.md를 줄였다.

> DEVELOPMENT.md의 최신 인계(S-037)와 GitHub 이슈 #7 본문, UI-05a 결과 2·2.3절, P4-09 결과 4절을 읽고 **UI-05b — 이슈 #7 관리 화면 정리 2단계**(DEVELOPMENT.md 1.11절: 나머지 옮김 + 실행 중 Case 의 취소 2단계 + 관리 화면 링크 제거·`?view=admin` 삭제(또는 개발자 전용)·관리 화면 의존 시험 대체·문서)를 한 plan(`plans/UI-PLAN-05b.md`)으로 진행해줘. P4-10 까지의 완료 상태를 보존하고 기준선을 확인한 뒤 plan 을 기록·공유하고 구현·검증·인계 절차를 지켜줘. P5 로 확대하지 말고, 실제 외부 쓰기는 따로 허용받은 범위만 해줘. 새 제품 판단이 필요하면 질문해줘.

P3은 제한을 명시한 완료 상태이고 P4-01~10(05b·06b·07b 포함)·UI-01~03·UI-04a~04d·UI-05a 도 완료됐다. 개발 순서는 **UI-01(완료) → P4-04(완료) + UI-02(완료) → UI-03 기본 화면(완료) → P4-05(완료) → P4-05b·P4-06(완료) → P4-06b(완료) → P4-07(완료) → UI-04a(완료) → P4-07b(완료) → UI-04b(완료) → UI-04c(완료) → UI-04d(완료) → UI-04 완료 확인·P4 수용 확인(완료, S-033) → P4-08·P4 종료(완료, S-034) → P4-09 정리 묶음(완료, S-035) → UI-05a(완료, S-036) → P4-10(완료, S-037) → UI-05b(이슈 #7 관리 화면 정리 2단계) → P5/P6**다. 중단은 실제 CLI(codex)로 트리 종료를 확인한 뒤 관리 화면에 연결했다. UI-04는 착수 시 세분 plan으로 나누고 P4-04에 전체 UI를 몰아넣지 않는다.

## 설계 방향

사람은 위임하지 않은 제품 선택·권한 확대·중요한 모호성·한도 변경·예외 수용에서 호출한다. AI는 업무에 맞는 작업 그래프를 만들고 시스템은 요청·권한·버전·증거·예산을 검사한다.

- WorkDepth(simple/standard/deep), Autonomy(기본 ask-on-decision/controlled), Budget(기본 무제한)을 분리한다. Fast Lane은 명확·저위험인 작업의 실행 경로다.
- 대표 대화 하나가 Case 하나다. 목표·Profile이 미정인 논의 준비 단계에서 명확한 업무 요청으로 같은 Case를 이어가며 소비·결정·제한을 보존하는 UI를 설계한다.
- AI intake 초안은 여섯 Case Profile의 목적에 맞게 구성한다. 별도 설계·계획·검토 실행을 모든 업무에 강제하지 않는다. P4-03의 목적별 완료 계약을 유지한다.
- Project는 복수 Repository를 포함한다. 한 Case의 저장소 작업은 같은 Runner에서 수행한다. 업무화 뒤 연결·허용 안에서 기록 이슈 하나를 만들며 대표 저장소가 코드와 이슈를 겸할 수 있도록 설계를 개정했다. 허용은 각각 검사한다.
- PC·라이트 테마·현재 프로젝트 중심의 대화/검토 화면을 사용한다. 현재 요청 처리 중 초안 편집은 가능하고 일반 전송은 잠그며 질문 카드·중단은 별도 경로로 처리하도록 설계한다.
- 현재 브라우저의 대화별 초안을 복구하고 현재 프로젝트의 보관 대화까지 검색한다. 본문 검색은 PC 연결 시 지원하며 읽던 결과물 버전·피드백 대상을 유지한다. 앱은 검토·참조 의견을 제공하고 파일 직접 편집은 외부 도구에서 한다.
- 미완료인 같은 문제의 목적 확장·Profile 변경은 이력·기준·예산을 보존하며 같은 Case에서 이행한다. 종료 후 설명은 기존 Case 예산에 누적하고 추가 실험·수정은 새 Case로 연결한다. 시간 한도를 선택하면 실행시간 합계를 기본 지표로 사용한다.
- 코드 변경의 기본 완료 지점은 전용 작업공간의 로컬 구현·검증이다. 원래 폴더 반영이나 기본 브랜치 병합을 뜻하지 않는다. 이미지 첨부·분석과 모바일 최적화는 후속 검토다.
- 요청된 push·PR은 유효한 허용 범위 안에서 자동 수행한다. controlled는 결과 후보 확인 후 요청된 게시와 필요한 외부 CI를 거쳐 종료한다. 허용만으로 게시가 의무가 되지는 않는다.
- 지식은 원본·적용 조건·버전을 관리하고 선택적으로 추출해 작업 범위·활동에 맞게 주입한다. 범용 검색이나 별도 외부 지식 시스템은 포함하지 않는다.

Windows 네이티브·한 사용자·Python/FastAPI·Python Runner·SQLite·React/TypeScript를 유지한다. 서버+PC 구성의 상세 원문은 PC에 두고 제어부는 상태·요약·참조만 보관한다 — 지식 원문(적용 내용·권위 메시지 한 건)만 사용자 결정으로 서버에 저장한다(P4-06b). worktree를 OS 격리라고 설명하지 않는다.

## 현재 구현과 실행 방법

P3-R1~R4에서 Autonomy·확인 지점·예산·저장소 선택의 강제와 같은 Runner의 복수 저장소 작업공간이 연결됐다. P4-01~02는 게이트·repair·예약 변경·부분 재검증·늦은 결과를, P4-03은 Profile 정의 v2의 목적별 완료 계약·충족 방식·결론·혼합 목적·실험 정리를 구현했다. 기존 v1 Case에는 새 완료 규칙을 소급하지 않는다. UI-01~03은 대화·요청 기반, 입력·실행 제어, **기본 대화 화면**을 구현했다(업무 단계의 자동 진행은 아직 없고 관리 화면에서 진행한다). 지식 적용·실제 GitHub 게시와 나머지 대화 UI(검색·알림·상세 설정 등)는 후속 구현이다. 상세 상태와 한계는 DEVELOPMENT.md를 따른다.

필요 환경은 기존 구현 기준 Windows, Python 3.12, Git, Node.js 22 이상과 설치·로그인된 Codex 또는 Claude Code다. 설치·PATH 문제와 미설치를 구별하고 저장소 스크립트가 사용하는 `.venv` 인터프리터를 확인한다.

```powershell
scripts\bootstrap.ps1
scripts\run-controller.ps1
# 다른 터미널에서 Runner
scripts\run-runner.ps1
# 시험 (웹 빌드·웹 단위 시험 → pytest → P1 계약. -SkipWeb 으로 웹을 뺀다)
scripts\run-tests.ps1
```

제어부 기본 주소는 `http://127.0.0.1:8765`다 — 기본 대화 화면이 `/`, 기존 관리 화면이 `/?view=admin` 이다. 제어부는 저장된 사용자 요청을 기본으로 자동 처리하고(`HADS_AUTO_PROCESS_REQUESTS=0` 이면 끈다) 업무화 뒤 업무 단계를 스스로 잇는다(`HADS_AUTO_PROGRESS_WORK=0` 이면 끄며, 처리기가 꺼지면 함께 꺼진다). 재작성·재시도 상한의 시스템 기본값은 `HADS_REPAIR_LIMIT`(기본 2)·`HADS_TASK_RETRY_LIMIT`(기본 1, 둘 다 0~10)이다. [P4-06 보고](p4/evidence/P4-06-results.md)의 마지막 검증은 웹 빌드·웹 단위 18건, pytest 672건, P1 계약 unittest 18건 통과와 실제 codex·실제 Edge 라이브(제품 규칙 21/21)다. 브라우저 시험은 설치된 Edge 를 Playwright 로 쓴다(`requirements.txt`, 브라우저를 내려받지 않는다). 개발 재개 시 현재 기준선을 다시 확인한다.

`.venv`, `web/node_modules`, `web/dist`는 로컬 환경·빌드 경로다. `var`에는 업무 DB·원문·증거가 있으므로 단순 캐시처럼 삭제하지 않는다. CLI 로그인은 각 도구의 기존 설정을 사용하고 시스템 업무 기록에 자격증명을 복사하지 않는다.

변경 관측만으로 의미적 정답을 증명하지 않으며 실행 증거의 실제 수집·자기보고 한계도 구분한다. P4-03의 무변경 목표 충족은 검증 실행의 관측을 요구한다. 실제 중단 확인·PC 원문 가용성·외부 효과 대조 등 남은 제약은 DEVELOPMENT.md의 이월표와 UI 설계의 구현 차이를 함께 확인한다.

## 읽을 문서

| 문서 | 역할 |
|---|---|
| [DEVELOPMENT.md](DEVELOPMENT.md) | 현재 구현·다음 작업·plan·검증·인계의 시작점 |
| [전체 설계](design-draft.md) / [사용자 결정](decisions.md) | v0.8 2차 범위·책임·정책과 D-01~90 |
| [대화 중심 UI](ui-conversation-design.md) | 확정 화면·상호작용, 현재 구현 차이, 구현 상세·개발 인계 |
| [자동화·예산](autonomy-budget-policy.md) | 위임·material delta·Fast Lane·예산·controlled 경계 |
| [Case Profile](case-profiles.md) / [intake·산출물](intent-artifacts.md) | 목적별 AI 초안·피드백·증거·완료 |
| [프로젝트 지식](project-knowledge.md) | 관리·선택적 추출·자동 주입·유효성 |
| [기능 요구](scenarios-functional-requirements.md) / [비기능 요구](nonfunctional-requirements.md) | 요구사항과 수용 기준 |
| [깊이·UX](sizing-and-review-ux.md) / [품질 게이트](quality-gates.md) / [게이트 운영](gate-operations.md) | 실제 적용 정책·질문·검증·repair |
| [검토 문맥](review-context-contract.md) / [완료](completion-lifecycle.md) | 입력·기준·결과 후보·예외·후속 Case |
| [실행·작업공간](execution-workspace-review.md) / [GitHub 기록](github-journal.md) | 복수 Repo·권한·부분 반영·요약 게시 |
| [데이터 경계](data-boundary-review.md) / [구현 기준](implementation-baseline.md) | Runner 원문·복구·기술·설치·운영 |
| [수용 시나리오](review-acceptance-matrix.md) / [검토 보고](design-review-report.md) | 문서 정합성과 후속 검증 |
| [설계 개정 이력](review-status.md) | 과거 결정과 v0.7·v0.8 1·2차 개정 관계 |
| [기술 조사](review-tech-findings.md) / [CLI 중지 조사](cli-pause-feasibility.md) | 날짜가 있는 기술 참고·실증 과제 |
| [v0.6 개발 인계 보존본](development-history-v0.6.md) / [당시 시연](implementation-walkthrough-v0.6.md) | 현재 정책과 구분한 P1~P3-03 이력 |

## 검증의 경계

설계 문서 정합성, 제품 자동 시험, 실제 CLI·외부 서비스·호스트 시험은 서로 다른 증거다. OpenCode는 문서 계약과 실증 상태를 구별한다. 안전 중지·절대 예산 통제·원문 비보관·복구·성능은 구현과 실행 모드별로 확인한다. 아직 검증하지 않은 기능을 지원 완료로 표시하지 않는다.
