# 개발 진행 안내서 — 새 세션의 시작점

기준: **설계 v0.8 2차 반영 / 사용자 결정 D-01~95(+2026-09-24 보충: D-29 진행 상한, D-67 지식 원문 서버 저장, D-80 자동 등록; D-91 쓰기 실행 샌드박스 해제, D-92 AI 후보의 범위·열람, D-93 대화 제목, D-94 명시로 켠 품질 게이트의 진행기 자동 검토·수정, D-95 실행 제한 시간 기본 1시간 — P4-10 에서 구현) / 2026-09-24**. 구현은 **UI-05a 완료·스키마 v28**(P4-09·UI-04d·UI-04c·UI-04b·P4-07b·UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01·P4-03 완료 위)이다. 가장 최근 세션은 **UI-05a — 이슈 #7 관리 화면(옛 UI) 정리 1단계**(S-036 — [UI-05a 결과](ui/evidence/UI-05a-results.md); 그 전 S-035 가 P4-09 정리 묶음 — [P4-09 결과](p4/evidence/P4-09-results.md))이고, 마지막 구현 작업은 **UI-05a**(사용자 결정 2026-09-24 — (가) 관리 화면 기능 전부의 **전수 대조 분류**(옮김·대신함·개발자 전용·버림, 결정 기록 — 결과 2절, UI-05b 몫 배정); (나) 새 화면 오른쪽 패널의 **`작업` 탭**: 작업 그래프 보기와 Task 추가·취소·**질문↔Task 연결 고치기**(어느 화면에도 없던 것)와 **실행 상세**(고정 입력 참조·영수증·문맥·진입 검사·이벤트·명령·작업공간 효과·출력) — 그래프 수정 뒤 진행기가 이어 가고(`replan:<rev>`), 종료 업무의 질문 연결은 거부(그 전엔 409 를 돌려주며 연결 표가 바뀌었다); (다) **D-94**: 관리 화면도 QG-02~07 판정을 기록하지 못했다는 사실을 확인해 사용자에게 물었고, 사용자가 **진행기가 켠 게이트를 스스로 돌린다**를 골랐다 — 명시로 켠 QG-02(계획·구현 전)·QG-03(구현 전)·QG-04(검증 전)가 막으면 별도 세션 검토 → 판정 기록(검증 1회 열고 닫음) → 실패면 지적을 실은 repair(게이트 수정 한도) → 한도 뒤에만 `quality_gate` 카드(한도 올림·재시도·끄기, 사유), 결합 기록 경로의 QG-02 는 구현 전에)이다. 각 완료 작업이 지금 제품에서 하는 일은 1절의 "구현된 것 — 요약" 표에, 당시 설명 문단은 [보존본](development-history-v0.8.md) 1절에 있다. 현재 구현과 새 목표를 구분한다. 코드·테스트·스키마·실행 스크립트·의존성 설정을 변경하기 전에 반드시 plan을 기록·공유한다. 이 규칙은 이 저장소의 개발 절차이며, 제품 Fast Lane에 매번 별도 plan 문서나 사람 승인을 요구한다는 뜻이 아니다.

**UI-05a(이슈 #7 관리 화면 정리 1단계)를 마쳤다(S-036, 아래 10절 — [UI-PLAN-05a](plans/UI-PLAN-05a.md)·[UI-05a 결과](ui/evidence/UI-05a-results.md)).** 전수 대조·`작업` 탭(작업 그래프 수정·실행 상세)·QG-02~07 진행기 자동 검토·수정(D-94) 전부 구현·검증(pytest +10, 브라우저 2, 스키마 무변경). 사람에게 물은 것 둘의 답: QG-02~07 은 **(ⅱ) 진행기가 스스로 돌림**(D-94 — 게이트와 성공 기준의 차이 설명 뒤), 실행 중 Case 의 취소 2단계는 **UI-05b 에 붙임**. 세션 중 등록된 이슈 #8·#9 는 사용자 결정으로 **P4-10**(다음 세션, UI-05b 앞)에 배정했고 실행 제한 시간 기본값은 **1시간**(D-95)이다. 관리 화면은 아직 있다. 이 세션의 변경은 사용자 지시로 커밋·push 했다(10절 S-036). 그 전 S-035 는 P4-09 정리 묶음(`5db52a9`)이었다. 다음은 **P4-10 — 이슈 #8·#9 진행기 보강**(사용자 결정 2026-09-24, S-036: 다음 세션·UI-05b 앞, 한 plan `plans/P4-PLAN-10.md` — #8 실행 제한 시간의 설정화(기본 **1시간**, D-95)·시간 초과를 실패와 구분(Runner `stop_reason`·적용 제한 기록)·같은 제한으로 자동 재시도하지 않고 '제한 시간 늘려 다시 시도' 카드; #9 검증이 기준 `not_met` 을 근거와 함께 보고하면 진행기가 수정 구현 → 재검증 Task 를 작업 그래프 새 리비전으로 더해 한도 안에서 돌림(`unverified` 는 사람 카드), 한도 뒤 사람 카드), 그 뒤 **UI-05b — 이슈 #7 관리 화면 정리 2단계**(결과 2.3절의 나머지 옮김 + 실행 중 Case 의 취소 2단계 + 관리 화면 링크 제거·`?view=admin` 삭제(개발자 전용을 숨은 화면으로 둘지 결정)·관리 화면 의존 시험 5곳 대체·문서, 끝나면 이슈 #7 닫기), 그 뒤 P5-01 이다. 주요 UI 동작·제품 정책·개발 순서는 D-68~95로 확정돼 있다. [대화 중심 UI 설계](ui-conversation-design.md)의 확정 내용과 [UI-05a 결과](ui/evidence/UI-05a-results.md)·[P4-09 결과](p4/evidence/P4-09-results.md)·[P4-08 결과](p4/evidence/P4-08-results.md)·[P4-ENV 결과](p4/evidence/P4-ENV-results.md)·[UI-04 결과](ui/evidence/UI-04-results.md)·[P4 수용 결과](p4/evidence/P4-ACCEPT-results.md), 아래 1.11절의 다음 작업(P4-10 → UI-05b → P5-01)을 먼저 읽는다. 참고 후보의 자동 활성화는 **사용자 결정(2026-09-24, S-028): 반자동**이며 P4-07b 로 구현됐다. 정확한 화면 치수·단축키·기술 상세는 실제 구현에서 검토한다.

**커밋·push는 세션마다 사용자 지시로만 한다.** 최근: UI-04a `4c347a0` 과 인계 기록 `6f33867`(S-028 — 사용자 지시 "그래 4번으로 가자. 다른세션에서 이어서 작업할 수 있게 정리하고 커밋 푸시해줘."), **P4-07b `f57c036` 과 인계 기록 `2bdfb8a`(S-029 — 사용자 지시 "커밋푸시해줘 다른세션에서 이어서 작업할게")**. **UI-04b `b388f1e` 과 인계 기록 `84252ce`(S-030 — 사용자 지시 "커밋 푸시해줘")**. **UI-04c `8461200` 과 인계 기록 `fae2af5`(S-031 — 사용자 지시 "좋아 커밋푸시해줘")**. **UI-04d `5c52486` 과 인계 기록 `44b6909`(S-032 — 사용자 지시 "이진 선택 그대로 두고 커밋 푸시해줘")**. **S-033(UI-04 완료 확인 + P4 수용 확인, 문서만) `7c15df5` 과 인계 기록 `4bbda6c`(사용자 지시 "이 문서 변경은 커밋 푸시하고 P4-PLAN-08 먼저 하고 닫아줘")**. **S-034(P4-08 + 검증 환경 실험 + P4 종료) `51a27e5` 과 인계 기록 `6ae491a`(사용자 지시 "좋아. 이번 세션 변경 커밋푸시해줘"); 그 뒤 `730555c`·`30c30c0` 는 이슈 #4~#7 배정 기록**. **S-035(P4-09) `5db52a9` 과 인계 기록(사용자 지시 "지금대로 두자. 커밋푸시해줘.")**. 각 허용은 그 변경과 인계 기록에만 적용되며 다음 변경의 외부 쓰기 허용이 아니다. 그 이전 세션(S-020~S-030)의 커밋·지시는 [보존본](development-history-v0.8.md) 6절의 각 인계와 `git log` 에 있다. 각 허용은 그 변경과 인계 기록에만 적용되며 다음 변경의 외부 쓰기 허용이 아니다. 완료된 작업의 설명·시작점·이전 인계는 [보존본](development-history-v0.8.md)으로 옮겼다(S-021, S-022~S-036 에 이어서). **S-036(UI-05a) 은 사용자 지시("라이브가 문제없이 끝나면 문서 정리 잘 하고 커밋푸시까지해줘.")로 변경 커밋과 인계 기록 커밋을 push 했다.**

다른 개발 세션에 전달할 요청:

> C:\git\human-ai-dev-system-design\DEVELOPMENT.md의 최신 인계(S-036)와 GitHub 이슈 #8·#9 본문(`gh issue view 8`·`9`), [UI-05a 결과](ui/evidence/UI-05a-results.md), decisions.md 의 D-94·D-95 를 읽고 **P4-10 — 이슈 #8·#9 진행기 보강**(1.11절: #8 실행 제한 시간을 프로젝트·업무 설정으로(기본 1시간, D-95 — 출처·이력은 진행 상한과 같은 방식), Runner 가 `stop_reason`·적용 제한을 결과에 싣고 제어부가 시간 초과를 실패와 구분해 기록·표시, 시간 초과는 같은 제한으로 자동 재시도하지 않고 '제한 시간 늘려 다시 시도' 카드; #9 검증이 기준 `not_met` 을 근거와 함께 보고하면 진행기가 수정 구현 → 재검증 Task 를 작업 그래프 새 리비전으로 더해 한도 안에서 돌리고 한도 뒤·`unverified` 는 사람 카드 — 기존 `remediation_cycle`·검증 1회 규칙·UI-05a 의 그래프 수정 경로 재사용)를 한 plan(`plans/P4-PLAN-10.md`)으로 진행해줘. UI-05a 까지의 완료 상태를 보존하고, 실제 코드·환경을 확인한 뒤(기준선) plan 을 기록·공유하고 구현·검증·인계를 해줘. UI-05b·P5 로 자동 확대하지 마. 실제 외부 쓰기(push·PR)는 시험 대상·범위를 따로 허용받은 경우만 해줘. 새 제품 판단이 필요하면 질문해줘.

**기본 수행 단위는 현재 하위 작업 하나**다. **P4-01~09(05b·06b·07b 포함)·UI-01~03·UI-04a~04d·UI-05a 는 끝났고 P3의 남은 라이브 하나(경로 C)는 사용자 지시로 스킵됐다.** P4 는 `DONE_WITH_LIMITATIONS`(4절 — 남은 한계는 9절)이고 **UI-05a(S-036)가 관리 화면 전수 대조·`작업` 탭·QG-02~07 진행기 자동 검토(D-94)를 마쳤다**(1.11절). 다음은 **P4-10**(이슈 #8·#9 — 사용자 결정: UI-05b 앞, 1.11절), 그 뒤 **UI-05b**(이슈 #7 관리 화면 정리 2단계), 그 뒤 **P5-01**(7절)이다. 사용자 지시가 우선하고, 필요한 기술 선택은 진행하되 새 제품 판단이 필요한 경우만 질문한다. 커밋·push 허용의 범위는 위와 같다.

## 1. 현재 상태

| 항목 | 현재 값 |
|---|---|
| 저장소·작업 위치 | `C:\git\human-ai-dev-system-design`, https://github.com/CheongMyungJ/human-ai-dev-system (비공개) |
| 설계 기준 | **v0.8 2차, D-01~95**. 대화 UI·준비 Case·요청 제어와 초안·검색·검토 버전·목적 변경·설명 소비·시간 지표·외부 편집·개발 순서 확정. **2026-09-24 보충:** D-29(진행 상한 = 설정), D-80(대화의 프로젝트 규칙 자동 활성 등록), **D-91**(쓰기 실행 샌드박스 해제), **D-92**(AI 후보의 범위·거부 열람), **D-93**(대화 제목), **D-94**(명시로 켠 품질 게이트는 진행기가 스스로 검토·수정), **D-95**(실행 제한 시간 기본 1시간·시간 초과 구분 — P4-10 에서 구현) |
| 구현 기준 | **UI-05a 완료**(P4-09·UI-04d·UI-04c·UI-04b·P4-07b·UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01·P4-03 위), 스키마 **v28**. 각 작업이 지금 제품에서 하는 일은 아래 "구현된 것 — 요약" 표, 당시의 상세 나열은 [보존본](development-history-v0.8.md) 1절. P1 제한 수용, P2 완료, P3 완료(제한 있음), **P4 완료(제한 있음, S-034) + P4-09 정리 묶음(S-035)**, UI 대화 중심 UI 완료(제한 있음) + **UI-05a(S-036)** |
| 현재 단계 | **UI-05 — 이슈 #7 관리 화면 정리**: **UI-05a 완료(S-036)** — 전수 대조(결과 2절)·`작업` 탭(작업 그래프 수정·실행 상세)·QG-02~07 진행기 자동 검토·수정(D-94). P4 는 `DONE_WITH_LIMITATIONS`(S-034) + P4-09(S-035). **다음: P4-10(이슈 #8·#9) → UI-05b → P5** |
| 다음 하위 작업 | **P4-10 — 이슈 #8·#9 진행기 보강**(사용자 결정 2026-09-24, S-036: 다음 세션·UI-05b 앞, 한 plan `plans/P4-PLAN-10.md` — #8 실행 제한 시간의 설정화(기본 **1시간**, D-95)·시간 초과를 실패와 구분(Runner `stop_reason`·적용 제한 기록)·같은 제한으로 자동 재시도하지 않고 '제한 시간 늘려 다시 시도' 카드; #9 검증이 기준 `not_met` 을 근거와 함께 보고하면 진행기가 수정 구현 → 재검증 Task 를 작업 그래프 새 리비전으로 더해 한도 안에서 돌림(`unverified` 는 사람 카드), 한도 뒤 사람 카드) — 착수 세션이 plan 을 기록·공유한 뒤 시작. 그 뒤 **UI-05b — 이슈 #7 관리 화면(옛 UI) 정리 2단계**([UI-05a 결과](ui/evidence/UI-05a-results.md) 2.3절의 나머지 옮김 + **실행 중 Case 의 취소 2단계**(사용자 결정 2026-09-24: UI-05b 에 붙임) + 링크·문구 제거 + `?view=admin` 삭제(또는 개발자 전용 명시) + 시험 5곳 대체 + 문서 → 이슈 #7 닫기), 그 뒤 **P5-01**(7절) |
| 활성 plan | **없다.** [UI-PLAN-05a](plans/UI-PLAN-05a.md)는 완료([UI-05a 결과](ui/evidence/UI-05a-results.md)). 다음 작업(P4-10)의 plan `plans/P4-PLAN-10.md` 를 착수 세션이 새로 기록·공유한다(그 뒤 UI-05b 의 `plans/UI-PLAN-05b.md`). 완료된 plan을 다시 열지 않음 |
| 다음 순서 | ~~UI-01~~ → ~~P4-04~~ → ~~UI-02~~ → ~~UI-03~~ → ~~P4-05~~ → ~~P4-05b+P4-06~~ → ~~P4-06b~~ → ~~P4-07~~ → ~~UI-04a~~ → ~~P4-07b~~ → ~~UI-04b~~ → ~~UI-04c~~ → ~~UI-04d~~ → ~~UI-04 완료 확인·P4 수용 확인~~(S-033) → ~~P4-08·P4 종료~~(S-034) → ~~P4-09 정리 묶음~~(S-035) → ~~UI-05a~~(S-036) → P4-10(이슈 #8·#9 진행기 보강) → UI-05b(이슈 #7 관리 화면 정리 2단계) → P5 → P6. 단계 안에서도 현재 작업 하나씩 계획·검증. 스킵 경로 C는 관련 예산 라이브가 필요해질 때 다시 검토 |
| 마지막 제품 검증 | **pytest 795 통과·0 실패·2 건너뜀(14분 45초) + P1 계약 unittest 18 통과 + 웹 단위 37 통과 + 웹 빌드**(**S-036 최종**, `scripts\run-tests.ps1` 종료 코드 0 "전체 시험 통과"; 실행 수 **+10** = `test_work_graph_edit.py` 3 · `test_quality_gate_progress.py` 5 · `test_web_shell.py` +2(실제 Edge·가짜 codex)). S-036 기준선은 785·0·2(14분 29초, 변경 전). 스키마 **v28** 그대로. 이전 수치(S-035: 785·0·2)는 당시 사실로 보존. **실제 codex 라이브 하나**([UI-05a 결과](ui/evidence/UI-05a-results.md) 3절 — QG-04 명시 ON: 검증 앞 검토 셋을 진행기가 스스로 만들고 판정 기록, 자동 완료; passed · 규칙 15/15 · 관찰 19) |
| 이 문서 개정 시작 상태 | S-036 시작 시 `main`/`47c388d` clean·`origin/main` 과 같음(fetch 로 확인). 종료 시 사용자 지시로 S-036 의 변경을 한 커밋으로 만들고 인계 기록을 이어서 커밋해 함께 push 했다(push 전에 fetch 로 원격 확인, 10절 S-036) |
| 남은 상세 | **UI-05a 가 넘긴 것은 [UI-05a 결과](ui/evidence/UI-05a-results.md) 2.3절(UI-05b 몫)·5절**(QG-05~07 명시 ON 은 실행 조건 아님·통과 뒤 대상 변경의 재검토는 P4-02 변경 사건 몫·QG-04 repair 는 기다리는 구현 Task 하나). P4-09 가 남긴 것은 [P4-09 결과](p4/evidence/P4-09-results.md) 4절(실행 중 취소 2단계 → UI-05b·되묻는 답·직전 이전 Case 하나·Claude 쓰기 실행 미실증·읽기 전용 실행의 도구 문제). UI-04 가 넘긴 것은 [UI-04 결과](ui/evidence/UI-04-results.md) 4절, P4 의 남은 한계는 [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md) 6절, 검증 환경은 [P4-ENV 결과](p4/evidence/P4-ENV-results.md). 파일별 포함 선택·본문 검색 색인은 필요할 때 별도 plan. 분할 검토·QG-08 독립 검토·같은 조사 Profile 안의 목적 추가 뒤 분석 재실행·읽기 전용 실행 전의 외부 변경 관측은 필요한 작업의 별도 plan. **여러 Runner 의 배정 경로는 P6-01**(9절). 경로 C 스킵·시험 기능 A·미기록 Autonomy의 controlled 취급은 유지 |
| 이번 허용 범위 | S-036: **UI-05a 의 plan·구현·검증·라이브·인계**(사용자 요청 "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan·검증·인계 절차를 지켜줘." — 안내서의 요청문이 UI-05a 를 지정). 코드·시험·지시문·라이브 스크립트·문서 변경(스키마 무변경). 사용자에게 물어 답을 받았다(QG-02~07 의 길 → D-94, 취소 2단계 → UI-05b, 세션 중 등록된 이슈 #8·#9 → 다음 세션 P4-10, 실행 제한 시간 기본값 → 1시간 D-95). 라이브 통과 뒤 사용자 지시("라이브가 문제없이 끝나면 문서 정리 잘 하고 커밋푸시까지해줘.")로 **변경 커밋·인계 기록 커밋·`origin/main` push**. 이 허용은 그 두 커밋에만 적용된다. PR·이슈 댓글·닫기는 하지 않았다(허용받지 않음). 제품의 실제 외부 게시는 여전히 P5다 |

### 구현된 것 — 요약

각 작업의 설명 문단은 [보존본](development-history-v0.8.md) 1절, 상세는 결과 문서에 있다.

| 작업 | 지금 제품이 하는 일 | 근거 |
|---|---|---|
| P3-R1~R4 | 정책·Profile·영속 모델(R1), Case×Repo 작업공간과 선택·쓰기 허용 강제(R2), 원자적 예산 예약·정산·hard 정지(R3), Autonomy 기반 진입·기본 자동 완료·controlled 확인·누적 material delta(R4) | [R1](p3/evidence/P3-R1-results.md)·[R2](p3/evidence/P3-R2-results.md)·[R3](p3/evidence/P3-R3-results.md)·[R4](p3/evidence/P3-R4-results.md) |
| P3-04 | 수직 통합 경로 A·B 확인(두 저장소 기능, controlled 대표 경로, Task×Repository v12). 경로 C(예산 라이브)는 사용자 지시로 스킵 | [P3-04](p3/evidence/P3-04-results.md) |
| P4-01 | QG-01~07 선택·명시 ON 게이트의 진입 조건, 조건부 독립 검토(별도 reviewer 세션), 초기 실패 뒤 기본 2회 repair 누적 | [P4-01](p4/evidence/P4-01-results.md) |
| P4-02 | 검증 1회 경계, 설정 변경 예약(진행 중 검사 무효화 없음), 강도 변경만 재검증·참조 대조 부분 재검증, 늦은 결과, 배정 직전 재확인 | [P4-02](p4/evidence/P4-02-results.md) |
| P4-03 | Profile 정의 v2 완료 계약 — 목적 의무·충족 방식 필수(생략 우회 차단), 결론 요구, 혼합 목적, 실험 근거·정리 | [P4-03](p4/evidence/P4-03-results.md) |
| UI-01 | 준비 단계 Case, 대화 메시지·정정·참조, 현재 요청과 서버 잠금, 멱등 접수, 카드 답변 한정, 논의 응답, 최초 업무화 | [UI-01](ui/evidence/UI-01-results.md) |
| P4-04 | 고정 문맥 등급(핵심/보조)·인라인 한도, Runner 영수증(해시 대조), 핵심 누락·초과 보류, 시작하지 않은 실행 소비 0, 결과 시점 최신성, 재시작 원시 출력 사용량 복구 | [P4-04](p4/evidence/P4-04-results.md) |
| UI-03 | 기본 대화 화면(`/`, 왼쪽 목록·가운데 대화·필요할 때 여는 검토 패널, 라이트 기본, 패널·테마·마지막 대화 기억), 제어부 요청 처리기(저장된 요청 → 읽기 전용 논의 응답 → 요청 종료, 기동 복구, 설정으로 끔), 준비 단계 응답의 AI 해석과 같은 Case 업무화(위임 근거 = 사용자 메시지, 해석 기록 v19), 본문은 열람 중계·탭 메모리·미연결이면 "연결 필요", 브라우저 초안(접수 확인된 스냅샷만 비움, 자동 전송 없음), 결과물 목록·열람 버전 고정·새 버전 안내·줄 비교·`대화에 참조`, 질문 카드, 기본값 요약, 기존 접수 네 경로의 기록 전 중계. 업무 단계 자동 진행은 미지원 표시 | [UI-03](ui/evidence/UI-03-results.md) |
| P4-05 | 업무 단계 진행기(업무화 뒤 의도 초안→QG-01·repair→질문·동의→controlled 시작→결합 기록/설계·계획→작업 그래프 순서의 조사·구현·검증→기준 판정→자동 완료/controlled 결과/예외 수용, 진입 검사 그대로, 사람·일시·환경 사유 구분), 실행 사이 요청 잠금 유지와 사람 대기·완료·막힘의 요청 종료(사유 코드), 사람 결정 뒤의 진행 요청(여는 메시지 없음), 검증·분석 실행의 기준 보고를 구조 검사로 기록, 확인 카드(의도 동의·controlled·delta·검토·피드백·예외+인수·다시 시도·계속 진행), 종료 Case 설명 전용 진입·종료 snapshot·종료 뒤 소비 구분·예산 변경 이력, 후속 Case 로 메시지 옮김, `HADS_AUTO_PROGRESS_WORK` | [P4-05](p4/evidence/P4-05-results.md) |
| P4-05b | 진행기의 재작성(`repair_limit`, 기본 2 — 다시 쓴 횟수)·재시도(`task_retry_limit`, 기본 1) 상한을 설정으로: 환경 변수 `HADS_REPAIR_LIMIT`·`HADS_TASK_RETRY_LIMIT`(0~10, 이상하면 기동 실패) + Case 별 이력(`progress_limit_setting`, 종료 Case 거부), 조회의 값·출처, `PUT /progress/limits` 뒤 진행기가 한 번 더 감, 상한 대기 카드 "재시도 1/1"·"한도를 올리고 계속", 관리 화면 진행 상한 절. 준비 산출물 재작성이 plan 대로 2회가 됐다 | [P4-05b](p4/evidence/P4-05b-results.md) |
| P4-06 | 지식 등록부(항목 `K-001`·버전·종류·필수/참고·후보/활성/대체/무효·범위(Project/저장소+경로)·활동·권위·출처), 수동 등록·개정·활성화·무효화·충돌 API, **대화에서 말한 프로젝트 규칙의 자동 활성 등록**(사용자 발언 권위, 등록 카드·무효화), 활동+범위 자동 주입(필수·권위 원문 = 핵심, 참고·후보 = 보조, 기존 참조 뒤), 실행별 Manifest(제공·한도 생략·활동/저장소 비적용·범위 미확정), 적용 필수의 미해결 충돌 → `knowledge_conflict` 대기, 관리 화면 지식 패널 | [P4-06](p4/evidence/P4-06-results.md) |
| P4-07 | **선택적 지식 추출**: 검증·분석·실험·구현 실행의 지시문에 후보 규칙(계기 목록·최대 3건·근거 한 줄·일반화 금지)이 붙고 응답 뒤의 `hads-knowledge` 블록을 Runner 가 떼어 원문을 서버에 올린 뒤 결과와 함께 보고, 제어부가 결과 뒤 훅에서 **후보(`ai_proposal`)로만** 등록(같은 원문·`supports` → 기존 항목의 근거 행, `supersedes`·`contradicts` → 관계를 가진 새 후보, 대상 사슬 그대로), 논의 응답의 `proposal: true` 항목도 후보(권위 메시지 없음), 후보 버전의 관측 문맥(실행·저장소·기준 커밋·도구)과 주입 머리 표시, QG-08 채택 확인 `adoption_check`(막음/경고)·`GET adoption-check`·활성화의 축소·`into_knowledge_id`·`adoption` 기록·후보 버전 대체 닫기, 새 화면 후보·제안·근거 카드, 관리 화면 채택 확인·활성화 폼, 가짜 codex 표지 둘, v24(`knowledge_evidence`·버전 컬럼 넷·`knowledge_intake` 재구성). **참고 후보 자동 활성화·독립 AI 검토·별도 추출 실행은 없다** | [P4-07](p4/evidence/P4-07-results.md) |
| P4-06b | **지식 원문의 서버 저장**(사용자 결정 2026-09-24, D-67 보충 — 원문 PC 경계의 유일한 예외): 적용 내용(4,000 자 상한)과 자동 등록 규칙의 권위 사용자 메시지 한 건을 `knowledge_body` 에 저장. 수동 등록은 중계 없이 저장(바로 `available`, PC 미연결이어도), 자동 등록은 Runner 가 결과 전에 원문·권위 메시지를 올림(하나도 등록되지 않으면 메시지 본문 삭제), 배정이 본문을 실어 Runner 가 해시가 맞을 때만 씀(`hash_mismatch` 면 사본으로 대신하지 않음), 옛 지식은 소유 PC 연결 시 자동 이행(`storage = runner → server`), 열람은 서버가 채움, 조회의 `storage`·`source_storage`·`body_source`, 패널·카드·등록 규칙의 "서버 저장 · 비밀값 금지", 패널 "내용 보기" | [P4-06b](p4/evidence/P4-06b-results.md) |
| UI-02 | Runner 제어 루프/실행 작업자 분리, CLI 트리의 Windows job 제어(일시 정지로 만들어 넣고 원장 기록 뒤 재개, 중단·잔류 종료·활성 0 확인, Runner 사망 시 OS 종료), 잔류 확인 근거·재확인, 요청 중단과 `interrupted`, `unknown` 해제, 원장 v2 재시작 대조(재실행·새 세대 없음), 보고 재시도, PC 미연결 전송 거부, 실행별 실제 상태 | [UI-02](ui/evidence/UI-02-results.md) |
| P4-07b | **참고 후보의 자동 활성 조건 표시 — 반자동**(사용자 결정 2026-09-24): 순수 함수 `auto_reference_check` 가 조건 여덟(후보 · 효력 참고 · 종류 운영 사실/알려진 문제 · 서로 다른 실행 둘 이상의 근거(출처 실행 + `supports`/`duplicate` 근거 행, 실행 id 로 중복 제거) · 그중 하나는 종료 코드 0 명령이 있는 검증/분석 실행(명령 기록 — 자기보고) · 관측한 저장소 범위 · 반증 관계/반증 후보/열린 충돌 없음 · 채택 확인 막음 없음)의 충족/미충족을 낸다. 조회(`knowledge_view` 후보 항목·등록 카드 행)의 `auto_reference{ready, met, unmet, findings, evidence_runs}`, `GET /api/knowledge/{id}/auto-reference`. **한 번에 활성화** `POST /api/projects/{id}/knowledge/activate-ready`(화면이 본 `knowledge_ids` 만; 각 후보를 조건 재계산 → `activate_knowledge` 효력 참고·범위 그대로·사유 "자동 활성 조건 충족 — 사람이 한 번에 활성화"·`adoption.by`·`adoption.auto_reference{met, evidence_runs}`; 미충족·거부는 건너뛰어 결과에, 200). 규칙 화면의 배지·조건 목록·후보 절 머리의 버튼, 후보 카드·관리 화면 패널의 배지. **클릭 없는 활성화 없음·필수 승격 없음·스키마 v24 그대로.** 논의 응답의 제안(관측 없음)은 조건 (4)를 만족하지 못한다 | [P4-07b](p4/evidence/P4-07b-results.md) |
| UI-04a | **결정 사항 패널**(오른쪽, D-80): 업무·목표·기준(의도 최신 버전·동의 상태·`원문 열기`·기준 판정)·결정(종류를 사람 말로·대상 의도 버전 열기·근거 메시지로)·이 대화에서 정한 규칙·후보(사용자 말/AI 제안/실행 후보/근거/거부, 지금 상태, `메시지 #n`, 프로젝트 규칙으로)·이 업무에 적용된 프로젝트 규칙(`knowledge-use` — Manifest 버전별 집계, "주입은 준수의 증거가 아니다")·답한 질문·AI 해석·진행 이력. **프로젝트 규칙 화면**(`?screen=rules`, 왼쪽 목록): 활성 필수/참고·후보·대체/무효 이력, 항목의 범위·활동·권위·저장 위치·관측·관계·근거·채택 기록·이력·출처(대화 제목·메시지 순번 → `&seq=N` 강조)·내용 보기, 채택 확인·활성화(효력·활동·저장소 축소·`into`)·개정·무효·충돌 기록/해소·수동 등록(출처 대화 선택). 카드 링크가 규칙 화면으로. 서버 조회에 출처 제목·순번·집계만 더함(본문 없음, 스키마 v24 그대로) | [UI-04a](ui/evidence/UI-04a-results.md) |
| UI-04b | **프로젝트 설정·상세 설정 화면 + 알림**(D-72·D-82, 사용자 동의 2026-09-24): **프로젝트 기본값 층**(스키마 **v25** `project_setting` — 기본 도구·Autonomy·기본 예산·진행 상한·인라인 한도의 이력; 순수 규칙 `domain/project_settings.py`, 강제 불가한 hard 예산 거부, `AutonomySource.PROJECT_DEFAULT`), 적용 시점 = **그 뒤에 만든 대화**(첫 정책 행 `project_default`·예산 행 `set_by = project_default`)·**새 실행**(`run.context_inline_limit`), 진행 상한은 Case 명시 → 프로젝트 → 시스템(조회 때 계산), 기존 기록 소급 없음. `GET/PUT /api/projects/{id}/settings`(`null` = 복귀), Case 복귀 `POST /autonomy/reset`(새 리비전)·`DELETE /progress/limits/{key}`·`POST /budget/project-defaults`. **프로젝트 설정 화면**(`?screen=settings`, 탭 기본값·프로젝트 규칙(UI-04a 화면 이동, 주소 `?screen=rules` 그대로)·저장소; 모델·깊이는 기본값 없음 표시, 도구를 확인한 PC). **대화의 상세 설정**(오른쪽 패널 `설정` 탭, 입력창 요약에서 열기): 적용 값·출처·적용 시각, Autonomy 변경·복귀, 예산 설정·해제·프로젝트 기본값 적용, 진행 상한 변경·복귀, 수준 조정, 저장소 선택(허용 명시), QG-02~07 설정(사유 필수). **브라우저 알림**: 조회의 전이(답변 필요·확인 필요·막힘·실행 상태 확인 필요·완료·다른 프로젝트 주의 증가), 일상 진행·첫 조회 제외, 보고 있는 대화 억제, 화면 안 기록, 켬/끔은 브라우저 설정. 설정은 권한·동의·인수가 아니다 | [UI-04b](ui/evidence/UI-04b-results.md) |
| UI-04c | **활성 목적·Profile 이행(D-86) + 실행시간 합계(D-88) + 외부 편집·작업 PC 열기(D-89)**(사용자 결정 2026-09-24 두 단계 묶음의 1단계): 스키마 **v26** — `case_profile_revision`(개정 이력)·`workspace_open_request`(열기 요청)·`intent_version.profile/profile_version/retained_fields_json`(버전별 Profile·유지 항목, 옛 행은 Case 값으로 채움)·`conversation_interpretation` 재구성(`profile_change`·`objectives_json`). **D-86**: `revise_profile`(거부: 준비 단계·v1/미기록·같은 Profile 에 추가 목적 없음·끝나지 않은 실행·종료) → Case 행의 Profile·kind·출처 `revised` + 개정 행(유지 항목 = 이전 Profile 의미 항목 누적, 유지 목적 = 이전 필수 의무 누적, 추가 목적); 새 의도 버전의 항목 = 현재 Profile + 유지 항목(`retained_fields`, 문서 v6), 유지 항목 기준의 의무는 그 항목을 가진 Profile 의 것(`derive_obligation(version=)`), 같은 키·같은 지문의 판정은 승계(`carried_from`, P4-02 그대로), 유지 목적은 `objectives_json` 에 합쳐져 P4-03 계약이 양쪽을 요구; `intent_state.profile_stale` → 진행기 `intent_revision` 실행(직전 원문·유지 항목·유지 목적·직전 기준을 받음) → 기존 의도 단계(QG-01·delta·**동의**); 경로 둘 — `POST /api/cases/{id}/profile-revisions`(사람, 사유 필수, 결정 사항 패널의 개정 폼)과 업무 단계 논의 응답의 해석 `profile_change`(근거 = 사용자 메시지, 이전 목적 항상 유지, 처리기가 `revise_profile`); 조회 `GET .../profile-revisions`(대응 목록 승계/재검사/대상 없음), `conversation_view.profile_revisions`, 타임라인 표식. 위임 근거·예약·시도 수·확인 지점·저장소 선택·이전 버전·기준·판정·결정은 그대로. **D-88**: 지표 그대로(`execution_seconds` = 실행별 배정~종료 합계, `elapsed_seconds` = 경과) — 화면의 지표 이름표·설명(`lib/budget.ts`), 상세 설정의 시간 절, "시간 한도 추가" 기본 = 실행시간 합계; `bump_generation` 이 옛 세대 예약을 `unresolved`/`reassigned` 로 닫고 `_metric_exposure` 가 옛 DB 의 열린 옛 세대 행에 시간을 더하지 않음. **D-89**: Runner 데스크톱 능력 행(`runner-host/desktop`: `open_folder` = Windows `os.startfile`, `open_editor` = PATH 의 `code`, `HADS_RUNNER_DESKTOP=off` 로 끔), `POST /api/cases/{id}/workspaces/{repo}/open`(준비된 작업공간·연결·`verified` 만, 아니면 409) → heartbeat 제어 `open_requests` → Runner 가 **자기 worktree 만** 열고 `POST /api/runner/open-requests/{id}/result`; 60초 미전달 만료; 배정의 `workspace.last_tree_digest` 와 Runner 의 실행 전 대조 `external_change_before_run`(쓰기 실행), 조회의 `unexpected_external_change` 가 지문도 비교; 결과물 패널의 작업공간 절(작업 PC = 작업공간을 만든 Runner·연결·경로·복사·열기·지원 상태·요청 결과·외부 변경 표시). 개정·열기는 권한·동의·인수가 아니다 | [UI-04c](ui/evidence/UI-04c-results.md) |
| UI-04d | **대화 검색(D-84) + 미커밋 포함 시작(D-77)**(사용자 결정 2026-09-24 두 단계 묶음의 2단계, UI-04 의 마지막 하위 plan): **D-84** — 왼쪽 검색 폼 → 검색 화면(`?screen=search`, 검색어는 주소에 없음). 서버 필드(제목·메시지 요약·업무화 요약·결정 종류·Profile 개정 사유·이 대화에서 정한 규칙 요약)는 제어부가 즉시 대조(낱말 AND·대소문자 무시, `domain/search.py` 를 제어부·Runner 가 공용), **본문은 소유 PC 가 제어 루프에서 후보 원문만 읽어**(경로 없음, 후보 밖 안 읽음) 일치(순번·발췌·수)를 올린다. 검색 요청·검색어·발췌는 제어부 **메모리에만**(`controller/transient.py`·`controller/search.py` — TTL 10분·PC 무응답 60초 `expired`·재시작 404, **DB 에 검색 표가 없다**). 범위를 값으로: 미연결 PC 의 본문 수 `excluded`("PC 미연결로 본문 N건 검색 제외"), 후보 상한(2,000건·최신순) `truncated`, 읽지 못한 원문 `unreadable`. 보관된 대화 포함, 다른 프로젝트 제외. 결과 → 메시지(`seq` 강조)·결정 사항 패널·규칙 화면. API `POST /api/projects/{id}/conversation-searches`·`GET /api/conversation-searches/{id}`·Runner `search-requests`/`results`. **D-77** — 스키마 **v27**(`case_workspace` 컬럼 여덟: `start_basis`·`basis_decided_by/at`·`committed_base`·`included_entries`·`included_tree_digest`·`basis_tree_digest`·`basis_entries`; 옛 행 NULL = 기록 없음). 작업 PC 가 원래 폴더에 커밋하지 않은 변경을 보면 **만들지 않고 묻는다**(`WorkspaceState.AWAITING_BASIS`, `NeedsBasis`) — 목록(상태·경로)은 메모리 중계(`app.state.uncommitted`, TTL 30분), 행에는 수·지문·HEAD. 진행기 `WaitReason.WORKSPACE_START_BASIS` 사람 대기 카드(목록·기준 커밋·`포함해서 시작`/`커밋된 코드에서 시작`, 이진). 깨끗한 트리는 묻지 않고 HEAD 에서. 포함 = **스냅샷 커밋**(`snapshot_commit` — 임시 인덱스 `read-tree HEAD`→`add -A`→`write-tree`→`commit-tree -p HEAD`, 원래 트리·인덱스·브랜치·HEAD 무변경) 위에서 `worktree add`; `base_commit` = 스냅샷(실행 효과·누적 수·코드 조합의 기준), `committed_base` = HEAD. 동시 편집 보호 = 지문 대조(`seen_digest` ≠ 행 지문 → 409 `basis_list_stale`; 포함 직전 다시 관측해 다르면 새 목록으로 재질문·선택 지움). 포함은 HEAD 기준에서만(다른 `base_ref` 와 함께면 거부). `awaiting_basis` 는 준비되지 않음 → 쓰기 실행 `workspace_not_ready`(진입 검사 무변경). API `POST /api/runner/workspaces/{case}/uncommitted`·`GET /api/cases/{id}/workspaces/{repo}/uncommitted`·`POST .../start-basis`, 진행기 `on_start_basis_decided`. 작업공간 절의 시작 코드 줄. 시험 도구: 가짜 CLI 가 쓰기 권한 실행에서만 파일을 쓰고 `Harness.prepare_workspace(basis="committed")` 가 선택을 대신 보낸다. **파일별 선택·후속 Case 기준 제안·화면의 ref 변경·색인은 없다.** 검색·선택은 권한·동의·인수가 아니다 | [UI-04d](ui/evidence/UI-04d-results.md) |
| P4-09 | **P5 전 정리 묶음**(사용자 결정 2026-09-24, 한 세션·한 plan; 스키마 **v28**): **(a)** 이슈 #2 — 설계·계획으로 이월했다가 답한 질문의 답 원문이 그 질문이 막던 Task 의 구현·검증·실험·분석 실행에 `question_answer`(핵심) 참조로 들어가고 지시문 머리에 질문 키·요약이 적힌다(`answered_deferred_questions_for_task`; 연결 없이 전부를 막던 질문은 모든 Task 에, Task 키 없는 실행은 전부; 재시도도 같은 입력). **(b)** 이슈 #3(D-91) — `PERMISSION_MAP` 의 `workspace_write` 가 codex `danger-full-access`·claude `bypassPermissions`(+`--strict-mcp-config` 유지), `read_only` 그대로; 능력 `write_sandbox = unsupported`. **(c)** D-77 마지막 문장 — 후속 Case 의 작업공간 요청에 이전 Case(가장 최근 `follow_up_change`)의 같은 저장소 브랜치가 실리고 작업 PC 가 그 끝 커밋이 HEAD 에 없을 때(`previous_result_tip`) 깨끗한 트리여도 묻는다(`awaiting_basis`, 행에 `previous_case_id/branch/commit`); `previous_result` 선택 → `base_ref` = 그 커밋, worktree 는 그 커밋에서(`base_commit`), `committed_base` = 그때의 HEAD, 미커밋 포함 없음; 카드가 이전 대화 제목·브랜치·커밋과 `이전 업무 결과에서 시작` 을 보이고 미커밋 0건이면 목록·포함 버튼을 숨긴다. **(e)** 이슈 #4 — `POST /api/cases/{id}/cancel`(사유 필수; `status ≠ finished` 실행이 있으면 409 `runs_unfinished`, 종료 Case 409): 결정 `case_cancellation`·후보 없는 `closure_record(cancelled)`·`status = cancelled`·처리 중 요청 `failed(case_cancelled)`·진행 `done/cancelled`·열린 예약 `unresolved`; 부분 결과·작업공간 보존, 되돌리지 않음(재개 거부), 취소 뒤 진행기·후보 등록 훅 무반응; 머리 `업무 취소`·"취소됨" 표시. **(f)** 이슈 #5(D-92) — AI 후보(추출·`proposal: true`)의 틀린 활동·경로는 `reported_scope` 로 보존한 채 후보 등록(주입 제외 `scope_unreadable`, 채택 확인 막음 → 활동 명시로 해소, 활성화 새 버전엔 없음), 사용자 말의 자동 등록은 거부 유지; `GET /api/cases/{id}/knowledge-intake/{run}/{i}` 열람(본문·AI 가 적은 범위·사유 읽을 말), 카드·패널의 `내용 보기`·`이 내용으로 수동 등록`(규칙 화면 폼 채우기), 지시문에 활동 여덟·경로 규칙·"모르면 비워 둔다". **(g)** 이슈 #6(D-93) — 해석 블록 `title`(40자) → 첫 응답 자동 제목(`ai:discussion`) → 업무 요청 응답의 한 번 갱신(`ai:work_start`), 사람 제목(`user`) 불변; `PUT /api/cases/{id}/title`(1~200자, 종료·보관 허용, 직전 값·주체·시각), 머리 클릭 편집·목록 ✎; `case.title_source` 등 넷. **(d)** 실제 codex 라이브 — RCA·maintenance 의도 작성 표본(`p4/live/profile_intents.py --only D,E`)과 진행 라이브(`HADS_LIVE_PREFIX=P4-09-live-progress`). 어느 부분도 권한·동의·인수를 만들지 않았다 | [P4-09](p4/evidence/P4-09-results.md) |
| UI-05a | **이슈 #7 관리 화면 정리 1단계**(사용자 결정 2026-09-24): 관리 화면 기능의 전수 대조 분류(결정 기록). 새 화면 오른쪽 패널 **`작업` 탭** — 작업 그래프(작업·막힌 이유·기준 대응·기다리는 결정·연결 없는 질문 경고)와 Task 추가·취소·**질문↔Task 연결 고치기**(이유 필수, 수정 뒤 진행기가 이어 감, 종료 업무 거부)·**실행 상세**(고정 입력 참조·영수증·문맥·진입 검사·이벤트·명령·작업공간 효과·출력 열기). 대기 카드·실행 id·진행 이력·이월 질문 카드에서 그리로 간다. **D-94**: 명시로 켠 QG-02(계획·구현 전)·QG-03(구현 전)·QG-04(검증 전)가 막으면 진행기가 별도 세션 검토(`quality_gate_review`, 작업 id `qg:<게이트>:<Task>`) → 발견을 검증 1회의 판정으로 기록 → 실패면 지적을 실은 repair(설계·계획 재작성·구현 다시, 게이트 수정 한도) → 다시 검토 → 한도 뒤에만 `quality_gate` 카드(한도 올림·재시도 한도 올림·끄기, 사유). 결합 기록 경로는 계획 작성에 QG-02 를 요구하지 않는다(구현 전에 결합 기록을 본다). 스키마 무변경 | [UI-05a](ui/evidence/UI-05a-results.md) |

**계속 유효한 결정·규칙** — 옮긴 문단에서 지금도 적용되는 것만 남긴다.

- **강제 축은 넷이다.** 조회 `enforcement` 에서 `autonomy`·`controlled_checkpoint`·`budget`·`repository_selection` 이 `enforced`, 게시 허용과 실제 push·PR 은 P5 다. 결과 후보 확인도 게시 권한을 만들지 않는다(D-65). 이후 작업(P4-01~05·UI-01~03)은 새 강제 축을 만들지 않았다. UI-03의 AI 해석도 권한이 아니다 — 업무화의 위임 근거는 사용자 메시지 원문이다. P4-05의 진행기·기준 기록도 권한·동의·인수를 만들지 않는다. P4-05b의 한도 올리기와 P4-06의 지식 등록·활성화·주입도 권한·인수가 아니며, 주입은 준수의 증거가 아니다. P4-06b 의 서버 저장·이행·해시 대조 영수증도 "제공했다"일 뿐 권한·준수가 아니다. P4-07 의 후보·근거·채택 확인도 권한·동의·인수가 아니며 활성화는 사람의 결정(새 버전)이다 — AI 제안은 어떤 경로로도 활성 필수가 되지 않는다. P4-07b 의 자동 활성 조건 충족은 표시일 뿐이고 한 번에 활성화는 사람의 클릭(참고 효력만)이다. UI-04b 의 프로젝트 기본값·Case 조정·복귀는 설정이며 권한·동의·인수·준수가 아니다 — 적용 시점은 그 뒤의 대화·실행이고 기존 기록을 바꾸지 않는다. UI-04c 의 Profile 개정도 권한이 아니다 — 위임 근거·예약·시도 수·확인 지점·저장소 선택은 그대로이고 새 의도 버전은 검토·동의를 다시 지나며 제품 쓰기는 진입 검사가 그대로 본다(목적 선언·개정만으로 조사 Profile 의 쓰기 가드가 풀리지 않는다). 작업 PC 열기는 쓰기 허용이 아니다. UI-04d 의 검색과 시작 기준 선택도 권한이 아니다 — 포함 선택은 그 저장소의 쓰기 허용을 만들지 않고(작업공간은 선택·허용 검사를 그대로 지난다) `awaiting_basis` 는 준비되지 않음이며 진입 검사는 바뀌지 않았다.
- **원문 PC 경계의 예외는 지식뿐이다**(사용자 결정 2026-09-24, P4-06b). `knowledge_body` 가 제어부 DB 의 유일한 본문 표이며 지식 원문과 자동 등록 규칙의 권위 사용자 메시지 한 건만 들어간다. 대화 전체·응답·의도·설계·계획·코드·로그는 여전히 PC 에만 있고 서버 로그에는 본문이 없다. 이 예외를 다른 원문으로 넓히는 것은 새 사용자 결정이다. **UI-04d 의 검색 발췌·검색어·미커밋 파일 목록은 예외가 아니다** — 제어부 프로세스 메모리에만 있고(열람 중계와 같은 자리) DB·로그에 없으며 재시작·TTL 에 사라진다(시험이 DB 의 모든 표를 읽어 확인한다).
- **`autonomy = NULL`(R1 이전 Case)은 사용자 결정(2026-09-22)으로 controlled 로 취급한다.** 저장값은 `NULL`·`migrated_unknown` 그대로이고 도출만 바뀐다. 그 Case 의 단계 검토 기본값은 v0.6(사람 검토)이다.
- **새 기본값을 기존 Case 에 소급하지 않는다.** v0.6 의 완료(사람 인수)·단계 검토(사람 검토) 기본값이 조건 충족 자동·자동 진행으로 뒤집혔지만 명시 설정과 Autonomy 미기록 Case 가 경계다. 기존 Case 는 Profile 정의 v1, `stage` NULL(업무 단계로 도출), P4-04 이전 실행은 영수증 없음(`not_reported`)으로 남는다.
- **예산 강제의 약속은 지표마다 다르다.** 실행 수·검토 수·문맥 크기는 `enforced_absolute`, 시간은 `enforced_no_absolute_cap`(진행 중 CLI 를 초 단위로 끊지 못함), 토큰·비용 hard 한도는 설정을 받지 않는다. 모르는 값을 0 으로 적지 않는다.
- **이전 계획·증거는 당시 사실이다.** 새 설계의 통과 증거로 재명명하지 않는다.
- **P4-09 도 새 권한을 만들지 않았다.** 이월 질문의 답은 입력 전달이고, 시작 기준 `previous_result` 는 코드의 선택이며(쓰기 허용·진입 검사는 그대로), 취소는 사람의 종료 결정이고(성공·예외 인수가 아니며 되돌리지 않는다), 후보의 관대한 받기는 후보로만이며 주입되지 않고, 제목은 표시값이다. **D-91 의 샌드박스 해제는 CLI 인자이지 제품의 게시 허용이 아니다** — AI 의 직접 push 를 막지 못한다는 사실은 사용자가 받아들인 손실로 D-91 에 적혀 있고 `publish` 권한·게시 규칙은 P5 그대로다.
- **UI-05a 도 새 권한을 만들지 않았다.** 그래프 수정은 기존 사람 재계획(FR-07)이고 질문 연결은 답이 아니며 실행 상세는 읽기다. 진행기의 게이트 검토는 읽기 전용 검토자 실행이고 repair 는 기존 목적의 실행으로 진입 검사를 그대로 거친다. **게이트 통과는 권한·인수가 아니고 게이트를 끄는 것은 통과가 아니다**(판정 기록은 남는다). 기본 적용(추천) 게이트는 검토 실행을 만들지 않는다 — 목적은 기존 구조 검사가 강제한다(D-94).

### 1.1 확정한 개발 순서와 작업 분할 — D-90

확정 설계는 [D-68~90](decisions.md)와 [대화 중심 UI](ui-conversation-design.md)에 모았다. 새 작업 ID는 개발 관리용 분할이며 구현 완료를 뜻하지 않는다. 각 행의 세부 범위·이행·시험은 해당 작업의 새 plan에서 구체화한다.

| 순서 | 하위 작업과 범위 | 다음 단계로 넘길 조건 |
|---|---|---|
| 1 | ~~**UI-01 대화·요청 기반**~~ **완료(2026-09-23)**: 준비 Case, 메시지·정정·버전 참조, 요청 처리 상태, 서버 전송 잠금과 접수 | 같은 Case의 준비→최초 업무화, 원문/기존 정책 보존, 중복·충돌 일반 전송 차단, 카드 답변의 대상 구분 검증 — [UI-01 결과](ui/evidence/UI-01-results.md) |
| 2 | ~~**P4-04 문맥·재개**~~ **완료(2026-09-23)**: 고정 입력의 등급·인라인 한도·영수증·핵심 누락 보류·시작하지 않은 실행·최신성·재시작 사용량 복구([P4-04 결과](p4/evidence/P4-04-results.md)). ~~**UI-02 입력·실행 제어**~~ **완료(2026-09-23)**: Runner 제어/실행 분리, 긴 실행 중 입력·카드 답변 저장, 트리 중단과 종료 확인, `unknown` 해제, 재시작 대조, PC 미연결 거부, 관리 화면 중단 버튼([UI-02 결과](ui/evidence/UI-02-results.md)) | 긴 실행 중 제어 수신, 실제 종료·잔류 활동과 상태 불명의 구분, 재시작·중복 방지·소비 보존을 검증한 뒤 중단 UI 연결 |
| 3 | ~~**UI-03 기본 대화 화면**~~ **완료(2026-09-23)**: PC·라이트 기본 shell, 프로젝트/대화 선택→논의/요청(처리기·AI 해석 업무화)→진행/질문→실제 결과 검토, 기본값 요약·초안 복구·검토 버전 유지([UI-03 결과](ui/evidence/UI-03-results.md)). 업무 단계 자동 진행은 미지원 표시(1.6절) | 새 기본 흐름을 실제 사용해 검토. 없는 산출물/지원 능력을 표시하지 않고 API 상태·원문 가용성과 일치 |
| 4 | ~~**P4-05 업무 단계 자동 진행·완료·예외·후속**~~ **완료(2026-09-23)**([P4-05 결과](p4/evidence/P4-05-results.md)). ~~**P4-05b 진행 상한·P4-06 지식 관리·적용**~~ **완료(2026-09-24)**([P4-05b](p4/evidence/P4-05b-results.md)·[P4-06](p4/evidence/P4-06-results.md)). ~~**P4-06b 지식 원문 서버 저장**~~ **완료(2026-09-24, 사용자 결정)**([P4-06b](p4/evidence/P4-06b-results.md)). ~~**P4-07 선택적 지식 추출**~~ **완료(2026-09-24)**([P4-07](p4/evidence/P4-07-results.md)). ~~**P4-07b 참고 후보의 자동 활성 조건 표시(반자동)**~~ **완료(2026-09-24, 사용자 결정)**([P4-07b](p4/evidence/P4-07b-results.md)). **UI-04 남은 업무·화면 연결**: ~~결정 사항 패널·프로젝트 규칙 화면~~ **UI-04a 완료(2026-09-24)**([UI-04a 결과](ui/evidence/UI-04a-results.md)), ~~프로젝트 설정·상세 설정·알림~~ **UI-04b 완료(2026-09-24)**([UI-04b 결과](ui/evidence/UI-04b-results.md)), ~~활성 목적·Profile 이행(D-86)·실행시간 합계(D-88)·외부 편집·작업 PC 열기(D-89)~~ **UI-04c 완료(2026-09-24)**([UI-04c 결과](ui/evidence/UI-04c-results.md)), ~~대화 검색(D-84)·미커밋 포함 시작(D-77)~~ **UI-04d 완료(2026-09-24)**([UI-04d 결과](ui/evidence/UI-04d-results.md)). **네 하위 plan 이 끝났고 UI-04 완료 확인은 S-033 이 했다**([UI-04 결과](ui/evidence/UI-04-results.md) — 확정 범위 전부 구현·검증됨, 남은 것은 4절) | 기존 기준·증거·소비·원래 파일·권한을 보존하면서 각 흐름 검증. UI-04는 하위 plan(04a, 04b …)으로 나누어 완료 범위 추적 |
| 5 | **P5 GitHub**, **P6 운영·전체 수용**: 연결/이슈/외부 결과, 원격·인증·복구·설치·전체 수용 | 연결과 게시 허용을 구분하고 실제 외부 결과와 전체 FR/NFR/AC 근거 확인. 앞당긴 검증이 전체 P6를 대체하지 않음 |

기본 UI를 사용할 수 있는 시점을 앞당기되 제어 기능은 먼저 검증한다. 특정 기능을 UI-03에서 지원하지 않으면 미지원으로 표시하며 임시 성공 동작을 만들지 않는다. P4-04에 전체 UI·일반 중단·미커밋 포함 작업을 몰아넣지 않는다. P4-06~07의 남은 첫 버전 기능도 없애지 않는다.

현재 저장소 guard는 기록 repo의 코드 선택·작업공간을 거부한다. D-73은 별도 코드 허용이 있으면 겸용하도록 바꾼 목표이며 과거 P3-R2의 통과 증거를 새 규칙의 근거로 재명명하지 않는다. 이 변경은 P5 연결 전에 저장소 정책·선택·작업공간의 모든 경로를 함께 검증한다.

### 1.2~1.10·1.12 완료 작업의 경계와 시작점 (보존본으로 옮김)

UI-01(1.2)·P4-04(1.3)·UI-02(1.4)·UI-03(1.5)·P4-05(1.6)·P4-06(1.7)·P4-05b(1.8)·P4-06b(1.9)·P4-07(1.10)·P4-07b(1.12)의 당시 목표·넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 `plans/` 의 각 plan 과 결과 문서(1절 요약 표의 링크)다. 완료된 plan 은 다시 열지 않는다.

### 1.11 UI-04 완료 확인·P4 수용 확인(S-033) → P4-08·검증 환경 실험·P4 종료(S-034) → P4-09 정리 묶음(S-035) → UI-05a(S-036) — 끝; 다음은 P4-10 → UI-05b → P5-01

**UI-04(남은 업무·화면 연결)의 확정 범위**(D-90 표 4행, [UI 설계](ui-conversation-design.md)): ~~결정 사항 패널·프로젝트 규칙 화면~~(**UI-04a 완료**, [UI-PLAN-04a](plans/UI-PLAN-04a.md)·[UI-04a 결과](ui/evidence/UI-04a-results.md)), ~~프로젝트 설정·상세 설정·알림~~(**UI-04b 완료**, [UI-PLAN-04b](plans/UI-PLAN-04b.md)·[UI-04b 결과](ui/evidence/UI-04b-results.md)), ~~활성 목적·Profile 이행(D-86)·실행시간 합계 계측(D-88)·외부 편집·작업 PC 열기(D-89)~~(**UI-04c 완료**, [UI-PLAN-04c](plans/UI-PLAN-04c.md)·[UI-04c 결과](ui/evidence/UI-04c-results.md)), ~~대화 검색(D-84)·미커밋 포함 시작(D-77)~~(**UI-04d 완료**, [UI-PLAN-04d](plans/UI-PLAN-04d.md)·[UI-04d 결과](ui/evidence/UI-04d-results.md)). **네 하위 plan 이 끝났고 S-033 의 완료 확인이 확정 범위 전부를 구현·검증됨으로 판정했다**([UI-04 결과](ui/evidence/UI-04-results.md)).

- **S-033 이 UI-04 완료 확인과 P4 수용 확인을 한 번에 했다**(사용자 결정 2026-09-24, S-032; [P4-PLAN-ACCEPT](plans/P4-PLAN-ACCEPT.md)). UI-04: D-90 표 4행 네 항목 전부 구현·검증됨, UI 설계 13절 19 항목 중 16 구현·검증됨·3 부분(7·9 = P5 접점, 8 = 후속 Case 기준 제안 남음) — [UI-04 결과](ui/evidence/UI-04-results.md). P4: 완료 조건 네 축 중 혼합 목적 계약·실패/복구/변경·Case 간 지식은 완비, "여섯 Profile 대표" 는 계약 수준 여섯 전부·진행기 경유는 feature·research·RCA 셋 — 빠진 것(defect-fix·refactoring·maintenance 의 진행기 경유 대표 흐름 + 혼합 목적의 진행기 경유 완료)을 [P4-PLAN-08](plans/P4-PLAN-08.md)로 기록했다(READY, 미착수) — [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md). **사용자 결정(2026-09-24): (가)** — S-034 가 같은 대화에서 P4-PLAN-08 을 마쳤고([P4-08 결과](p4/evidence/P4-08-results.md) — `tests/test_profile_flows.py` 13건, 제품 코드 무변경·결함 없음), 검증 환경 실험([P4-ENV 결과](p4/evidence/P4-ENV-results.md) — 원인은 codex 의 Windows 제한 토큰 샌드박스가 사용자 프로필의 Python 을 읽지 못하는 것; 인터프리터 폴더에 `CodexSandboxUsers` 그룹의 읽기·실행을 주면 제품 기본 `--sandbox workspace-write` 에서 실제 codex 가 `python -m unittest` 를 통과시킨다 — codex 직접 호출 관찰)을 했으며, P4 를 `DONE_WITH_LIMITATIONS` 로 닫았다(4절). **다음 세션 = P4-09 정리 묶음**(사용자 결정 2026-09-24, S-034 보고 뒤 — 아래 항목, 한 세션에), 그 뒤 UI-05a·UI-05b(이슈 #7, 아래), 그 뒤 P5-01(7절). 사람에게 물을 것은 10절 S-034.
- ~~**P4-09 — P5 전 정리 묶음**~~ **완료(S-035, 2026-09-24)** — [P4-PLAN-09](plans/P4-PLAN-09.md)·[P4-09 결과](p4/evidence/P4-09-results.md). (a) 이슈 #2 이월 질문 답 전달, (b) 이슈 #3 샌드박스 해제(D-91), (c) 후속 Case 시작 기준(`previous_result`, v28), (e) 이슈 #4 업무 취소(1단계), (f) 이슈 #5 지식 후보의 거부·열람(D-92, v28), (g) 이슈 #6 대화 제목(D-93, v28), (d) 실제 codex 라이브(RCA·maintenance 의도 표본 — 지시대로 항목·의무·결론 요구를 적었고 maintenance 는 최상위 `objectives` 를 비워 둠; 진행 라이브는 결과 3절). 남은 것은 결과 4절(실행 중 취소 2단계·되묻는 답·직전 이전 Case 하나·Claude 쓰기 실행 미실증). 이슈 #2~#6 은 닫았다. **코드 대조 시작점:** (a) `controller/repository.py` `answered_deferred_questions_for_task`·`_compose_base_refs`(`task_key`)·`list_context_refs`(`question`), `runner/prompts.py` `question_head`, `tests/test_question_answers_to_tasks.py`; (b) `runner/cli_adapter.py` `PERMISSION_MAP`·`WRITE_SANDBOX_NOTE`, `tests/test_cli_adapter_permissions.py`; (c) `runner/workspace.py` `previous_result_tip`·`prepare(previous_branch=, previous_commit=)`, `Repository.previous_result_for_workspace`·`report_workspace_uncommitted(previous_*)`·`decide_start_basis`·`_previous_result_view`, `domain/models.py` `StartBasis.PREVIOUS_RESULT`, `web/src/shell/ProgressCards.tsx` `StartBasisCard`, `tests/test_start_basis.py` P4-09(c) 절; (e) `Repository.cancel_case`·`case_is_cancelled`·`cancellation_view`, `controller/api.py` `cancel_case`, `controller/work_progressor.py` `advance`·`on_run_finished` 의 취소 가드, `web/src/shell/ConversationView.tsx` `CancelButton`, `tests/test_case_cancel.py`; (f) `domain/knowledge.py` `parse_report_item(lenient_scope=)`·`REFUSAL_TEXT`·`ADOPTION_SCOPE_UNREADABLE`·`Decision.SCOPE_UNREADABLE`, `Repository.knowledge_intake_detail`·`register_knowledge(reported_scope=)`, `web/src/lib/knowledgeText.ts`·`ProgressCards.tsx` `IntakeDetailView`·`ProjectRules.tsx` `RegisterForm`(채우기), `tests/test_knowledge_scope.py`; (g) `domain/conversation.py` `normalize_title`, `Repository.apply_auto_title`·`set_case_title`·`title_view`, `controller/request_processor.py` `finish`, `web/src/shell/ConversationView.tsx` `TitleEditor`·`Sidebar.tsx` 이름 바꾸기, `tests/test_case_title.py`; (d) `p4/live/profile_intents.py`(`--prefix`·`--only`, D·E), `ui/live/p405_progress.py`(`HADS_LIVE_PREFIX`). 원래 묶음 설명은 [P4-PLAN-09](plans/P4-PLAN-09.md) 0~3절.
- ~~**UI-05a — 이슈 #7 관리 화면 정리 1단계**~~ **완료(S-036, 2026-09-24)** — [UI-PLAN-05a](plans/UI-PLAN-05a.md)·[UI-05a 결과](ui/evidence/UI-05a-results.md). 전수 대조(결과 2절 — **관리 화면도 QG-02~07 판정을 기록하지 못했다**는 사실 포함), `작업` 탭(작업 그래프 수정·실행 상세), D-94(명시로 켠 QG-02~04 의 진행기 자동 검토·수정·한도 뒤 카드). **사람에게 물은 것의 답:** QG-02~07 의 길 → (ⅱ) 진행기가 스스로 돌림(D-94), 실행 중 Case 의 취소 2단계 → UI-05b 에 붙임. **코드 대조 시작점:** 작업 탭 — `web/src/shell/WorkPanel.tsx`(`GraphSection`·`TaskItem`·`QuestionLinkItem`·`AddTaskForm`·`RunList`·`RunDetail`), `ReviewPanel.tsx` `PanelTab`·`Shell.tsx` `openRun`·`events.ts` `hads:open-run`, `controller/api.py` `add_task`·`cancel_task`·`set_question_blocks` 의 `_after_human_input`, `Repository.set_question_blocks` 의 `guard_open_case`, `tests/test_work_graph_edit.py`; D-94 — `domain/work_flow.py` `quality_gate_step`·`GATED_PURPOSES`·`Step.gate`, `Repository.gate_subject`·`quality_gate_progress`·`open_gate_review`·`apply_gate_run_effects`·`quality_gate_for_assignment`·`quality_gate_findings_for_assignment`·`quality_gate_blockers_for_run`(결합 기록 경로)·`complete_quality_gate_run(blocked=)`, `WorkProgressor.advance`(걸음 치환)·`_link_gate`, `api.py` `runner_result`(`run_finished_gate`)·`put_quality_gate_policy`(진행기 호출), `runner/agent.py`·`prompts.py` `build_quality_gate_block`·`build_quality_findings_block`, `web/src/shell/ProgressCards.tsx` `QualityGateCard`, `tests/test_quality_gate_progress.py`, `tests/fake_cli/fake_codex.py`(`HADS_FAKE_PLAN_QUESTION`·`HADS_FAKE_GATE_FAIL`), 라이브 `ui/live/p405_progress.py`(`HADS_LIVE_GATE`).
- **P4-10 — 이슈 #8·#9 진행기 보강(다음, 사용자 결정 2026-09-24 S-036: 다음 세션·UI-05b 앞, 한 plan):** 세션 중 등록된 두 이슈. **#8** — `runner/cli_adapter.py` 의 실행 제한 시간이 고정 600초이고, 시간 초과(`stop_reason=timeout`, 결과 `unknown`)가 제어부로 가지 않아 진행기가 일반 실패처럼 같은 조건으로 재시도한 뒤 `task_failed`("거듭 실패")로 멈춘다(실제 관측: Android 빌드·에뮬레이터 검증이 두 번 다 10분에 끊김). 고칠 것: Runner 가 `stop_reason`·적용 제한을 결과에 싣고 제어부가 실행 기록에 남김, 제한 시간 설정(**기본 1시간 — D-95**, 프로젝트 기본값·업무 덮어쓰기, 출처·이력은 진행 상한 방식), 시간 초과는 같은 제한으로 자동 재시도하지 않고 카드('제한 시간 늘려 다시 시도' + 작업 탭의 그 실행 상세 링크), 부분 결과는 기준 판정에 쓰지 않음. 참고: 같은 실행에서 AI 가 비교용 worktree 삭제를 `blocked by policy` 로 막혔다 — D-91 뒤 정책과 맞는지 확인. **#9** — 검증이 `not_met` 을 보고해도 검증 Task 는 `done` 이 되고 구현으로 돌아가지 않는다. 고칠 것: 완료된 검증이 근거와 함께 `not_met` 을 보고하면 진행기가 그 기준에 연결된 구현의 수정 Task(`origin: remediation`)와 재검증 Task 를 작업 그래프 새 리비전으로 더해(사유 기록) 한도 안에서 돌림(한도 키·기본값·0 이면 끔은 plan 에서 정한다), 한도 뒤·`unverified` 는 사람 카드(미충족 기준·시도 이력·검증 보고). `remediation_cycle`·검증 1회 규칙(D-30)·UI-05a 의 작업 그래프 수정 경로·D-94 의 repair 모양을 재사용. 완료 조건은 두 이슈 본문 그대로다. 이슈 댓글·닫기는 사용자 허용 시에만.
- **UI-05b(다음) — 이슈 #7 관리 화면 정리 2단계. 아래는 UI-05a·05b 의 원래 배정 설명이다 — UI-05a 는 끝났고 QG-02~07 은 D-94 로 정해졌다. UI-05b 에 더해진 것: [UI-05a 결과](ui/evidence/UI-05a-results.md) 2.3절(의도 피드백 보내기·QG-01 발견 표시·단계별 검토 방식·사람 판단 기준 판정·완료 의미 표시)과 실행 중 Case 의 취소 2단계(사용자 결정 2026-09-24).** 원래 설명: **UI-05a·UI-05b — 이슈 #7 관리 화면(옛 UI) 정리(사용자 결정 2026-09-24: "그래 그렇게 하자" — P4-09 뒤·P5-01 전, 두 세션):** 관리 화면(`?view=admin`, UI-01~02 시절 화면)은 유지하지 않는다 — 관리 화면에만 있는 기능 중 필요한 것을 새 화면(대화 화면)으로 옮기고 마지막에 지운다. 이유: 두 화면이 어긋나고, 관리 화면의 실행 버튼이 진행기의 실행과 겹치며(9절), 막힘 해소가 관리 화면에만 있다. **UI-05a**: 이슈 1단계 **전수 대조**(관리 화면이 부르는 API 를 항목마다 `옮김` / `새 화면·진행기가 이미 대신함` / `개발자 전용` / `버림` 으로 분류 — 결정 기록) + **필수 셋 옮기기**: QG-02~07 진행(막힘 해소 — 진행기가 돌리지 않아 켠 게이트가 막으면 여기서만 풀린다), 작업 그래프 수정(Task 추가·취소 + 질문↔Task 연결 고치기 `setQuestionBlocks` — 지금은 어느 화면에도 없다), 실행 상세 보기(고정 입력 `run_context_ref`·수신 확인·이벤트·출력 — 이슈 #2 의 진단에 쓰인 정보). **시작 때 사용자에게 물을 것:** QG-02~07 을 (ⅰ) 관리 화면의 수동 진행을 새 화면으로 옮길지, (ⅱ) 진행기가 게이트를 스스로 돌리고(P4-01·02 의 검증 1회 모델을 진행기에 잇는 별도 plan — 9절) 사람은 결과·repair 카드만 볼지 — 후자가 제품 방향(이슈 #1 "사람은 AI 가 판단 못 하는 곳에서만")에 맞지만 더 큰 변경이다. **UI-05b**: 나머지 옮김(다른 브랜치·커밋 기준 시작 — P4-09 (c) 의 시작 기준 카드에 "다른 커밋에서 시작" 입력으로 붙인다; 코드 구성 `buildComposition`), 결과·완료·준비·의도·대화 영역의 항목은 1단계 분류대로(대신함이면 버림, 개발자 전용은 주소로만 들어가는 숨은 화면 여부를 이때 결정), 새 화면의 관리 화면 링크 제거(`ProgressCards.tsx`·`ConversationView.tsx`·`CaseSettings.tsx`·`Sidebar.tsx`), `?view=admin` 삭제(또는 개발자 전용 명시), `tests/test_web_shell.py` 의 관리 화면 의존 5곳 대체(지식 패널 3곳은 규칙 화면으로), `DEVELOPMENT.md`·`ui-conversation-design.md` 의 관리 화면 언급 정리. **완료 조건은 이슈 #7 그대로** — 사람이 해야 하는 모든 조작(막힘 해소 포함)이 새 화면에서 되고, 새 화면에 관리 화면 링크가 없고, `?view=admin` 이 없거나 개발자 전용으로 명시되고, 기존 시험 전부 통과·옮긴 기능마다 브라우저 시험. 끝나면 이슈 #7 을 닫는다. 코드 시작점: `web/src/App.tsx`·`AdminEntry.tsx`·`*Panel.tsx`·`main.tsx` 의 `?view=admin` 분기, `web/src/api.ts` 의 `gateApi`·`graphApi`·`runApi`, `controller/api.py` 의 해당 끝점, P4-09 (c) 가 만든 시작 기준 카드.
- **P4-08 이 만든 것·코드 대조 시작점:** `tests/test_profile_flows.py`(13건 — `_draft`·`_tasks`/`_plan`·`_verification` 도우미), `p4/live/verify_env.py`(검증 환경 실험, 제품 코드 import 없음). 제품 쪽 분기(무변경): `domain/work_flow.py` `_investigation`·`decide_criteria`(`preserved`·`already_satisfied` 분기), `domain/models.py` `CriterionObligation`·`INVESTIGATION_ONLY_PROFILES`, `domain/completion_meaning.py` `check_result`, `tests/conftest.py` `processing_harness`·`FakeCliExecutor`·`met_satisfaction_for`, `tests/test_work_progressor.py` 의 feature 대표 시험(`test_a_clear_feature_request_runs_to_auto_completion_with_only_the_agreement`).
- **UI-04d 가 넘긴 것(필요할 때 별도 plan):** 파일별 포함 선택(**사용자 결정 2026-09-24: 이진 선택(전부 포함/전부 제외) 그대로 둔다** — 필요해지면 경로가 브라우저→서버→PC 로 오가는 경계를 정한다), 후속 Case 의 "이전 업무 결과를 기준으로 제안"(D-77 마지막 문장 — 후속 대화의 작업공간도 지금은 HEAD 기준), 새 화면의 ref 변경 입력(다른 브랜치·커밋 기준은 기존 API·관리 화면 — 그때는 커밋된 코드에서만), 본문 검색 색인(지금은 후보 상한 2,000건까지 선형·`truncated`), 여러 PC 의 본문 검색(소유 PC 별로 나눠 연결된 PC 만 — 지금 운영은 한 Runner).
- **UI-04c 가 넘긴 것:** 개정은 정의판 v2 Case 만(v1·미기록은 거부, D-62). 같은 조사 Profile 안의 목적 추가는 분석을 다시 돌리지 않는다(분석 1회 규칙). 개정 뒤 새 의도 버전의 더한 기준은 material delta 로 확인 대상이고 그 뒤 동의다. AI 해석 `profile_change` 는 이전 목적을 항상 유지한다. 끝나지 않은 실행이 있으면 개정을 거부한다. D-88 은 지표를 바꾸지 않았다. D-89 의 열기는 Windows 폴더·PATH 의 `code` 만 확인하며 외부 변경 관측은 쓰기 실행 전뿐이다. 시험 Runner 는 `HADS_RUNNER_DESKTOP=off` 로 열기를 끈다.
- **참고 후보 자동 활성 조건의 Project 설정화:** 사용자 결정(2026-09-24, S-030) — **나중에 필요하면 한다.** 지금은 코드 고정(`domain.knowledge.AUTO_REFERENCE_*`) 그대로다.
- 코드 대조 시작점(UI-04d 가 만든 것): D-84 — `domain/search.py`(공용 규칙), `controller/transient.py`(`TransientStore`), `controller/search.py`(`SearchRegistry` — `create`·`view`·`pending_for`·`record_results`), `Repository.search_candidates`, `runner/agent.py` `serve_search_requests`, `web/src/lib/search.ts`·`shell/SearchScreen.tsx`·`Sidebar.tsx` 검색 폼·`Shell.tsx` `startSearch`; D-77 — `runner/workspace.py`(`NeedsBasis`·`snapshot_commit`·`prepare(start_basis=, expected_tree_digest=)`), `Repository.report_workspace_uncommitted`·`decide_start_basis`·`report_workspace_ready(start_basis=…)`·`workspace_state`, `work_flow._task_phase` 의 `WORKSPACE_START_BASIS`, `WorkProgressor.on_start_basis_decided`, `api.py` 의 `uncommitted`·`start-basis` 끝점, `shell/ProgressCards.tsx` `StartBasisCard`, `ReviewPanel.tsx` `WorkspaceRow` 의 시작 코드 줄; 시험 도우미 `Harness.prepare_workspace(basis=)`·`decide_start_basis`, `FakeCliExecutor` 의 쓰기 권한 규칙.

## 2. 다음 세션 시작 절차

1. `git status --short --branch`, 최근 커밋·변경 파일을 확인한다. 이 문서 개정의 미커밋 파일과 사용자 작업을 구별한다. 자동 reset·clean·stash·강제 checkout·rebase를 하지 않는다. 이전 인계의 원격 동기 상태를 현재 사실로 가정하지 않는다.
2. 현재 상태와 최신 인계(10절), [UI 설계](ui-conversation-design.md), 최근 결과 문서(1절 요약 표의 링크), [사용자 결정](decisions.md), [전체 설계](design-draft.md), [문맥](review-context-contract.md), [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [완료](completion-lifecycle.md)을 읽는다. 전체 옛 개발 일지를 다시 읽을 필요는 없지만 현재 하위 작업과 UI 설계의 접점·미구현 경계는 확인한다.
3. 실제 코드의 정책·스키마·진입 검사·화면과 새 설계의 차이를 확인한다. 문서상 목표를 구현 사실로 취급하지 않는다. 기존 Case·결정·원문·증거·실행 이력은 보존한다.
4. 기존 실행 환경을 확인하고 `scripts\bootstrap.ps1`, `scripts\run-tests.ps1` 등 저장소의 재현 경로로 기준선을 확인한다. 설치/복구가 필요하면 기존 허용·환경에 맞게 처리한다. 1절의 시험 수치(pytest 696·P1 계약 18·웹 단위 21)는 인계 시점 수치이며 현재 시험 결과를 대신하지 않는다. `scripts\run-tests.ps1` 은 UI-03부터 웹 빌드·웹 단위 시험(`npm test`)을 먼저 돌리고(`-SkipWeb` 으로 뺀다), `tests/test_web_shell.py` 는 설치된 Edge 를 Playwright 로 연다(없으면 건너뛴다). `tests/test_process_control.py`·`test_runner_process_control.py` 는 Windows 에서 실제 프로세스를 만든다(비 Windows 는 건너뛴다).
5. **P4-PLAN-01~07(05b·06b·07b 포함)·UI-PLAN-01~03·UI-PLAN-04a·UI-PLAN-04b·UI-PLAN-04c 는 완료다.** 다시 열지 말고 다음 작업의 plan을 기록·공유한 뒤 구현한다. 완료 작업이 남긴 기록·판정·모델(정책 리비전·예약·재검증 판정·repair 누적·Profile 정의판·준비 단계·메시지·요청·잠금·업무화 기록·문맥 등급·영수증·원장 v2·job 트리 제어·요청 처리기·해석 기록·진행 행·상한 이력·지식 버전·권위·Manifest·자동 등록·후보·근거·채택 기록·프로젝트 설정 이력·Profile 개정 이력·버전별 Profile·유지 항목·열기 요청)을 보존한다 — 무엇이 있는지는 1절 요약 표와 각 결과 문서, 당시의 나열은 [보존본](development-history-v0.8.md) 1절. 커밋은 `git log` 와 각 인계에 있다 — 시작 시 `git status`·`git log`·원격 ref로 확인하고, 이전 인계의 상태를 현재 사실로 가정하지 않는다.
6. 작업을 수행한 뒤 현재 표와 진행표·plan·결과를 함께 갱신한다. 선택한 하위 작업에서 이후 항목으로 자동 확대하지 않는다. P3-R4의 고정 컨텍스트(`run_context_ref`)·material delta, P4-01~02의 게이트 실행·재검증, P4-03의 완료 의미(`completion_meaning`)를 재사용한다. 기존 판정을 소급 변경하지 않는다.

기본 작업공간은 이 checkout이다. 다른 작업공간이 필요하면 정확한 경로·이유를 plan과 인계에 기록한다. 다른 세션과 같은 파일을 동시에 수정하지 않는다. 이 안내서는 구현을 위임하지만 외부 쓰기나 기존 데이터 파괴를 포괄하지 않는다.

## 3. 계획·개정·증거 관리

- plan은 작업 목록뿐 아니라 요구에서 도출한 성공 기준과 검증 방법을 포함한다. 활성 plan을 이어 쓰고 범위/기준이 바뀌면 구현 전에 변경 이유를 기록한다.
- 단계/작업별 구현 완료, 자동 시험, 실제 CLI 시험, 외부 서비스 시험, 문서 검토를 구별한다.
- 기존 테스트가 v0.6 정책을 고정했다면 그 의미를 검토해 바꾸고 대응하는 v0.7 거절·보존 시험을 추가한다. 실패를 피하려고 검사를 삭제하지 않는다.
- 새 정책을 기존 승인·동의·권한에 소급 적용하지 않는다. 마이그레이션은 기존 정책값·원문·주체·버전을 명시 보존하고 새 Case의 기본값과 구분한다. 진행 중 Case의 정책 변경은 영향과 권한을 검토하고 종료 기록은 다시 쓰지 않는다.
- 구현하지 않은 모드는 비활성/미지원으로 표시한다. R1에 모델이 생겼다는 이유로 R4 자동 실행이나 P5 게시를 지원한다고 표시하지 않는다. R2가 쓰기 허용을 강제한다고 게시 허용까지 강제되는 것으로 표시하지 않는다.
- 파일/필드 이름과 수치 기본값 등은 상세 설계 제안이며, 사용자 확정 정책과 구별한다. 스키마 번호는 실제 마이그레이션을 만들 때 정한다.

| Plan | 상태·근거 |
|---|---|
| P4-PLAN-10 (착수 세션이 기록) | **다음 — 이슈 #8·#9 진행기 보강**(사용자 결정 2026-09-24: 다음 세션·UI-05b 앞): 실행 제한 시간 설정화(기본 1시간, D-95)·시간 초과 구분·자동 재시도 않음·카드, 검증 미충족 → 수정 구현 → 재검증 사이클·한도 뒤 카드. 1.11절 |
| UI-PLAN-05b (착수 세션이 기록) | **P4-10 뒤 — 이슈 #7 관리 화면 정리 2단계**: [UI-05a 결과](ui/evidence/UI-05a-results.md) 2.3절의 나머지 옮김 + 실행 중 Case 의 취소 2단계 + 링크·문구 제거 + 관리 화면 삭제·시험 5곳 대체·문서 → 이슈 #7 닫기. 1.11절 |
| [UI-PLAN-05a](plans/UI-PLAN-05a.md) | 완료(S-036), AC-1~14. [실행 결과](ui/evidence/UI-05a-results.md) — 전수 대조(2절), `작업` 탭(작업 그래프 보기·추가·취소·질문 연결·실행 상세), 그래프 수정 뒤 진행기·종료 업무의 연결 거부, D-94(명시로 켠 QG-02~04 의 진행기 자동 검토·판정 기록·repair·한도 뒤 카드, 결합 기록 경로의 QG-02). 관리 화면 삭제·나머지 옮김은 UI-05b |
| [P4-PLAN-09](plans/P4-PLAN-09.md) | 완료(S-035), AC-1~37(AC-34·35 라이브는 관찰). [실행 결과](p4/evidence/P4-09-results.md) — (a) 이월 질문 답 전달(핵심 참조·지시문 머리·재시도 동일), (b) 샌드박스 해제(D-91, 능력 `write_sandbox`), (c) 후속 Case 의 `previous_result`(작업 PC 판단·카드·v28), (e) 대기 중 Case 취소(후보 없는 종료 기록·보존·훅 무반응·v28), (f) AI 후보의 범위 보존·주입 제외·`scope_unreadable`·열람·수동 등록 채우기(D-92, v28), (g) 자동 제목·이름 바꾸기(D-93, v28), (d) 실제 codex 라이브 둘. 실행 중 취소(2단계)·되묻는 답 판정·다른 커밋에서 시작 입력은 범위 밖 |
| [P4-PLAN-08](plans/P4-PLAN-08.md) | 완료(S-034), AC-1~7(AC-8 실제 codex 의도 작성은 선택 사항 — 미수행). [실행 결과](p4/evidence/P4-08-results.md) — `tests/test_profile_flows.py` 13건: defect-fix 복원·무변경 완료·미재현, refactoring 개선+보존·보존 기준 없음·보존 미충족, maintenance 조건부 보존 셋, 혼합 목적 개정 경로 둘·처음부터 선언 둘. 제품 코드 무변경·결함 없음. 검증 환경 실험은 별도 [P4-ENV 결과](p4/evidence/P4-ENV-results.md)(plan 없이 사용자 허용으로 한 관찰 — 제품 변경 없음, 이 PC 의 ACL 조치 하나) |
| [P4-PLAN-ACCEPT](plans/P4-PLAN-ACCEPT.md) | 완료, AC-1~9(문서만, 코드 변경 없음). [UI-04 결과](ui/evidence/UI-04-results.md) — D-90 표 4행 전수·UI 설계 13절 19 항목 대응(16 검증·3 부분)·라이브 현황·남은 것; [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md) — 완료 조건 네 축·case-profiles 6절 시나리오 10·점검표 AC-01~20·30~36·39·빠진 것(P4-PLAN-08)·남은 한계 |
| [UI-PLAN-04d](plans/UI-PLAN-04d.md) | 완료, AC-1~12(AC-13 실제 codex 라이브는 선택 사항 — 미수행). [실행 결과](ui/evidence/UI-04d-results.md) — D-84 서버 필드 즉시·소유 PC 의 본문 검색(메모리 중계, DB 에 검색 표 없음)·범위 표시(미연결 제외 수·상한·만료)·결과 이동(메시지·결정·규칙), D-77 더러운 트리의 선택 대기(`awaiting_basis`, 목록 메모리 중계)·이진 선택 카드·스냅샷 커밋 포함·지문 대조의 동시 편집 보호·작업공간 절의 시작 기준, 스키마 v27. 파일별 선택·후속 Case 기준 제안·화면의 ref 변경·색인은 범위 밖 |
| [UI-PLAN-04c](plans/UI-PLAN-04c.md) | 완료, AC-1~15(AC-16 실제 codex 라이브는 선택 사항 — 미수행). [실행 결과](ui/evidence/UI-04c-results.md) — D-86 개정 이력·유지 항목·유지 목적·기준 승계·개정 폼·`profile_change` 해석·새 버전의 검토·동의, D-88 지표 이름표·시간 절·시간 한도 기본값·재배정 옛 세대 계측 수정, D-89 데스크톱 능력·열기 명령(자기 worktree 만·만료)·실행 전 지문 대조·작업공간 절, 스키마 v26. v1 Case 개정·분석 재실행·읽기 전용 실행 전 관측·다른 편집기는 범위 밖 |
| [UI-PLAN-04b](plans/UI-PLAN-04b.md) | 완료, AC-1~11 + 보충 AC-13(예산 도달 알림 — 사용자 결정 2026-09-24, plan 10절)(AC-12 실제 codex 라이브는 선택 사항 — 미수행). [실행 결과](ui/evidence/UI-04b-results.md) — 프로젝트 기본값 층(스키마 v25, 이력·적용 시점·복귀), 프로젝트 설정 화면(기본값·규칙·저장소 탭), 대화의 상세 설정 탭, 브라우저 알림(전이·억제·기록·예산 도달). Task 층·Profile 기본값 층·모델/깊이 기본값·서버 푸시·자동 활성 조건 설정화(사용자 결정: 나중에 필요하면)는 범위 밖 |
| [P4-PLAN-07b](plans/P4-PLAN-07b.md) | 완료, AC-1~9. [실행 결과](p4/evidence/P4-07b-results.md) — 참고 후보의 자동 활성 조건(여덟)의 계산·표시(`auto_reference_check`·조회·배지), 사람의 한 번에 활성화(`activate-ready`, 효력 참고·범위 그대로·충족 항목 기록), 미충족·거부 건너뜀, 다음 실행 주입. 스키마 v24 그대로. 클릭 없는 활성화·필수 승격·독립 검토·조건 설정화는 범위 밖. 실제 codex 라이브는 선택 사항이라 하지 않았다 |
| [UI-PLAN-04a](plans/UI-PLAN-04a.md) | 완료, AC-1~14. [실행 결과](ui/evidence/UI-04a-results.md) — 결정 사항 패널(업무·목표·기준·결정·이 대화에서 정한 규칙·후보·적용된 규칙·질문·해석·이력, 메시지로 이동)·프로젝트 규칙 화면(등록부·채택 확인·활성화·개정·무효·충돌·수동 등록·원래 대화로)·주소 규칙 `seq`·조회 확장·`knowledge-use` 집계, 스키마 v24 그대로. 실제 codex·Edge 라이브 26/26(규칙 옮김·제안 둘은 AI 관찰). 프로젝트 설정 화면·검색·알림·D-86 은 UI-04 의 남은 하위 plan |
| [P4-PLAN-07](plans/P4-PLAN-07.md) | 완료, AC-1~14. [실행 결과](p4/evidence/P4-07-results.md) — 작업 실행의 후보 블록·논의 응답의 제안 표지·근거 행(같은 내용·`supports`)·관계 후보(`supersedes`·`contradicts`)·관측 문맥·QG-08 채택 확인·활성화(축소·`into`)·카드·관리 화면, v24. 실제 codex 라이브 두 회차(첫째 5/5 후보 없음, 둘째 13/13 — 정리 응답의 제안 둘 → 활성화 → 다른 대화의 검증 실행 2개 주입). 참고 후보 자동 활성화(사용자 판단 대기)·독립 검토·별도 추출 실행·UI-04 는 범위 밖 |
| P4-PLAN-01~06b(05b 포함)·UI-PLAN-01~03 | 완료. 행별 AC·결과 링크는 [보존본](development-history-v0.8.md) 5절 |
| P3 plan(P3-PLAN-01~04·R1~R4) | 완료. **P3-PLAN-04만 AC-17 라이브 미수행**(사용자 지시로 스킵). 행별 AC·결과 링크는 [보존본](development-history-v0.8.md) 5절 |
| P1·P2 plan | [보존본의 plan 목록](development-history-v0.6.md) 및 `plans/` 원문 유지 |

## 4. 단계 진행표

| 단계 | 상태 | 다음 작업 | 완료 근거/변경 사항 |
|---|---|---|---|
| P1 CLI 계약·실증 | ACCEPTED_WITH_LIMITATIONS | — | [환경](p1-environment-contract.md), [CLI 실증](p1/evidence/P1-02-results.md), [중지 실증](p1/evidence/P1-03-results.md). 제한은 아래 이월표 유지 |
| P2 최소 흐름 | DONE (v0.6) | — | [P2-04 결과](p2/evidence/P2-04-results.md). 당시 승인 중심 동작의 사실 보존 |
| P3 실행 기반·새 정책·통합 | DONE_WITH_LIMITATIONS | — | P3-01~03·R1~R4·P3-04 완료. 라이브는 경로 A·B 확인, **경로 C 는 사용자 지시로 스킵**(AC-17 라이브 증거 없음) |
| UI 대화 중심 UI | **DONE_WITH_LIMITATIONS**(UI-04 완료 확인 S-033; 남은 것은 [UI-04 결과](ui/evidence/UI-04-results.md) 4절) + **UI-05a 완료(S-036)** | **UI-05b**(이슈 #7 관리 화면 정리 2단계 — P4-10 뒤·P5 전, 1.11절) | [UI-05a](ui/evidence/UI-05a-results.md): 전수 대조·`작업` 탭·D-94 게이트 카드. [UI-04 완료 확인](ui/evidence/UI-04-results.md). [P4-09](p4/evidence/P4-09-results.md): 취소 버튼·자동 제목·이름 바꾸기·거부 후보 열람·이전 결과 카드. [UI-01](ui/evidence/UI-01-results.md)~[UI-04d](ui/evidence/UI-04d-results.md): 1절 요약 표 |
| P4 품질·여섯 업무·지식 | **DONE_WITH_LIMITATIONS**(S-034 — P4-01~08 완료, 수용 확인 완료; **P4-09 정리 묶음 완료 S-035**; 제한: ~~QG-02~07 진행기 미실행~~(UI-05a·D-94 로 명시 ON 의 QG-02~04 해소)·QG-08 독립 AI 검토 없음·경로 C 스킵·검증 환경(읽기 전용 실행)은 PC 별 조치(9절)) | **P4-10**(이슈 #8·#9 — 다음 세션, 1.11절) | [P4 수용 확인](p4/evidence/P4-ACCEPT-results.md): 네 축 대조·시나리오 10·점검표 행. [P4-08](p4/evidence/P4-08-results.md): 세 Profile 과 혼합 목적의 진행기 경유 시험 13건. [P4-ENV](p4/evidence/P4-ENV-results.md): 실제 codex 가 샌드박스에서 시험을 돌리는 조건. [P4-09](p4/evidence/P4-09-results.md): 이슈 #2~#6·후속 Case 기준·실제 codex 라이브(RCA·maintenance 표본, 샌드박스 해제 뒤 진행) |
| P5 GitHub·외부 완료 | PENDING(P4-10·UI-05b 뒤) | P5-01 | 범위 사전 허용과 controlled 게시 전 확인으로 개정 |
| P6 원격·운영·전체 수용 | PENDING | P6-01 | 기존 데이터/단절/운영 요구 유지, 새 계약도 포함 |

순서는 `P3-03(완료) → R1(완료) → R2(완료) → R3(완료) → R4(완료) → P3-04(완료)`다. 번호 R1~R4는 새 정책 반영 작업이며 기존 P3-01~03의 재개·재번호가 아니다.

## 5. P3의 남은 작업

P3는 `DONE_WITH_LIMITATIONS` 로 끝났다(4절). 하위 작업 표와 R1~R4·완료 조건 문단은 [보존본](development-history-v0.8.md) 3절로 옮겼다. 남은 것은 **경로 C(예산 도달의 기능 흐름 라이브)** 하나이며 사용자 지시로 스킵했다(9절).

## 6. P4 — 품질·다양한 업무·지식

참조: [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [Profile](case-profiles.md), [문맥](review-context-contract.md), [지식](project-knowledge.md), [완료](completion-lifecycle.md).

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| ~~P4-01 게이트·repair~~ | **완료(2026-09-22).** QG-01 검사 강도, QG-02~07 선택, Case/Task 설정, 조건부 별도 의미 검토, 스키마 v13 | 필수 기준/증거 유지, 초기 실패 후 기본 2회, 재분할/세션교체 초기화 없음, OFF/미적용/통과 구별. [실행 결과](p4/evidence/P4-01-results.md) |
| ~~P4-02 변경·예약~~ | **완료(2026-09-22).** 검증 1회 경계, 설정 변경 예약·적용 시각·취소, 요청/코드/권한 변경의 부분 재검증, 늦은 결과, 배정 직전 재확인, 스키마 v14 | 원래 판정·정책 보존, 예약이 진행 중 검사를 무효화하지 않음, 강도 변경만 재검증, `unknown` 을 재사용으로 적지 않음, 늦은 결과가 repair·배정을 만들지 않음. [실행 결과](p4/evidence/P4-02-results.md) |
| ~~P4-03 여섯 Profile~~ | **완료(2026-09-23).** Profile 정의 v2 완료 계약, 기준의 목적 의무·충족 방식 필수, 원인·조사 결론 요구, 혼합 목적 선언, 실험 근거 제한·정리 상태, 스키마 v15 | 목적별 필수 의무·허용 방식, 생략 우회 차단, 판단 불가 구별, 무변경 성공은 검증 실행 관측, 기준 없는 목적은 예외 불가, v1 Case 소급 없음. **Profile 재분류는 범위 밖.** [실행 결과](p4/evidence/P4-03-results.md) |
| ~~P4-04 컨텍스트·재개~~ | **완료(2026-09-23).** 고정 입력의 등급(핵심/보조)·인라인 한도·영수증(해시 대조), 핵심 누락·초과 보류, 시작하지 않은 실행 소비 0, 결과 시점 최신성, 새 세션 보존, Runner 재시작의 원시 출력 사용량 복구, 스키마 v17. **분할 검토·체크포인트·단계적 조회는 넣지 않았다**(plan 3.9절) | 원문 누락·stale 표시, 필수 조건 유지, 누락을 성공으로 적지 않음, 작성자의 완료 요약만으로 검토하지 않음(P4-01 규칙 유지). [실행 결과](p4/evidence/P4-04-results.md) |
| ~~P4-05 업무 진행·완료·예외·후속~~ | **완료(2026-09-23).** 업무 단계 진행기(의도 초안→QG-01·repair→질문·동의→controlled 시작→준비→작업 그래프·작업공간→구현·검증→기준 판정→자동 완료/controlled 결과/예외 수용), 실행 사이 잠금 유지·사람 대기에서만 전송 열림·진행 요청, 기준 판정의 제품 기록(구조 검사), 확인 카드, 설명 전용 진입·종료 snapshot·종료 뒤 예산 변경·후속 Case, 스키마 v20. **QG-02~07 자동 실행·검증 1회 중단·D-86 은 넣지 않았다**(plan 3.9절) | 기존 게이트·repair·예약·완료 계약·Autonomy·예산 강제 보존, 자동 사람 인수 없음, 예외가 권한을 만들지 않음, 설명 소비 같은 Case 누적. [실행 결과](p4/evidence/P4-05-results.md) |
| ~~P4-05b 진행 상한 설정~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-05b-results.md). 재작성·재시도 상한을 시스템 기본값(환경 변수) + Case 별 이력 조정으로. 상한 대기 카드의 "한도를 올리고 계속", 관리 화면 정책 패널 표시·변경. Project 층은 UI-04 (사용자 결정 2026-09-24, 1.8절) | 우선순위·이력, 올린 뒤 한 번 더 가고 다시 멈춤, 0 의 뜻, 종료 Case 거부, v20 → v21 이행 |
| ~~P4-06b 지식 원문 서버 저장~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-06b-results.md). 사용자 결정(D-67 보충)으로 지식 적용 내용과 자동 등록 규칙의 권위 사용자 메시지 한 건을 서버 DB(`knowledge_body`, 유일한 본문 표)에 저장해 어느 PC 의 실행에도 배정으로 싣고, Runner 는 해시가 맞을 때만 씀. 수동 등록은 중계 없이 저장, 옛 지식은 소유 PC 연결 시 이행, 열람은 서버가 채움, 화면·카드·등록 규칙의 서버 저장 안내. 다른 원문 경계는 그대로, 스키마 v23 | 두 PC 시험 — 다른 PC·소유 PC 미연결에서 필수 주입·영수증 `read`, 해시 불일치는 읽지 않음, 표식은 `knowledge_body` 에만·로그 없음, 이행 v22 → v23. 실제 codex 두 Runner 라이브 |
| ~~P4-06 지식 관리·적용~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-06-results.md). 원본/짧은 적용 내용/조건/필수·참고/버전·상태, Runner 보관, 활동+범위 자동 주입·Manifest, 대화 규칙 자동 활성 등록(사용자 결정), 스키마 v21·v22 | 기존 확정 지식 수동/자동 등록 경로로 검증. 규칙의 권위 승계, scope 확대 재적용, 필수 내용 실제 제공, 원문 불가/충돌 의존 작업 보류, 주입과 준수 구분 |
| ~~P4-07 선택적 지식 추출~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-07-results.md). 검증·분석·실험·구현 실행이 결과와 함께 남기는 후보(AI 제안, 최대 3건·근거 한 줄), 논의 응답의 제안 표지(`proposal: true`), 같은 내용·`supports` → 기존 항목의 근거 행, `supersedes`·`contradicts` → 관계를 가진 새 후보(대상의 버전 사슬 그대로), 관측 문맥(실행·저장소·기준 커밋·도구), QG-08 채택 확인(`adoption_check`)과 사람의 활성화(범위 축소·다른 항목의 새 버전으로), 후보·제안·근거 카드, 관리 화면의 채택 확인·활성화, 스키마 v24 | AI 새 일반화가 강제규칙이 되지 않음(DB CHECK 그대로). 아무 후보 없는 Case 정상완료. 별도 추출 실행 없음(발생 비용은 그 실행의 것). 다른 Case 실행에 후보(보조)·활성화 버전(효력대로) 주입. **참고 후보 자동 활성화·독립 AI 검토는 없음**(전자는 사용자 결정 2026-09-24: 반자동 — P4-07b) |
| ~~P4-07b 참고 후보의 자동 활성 조건 표시~~ | **완료(2026-09-24, 사용자 결정 S-028: 반자동).** [실행 결과](p4/evidence/P4-07b-results.md). 시스템이 조건 여덟(후보·효력 참고·종류 운영 사실/알려진 문제·서로 다른 실행 둘 이상의 근거·그중 종료 코드 0 명령의 검증/분석 실행·관측 저장소 범위·반증/충돌 없음·채택 확인 막음 없음)의 충족을 계산해 규칙 화면·카드·관리 패널에 표시하고, 사람이 규칙 화면에서 한 번에 활성화한다(`activate-ready`). 스키마 v24 그대로 | 클릭 없는 활성화 없음, 필수 승격 없음, 조건 미충족 사유 표시, 한 번에 활성화가 각 후보의 채택 확인을 그대로 지남(거부·미충족은 건너뛰고 적음), 활성 참고가 다음 실행에 참고로 주입 — 전부 시험으로 고정. 논의 응답의 제안은 관측이 없어 조건 (4) 불가(사람이 폼에서 활성화) |
| ~~P4-08 여섯 Profile 진행기 경유 대표 흐름 + 혼합 목적 완료~~ | **완료(2026-09-24, S-034).** [실행 결과](p4/evidence/P4-08-results.md). `tests/test_profile_flows.py` 13건 — defect-fix(복원·무변경 완료·미재현)·refactoring(개선+보존·보존 기준 없음·보존 미충족)·maintenance(조건부 보존 셋)·혼합 목적(개정 경로·처음부터 선언, 양쪽/한쪽). 제품 코드 무변경·결함 없음 | `preserved`·`already_satisfied`·`investigated`(검증 근거) 분기 통과, 자동 완료/`objective_without_criteria`/예외 카드 구분, 강제 축 그대로, 실제 codex 의도 작성은 선택 사항 미수행 |
| P4-09 P5 전 정리 묶음 | **완료(S-035, 사용자 결정 2026-09-24, 한 세션·한 plan).** (a) 이슈 #2 이월 질문 답 전달 (b) 이슈 #3 쓰기 실행 샌드박스 해제(D-91) (c) 후속 Case 시작 기준(D-77) (e) 이슈 #4 업무 취소 (f) 이슈 #5 지식 후보의 거부·열람(D-92) (g) 이슈 #6 대화 제목(D-93) (d) 실제 codex 라이브(RCA·maintenance 표본·실제 검증 `met`) — 1.11절 | 답 원문이 작업 실행의 핵심 입력(재시도 포함), 읽기 전용만 샌드박스·게시 규칙 그대로, 카드는 사람 선택·자동 없음·소급 없음, 취소는 사람 결정·부분 결과 보존·되돌리지 않음·늦은 결과 무반응, AI 후보는 받되 채택 확인이 막음·사용자 말은 거부 유지·거부 내용 열람, 자동 제목은 사람 제목을 덮어쓰지 않음, 라이브는 관찰. 강제 축 넷 그대로 |

**P4 완료 조건:** 여섯 Profile 대표·혼합 목적·실패/복구/변경을 재현하고 Case 간 최소 지식 관리·추출·주입을 검증한다. 지식은 첫 버전 범위이며 FR-19/QG-08을 일괄 후속 확장으로 제외하지 않는다. QG-08은 모든 후보의 별도 AI 실행·사람 승인을 의무화하는 절차가 아니다. 범용 검색·외부 지식 시스템과 공동 사용 UI는 범위 밖이다.

**수용 확인(S-033, [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md)):** 혼합 목적의 계약·실패/복구/변경·Case 간 지식 관리·추출·주입은 근거 완비(자동·브라우저·이행 시험 + 실제 codex 라이브 P4-05·06·06b·07·UI-04a). 여섯 Profile 은 계약·판정 수준(P4-03)에서 전부, 제품 흐름 수준(P4-05 진행기 경유)에서는 feature·research·RCA 만 재현됐다 — defect-fix·refactoring·maintenance 와 혼합 목적의 진행기 경유 완료는 [P4-PLAN-08](plans/P4-PLAN-08.md)(READY). FR-19/QG-08 은 채택 확인·후보의 권한 없음·주입≠준수까지 첫 버전 범위로 구현됐고 독립 AI 검토 실행은 선택 사항으로 없다. P4 를 닫을지는 사용자의 결정이다. **S-034(사용자 결정 2026-09-24):** P4-08 을 마쳤고([P4-08 결과](p4/evidence/P4-08-results.md)) 검증 환경 실험([P4-ENV 결과](p4/evidence/P4-ENV-results.md))으로 실제 codex 가 시험을 돌리는 조건을 찾았으며 P4 를 `DONE_WITH_LIMITATIONS` 로 닫았다(4절). 남은 제한은 9절.

## 7. P5 — GitHub·외부 결과

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| P5-01 연결·읽기 | 대표 Repo의 Git remote 후보·기존 인증/접근 확인, 선택 연결, 업무 이슈 위치/참여 Repo, 원래 이슈·PR·CI | 읽기 출처/버전 연결, 비밀정보 비기록, 모호한 remote 선택, 연결 유예와 연결≠게시 허용, 읽지 못한 CI 증거 표시 |
| P5-02 기록 게시 | 논의 단계에는 이슈 없음. 업무화 뒤 연결·허용 안에서 지정 Repo의 Case당 새 이슈, 요약 append·Outbox | 참여 Repo별 이슈 복제 없음, 코드/diff/상세 로그 제외, 코드·기록 겸용 가능하되 별도 코드 권한 필요, 응답유실 중복 방지 |
| P5-03 범위 허용 push·PR | Case 시작 Grant, 정확한 대상/payload 직전 검사, 저장소별 외부 결과 | 범위 내 반복승인 없음, 미허용 Repo/ref/행동 차단, 부분 실패·결과 불명 대조, 같은 원격 ref/PR 경합 검증 |
| P5-04 controlled·CI·리뷰 | 결과 확인→자동게시→필수CI→closure, PR 피드백과 stale 검사 | CI 수정으로 후보내용 변경시 재확인. 동일내용 재전송 반복확인 없음. 상대 Repo 준비 대기와 코드결함 구분. 완료후 수정 새 Case, 일반 이슈댓글 승인/명령 해석 금지 |

실제 외부 쓰기 시험은 시험 대상·범위를 별도로 허용받은 경우만 수행한다. 이 제품의 자동 게시 설계를 개발한다는 지시가 이 개발 저장소의 push 권한은 아니다. 실제 시험을 못 하면 계약시험과 미검증을 구분한다. 강제 push·병합·배포·이슈 종료·CI 수동 재실행·GitLab은 범위 밖이다.

## 8. P6 — 원격·운영·전체 수용

| 하위 작업 | 범위·검증 |
|---|---|
| P6-01 원격·인증 | 소유자/Runner 등록·폐기, LAN/사용자 VPN 암호화, 미등록/권한 회수 거부. 한 Case를 여러 PC에 분산하지 않음 |
| P6-02 데이터 경계 | 상세 문서·대화·지식·코드·증거가 소유 Runner에 남고 제어부 DB/로그/큐/캐시에 영구 복제되지 않음. 과거 지식 소유 PC 오프라인 시 가용성 표시 |
| P6-03 단절·복구 | 실제 CLI/모드별 도구 경계 정지·다음 호출 차단, 지연 결과·부분 외부 효과·예산 예약 재조정. 종료불명 중복쓰기 금지 |
| P6-04 백업·설치 | 호스트별 하루 1회·최근 7개·원본 자동삭제 없음, 부분복원/미푸시·미커밋 보존. Python/Git 준비 후 일반 프로세스 설치·업그레이드·빌드UI |
| P6-05 규모·최종 수용 | 10 Project·3 Runner·동시 AI 2·Runner쓰기 기본 1에서 실제 측정. 전체 FR/NFR/[수용 시나리오](review-acceptance-matrix.md)와 증거 연결. 지표 미제공·한도 강제불가·미실증을 정직하게 표시 |

세 Windows 배포 형태를 확인한다. 모의 프로세스와 실제 호스트·외부 서비스 시험을 구분한다. OpenCode 등 미검증 제한은 보존하고 필수 기준을 임의 면제하지 않는다. Linux/WSL·공동 사용·복수Project Case·분산Case·인터넷 직접 공개는 이후 범위다.

## 9. 이어받는 한계와 처리 위치

| 현재 구현/관측의 한계 | 후속 처리 |
|---|---|
| **가벼운 확인은 원문의 의미를 보지 않는다** | `conformance_check.unverified_scope` 에 그대로 남는다. **P4-01이 해소한 것은 선택·강제 경계**다 — 독립 검토가 필요한 조건에서는 가벼운 확인으로 통과하지 않고 Case 설정으로 낮출 수 없으며, 별도 reviewer 세션과 고정 참조를 요구한다. 가벼운 확인 자체가 의미 검토로 승격되지는 않는다 |
| **delta 분류가 본문을 읽지 않는다** | 제어부는 구조화된 사실만 보고 동의된 항목의 AI 출처 변경을 전부 `material` 로 본다. **P4-02가 정한 것은 그 다음 질문이다** — 변경이 어느 판정을 내리고 어느 증거를 재사용하는지를 `change_impact` 가 **참조 대조로만** 정하고, 범위를 모르면 `unknown` 으로 재검증한다. 분류기를 의미 판정기로 바꾸지는 않았다 |
| **Fast Lane 판정은 수준 판단에 의존한다** | 수준은 AI가 축별 근거로 제안하고 사람이 조정한다(P3-01). P4-01이 설계·계획 품질 게이트와 조건부 독립 검토를 붙일 수 있게 했어도 수준 판단 자체의 진위를 자동 판정하지 않는다. **P4-03에서 본 것:** 조사 Profile(RCA·research)의 대표 흐름은 분석·실험 실행만 쓰고 준비 산출물을 요구하지 않으므로 Fast Lane 판정이 끼어들 자리가 없다. Fast Lane은 제품 변경 Profile의 준비 양을 정하는 판정이며 P4-03은 그것을 바꾸지 않았다 |
| **실험 표시는 배정 목적에서 나온다** | `run.is_experiment` 는 요청이 주장하지 못하고 목적에서 도출된다. **P4-03이 두 가지를 더했다** — 실험 실행은 조사 결론(`investigated`)의 근거로만 쓰이고 제품 의무의 근거가 되지 않으며, 실험이 작업공간에 **임시 변경을 남겼는지**를 실행 전후 트리 지문으로 도출한다(`left_changes` 등). 제품 목적 Case의 잔여는 자동 완료를 막는다. **관측은 여전히 격리가 아니다** — "되돌렸다"는 관측한 트리가 같다는 뜻이고, 작업공간 밖 변경은 감지만 한다(D-44) |
| **시간 예산의 hard 한도는 절대 상한이 아니다** | 돌고 있는 CLI 를 초 단위로 끊을 수 없다(P1-03 `cancel_confirmed = unknown`). `enforced_no_absolute_cap` 으로 표시하고 실제 초과를 값·원인과 함께 기록한다. **UI-02가 사람의 중단으로 트리를 끝내고 확인하는 능력을 만들었다**(codex 실증). 한도 도달에 자동 중단을 연결하지는 않았다 — 한도 강제의 의미를 바꾸는 결정이다(D-88). 여전히 절대 상한이 아니며 전체 복구는 **P6-03**. D-88 지표 선택도 이 한계를 없애지 않는다 |
| **검증의 취소 요청과 실제 종료는 여전히 다른 값이다** | P4-02는 `stop_requested_at` 만 적고 상태를 `completed` 로 바꾸지 않는다. 요청 뒤 도착한 결과는 원래 실행에 저장되고 `late_result` 로 남는다. **실제로 언제 멈췄는지는 모르는 그대로**다. **UI-02가 실행 단위의 실제 종료·잔류 확인을 만들었지만 검증 1회에는 연결하지 않았다** — 검증 1회(`quality_gate_run`)와 reviewer 실행이 결과 기록 때에야 연결돼(`reviewer_run_id`) 실행 중에 끝낼 프로세스를 모른다. 연결에는 P4-02 모델 변경이 필요하다(검증 흐름을 다루는 작업의 별도 plan). P6-03 전체 복구 수용 유지 |
| **권한 철회 기계 자체는 만들지 않았다** | P4-02는 권한 변경을 변경 사건으로 받아 영향받는 증거만 재검증 대상으로 만들고, 그것이 게이트 예약과 달리 검증 종료를 기다리지 않으며 기존 판정·외부 효과를 되돌리지 않는다는 경계를 시험으로 고정했다. 실제 철회·중지 제어의 실증은 **P6-03**, 외부 효과 대조는 **P5** |
| **토큰·비용은 어댑터가 줄 때만 안다** | 미제공은 `unavailable` 이며 0 이 아니다. codex 는 비용을 주지 않아 `estimated_cost.complete = false` 다. 실제 청구액 대조는 범위 밖이며 추정 지표의 hard 한도는 계속 설정을 받지 않는다 |
| **CLI 내부 모델 호출 수는 관측 범위 밖이다** | `run_count` 는 **시스템이 시작한 호출**이다. 그 계약을 `reservation_contract` 에 값으로 남겼고 다른 것으로 표시하지 않는다 |
| **예약은 한 제어부 DB 의 트랜잭션으로만 보장된다** | 여러 제어부·원격 환경의 예약 조정은 **P6-03**. 지금 보장 범위를 분산 환경으로 넓혀 표시하지 않는다 |
| **게시 허용(`publish_allowed`)은 여전히 기록뿐이다** | 네 축이 강제로 바뀌었다고 게시까지 강제되는 것으로 읽지 않는다. controlled 의 결과 후보 확인도 push·게시 권한을 만들지 않는다(D-65). 실제 push·PR·기록 이슈와 외부 CI 대기는 P5다 |
| **한 실행은 작업공간 하나에서 돈다** | **P3-04에서 실제로 돌려 봤고 바꾸지 않았다.** 계획이 Task 마다 저장소를 정하고(v12) 각 실행이 그 worktree 에서 돌면 두 저장소 기능이 실제로 완성된다 — 계약을 계획 산출물이 고정하기 때문이다. 한 실행이 여러 작업공간을 보게 하는 것은 **필요하지 않았다.** 다만 이 시험의 계약이 단순했다는 것도 사실이며, 드러나지 않은 것을 "문제없음"으로 적지 않는다 |
| ~~**검증 실행이 이 호스트에서 시험을 돌리지 못한다**~~(P3-03·P3-04 관측 → P4-ENV 원인 → **P4-09 (b) D-91 로 해소, S-035**) | 쓰기 실행(구현·검증·실험)의 CLI 샌드박스를 껐다(codex `danger-full-access`). **제품 경유 라이브에서 처음으로 검증 실행이 실제 명령을 돌려 기준 전부가 `met` 으로 적혔다**([P4-09 결과](p4/evidence/P4-09-results.md) 3.2절 — 이 PC 의 Python 프로젝트). 읽기 전용 실행(분석)의 도구 문제는 남는다 — P4-ENV 의 ACL 조치 유지, 필요하면 Runner 기동 시 점검·프로젝트별 검증 환경 설정(P4-ENV 4절)은 별도 |
| **근거의 진위는 여전히 시스템이 판정하지 않는다** | P4-01은 요청/기준/대상/증거 **참조**와 독립 reviewer를 요구하고 작성자의 완료 주장만으로 일반 게이트를 통과시키지 않는다. 그러나 참조된 명령·종료 코드가 실제인지까지 검증하지는 않는다. 앞선 거짓 `met` 기록은 소급 승격하지 않으며 이 한계를 숨기지 않는다 |
| **조합의 변화는 관측으로만 안다** | 제어부는 git 을 보지 않는다. 실행이 없는 저장소의 항목은 `snapshot_incomplete` 이며 그것은 "모른다"이지 "안 바뀌었다"가 아니다 |
| **`run.repository_id = NULL` 인 옛 실행** | 작업공간이 하나뿐이면 그것으로 해석하고, 저장소가 늘어나면 어느 항목에도 붙지 않는다. 화면이 `unattributed_run_effects` 로 드러내며 지어내지 않는다 |
| Project·Task 단위 정책 조정 없음, ~~Profile 재분류 경로 없음~~ | Project 기본값은 UI-04b 가, **활성 Case 의 Profile 개정(D-86)은 UI-04c 가 구현했다** — P4-03 이 미룬 `relates_to` 문제는 이전 Profile 의 의미 항목을 새 문서에 **유지 항목**으로 남기고(버전 행이 자기 Profile·유지 항목을 적는다) 그 항목의 기준을 그 Profile 의 의무로 도출해 풀었다. 남은 한계: 개정은 정의판 v2 Case 만(v1·미기록은 거부), 같은 조사 Profile 안의 목적 추가는 분석을 다시 돌리지 않는다, 개정 뒤 더한 기준은 material delta 로 확인 대상이다(기존 규칙), 끝나지 않은 실행이 있으면 거부한다. Task 층 정책 조정은 여전히 없다 |
| `project.repo_path` 와 `project_repository` 에 같은 값이 두 곳에 있음 | **R2에서 정본을 `project_repository` 로 옮겼다.** 컬럼은 지우지 않았고 조회가 불일치를 드러낸다(`legacy_repo_path_matches_registry`). 컬럼 제거는 이후 범위 |
| 실제 변경이 없으면 구현 실행 실패 | **R4가 기준 판정 쪽을 나눴다** — `satisfaction` 으로 무변경 충족(`already_satisfied`, 검증 실행 증거 필요)과 미재현(`not_reproduced`, `met` 불가)을 구분한다. **실행 자체의 판정은 그대로다** — 변경도 명령도 없는 구현 실행은 여전히 실패다. **P4-03이 정한 것:** 무변경 완료는 구현 실행의 성공이 아니라 **검증 실행의 관측**(`already_satisfied`·`preserved` 는 검증 실행만 근거)이고, 조사 Profile의 무코드 완료는 분석·실험 실행의 근거(`investigated`)다. 그래서 구현 실행의 판정을 바꿀 필요가 없었다 |
| 명령·종료 코드가 AI 자기보고 | **P4-01에서도 남는다.** 일반 게이트는 증거 참조와 reviewer 분리를 강제하지만 외부 실행 사실을 새로 증명하지 않는다. 자기보고를 검증된 실행으로 소급 승격하지 않으며 실제 실행 확인 경로 없음을 판정과 함께 보인다 |
| Task·기준 연결 적합성, 질문 blocks 자유문자열/불명 범위 전체차단 | **P4-01이 부분 해소했고 P4-02가 변경 뒤 유효성을 붙였다.** required 발견은 전달된 `criteria_refs` 에 연결돼야 하고, 연결되지 않은 의견은 advisory다. 요청·코드·권한이 바뀌면 그 참조를 가진 검사만 `needs_recheck` 가 되고 나머지는 이유·참조 버전과 함께 재사용된다. 다만 **참조 관계의 의미 진위**까지 제어부가 읽지는 않으며 불명 관계는 계속 안전한 쪽으로 막는다 |
| 작업공간 밖 변경은 감지만 하고 막지 못함, 원래 Repo 밖은 미관측 | 기존 capability/한계 보존. worktree 는 파일 배치의 분리이며 OS 격리가 아니다(D-44). **R4의 로컬 실험도 같다** — 쓰기를 여는 조건은 늘렸지만 통제 능력은 그대로이며 격리 보장으로 표시하지 않는다 |
| 기준 커밋 기록이 없을 때 P3-03은 merge-base/HEAD 복구 | 복구 경로는 그대로다. **R2의 조합이 `snapshot_incomplete` 로** "기준 커밋만으로는 실제 입력을 설명할 수 없다"를 표시한다. 현재 HEAD를 원래 검증 기준으로 조용히 승격하지 않음 |
| **예산 도달의 기능 흐름 라이브가 없다** | R3 의 예약·정산·정지는 자동 시험으로 고정돼 있고 hard 도달의 거부도 그 시험이 확인한다. **기능이 진행되는 도중에 걸리는 모습**은 P3-04 의 경로 C 이며 **사용자 지시로 스킵**했다(2026-09-22). 예산 라이브가 필요해지는 작업에서 `p3/live/path_bc.py --path c` 로 다시 본다 |
| **사람의 예외 수용이 종료를 연다** | 미검증 기준이 남아도 사람이 그것을 수용하면 Case 가 닫힌다(`closed_with_exceptions`). 원래 판정은 보존되고 자동 완료는 스스로 수용하지 못한다. **P4-05가 그 자리를 새 화면의 예외 카드(기준 선택·범위 입력·인수)로 옮겼다.** 예외의 **적절성** 판정은 여전히 없다 — 무엇을 수용했는지가 값으로 남는다 |
| 잔류 활동 확인의 범위, 안전중지 제품경로 미연결 | **UI-02가 잔류를 확인한다** — CLI 가 job 안에서 돌고 확인 근거(`job_empty`·`job_terminated`·`job_closed_kill_on_close`·`host_rebooted`)가 있을 때만 `none` 이다. 확인되면 시간 지표도 확정된다. **비 Windows·시작 기록 없는 옛 실행은 여전히 `unknown`** 이다. P1-03의 훅 기반 안전 경계 정지(`safe_stop_next_call`)는 제품 경로에 연결하지 않았다(사람의 중단과 구분) — P6-03. R4의 자동 완료는 미정리 실행(끝났는지 모르는 중단 실행 포함)이 남으면 종료를 확정하지 않는다 |
| **종료 Case 는 설명 전용 진입만 연다**(P4-05) | 요청에 묶인 읽기 전용 논의 응답만 진입 검사가 허용하고 다른 모든 Run·정책 변경은 `case_already_closed` 그대로다. 종료 시점 소비·한도 snapshot(`closure_record.snapshot_json`)과 종료 뒤 소비 구분, 예산 한도 변경 이력이 있다. **v20 이전에 종료된 Case 는 snapshot 이 없어** 종료 뒤 소비를 나누지 못한다(`since_closure = null`) — 지어내지 않는다 |
| 시간 계측과 UI 기본 지표의 차이 | 현재 실행시간은 `assigned_at`~`finished_at`, 경과시간은 `Case.created_at`~조회 시각이다. **P4-05가 종료 시 소비 snapshot 을 만들었다.** **UI-04c 가 D-88 을 그 위에 구현했다** — 지표의 뜻은 그대로(실행 안에 사람 대기는 없고 대기열 대기는 밖이다; Runner 준비·보고 시간은 든다)이며 화면의 기본값·이름표·시간 절과 재배정 옛 세대의 계측 결함(벽시계로 영원히 자람)만 고쳤다. 재배정 자체의 정산 의미(옛 세대의 소비가 실제인가)는 P6-03 그대로다. 기존 지표·한도·미확정 정산을 조용히 바꾸지 않는다 |
| ~~크기 한도 미구현~~, 열람 요청 만료·원문 PC 경계 | **P4-04가 크기 쪽을 구현했다** — 실행당 인라인 한도, 보조만 드러내어 생략, 핵심 초과·핵심 원문 미확인은 보류, Runner 영수증의 해시 대조. **P4-06b 가 지식 원문만 서버로 옮겼다**(사용자 결정) — 그 열람은 PC 와 무관하다. 열람 요청 만료와 지식 외 원문 PC 경계 전체는 **P6-02** |
| **한도는 바이트이고 제안값이다**(P4-04) | 인라인 한도 기본 256 KiB 는 상세 설계 제안값이며 토큰이 아니라 `artifact_ref.byte_size` 다. CLI 가 작업공간에서 더 읽는 것과 CLI 내부 압축은 여전히 모른다(`context_bytes` 계약 그대로). 설정은 제어부 환경 변수 + **프로젝트 기본값**(UI-04b, 설정 화면 — 새 실행부터)이며 Case 별 조정은 없다(대화별 조정이 필요해지면 그때 정한다) |
| **분할 검토·검토 체크포인트·단계적 조회가 없다**(P4-04) | 핵심이 한도를 넘으면 **보류**로 드러낼 뿐 나눠서 검토하지 않는다. 지금 고정 문맥은 의도·준비 문서와 대화라 나눌 대상이 거의 없고, 큰 대상(diff)은 CLI 가 작업공간에서 읽는다. 하위 검토를 게이트 판정에 합치려면 P4-01/02 판정·repair 모델을 함께 바꿔야 한다 — 실제로 큰 대상 검토가 필요한 작업에서 별도 plan. 생략한 보조 자료를 CLI 가 스스로 읽는 경로는 CLI·권한 모드별 실증이 필요하다 |
| **Runner 가 본 원문 누락을 제어부가 가용성으로 되먹이지 않는다**(P4-04) | 영수증의 `missing`·`hash_mismatch` 는 그 실행의 기록이고 `artifact_ref.availability` 를 바꾸지 않는다. 같은 누락은 다음 실행마다 영수증으로 다시 드러나고 소비 0 으로 끝난다. 원문 목록 재동기화·백업 복원 대조는 **P6-04** |
| **사람의 재배정은 원장이 재실행을 막아도 새 세대 예약을 잡는다**(P3-R3 규칙, P4-04에서 확인) | **UI-02가 재시작 경로를 고쳤다** — 재기동한 Runner 는 세대를 올리지 않고 원장으로 대조하므로 새 예약이 없다. 사람이 쓰는 재배정(`POST /api/runs/{id}/reassign`)은 그대로 새 세대 예약을 잡는다(과대 쪽). 그 정산은 **P6-03** |
| **최신성은 표시다**(P4-04) | 고정 뒤 새 입력이 있어도 결과·판정·채택을 바꾸지 않는다. 게이트 판정의 재검증은 P4-02 의 변경 사건·참조 대조가 맡는다. **P4-05 의 진행기는 최신성을 조건으로 쓰지 않는다** — 새 의도 버전은 준비·그래프의 `stale` 로 잡히고, 결과 채택 시 최신 입력 대조는 여전히 표시다 |
| **요청 `unknown` 은 확인 근거가 있어야만 풀린다**(UI-01 → UI-02) | UI-02가 해제 경로를 만들었다 — 확인되지 않은 실행을 Runner 가 다시 확인해 근거가 오면 `interrupted` 로 풀린다. **확인하지 못하면 계속 잠겨 있을 수 있다** — 사람이 확인을 선언하는 경로는 만들지 않았다(D-76). 비 Windows·옛 실행은 재부팅 근거로만 풀린다. 그 동안 다른 대화는 쓸 수 있다 |
| **배정은 원문 소유 PC 를 보지 않는다**(S-026 발견, P4-06b 라이브 둘째 시도) | `claim_assignments` 는 대기 실행을 **먼저 폴링한 Runner** 에게 준다. Runner 가 둘이면 PC A 의 대화 응답을 PC B 가 맡아 지시 원문(A 소유)을 읽지 못해 `required_context_unavailable` 로 실패한다 — 도구 능력도 보지 않는다. 지금까지 모든 시험·라이브가 한 Runner 였다(두 PC 시험·라이브는 Runner 를 차례로 돌린다). "한 Case 를 여러 PC 에 분산하지 않는다"(P6-01)와 같은 자리이며 **P4-06b 는 배정 경로를 바꾸지 않았다** — 지식 원문은 어느 PC 에서도 읽히지만 지시·대화 원문은 여전히 소유 PC 만 읽는다. 원문 소유 PC·도구 능력으로 배정하는 규칙은 P6-01 의 plan 에서 정한다(제품 판단: 소유 PC 가 미연결이면 기다리는가, 사람에게 알리는가). **사용자가 S-026 에 위임 → 판단: P6-01 에 둔다.** 지금 운영은 한 제어부·한 Runner 이고(모든 시험·라이브가 그렇다), 미연결 시의 대기/알림은 Runner 등록·인증 설계와 함께 정해야 한다. 그때까지의 운영 지침: **한 제어부에 Runner 하나**(둘을 쓰려면 차례로) |
| **PC 연결 판정은 heartbeat 공백 하나다**(UI-02) | 미연결(기본 15초, 상세 설계 제안값·제어부 환경 변수)이면 사용자 입력이 409 `runner_disconnected` 다. 한 제어부·한 PC 기준이며 네트워크 분할·원격 PC 인증은 **P6-01·P6-03**. 원문 열람 요청은 서버가 미연결이어도 기존대로 대기시킨다. **UI-03 화면은 미연결 PC 로 새 열람 요청을 만들지 않고 "연결 필요"를 보인다**(다른 호출자는 여전히 대기시킬 수 있다) |
| **정상 종료 실행의 잔류 `unknown` 은 요청을 잠그지 않는다**(UI-02) | UI-01 규칙을 소급하지 않으려고 유지했다(지금까지의 모든 실제 CLI 실행의 값). 새 어댑터는 정상 종료에서도 확인해 `none` 을 보고하므로 새 실행에서는 드물다. 비 Windows·확인 실패의 정상 종료 실행은 잔류가 남았을 수 있는데 잠그지 않는다 — 트리 제어를 넓히는 작업에서 다시 본다 |
| **정상 종료 뒤 남은 프로세스를 끝낸다**(UI-02) | 실행의 경계를 CLI 호출 하나로 보고 루트가 끝난 뒤 남은 프로세스를 그 실행의 잔류로 종료·계수한다(P1-03 (가)). AI 가 일부러 띄운 백그라운드 프로세스(개발 서버 등)도 끝난다. 결과에 종료한 수가 드러난다. 이 기본이 맞지 않는 업무가 생기면 제품 판단으로 다시 묻는다 |
| **claude 실제 중단 미실증·비 Windows 트리 제어 없음**(UI-02) | 같은 OS 수단이지만 claude 는 실제로 중단해 보지 않아 `cancel_confirmed = unknown` 이다(D-76 "CLI별 실증"). 비 Windows 는 트리 제어를 지원하지 않는다고 보고한다(`process_tree_control = unsupported`) — Linux/WSL 은 이후 범위 |
| **P4-02 시험 하나가 시계 해상도로 흔들린다**(S-022 관찰) | `test_quality_changes.py::test_the_reservation_lands_when_the_verification_ends_even_on_a_failure` 가 예약 요청 시각과 반영 시각이 **다르다**고 단언한다. 이 PC 의 Python 3.12 벽시계는 `GetSystemTimeAsFileTime`(해상도 15.6ms)이라 두 요청이 한 틱 안에 끝나면 같은 값이 된다. UI-02 를 적용하지 않은 HEAD 워크트리에서도 3회 중 2회 실패해 **이전부터 있던 흔들림**임을 확인했다. 제품 규칙(요청→반영 순서)의 문제가 아니라 시험의 가정이다. UI-02 범위 밖이라 고치지 않았다 — 순서(`>=`)로 바꿀지는 그 영역을 다루는 작업에서 정한다 |
| **요청 처리는 논의 응답과 업무 단계 진행이다**(UI-01 → UI-03 → P4-05) | UI-03 의 제어부 요청 처리기가 저장된 요청마다 **읽기 전용 논의 응답 하나**를 만들고 그 결과로 요청을 끝낸다(기동 복구, `HADS_AUTO_PROCESS_REQUESTS=0` 이면 UI-01 명시 계약). **P4-05 부터 업무 단계의 다음 작업은 진행기가 잇는다**(`HADS_AUTO_PROGRESS_WORK`, 처리기가 켜져 있을 때). 업무 단계의 사용자 메시지는 여전히 읽기 전용 응답만 받고 해석하지 않는다(D-86 은 UI-04). 처리기가 켜진 제어부에서 관리 화면의 "논의 응답 실행"·"처리 완료 기록"은 이미 처리된 요청에 거부로 답하고, 관리 화면의 실행 버튼은 진행기가 만든 실행과 겹칠 수 있다(진행기는 끝나지 않은 실행이 있으면 기다린다) |
| **AI 해석 업무화는 되돌릴 경로가 없다**(UI-01 → UI-03) | 준비 단계 응답의 AI 해석이 명확한 업무 요청이면 같은 Case 가 업무화된다(D-69). 논의를 업무로 잘못 읽으면 준비 단계로 돌아가는 경로가 없고 Profile 변경은 D-86(UI-04)이다. 그래서 규칙을 좁게 뒀다(명시적 작업 요청만, 모호하면 논의로 두고 글로 묻는다). 실제 codex 라이브에서 논의 1건은 논의로, 분석 요청 1건은 `research` 업무 요청으로 읽었다 — 표본은 회차당 둘이다. 해석은 권한이 아니며 업무화는 실행을 시작하지 않는다 |
| **기존 접수 네 경로의 멱등 키가 없다**(UI-01 → UI-03) | UI-03 이 `/artifacts`·`/intent-drafts`·`/feedback`·`/questions/{id}/answer` 도 **기록 전에 중계**하게 바꿔 커밋-중계 경합은 없어졌다. 멱등 키는 여전히 없다(새 화면의 사용자 입력은 메시지 경로를 쓴다). 접수는 기록됐는데 뒤의 기록(의도 버전·피드백)이 거부되는 짧은 창에서는 원문이 PC 에 저장될 수 있다 — 가리키는 기록이 없는 원문이며, 이전에는 같은 경우 유실로 표시됐다 |
| **요약이 본문 발췌인지 서버가 판별하지 못한다** | 메시지를 포함한 모든 `summary` 는 호출자 규칙이다. UI-01 화면·하네스는 본문이 아닌 표시(`사용자 메시지 · N자`)를 쓴다. **UI-04d 의 검색은 그 규칙 위에 선다** — 서버 필드 검색은 요약을 "본문이 아닌 짧은 값" 으로 대조할 뿐이며(그래서 오프라인 검색은 사실상 제목·규칙·개정 사유·업무화 요약·결정 종류다), 본문 일치는 소유 PC 만 낸다. 호출자가 요약에 본문을 넣으면 서버가 그것을 찾게 되는 것은 여전히 그 호출자의 규칙 위반이다 |
| **검색은 색인 없이 선형이고 메모리다**(UI-04d) | 후보 상한(2,000건·최신순)까지 소유 PC 가 원문을 하나씩 읽는다 — 넘으면 `truncated` 로 드러내고 오래된 메시지는 보지 않는다. 낱말 AND 부분 문자열 일치뿐이다(정규식·형태소·유사어 없음; 조사가 붙은 낱말은 부분 일치로 잡힌다). 검색 요청·발췌는 제어부 메모리에만 있어 재시작·10분 뒤에는 404 이고 PC 가 60초 안에 답하지 않으면 `expired` 다(빈 결과가 아니다). 여러 PC 면 연결된 PC 만 검색하고 나머지는 제외 수다. 색인·다른 일치 규칙은 필요해질 때 |
| **미커밋 포함은 이진 선택이고 HEAD 기준에서만이다**(UI-04d) | 전부 포함 / 전부 제외뿐이다(파일별 선택은 경로가 브라우저→서버→PC 로 오가야 해 넣지 않았다). 다른 브랜치·커밋 기준(`base_ref`)은 기존 API·관리 화면으로 고르고 그때는 커밋된 코드에서만 시작한다(포함과 함께 오면 `failed`). 목록은 메모리라 재시작·30분 뒤엔 카드가 "PC 에서 다시 불러온다" 를 보인다. 스냅샷 커밋은 Case 브랜치의 첫 커밋으로 남아 P5 의 push·PR 에 따라간다. `.gitignore` 된 파일은 목록에도 포함에도 없다. 후속 Case 의 "이전 결과 기준" 제안(D-77)은 없다 — 후속 대화도 HEAD 기준이다. 옛 작업공간 행의 `start_basis` 는 NULL(기록 없음)이며 `committed` 로 읽지 않는다 | **P4-09 (c):** 후속 Case 는 이전 업무 결과(이전 Case 브랜치의 끝 커밋)에서도 시작할 수 있다 — 직전 이전 Case 하나의 같은 저장소 브랜치만 보며, 이전 결과를 고르면 미커밋 변경은 포함하지 않는다(포함은 HEAD 기준에서만). 갈라진 경우 HEAD 의 새 커밋은 없다(카드가 그때의 커밋된 코드를 함께 적는다). "다른 커밋에서 시작" 입력은 UI-05b |
| **시험용 가짜 CLI 는 쓰기 권한 실행에서만 파일을 쓴다**(UI-04d, 시험 도구) | 그 전에는 읽기 전용 실행(논의 응답·의도 작성)마다 사용자의 원래 저장소에 파일을 써 트리를 더럽혔고, D-77 아래서는 모든 진행기 시험이 선택 대기로 갔다. 실제 읽기 전용 CLI 가 하지 않는 일이라 도구를 고쳤다 — 제품 규칙이 아니다. 실제 CLI 가 읽기 전용 실행에서 원래 저장소를 고치는 경우는 P3-03 의 `outside_workspace_changed` 관측 그대로다 |
| ~~**여섯 Profile 의 진행기 경유 대표 흐름은 셋뿐이다**~~(S-033 발견 → **S-034 해소**) | [P4-08 결과](p4/evidence/P4-08-results.md) — `tests/test_profile_flows.py` 13건이 defect-fix·refactoring·maintenance 와 혼합 목적을 진행기 경유로 고정했다(`decide_criteria` 의 `preserved`·`already_satisfied`·검증 근거 `investigated` 분기 포함). 제품 코드 무변경. 남은 것: 검증 실행의 트리 변경은 제품 변경으로 세지 않는다(P4-05 규칙, P4-08 결과 4절) |
| ~~**이월 질문의 답이 작업 실행의 입력이 아니다**~~(이슈 #2 → **P4-09 (a) 해소, S-035**) | 답한 이월 질문의 답 원문이 그 질문이 막던 Task 의 구현·검증·실험·분석 실행에 핵심 참조로 들어가고 지시문 머리에 질문 키·요약이 적힌다([P4-09 결과](p4/evidence/P4-09-results.md)). **남는 것:** 되묻는 답(결정이 아닌 답)도 `answered` 로 닫혀 그대로 전달된다 — 그 Task 가 결정을 얻지 못할 수 있다(별도 검토); 설계 단계 답이 계획 작성 입력에 가는지는 관찰만 남았다 |
| ~~**업무(Case)를 취소할 경로가 없다**~~(이슈 #4 → **P4-09 (e) 1단계 해소, S-035**) | 끝나지 않은 실행이 없는 Case 를 사유·행위자로 취소한다(후보 없는 종료 기록, 보존, 되돌리지 않음, 취소 뒤 훅 무반응). **실행 중 Case 의 취소(2단계 — 중단 연결·종료 확인 대기·"취소 요청됨" 표시)는 없다** — 먼저 요청 중단(대화 화면)·실행 중단(관리 화면) 뒤 취소한다 |
| ~~**거부된 지식 후보의 내용을 볼 수 없고 거부 자체가 지시문의 빈틈에서 난다**~~(이슈 #5 → **P4-09 (f) 해소, S-035, D-92**) | 지시문에 활동 여덟·경로 규칙, AI 후보는 원래 값을 보존한 채 후보 등록(주입 제외·채택 확인 `scope_unreadable`), 거부·등록 행의 내용 열람과 수동 등록 채우기. **남는 것:** 사용자 말의 자동 등록은 범위가 틀리면 여전히 거부다(의도한 유지); 범위를 읽지 못한 후보의 주입 제외는 상세 설계 선택 |
| ~~**대화 제목이 "새 대화" 로 고정되고 바꿀 수 없다**~~(이슈 #6 → **P4-09 (g) 해소, S-035, D-93**) | 첫 논의 응답의 40자 자동 제목과 업무 요청 응답의 한 번 갱신, 사람의 이름 바꾸기(머리·목록, 종료·보관 대화도), 출처·직전 값 기록. **남는 것:** 자동 제목은 해석 블록을 받는 응답(준비 단계·종료 Case 의 논의 응답)에서만 온다; 옛 행의 출처는 NULL 이며 기본 문구일 때만 자동 제목을 받는다 |
| **관리 화면(옛 UI)이 새 화면과 어긋나고 막힘 해소가 거기에만 있다**(이슈 #7, 2026-09-24 → **UI-05a 1단계 완료, S-036**) | 전수 대조를 결정 기록으로 남겼고([UI-05a 결과](ui/evidence/UI-05a-results.md) 2절), 작업 그래프 수정·질문↔Task 연결·실행 상세가 새 화면 `작업` 탭에 생겼으며, QG-02~07 막힘은 진행기가 스스로 푼다(D-94). **남는 것(UI-05b):** 2.3절의 나머지 옮김, 실행 중 취소 2단계, 새 화면의 관리 화면 링크·문구, `?view=admin` 삭제, 관리 화면 의존 시험 5곳 |
| **실제 codex 의 Profile 표본**(S-033 정리 → **P4-09 (d) 로 RCA·maintenance 의도 작성 추가, S-035**) | 의도 작성은 여섯 Profile 전부(RCA·maintenance 는 [P4-09 결과](p4/evidence/P4-09-results.md) 3절 — 지시대로 항목·의무·결론 요구를 적었고 maintenance 는 최상위 `objectives` 를 비워 둠(기준의 의무로 목적이 도출돼 완료 의미는 성립)), 업무 진행은 feature 뿐이다. 검증 실행이 실제 시험을 돌리는지는 같은 결과 3절(샌드박스 해제 뒤 진행 라이브) |
| **의도 초안의 출처 표기가 Profile 지시를 사용자 요구로 적었다**(UI-01 라이브 관찰) | defect-fix 초안의 "미재현만으로 해결을 선언하지 않는다"가 `user_requirement` 로 적혔다(두 회차 모두). 논의의 금지 자체는 올바르게 `user_requirement` 였다. QG-01 의 `assumption_presented_as_requirement` 가 잡을 자리이며 이 라이브는 QG-01 을 돌리지 않았다. 의도 지시문을 다루는 후속 작업에서 본다 |

| **대화 본문은 탭 메모리에만 있고 새 탭마다 PC 에서 다시 불러온다**(UI-03) | 브라우저 저장소에 본문을 쓰지 않는다는 선택의 대가다. 긴 대화는 열람 요청이 많고(동시 3개), PC 가 미연결이면 새 탭에서는 본문을 못 본다("연결 필요"). **대화 검색은 UI-04d 가 구현했다**(본문은 PC 가 검색, 색인 없음) — 검색 결과에서 이동한 뒤 본문은 여전히 열람 경로로 온다 |
| **화면 갱신은 조회 반복이다**(UI-03) | 처리 중 1.5초·평소 5초. 목록은 열린 대화의 요청·단계가 바뀌면 바로 다시 묻는다. **알림(D-82)은 UI-04b 가 이 조회 위에 전이로 만들었다**(브라우저 알림, 서버 푸시 없음 — 탭이 닫혀 있으면 알림이 없고 5초 안의 두 전이는 마지막 것만 보인다) |
| **AI 응답·의도 원문은 글 그대로 보인다**(UI-03) | codex 는 Markdown(표·굵게·링크)으로 답하고 링크에 작업공간 절대 경로가 들어간다. 화면은 렌더링하지 않고 글 그대로 보인다(삽입 위험이 없다). 의도 원문은 정규 문서(JSON)를 정리해 보일 뿐 항목별 보기는 없다. 읽기 좋은 표시는 UI-04 에서 본다 |
| **제어부 DB 접근은 문장 단위로 직렬화된다**(UI-03) | 공유 연결의 동시 사용 결함(P2 부터, 새 화면이 드러냄)을 고치면서 모든 문장이 한 프로세스 잠금 안에서 돈다. 한 사용자·한 PC 의 부하 재현(25초에 9,000여 건 생성·조회)에서 0 오류였다. 큰 규모·여러 제어부는 P6-05·P6-03 |
| **브라우저 시험은 설치된 Edge 에 의존한다**(UI-03) | `tests/test_web_shell.py` 는 Playwright 로 설치된 Edge 를 연다(브라우저를 내려받지 않는다). Edge·playwright 가 없거나 `web/dist` 가 소스보다 오래됐으면 건너뛰고 사유를 보인다 |
| **권한 확대 카드는 새 화면에 없다**(UI-03 → P4-05 → UI-04b) | P4-05가 의도 동의·controlled 시작/결과·material delta·단계 검토·피드백·예외 수용 카드를 새 화면에 옮겼다. 권한 확대(게시·push 허용)는 P5. **UI-04b 가 상세 설정·수준 조정·저장소 선택·QG-02~07 설정을 새 화면(오른쪽 패널 설정 탭)으로 옮겼다**(관리 화면은 그대로). **UI-04a 가 지식 등록부·후보·채택 확인·활성화를 새 화면(프로젝트 규칙)으로 옮겼다** |
| **기준 판정은 검증·분석 실행의 보고다**(P4-05) | 제품이 `policy:work_progressor` 로 적는 기준 판정은 AI 가 보고한 것을 구조 검사(완료 실행·종료 코드 0 명령·`verifies` 연결·의무별 방식·결론 요구)로 거른 것이다. 명령·종료 코드의 진위는 여전히 자기보고이며(위 행), 검증 Task 가 없는 기준은 `unverified` 로 남아 예외 수용으로만 닫힌다. 규칙의 범위는 사용자가 확인했다(2026-09-24, 10절 S-024) |
| ~~**QG-02~07 은 진행기가 돌리지 않는다**~~(P4-05 → **UI-05a 해소, S-036, D-94**) | 명시로 켠 QG-02·03·04 가 막으면 진행기가 별도 세션 검토 → 판정 기록 → repair(게이트 수정 한도) → 한도 뒤 `quality_gate` 카드. 코드 대조로 **관리 화면도 판정을 기록하지 못했다**는 사실을 확인했다(그 전에는 켜면 끄는 것 말고 풀 길이 없었다). **남는 것:** QG-05·06·07 의 명시 ON 은 여전히 어느 실행의 조건도 아니다(기록뿐 — 연결은 별도 plan). 통과 뒤 대상이 바뀐 재검토는 P4-02 변경 사건 몫. 실제 codex 의 게이트 검토 품질은 라이브 관찰뿐 |
| **실행 제한 시간이 고정 10분이고 시간 초과가 실패와 구분되지 않는다**(이슈 #8, 2026-09-24 등록 → **P4-10**) | `runner/cli_adapter.py` 의 600초 고정, `stop_reason` 이 제어부로 가지 않음, 진행기가 같은 조건으로 재시도 → `task_failed`. 사용자 결정: 기본 1시간(D-95)·설정화·시간 초과 구분·자동 재시도 않음 — P4-10 에서 구현 |
| **검증이 기준 미충족을 보고해도 구현으로 돌아가지 않는다**(이슈 #9, 2026-09-24 등록 → **P4-10**) | 검증 Task 는 `done` 이 되고 미충족은 `criteria_unresolved` 카드(예외 수용·수정 요청)로만 끝난다. 지금의 우회로는 대화의 수정 요청(후속 업무)이나 `작업` 탭에서 수정·재검증 Task 를 직접 더하기(UI-05a). P4-10 에서 진행기의 수정 구현 → 재검증 사이클로 |
| **재작성·재시도 상한은 설정이다**(P4-05 → P4-05b) | 기본 재작성 2·재시도 1, 환경 변수 기본값 + Case 별 이력. **상한 뒤 "계속 진행" 만으로는 한 번 더 가지 않는다** — P4-05 가 "한 번 더 시도한다" 고 적은 것은 실제 동작과 달랐다(S-025 에 시험과 함께 바로잡음). 한 번 더 가는 길은 한도를 올리는 것이다. **Project 층은 UI-04b 가 더했다**(Case 명시 → 프로젝트 설정 → 시스템 기본값, 조회 때 계산, 새 화면 상세 설정에서 변경·복귀). QG-02~07 의 게이트별 수정 한도(P4-01)는 다른 설정이며 상세 설정 탭에서 게이트별로 바꾼다 |
| **진행 요청의 중단 뒤 재개는 사람이 한다**(P4-05) | 진행 요청이 `interrupted`·`unknown` 으로 끝나면 진행은 `paused` 이고 스스로 재개하지 않는다(D-76). 기동 복구도 요청이 끝나 있으면 멈춤으로 표시한다. 계속 진행은 새 진행 요청(`system_resume`)을 연다 |
| **옮겨 적은 규칙의 충실성은 판정하지 않는다**(P4-06, 사용자 결정의 알려진 위험) | 대화의 규칙은 AI 가 옮긴 글로 바로 활성 필수가 된다. 주입할 때 사용자 말(권위 원문)을 함께 넣고 카드·관리 화면에서 무효·개정할 수 있을 뿐이다. 실제 codex 라이브에서는 글자 그대로 옮겼지만 "이 프로젝트에서는" 을 **저장소 범위**로 좁혀 적었다(저장소가 하나라 적용은 같았다). AI 가 범위를 넓히거나 좁혀 옮길 수 있다 |
| **주입은 준수가 아니다**(P4-06) | 필수 규칙을 제공해도 지켰는지는 기존 기준 판정·검토가 본다. 규칙 준수를 따로 보는 게이트는 없다. 라이브의 구현은 규칙(한 줄 docstring)을 따랐지만 그것은 AI 의 행동이며 시스템이 강제·판정한 것이 아니다 |
| **지식 적용 범위의 한계**(P4-06) | 경로 조건은 조건으로 줄 뿐 실행이 다룰 경로를 미리 알지 못한다(`paths_unresolved`). 실행 중 범위가 넓어져도 그 실행에 다시 넣지 못한다. 논의 응답에는 `discussion` 활동을 적은 항목만 간다. 충돌은 사람이 기록한다(자동 탐지 없음). ~~다른 PC 의 실행은 원문을 읽지 못하면 필수면 보류다~~ — **P4-06b 가 해소했다**(서버 본문을 배정에 싣는다; 아래 행). 자동 등록은 요청 처리기가 켜져 있을 때만 |
| **지식 본문은 서버에 있고 비밀값은 검사하지 않는다**(P4-06b, 사용자 결정) | `knowledge_body` 는 유일한 본문 표이며 지식 원문과 권위 사용자 메시지 한 건만 담는다. 화면·카드·등록 규칙이 "비밀값을 적지 말 것"을 알릴 뿐 시스템이 비밀값을 판정하지 않는다 — 등록된 뒤 알게 되면 무효화(상태만 바뀌고 본문은 이력으로 남는다)이고 본문 삭제 경로는 없다. Runner 는 새 지식 원문의 사본을 두지 않는다(서버 백업이 지식 본문을 맡는다 — P6-04). 자동 등록 응답의 권위 메시지는 결과 시점에 올라가고 하나도 등록되지 않으면 지운다 — 그 사이 짧은 창에 DB 에 있다 |
| **옛 지식의 이행은 소유 PC 연결에 달렸다**(P4-06b) | v22 방식으로 소유 PC 에만 있는 지식 원문·권위 메시지(`storage = runner`)는 그 PC 가 연결돼 제어 루프를 돌 때 올라온다. 그 전까지 다른 PC 의 필수 의존 작업은 P4-06 그대로 `missing` 으로 보류되고 진입 검사는 여전히 이것을 미리 알지 못한다. 연결되지 않는 PC 의 원문은 지어내지 않는다 |
| **후보는 AI 의 판단이고 중복은 원문 해시다**(P4-07) | 무엇이 "의미 있는 사건"인지 시스템이 판정하지 않는다 — 계기 목록·상한 3건·근거 한 줄을 요구할 뿐이고, 후보를 안 남긴 실행이 "재사용할 것이 없었다"는 뜻은 아니다. 같은 뜻의 다른 문장은 다른 후보가 된다(`relates_to`/`supports` 로 AI 가 잇거나 사람이 무효·`into` 로 정리). 논의 응답의 `proposal: true` 도 AI 의 표지다 — 사용자 말이 후보로 적히면 사람이 활성화하고, 제안이 사용자 말로 적히면 카드에서 무효로 한다(P4-06 의 충실성 한계와 같다) |
| **반증 후보는 대상을 자동으로 바꾸지 않는다**(P4-07) | 활성 규칙과 다른 관측은 관계(`contradicts`)를 가진 후보로만 남고, 사람이 대상을 무효·개정하거나 후보를 그 항목의 새 버전으로 적용한다. 그 전까지 활성 규칙은 그대로 주입된다("코드 변경만으로 규칙을 무효화하지 않는다"). 자동 충돌 기록도 없다 — 사람이 충돌을 기록하면 채택 확인이 막는다 |
| **참고 후보의 자동 활성화는 반자동이다**(P4-07 → 사용자 결정 2026-09-24 → P4-07b 구현) | project-knowledge 2절의 "근거·조건을 확인하여 참고로 자동 활성화할 수 있다"의 조건을 S-027 이 물을 수 없어 가장 좁은 쪽(없음)으로 뒀고, S-028 에서 사용자가 반자동으로 정했고, **S-029(P4-07b)가 구현했다** — 시스템이 조건 여덟의 충족을 계산해 규칙 화면·카드·관리 패널에 표시하고 사람이 규칙 화면에서 한 번에 활성화한다. **사람의 클릭 없는 활성화는 없다**(`activate_ready_knowledge` 는 API 끝점에서만 불리고 훅·처리기·기동 복구 어디에도 없다). 남은 한계: 조건 (3b)의 명령·종료 코드는 자기보고(위 행), 논의 응답의 제안은 관측 문맥이 없어 조건 (4)를 만족하지 못해 사람이 항목의 폼에서 활성화한다, 같은 내용의 재보고(`duplicate`)도 근거 실행으로 센다(같은 뜻의 다른 문장은 아니다), 조건 결과는 저장하지 않고 조회 때 계산한다(후보마다 실행·명령 조회). 완전 자동은 후보가 실제로 쌓이면 사용자에게 다시 묻는다. **조건의 Project 설정화는 사용자 결정(2026-09-24, S-030): 나중에 필요하면**(지금은 코드 고정). QG-08 의 독립 AI 검토 실행은 여전히 없다(`adoption.independent_review = not_run`) |
| **별도 추출 실행이 없다**(P4-07) | 후보는 기존 작업 실행의 응답에 얹히거나 사람이 청한 논의 응답의 제안으로 나온다. 추출만 따로 도는 목적·진입 조건·예산이 없으므로 "추출 예산 부족"은 그 작업 실행의 진입 거부와 같은 것이고 별도 미처리 상태가 없다. 관측 문맥의 기준 커밋은 작업공간이 있는 실행(구현·검증·실험)에서만 오고 분석 실행은 저장소만 안다 |
| **이행 시험의 건너뜀이 늘어난다**(S-027 관찰) | `tests/test_migration.py` 의 v1·v2 이행 시험은 `controller/schema.sql` 을 바꾼 **최근 20 커밋** 안에서 옛 스키마 커밋을 찾고 없으면 건너뛴다. 스키마 커밋이 늘어 S-026 의 1건에서 S-027 시작 시 2건이 됐다(제품 무관, 시험 창의 문제). 창을 넓히거나 옛 스키마를 고정 자료로 두는 것은 그 시험을 다루는 작업에서 정한다 — 이 세션은 바꾸지 않았다 |
| **결정 사항 패널은 본문을 보이지 않는다**(UI-04a) | 목표·범위·제약의 본문은 PC 의 의도 원문이다 — 패널은 서버가 아는 버전·동의·기준 요약·판정만 보이고 `원문 열기` 로 결과물 패널을 연다. 항목별 표시(목표·범위·제약)는 원문 열람과 함께 뒤 작업에서 본다 |
| **적용된 규칙 집계는 Manifest 다**(UI-04a) | `knowledge-use` 는 이 Case 실행들의 Manifest 를 버전별로 센 것(`provided` 만 제공, 나머지는 사유별 수, v22 이전 실행은 `runs_unrecorded`)이며 실제로 읽었는가(영수증)·지켰는가(판정)는 세지 않는다. 실행별 영수증은 관리 화면의 실행 문맥에 있다. 라이브에서 활동 `verification` 후보는 검증 실행이 없는 업무에 "넣지 않음 5" 로 보였다 — 맞는 표시다 |
| ~~**프로젝트 규칙의 자리는 임시다**~~(UI-04a → UI-04b) | **UI-04b 가 프로젝트 설정 화면의 탭으로 옮겼다**(주소 `?screen=rules` 그대로, 왼쪽 목록의 진입은 프로젝트 설정 아래). 수동 등록은 **출처 대화를 고른다**(API 계약 — 출처 없는 규칙을 만들지 않는다). `into` 로 다른 항목의 새 버전이 된 후보는 두 항목의 이력·채택 기록으로만 남고 관계선은 그리지 않는다 |
| **원래 메시지로 이동은 순번이다**(UI-04a) | `&seq=N` 은 그 메시지로 스크롤·강조할 뿐 본문은 열람 경로로 오며 PC 미연결이면 "연결 필요" 그대로다. 검색(D-84)이 아니다. 그 순번의 메시지가 아직 목록에 없으면 이동하지 않고 주소의 `seq` 는 남는다(다음 조회에서 다시 본다) |
| **가짜 codex 는 첫 메시지의 규칙을 지시문으로 옮긴다**(S-028 관찰, 시험 도구) | `tests/fake_cli/fake_codex.py` 의 `HADS_FAKE_RULE` 은 고정 컨텍스트 뒤의 글에서 규칙을 옮기므로 첫 메시지(고정 컨텍스트 없음)에서는 지시문 머리를 적용 내용으로 적는다. 제품 규칙이 아니라 시험 도구의 특성이며 브라우저 시험은 두 번째 메시지에서 규칙을 말한다. 고치는 것은 그 도구를 다루는 작업에서 |
| **후속 대화의 메시지 옮김은 같은 접수를 가리킨다**(P4-05) | 종료 Case 의 수정 요청 메시지를 새 대화의 첫 메시지로 **옮겨 적는다**(같은 원문·같은 intake). 원문은 한 번 저장됐고 두 Case 가 참조한다. 원래 Case 의 메시지·응답은 남는다. AI 가 설명 질문을 수정 요청으로 잘못 읽으면 되돌릴 경로가 없다(새 대화는 준비 단계이므로 보관하면 된다) |
| **알림은 조회 위의 전이다**(UI-04b) | 화면이 5초 조회로 가진 서버 도출 값(답변 필요·진행 상태·요청 상태·종료·다른 프로젝트의 주의 수)의 전이를 계산해 브라우저 알림을 띄운다. 첫 조회·빈 새 대화·진행 중·처리 중·멈춤은 알리지 않고, 보고 있는 대화(문서 보임·포커스·같은 대화)는 억제해 기록에만 남긴다. 예산 hard 도달은 사용자 결정(2026-09-24)으로 보충했다 — 목록 행의 `budget_stopped`(hard 한도가 있는 대화만 계산, 도출값)의 거짓→참 전이. 실행 실패 자체는 별도 알림이 없고 막힘·확인 필요·요청 상태로 드러난다. 권한이 없으면 기록만 남고, 기록은 알림이 아니다. 실제 팝업의 모양은 헤드리스 시험 밖이다 |
| **프로젝트 기본값은 Project 층 하나이고 모델·깊이 기본값이 없다**(UI-04b) | 정책 문서 5절의 다섯 층 중 Case 명시·Project 기본값·시스템 기본값 셋만 있다. Task 층·Profile 기본값 층은 없고 조회에 그 출처가 나타나지 않는다. 모델 설정 자체가 없고(CLI 가 정한다) 깊이는 업무별 수준 판단이라 프로젝트 기본값을 두지 않았다 — 두려면 수준 판단의 의미를 바꾸는 제품 판단이다. 적용 시점은 그 뒤에 만든 대화·실행이고 기존 기록은 바뀌지 않는다; 진행 상한만 조회 때 계산되는 유효값이라 Case 설정 없는 기존 대화에도 지금 값이 보인다(화면이 출처를 보인다) |
| **도구 변경은 확인이 아니다**(UI-04b) | 프로젝트 기본 도구를 바꾸면 컬럼·이력에 남고 그 뒤 실행의 도구가 된다. 화면이 그 도구를 확인한 PC 를 보이지만 확인 안 된 도구를 막지 않는다 — 실행 때 진입 검사가 `tool_not_available` 로 거부한다. 진행 중 실행은 그대로다 |
| **개정의 되돌림은 또 하나의 개정이다**(UI-04c) | AI 해석 `profile_change` 나 사람의 폼이 잘못 개정했으면 이전 Profile 로 다시 개정한다(개정 n+1, 유지 항목이 누적된다). 삭제·되감기 경로는 없다 — 이력이 사실이다. AI 해석 규칙은 UI-03 과 같이 좁다(같은 문제의 명시적 요청만, 애매하면 논의) — 실제 codex 의 판단은 라이브에서만 보인다(이번에는 하지 않았다) |
| **유지 항목은 누적되고 문서가 길어진다**(UI-04c) | 개정마다 이전 Profile 의 의미 항목이 유지 항목으로 남는다(RCA → defect_fix → feature 면 열 항목이 남는다). 항목을 지우면 그 항목의 기준이 대상을 잃으므로 지우지 않는다. 유지 항목의 기준이 "이전 목적" 인지는 의무로 구별되고 화면은 유지 항목 수만 보인다 — 항목별 표시는 원문 열람과 함께 뒤 작업(UI-04a 의 한계와 같다) |
| **열기 결과는 호출 성공이다**(UI-04c) | `done` 은 Runner 의 `os.startfile`/`code` 호출이 예외 없이 끝났다는 뜻이며 창이 실제로 떴는지는 관측하지 않는다. 지원 확인은 기동 때 한 번(Windows 여부·PATH 의 `code`)이고 그 뒤 편집기를 지웠어도 능력 행은 남는다 — 그 경우 열기가 `failed` 로 돌아온다. 다른 편집기·비 Windows 폴더 열기는 `unsupported` 다 |
| **외부 변경 관측은 쓰기 실행 전뿐이다**(UI-04c) | 트리 지문은 쓰기 실행의 전후에만 계산된다. 읽기 전용 실행(검증·분석) 사이의 외부 변경은 다음 쓰기 실행이 드러내고, 제어부는 트리를 보지 못하므로 화면이 "지금 외부 변경이 있는가" 는 모른다. 보존은 worktree 를 초기화하지 않는 기존 규칙이며, CLI 가 같은 파일을 다시 쓰면 그것은 AI 의 변경이다(시스템이 되돌린 것이 아니다) |

해소된 행(Autonomy 미강제, `autonomy = NULL` 진행 미정, 여섯 칸 초안·일괄 동의, Case당 단일 작업공간)은 [보존본](development-history-v0.8.md) 4절로 옮겼다.

나머지 미검증·제한은 [P3-03 원문](p3/evidence/P3-03-results.md)·[P3-R1 원문](p3/evidence/P3-R1-results.md)·[P3-R2 원문](p3/evidence/P3-R2-results.md)·[P3-R3 원문](p3/evidence/P3-R3-results.md)·[P3-R4 원문](p3/evidence/P3-R4-results.md)의 미검증·위험 절에서 확인한다. 이 표는 위험을 없앴다는 기록이 아니라 배정 위치다.

## 10. 검증·인계 규칙

- 활성 plan의 성공 기준에 실제 근거를 연결하고 UI/API 우회·핵심 실패·재시작·데이터 보존을 필요한 범위에서 검증한다. 코드가 있거나 CLI가 정상 종료했다고 완료하지 않는다.
- 결과·판정·사람 확인·권한·외부 전달 상태를 구분한다. 제품 기대값을 구현 코드에서 그대로 복제한 시험으로 대체하지 않는다.
- 구현 후 diff와 정책 우회·비밀정보·원문 보관·변경 범위를 검토한다. 문서만 수정한 세션은 문서 링크·식별자·내용 일관성을 확인하고 실행하지 않은 제품 시험을 통과로 적지 않는다.
- 종료 시 현재 상태·진행표·plan·결과·다음 작업을 함께 갱신한다. 미충족은 완료로 표시하지 않고 `WAITING_USER`/`BLOCKED`/미완료 이유와 독립 진행 가능 범위를 기록한다.
- 실제 브랜치/HEAD/미커밋 소유·남은 프로세스·자원·실행 명령을 남긴다. 사용자 변경을 섞거나 임의 삭제하지 않는다. 외부 push·PR 허용은 실제 범위를 확인한다.

### 최신 개발 인계 — S-036 / 2026-09-24 / UI-05a 이슈 #7 관리 화면 정리 1단계 + QG-02~07 진행기 자동 검토(D-94) (**완료**)

- **사용자 요청:** "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan·검증·인계 절차를 지켜줘." — 안내서의 요청문이 **UI-05a**(이슈 #7
  1단계: 전수 대조 + 필수 셋 옮기기, 시작 때 QG-02~07 의 길을 묻는다)를 지정했다. 세션 중 사용자가 이슈 #8·#9 등록을 알리며 계획에 넣자고
  했고, 끝에 "라이브가 문제없이 끝나면 문서 정리 잘 하고 커밋푸시까지해줘." 라고 지시했다.
- **수행:** (1) 기준선 `scripts\run-tests.ps1`(785·0·2, 웹 37, P1 18 — 도는 동안 읽기·plan·결과 문서만) → (2) 보조 에이전트로 관리 화면 전수
  대조(읽기 전용) + 직접 코드 대조 → [UI-PLAN-05a](plans/UI-PLAN-05a.md) 기록 → (3) **판단과 무관한 부분 먼저**: `작업` 탭(작업 그래프·실행 상세)과
  서버 보강(수정 뒤 진행기, 종료 업무의 연결 거부) + 제어부 시험 3·브라우저 1 → (4) **QG-02~07 의 길을 물음** — 코드 대조로 관리 화면도 판정을
  기록하지 못한다는 사실을 함께 알렸고, 사용자가 게이트와 성공 기준의 차이를 물어 설명한 뒤 **(ⅱ) 진행기가 스스로 돌림**을 골랐다(D-94);
  취소 2단계 시점 → UI-05b → (5) D-94 구현(순수 규칙·저장소·진행기·Runner·지시문·카드) + 제어부 시험 5·브라우저 1 → (6) 최종 전체 시험
  **795·0·2**(웹 37·P1 18, 종료 코드 0) → (7) 이슈 #8·#9 를 읽고 물음 — **다음 세션 P4-10(UI-05b 앞)**, 제한 시간 기본 **1시간**(D-95) → (8) 실제
  codex 라이브(QG-04 명시 ON) **passed 15/15** → (9) 문서·인계 → (10) 사용자 지시로 커밋·push.
- **제품 판단:** 사용자 결정 둘을 D-94·D-95 로 적었다(1.11절). 새로 내린 상세 설계 선택(plan 0·3.4·9절): 작업 탭은 오른쪽 패널의 넷째 탭,
  게이트 대상 이름은 고치는 동안 고정(`design`·`plan:<Task>`·`impl:<Task>`)·버전은 입력 해시, 검토 실행 작업 id `qg:<게이트>:<Task>`, 검토 실패는
  `blocked`(검사 강도·세션을 따지지 않음)·재시도 한도 안에서 재검토, QG-04 repair 는 기다리는 구현 Task 다시 실행, 결합 기록 경로의 QG-02 는
  구현 전에, 게이트 설정 변경·그래프 수정은 기록 뒤 진행기 호출.
- **변경 파일:** 제품 — `controller/api.py`(그래프 세 끝점·게이트 설정의 진행기 호출, 결과 보고의 `quality_gate_findings`·`run_finished_gate`)·
  `repository.py`(`gate_subject`·`quality_gate_progress`·`open_gate_review`·`apply_gate_run_effects`·배정의 게이트 정보·지적, 결합 기록 경로의
  QG-02, `complete_quality_gate_run(blocked=)`, `set_question_blocks` 가드)·`work_progressor.py`(걸음 치환·`_link_gate`), `domain/work_flow.py`
  (`quality_gate_step`·`GATED_PURPOSES`·`Step.gate`·대기 문구), `runner/agent.py`·`prompts.py`(게이트 블록·지적 블록), `web/src/shell/WorkPanel.tsx`
  (새)·`ReviewPanel.tsx`·`Shell.tsx`·`ConversationView.tsx`·`ProgressCards.tsx`(`QualityGateCard`·작업 버튼)·`CaseSettings.tsx`·`events.ts`·
  `shell.css`·`web/src/api.ts`(`setPolicy` 의 작업 범위). 시험 — 새 `tests/test_work_graph_edit.py`·`test_quality_gate_progress.py`, 수정
  `test_web_shell.py`(+2)·`fake_cli/fake_codex.py`(`HADS_FAKE_PLAN_QUESTION`·`HADS_FAKE_GATE_FAIL`). 라이브 — `ui/live/p405_progress.py`
  (`HADS_LIVE_GATE`·`HADS_LIVE_OUT`). 증거 — `ui/evidence/UI-05a-results.md`·`UI-05a-live-gate*`. 문서 — `plans/UI-PLAN-05a.md`·`decisions.md`
  (D-94·D-95)·`quality-gates.md`·`ui-conversation-design.md`·`DEVELOPMENT.md`·`README.md`·`development-history-v0.8.md`(S-032 인계 옮김).
  스키마·의존성 변경 없음.
- **검증:** 최종 `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **37**, pytest **795 통과·0 실패·2 건너뜀**(14분 45초), P1 계약 **18**, "전체
  시험 통과". 새 시험 전부 전체 실행 안에서 통과. 서버 보강 시험은 변경을 되돌리면 실패함을 확인했다. 줄 끝 LF·CR 없음, 문서 링크 확인.
- **실제 CLI:** codex-cli 0.156.1, 제품 경유, 실제 Edge. `HADS_LIVE_GATE=QG-04` — 검증 실행 셋 앞마다 진행기가 QG-04 검토를 스스로 만들었고
  실제 codex 가 발견 JSON(셋 다 발견 없음)을 내 `pass`·독립 검사로 기록됐다, 자동 완료([UI-05a 결과](ui/evidence/UI-05a-results.md) 3절). 실패
  → repair 경로는 가짜 CLI 시험뿐이다. 무관한 관찰 둘(남은 검증 Task 가 있는데 기준 충족으로 자동 완료, 출력 없이도 `met` 보고)은 P4-10 에서 본다.
- **경계:** 새 권한·동의·인수 없음(1절 규칙). 게이트 통과는 권한이 아니고 끄는 것은 통과가 아니다. 진입 검사 변경은 결합 기록 경로의 QG-02 하나.
  강제 축·게시 규칙(P5)·스키마 v28 그대로. 관리 화면은 그대로 있다(UI-05b).
- **다음 행동:** **P4-10**(이슈 #8·#9 — 1.11절, 다음 세션) → **UI-05b**(이슈 #7 2단계 + 실행 중 취소 2단계) → P5-01.
- **사람에게 물어야 할 것 — 사용자의 답(2026-09-24):** (1) QG-02~07 의 길 → **(ⅱ) 진행기가 스스로 돌림**(D-94). (2) 실행 중 Case 의 취소 2단계
  시점 → **UI-05b 에 붙임**. (3) 이슈 #8·#9 → **다음 세션 P4-10(UI-05b 앞)**. (4) 실행 제한 시간 기본값 → **모두 1시간**(D-95). 남은 질문 없음 —
  UI-05b 의 `?view=admin` 처리(삭제 / 개발자 전용 숨은 화면)는 그 세션이 결과 2절의 개발자 전용 목록을 보고 정한다(필요하면 묻는다).
- **사용자 지시로 한 일:** 라이브 통과 뒤 이 세션의 변경(코드·시험·라이브 스크립트·증거·문서)을 **한 커밋**으로 만들고, 이 인계 기록(커밋 해시)을
  이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `47c388d` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다.
  PR·이슈 댓글·닫기는 하지 않았다.
- **작업공간:** 시작 `main`/`47c388d` clean. `web/dist` 는 빌드 산출물. 라이브 임시 폴더 `%LOCALAPPDATA%\Temp\hads-p4-05-live\<tag>`. 이 PC 의
  환경 변화 없음.
- **남은 자원:** 시험·라이브가 띄운 제어부·Runner·Edge 는 각자 내렸다(라이브 "남은 프로세스 0", 멈춘 중간 실행 뒤 pytest·headless Edge 없음
  확인). 사용자의 제어부·Runner(port 8765)는 건드리지 않았다. 로그는 세션 스크래치 폴더의 `baseline.out`·`mid.out`(중간, 멈춤)·`final.out`·
  `live_gate.out`.
- **다음 세션이 이어서 할 때:** P4-10 은 1.11절의 P4-10 항목과 이슈 #8·#9 본문(`gh issue view 8`·`9`)이 시작점이다. #8 의 코드 시작점:
  `runner/cli_adapter.py`(`timeout`·`stop_reason`)·`runner/agent.py`(결과 보고)·`domain/work_flow.py` `_graph_phase`(재시도)·진행 상한
  (`progress_limit_setting`·`ProgressLimits`)·`ProgressCards.tsx` `LimitWaitCard`. #9: `controller/work_graph.py`(Task 상태 도출)·
  `domain/work_flow.py` `decide_criteria`·`_completion_phase`·`quality_gate_step`(repair 모양)·UI-05a 의 그래프 수정 경로(`Repository.add_task`·
  `_replan`)·`remediation_cycle`. 그 뒤 UI-05b 는 [UI-05a 결과](ui/evidence/UI-05a-results.md) 2.3절과 1.11절이 시작점이다.

### 이전 개발 인계 — S-035 / 2026-09-24 / P4-09 P5 전 정리 묶음 (**완료 — DONE_WITH_LIMITATIONS, 라이브는 관찰**)

- **사용자 요청:** "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan 검증 인계 절차를 지켜줘." — 안내서의 요청문이 **P4-09 정리 묶음**(사용자 결정 2026-09-24: 이슈 #2~#6·후속 Case 시작 기준·실제 codex 라이브를 한 세션·한 plan 에)을 지정했다.
- **수행:** (1) 기준선 `scripts\run-tests.ps1`(753·0·2, 웹 37, P1 18) → (2) [P4-PLAN-09](plans/P4-PLAN-09.md) 기록 + `decisions.md` 에 **D-91·D-92·D-93** → (3) (a)→(b)→(c)+(e)(스키마 **v28** 한 번)→(f)→(g) 구현·시험(새 시험 29 + 브라우저 3; 부분별 성공 기준 AC-1~33 전부) → (4) 최종 전체 시험 **785·0·2**(웹 37·P1 18, 종료 코드 0) → (5) **(d) 실제 codex 라이브 둘**: RCA·maintenance 의도 작성 표본(`p4/live/profile_intents.py --prefix P4-09-live-intents --only D,E`)과 샌드박스 해제 뒤 진행 라이브(`ui/live/p405_progress.py`, `HADS_LIVE_PREFIX=P4-09-live-progress`) → (6) 문서(D-91~93·환경 계약·프로젝트 지식·완료 수명 주기·UI 설계·문맥 계약·DEVELOPMENT·README·[P4-09 결과](p4/evidence/P4-09-results.md)) → (7) 이슈 #2~#6 에 해소 댓글을 달고 닫았다(사용자 요청) → (8) 이 인계. 상세는 [P4-09 결과](p4/evidence/P4-09-results.md) 1절.
- **제품 판단:** 사용자 결정(이슈 본문·S-034 의 답)을 D-91·D-92·D-93 으로 적었고 새로 내린 것은 없다. 상세 설계 선택(plan 0절·9절): Claude 쓰기 실행의 `--strict-mcp-config` 유지, 범위를 읽지 못한 후보의 주입 제외, 이전 결과 선택 시 미커밋 미포함, 취소 1단계만(실행 중이면 `runs_unfinished`), 자동 제목은 해석 블록 `title` 로(첫 응답 + 업무 요청 응답 한 번), 옛 행의 제목 출처 NULL 은 기본 문구일 때만 기본값으로.
- **변경 파일:** 제품 — `controller/repository.py`(질문 답 참조·이전 결과·취소·후보 범위·열람·제목)·`api.py`(취소·제목·열람·`activities` 질의·시작 기준 모델)·`db.py`(v28)·`schema.sql`·`request_processor.py`(자동 제목)·`work_progressor.py`(취소 가드), `domain/models.py`(`StartBasis.PREVIOUS_RESULT`·`DecisionKind.CASE_CANCELLATION`)·`budget.py`(`SettleSource.CASE_CANCELLED`)·`conversation.py`(`title`)·`knowledge.py`(관대한 받기·`scope_unreadable`·읽을 말)·`work_flow.py`(카드 값), `runner/cli_adapter.py`(D-91)·`agent.py`(질문 머리·이전 결과)·`prompts.py`(질문 머리·활동·경로·제목 규칙)·`workspace.py`(`previous_result_tip`·`previous_result`), `web/src/api.ts`·`lib/knowledgeText.ts`(새)·`shell/ConversationView.tsx`(제목 편집·취소)·`Sidebar.tsx`(이름 바꾸기·취소 배지)·`Shell.tsx`·`ProgressCards.tsx`(이전 결과 카드·열람·수동 등록 채우기)·`ProjectRules.tsx`(범위 배지·채우기)·`ReviewPanel.tsx`. 시험 — 새 `tests/test_question_answers_to_tasks.py`·`test_cli_adapter_permissions.py`·`test_case_cancel.py`·`test_knowledge_scope.py`·`test_case_title.py`, 수정 `test_start_basis.py`·`test_web_shell.py`·`test_request_processor.py`·`fake_cli/fake_codex.py`(`HADS_FAKE_TITLE`·`HADS_FAKE_RULE_BADSCOPE`·마지막 메시지 추출 수정). 라이브 — `p4/live/profile_intents.py`·`ui/live/p405_progress.py`. 증거 — `p4/evidence/P4-09-results.md`·`P4-09-live-intents*`·`P4-09-live-progress*`. 문서 — 위 (6). 의존성 변경 없음.
- **검증:** 최종 `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **37**, pytest **785 통과·0 실패·2 건너뜀**(15분 25초), P1 계약 **18**, "전체 시험 통과". 새 시험 전부 전체 실행 안에서 통과. 문서 링크·참조 파일 확인, 줄 끝 LF(CR 없음).
- **실제 CLI:** codex-cli 0.156.1, 제품 경유. 의도 표본 D(RCA)·E(maintenance) — [P4-09 결과](p4/evidence/P4-09-results.md) 3절(D 는 현상·관찰·원인 질문·결론 요구·종료조건을 채우고 `objectives: [cause]`·`definitive_required`; E 는 유지 대상·현재/목표 상태·보존 조건과 기준 셋을 채웠고 최상위 `objectives` 는 비워 둠). 진행 라이브: **passed · 제품 규칙 18/18 · 관찰 16**(`P4-09-live-progress.log`, 태그 203311030, 실제 Edge) — **샌드박스 해제 뒤 검증 실행 둘이 실제 명령을 돌려 기준 다섯이 전부 `met`(`changed_and_verified`·`preserved`)으로 적혔고 사람 인수 없이 자동 완료됐다**(그 전 라이브는 검증이 시험을 돌리지 못해 `unverified` → 예외 수용이었다). 같은 라이브에서 (a) 계획 질문 `plan:p1` 의 답이 T1·T2 구현 실행에 `question_answer` 핵심 참조로 들어갔고, (g) 실제 codex 가 두 대화의 자동 제목("로그 출력에 INFO·ERROR 수준 추가", 후속 "log()에 호출 함수 출처 기록", 출처 `ai:work_start`)을 냈다. (c) 의 이전 결과 카드와 (e) 의 취소는 이 라이브 경로에 없어 관찰하지 못했다(가짜 codex·Edge 시험으로 확인).
- **경계:** 강제 축 넷·진입 검사·게시 규칙(P5) 무변경. 어느 부분도 권한·동의·인수를 만들지 않았다(1절 규칙 목록). D-91 의 손실은 사용자 수용.
- **다음 행동:** **UI-05a**(이슈 #7 관리 화면 정리 1단계 — 1.11절; 시작 때 QG-02~07 의 길을 사용자에게 묻는다) → UI-05b → P5-01.
- **사람에게 물어야 할 것 — 사용자의 답(2026-09-24):** (1) 이 세션 변경의 커밋·push → **지시함**("지금대로 두자. 커밋푸시해줘." — `5db52a9` + 인계 기록 커밋). (2) UI-05a 시작 때 QG-02~07 의 길(옮김 / 진행기 자동 실행). (3) 실행 중 Case 의 취소 2단계를 언제 할지(UI-05a·b 에 붙일지, P5 뒤인지). (4) maintenance 의도의 최상위 `objectives` 빈 값을 지시문으로 요구할지 → **사용자 결정(2026-09-24): 지금대로 둔다**(최상위 목록은 더하는 목적의 자리, 비면 Profile 기본 목적으로 읽는다 — 지시문·코드 변경 없음).
- **사용자 지시로 한 일:** 작업 보고 뒤 사용자가 maintenance `objectives` 를 지금대로 두기로 하고 커밋·push 를 지시했다. 이 세션의 변경(코드·시험·문서·증거)을 **한 커밋 `5db52a9`** 로 만든 뒤 이 인계 기록(DEVELOPMENT·README·결과 문서의 결정 한 줄)을 이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `30c30c0` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다. PR 은 만들지 않았다.
- **작업공간:** 시작 `main`/`30c30c0` clean. 이 세션의 변경은 **`5db52a9`** 와 인계 기록 커밋으로 `origin/main` 에 push 됐다. `web/dist` 는 빌드 산출물. 라이브 임시 폴더 `%LOCALAPPDATA%\Temp\hads-p4-03-live\203057259`(의도 표본)·`hads-p4-05-live\<tag>`(진행). 이 PC 의 환경 변화 없음(P4-ENV 의 ACL 조치는 그대로).
- **남은 자원:** 시험·라이브가 띄운 제어부·Runner·Edge 는 각 스크립트가 내렸다. 사용자의 제어부·Runner(port 8765)는 건드리지 않았다. 새 의존성 없음. 로그는 세션 스크래치 폴더의 `baseline.out`·`final.out`·`live_intents.out`·`live_p405.out`.
- **다음 세션이 이어서 할 때:** UI-05a 는 1.11절의 설명·이슈 #7 본문(`gh issue view 7`)·`web/src/App.tsx`·`AdminEntry.tsx`·`*Panel.tsx`·`controller/api.py` 의 `gateApi`·`graphApi`·`runApi` 끝점이 시작점이며, P4-09 (c) 가 만든 시작 기준 카드(`StartBasisCard`)에 "다른 커밋에서 시작" 입력을 붙이는 것은 UI-05b 다. 시작 때 QG-02~07 의 길을 사용자에게 묻는다.

### 이전 개발 인계 — S-034 / 2026-09-24 / P4-08 여섯 Profile 진행기 경유 재현 + 검증 환경 실험 + P4 종료 (**완료**)

- **사용자 요청:** S-033 보고 뒤 "이 문서 변경은 커밋 푸시하고 P4-PLAN-08 먼저 하고 닫아줘. codex 사용 가능하니 환경실험 해도되고 4번은 너가 제안해주고 5번은 굳이 안해도되고 6번은 무슨말인지 이해하기 쉽게 설명해줘". 같은 대화에서 S-033 의 문서 커밋·push 뒤 이어서 했다.
- **수행:** (1) S-033 문서 변경 커밋 `7c15df5` + 인계 기록 `4bbda6c` → `origin/main` push(push 전 fetch 로 원격 `44b6909` 확인). (2) **P4-PLAN-08**: 기준선은 S-033 의 실행(740·0·2, 코드 변경 없음)을 그대로 썼다. `tests/test_profile_flows.py`(13건)를 써서 defect-fix(복원·무변경 완료·미재현)·refactoring(개선+보존·보존 기준 없음·보존 미충족)·maintenance(조건부 보존 셋)·혼합 목적(개정 경로 둘·처음부터 선언 둘)을 진행기 경유로 고정했다. 첫 회차 10/13 → 시험 결함 둘(검증 실행도 쓰기 권한이라 가짜 CLI 가 파일을 쓴다 → 검증 전 `write_files = {}`; 동의가 다음 실행을 동기적으로 만든다 → 동의 전에 실행 수를 센다) 고침 → 13/13. **제품 코드 무변경·제품 결함 없음** — [P4-08 결과](p4/evidence/P4-08-results.md). (3) **검증 환경 실험**(사용자 허용): 제품을 거치지 않고 `codex sandbox`(AI 없음)로 원인을, `codex exec`(실제 codex, 네 조건 + A 재실행 2회)로 조건을 찾았다 — [P4-ENV 결과](p4/evidence/P4-ENV-results.md). (4) P4 를 `DONE_WITH_LIMITATIONS` 로 닫았다(1·4·6절). (5) 이 인계.
- **제품 판단:** 새로 내리지 않았다. P4-08 은 시험 추가뿐이고, 환경 실험의 조치는 **이 PC 의 환경**(Python 3.12 폴더에 codex 샌드박스 그룹 `CodexSandboxUsers` 의 읽기·실행 ACE 하나 — 되돌리기는 P4-ENV 결과 2절)이지 제품·저장소 변경이 아니다. 제품이 그 조치를 안내·검사할지, 검증 지시문에 인터프리터 경로를 실을지는 아래 "사람에게 물어야 할 것".
- **변경 파일:** 새 `tests/test_profile_flows.py`, `p4/live/verify_env.py`, `p4/evidence/P4-08-results.md`, `p4/evidence/P4-ENV-results.md`, `p4/evidence/P4-ENV-live.log`·`P4-ENV-live-results.json`(최종 회차 `183717`)·`P4-ENV-live-183132.*`(첫 회차, 네 조건)·`P4-ENV-live-183504.*`(둘째 회차); 수정 `plans/P4-PLAN-08.md`(완료)·`p4/evidence/P4-ACCEPT-results.md`(빠진 것 해소·사용자의 답)·`case-profiles.md`(6절)·`DEVELOPMENT.md`·`README.md`·`development-history-v0.8.md`(S-031 인계 옮김). 제품 코드·스키마·정책·지시문·의존성 변경 없음.
- **검증:** 최종 `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **37**, pytest **752 통과·1 실패·2 건너뜀**(15분 28초, 실행 수 +13). 실패 1은 9절의 기존 시계 해상도 흔들림 시험(`tests/test_quality_changes.py::test_the_reservation_lands_when_the_verification_ends_even_on_a_failure`) — 단독 3회 전부 통과, 이번 변경과 무관. 스크립트가 pytest 실패로 멈춰 P1 계약은 따로 실행: **18** 통과. 새 시험 13건은 전체 실행 안에서 통과. 문서 링크·시험 이름 확인 스크립트 통과, 줄 끝 LF.
- **실제 CLI:** 검증 환경 실험에서 실제 `codex-cli 0.156.1` 을 **제품 없이 직접** 불렀다(네 조건 + 재실행 둘; 사용자 설정 `[windows] sandbox = "elevated"`·`approval_policy = "never"`). 제품 경유 라이브(검증 실행이 `met` 을 실제 명령으로 적는 것)는 하지 않았다 — 다음 라이브에서 본다. P4-08 AC-8(RCA·maintenance 의도 작성 표본)은 선택 사항 미수행.
- **경계:** 강제 축 넷·`enforcement`·진입 검사·정책·스키마 v27 그대로. 검증 실행의 트리 변경은 제품 변경으로 세지 않는다(P4-05 규칙 그대로, P4-08 결과 4절). 환경 조치는 이 PC 뿐이며 제품·저장소에는 기록만 남겼다.
- **다음 행동(사용자 결정, 보고 뒤 — "2번 너말대로 하자. 3번 샌드박스 끄자. 4번은 따로 할 게 없다. 5번 표본 관찰 수행. #2 이슈의 버그도 해결. P5 전에 싹 정리. 다 한번에"):** **P4-09 — P5 전 정리 묶음**을 한 세션·한 plan 으로 — (a) 이슈 #2 이월 질문 답 전달, (b) 이슈 #3 쓰기 실행 샌드박스 해제(D-91), (c) 후속 Case 시작 기준(제안 채택), (e) 이슈 #4 업무 취소, (f) 이슈 #5 지식 후보의 거부·열람(D-92), (g) 이슈 #6 대화 제목(D-93), (d) 실제 codex 라이브(RCA·maintenance 표본 + 실제 검증 `met`). **push 뒤 사용자 요청("4,5,6번 이슈도 추가됐어 이것도 하나로 묶어서")으로 (e)(f)(g)를 같은 묶음에 더했다.** 상세는 1.11절. **그 뒤 UI-05a·UI-05b — 이슈 #7 관리 화면 정리**(사용자 결정 2026-09-24 "그래 그렇게 하자": P4-09 뒤·P5-01 전, 두 세션 — 1.11절), 그 뒤 P5-01.
- **제안(사용자가 "너가 제안해주고" 라 한 것 — 후속 Case 의 이전 결과 기준, D-77 마지막 문장):** 지금은 종료 Case 의 수정 요청이 새 대화로 옮겨져도 그 작업공간이 HEAD 에서 시작하므로, 이전 Case 의 아직 push·병합되지 않은 변경이 후속 작업에 없다. **제안: P5-01 전에 작은 별도 plan(가칭 UI-05a)** — 후속 Case 의 시작 기준 카드에 "이전 업무 결과(그 Case 브랜치의 끝 커밋)에서 시작 / 커밋된 코드(HEAD)에서 시작" 을 사람이 고르고(기본 선택·자동 없음), 고르면 `base_ref` 를 그 커밋으로 두고 `start_basis` 에 새 값(`previous_result`)과 이전 Case·커밋을 기록한다. 이전 Case 의 브랜치가 없거나 HEAD 가 이미 그 커밋을 포함하면 제안하지 않는다. P5 의 push·PR 이 후속 Case 의 브랜치를 다루기 전에 시작 기준이 맞아야 하므로 P5-01 앞을 권한다. 사용자가 P5 뒤로 미루면 그대로 둔다.
- **설명(사용자가 뜻을 물은 것 — 실제 codex 의 RCA·maintenance 의도 작성 표본):** 지금까지 실제 codex 에게 "의도 초안을 써 달라" 고 시켜 본 업무 유형은 feature·defect-fix·research·refactoring 넷이다. 자동 시험은 가짜 CLI 가 정해진 답을 내므로 제품 규칙은 고정돼 있지만, **실제 AI 가 RCA(설명할 현상·관찰 근거·원인 질문·결론 요구·종료조건)와 maintenance(유지 대상·현재/목표 상태·작업 이유·보존할 호환성) 항목을 지시대로 채우는가**는 한 번도 본 적이 없다. 하는 일은 `p4/live/profile_intents.py` 에 요청 둘을 더해 실제 codex 로 의도 작성만 두 번 돌리고(각 30초 안팎, 코드 변경 없음) 무엇을 썼는지 관찰로 적는 것이다 — 제품 규칙을 바꾸지 않고 통과/실패도 아니며 지시문의 구멍을 찾는 목적이다. 하지 않아도 P4 종료에는 영향이 없다(9절의 한계로 남는다).
- **사람에게 물어야 할 것 — 사용자의 답(2026-09-24):** (2) 후속 Case 기준 제안 → **채택**(P4-09 (c)). (3) 검증 환경의 제품 반영 → **쓰기 실행의 샌드박스 해제**(이슈 #3 의 결정 — 다른 세션에서 이미 내려진 것과 같다; P4-09 (b), D-91). (4) ACL 조치 → 따로 할 것 없음(유지). (5) 표본 관찰 → **한다**(P4-09 (d)). 더해 **이슈 #2 의 결함도 고친다**(P4-09 (a)). 넷을 P5 전에 **한 세션에** 한다. (1) 이 세션(S-034) 변경의 커밋·push → **지시함**("좋아. 이번 세션 변경 커밋푸시해줘" — `51a27e5` + 인계 기록 커밋).
- **사용자 지시로 한 일:** 작업 보고와 P4-09 묶음 기록 뒤 사용자가 커밋·push 를 지시했다. 이 세션의 변경(시험·실험 스크립트·증거·문서)을 **한 커밋 `51a27e5`** 로 만든 뒤 이 인계 기록(DEVELOPMENT·README)을 이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `4bbda6c` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다. PR 은 만들지 않았다. 이슈 #2~#6 에는 P4-09, 이슈 #7 에는 UI-05a·05b 배정 댓글을 남겼다.
- **작업공간:** 시작 `main`/`4bbda6c`(S-033 push 뒤). 이 세션의 변경은 **`51a27e5`** 와 인계 기록 커밋으로 `origin/main` 에 push 됐다. `web/dist` 는 빌드 산출물. 이 PC 의 환경 변화: `%LOCALAPPDATA%\Programs\Python\Python312` 에 `CodexSandboxUsers:(OI)(CI)(RX)` ACE 추가(2026-09-24). 실험 임시 폴더 `%LOCALAPPDATA%\Temp\hads-p4-env-live\{probe,183132,183504,183717}`.
- **남은 자원:** 시험이 띄운 제어부·Runner·Edge 는 pytest 가 내렸다. 사용자의 제어부·Runner(port 8765, `runner-local-1`)는 세션 내내 떠 있었고 건드리지 않았다(S-033 인계 그대로). 새 의존성 없음. 저장소 `var\` 는 건드리지 않았다. 로그는 세션 스크래치 폴더의 `full.out`·`flows*.out`·`env.out`.
- **다음 세션이 이어서 할 때:** P4-09 는 1.11절의 묶음 설명과 코드 대조 시작점에서 시작한다 — 이슈 #2~#6 본문(`gh issue view 2` … `6`)을 먼저 읽고, `plans/P4-PLAN-09.md` 를 (a)→(b)→(c)+(e)→(f)→(g)→(d) 순서로 부분별 성공 기준과 함께 기록한 뒤 기준선을 잡고 착수한다(기준선이 도는 동안은 읽기·plan 뿐). (b)(f)(g) 는 `decisions.md` 에 D-91·D-92·D-93 을 먼저 적는다. (c)(e) 의 스키마 변경은 v28 한 번으로 한다. (d) 의 라이브는 마지막에 돌린다. 한 부분이 막히면 나머지를 끝내고 막힌 것을 인계에 적는다. 끝나면 이슈 #2~#6 을 닫는다. 그 뒤 UI-05a 는 1.11절의 설명과 이슈 #7 본문(`gh issue view 7`)이 시작점이며 시작 때 QG-02~07 의 길(옮김 / 진행기 자동 실행)을 사용자에게 묻는다. 그 뒤 P5-01 은 7절 표·1.1절 마지막 문단·`controller/repository.py` 의 저장소 등록(`project_repository`)이 시작점이다.

### 이전 개발 인계 — S-033 / 2026-09-24 / UI-04 완료 확인 + P4 수용 확인 (**완료 — 판정 기록, 문서만**)

- **사용자 요청:** "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan·검증·인계 절차를 지켜줘." 다음 단계는 1.11절의 **UI-04 완료 확인 + P4 수용 확인**(사용자 결정 2026-09-24, S-032: 한 세션에서 한 번에)이었다.
- **수행:** 시작 상태(`main`/`44b6909`, clean, `git fetch` 뒤 `origin/main` 과 같음)를 확인하고 기준선(`scripts\run-tests.ps1`)을 띄운 뒤 [P4-PLAN-ACCEPT](plans/P4-PLAN-ACCEPT.md)를 **대조 전에 기록**하고 대조했다. 결과 둘 — [UI-04 결과](ui/evidence/UI-04-results.md)(D-90 표 4행 네 항목 전수 → 전부 구현·검증됨; UI 설계 13절 19 항목 → 16 구현·검증됨·3 부분(7·9 = P5 접점, 8 = 후속 Case 기준 제안 남음); 라이브 현황 — 04a 만 실제 codex, 04b~04d 는 선택 사항 미수행; 남은 것 목록), [P4 수용 결과](p4/evidence/P4-ACCEPT-results.md)(완료 조건 네 축 — 혼합 목적 계약·실패/복구/변경·Case 간 지식은 완비, "여섯 Profile 대표" 는 계약·판정 수준 여섯 전부·진행기 경유 셋(feature·research·RCA)만; case-profiles 6절 시나리오 10·점검표 AC-01~20·30~36·39 대조). **빠진 것 하나**(defect-fix·refactoring·maintenance 의 진행기 경유 대표 흐름 + 혼합 목적의 진행기 경유 완료 — `domain/work_flow.py` `decide_criteria` 의 `preserved`·`already_satisfied` 분기를 지나는 시험이 없다)를 [P4-PLAN-08](plans/P4-PLAN-08.md)로 **기록만** 했다(READY, 착수 안 함 — 4절 규칙).
- **제품 판단:** 새로 내리지 않았다. 판정 규칙(구현·검증됨 / 남은 것 / 빠진 것)과 "제품 흐름 수준(진행기 경유)을 기준으로 빠진 것을 가른다" 는 이 저장소의 검증 규칙(3·10절)의 적용이며(plan 0·9절), P4 를 닫을지는 사용자의 결정으로 남겼다.
- **변경 파일:** 문서뿐 — 새 파일 `plans/P4-PLAN-ACCEPT.md`·`plans/P4-PLAN-08.md`·`ui/evidence/UI-04-results.md`·`p4/evidence/P4-ACCEPT-results.md`; 수정 `DEVELOPMENT.md`·`README.md`·`development-history-v0.8.md`(10절에 남아 있던 S-030 인계 사본 제거 — S-032 가 이미 보존본 6절에 옮겨 두었다; 머리에 그 사실을 적음)·`review-acceptance-matrix.md`(머리 문단의 현재 기준·대조 위치)·`ui-conversation-design.md`(13절 머리에 대조 위치)·`case-profiles.md`(6절 끝에 대조 위치). 코드·시험·스키마·실행 스크립트·의존성 변경 없음(`git status --short` 가 `.md` 만 보인다).
- **검증:** 기준선 = 최종(코드 변경 없음): `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **37**, pytest **740 통과·0 실패·2 건너뜀**(15분 7초), P1 계약 **18**, "전체 시험 통과"(종료 코드 0; S-032 최종과 같은 수치, 건너뜀 2건은 이행 시험의 옛 스키마 커밋 창 — 9절). 결과 문서·plan 에 적은 `test_*` 이름 전부를 `tests/` 에서 찾았고, 새·수정 문서의 상대 링크가 전부 실제 파일을 가리킨다(스크립트 확인). 줄 끝 LF(`git ls-files --eol`, 외따로 CR 없음).
- **실제 CLI:** 하지 않았다(새 기능 없음).
- **경계:** 강제 축 넷·`enforcement`·진입 검사·정책·스키마 v27·시험 그대로. 이전 결과의 수치·라이브는 당시 사실로 표기했고 새 통과로 재명명하지 않았다. 완료 확인은 판정 기록이며 권한·동의·인수가 아니다.
- **다음 행동:** 사용자 결정에 따라 — (가) **P4-PLAN-08 착수**(시험 추가, 코드는 결함이 드러날 때만) 뒤 P4 종료 판정, 또는 (나) P4 를 제한 있는 완료로 닫고 **P5-01**. 이 문서는 (가)를 제안한다. 어느 쪽이든 완료된 plan 을 다시 열지 않는다.
- **사람에게 물어야 할 것 — 사용자의 답(2026-09-24, "이 문서 변경은 커밋 푸시하고 P4-PLAN-08 먼저 하고 닫아줘. codex 사용 가능하니 환경실험 해도되고 4번은 너가 제안해주고 5번은 굳이 안해도되고 6번은 무슨말인지 이해하기 쉽게 설명해줘"):** (1) 문서 변경의 커밋·push → **지시함**(`7c15df5` + 인계 기록 커밋). (2) P4 종료 판정 → **(가) P4-PLAN-08 을 먼저 하고 닫는다**(같은 대화에서 이어서, 인계 S-034). (3) 검증 환경 실험 → **허용**(codex 사용 가능). (4) 후속 Case 의 이전 결과 기준 제안 → **AI 가 제안한다**(S-034 인계). (5) 04b~04d 실제 codex 라이브 → **하지 않는다**. (6) 실제 codex 의 RCA·maintenance 의도 작성 표본 → 설명 요청(S-034 보고에 적음).
- **사용자 지시로 한 일:** 작업 보고 뒤 사용자가 커밋·push 를 지시했다. 이 세션의 문서 변경을 **한 커밋 `7c15df5`** 로 만든 뒤 이 인계 기록(DEVELOPMENT·README)을 이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `44b6909` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다. PR 은 만들지 않았다.
- **작업공간:** 시작 `main`/`44b6909` clean. 이 세션의 문서 변경은 **`7c15df5`** 와 인계 기록 커밋으로 `origin/main` 에 push 됐다. `web/dist` 는 기준선의 빌드 산출물(`.gitignore`). 이어서 같은 대화에서 P4-PLAN-08 을 착수한다(S-034).
- **남은 자원:** 기준선이 띄운 제어부·Runner·Edge 는 pytest 가 내렸다. **사용자의 제어부·Runner 가 세션 중 떠 있었다** — `python -m uvicorn controller.app:app --host 127.0.0.1 --port 8765`(17:34:51 시작)와 `python -m runner.agent`(17:35:03 시작, `runner-local-1`, 2026-09-20 등록, heartbeat 진행 중), 부모는 이 세션과 무관한 pwsh 둘. 이 세션이 만든 것이 아니며 **건드리지 않았다**(기준선은 자기 임시 제어부·포트로 돌아 무관하게 통과했다). 새 의존성 없음. 라이브 데이터 없음. 저장소 `var\` 는 건드리지 않았다. 기준선 로그는 세션 스크래치 폴더의 `baseline.out`.
- **다음 세션이 이어서 할 때:** P4-PLAN-08 을 한다면 시작점은 그 plan 1절(코드 대조)·3절(방법)과 1.11절의 시작점 — `tests/conftest.py` 의 `processing_harness`·`FakeCliExecutor`·`met_satisfaction_for`, `tests/test_work_progressor.py` 의 feature 대표 시험을 본떠 Profile 별로 만든다. 기준선을 다시 잡은 뒤 코드를 만진다(기준선이 도는 동안은 읽기·plan 뿐). P5-01 로 간다면 7절 표와 1.1절 마지막 문단(기록 저장소 겸용은 P5 연결 전에 저장소 정책·선택·작업공간을 함께 검증)이 시작점이다.

### 그 이전 인계

UI 개편 1·2차 설계 인계와 S-011~S-032 는 [보존본](development-history-v0.8.md) 6절, S-001~S-010 은 [v0.6 보존본](development-history-v0.6.md)에 있다.
