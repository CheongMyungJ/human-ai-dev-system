# 개발 진행 안내서 — 새 세션의 시작점

기준: **설계 v0.8 2차 반영 / 사용자 결정 D-01~90(+2026-09-24 보충: D-29 진행 상한, D-67 지식 원문 서버 저장, D-80 자동 등록) / 2026-09-24**. 구현은 **P4-07b 완료·스키마 v24**(UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01·P4-03 완료 위)이다. **참고 후보의 자동 활성 조건 표시 — 반자동**(P4-07b, 사용자 결정 2026-09-24 — 시스템이 조건 여덟의 충족을 계산해 배지로 보이고 사람이 프로젝트 규칙 화면에서 한 번에 활성화, 클릭 없는 활성화 없음·필수 승격 없음·스키마 변경 없음)이 더해졌다. **결정 사항 패널·프로젝트 규칙 화면**(UI-04a — 이 대화의 목표·기준·결정·정한 규칙·후보·적용된 규칙을 보고 이동, 공통 규칙의 확인·변경과 원래 대화·메시지로 이동, D-80)이 있다. **선택적 지식 추출**(P4-07 — 작업 실행이 결과와 함께 남기는 후보, 논의 응답의 제안 표지, 근거 추가·중복 연결·대체 제안·반증 관계, 관측 문맥, QG-08 채택 확인과 사람의 활성화), **지식 원문(적용 내용과 자동 등록 규칙의 권위 사용자 메시지 한 건)의 서버 저장과 배정 싣기·해시 대조·옛 지식 이행**(P4-06b, 원문 PC 경계의 유일한 예외), **프로젝트 지식 등록부와 활동+범위 자동 주입·실행별 Manifest**, 대화에서 말한 프로젝트 규칙의 **자동 활성 등록**, **진행 상한 설정**(환경 변수 기본값 + Case 별 이력)이 더해졌다. **기본 대화 화면(`/`)**, 제어부의 **요청 처리기**와 **AI 해석에 의한 최초 업무화**, **업무 단계 진행기**(업무화 뒤 의도 초안~완료를 제품이 잇고 사람 대기·완료·막힘에서만 요청을 끝냄), 확인 카드(의도 동의·controlled·delta·검토·피드백·예외), 완료 후 **설명 전용 진입**과 **후속 Case 이동**, 현재 브라우저의 **초안 복구**, 결과물의 **검토 버전 유지**가 있다. 현재 구현과 새 목표를 구분한다. 코드·테스트·스키마·실행 스크립트·의존성 설정을 변경하기 전에 반드시 plan을 기록·공유한다. 이 규칙은 이 저장소의 개발 절차이며, 제품 Fast Lane에 매번 별도 plan 문서나 사람 승인을 요구한다는 뜻이 아니다.

**P4-07b(참고 후보의 자동 활성 조건 표시 — 반자동)를 구현·검증했다(S-029, 아래 10절).** UI-04a(S-028)·P4-07(S-027)·P4-06b(S-026)·P4-06·P4-05b(S-025)·P4-05(S-024)·UI-03(S-023)·UI-02(S-022)·P4-04(S-021)·UI-01(S-020)도 완료다. 주요 UI 동작·제품 정책·개발 순서는 D-68~90으로 확정돼 있다. [대화 중심 UI 설계](ui-conversation-design.md)의 확정 내용과 [P4-07b 결과](p4/evidence/P4-07b-results.md)·[UI-04a 결과](ui/evidence/UI-04a-results.md)·[P4-07 결과](p4/evidence/P4-07-results.md), 아래 1.11절의 다음 작업(UI-04b)을 먼저 읽는다. 참고 후보의 자동 활성화는 **사용자 결정(2026-09-24, S-028): 반자동**이며 P4-07b 로 구현됐다 — 시스템이 조건 충족을 계산해 표시하고 사람이 한 번에 활성화한다(완전 자동은 후보가 실제로 쌓이면 다시 묻는다). 정확한 화면 치수·단축키·기술 상세는 실제 구현에서 검토한다.

**커밋·push는 세션마다 사용자 지시로만 한다.** 최근: UI 설계 문서 `a8ee278`, UI-01 `a509063`(S-020), P4-04 `5580896`과 문서 정리 `5840b83`(S-021). UI-02 `592eb39`과 인계 기록(S-022) — 작업 뒤 사용자 지시로 커밋·push했다. UI-03 `d51fc02`과 인계 기록(S-023) — 작업 뒤 사용자 지시로 커밋·push했다. P4-05 `b475146`과 인계 기록(S-024) — 작업 뒤 사용자 지시로 커밋·push했다. P4-05b·P4-06 `d74726a`과 인계 기록(S-025) — 작업 뒤 사용자 지시로 커밋·push했다. P4-06b `d1dcc4e`과 인계 기록 `c0c49fc`(S-026) — 작업 뒤 사용자 지시로 커밋·push했다. P4-07 `8defdfd`과 인계 기록 `edcdfff`(S-027) — 작업 뒤 사용자 지시("좋아 다된거야? 그럼커밋푸시해줘")로 커밋·push했다. UI-04a 와 인계 기록(S-028) — 작업 보고·논의 뒤 사용자 지시("그래 4번으로 가자. 다른세션에서 이어서 작업할 수 있게 정리하고 커밋 푸시해줘.")로 UI-04a 를 `4c347a0` 으로 커밋하고 인계 기록을 이어서 커밋해 함께 push했다. **P4-07b `f57c036` 과 인계 기록(S-029) — 작업 보고 뒤 사용자 지시("커밋푸시해줘 다른세션에서 이어서 작업할게")로 커밋·push했다(10절).** 각 허용은 그 변경과 인계 기록에만 적용되며 다음 변경의 외부 쓰기 허용이 아니다. 완료된 작업의 설명·시작점·이전 인계는 [보존본](development-history-v0.8.md)으로 옮겼다(S-021, S-022~S-028 에 이어서).

다른 개발 세션에 전달할 요청:

> C:\git\human-ai-dev-system-design\DEVELOPMENT.md의 최신 인계(S-029)와 P4-07b·UI-04a 결과를 읽고 **UI-04b — 프로젝트 설정·상세 설정 화면 + 알림**(1.11절, 사용자 동의 2026-09-24)을 새 plan(UI-PLAN-04b)으로 진행해줘(한 세션은 한 작업). P4-07b·UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01 완료 상태를 보존하고, 실제 코드·환경을 확인한 뒤 plan 을 기록·공유하고 구현·검증·인계를 해줘. P5 까지 자동 확대하지 마. 새 제품 판단이 필요하면 질문해줘.

**기본 수행 단위는 현재 하위 작업 하나**다. **P4-01~07(05b·06b·07b 포함)·UI-01~03·UI-04a 는 끝났고 P3의 남은 라이브 하나(경로 C)는 사용자 지시로 스킵됐다.** 다음은 **UI-04b** 다. P4 의 전체 완료 조건 수용은 UI-04 뒤 별도 확인이다. 사용자 지시가 우선하고, 필요한 기술 선택은 진행하되 새 제품 판단이 필요한 경우만 질문한다. 커밋·push 허용의 범위는 위와 같다.

## 1. 현재 상태

| 항목 | 현재 값 |
|---|---|
| 저장소·작업 위치 | `C:\git\human-ai-dev-system-design`, https://github.com/CheongMyungJ/human-ai-dev-system (비공개) |
| 설계 기준 | **v0.8 2차, D-01~90**. 대화 UI·준비 Case·요청 제어와 초안·검색·검토 버전·목적 변경·설명 소비·시간 지표·외부 편집·개발 순서 확정. **2026-09-24 보충:** D-29(진행 상한 = 설정), D-80(대화의 프로젝트 규칙 자동 활성 등록) |
| 구현 기준 | **P4-07b 완료**(UI-04a·P4-07·P4-06b·P4-06·P4-05b·P4-05·UI-03·UI-02·P4-04·UI-01 위). **참고 후보의 자동 활성 조건 표시 — 반자동(사용자 결정 2026-09-24): `domain.knowledge.auto_reference_check`(조건 여덟 — 후보·효력 참고·종류 운영 사실/알려진 문제·서로 다른 실행 둘 이상의 근거·그중 종료 코드 0 명령의 검증/분석 실행·관측한 저장소 범위·반증/충돌 없음·채택 확인 막음 없음), 조회의 `auto_reference`(`knowledge_view` 후보 항목·`knowledge_registrations_view` 후보 행), `GET /api/knowledge/{id}/auto-reference`, `POST /api/projects/{id}/knowledge/activate-ready`(화면이 본 id 만, 각 후보는 `activate_knowledge` 를 효력 참고·범위 그대로 지남, 미충족·거부는 건너뛰어 결과에), `adoption.auto_reference`(충족 항목·근거 실행), 규칙 화면의 배지·조건 목록·"조건 충족 n건 한 번에 활성화(사람의 결정)" 버튼, 후보 카드·관리 화면 패널의 배지 — 클릭 없는 활성화 없음·필수 승격 없음·스키마 변경 없음(P4-07b)**, **결정 사항 패널·프로젝트 규칙 화면 — 이 대화의 업무·목표·기준(의도 버전·동의·기준 판정)·결정(종류·대상 버전·근거 메시지)·이 대화에서 정한 규칙·후보(출처별, 메시지로 이동)·이 업무에 적용된 규칙(Manifest 버전별 집계 `GET /api/cases/{id}/knowledge-use`), 프로젝트 규칙 화면(`?screen=rules`)의 등록부·채택 확인·활성화(축소·`into`)·개정·무효·충돌·수동 등록(출처 대화)·원래 대화로(`&seq=N` 메시지 강조), 조회의 `source_case_title`·`source_message_seq`·`reply_seq`(UI-04a — 스키마 변경 없음)**, **선택적 지식 추출 — 작업 실행(검증·분석·실험·구현)의 후보 블록(최대 3건·근거 한 줄) → `ai_proposal` 후보, 논의 응답의 `proposal: true`, 같은 내용·`supports` → 근거 행(`knowledge_evidence`), `supersedes`·`contradicts` → 관계를 가진 새 후보(대상 사슬 그대로), 관측 문맥(`observed`), QG-08 채택 확인(`adoption_check`)·활성화(축소·`into`)·`adoption` 기록, 후보·제안·근거 카드, 관리 화면 채택 확인·활성화(P4-07 — 참고 후보 자동 활성화·독립 검토 없음)**, **지식 원문(적용 내용·권위 사용자 메시지 한 건)의 서버 저장 `knowledge_body`·배정 싣기·Runner 해시 대조·수동 등록의 중계 제거·옛 지식의 소유 PC 연결 시 이행·서버 열람·화면 안내(P4-06b, 사용자 결정 — 원문 PC 경계의 유일한 예외)**, **지식 등록부(항목·버전·효력·상태·범위·활동·권위)·수동 등록과 대화의 자동 활성 등록·활동+범위 자동 주입(필수 = 핵심 입력, 권위 원문 동반)·실행별 Manifest·적용 필수의 충돌 보류(P4-06)**, **진행 상한 설정(환경 변수 기본값 + Case 별 이력, 상한 카드의 "한도를 올리고 계속", P4-05b)**. P1 제한 수용, P2 완료, P3 완료(제한 있음), QG-01~07·repair(P4-01), 설정 변경 예약·부분 재검증·늦은 결과(P4-02), Profile 정의 v2 완료 계약(P4-03), 준비 단계 Case·대화 메시지·현재 요청과 서버 잠금·멱등 접수·카드 답변 한정·논의 응답 실행·최초 업무화(UI-01), 고정 문맥의 등급·인라인 한도·영수증·핵심 누락 보류·시작하지 않은 실행 소비 0·결과 시점 최신성·재시작 원시 출력 사용량 복구(P4-04), **Runner 제어/실행 분리·CLI 트리 job 제어(중단·잔류 종료·활성 0 확인)·잔류 확인 근거·요청 중단과 `interrupted`·`unknown` 해제·재시작 대조(재실행·새 세대 없음)·PC 미연결 전송 거부(UI-02)**, **기본 대화 화면·요청 처리기(논의 응답 자동 처리·기동 복구)·AI 해석 최초 업무화(해석 기록, 본문 없음)·브라우저 초안 복구·결과물 검토 버전 유지·기본값 요약·기존 접수 네 경로의 기록 전 중계(UI-03)**, **업무 단계 진행기(의도 초안~완료를 잇고 사람 대기·완료·막힘에서만 요청 종료, 진행 요청, 기준 판정의 제품 기록, 재작성·재시도 상한)·확인 카드·완료 후 설명 전용 진입·종료 snapshot·종료 뒤 예산 변경 이력·후속 Case 이동(P4-05)**. 스키마 **v24** |
| 현재 단계 | **P4 — 품질·다양한 업무·지식** (`IN_PROGRESS`, 하위 작업 P4-01~07·07b 전부 완료)에 새 UI 작업을 연결. UI-01~03 완료, **UI-04 진행 중 — 04a 완료, 04b 미착수** |
| 다음 하위 작업 | **UI-04b — 프로젝트 설정·상세 설정 화면 + 알림**(`READY`, 범위는 사용자 동의 2026-09-24, 1.11절) |
| 활성 plan | **없다.** [P4-PLAN-07b](plans/P4-PLAN-07b.md)는 완료됐고 실행 결과([P4-07b](p4/evidence/P4-07b-results.md))가 있다. 다음 작업의 plan을 새로 기록·공유한다. 완료된 plan을 다시 열지 않음 |
| 다음 순서 | ~~UI-01~~ → ~~P4-04~~ → ~~UI-02~~ → ~~UI-03~~ → ~~P4-05~~ → ~~P4-05b+P4-06~~ → ~~P4-06b~~ → ~~P4-07~~ → ~~UI-04a~~ → ~~P4-07b~~ → UI-04b …(UI-04 의 남은 하위 plan) → P5 → P6. 단계 안에서도 현재 작업 하나씩 계획·검증. 스킵 경로 C는 관련 예산 라이브가 필요해질 때 다시 검토 |
| 마지막 제품 검증 | **pytest 696 통과·2 건너뜀 + P1 계약 unittest 18 통과 + 웹 단위 21 통과 + 웹 빌드**, 11분 17초, 기준선 693 에서 +3(S-029, `scripts\run-tests.ps1`), 스키마 **v24**(변경 없음). 집중: P4-07b 순수 1 + 서버 1(`test_knowledge_extraction.py`) + 브라우저 1(`test_web_shell.py`, 실제 Edge·가짜 codex). 건너뜀 2건은 이행 시험의 옛 스키마 커밋 창(9절). **실제 codex 라이브는 하지 않았다**(선택 사항 — [P4-07b 결과](p4/evidence/P4-07b-results.md) 3절). 이전 수치(UI-04a: pytest 693·라이브 26/26, P4-07: 688·13/13, P4-06b: 678·29/29)는 당시 사실로 보존 |
| 이 문서 개정 시작 상태 | S-029 시작 시 `main`, HEAD `6f33867`(S-028 인계 push 기록), 작업 트리 clean, `git fetch` 뒤 `origin/main` 과 같음으로 관측. 종료 시 사용자 지시로 P4-07b 를 `f57c036` 으로 커밋하고 인계 기록을 이어서 커밋해 함께 push한다(push 전에 fetch 해 원격이 `6f33867` 그대로임을 확인, 10절 S-029) |
| 남은 상세 | [UI 설계](ui-conversation-design.md) 14절의 실제 화면·CLI 능력 검증. UI-01이 준비 Case·요청 상태/API·잠금·이행을, P4-04가 고정 문맥의 계획·영수증·한도·재시작 복구를 정했다. UI-02가 Runner 제어/실행 분리·트리 중단과 확인·요청 `unknown` 해제·PC 단절 전송 거부를 정했다. UI-03이 기본 화면·요청 처리기·AI 해석 업무화·초안·검토 버전을, P4-05가 업무 단계 진행기·진행 요청·기준 판정 기록·확인 카드·설명 전용 진입·후속 Case 를, P4-05b가 진행 상한 설정을, P4-06이 지식 등록부·자동 주입·Manifest·자동 등록을, P4-06b가 지식 원문의 서버 저장·배정 싣기·이행을, P4-07이 후보 추출·근거·관계·채택 확인·활성화를 정했다. UI-04a 가 결정 사항 패널·프로젝트 규칙 화면(후보·채택 확인의 새 화면 자리)을 정했다. **P4-07b 가 참고 후보의 자동 활성 조건 표시와 사람의 한 번에 활성화(반자동)를 정했다** — 조건의 Project 설정화는 UI-04b 상세 설정의 후보다. 프로젝트 설정·상세 설정·알림·대화 검색·미커밋 포함 시작·외부 편집·활성 Case 변경(D-86)은 UI-04 의 남은 하위 plan, 분할 검토·QG-02~07 자동 실행·QG-08 독립 검토는 필요한 작업의 별도 plan. **여러 Runner 의 배정 경로(원문 소유 PC 로 배정)는 P6-01**(S-026 발견, 9절). 경로 C 스킵·시험 기능 A·미기록 Autonomy의 controlled 취급은 유지 |
| 이번 허용 범위 | S-029: **P4-07b 의 plan·구현·검증·인계**(사용자 요청 "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan·검증·인계 절차를 지켜줘."). 실제 codex 라이브는 선택 사항이라 하지 않았다(3절·결과 문서). 새 의존성 없음. 작업 보고 뒤 사용자 지시("커밋푸시해줘 다른세션에서 이어서 작업할게")로 **이 세션 작업의 커밋·인계 기록 커밋·`origin/main` push**. PR 은 만들지 않는다. S-028의 허용을 승계한 것이 아니며 제품의 실제 외부 게시는 여전히 P5다 |

### 구현된 것 — 요약

각 작업의 설명 문단은 [보존본](development-history-v0.8.md) 1절, 상세는 결과 문서에 있다.

| 작업 | 지금 제품이 하는 일 | 근거 |
|---|---|---|
| P3-R1~R4 | 정책·Profile·영속 모델(R1), Case×Repo 작업공간과 선택·쓰기 허용 강제(R2), 원자적 예산 예약·정산·hard 정지(R3), Autonomy 기반 진입·기본 자동 완료·controlled 확인·누적 material delta(R4) | [R1](p3/evidence/P3-R1-results.md)·[R2](p3/evidence/P3-R2-results.md)·[R3](p3/evidence/P3-R3-results.md)·[R4](p3/evidence/P3-R4-results.md) |
| P3-04 | 수직 통합 경로 A·B 확인(두 저장소 기능, controlled 대표 경로, Task×Repository v12). 경로 C(예산 라이브)는 사용자 지시로 스킵 | [P3-04](p3/evidence/P3-04-results.md) |
| P4-01 | QG-01~07 선택·명시 ON 게이트의 진입 조건, 조건부 독립 검토(별도 reviewer 세션), 초기 실패 뒤 기본 2회 repair 누적 | [P4-01](p4/evidence/P4-01-results.md) |
| P4-02 | 검증 1회 경계, 설정 변경 예약(진행 중 검사 무효화 없음), 강도 변경만 재검증·참조 대조 부분 재검증, 늦은 결과, 배정 직전 재확인 | [P4-02](p4/evidence/P4-02-results.md) |
| P4-03 | Profile 정의 v2 완료 계약 — 목적 의무·충족 방식 필수(생략 우회 차단), 결론 요구, 혼합 목적, 실험 근거·정리 | [P4-03](p4/evidence/P4-03-results.md) |
| UI-01 | 준비 단계 Case, 대화 메시지·정정·참조, 현재 요청과 서버 잠금, 멱등 접수, 카드 답변 한정, 논의 응답, 최초 업무화 | [UI-01](ui/evidence/UI-01-results.md) |
| P4-04 | 고정 문맥 등급(핵심/보조)·인라인 한도, Runner 영수증(해시 대조), 핵심 누락·초과 보류, 시작하지 않은 실행 소비 0, 결과 시점 최신성, 재시작 원시 출력 사용량 복구 | [P4-04](p4/evidence/P4-04-results.md) |
| UI-03 | 기본 대화 화면(`/`, 왼쪽 목록·가운데 대화·필요할 때 여는 검토 패널, 라이트 기본, 패널·테마·마지막 대화 기억), 제어부 요청 처리기(저장된 요청 → 읽기 전용 논의 응답 → 요청 종료, 기동 복구, 설정으로 끔), 준비 단계 응답의 AI 해석과 같은 Case 업무화(위임 근거 = 사용자 메시지, 해석 기록 v19), 본문은 열람 중계·탭 메모리·미연결이면 "연결 필요", 브라우저 초안(접수 확인된 스냅샷만 비움, 자동 전송 없음), 결과물 목록·열람 버전 고정·새 버전 안내·줄 비교·`대화에 참조`, 질문 카드, 기본값 요약, 기존 접수 네 경로의 기록 전 중계. 업무 단계 자동 진행은 미지원 표시 | [UI-03](ui/evidence/UI-03-results.md) |
| P4-05 | 업무 단계 진행기(업무화 뒤 의도 초안→QG-01·repair→질문·동의→controlled 시작→결합 기록/설계·계획→작업 그래프 순서의 조사·구현·검증→기준 판정→자동 완료/controlled 결과/예외 수용, 진입 검사 그대로, 사람·일시·환경 사유 구분), 실행 사이 요청 잠금 유지와 사람 대기·완료·막힘의 요청 종료(사유 코드), 사람 결정 뒤의 진행 요청(여는 메시지 없음), 검증·분석 실행의 기준 보고를 구조 검사로 기록, 확인 카드(의도 동의·controlled·delta·검토·피드백·예외+인수·다시 시도·계속 진행), 종료 Case 설명 전용 진입·종료 snapshot·종료 뒤 소비 구분·예산 변경 이력, 후속 Case 로 메시지 옮김, `HADS_AUTO_PROGRESS_WORK` | [P4-05](p4/evidence/P4-05-results.md) |
| P4-05b | 진행기의 재작성(`repair_limit`, 기본 2 — 다시 쓴 횟수)·재시도(`task_retry_limit`, 기본 1) 상한을 설정으로: 환경 변수 `HADS_REPAIR_LIMIT`·`HADS_TASK_RETRY_LIMIT`(0~10, 이상하면 기동 실패) + Case 별 이력(`progress_limit_setting`, 종료 Case 거부), 조회의 값·출처, `PUT /progress/limits` 뒤 진행기가 한 번 더 감, 상한 대기 카드 "재시도 1/1"·"한도를 올리고 계속", 관리 화면 진행 상한 절. 준비 산출물 재작성이 plan 대로 2회가 됐다 | [P4-05b](p4/evidence/P4-05b-results.md) |
| P4-06 | 지식 등록부(항목 `K-001`·버전·종류·필수/참고·후보/활성/대체/무효·범위(Project/저장소+경로)·활동·권위·출처), 수동 등록·개정·활성화·무효화·충돌 API, **대화에서 말한 프로젝트 규칙의 자동 활성 등록**(사용자 발언 권위, 등록 카드·무효화), 활동+범위 자동 주입(필수·권위 원문 = 핵심, 참고·후보 = 보조, 기존 참조 뒤), 실행별 Manifest(제공·한도 생략·활동/저장소 비적용·범위 미확정), 적용 필수의 미해결 충돌 → `knowledge_conflict` 대기, 관리 화면 지식 패널 | [P4-06](p4/evidence/P4-06-results.md) |
| P4-07 | **선택적 지식 추출**: 검증·분석·실험·구현 실행의 지시문에 후보 규칙(계기 목록·최대 3건·근거 한 줄·일반화 금지)이 붙고 응답 뒤의 `hads-knowledge` 블록을 Runner 가 떼어 원문을 서버에 올린 뒤 결과와 함께 보고, 제어부가 결과 뒤 훅에서 **후보(`ai_proposal`)로만** 등록(같은 원문·`supports` → 기존 항목의 근거 행, `supersedes`·`contradicts` → 관계를 가진 새 후보, 대상 사슬 그대로), 논의 응답의 `proposal: true` 항목도 후보(권위 메시지 없음), 후보 버전의 관측 문맥(실행·저장소·기준 커밋·도구)과 주입 머리 표시, QG-08 채택 확인 `adoption_check`(막음/경고)·`GET adoption-check`·활성화의 축소·`into_knowledge_id`·`adoption` 기록·후보 버전 대체 닫기, 새 화면 후보·제안·근거 카드, 관리 화면 채택 확인·활성화 폼, 가짜 codex 표지 둘, v24(`knowledge_evidence`·버전 컬럼 넷·`knowledge_intake` 재구성). **참고 후보 자동 활성화·독립 AI 검토·별도 추출 실행은 없다** | [P4-07](p4/evidence/P4-07-results.md) |
| P4-06b | **지식 원문의 서버 저장**(사용자 결정 2026-09-24, D-67 보충 — 원문 PC 경계의 유일한 예외): 적용 내용(4,000 자 상한)과 자동 등록 규칙의 권위 사용자 메시지 한 건을 `knowledge_body` 에 저장. 수동 등록은 중계 없이 저장(바로 `available`, PC 미연결이어도), 자동 등록은 Runner 가 결과 전에 원문·권위 메시지를 올림(하나도 등록되지 않으면 메시지 본문 삭제), 배정이 본문을 실어 Runner 가 해시가 맞을 때만 씀(`hash_mismatch` 면 사본으로 대신하지 않음), 옛 지식은 소유 PC 연결 시 자동 이행(`storage = runner → server`), 열람은 서버가 채움, 조회의 `storage`·`source_storage`·`body_source`, 패널·카드·등록 규칙의 "서버 저장 · 비밀값 금지", 패널 "내용 보기" | [P4-06b](p4/evidence/P4-06b-results.md) |
| UI-02 | Runner 제어 루프/실행 작업자 분리, CLI 트리의 Windows job 제어(일시 정지로 만들어 넣고 원장 기록 뒤 재개, 중단·잔류 종료·활성 0 확인, Runner 사망 시 OS 종료), 잔류 확인 근거·재확인, 요청 중단과 `interrupted`, `unknown` 해제, 원장 v2 재시작 대조(재실행·새 세대 없음), 보고 재시도, PC 미연결 전송 거부, 실행별 실제 상태 | [UI-02](ui/evidence/UI-02-results.md) |
| P4-07b | **참고 후보의 자동 활성 조건 표시 — 반자동**(사용자 결정 2026-09-24): 순수 함수 `auto_reference_check` 가 조건 여덟(후보 · 효력 참고 · 종류 운영 사실/알려진 문제 · 서로 다른 실행 둘 이상의 근거(출처 실행 + `supports`/`duplicate` 근거 행, 실행 id 로 중복 제거) · 그중 하나는 종료 코드 0 명령이 있는 검증/분석 실행(명령 기록 — 자기보고) · 관측한 저장소 범위 · 반증 관계/반증 후보/열린 충돌 없음 · 채택 확인 막음 없음)의 충족/미충족을 낸다. 조회(`knowledge_view` 후보 항목·등록 카드 행)의 `auto_reference{ready, met, unmet, findings, evidence_runs}`, `GET /api/knowledge/{id}/auto-reference`. **한 번에 활성화** `POST /api/projects/{id}/knowledge/activate-ready`(화면이 본 `knowledge_ids` 만; 각 후보를 조건 재계산 → `activate_knowledge` 효력 참고·범위 그대로·사유 "자동 활성 조건 충족 — 사람이 한 번에 활성화"·`adoption.by`·`adoption.auto_reference{met, evidence_runs}`; 미충족·거부는 건너뛰어 결과에, 200). 규칙 화면의 배지·조건 목록·후보 절 머리의 버튼, 후보 카드·관리 화면 패널의 배지. **클릭 없는 활성화 없음·필수 승격 없음·스키마 v24 그대로.** 논의 응답의 제안(관측 없음)은 조건 (4)를 만족하지 못한다 | [P4-07b](p4/evidence/P4-07b-results.md) |
| UI-04a | **결정 사항 패널**(오른쪽, D-80): 업무·목표·기준(의도 최신 버전·동의 상태·`원문 열기`·기준 판정)·결정(종류를 사람 말로·대상 의도 버전 열기·근거 메시지로)·이 대화에서 정한 규칙·후보(사용자 말/AI 제안/실행 후보/근거/거부, 지금 상태, `메시지 #n`, 프로젝트 규칙으로)·이 업무에 적용된 프로젝트 규칙(`knowledge-use` — Manifest 버전별 집계, "주입은 준수의 증거가 아니다")·답한 질문·AI 해석·진행 이력. **프로젝트 규칙 화면**(`?screen=rules`, 왼쪽 목록): 활성 필수/참고·후보·대체/무효 이력, 항목의 범위·활동·권위·저장 위치·관측·관계·근거·채택 기록·이력·출처(대화 제목·메시지 순번 → `&seq=N` 강조)·내용 보기, 채택 확인·활성화(효력·활동·저장소 축소·`into`)·개정·무효·충돌 기록/해소·수동 등록(출처 대화 선택). 카드 링크가 규칙 화면으로. 서버 조회에 출처 제목·순번·집계만 더함(본문 없음, 스키마 v24 그대로) | [UI-04a](ui/evidence/UI-04a-results.md) |

**계속 유효한 결정·규칙** — 옮긴 문단에서 지금도 적용되는 것만 남긴다.

- **강제 축은 넷이다.** 조회 `enforcement` 에서 `autonomy`·`controlled_checkpoint`·`budget`·`repository_selection` 이 `enforced`, 게시 허용과 실제 push·PR 은 P5 다. 결과 후보 확인도 게시 권한을 만들지 않는다(D-65). 이후 작업(P4-01~05·UI-01~03)은 새 강제 축을 만들지 않았다. UI-03의 AI 해석도 권한이 아니다 — 업무화의 위임 근거는 사용자 메시지 원문이다. P4-05의 진행기·기준 기록도 권한·동의·인수를 만들지 않는다. P4-05b의 한도 올리기와 P4-06의 지식 등록·활성화·주입도 권한·인수가 아니며, 주입은 준수의 증거가 아니다. P4-06b 의 서버 저장·이행·해시 대조 영수증도 "제공했다"일 뿐 권한·준수가 아니다. P4-07 의 후보·근거·채택 확인도 권한·동의·인수가 아니며 활성화는 사람의 결정(새 버전)이다 — AI 제안은 어떤 경로로도 활성 필수가 되지 않는다. P4-07b 의 자동 활성 조건 충족은 표시일 뿐이고 한 번에 활성화는 사람의 클릭(참고 효력만)이다.
- **원문 PC 경계의 예외는 지식뿐이다**(사용자 결정 2026-09-24, P4-06b). `knowledge_body` 가 제어부 DB 의 유일한 본문 표이며 지식 원문과 자동 등록 규칙의 권위 사용자 메시지 한 건만 들어간다. 대화 전체·응답·의도·설계·계획·코드·로그는 여전히 PC 에만 있고 서버 로그에는 본문이 없다. 이 예외를 다른 원문으로 넓히는 것은 새 사용자 결정이다.
- **`autonomy = NULL`(R1 이전 Case)은 사용자 결정(2026-09-22)으로 controlled 로 취급한다.** 저장값은 `NULL`·`migrated_unknown` 그대로이고 도출만 바뀐다. 그 Case 의 단계 검토 기본값은 v0.6(사람 검토)이다.
- **새 기본값을 기존 Case 에 소급하지 않는다.** v0.6 의 완료(사람 인수)·단계 검토(사람 검토) 기본값이 조건 충족 자동·자동 진행으로 뒤집혔지만 명시 설정과 Autonomy 미기록 Case 가 경계다. 기존 Case 는 Profile 정의 v1, `stage` NULL(업무 단계로 도출), P4-04 이전 실행은 영수증 없음(`not_reported`)으로 남는다.
- **예산 강제의 약속은 지표마다 다르다.** 실행 수·검토 수·문맥 크기는 `enforced_absolute`, 시간은 `enforced_no_absolute_cap`(진행 중 CLI 를 초 단위로 끊지 못함), 토큰·비용 hard 한도는 설정을 받지 않는다. 모르는 값을 0 으로 적지 않는다.
- **이전 계획·증거는 당시 사실이다.** 새 설계의 통과 증거로 재명명하지 않는다.

### 1.1 확정한 개발 순서와 작업 분할 — D-90

확정 설계는 [D-68~90](decisions.md)와 [대화 중심 UI](ui-conversation-design.md)에 모았다. 새 작업 ID는 개발 관리용 분할이며 구현 완료를 뜻하지 않는다. 각 행의 세부 범위·이행·시험은 해당 작업의 새 plan에서 구체화한다.

| 순서 | 하위 작업과 범위 | 다음 단계로 넘길 조건 |
|---|---|---|
| 1 | ~~**UI-01 대화·요청 기반**~~ **완료(2026-09-23)**: 준비 Case, 메시지·정정·버전 참조, 요청 처리 상태, 서버 전송 잠금과 접수 | 같은 Case의 준비→최초 업무화, 원문/기존 정책 보존, 중복·충돌 일반 전송 차단, 카드 답변의 대상 구분 검증 — [UI-01 결과](ui/evidence/UI-01-results.md) |
| 2 | ~~**P4-04 문맥·재개**~~ **완료(2026-09-23)**: 고정 입력의 등급·인라인 한도·영수증·핵심 누락 보류·시작하지 않은 실행·최신성·재시작 사용량 복구([P4-04 결과](p4/evidence/P4-04-results.md)). ~~**UI-02 입력·실행 제어**~~ **완료(2026-09-23)**: Runner 제어/실행 분리, 긴 실행 중 입력·카드 답변 저장, 트리 중단과 종료 확인, `unknown` 해제, 재시작 대조, PC 미연결 거부, 관리 화면 중단 버튼([UI-02 결과](ui/evidence/UI-02-results.md)) | 긴 실행 중 제어 수신, 실제 종료·잔류 활동과 상태 불명의 구분, 재시작·중복 방지·소비 보존을 검증한 뒤 중단 UI 연결 |
| 3 | ~~**UI-03 기본 대화 화면**~~ **완료(2026-09-23)**: PC·라이트 기본 shell, 프로젝트/대화 선택→논의/요청(처리기·AI 해석 업무화)→진행/질문→실제 결과 검토, 기본값 요약·초안 복구·검토 버전 유지([UI-03 결과](ui/evidence/UI-03-results.md)). 업무 단계 자동 진행은 미지원 표시(1.6절) | 새 기본 흐름을 실제 사용해 검토. 없는 산출물/지원 능력을 표시하지 않고 API 상태·원문 가용성과 일치 |
| 4 | ~~**P4-05 업무 단계 자동 진행·완료·예외·후속**~~ **완료(2026-09-23)**([P4-05 결과](p4/evidence/P4-05-results.md)). ~~**P4-05b 진행 상한·P4-06 지식 관리·적용**~~ **완료(2026-09-24)**([P4-05b](p4/evidence/P4-05b-results.md)·[P4-06](p4/evidence/P4-06-results.md)). ~~**P4-06b 지식 원문 서버 저장**~~ **완료(2026-09-24, 사용자 결정)**([P4-06b](p4/evidence/P4-06b-results.md)). ~~**P4-07 선택적 지식 추출**~~ **완료(2026-09-24)**([P4-07](p4/evidence/P4-07-results.md)). ~~**P4-07b 참고 후보의 자동 활성 조건 표시(반자동)**~~ **완료(2026-09-24, 사용자 결정)**([P4-07b](p4/evidence/P4-07b-results.md)). **UI-04 남은 업무·화면 연결**: ~~결정 사항 패널·프로젝트 규칙 화면~~ **UI-04a 완료(2026-09-24)**([UI-04a 결과](ui/evidence/UI-04a-results.md)), 남은 것 — 프로젝트 설정·상세 설정·알림, 활성 목적·Profile 이행(D-86), 대화 검색(D-84), 미커밋 포함 시작(D-77), 외부 편집(D-89) | 기존 기준·증거·소비·원래 파일·권한을 보존하면서 각 흐름 검증. UI-04는 하위 plan(04a, 04b …)으로 나누어 완료 범위 추적 |
| 5 | **P5 GitHub**, **P6 운영·전체 수용**: 연결/이슈/외부 결과, 원격·인증·복구·설치·전체 수용 | 연결과 게시 허용을 구분하고 실제 외부 결과와 전체 FR/NFR/AC 근거 확인. 앞당긴 검증이 전체 P6를 대체하지 않음 |

기본 UI를 사용할 수 있는 시점을 앞당기되 제어 기능은 먼저 검증한다. 특정 기능을 UI-03에서 지원하지 않으면 미지원으로 표시하며 임시 성공 동작을 만들지 않는다. P4-04에 전체 UI·일반 중단·미커밋 포함 작업을 몰아넣지 않는다. P4-06~07의 남은 첫 버전 기능도 없애지 않는다.

현재 저장소 guard는 기록 repo의 코드 선택·작업공간을 거부한다. D-73은 별도 코드 허용이 있으면 겸용하도록 바꾼 목표이며 과거 P3-R2의 통과 증거를 새 규칙의 근거로 재명명하지 않는다. 이 변경은 P5 연결 전에 저장소 정책·선택·작업공간의 모든 경로를 함께 검증한다.

### 1.2 UI-01의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·범위·시작점·검증 초점은 [보존본](development-history-v0.8.md) 2절, 구현은 [UI-PLAN-01](plans/UI-PLAN-01.md)·[UI-01 결과](ui/evidence/UI-01-results.md)다.

### 1.3 P4-04의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·UI-01 이 넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-04](plans/P4-PLAN-04.md)·[P4-04 결과](p4/evidence/P4-04-results.md)다.

### 1.4 UI-02의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·P4-04가 넘긴 것·실측 근거·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [UI-PLAN-02](plans/UI-PLAN-02.md)·[UI-02 결과](ui/evidence/UI-02-results.md)다.

### 1.5 UI-03의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·UI-02가 넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [UI-PLAN-03](plans/UI-PLAN-03.md)·[UI-03 결과](ui/evidence/UI-03-results.md)다.

### 1.6 P4-05의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·UI-03이 넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-05](plans/P4-PLAN-05.md)·[P4-05 결과](p4/evidence/P4-05-results.md)다.

### 1.7 P4-06의 경계와 시작점 (완료 — 보존본으로 옮김)

완료. 당시의 목표·P4-05가 넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-06](plans/P4-PLAN-06.md)·[P4-06 결과](p4/evidence/P4-06-results.md)다.

### 1.8 P4-05b 진행 상한 설정 (완료 — 보존본으로 옮김)

완료. 당시의 설계 제안(사용자 결정 2026-09-24)은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-05b](plans/P4-PLAN-05b.md)·[P4-05b 결과](p4/evidence/P4-05b-results.md)다.

### 1.9 P4-06b 지식 원문 서버 저장 (완료 — 보존본으로 옮김)

완료. 당시의 배경·확정 사항·시작점(사용자 결정 2026-09-24)은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-06b](plans/P4-PLAN-06b.md)·[P4-06b 결과](p4/evidence/P4-06b-results.md)다.

### 1.10 P4-07 선택적 지식 추출 (완료 — 보존본으로 옮김)

완료. 당시의 목표·P4-06/06b 가 넘긴 것·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-07](plans/P4-PLAN-07.md)·[P4-07 결과](p4/evidence/P4-07-results.md)다.

### 1.11 UI-04 의 진행 — 04a 완료, 다음 하위 plan(UI-04b)의 경계와 시작점

**UI-04(남은 업무·화면 연결)의 확정 범위**(D-90 표 4행, [UI 설계](ui-conversation-design.md)): ~~결정 사항 패널·프로젝트 규칙 화면~~(**UI-04a 완료**, [UI-PLAN-04a](plans/UI-PLAN-04a.md)·[UI-04a 결과](ui/evidence/UI-04a-results.md)), 프로젝트 설정·상세 설정(프로젝트 기본값·Case 조정, 인라인 한도·진행 상한 Project 층·수준 조정·저장소 선택·QG-02~07, D-72), 알림(D-82), 활성 목적·Profile 이행(D-86), 대화 검색(D-84), 미커밋 포함 시작(D-77), 외부 편집·작업 PC 열기(D-89), 실행시간 합계 계측(D-88). 하위 plan 마다 그 세션이 범위를 정하고 기록·공유한 뒤 구현한다.

- **UI-04a 가 넘긴 것:** 프로젝트 규칙 화면은 왼쪽 목록에서 연다(`?screen=rules`) — **프로젝트 설정 화면이 생기면 그 안으로 옮긴다**(설계 "프로젝트 설정의 프로젝트 규칙"). 결정 사항 패널은 보고 이동하는 자리이며 목표·범위·제약의 **본문**은 PC 원문(결과물 패널이 연다). 주소 규칙 `web/src/lib/address.ts`(`screen`·`item`·`seq`), 메시지 이동 사건 `hads:focus-message`, 서버 집계 `case_knowledge_use_view`(`GET /api/cases/{id}/knowledge-use`), 카드 링크는 규칙 화면. 독립 검토·검색은 넣지 않았다.
- **P4-07b 가 넘긴 것:** 규칙 화면의 후보 절 머리에 "조건 충족 n건 한 번에 활성화" 버튼(`rules-activate-ready`)과 항목 배지(`rule-auto-{key}`)가 있다 — 규칙 화면을 설정 화면으로 옮길 때 함께 간다. 조건 여덟은 코드에 고정돼 있고(`domain.knowledge.AUTO_REFERENCE_*`) Project 설정이 아니다 — 설정화는 UI-04b 상세 설정에서 필요하면 정한다(새 제품 판단이면 묻는다). 조건 결과는 저장하지 않고 조회 때 계산한다(후보 수만큼 실행·명령 조회 — 후보가 많아지면 비용을 본다).
- **다음 하위 plan(UI-04b — 사용자 동의 2026-09-24, S-028):** **프로젝트 설정·상세 설정 화면 + 알림(D-72·D-82)** — 프로젝트 기본값(도구·모델·깊이·Autonomy·예산)의 표시와 Case 조정·출처·적용 시점·기본값 복귀, 인라인 한도·진행 상한의 Project 층, 수준 조정·저장소 선택·QG-02~07 의 새 화면 자리, 프로젝트 규칙의 자리 이동, 답변·확인 필요·차단·완료의 PC 알림(보고 있는 대화는 팝업 억제). 이 범위는 관리 화면의 정책 패널이 쓰는 기존 API 를 새 화면으로 옮기는 일이 대부분이며 D-86·D-84·D-77·D-89 보다 의존이 적다. D-86(활성 Case 변경)은 P4-03 이 미룬 항목 대응·재검증이 있어 별도 하위 plan 으로 둔다.
- 코드 대조 시작점: `web/src/PolicyPanel.tsx`(정책·상한·QG 설정의 관리 화면), `web/src/shell/{Shell,Sidebar,ProjectRules,Composer}.tsx`(기본값 요약은 `Composer` 의 설정 요약), `controller/api.py` 의 `/api/cases/{id}/policy`·`/progress/limits`·`/quality-gates`·`/api/projects`, `controller/config.py`(환경 변수 기본값), `domain/progression.py`.

### 1.12 P4-07b 참고 후보의 자동 활성 조건 표시(반자동) (완료 — 보존본으로 옮김)

완료. 당시의 사용자 결정(2026-09-24, S-028)·조건·경계·시작점은 [보존본](development-history-v0.8.md) 2절, 구현은 [P4-PLAN-07b](plans/P4-PLAN-07b.md)·[P4-07b 결과](p4/evidence/P4-07b-results.md)다.

## 2. 다음 세션 시작 절차

1. `git status --short --branch`, 최근 커밋·변경 파일을 확인한다. 이 문서 개정의 미커밋 파일과 사용자 작업을 구별한다. 자동 reset·clean·stash·강제 checkout·rebase를 하지 않는다. 이전 인계의 원격 동기 상태를 현재 사실로 가정하지 않는다.
2. 현재 상태와 최신 인계(10절), [UI 설계](ui-conversation-design.md), [UI-02 결과](ui/evidence/UI-02-results.md), [사용자 결정](decisions.md), [전체 설계](design-draft.md), [문맥](review-context-contract.md), [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [완료](completion-lifecycle.md)을 읽는다. 전체 옛 개발 일지를 다시 읽을 필요는 없지만 현재 하위 작업과 UI 설계의 접점·미구현 경계는 확인한다.
3. 실제 코드의 정책·스키마·진입 검사·화면과 새 설계의 차이를 확인한다. 문서상 목표를 구현 사실로 취급하지 않는다. 기존 Case·결정·원문·증거·실행 이력은 보존한다.
4. 기존 실행 환경을 확인하고 `scripts\bootstrap.ps1`, `scripts\run-tests.ps1` 등 저장소의 재현 경로로 기준선을 확인한다. 설치/복구가 필요하면 기존 허용·환경에 맞게 처리한다. 672(pytest)+18(P1 계약)+18(웹 단위) 은 인계 시점 수치이며 현재 시험 결과를 대신하지 않는다. `scripts\run-tests.ps1` 은 UI-03부터 웹 빌드·웹 단위 시험(`npm test`)을 먼저 돌리고(`-SkipWeb` 으로 뺀다), `tests/test_web_shell.py` 는 설치된 Edge 를 Playwright 로 연다(없으면 건너뛴다). `tests/test_process_control.py`·`test_runner_process_control.py` 는 Windows 에서 실제 프로세스를 만든다(비 Windows 는 건너뛴다).
5. **P4-PLAN-01~06(05b 포함)·UI-PLAN-01~03은 완료다.** 다시 열지 말고 다음 작업의 plan을 기록·공유한 뒤 구현한다. P4-01~03이 남긴 정책 리비전·예약·재검증 판정·repair 누적·Profile 정의판·목적 의무·결론, UI-01의 준비 단계·메시지·요청·잠금·업무화 기록, P4-04의 문맥 등급·인라인 한도·영수증·`not_started_reason`·최신성·복구 정산 출처, **UI-02의 제어/실행 분리·원장 v2(시작 기록)·job 트리 제어·잔류 관측과 근거·중단·`interrupted`·재시작 대조·PC 연결 판정**, **UI-03의 요청 처리기(읽기 전용 응답 하나·기동 복구)·해석 기록과 업무화 규칙·초안 비우기 규칙·열람 버전 고정**, **P4-05의 진행 행·진행 요청(`origin`)·기준 보고의 구조 검사·종료 snapshot·설명 전용 진입·후속 연결**, **P4-05b의 상한 설정·이력(다시 쓴/다시 한 횟수로 셈, 시도 수 초기화 없음)**, **P4-06의 지식 버전·권위 규칙·선택/Manifest·자동 등록(`knowledge_intake`)·충돌 보류**를 보존한다. UI-01은 `a509063`, P4-04는 `5580896`, UI-02는 `592eb39`, UI-03은 `d51fc02`, P4-05는 `b475146`, P4-05b·P4-06은 `d74726a`에 커밋·push됐다 — 시작 시 `git status`·`git log`·원격 ref로 확인하고, 이전 인계의 상태를 현재 사실로 가정하지 않는다.
6. 작업을 수행한 뒤 현재 표와 진행표·plan·결과를 함께 갱신한다. 선택한 하위 작업에서 이후 항목으로 자동 확대하지 않는다. P3-R4의 고정 컨텍스트(`run_context_ref`)·material delta, P4-01~02의 게이트 실행·재검증, P4-03의 완료 의미(`completion_meaning`)를 재사용한다. 기존 판정을 소급 변경하지 않는다.

기본 작업공간은 이 checkout이다. 다른 작업공간이 필요하면 정확한 경로·이유를 plan과 인계에 기록한다. 다른 세션과 같은 파일을 동시에 수정하지 않는다. 이 안내서는 구현을 위임하지만 외부 쓰기나 기존 데이터 파괴를 포괄하지 않는다.

## 3. 계획·개정·증거 관리

- plan은 작업 목록뿐 아니라 요구에서 도출한 성공 기준과 검증 방법을 포함한다. 활성 plan을 이어 쓰고 범위/기준이 바뀌면 구현 전에 변경 이유를 기록한다.
- 단계/작업별 구현 완료, 자동 시험, 실제 CLI 시험, 외부 서비스 시험, 문서 검토를 구별한다.
- 기존 테스트가 v0.6 정책을 고정했다면 그 의미를 검토해 바꾸고 대응하는 v0.7 거절·보존 시험을 추가한다. 실패를 피하려고 검사를 삭제하지 않는다.
- 새 정책을 기존 승인·동의·권한에 소급 적용하지 않는다. 마이그레이션은 기존 정책값·원문·주체·버전을 명시 보존하고 새 Case의 기본값과 구분한다. 진행 중 Case의 정책 변경은 영향과 권한을 검토하고 종료 기록은 다시 쓰지 않는다.
- 구현하지 않은 모드는 비활성/미지원으로 표시한다. R1에 모델이 생겼다는 이유로 R4 자동 실행이나 P5 게시를 지원한다고 표시하지 않는다. R2가 쓰기 허용을 강제한다고 게시 허용까지 강제되는 것으로 표시하지 않는다.
- 파일/필드 이름과 수치 기본값 등은 상세 설계 제안이며, 사용자 확정 정책과 구별한다. 스키마 번호는 실제 마이그레이션을 만들 때 정한다.

| Plan | 상태·근거 |
|---|---|
| [P4-PLAN-07b](plans/P4-PLAN-07b.md) | 완료, AC-1~9. [실행 결과](p4/evidence/P4-07b-results.md) — 참고 후보의 자동 활성 조건(여덟)의 계산·표시(`auto_reference_check`·조회·배지), 사람의 한 번에 활성화(`activate-ready`, 효력 참고·범위 그대로·충족 항목 기록), 미충족·거부 건너뜀, 다음 실행 주입. 스키마 v24 그대로. 클릭 없는 활성화·필수 승격·독립 검토·조건 설정화는 범위 밖. 실제 codex 라이브는 선택 사항이라 하지 않았다 |
| [UI-PLAN-04a](plans/UI-PLAN-04a.md) | 완료, AC-1~14. [실행 결과](ui/evidence/UI-04a-results.md) — 결정 사항 패널(업무·목표·기준·결정·이 대화에서 정한 규칙·후보·적용된 규칙·질문·해석·이력, 메시지로 이동)·프로젝트 규칙 화면(등록부·채택 확인·활성화·개정·무효·충돌·수동 등록·원래 대화로)·주소 규칙 `seq`·조회 확장·`knowledge-use` 집계, 스키마 v24 그대로. 실제 codex·Edge 라이브 26/26(규칙 옮김·제안 둘은 AI 관찰). 프로젝트 설정 화면·검색·알림·D-86 은 UI-04 의 남은 하위 plan |
| [P4-PLAN-07](plans/P4-PLAN-07.md) | 완료, AC-1~14. [실행 결과](p4/evidence/P4-07-results.md) — 작업 실행의 후보 블록·논의 응답의 제안 표지·근거 행(같은 내용·`supports`)·관계 후보(`supersedes`·`contradicts`)·관측 문맥·QG-08 채택 확인·활성화(축소·`into`)·카드·관리 화면, v24. 실제 codex 라이브 두 회차(첫째 5/5 후보 없음, 둘째 13/13 — 정리 응답의 제안 둘 → 활성화 → 다른 대화의 검증 실행 2개 주입). 참고 후보 자동 활성화(사용자 판단 대기)·독립 검토·별도 추출 실행·UI-04 는 범위 밖 |
| [P4-PLAN-06b](plans/P4-PLAN-06b.md) | 완료, AC-1~14. [실행 결과](p4/evidence/P4-06b-results.md) — 지식 원문·권위 메시지의 서버 저장(사용자 결정, `knowledge_body`), 배정 싣기·해시 대조, 수동 등록의 중계 제거, 옛 지식 이행, 서버 열람, 화면 안내, v23. 실제 codex 두 Runner 라이브. 지식 외 원문 경계 변경·P4-07·UI-04 는 범위 밖 |
| [P4-PLAN-06](plans/P4-PLAN-06.md) | 완료, AC-1~18. [실행 결과](p4/evidence/P4-06-results.md) — 지식 등록부·수동/자동 등록(사용자 결정: 대화의 규칙 자동 활성)·활동+범위 주입·Manifest·충돌 보류·원문 경계, v22. 실제 codex 라이브 21/21. 선택 추출(P4-07)·결정 사항/프로젝트 규칙 화면(UI-04)은 범위 밖 |
| [P4-PLAN-05b](plans/P4-PLAN-05b.md) | 완료, AC-1~10. [실행 결과](p4/evidence/P4-05b-results.md) — 진행 상한 설정(환경 변수 + Case 이력), 한도를 올리고 계속, 준비 재작성 2회로 바로잡음, v21. Project 층은 UI-04 |
| [P4-PLAN-05](plans/P4-PLAN-05.md) | 완료, AC-1~21. [실행 결과](p4/evidence/P4-05-results.md) — 업무 단계 진행기·진행 요청·기준 판정 기록·확인 카드·설명 전용 진입·종료 snapshot·후속 Case, v20. QG-02~07 자동 실행·검증 1회 중단·D-86·상세 설정은 범위 밖(3.9절) |
| [UI-PLAN-03](plans/UI-PLAN-03.md) | 완료, AC-1~20. [실행 결과](ui/evidence/UI-03-results.md) — 기본 대화 화면·요청 처리기·AI 해석 최초 업무화·브라우저 초안 복구·결과물 검토 버전 유지·기본값 요약·네 접수 경로 기록 전 중계, v19. 업무 단계 자동 진행·확인 카드·검색·알림·상세 설정은 범위 밖(3.11절) |
| [UI-PLAN-02](plans/UI-PLAN-02.md) | 완료, AC-1~20. [실행 결과](ui/evidence/UI-02-results.md) — Runner 제어/실행 분리·job 트리 제어·잔류 확인 근거·요청 중단과 `interrupted`·`unknown` 해제·원장 v2 재시작 대조·PC 미연결 거부, v18. 검증 1회 중단·시간 예산 자동 중단은 범위 밖(3.9절) |
| [P4-PLAN-04](plans/P4-PLAN-04.md) | 완료, AC-1~20. [실행 결과](p4/evidence/P4-04-results.md) — 문맥 등급·인라인 한도(보조만 생략, 핵심 초과 보류)·핵심 가용성 진입 검사·Runner 영수증(해시 대조)·핵심 미확인 시 실행 전 중단·시작하지 않은 실행 소비 0·결과 시점 최신성·재시작 원시 출력 사용량 복구, v17. 분할 검토·체크포인트·단계적 조회는 범위 밖(3.9절) |
| [UI-PLAN-01](plans/UI-PLAN-01.md) | 완료, AC-1~20. [실행 결과](ui/evidence/UI-01-results.md) — 준비 Case·대화 메시지·현재 요청과 서버 잠금·멱등 접수·카드 답변 한정·논의 응답 실행·최초 업무화·대화 문맥, v16. `unknown` 해제·PC 단절은 UI-02 |
| [P4-PLAN-03](plans/P4-PLAN-03.md) | 완료, AC-1~21. [실행 결과](p4/evidence/P4-03-results.md) — Profile 정의 v2 완료 계약, 목적 의무·충족 방식 필수, 결론 요구, 혼합 목적, 실험 근거·정리, v15. Profile 재분류는 범위 밖(3.8절) |
| [P4-PLAN-02](plans/P4-PLAN-02.md) | 완료, AC-1~22. [실행 결과](p4/evidence/P4-02-results.md) — 설정 변경 예약, 부분 재검증, 늦은 결과, 배정 직전 재확인, v14 |
| [P4-PLAN-01](plans/P4-PLAN-01.md) | 완료, AC-1~20. [실행 결과](p4/evidence/P4-01-results.md) — QG-01~07, 조건부 독립 검토, 기본 2회 repair, v13 |
| P3 plan(P3-PLAN-01~04·R1~R4) | 완료. **P3-PLAN-04만 AC-17 라이브 미수행**(사용자 지시로 스킵). 행별 AC·결과 링크는 [보존본](development-history-v0.8.md) 5절 |
| P1·P2 plan | [보존본의 plan 목록](development-history-v0.6.md) 및 `plans/` 원문 유지 |

## 4. 단계 진행표

| 단계 | 상태 | 다음 작업 | 완료 근거/변경 사항 |
|---|---|---|---|
| P1 CLI 계약·실증 | ACCEPTED_WITH_LIMITATIONS | — | [환경](p1-environment-contract.md), [CLI 실증](p1/evidence/P1-02-results.md), [중지 실증](p1/evidence/P1-03-results.md). 제한은 아래 이월표 유지 |
| P2 최소 흐름 | DONE (v0.6) | — | [P2-04 결과](p2/evidence/P2-04-results.md). 당시 승인 중심 동작의 사실 보존 |
| P3 실행 기반·새 정책·통합 | DONE_WITH_LIMITATIONS | — | P3-01~03·R1~R4·P3-04 완료. 라이브는 경로 A·B 확인, **경로 C 는 사용자 지시로 스킵**(AC-17 라이브 증거 없음) |
| UI 대화 중심 UI | IN_PROGRESS | **UI-04b**(UI-04 의 다음 하위 plan) | [UI-01](ui/evidence/UI-01-results.md): 준비 Case·대화 메시지·현재 요청과 서버 잠금·멱등 접수·카드 답변 한정·논의 응답·최초 업무화·v16. [UI-02](ui/evidence/UI-02-results.md): Runner 제어/실행 분리·job 트리 중단과 종료 확인·요청 중단과 `interrupted`·`unknown` 해제·재시작 대조·PC 미연결 거부·v18. [UI-03](ui/evidence/UI-03-results.md): 기본 대화 화면·요청 처리기·AI 해석 업무화·초안 복구·검토 버전 유지·v19. [P4-05](p4/evidence/P4-05-results.md)의 확인 카드·진행 배너·후속 이동이 새 화면에 있다. [UI-04a](ui/evidence/UI-04a-results.md): 결정 사항 패널·프로젝트 규칙 화면·원래 메시지로 이동·Manifest 집계(스키마 변경 없음). UI-04 의 남은 하위 plan 미착수 |
| P4 품질·여섯 업무·지식 | IN_PROGRESS(P4-01~07·07b 완료, 전체 완료 조건은 UI-04 뒤 P4 수용에서) | — (UI-04 뒤 P4 수용 확인) | [P4-01](p4/evidence/P4-01-results.md): QG-01~07 정책·별도 의미 검토·repair·v13. [P4-02](p4/evidence/P4-02-results.md): 설정 변경 예약·부분 재검증·늦은 결과·v14. [P4-03](p4/evidence/P4-03-results.md): Profile 정의 v2 완료 계약·목적 의무·결론·혼합 목적·실험 정리·v15. [P4-04](p4/evidence/P4-04-results.md): 문맥 등급·인라인 한도·영수증·핵심 누락 보류·시작하지 않은 실행·최신성·재시작 사용량 복구·v17. [P4-05](p4/evidence/P4-05-results.md): 업무 단계 진행기·진행 요청·기준 판정 기록·확인 카드·설명 전용 진입·종료 snapshot·후속 Case·v20. [P4-05b](p4/evidence/P4-05b-results.md): 진행 상한 설정·v21. [P4-06](p4/evidence/P4-06-results.md): 지식 등록부·자동 주입·Manifest·자동 등록·충돌 보류·v22. [P4-06b](p4/evidence/P4-06b-results.md): 지식 원문·권위 메시지의 서버 저장·배정 싣기·이행·v23(사용자 결정 2026-09-24). [P4-07](p4/evidence/P4-07-results.md): 작업 실행의 후보·근거·관계·관측 문맥·QG-08 채택 확인·활성화·v24. [P4-07b](p4/evidence/P4-07b-results.md): 참고 후보의 자동 활성 조건 표시와 사람의 한 번에 활성화(반자동, 사용자 결정 2026-09-24, 스키마 변경 없음). P4 의 "완료 조건"(여섯 Profile 대표·혼합 목적·실패/복구/변경 재현, Case 간 지식 관리·추출·주입) 전체 수용은 UI-04 뒤 별도 확인이며 자동 착수하지 않는다 |
| P5 GitHub·외부 완료 | PENDING | P5-01 | 범위 사전 허용과 controlled 게시 전 확인으로 개정 |
| P6 원격·운영·전체 수용 | PENDING | P6-01 | 기존 데이터/단절/운영 요구 유지, 새 계약도 포함 |

순서는 `P3-03(완료) → R1(완료) → R2(완료) → R3(완료) → R4(완료) → P3-04(완료)`다. 번호 R1~R4는 새 정책 반영 작업이며 기존 P3-01~03의 재개·재번호가 아니다.

## 5. P3의 남은 작업

P3는 `DONE_WITH_LIMITATIONS` 로 끝났다(4절). 하위 작업 표와 R1~R4·완료 조건 문단은 [보존본](development-history-v0.8.md) 3절로 옮겼다. 남은 것은 **경로 C(예산 도달의 기능 흐름 라이브)** 하나이며 사용자 지시로 스킵했다(9절).

## 6. P4 — 품질·다양한 업무·지식

참조: [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [Profile](case-profiles.md), [문맥](review-context-contract.md), [지식](project-knowledge.md), [완료](completion-lifecycle.md).

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| ~~P4-01 게이트·repair~~ | **완료(2026-09-22).** QG-01 검사 강도, QG-02~07 선택, Case/Task 설정, 조건부 별도 의미 검토, 스키마 v13 | 필수 기준/증거 유지, 초기 실패 후 기본 2회, 재분할/세션교체 초기화 없음, OFF/미적용/통과 구별. [실행 결과](p4/evidence/P4-01-results.md) |
| ~~P4-02 변경·예약~~ | **완료(2026-09-22).** 검증 1회 경계, 설정 변경 예약·적용 시각·취소, 요청/코드/권한 변경의 부분 재검증, 늦은 결과, 배정 직전 재확인, 스키마 v14 | 원래 판정·정책 보존, 예약이 진행 중 검사를 무효화하지 않음, 강도 변경만 재검증, `unknown` 을 재사용으로 적지 않음, 늦은 결과가 repair·배정을 만들지 않음. [실행 결과](p4/evidence/P4-02-results.md) |
| ~~P4-03 여섯 Profile~~ | **완료(2026-09-23).** Profile 정의 v2 완료 계약, 기준의 목적 의무·충족 방식 필수, 원인·조사 결론 요구, 혼합 목적 선언, 실험 근거 제한·정리 상태, 스키마 v15 | 목적별 필수 의무·허용 방식, 생략 우회 차단, 판단 불가 구별, 무변경 성공은 검증 실행 관측, 기준 없는 목적은 예외 불가, v1 Case 소급 없음. **Profile 재분류는 범위 밖.** [실행 결과](p4/evidence/P4-03-results.md) |
| ~~P4-04 컨텍스트·재개~~ | **완료(2026-09-23).** 고정 입력의 등급(핵심/보조)·인라인 한도·영수증(해시 대조), 핵심 누락·초과 보류, 시작하지 않은 실행 소비 0, 결과 시점 최신성, 새 세션 보존, Runner 재시작의 원시 출력 사용량 복구, 스키마 v17. **분할 검토·체크포인트·단계적 조회는 넣지 않았다**(plan 3.9절) | 원문 누락·stale 표시, 필수 조건 유지, 누락을 성공으로 적지 않음, 작성자의 완료 요약만으로 검토하지 않음(P4-01 규칙 유지). [실행 결과](p4/evidence/P4-04-results.md) |
| ~~P4-05 업무 진행·완료·예외·후속~~ | **완료(2026-09-23).** 업무 단계 진행기(의도 초안→QG-01·repair→질문·동의→controlled 시작→준비→작업 그래프·작업공간→구현·검증→기준 판정→자동 완료/controlled 결과/예외 수용), 실행 사이 잠금 유지·사람 대기에서만 전송 열림·진행 요청, 기준 판정의 제품 기록(구조 검사), 확인 카드, 설명 전용 진입·종료 snapshot·종료 뒤 예산 변경·후속 Case, 스키마 v20. **QG-02~07 자동 실행·검증 1회 중단·D-86 은 넣지 않았다**(plan 3.9절) | 기존 게이트·repair·예약·완료 계약·Autonomy·예산 강제 보존, 자동 사람 인수 없음, 예외가 권한을 만들지 않음, 설명 소비 같은 Case 누적. [실행 결과](p4/evidence/P4-05-results.md) |
| ~~P4-05b 진행 상한 설정~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-05b-results.md). 재작성·재시도 상한을 시스템 기본값(환경 변수) + Case 별 이력 조정으로. 상한 대기 카드의 "한도를 올리고 계속", 관리 화면 정책 패널 표시·변경. Project 층은 UI-04 (사용자 결정 2026-09-24, 1.8절) | 우선순위·이력, 올린 뒤 한 번 더 가고 다시 멈춤, 0 의 뜻, 종료 Case 거부, v20 → v21 이행 |
| ~~P4-06b 지식 원문 서버 저장~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-06b-results.md). 사용자 결정(D-67 보충)으로 지식 적용 내용과 자동 등록 규칙의 권위 사용자 메시지 한 건을 서버 DB(`knowledge_body`, 유일한 본문 표)에 저장해 어느 PC 의 실행에도 배정으로 싣고, Runner 는 해시가 맞을 때만 씀. 수동 등록은 중계 없이 저장, 옛 지식은 소유 PC 연결 시 이행, 열람은 서버가 채움, 화면·카드·등록 규칙의 서버 저장 안내. 다른 원문 경계는 그대로, 스키마 v23 | 두 PC 시험 — 다른 PC·소유 PC 미연결에서 필수 주입·영수증 `read`, 해시 불일치는 읽지 않음, 표식은 `knowledge_body` 에만·로그 없음, 이행 v22 → v23. 실제 codex 두 Runner 라이브 |
| ~~P4-06 지식 관리·적용~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-06-results.md). 원본/짧은 적용 내용/조건/필수·참고/버전·상태, Runner 보관, 활동+범위 자동 주입·Manifest, 대화 규칙 자동 활성 등록(사용자 결정), 스키마 v21·v22 | 기존 확정 지식 수동/자동 등록 경로로 검증. 규칙의 권위 승계, scope 확대 재적용, 필수 내용 실제 제공, 원문 불가/충돌 의존 작업 보류, 주입과 준수 구분 |
| ~~P4-07 선택적 지식 추출~~ | **완료(2026-09-24).** [실행 결과](p4/evidence/P4-07-results.md). 검증·분석·실험·구현 실행이 결과와 함께 남기는 후보(AI 제안, 최대 3건·근거 한 줄), 논의 응답의 제안 표지(`proposal: true`), 같은 내용·`supports` → 기존 항목의 근거 행, `supersedes`·`contradicts` → 관계를 가진 새 후보(대상의 버전 사슬 그대로), 관측 문맥(실행·저장소·기준 커밋·도구), QG-08 채택 확인(`adoption_check`)과 사람의 활성화(범위 축소·다른 항목의 새 버전으로), 후보·제안·근거 카드, 관리 화면의 채택 확인·활성화, 스키마 v24 | AI 새 일반화가 강제규칙이 되지 않음(DB CHECK 그대로). 아무 후보 없는 Case 정상완료. 별도 추출 실행 없음(발생 비용은 그 실행의 것). 다른 Case 실행에 후보(보조)·활성화 버전(효력대로) 주입. **참고 후보 자동 활성화·독립 AI 검토는 없음**(전자는 사용자 결정 2026-09-24: 반자동 — P4-07b) |
| ~~P4-07b 참고 후보의 자동 활성 조건 표시~~ | **완료(2026-09-24, 사용자 결정 S-028: 반자동).** [실행 결과](p4/evidence/P4-07b-results.md). 시스템이 조건 여덟(후보·효력 참고·종류 운영 사실/알려진 문제·서로 다른 실행 둘 이상의 근거·그중 종료 코드 0 명령의 검증/분석 실행·관측 저장소 범위·반증/충돌 없음·채택 확인 막음 없음)의 충족을 계산해 규칙 화면·카드·관리 패널에 표시하고, 사람이 규칙 화면에서 한 번에 활성화한다(`activate-ready`). 스키마 v24 그대로 | 클릭 없는 활성화 없음, 필수 승격 없음, 조건 미충족 사유 표시, 한 번에 활성화가 각 후보의 채택 확인을 그대로 지남(거부·미충족은 건너뛰고 적음), 활성 참고가 다음 실행에 참고로 주입 — 전부 시험으로 고정. 논의 응답의 제안은 관측이 없어 조건 (4) 불가(사람이 폼에서 활성화) |

**P4 완료 조건:** 여섯 Profile 대표·혼합 목적·실패/복구/변경을 재현하고 Case 간 최소 지식 관리·추출·주입을 검증한다. 지식은 첫 버전 범위이며 FR-19/QG-08을 일괄 후속 확장으로 제외하지 않는다. QG-08은 모든 후보의 별도 AI 실행·사람 승인을 의무화하는 절차가 아니다. 범용 검색·외부 지식 시스템과 공동 사용 UI는 범위 밖이다.

## 7. P5 — GitHub·외부 결과

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| P5-01 연결·읽기 | 대표 Repo의 Git remote 후보·기존 인증/접근 확인, 선택 연결, 업무 이슈 위치/참여 Repo, 원래 이슈·PR·CI | 읽기 출처/버전 연결, 비밀정보 비기록, 모호한 remote 선택, 연결 유예와 연결≠게시 허용, 읽지 못한 CI 증거 표시 |
| P5-02 기록 게시 | 논의 단계에는 이슈 없음. 업무화 뒤 연결·허용 안에서 지정 Repo의 Case당 새 이슈, 요약 append·Outbox | 참여 Repo별 이슈 복제 없음, 코드/diff/상세 로그 제외, 코드·기록 겸용 가능하되 별도 코드 권한 필요, 응답유실 중복 방지 |
| P5-03 범위 허용 push·PR | Case 시작 Grant, 정확한 대상/payload 직전 검사, 저장소별 외부 결과 | 범위 내 반복승인 없음, 미허용 Repo/ref/행동 차단, 부분 실패·결과 불명 대조, 같은 원격 ref/PR 경합 검증 |
| P5-04 controlled·CI·리뷰 | 결과 확인→자동게시→필수CI→closure, PR 피드백과 stale 검사 | CI 수정으로 후보내용 변경시 재확인. 동일내용 재전송 반복확인 없음. 상대 Repo 준비 대기와 코드결함 구분. 완료후 수정 새 Case, 일반 이슈댓글 승인/명령 해석 금지 |

실제 외부 쓰기 시험은 시험 대상·범위를 별도로 허용받은 경우만 수행한다. 이 제품의 자동 게시 설계를 개발한다는 지시가 이 개발 저장소의 push 권한은 아니다. 실제 시험을 못 하면 계약시험과 미검증을 구분한다. 강제 push·병합·배포·이슈 종료·CI 수동 재실행·GitLab은 범위 밖이다.

## 8. P6 — 원격·운영·전체 수용

| 하위 작업 | 범위·검증 |
|---|---|
| P6-01 원격·인증 | 소유자/Runner 등록·폐기, LAN/사용자 VPN 암호화, 미등록/권한 회수 거부. 한 Case를 여러 PC에 분산하지 않음 |
| P6-02 데이터 경계 | 상세 문서·대화·지식·코드·증거가 소유 Runner에 남고 제어부 DB/로그/큐/캐시에 영구 복제되지 않음. 과거 지식 소유 PC 오프라인 시 가용성 표시 |
| P6-03 단절·복구 | 실제 CLI/모드별 도구 경계 정지·다음 호출 차단, 지연 결과·부분 외부 효과·예산 예약 재조정. 종료불명 중복쓰기 금지 |
| P6-04 백업·설치 | 호스트별 하루 1회·최근 7개·원본 자동삭제 없음, 부분복원/미푸시·미커밋 보존. Python/Git 준비 후 일반 프로세스 설치·업그레이드·빌드UI |
| P6-05 규모·최종 수용 | 10 Project·3 Runner·동시 AI 2·Runner쓰기 기본 1에서 실제 측정. 전체 FR/NFR/[수용 시나리오](review-acceptance-matrix.md)와 증거 연결. 지표 미제공·한도 강제불가·미실증을 정직하게 표시 |

세 Windows 배포 형태를 확인한다. 모의 프로세스와 실제 호스트·외부 서비스 시험을 구분한다. OpenCode 등 미검증 제한은 보존하고 필수 기준을 임의 면제하지 않는다. Linux/WSL·공동 사용·복수Project Case·분산Case·인터넷 직접 공개는 이후 범위다.

## 9. 이어받는 한계와 처리 위치

| 현재 구현/관측의 한계 | 후속 처리 |
|---|---|
| **가벼운 확인은 원문의 의미를 보지 않는다** | `conformance_check.unverified_scope` 에 그대로 남는다. **P4-01이 해소한 것은 선택·강제 경계**다 — 독립 검토가 필요한 조건에서는 가벼운 확인으로 통과하지 않고 Case 설정으로 낮출 수 없으며, 별도 reviewer 세션과 고정 참조를 요구한다. 가벼운 확인 자체가 의미 검토로 승격되지는 않는다 |
| **delta 분류가 본문을 읽지 않는다** | 제어부는 구조화된 사실만 보고 동의된 항목의 AI 출처 변경을 전부 `material` 로 본다. **P4-02가 정한 것은 그 다음 질문이다** — 변경이 어느 판정을 내리고 어느 증거를 재사용하는지를 `change_impact` 가 **참조 대조로만** 정하고, 범위를 모르면 `unknown` 으로 재검증한다. 분류기를 의미 판정기로 바꾸지는 않았다 |
| **Fast Lane 판정은 수준 판단에 의존한다** | 수준은 AI가 축별 근거로 제안하고 사람이 조정한다(P3-01). P4-01이 설계·계획 품질 게이트와 조건부 독립 검토를 붙일 수 있게 했어도 수준 판단 자체의 진위를 자동 판정하지 않는다. **P4-03에서 본 것:** 조사 Profile(RCA·research)의 대표 흐름은 분석·실험 실행만 쓰고 준비 산출물을 요구하지 않으므로 Fast Lane 판정이 끼어들 자리가 없다. Fast Lane은 제품 변경 Profile의 준비 양을 정하는 판정이며 P4-03은 그것을 바꾸지 않았다 |
| **실험 표시는 배정 목적에서 나온다** | `run.is_experiment` 는 요청이 주장하지 못하고 목적에서 도출된다. **P4-03이 두 가지를 더했다** — 실험 실행은 조사 결론(`investigated`)의 근거로만 쓰이고 제품 의무의 근거가 되지 않으며, 실험이 작업공간에 **임시 변경을 남겼는지**를 실행 전후 트리 지문으로 도출한다(`left_changes` 등). 제품 목적 Case의 잔여는 자동 완료를 막는다. **관측은 여전히 격리가 아니다** — "되돌렸다"는 관측한 트리가 같다는 뜻이고, 작업공간 밖 변경은 감지만 한다(D-44) |
| **시간 예산의 hard 한도는 절대 상한이 아니다** | 돌고 있는 CLI 를 초 단위로 끊을 수 없다(P1-03 `cancel_confirmed = unknown`). `enforced_no_absolute_cap` 으로 표시하고 실제 초과를 값·원인과 함께 기록한다. **UI-02가 사람의 중단으로 트리를 끝내고 확인하는 능력을 만들었다**(codex 실증). 한도 도달에 자동 중단을 연결하지는 않았다 — 한도 강제의 의미를 바꾸는 결정이다(D-88). 여전히 절대 상한이 아니며 전체 복구는 **P6-03**. D-88 지표 선택도 이 한계를 없애지 않는다 |
| **검증의 취소 요청과 실제 종료는 여전히 다른 값이다** | P4-02는 `stop_requested_at` 만 적고 상태를 `completed` 로 바꾸지 않는다. 요청 뒤 도착한 결과는 원래 실행에 저장되고 `late_result` 로 남는다. **실제로 언제 멈췄는지는 모르는 그대로**다. **UI-02가 실행 단위의 실제 종료·잔류 확인을 만들었지만 검증 1회에는 연결하지 않았다** — 검증 1회(`quality_gate_run`)와 reviewer 실행이 결과 기록 때에야 연결돼(`reviewer_run_id`) 실행 중에 끝낼 프로세스를 모른다. 연결에는 P4-02 모델 변경이 필요하다(검증 흐름을 다루는 작업의 별도 plan). P6-03 전체 복구 수용 유지 |
| **권한 철회 기계 자체는 만들지 않았다** | P4-02는 권한 변경을 변경 사건으로 받아 영향받는 증거만 재검증 대상으로 만들고, 그것이 게이트 예약과 달리 검증 종료를 기다리지 않으며 기존 판정·외부 효과를 되돌리지 않는다는 경계를 시험으로 고정했다. 실제 철회·중지 제어의 실증은 **P6-03**, 외부 효과 대조는 **P5** |
| **토큰·비용은 어댑터가 줄 때만 안다** | 미제공은 `unavailable` 이며 0 이 아니다. codex 는 비용을 주지 않아 `estimated_cost.complete = false` 다. 실제 청구액 대조는 범위 밖이며 추정 지표의 hard 한도는 계속 설정을 받지 않는다 |
| **CLI 내부 모델 호출 수는 관측 범위 밖이다** | `run_count` 는 **시스템이 시작한 호출**이다. 그 계약을 `reservation_contract` 에 값으로 남겼고 다른 것으로 표시하지 않는다 |
| **예약은 한 제어부 DB 의 트랜잭션으로만 보장된다** | 여러 제어부·원격 환경의 예약 조정은 **P6-03**. 지금 보장 범위를 분산 환경으로 넓혀 표시하지 않는다 |
| **게시 허용(`publish_allowed`)은 여전히 기록뿐이다** | 네 축이 강제로 바뀌었다고 게시까지 강제되는 것으로 읽지 않는다. controlled 의 결과 후보 확인도 push·게시 권한을 만들지 않는다(D-65). 실제 push·PR·기록 이슈와 외부 CI 대기는 P5다 |
| **한 실행은 작업공간 하나에서 돈다** | **P3-04에서 실제로 돌려 봤고 바꾸지 않았다.** 계획이 Task 마다 저장소를 정하고(v12) 각 실행이 그 worktree 에서 돌면 두 저장소 기능이 실제로 완성된다 — 계약을 계획 산출물이 고정하기 때문이다. 한 실행이 여러 작업공간을 보게 하는 것은 **필요하지 않았다.** 다만 이 시험의 계약이 단순했다는 것도 사실이며, 드러나지 않은 것을 "문제없음"으로 적지 않는다 |
| **검증 실행이 이 호스트에서 시험을 돌리지 못한다** | CLI 안에서 `python` 도 `py` 도 찾지 못한다(P3-03·P3-04 가 같은 것을 관측했다). 그래서 `met` 판정의 라이브 증거가 없고 기준이 `unverified` 로 남는다. **자동 완료 Case 는 그 상태에서 닫히지 않았고**(경로 A), **controlled Case 는 사람이 예외로 수용해 `closed_with_exceptions` 로 닫혔다**(경로 B) — 둘 다 맞는 동작이며 어느 쪽도 검증된 완료가 아니다. **절대 경로 인터프리터는 시도하지 않았다** — "불가"의 범위는 관측한 만큼이다. 실제 시험 실행은 환경을 갖춘 뒤에 본다 |
| **근거의 진위는 여전히 시스템이 판정하지 않는다** | P4-01은 요청/기준/대상/증거 **참조**와 독립 reviewer를 요구하고 작성자의 완료 주장만으로 일반 게이트를 통과시키지 않는다. 그러나 참조된 명령·종료 코드가 실제인지까지 검증하지는 않는다. 앞선 거짓 `met` 기록은 소급 승격하지 않으며 이 한계를 숨기지 않는다 |
| **조합의 변화는 관측으로만 안다** | 제어부는 git 을 보지 않는다. 실행이 없는 저장소의 항목은 `snapshot_incomplete` 이며 그것은 "모른다"이지 "안 바뀌었다"가 아니다 |
| **`run.repository_id = NULL` 인 옛 실행** | 작업공간이 하나뿐이면 그것으로 해석하고, 저장소가 늘어나면 어느 항목에도 붙지 않는다. 화면이 `unattributed_run_effects` 로 드러내며 지어내지 않는다 |
| Project·Task 단위 정책 조정 없음, Profile 재분류 경로 없음 | Project 기본값과 설정 출처는 UI 작업에서 보완한다. **D-86은 미완료 같은 문제를 같은 Case에서 의도/Profile 개정한다는 제품 방향을 확정했다.** UI-04에서 과거 항목·기준·증거·소비·repair 보존과 새 필드 대응을 구현한다. P4-03이 미룬 이유는 다음 의도의 항목 집합 변경으로 기존 기준의 `relates_to`가 대상을 잃기 때문이다. 기존 혼합 목적 완료 계약만으로 이행·쓰기 허용이 구현된 것은 아니다 |
| `project.repo_path` 와 `project_repository` 에 같은 값이 두 곳에 있음 | **R2에서 정본을 `project_repository` 로 옮겼다.** 컬럼은 지우지 않았고 조회가 불일치를 드러낸다(`legacy_repo_path_matches_registry`). 컬럼 제거는 이후 범위 |
| 실제 변경이 없으면 구현 실행 실패 | **R4가 기준 판정 쪽을 나눴다** — `satisfaction` 으로 무변경 충족(`already_satisfied`, 검증 실행 증거 필요)과 미재현(`not_reproduced`, `met` 불가)을 구분한다. **실행 자체의 판정은 그대로다** — 변경도 명령도 없는 구현 실행은 여전히 실패다. **P4-03이 정한 것:** 무변경 완료는 구현 실행의 성공이 아니라 **검증 실행의 관측**(`already_satisfied`·`preserved` 는 검증 실행만 근거)이고, 조사 Profile의 무코드 완료는 분석·실험 실행의 근거(`investigated`)다. 그래서 구현 실행의 판정을 바꿀 필요가 없었다 |
| 명령·종료 코드가 AI 자기보고 | **P4-01에서도 남는다.** 일반 게이트는 증거 참조와 reviewer 분리를 강제하지만 외부 실행 사실을 새로 증명하지 않는다. 자기보고를 검증된 실행으로 소급 승격하지 않으며 실제 실행 확인 경로 없음을 판정과 함께 보인다 |
| Task·기준 연결 적합성, 질문 blocks 자유문자열/불명 범위 전체차단 | **P4-01이 부분 해소했고 P4-02가 변경 뒤 유효성을 붙였다.** required 발견은 전달된 `criteria_refs` 에 연결돼야 하고, 연결되지 않은 의견은 advisory다. 요청·코드·권한이 바뀌면 그 참조를 가진 검사만 `needs_recheck` 가 되고 나머지는 이유·참조 버전과 함께 재사용된다. 다만 **참조 관계의 의미 진위**까지 제어부가 읽지는 않으며 불명 관계는 계속 안전한 쪽으로 막는다 |
| 작업공간 밖 변경은 감지만 하고 막지 못함, 원래 Repo 밖은 미관측 | 기존 capability/한계 보존. worktree 는 파일 배치의 분리이며 OS 격리가 아니다(D-44). **R4의 로컬 실험도 같다** — 쓰기를 여는 조건은 늘렸지만 통제 능력은 그대로이며 격리 보장으로 표시하지 않는다 |
| 기준 커밋 기록이 없을 때 P3-03은 merge-base/HEAD 복구 | 복구 경로는 그대로다. **R2의 조합이 `snapshot_incomplete` 로** "기준 커밋만으로는 실제 입력을 설명할 수 없다"를 표시한다. 현재 HEAD를 원래 검증 기준으로 조용히 승격하지 않음 |
| **예산 도달의 기능 흐름 라이브가 없다** | R3 의 예약·정산·정지는 자동 시험으로 고정돼 있고 hard 도달의 거부도 그 시험이 확인한다. **기능이 진행되는 도중에 걸리는 모습**은 P3-04 의 경로 C 이며 **사용자 지시로 스킵**했다(2026-09-22). 예산 라이브가 필요해지는 작업에서 `p3/live/path_bc.py --path c` 로 다시 본다 |
| **사람의 예외 수용이 종료를 연다** | 미검증 기준이 남아도 사람이 그것을 수용하면 Case 가 닫힌다(`closed_with_exceptions`). 원래 판정은 보존되고 자동 완료는 스스로 수용하지 못한다. **P4-05가 그 자리를 새 화면의 예외 카드(기준 선택·범위 입력·인수)로 옮겼다.** 예외의 **적절성** 판정은 여전히 없다 — 무엇을 수용했는지가 값으로 남는다 |
| 잔류 활동 확인의 범위, 안전중지 제품경로 미연결 | **UI-02가 잔류를 확인한다** — CLI 가 job 안에서 돌고 확인 근거(`job_empty`·`job_terminated`·`job_closed_kill_on_close`·`host_rebooted`)가 있을 때만 `none` 이다. 확인되면 시간 지표도 확정된다. **비 Windows·시작 기록 없는 옛 실행은 여전히 `unknown`** 이다. P1-03의 훅 기반 안전 경계 정지(`safe_stop_next_call`)는 제품 경로에 연결하지 않았다(사람의 중단과 구분) — P6-03. R4의 자동 완료는 미정리 실행(끝났는지 모르는 중단 실행 포함)이 남으면 종료를 확정하지 않는다 |
| **종료 Case 는 설명 전용 진입만 연다**(P4-05) | 요청에 묶인 읽기 전용 논의 응답만 진입 검사가 허용하고 다른 모든 Run·정책 변경은 `case_already_closed` 그대로다. 종료 시점 소비·한도 snapshot(`closure_record.snapshot_json`)과 종료 뒤 소비 구분, 예산 한도 변경 이력이 있다. **v20 이전에 종료된 Case 는 snapshot 이 없어** 종료 뒤 소비를 나누지 못한다(`since_closure = null`) — 지어내지 않는다 |
| 시간 계측과 UI 기본 지표의 차이 | 현재 실행시간은 `assigned_at`~`finished_at`, 경과시간은 `Case.created_at`~조회 시각이다. **P4-05가 종료 시 소비 snapshot 을 만들었다.** D-88 의 실행시간 합계(Run 내부 사람 대기 제외) 계측은 남아 있다(UI-04 상세 설정과 함께). 기존 지표·한도·미확정 정산을 조용히 바꾸지 않는다 |
| ~~크기 한도 미구현~~, 열람 요청 만료·원문 PC 경계 | **P4-04가 크기 쪽을 구현했다** — 실행당 인라인 한도, 보조만 드러내어 생략, 핵심 초과·핵심 원문 미확인은 보류, Runner 영수증의 해시 대조. **P4-06b 가 지식 원문만 서버로 옮겼다**(사용자 결정) — 그 열람은 PC 와 무관하다. 열람 요청 만료와 지식 외 원문 PC 경계 전체는 **P6-02** |
| **한도는 바이트이고 제안값이다**(P4-04) | 인라인 한도 기본 256 KiB 는 상세 설계 제안값이며 토큰이 아니라 `artifact_ref.byte_size` 다. CLI 가 작업공간에서 더 읽는 것과 CLI 내부 압축은 여전히 모른다(`context_bytes` 계약 그대로). 설정은 제어부 환경 변수뿐이고 Case·Project 별 조정·화면은 없다(UI-04 상세 설정) |
| **분할 검토·검토 체크포인트·단계적 조회가 없다**(P4-04) | 핵심이 한도를 넘으면 **보류**로 드러낼 뿐 나눠서 검토하지 않는다. 지금 고정 문맥은 의도·준비 문서와 대화라 나눌 대상이 거의 없고, 큰 대상(diff)은 CLI 가 작업공간에서 읽는다. 하위 검토를 게이트 판정에 합치려면 P4-01/02 판정·repair 모델을 함께 바꿔야 한다 — 실제로 큰 대상 검토가 필요한 작업에서 별도 plan. 생략한 보조 자료를 CLI 가 스스로 읽는 경로는 CLI·권한 모드별 실증이 필요하다 |
| **Runner 가 본 원문 누락을 제어부가 가용성으로 되먹이지 않는다**(P4-04) | 영수증의 `missing`·`hash_mismatch` 는 그 실행의 기록이고 `artifact_ref.availability` 를 바꾸지 않는다. 같은 누락은 다음 실행마다 영수증으로 다시 드러나고 소비 0 으로 끝난다. 원문 목록 재동기화·백업 복원 대조는 **P6-04** |
| **사람의 재배정은 원장이 재실행을 막아도 새 세대 예약을 잡는다**(P3-R3 규칙, P4-04에서 확인) | **UI-02가 재시작 경로를 고쳤다** — 재기동한 Runner 는 세대를 올리지 않고 원장으로 대조하므로 새 예약이 없다. 사람이 쓰는 재배정(`POST /api/runs/{id}/reassign`)은 그대로 새 세대 예약을 잡는다(과대 쪽). 그 정산은 **P6-03** |
| **최신성은 표시다**(P4-04) | 고정 뒤 새 입력이 있어도 결과·판정·채택을 바꾸지 않는다. 게이트 판정의 재검증은 P4-02 의 변경 사건·참조 대조가 맡는다. **P4-05 의 진행기는 최신성을 조건으로 쓰지 않는다** — 새 의도 버전은 준비·그래프의 `stale` 로 잡히고, 결과 채택 시 최신 입력 대조는 여전히 표시다 |
| **요청 `unknown` 은 확인 근거가 있어야만 풀린다**(UI-01 → UI-02) | UI-02가 해제 경로를 만들었다 — 확인되지 않은 실행을 Runner 가 다시 확인해 근거가 오면 `interrupted` 로 풀린다. **확인하지 못하면 계속 잠겨 있을 수 있다** — 사람이 확인을 선언하는 경로는 만들지 않았다(D-76). 비 Windows·옛 실행은 재부팅 근거로만 풀린다. 그 동안 다른 대화는 쓸 수 있다 |
| **배정은 원문 소유 PC 를 보지 않는다**(S-026 발견, P4-06b 라이브 둘째 시도) | `claim_assignments` 는 대기 실행을 **먼저 폴링한 Runner** 에게 준다. Runner 가 둘이면 PC A 의 대화 응답을 PC B 가 맡아 지시 원문(A 소유)을 읽지 못해 `required_context_unavailable` 로 실패한다 — 도구 능력도 보지 않는다. 지금까지 모든 시험·라이브가 한 Runner 였다(두 PC 시험·라이브는 Runner 를 차례로 돌린다). "한 Case 를 여러 PC 에 분산하지 않는다"(P6-01)와 같은 자리이며 **P4-06b 는 배정 경로를 바꾸지 않았다** — 지식 원문은 어느 PC 에서도 읽히지만 지시·대화 원문은 여전히 소유 PC 만 읽는다. 원문 소유 PC·도구 능력으로 배정하는 규칙은 P6-01 의 plan 에서 정한다(제품 판단: 소유 PC 가 미연결이면 기다리는가, 사람에게 알리는가). **사용자가 S-026 에 위임 → 판단: P6-01 에 둔다.** 지금 운영은 한 제어부·한 Runner 이고(모든 시험·라이브가 그렇다), 미연결 시의 대기/알림은 Runner 등록·인증 설계와 함께 정해야 한다. 그때까지의 운영 지침: **한 제어부에 Runner 하나**(둘을 쓰려면 차례로) |
| **PC 연결 판정은 heartbeat 공백 하나다**(UI-02) | 미연결(기본 15초, 상세 설계 제안값·제어부 환경 변수)이면 사용자 입력이 409 `runner_disconnected` 다. 한 제어부·한 PC 기준이며 네트워크 분할·원격 PC 인증은 **P6-01·P6-03**. 원문 열람 요청은 서버가 미연결이어도 기존대로 대기시킨다. **UI-03 화면은 미연결 PC 로 새 열람 요청을 만들지 않고 "연결 필요"를 보인다**(다른 호출자는 여전히 대기시킬 수 있다) |
| **정상 종료 실행의 잔류 `unknown` 은 요청을 잠그지 않는다**(UI-02) | UI-01 규칙을 소급하지 않으려고 유지했다(지금까지의 모든 실제 CLI 실행의 값). 새 어댑터는 정상 종료에서도 확인해 `none` 을 보고하므로 새 실행에서는 드물다. 비 Windows·확인 실패의 정상 종료 실행은 잔류가 남았을 수 있는데 잠그지 않는다 — 트리 제어를 넓히는 작업에서 다시 본다 |
| **정상 종료 뒤 남은 프로세스를 끝낸다**(UI-02) | 실행의 경계를 CLI 호출 하나로 보고 루트가 끝난 뒤 남은 프로세스를 그 실행의 잔류로 종료·계수한다(P1-03 (가)). AI 가 일부러 띄운 백그라운드 프로세스(개발 서버 등)도 끝난다. 결과에 종료한 수가 드러난다. 이 기본이 맞지 않는 업무가 생기면 제품 판단으로 다시 묻는다 |
| **claude 실제 중단 미실증·비 Windows 트리 제어 없음**(UI-02) | 같은 OS 수단이지만 claude 는 실제로 중단해 보지 않아 `cancel_confirmed = unknown` 이다(D-76 "CLI별 실증"). 비 Windows 는 트리 제어를 지원하지 않는다고 보고한다(`process_tree_control = unsupported`) — Linux/WSL 은 이후 범위 |
| **P4-02 시험 하나가 시계 해상도로 흔들린다**(S-022 관찰) | `test_quality_changes.py::test_the_reservation_lands_when_the_verification_ends_even_on_a_failure` 가 예약 요청 시각과 반영 시각이 **다르다**고 단언한다. 이 PC 의 Python 3.12 벽시계는 `GetSystemTimeAsFileTime`(해상도 15.6ms)이라 두 요청이 한 틱 안에 끝나면 같은 값이 된다. UI-02 를 적용하지 않은 HEAD 워크트리에서도 3회 중 2회 실패해 **이전부터 있던 흔들림**임을 확인했다. 제품 규칙(요청→반영 순서)의 문제가 아니라 시험의 가정이다. UI-02 범위 밖이라 고치지 않았다 — 순서(`>=`)로 바꿀지는 그 영역을 다루는 작업에서 정한다 |
| **요청 처리는 논의 응답과 업무 단계 진행이다**(UI-01 → UI-03 → P4-05) | UI-03 의 제어부 요청 처리기가 저장된 요청마다 **읽기 전용 논의 응답 하나**를 만들고 그 결과로 요청을 끝낸다(기동 복구, `HADS_AUTO_PROCESS_REQUESTS=0` 이면 UI-01 명시 계약). **P4-05 부터 업무 단계의 다음 작업은 진행기가 잇는다**(`HADS_AUTO_PROGRESS_WORK`, 처리기가 켜져 있을 때). 업무 단계의 사용자 메시지는 여전히 읽기 전용 응답만 받고 해석하지 않는다(D-86 은 UI-04). 처리기가 켜진 제어부에서 관리 화면의 "논의 응답 실행"·"처리 완료 기록"은 이미 처리된 요청에 거부로 답하고, 관리 화면의 실행 버튼은 진행기가 만든 실행과 겹칠 수 있다(진행기는 끝나지 않은 실행이 있으면 기다린다) |
| **AI 해석 업무화는 되돌릴 경로가 없다**(UI-01 → UI-03) | 준비 단계 응답의 AI 해석이 명확한 업무 요청이면 같은 Case 가 업무화된다(D-69). 논의를 업무로 잘못 읽으면 준비 단계로 돌아가는 경로가 없고 Profile 변경은 D-86(UI-04)이다. 그래서 규칙을 좁게 뒀다(명시적 작업 요청만, 모호하면 논의로 두고 글로 묻는다). 실제 codex 라이브에서 논의 1건은 논의로, 분석 요청 1건은 `research` 업무 요청으로 읽었다 — 표본은 회차당 둘이다. 해석은 권한이 아니며 업무화는 실행을 시작하지 않는다 |
| **기존 접수 네 경로의 멱등 키가 없다**(UI-01 → UI-03) | UI-03 이 `/artifacts`·`/intent-drafts`·`/feedback`·`/questions/{id}/answer` 도 **기록 전에 중계**하게 바꿔 커밋-중계 경합은 없어졌다. 멱등 키는 여전히 없다(새 화면의 사용자 입력은 메시지 경로를 쓴다). 접수는 기록됐는데 뒤의 기록(의도 버전·피드백)이 거부되는 짧은 창에서는 원문이 PC 에 저장될 수 있다 — 가리키는 기록이 없는 원문이며, 이전에는 같은 경우 유실로 표시됐다 |
| **요약이 본문 발췌인지 서버가 판별하지 못한다** | 메시지를 포함한 모든 `summary` 는 호출자 규칙이다. UI-01 화면·하네스는 본문이 아닌 표시(`사용자 메시지 · N자`)를 쓴다. D-84 대화 검색의 서버 측 요약을 정할 때(**UI-04**) 다시 본다 |
| **의도 초안의 출처 표기가 Profile 지시를 사용자 요구로 적었다**(UI-01 라이브 관찰) | defect-fix 초안의 "미재현만으로 해결을 선언하지 않는다"가 `user_requirement` 로 적혔다(두 회차 모두). 논의의 금지 자체는 올바르게 `user_requirement` 였다. QG-01 의 `assumption_presented_as_requirement` 가 잡을 자리이며 이 라이브는 QG-01 을 돌리지 않았다. 의도 지시문을 다루는 후속 작업에서 본다 |

| **대화 본문은 탭 메모리에만 있고 새 탭마다 PC 에서 다시 불러온다**(UI-03) | 브라우저 저장소에 본문을 쓰지 않는다는 선택의 대가다. 긴 대화는 열람 요청이 많고(동시 3개), PC 가 미연결이면 새 탭에서는 본문을 못 본다("연결 필요"). 대화 검색(D-84)·본문 색인은 UI-04 |
| **화면 갱신은 조회 반복이다**(UI-03) | 처리 중 1.5초·평소 5초. 목록은 열린 대화의 요청·단계가 바뀌면 바로 다시 묻는다. 푸시·알림(D-82)은 UI-04 |
| **AI 응답·의도 원문은 글 그대로 보인다**(UI-03) | codex 는 Markdown(표·굵게·링크)으로 답하고 링크에 작업공간 절대 경로가 들어간다. 화면은 렌더링하지 않고 글 그대로 보인다(삽입 위험이 없다). 의도 원문은 정규 문서(JSON)를 정리해 보일 뿐 항목별 보기는 없다. 읽기 좋은 표시는 UI-04 에서 본다 |
| **제어부 DB 접근은 문장 단위로 직렬화된다**(UI-03) | 공유 연결의 동시 사용 결함(P2 부터, 새 화면이 드러냄)을 고치면서 모든 문장이 한 프로세스 잠금 안에서 돈다. 한 사용자·한 PC 의 부하 재현(25초에 9,000여 건 생성·조회)에서 0 오류였다. 큰 규모·여러 제어부는 P6-05·P6-03 |
| **브라우저 시험은 설치된 Edge 에 의존한다**(UI-03) | `tests/test_web_shell.py` 는 Playwright 로 설치된 Edge 를 연다(브라우저를 내려받지 않는다). Edge·playwright 가 없거나 `web/dist` 가 소스보다 오래됐으면 건너뛰고 사유를 보인다 |
| **권한 확대 카드·상세 설정은 새 화면에 없다**(UI-03 → P4-05) | P4-05가 의도 동의·controlled 시작/결과·material delta·단계 검토·피드백·예외 수용 카드를 새 화면에 옮겼다. 권한 확대(게시·push 허용)는 P5, 상세 설정·수준 조정·저장소 선택·QG-02~07 은 관리 화면(UI-04 의 남은 하위 plan — 04b 제안). **UI-04a 가 지식 등록부·후보·채택 확인·활성화를 새 화면(프로젝트 규칙)으로 옮겼다** |
| **기준 판정은 검증·분석 실행의 보고다**(P4-05) | 제품이 `policy:work_progressor` 로 적는 기준 판정은 AI 가 보고한 것을 구조 검사(완료 실행·종료 코드 0 명령·`verifies` 연결·의무별 방식·결론 요구)로 거른 것이다. 명령·종료 코드의 진위는 여전히 자기보고이며(위 행), 검증 Task 가 없는 기준은 `unverified` 로 남아 예외 수용으로만 닫힌다. 규칙의 범위는 사용자가 확인했다(2026-09-24, 10절 S-024) |
| **QG-02~07 은 진행기가 돌리지 않는다**(P4-05) | 명시 ON 게이트가 막으면 `quality_gate` 사람 대기로 보이고 관리 화면에서 진행한다. 검증 1회 모델(P4-01·02)을 진행기에 잇는 것은 별도 plan |
| **재작성·재시도 상한은 설정이다**(P4-05 → P4-05b) | 기본 재작성 2·재시도 1, 환경 변수 기본값 + Case 별 이력. **상한 뒤 "계속 진행" 만으로는 한 번 더 가지 않는다** — P4-05 가 "한 번 더 시도한다" 고 적은 것은 실제 동작과 달랐다(S-025 에 시험과 함께 바로잡음). 한 번 더 가는 길은 한도를 올리는 것이다. Project 층·새 화면 상세 설정은 UI-04. QG-02~07 의 게이트별 수정 한도(P4-01)는 다른 설정이다 |
| **진행 요청의 중단 뒤 재개는 사람이 한다**(P4-05) | 진행 요청이 `interrupted`·`unknown` 으로 끝나면 진행은 `paused` 이고 스스로 재개하지 않는다(D-76). 기동 복구도 요청이 끝나 있으면 멈춤으로 표시한다. 계속 진행은 새 진행 요청(`system_resume`)을 연다 |
| **옮겨 적은 규칙의 충실성은 판정하지 않는다**(P4-06, 사용자 결정의 알려진 위험) | 대화의 규칙은 AI 가 옮긴 글로 바로 활성 필수가 된다. 주입할 때 사용자 말(권위 원문)을 함께 넣고 카드·관리 화면에서 무효·개정할 수 있을 뿐이다. 실제 codex 라이브에서는 글자 그대로 옮겼지만 "이 프로젝트에서는" 을 **저장소 범위**로 좁혀 적었다(저장소가 하나라 적용은 같았다). AI 가 범위를 넓히거나 좁혀 옮길 수 있다 |
| **주입은 준수가 아니다**(P4-06) | 필수 규칙을 제공해도 지켰는지는 기존 기준 판정·검토가 본다. 규칙 준수를 따로 보는 게이트는 없다. 라이브의 구현은 규칙(한 줄 docstring)을 따랐지만 그것은 AI 의 행동이며 시스템이 강제·판정한 것이 아니다 |
| **지식 적용 범위의 한계**(P4-06) | 경로 조건은 조건으로 줄 뿐 실행이 다룰 경로를 미리 알지 못한다(`paths_unresolved`). 실행 중 범위가 넓어져도 그 실행에 다시 넣지 못한다. 논의 응답에는 `discussion` 활동을 적은 항목만 간다. 충돌은 사람이 기록한다(자동 탐지 없음). ~~다른 PC 의 실행은 원문을 읽지 못하면 필수면 보류다~~ — **P4-06b 가 해소했다**(서버 본문을 배정에 싣는다; 아래 행). 자동 등록은 요청 처리기가 켜져 있을 때만 |
| **지식 본문은 서버에 있고 비밀값은 검사하지 않는다**(P4-06b, 사용자 결정) | `knowledge_body` 는 유일한 본문 표이며 지식 원문과 권위 사용자 메시지 한 건만 담는다. 화면·카드·등록 규칙이 "비밀값을 적지 말 것"을 알릴 뿐 시스템이 비밀값을 판정하지 않는다 — 등록된 뒤 알게 되면 무효화(상태만 바뀌고 본문은 이력으로 남는다)이고 본문 삭제 경로는 없다. Runner 는 새 지식 원문의 사본을 두지 않는다(서버 백업이 지식 본문을 맡는다 — P6-04). 자동 등록 응답의 권위 메시지는 결과 시점에 올라가고 하나도 등록되지 않으면 지운다 — 그 사이 짧은 창에 DB 에 있다 |
| **옛 지식의 이행은 소유 PC 연결에 달렸다**(P4-06b) | v22 방식으로 소유 PC 에만 있는 지식 원문·권위 메시지(`storage = runner`)는 그 PC 가 연결돼 제어 루프를 돌 때 올라온다. 그 전까지 다른 PC 의 필수 의존 작업은 P4-06 그대로 `missing` 으로 보류되고 진입 검사는 여전히 이것을 미리 알지 못한다. 연결되지 않는 PC 의 원문은 지어내지 않는다 |
| **후보는 AI 의 판단이고 중복은 원문 해시다**(P4-07) | 무엇이 "의미 있는 사건"인지 시스템이 판정하지 않는다 — 계기 목록·상한 3건·근거 한 줄을 요구할 뿐이고, 후보를 안 남긴 실행이 "재사용할 것이 없었다"는 뜻은 아니다. 같은 뜻의 다른 문장은 다른 후보가 된다(`relates_to`/`supports` 로 AI 가 잇거나 사람이 무효·`into` 로 정리). 논의 응답의 `proposal: true` 도 AI 의 표지다 — 사용자 말이 후보로 적히면 사람이 활성화하고, 제안이 사용자 말로 적히면 카드에서 무효로 한다(P4-06 의 충실성 한계와 같다) |
| **반증 후보는 대상을 자동으로 바꾸지 않는다**(P4-07) | 활성 규칙과 다른 관측은 관계(`contradicts`)를 가진 후보로만 남고, 사람이 대상을 무효·개정하거나 후보를 그 항목의 새 버전으로 적용한다. 그 전까지 활성 규칙은 그대로 주입된다("코드 변경만으로 규칙을 무효화하지 않는다"). 자동 충돌 기록도 없다 — 사람이 충돌을 기록하면 채택 확인이 막는다 |
| **참고 후보의 자동 활성화는 반자동이다**(P4-07 → 사용자 결정 2026-09-24 → P4-07b 구현) | project-knowledge 2절의 "근거·조건을 확인하여 참고로 자동 활성화할 수 있다"의 조건을 S-027 이 물을 수 없어 가장 좁은 쪽(없음)으로 뒀고, S-028 에서 사용자가 반자동으로 정했고, **S-029(P4-07b)가 구현했다** — 시스템이 조건 여덟의 충족을 계산해 규칙 화면·카드·관리 패널에 표시하고 사람이 규칙 화면에서 한 번에 활성화한다. **사람의 클릭 없는 활성화는 없다**(`activate_ready_knowledge` 는 API 끝점에서만 불리고 훅·처리기·기동 복구 어디에도 없다). 남은 한계: 조건 (3b)의 명령·종료 코드는 자기보고(위 행), 논의 응답의 제안은 관측 문맥이 없어 조건 (4)를 만족하지 못해 사람이 항목의 폼에서 활성화한다, 같은 내용의 재보고(`duplicate`)도 근거 실행으로 센다(같은 뜻의 다른 문장은 아니다), 조건 결과는 저장하지 않고 조회 때 계산한다(후보마다 실행·명령 조회). 완전 자동은 후보가 실제로 쌓이면 사용자에게 다시 묻는다. QG-08 의 독립 AI 검토 실행은 여전히 없다(`adoption.independent_review = not_run`) |
| **별도 추출 실행이 없다**(P4-07) | 후보는 기존 작업 실행의 응답에 얹히거나 사람이 청한 논의 응답의 제안으로 나온다. 추출만 따로 도는 목적·진입 조건·예산이 없으므로 "추출 예산 부족"은 그 작업 실행의 진입 거부와 같은 것이고 별도 미처리 상태가 없다. 관측 문맥의 기준 커밋은 작업공간이 있는 실행(구현·검증·실험)에서만 오고 분석 실행은 저장소만 안다 |
| **이행 시험의 건너뜀이 늘어난다**(S-027 관찰) | `tests/test_migration.py` 의 v1·v2 이행 시험은 `controller/schema.sql` 을 바꾼 **최근 20 커밋** 안에서 옛 스키마 커밋을 찾고 없으면 건너뛴다. 스키마 커밋이 늘어 S-026 의 1건에서 S-027 시작 시 2건이 됐다(제품 무관, 시험 창의 문제). 창을 넓히거나 옛 스키마를 고정 자료로 두는 것은 그 시험을 다루는 작업에서 정한다 — 이 세션은 바꾸지 않았다 |
| **결정 사항 패널은 본문을 보이지 않는다**(UI-04a) | 목표·범위·제약의 본문은 PC 의 의도 원문이다 — 패널은 서버가 아는 버전·동의·기준 요약·판정만 보이고 `원문 열기` 로 결과물 패널을 연다. 항목별 표시(목표·범위·제약)는 원문 열람과 함께 뒤 작업에서 본다 |
| **적용된 규칙 집계는 Manifest 다**(UI-04a) | `knowledge-use` 는 이 Case 실행들의 Manifest 를 버전별로 센 것(`provided` 만 제공, 나머지는 사유별 수, v22 이전 실행은 `runs_unrecorded`)이며 실제로 읽었는가(영수증)·지켰는가(판정)는 세지 않는다. 실행별 영수증은 관리 화면의 실행 문맥에 있다. 라이브에서 활동 `verification` 후보는 검증 실행이 없는 업무에 "넣지 않음 5" 로 보였다 — 맞는 표시다 |
| **프로젝트 규칙의 자리는 임시다**(UI-04a) | 프로젝트 설정 화면이 아직 없어 왼쪽 목록에서 연다. 설정 화면(UI-04b 제안)이 생기면 그 안으로 옮긴다. 수동 등록은 **출처 대화를 고른다**(API 계약 — 출처 없는 규칙을 만들지 않는다). `into` 로 다른 항목의 새 버전이 된 후보는 두 항목의 이력·채택 기록으로만 남고 관계선은 그리지 않는다 |
| **원래 메시지로 이동은 순번이다**(UI-04a) | `&seq=N` 은 그 메시지로 스크롤·강조할 뿐 본문은 열람 경로로 오며 PC 미연결이면 "연결 필요" 그대로다. 검색(D-84)이 아니다. 그 순번의 메시지가 아직 목록에 없으면 이동하지 않고 주소의 `seq` 는 남는다(다음 조회에서 다시 본다) |
| **가짜 codex 는 첫 메시지의 규칙을 지시문으로 옮긴다**(S-028 관찰, 시험 도구) | `tests/fake_cli/fake_codex.py` 의 `HADS_FAKE_RULE` 은 고정 컨텍스트 뒤의 글에서 규칙을 옮기므로 첫 메시지(고정 컨텍스트 없음)에서는 지시문 머리를 적용 내용으로 적는다. 제품 규칙이 아니라 시험 도구의 특성이며 브라우저 시험은 두 번째 메시지에서 규칙을 말한다. 고치는 것은 그 도구를 다루는 작업에서 |
| **후속 대화의 메시지 옮김은 같은 접수를 가리킨다**(P4-05) | 종료 Case 의 수정 요청 메시지를 새 대화의 첫 메시지로 **옮겨 적는다**(같은 원문·같은 intake). 원문은 한 번 저장됐고 두 Case 가 참조한다. 원래 Case 의 메시지·응답은 남는다. AI 가 설명 질문을 수정 요청으로 잘못 읽으면 되돌릴 경로가 없다(새 대화는 준비 단계이므로 보관하면 된다) |

해소된 행(Autonomy 미강제, `autonomy = NULL` 진행 미정, 여섯 칸 초안·일괄 동의, Case당 단일 작업공간)은 [보존본](development-history-v0.8.md) 4절로 옮겼다.

나머지 미검증·제한은 [P3-03 원문](p3/evidence/P3-03-results.md)·[P3-R1 원문](p3/evidence/P3-R1-results.md)·[P3-R2 원문](p3/evidence/P3-R2-results.md)·[P3-R3 원문](p3/evidence/P3-R3-results.md)·[P3-R4 원문](p3/evidence/P3-R4-results.md)의 미검증·위험 절에서 확인한다. 이 표는 위험을 없앴다는 기록이 아니라 배정 위치다.

## 10. 검증·인계 규칙

- 활성 plan의 성공 기준에 실제 근거를 연결하고 UI/API 우회·핵심 실패·재시작·데이터 보존을 필요한 범위에서 검증한다. 코드가 있거나 CLI가 정상 종료했다고 완료하지 않는다.
- 결과·판정·사람 확인·권한·외부 전달 상태를 구분한다. 제품 기대값을 구현 코드에서 그대로 복제한 시험으로 대체하지 않는다.
- 구현 후 diff와 정책 우회·비밀정보·원문 보관·변경 범위를 검토한다. 문서만 수정한 세션은 문서 링크·식별자·내용 일관성을 확인하고 실행하지 않은 제품 시험을 통과로 적지 않는다.
- 종료 시 현재 상태·진행표·plan·결과·다음 작업을 함께 갱신한다. 미충족은 완료로 표시하지 않고 `WAITING_USER`/`BLOCKED`/미완료 이유와 독립 진행 가능 범위를 기록한다.
- 실제 브랜치/HEAD/미커밋 소유·남은 프로세스·자원·실행 명령을 남긴다. 사용자 변경을 섞거나 임의 삭제하지 않는다. 외부 push·PR 허용은 실제 범위를 확인한다.

### 최신 개발 인계 — S-029 / 2026-09-24 / P4-07b 참고 후보의 자동 활성 조건 표시(반자동) (**완료**)

- **사용자 요청:** "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan·검증·인계 절차를 지켜줘." 다음 단계는 1.12절의 **P4-07b**(사용자 결정 2026-09-24, S-028: 반자동)였다.
- **수행:** 시작 상태(`main`/`6f33867`, clean, `git fetch` 뒤 `origin/main` 과 같음)와 기준선(`scripts\run-tests.ps1` → 웹 빌드·웹 단위 21, pytest **693 통과·2 건너뜀**, P1 계약 18, 10분 38초)을 확인한 뒤 [P4-PLAN-07b](plans/P4-PLAN-07b.md)를 **구현 전에 기록**하고 구현·검증했다. 결과 [P4-07b](p4/evidence/P4-07b-results.md). 기준선 실행의 마지막 15% 쯤(하위 프로세스를 띄우는 시험)은 서버 코드를 고친 뒤 돌았다 — 고친 것이 추가 전용(새 끝점·새 조회 칸)이라 수치는 변경 전 상태로 본다(결과 문서 3절에 적음).
- **제품 판단:** 새로 내리지 않았다 — 사용자 결정(반자동·S-027 조건 그대로)을 구현했다. 상세 설계 선택(조건 코드 이름·근거 실행 세는 법·화면이 본 것만 활성화·논의 제안의 조건 (4) 불가)은 plan 0·9절.
- **서버:** `domain/knowledge.py` 의 `AutoReferenceFinding`·`AUTO_REFERENCE_CODES`(여덟)·`auto_reference_check`·`auto_reference_ready`·`auto_reference_summary`·`AUTO_REFERENCE_REASON`; `controller/repository.py` 의 `auto_reference_check_for`·`activate_ready_knowledge`·`activate_knowledge(extra_adoption=)`·`knowledge_view`/`knowledge_registrations_view` 의 `auto_reference`; `controller/api.py` 의 `GET /api/knowledge/{id}/auto-reference`·`POST /api/projects/{id}/knowledge/activate-ready`(`KnowledgeActivateReadyIn`). 스키마·Runner·지시문·진입 검사·권위 코드는 바꾸지 않았다.
- **화면:** `web/src/api.ts`(형·라벨·`activateReady`), `shell/ProjectRules.tsx`(배지·조건 목록·한 번에 활성화 버튼·결과 줄·`autoReferenceText`), `shell/ProgressCards.tsx`(후보 카드 배지), `KnowledgePanel.tsx`(관리 패널 배지·안내).
- **기존 시험의 의미 검토:** 기존 시험은 바꾸지 않았다(전부 통과). 새 시험: 순수 1 + 서버 1(`test_knowledge_extraction.py`) + 브라우저 1(`test_web_shell.py`). 서버 시험의 거부 건너뜀(AC-5)은 활성화 함수를 끼워 넣어 봤다(같은 호출 안에서 조건 계산과 채택 확인 사이에 끼어들 실제 경로가 없다).
- **검증:** 최종 `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **21**, pytest **696 통과·2 건너뜀·0 실패**(11분 17초, +3 = 순수 1 + 서버 1 + 브라우저 1), P1 계약 **18**, "전체 시험 통과". 새 시험을 따로 돌린 첫 회차에서 셋이 실패했다 — 순수 시험 도우미의 잘못된 단언, 옛 후보 버전에 없는 DB CHECK 를 기대한 서버 시험의 가정, 절이 바뀐 뒤에도 펼쳐진 항목을 다시 접은 브라우저 시험 — 셋 다 시험을 고쳤고 제품은 바꾸지 않았다. 분리 실행한 브라우저 시험의 `-q` 요약 줄이 로그 파일에 찍히지 않아(`.` 100% 는 찍힘) 대기 루프가 멈춰 있었다 — 시험은 통과였고 전체 실행에서 다시 통과했다.
- **실제 CLI:** 하지 않았다(선택 사항, 결과 문서 3절 — 실제 codex 가 후보에 근거 둘을 만들 근거가 없다).
- **경계:** 강제 축은 넷 그대로. 조건 충족 표시·한 번에 활성화는 권한·동의·인수·준수가 아니다. 클릭 없는 활성화 경로 없음(`activate_ready_knowledge` 는 API 끝점에서만). 효력 참고만, 필수 승격 없음(DB CHECK). 스키마 v24·원문 경계 그대로(조회에 더한 것은 코드·실행 id·수).
- **다음 행동:** **UI-04b**(1.11절, 사용자 동의)를 다른 세션에서 새 plan(UI-PLAN-04b)으로. 완료된 plan 을 다시 열지 않는다.
- **사람에게 물어야 할 것:** (1) 이 세션 작업의 커밋·push 여부 — **사용자의 답(2026-09-24): "커밋푸시해줘 다른세션에서 이어서 작업할게"** → 커밋·push 지시. (2) 조건의 Project 설정화를 UI-04b 상세 설정에 넣을지 — 새 제품 판단이며 UI-04b plan 에서 묻는다(지금은 코드 고정). 완전 자동은 후보가 실제로 쌓이면 다시 묻는다(사용자 결정).
- **사용자 지시로 한 일:** 작업 보고 뒤 사용자가 커밋·push 를 지시했다. 이 세션 작업을 **`f57c036`** 으로 커밋한 뒤 이 인계 기록을 이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `6f33867` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다. PR 은 만들지 않았다.
- **작업공간:** 시작 `main`/`6f33867` clean. 이 세션의 변경은 `f57c036`(P4-07b)과 인계 기록 커밋으로 `origin/main` 에 push됐다. 새 파일 `plans/P4-PLAN-07b.md`, `p4/evidence/P4-07b-results.md`. 수정 파일 `domain/knowledge.py`, `controller/{api,repository}.py`, `web/src/{api.ts,KnowledgePanel.tsx}`, `web/src/shell/{ProjectRules,ProgressCards}.tsx`, `tests/{test_knowledge_extraction,test_web_shell}.py`, 문서 `DEVELOPMENT.md`·`development-history-v0.8.md`(1.12절·S-027 인계 옮김)·`README.md`·`decisions.md`(D-67 구현 기록)·`project-knowledge.md`(3절)·`quality-gates.md`(QG-08)·`review-acceptance-matrix.md`(AC-32)·`ui-conversation-design.md`(11절). `web/dist` 는 빌드 산출물(`.gitignore`). 줄 끝은 `git ls-files --eol` 로 확인했다(LF). `git status --short` 로 재확인한다.
- **남은 자원:** 시험이 띄운 제어부·Runner·Edge 는 pytest 가 내렸다. 새 의존성 없음. 세션 시작 시 이 PC 에 사용자의 codex 프로세스(00:54 기동)와 Edge 가 있었고 건드리지 않았다. 라이브 데이터 없음(라이브 미수행). 저장소 `var\` 는 건드리지 않았다.
- **다음 세션이 이어서 할 때:** 조건은 `domain.knowledge.AUTO_REFERENCE_CODES` 순서로 표시되고 `auto_reference_check_for` 가 사실을 모은다. 한 번에 활성화 버튼은 `ProjectRules.tsx` 의 후보 절 머리(`rules-ready-bar`)에 있다 — 규칙 화면을 설정 화면(UI-04b)으로 옮기면 함께 옮긴다. 서버 시험의 도우미는 `_auto(h, id)`·`_auto_check`·`ALL_AUTO_CODES`, 브라우저 시험의 `_git_repo`·`_run_work_with_candidate`. 분리 실행은 `.cmd` 스크립트로 띄운다(`-k` 표현식).

### 이전 개발 인계 — S-028 / 2026-09-24 / UI-04a 결정 사항 패널·프로젝트 규칙 화면 (**완료**)

- **사용자 요청:** "DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. plan 검증 인계 절차를 지켜줘." 다음 단계는 1.11절의 **UI-04**(시작 시 하위 plan 으로 나눈다)였고, 첫 하위 plan 의 범위는 S-027 인계의 제안(결정 사항 패널·프로젝트 규칙 화면)을 그대로 잡았다(사용자 답은 없었다 — 자율 실행).
- **수행:** 시작 상태(`main`/`edcdfff`, clean, `git fetch` 뒤 `origin/main` 과 같음)와 기준선(`scripts\run-tests.ps1` → 웹 빌드·웹 단위 18, pytest **688 통과·2 건너뜀**, P1 계약 18, 10분 17초)을 직접 확인한 뒤 [UI-PLAN-04a](plans/UI-PLAN-04a.md)를 **구현 전에 기록**하고 구현·검증했다. 결과 [UI-04a](ui/evidence/UI-04a-results.md).
- **제품 판단:** 이 세션의 구현에서는 새로 내리지 않았다(자동 활성화 없음 그대로). 작업 보고 뒤 논의에서 **사용자가 참고 후보의 자동 활성화를 반자동(4번)으로 정했다**(아래 (1), 1.12절 P4-07b). 프로젝트 규칙 화면의 자리(왼쪽 목록에서 열고 설정 화면이 생기면 옮김)와 수동 등록의 출처 대화 선택은 상세 설계 선택이다(plan 0·9절).
- **서버:** `_knowledge_version_row` 의 `source_case_title`·`source_message_seq`(+ `_case_title`), 근거 행의 `source_case_title`, `knowledge_registrations_view` 의 `source_message_seq`·`reply_seq`, `case_knowledge_use_view` → `GET /api/cases/{id}/knowledge-use`(이 Case 실행들의 Manifest 를 버전별로 집계, 제공 당시 `state` 와 지금 `state_now`, `runs_unrecorded`). 스키마·정책·진입 검사·권위 코드는 바꾸지 않았다.
- **화면:** `web/src/lib/address.ts`(주소 규칙 `screen`·`item`·`seq`, 단위 시험 3)·`lib/knowledgeBody.ts`(공용 본문 읽기), `shell/ProjectRules.tsx`(새 화면), `shell/ReviewPanel.tsx` 의 `Decisions` 재구성(업무·목표·기준·결정·이 대화에서 정한 규칙·후보·적용된 규칙·질문·해석·이력), `Shell.tsx`(`screen`·`rulesItem`·`focusSeq`)·`Sidebar.tsx`(`프로젝트 규칙`)·`ConversationView.tsx`(`focusSeq`·강조·`hads:focus-message`)·`events.ts`·`ProgressCards.tsx`(카드 링크 → 규칙 화면)·`api.ts`·`shell.css`. 관리 화면 지식 패널은 그대로다.
- **기존 시험의 의미 검토:** 기존 시험은 바꾸지 않았다(전부 통과). 새 브라우저 시험은 가짜 codex 의 특성(첫 메시지의 규칙 → 지시문 머리) 때문에 두 번째 메시지에서 규칙을 말한다(9절).
- **검증:** 최종 `scripts\run-tests.ps1` → 웹 빌드 성공, 웹 단위 **21**(+3), pytest **693 통과·2 건너뜀·0 실패**(10분 37초, +5 = `test_decisions_view.py` 3 + 브라우저 2), P1 계약 **18**. 새 브라우저 시험 하나가 처음 두 번 실패했다 — 가짜 codex 의 첫 메시지 규칙 옮김(시험 도구)·응답 뒤 빈 입력창의 버튼 상태(시험 결함) — 둘 다 시험을 고쳤고 제품은 바꾸지 않았다.
- **실제 CLI·실제 브라우저:** `ui/live/ui04a_rules.py`(제품 코드 import 없음, 실제 `codex-cli 0.156.1`·설치된 Edge) 회차 `092723318` **통과 26/26, 관찰 7, 남은 프로세스 0**(화면 6장). 실제 codex 가 규칙을 12초 만에 옮겨 등록했고(저장소 범위로 좁혀 적음 — P4-06 라이브와 같다) 정리 응답에 제안 둘(K-002·K-003, 활동 `verification`)을 붙였다. 규칙 화면에서 채택 확인·활성화(K-002 v2), 다른 대화의 업무(실행 5)에 K-001 제공 4(논의 응답 비적용), K-002·K-003 은 검증 실행이 없어 "넣지 않음 5" 로 보였다. 첫 시도 `092249006` 은 하네스 결함으로 시작 전에 끝났다(제품 무관).
- **경계:** 강제 축은 넷 그대로. 패널·규칙 화면은 보고·이동·사람의 변경 자리이며 등록·활성화·개정·무효·집계는 권한·동의·인수·준수가 아니다. AI 제안은 어떤 경로로도 활성 필수가 되지 않는다(DB CHECK). 원문 경계 그대로(새 조회는 제목·순번·수뿐). 자동 활성화·독립 검토·검색·설정·알림은 넣지 않았다.
- **다음 행동:** **P4-07b**(1.12절, 작은 작업)를 다른 세션에서 새 plan 으로 먼저 하고, 그 뒤 **UI-04b**(1.11절, 사용자 동의). 순서는 이 세션의 판단(규칙 화면이 막 생겨 문맥이 살아 있고 작업이 작다)이며 사용자가 바꿔도 된다. 완료된 plan 을 다시 열지 않는다.
- **사람에게 물어야 할 것:** (1) 참고 후보의 **자동 활성화 조건** — 네 선택지(없음 유지 / S-027 조건 그대로 완전 자동 / 조건 수정 / 반자동)를 설명했고 **사용자의 답(2026-09-24): 4번 반자동** — 시스템이 S-027 조건(효력 `reference`·종류 `operation`/`known_problem`·서로 다른 실행 둘 이상의 근거 중 하나는 종료 코드 0 명령이 있는 검증·분석 실행·관측한 저장소 범위·반증/충돌 없음·채택 확인 막음 없음) 충족을 계산해 프로젝트 규칙 화면에 "자동 활성 조건 충족"으로 표시하고 사람이 한 번에 활성화한다. 사람의 클릭 없이 활성화하지 않고 필수로는 올리지 않는다. 후보가 실제로 쌓이면 완전 자동으로 넓힐지 그때 다시 묻는다. → 작은 후속 작업 **P4-07b**(1.12절). (2) UI-04b 의 범위 제안(프로젝트 설정·상세 설정 + 알림)에 동의하는지 — **사용자의 답(2026-09-24): 제안에 동의.** UI-04b = 프로젝트 설정·상세 설정 화면 + 알림으로 확정. (3) 이 세션 작업의 커밋·push 여부 — **사용자의 답(2026-09-24): "그래 4번으로 가자. 다른세션에서 이어서 작업할 수 있게 정리하고 커밋 푸시해줘."** → 커밋·push 지시.
- **사용자 지시로 한 일:** 작업 보고 뒤 논의에서 사용자가 (1) 참고 후보의 자동 활성화를 **반자동**으로, (2) UI-04b 의 범위를 제안대로 정하고 커밋·push 를 지시했다. 두 판단을 이 문서(1절·1.11절·1.12절·6절·9절·이 인계)·README·decisions·project-knowledge·UI-04a 결과에 기록하고, 이 세션 작업을 **`4c347a0`** 으로 커밋한 뒤 이 인계 기록을 이어서 커밋해 둘을 함께 `origin/main` 에 push 했다(push 전에 fetch 해 원격이 `edcdfff` 그대로임을 확인). 허용은 이 두 커밋에만 적용된다. PR 은 만들지 않았다.
- **작업공간:** 시작 `main`/`edcdfff` clean. 이 세션의 변경은 `4c347a0`(UI-04a)와 인계 기록 커밋으로 `origin/main` 에 push됐다 — 새 파일 `plans/UI-PLAN-04a.md`, `tests/test_decisions_view.py`, `ui/live/ui04a_rules.py`, `ui/evidence/UI-04a-results.md`·`UI-04a-live.log`·`UI-04a-live-results.json`·`UI-04a-live-{A-panel,A-rules,A-focus,B-check,B-activated,C-applied}.png`, `web/src/lib/{address,address.test,knowledgeBody}.ts`, `web/src/shell/ProjectRules.tsx`. 수정 파일 `controller/{api,repository}.py`, `tests/test_web_shell.py`, `web/src/{api.ts,KnowledgePanel.tsx}`, `web/src/shell/{ConversationView,ProgressCards,ReviewPanel,Shell,Sidebar}.tsx`·`{events.ts,shell.css}`, 문서 `DEVELOPMENT.md`·`development-history-v0.8.md`(S-026 인계 옮김)·`README.md`·`decisions.md`(D-80 구현 기록)·`ui-conversation-design.md`(11절)·`project-knowledge.md`(2절)·`review-acceptance-matrix.md`(AC-46). `web/dist` 는 빌드 산출물(`.gitignore`). 편집이 남긴 CRLF 는 LF 로 되돌렸다(`git ls-files --eol`). `git status --short` 로 재확인한다.
- **남은 자원:** 라이브 제어부·Runner·codex·Edge 는 스크립트가 내렸다(남은 프로세스 0). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-ui-04a-live\{092249006, 092723318}` 에 있고 저장소 `var\` 는 건드리지 않았다. 새 의존성 없음. 세션 시작 시 이 PC 에 사용자의 codex 프로세스(00:54 기동)와 Edge 가 있었고 건드리지 않았다.
- **다음 세션이 이어서 할 때:** 결정 사항 패널은 `ReviewPanel.tsx` 의 `Decisions`(절마다 `data-testid` `decisions-*`), 규칙 화면은 `ProjectRules.tsx`(`rule-{key}`·`rule-*-{key}`·`rules-*`), 주소는 `lib/address.ts` 의 `parseAddress`/`formatAddress`/`rulesLink`/`messageLink`, 서버 집계는 `case_knowledge_use_view`. 브라우저 시험 둘은 `test_web_shell.py` 끝의 UI-04a 절. 라이브는 `ui/live/ui04a_rules.py`(p407 의 `Probe`·`drive_work`·`start_work` 재사용). 분리 실행은 `-k` 표현식을 쪼개는 `Start-Process` 대신 `.cmd` 스크립트로 띄운다.

### 그 이전 인계

UI 개편 1·2차 설계 인계와 S-011~S-027 은 [보존본](development-history-v0.8.md) 6절, S-001~S-010 은 [v0.6 보존본](development-history-v0.6.md)에 있다.
