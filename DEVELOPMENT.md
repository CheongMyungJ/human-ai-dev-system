# 개발 진행 안내서 — 새 세션의 시작점

기준: **설계 v0.8 2차 반영 / 사용자 결정 D-01~90 / 2026-09-23**. 구현은 **P4-03 완료·스키마 v15**이며 대화 중심 UI 개편은 아직 구현하지 않았다. 현재 구현과 새 목표를 구분한다. 코드·테스트·스키마·실행 스크립트·의존성 설정을 변경하기 전에 반드시 plan을 기록·공유한다. 이 규칙은 이 저장소의 개발 절차이며, 제품 Fast Lane에 매번 별도 plan 문서나 사람 승인을 요구한다는 뜻이 아니다.

**주요 UI 동작·제품 정책·개발 순서는 확정했고 개발 인계를 정리했다.** 사용자는 추가 8개 결정의 2차 문서 반영을 허용했다. 이번 세션은 문서만 수정하며 구현은 다른 개발 세션에서 이어간다. [대화 중심 UI 설계](ui-conversation-design.md)의 확정 내용·미구현 차이와 아래 UI-01 범위를 먼저 읽는다. 정확한 화면 치수·단축키·기술 상세는 실제 구현에서 검토한다.

**문서 반영 후 사용자 후속 지시(2026-09-23):** 이번 UI 설계·개발 인계 문서 20개의 커밋과 `origin/main` push를 허용했다. 아래 문서 반영 당시의 미커밋·미푸시 기록과 구분한다. 실제 커밋·반영 결과는 `git log`와 원격 ref로 확인하며 이 허용을 다음 코드 변경에 확대하지 않는다.

다른 개발 세션에 전달할 요청:

> C:\git\human-ai-dev-system-design\DEVELOPMENT.md와 ui-conversation-design.md의 최신 인계를 읽고 UI-01 대화·요청 기반 개발을 진행해줘. P4-03 완료 상태와 기존 문서 변경을 보존하고, 실제 코드·환경을 확인한 뒤 UI-PLAN-01을 기록·공유하고 구현·검증·인계를 해줘. 이번 범위는 UI-01 하나이며 P4-04나 전체 UI 개편까지 자동 확대하지 마. 새 제품 판단이 필요하면 질문해줘.

**기본 수행 단위는 현재 하위 작업 하나**다. **P4-01·P4-02·P4-03은 끝났고 P3의 남은 라이브 하나(경로 C)는 사용자 지시로 스킵됐다.** D-90에 따라 다음은 **UI-01**, 이후 P4-04·UI-02를 거쳐 기본 UI로 진행한다. P4 전체로 범위를 자동 확대하지 않는다. 사용자 지시가 우선하고, 필요한 기술 선택은 진행하되 새 제품 판단이 필요한 경우만 질문한다. P4-03의 변경은 **당시 사용자 지시로 커밋·push 했다**(`bf86590`, S-019 인계). 그 허용은 이번 문서나 후속 개발의 외부 쓰기 허용이 아니다.

## 1. 현재 상태

| 항목 | 현재 값 |
|---|---|
| 저장소·작업 위치 | `C:\git\human-ai-dev-system-design`, https://github.com/CheongMyungJ/human-ai-dev-system (비공개) |
| 설계 기준 | **v0.8 2차, D-01~90**. 대화 UI·준비 Case·요청 제어와 초안·검색·검토 버전·목적 변경·설명 소비·시간 지표·외부 편집·개발 순서 확정 |
| 구현 기준 | **P4-03 완료**. P1 제한 수용, P2 완료, P3 완료(제한 있음), QG-01~07 정책·조건부 독립 검토·기본 2회 repair(P4-01), 설정 변경 예약·부분 재검증·늦은 결과(P4-02), **Profile 정의 v2의 목적별 완료 계약·목적 의무·결론 요구·혼합 목적·실험 정리(P4-03)**. 기존 QG-01은 소급 복제하지 않고 연결한다. 스키마 **v15** |
| 현재 단계 | **P4 — 품질·다양한 업무·지식** (`IN_PROGRESS`)에 새 UI 작업을 연결. P4-01~03 완료, UI-01~04·P4-04 이후 미착수 |
| 다음 하위 작업 | **UI-01 — 대화·요청 기반** (`READY`, 미착수). 준비 Case·대화/정정/참조·요청 처리·서버 전송 잠금과 기존 이력 보존. 상세는 1.2절 |
| 활성 plan | **없다.** [P4-PLAN-03](plans/P4-PLAN-03.md)은 완료됐고 [실행 결과](p4/evidence/P4-03-results.md)가 있다. 다음 개발 세션에서 **UI-PLAN-01을 새로 기록·공유**한다. 완료된 plan을 다시 열지 않음 |
| 다음 순서 | UI-01 → P4-04·UI-02 → UI-03 기본 화면 → P4-05~07·UI-04 → P5 → P6. 단계 안에서도 현재 작업 하나씩 계획·검증. 스킵 경로 C는 관련 예산 라이브가 필요해질 때 다시 검토 |
| 마지막 제품 검증 | **pytest 481 + P1 계약 unittest 18 통과**, 스키마 **v15**, `npm run build` 성공. P4-03 집중 시험 29·이행 집중 시험 10 통과, 파일 DB 재개와 v14→v15 반복 이행 확인. **실제 `codex-cli 0.154.0` 의도 초안 3건**으로 새 출력 계약 확인(결과는 [P4-03 근거](p4/evidence/P4-03-results.md) 3절). 이전 수치는 당시 사실로 보존 |
| 이 문서 개정 시작 상태 | UI 1차 문서 반영 직전 `main`, HEAD `416283b`(P4-03 인계), 작업 트리 clean, 로컬 추적 ref `origin/main`과 차이 없음으로 관측. 원격을 새로 조회한 것은 아님. P4-03 구현 커밋은 `bf86590` |
| 남은 상세 | [UI 설계](ui-conversation-design.md) 14절의 실제 화면·상태/API·이행·CLI 능력 검증. 활성 Case 변경의 제품 방향은 D-86으로 확정했고 구체 이행은 미구현이다. 경로 C 스킵·시험 기능 A·미기록 Autonomy의 controlled 취급은 유지 |
| 이번 허용 범위 | **2차 문서 반영·개발 인계**, 이어진 사용자 지시로 **이번 문서의 커밋·origin/main push 허용**. 제품 실행·코드 변경·PR은 포함하지 않는다. S-019의 허용을 승계한 것이 아니며 제품의 실제 외부 게시는 여전히 P5다 |

P3-R4 구현은 R1이 기록만 하던 **Autonomy를 진입과 완료의 판단**으로 바꿨다. `controlled`는 시작 확인 전에 설계·계획·구현·검증·실험을 배정하지 않고 결과 후보 확인 전에 종료하지 않는다(D-65). 기본 `ask-on-decision`은 반대로 **연다** — 명확한 요청을 그 범위의 실행 위임으로 인정해 설계·계획의 사람 검토 없이 진행하고, 조건을 충족하면 **사람 인수 기록 없이** 자동 완료한다(D-31). Fast Lane이면 준비가 설계+계획 두 건에서 **결합 기록 한 건**으로 줄고(D-60), 요청 정합성 확인은 명확·저위험에 **가벼운 확인**을 쓰되 그것을 독립 의미 검토로 표시하지 않는다(D-25). 누적 material delta는 **마지막 유효 위임과** 비교되어 AI 출처의 변경이 의존 작업을 막고, 조사 Profile의 Case는 로컬 실험을 수행하되 제품 수정으로 목적을 확대하지 않는다(D-66).

**R1~R3의 강제는 전부 "막는" 방향이었고 R4는 방향이 둘이다.** 여는 쪽이 더 위험하다 — 조건을 갖추지 않았는데 열리면 아무도 보지 않은 결과가 완료가 되고, 조건을 갖췄는데 사람 인수 기록을 함께 만들면 있지도 않은 확인이 근거로 남는다. 그래서 자동 완료는 미해결 기준·질문·피드백·미정리 실행을 전부 지나야 하고 그 인수는 `mode = auto_policy`·actor = 정책 식별자다. **`autonomy = NULL`인 R1 이전 Case는 사용자 결정으로 controlled로 취급하되 값은 여전히 미기록이다** — 적용과 기록을 나눈 자리이며, 없는 사람의 선택을 적지 않는다.

**v0.6 기본값 둘이 뒤집혔고 기존 Case에는 소급하지 않는다.** 완료(사람 인수 → 조건 충족 자동)와 단계 검토(사람 검토 → 자동 진행)이며, 명시 설정 행과 Autonomy 미기록 Case가 그 경계다.

P3-R3 구현은 R1이 기록만 하던 예산을 **실제 배정 판단**으로 바꿨다. 배정 전에 Run 생성과 **같은 트랜잭션**에서 `이미 사용한 양 + 진행 중 예약 + 새 실행 예약 ≤ 한도`를 검사하고(`BEGIN IMMEDIATE`), 종료 후 정산하며, 소비는 역할·Task·저장소·세션·재배정으로 나뉘거나 초기화되지 않는다(D-61). hard 도달은 그 한도를 **소비하는** 새 실행과 재배정만 막고 진행 중 실행의 결과·이벤트·산출물은 계속 받는다 — 완료도 취소도 아니며 `case.status`를 바꾸지 않는다. 재개 경로는 한도 변경 하나뿐이고 `continue` 류의 예외 인자가 없다.

**다만 "강제한다"가 지표마다 같은 약속이 아니다.** 실행 수·검토 수·컨텍스트 크기는 배정 전에 정확히 잡을 수 있어 `enforced_absolute`이고, 시간 지표는 `enforced_no_absolute_cap` — 돌고 있는 CLI를 초 단위로 끊을 능력이 없으므로(P1-03 `cancel_confirmed = unknown`) 새 배정만 막고 진행 중 실행의 초과 노출이 있을 수 있다. 토큰·비용의 hard 한도는 여전히 **설정을 받지 않는다**. 모르는 값을 0으로 적지 않으며 각 지표의 `complete`가 "이 수가 소비 전부인가"를 말한다.

P3-R2 구현은 R1이 만든 기록 모델 위에 **실행과 강제**를 붙였다. 작업공간이 `Case × Repository`가 되어 저장소마다 전용 브랜치·worktree·시작 기준·사용자 dirty tree 관측을 따로 갖고(D-39), Case의 저장소 선택과 코드 쓰기 허용이 **작업공간을 만들 수 있는지를 실제로 정한다** — 미선택·명시 제외·쓰기 미허용·기록 저장소는 각각 다른 사유로 거부된다. 허용 안의 저장소는 `auto_in_allowance`로 자동 기록되고, 허용을 넓히는 요구(명시 제외·새 권한·게시 허용)는 사유와 함께 거절된다(D-38·D-63·D-64). 실행은 `run.repository_id`가 가리키는 저장소의 worktree에서 돌며, 작업공간이 둘 이상인데 대상이 미기록이면 고르지 않고 거부한다. 코드 조합(`Repo ID → 정확한 스냅샷 참조`)이 검증 대상을 고정하고, 근거의 유효성은 **저장소별로** 도출되어 무관한 저장소의 변경이 증거를 폐기하지 않는다. 조합이 코드 대상을 전부 담지 않으면 통합은 미검증으로 남는다.

**이제 강제되지 않는 축은 하나다.** 조회 응답의 `enforcement`에서 `autonomy`(R4)·`controlled_checkpoint`(R4)·`budget`(R3)·`repository_selection`(R2) 넷이 `enforced`이고, **게시 허용과 실제 push·PR은 여전히 P5**다. 네 축이 강제로 바뀐 것이 게시 권한을 만들지 않는다 — 결과 후보 확인도 push·게시 권한을 만들지 않는다(D-65). 지식 적용은 여전히 없다(P4-06). 이전 계획·증거는 당시 사실로 유지하고 새 설계 통과 증거로 재명명하지 않는다. 상세 과거 인계는 [v0.6 보존본](development-history-v0.6.md), 완료된 plan은 [P3-PLAN-R4](plans/P3-PLAN-R4.md)·[P3-PLAN-R3](plans/P3-PLAN-R3.md)·[P3-PLAN-R2](plans/P3-PLAN-R2.md)·[P3-PLAN-R1](plans/P3-PLAN-R1.md)·[P3-PLAN-03](plans/P3-PLAN-03.md)을 따른다.

P4-01은 위 네 `enforcement` 축을 재명명하지 않고 **품질 채택 조건을 별도 모델로 추가했다.** QG-01은 필수이며 기존 정합성 판정의 light/independent 강도를 그대로 쓴다. QG-02~07은 추천과 Case/Task 명시 설정을 구분하고, 명시 ON 게이트는 관련 실행의 실제 진입 조건이다. 독립 검토는 별도 reviewer 세션과 고정 참조를 요구하며 작성자의 완료 주장만으로 통과하지 않는다. 실패 뒤 repair는 최초 실패와 별도인 Case+gate+subject 누적이고 기본 두 번이다. 세션·Task·토글로 초기화되지 않는다. 상세는 [P4-01 결과](p4/evidence/P4-01-results.md)를 따른다.

P4-02는 그 모델에 **시간축**을 붙였다. 새 강제 축을 만들지 않았다 — `enforcement` 는 여전히 넷이 `enforced` 이고 `publish` 만 P5 다. 달라진 것은 **언제**다. 검증 1회에 시작과 끝이 생겨 시작 시점의 정책 리비전·검사 강도가 그 실행에 고정되고, 진행 중 설정 변경은 적용되지 않고 **예약**된다(D-30). 예약은 유효 정책에 섞이지 않고 진행 중 검사를 멈추지도, 그 결과를 무효화하지도 않는다 — 예약은 취소 명령이 아니다. 종료 시 `현재 결과 보존 → 예약 적용 → 효과 재평가` 순서를 한 트랜잭션에서 지키며 **판정이 실패여도 반영한다.**

**무엇이 판정을 내리는가도 좁아졌다.** P4-01은 `setting` 이 바뀌기만 해도 기존 판정을 `needs_recheck` 로 내렸는데, ON/OFF 토글은 이미 끝난 검사가 무엇을 보았는지 바꾸지 않는다. 이제 **검사 강도 변경 하나**만 판정을 내리고 토글·repair 한도 변경은 내리지 않는다. 반대로 요청·코드·권한 변경은 참조를 대조해 **영향받는 검사만** 재검증 대상으로 만들고 나머지는 이유와 참조 버전을 적은 뒤 재사용한다. 범위를 확인하지 못하면 `unknown` 이며 그것은 재검증 대상이다 — 확인하지 못한 것을 "무관하다"로 적지 않는다.

**늦은 결과와 배정 직전 재확인이 그 시간축의 나머지 절반이다.** 취소 요청 뒤·게이트 OFF 뒤·시작 리비전이 달라진 뒤 도착한 결과는 원래 실행에 저장되고 `historical`·`late_result` 로 남으며 repair 주기를 만들지 않는다. 배정 요청이 기대 정책을 적었는데 그 사이 바뀌었다면 새 사유 `quality_gate_policy_changed` 로 거부한다. **예약만 있는 상태는 이 사유가 아니다** — 예약은 차단이 아니며, 막으면 설정을 바꾸려는 사람이 진행 중 업무를 멈추게 된다. 상세는 [P4-02 결과](p4/evidence/P4-02-results.md)를 따른다.

P4-03은 **완료 판정이 Profile을 보게** 했다. 그 전까지 여섯 Profile 모두 "현재 기준이 전부 `met`" 하나였다. 새 Case는 **Profile 정의 v2**를 받고, v2는 목적별 완료 계약을 가진다 — 기준마다 **목적 의무**(behavior·restoration·cause·answer·improvement·preservation·target_state)가 붙고, 요구된 의무마다 기준이 하나 이상 있어야 하며(refactoring은 개선과 보존 **둘 다**), `met`은 그 의무에 허용된 **충족 방식**을 반드시 적는다. 새 강제 축은 아니다 — `enforcement`는 여전히 넷이 `enforced`이고 `publish`만 P5다. 달라진 것은 **완료가 무엇을 뜻하는가**다.

**가장 중요한 수정은 생략 우회다.** P3-R4는 `not_reproduced`로 적은 `met`만 거부했고 방식을 **비워 둔** `met`은 받았다 — 미재현을 해결로 만드는 데 필요한 것은 아무것도 적지 않는 것 하나였다. **판단 불가도 값이 됐다.** 원인·조사 기준은 결론 요구를 갖고(기록이 없으면 확정 필수로 취급), 결과는 확정/판단 불가를 적는다. 원인 확정 필수의 판단 불가는 `not_met`이며 사람의 예외 수용으로만 닫히고, 판단 불가 허용 조사는 근거 있는 판단 불가로 정상 완료한다. 혼합 목적("원인 확정과 수정")은 의도가 선언하고 목적마다 기준이 필요하다. 실험 실행은 조사 근거로만 쓰이고, 실험이 작업공간에 남긴 임시 변경은 **관측으로 도출**해 제품 목적 Case의 자동 완료를 보류한다. **기존 Case는 v1 그대로다** — 새 규칙을 소급하지 않는다. 상세는 [P4-03 결과](p4/evidence/P4-03-results.md)를 따른다.

### 1.1 확정한 개발 순서와 작업 분할 — D-90

확정 설계는 [D-68~90](decisions.md)와 [대화 중심 UI](ui-conversation-design.md)에 모았다. 새 작업 ID는 개발 관리용 분할이며 구현 완료를 뜻하지 않는다. 각 행의 세부 범위·이행·시험은 해당 작업의 새 plan에서 구체화한다.

| 순서 | 하위 작업과 범위 | 다음 단계로 넘길 조건 |
|---|---|---|
| 1 | **UI-01 대화·요청 기반**: 준비 Case, 메시지·정정·버전 참조, 요청 처리 상태, 서버 전송 잠금과 접수 | 같은 Case의 준비→최초 업무화, 원문/기존 정책 보존, 중복·충돌 일반 전송 차단, 카드 답변의 대상 구분 검증 |
| 2 | **P4-04 문맥·재개**, **UI-02 입력·실행 제어**를 별도 plan으로 연결. 고정 입력·새 세션·usage 복구와 Runner 수신/저장/heartbeat/실행 분리, 질문·확인 답변·중단·재연결 | 긴 실행 중 제어 수신, 실제 종료·잔류 활동과 상태 불명의 구분, 재시작·중복 방지·소비 보존을 검증한 뒤 중단 UI 연결 |
| 3 | **UI-03 기본 대화 화면**: PC·라이트 기본 shell, 프로젝트/대화 선택→논의/요청→진행/질문→실제 결과 검토. 기본값 요약·초안 복구·검토 버전 유지 연결 | 새 기본 흐름을 실제 사용해 검토. 없는 산출물/지원 능력을 표시하지 않고 API 상태·원문 가용성과 일치 |
| 4 | **P4-05~07과 UI-04 남은 업무·화면 연결**: 완료/예외/설명/후속, 활성 목적·Profile 이행, 지식, 대화 검색, 미커밋 포함 시작, 외부 편집·상세 설정·알림 등 남은 확정 범위 | 기존 기준·증거·소비·원래 파일·권한을 보존하면서 각 흐름 검증. UI-04는 시작 시 여러 하위 plan으로 나누어 완료 범위 추적 |
| 5 | **P5 GitHub**, **P6 운영·전체 수용**: 연결/이슈/외부 결과, 원격·인증·복구·설치·전체 수용 | 연결과 게시 허용을 구분하고 실제 외부 결과와 전체 FR/NFR/AC 근거 확인. 앞당긴 검증이 전체 P6를 대체하지 않음 |

기본 UI를 사용할 수 있는 시점을 앞당기되 제어 기능은 먼저 검증한다. 특정 기능을 UI-03에서 지원하지 않으면 미지원으로 표시하며 임시 성공 동작을 만들지 않는다. P4-04에 전체 UI·일반 중단·미커밋 포함 작업을 몰아넣지 않는다. P4-05~07의 남은 첫 버전 기능도 없애지 않는다.

P4-04의 고정 입력·새 세션·usage 복원은 이 설계와 연결되지만 UI 전체 구현을 포함하지 않는다. 원문·결정·명시 금지·권한·정책·repair·소비와 함께 **P4-03의 Profile 버전, 목적 선언, 기준 의무·충족 방식·결론 요구, 후보의 완료 의미**를 보존해야 한다. 준비→최초 Profile 배정과 기존 활성 Profile 재분류는 다른 작업이다. 종료 Case 설명 질문도 현재 공통 Run 거부와 구분하는 경로가 필요하다.

현재 저장소 guard는 기록 repo의 코드 선택·작업공간을 거부한다. D-73은 별도 코드 허용이 있으면 겸용하도록 바꾼 목표이며 과거 P3-R2의 통과 증거를 새 규칙의 근거로 재명명하지 않는다. 이 변경은 P5 연결 전에 저장소 정책·선택·작업공간의 모든 경로를 함께 검증한다.

### 1.2 다음 작업 UI-01의 경계와 시작점

**목표:** 기존 Case/Task/Run 위에 대표 대화와 현재 요청을 표현하여, 준비 논의에서 같은 Case의 최초 업무화로 이어지는 영속 기반을 만든다. 전체 UI 구현보다 먼저 정책·데이터·접수 동작을 고정한다.

- 목표/Profile 미정 준비 Case와 메시지 순서·원문 참조·정정 관계를 모델링한다. Case 종료 상태·현재 요청 상태·보관 가시성을 분리하고 기존 Case를 준비 단계나 새 Profile 정의판으로 소급 변경하지 않는다.
- 현재 요청이 여러 Task/Run을 포함하도록 관계를 정의한다. 일반 전송은 처리 중 서버에서 차단하고 UI의 비활성만 믿지 않는다. 질문·확인 카드 답변은 대상 요청/버전에 한정하며 질문 하나의 답을 일반 후속 요청이나 전체 승인으로 취급하지 않는다.
- 원문 PC 저장+서버 참조 등록, 재전송 멱등성, 접수 불명 대조를 기존 intake와 연결한다. 일반 후속 전송 대기열·자동 전송·서버 원문 영구 복제를 새로 만들지 않는다. 브라우저 초안 UI는 UI-03에서 이 접수 계약에 연결한다.
- 준비→최초 업무화는 유효한 요청·금지·Autonomy·권한·예산·P4-03 완료 의미를 이어받는다. 최초 Profile 배정과 활성 Profile 이행(D-86)은 구분한다. 기존 활성 이행·종료 후 설명 전용 실행·중단 프로세스 제어는 후속 작업이다.
- plan에서 준비 단계 논의 실행의 허용 목적·쓰기 제한·사용량 귀속, 요청 완료/실패/불명 전이, 입력 저장과 상태 갱신의 실패 경계를 정한다. 기존 실행 진입·예산 예약·게이트를 우회하지 않는지 검증한다.

코드 대조 시작점은 `controller/repository.py`의 `create_case`·intake/참조·usage, `controller/admission.py`의 목적별 진입·종료 Case 거부, `controller/schema.sql`과 이행, `runner/agent.py`의 동기 실행 루프, 기존 `web/src`의 Case/입력 폼이다. 필드명·DB 구조를 이 인계만 보고 먼저 고정하지 않는다.

**UI-01 검증 초점:** 준비 Case의 저장/재시작, 같은 Case 업무화와 누적 이력, 기존 v1/v2 Case 보존, 요청 처리 중 동시 전송 경쟁·재전송 멱등성, Run 사이 잠금 유지, 질문 대상/버전 거부, 원문 저장 실패·접수 불명 시 거짓 완료 방지. 제품 경계를 확인하는 시험과 필요한 실제 CLI 확인을 plan에 연결한다. 이 문서 목록 자체는 실행 결과가 아니다.

## 2. 다음 세션 시작 절차

1. `git status --short --branch`, 최근 커밋·변경 파일을 확인한다. 이 문서 개정의 미커밋 파일과 사용자 작업을 구별한다. 자동 reset·clean·stash·강제 checkout·rebase를 하지 않는다. 이전 인계의 원격 동기 상태를 현재 사실로 가정하지 않는다.
2. 현재 상태와 최신 2차 인계, [UI 설계](ui-conversation-design.md), P4-03 결과, [사용자 결정](decisions.md), [전체 설계](design-draft.md), [문맥](review-context-contract.md), [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [완료](completion-lifecycle.md)을 읽는다. 전체 옛 개발 일지를 다시 읽을 필요는 없지만 현재 하위 작업과 UI 설계의 접점·미구현 경계는 확인한다.
3. 실제 코드의 정책·스키마·진입 검사·화면과 새 설계의 차이를 확인한다. 문서상 목표를 구현 사실로 취급하지 않는다. 기존 Case·결정·원문·증거·실행 이력은 보존한다.
4. 기존 실행 환경을 확인하고 `scripts\bootstrap.ps1`, `scripts\run-tests.ps1` 등 저장소의 재현 경로로 기준선을 확인한다. 설치/복구가 필요하면 기존 허용·환경에 맞게 처리한다. 481+18은 인계 시점 수치이며 현재 시험 결과를 대신하지 않는다.
5. **P4-PLAN-01~03은 완료다.** 다시 열지 말고 다음 **UI-PLAN-01을 기록·공유한 뒤** UI-01을 구현한다. P4-04는 그 기반을 이어받는 별도 작업이다. P4-01~03이 남긴 정책 리비전·예약·재검증 판정·repair 누적·Profile 정의판·목적 의무·결론을 보존한다.
6. 작업을 수행한 뒤 현재 표와 진행표·plan·결과를 함께 갱신한다. 선택한 하위 작업에서 이후 항목으로 자동 확대하지 않는다. P3-R4의 고정 컨텍스트(`run_context_ref`)·material delta, P4-01~02의 게이트 실행·재검증, P4-03의 완료 의미(`completion_meaning`)를 재사용한다. 기존 판정을 소급 변경하지 않는다.

기본 작업공간은 이 checkout이다. 다른 작업공간이 필요하면 정확한 경로·이유를 plan과 인계에 기록한다. 다른 세션과 같은 파일을 동시에 수정하지 않는다. 이 안내서는 구현을 위임하지만 외부 쓰기나 기존 데이터 파괴를 포괄하지 않는다.

## 3. 계획·개정·증거 관리

- plan은 작업 목록뿐 아니라 요구에서 도출한 성공 기준과 검증 방법을 포함한다. 활성 plan을 이어 쓰고 범위/기준이 바뀌면 구현 전에 변경 이유를 기록한다.
- 단계/작업별 구현 완료, 자동 시험, 실제 CLI 시험, 외부 서비스 시험, 문서 검토를 구별한다.
- 기존 테스트가 v0.6 정책을 고정했다면 그 의미를 검토해 바꾸고 대응하는 v0.7 거절·보존 시험을 추가한다. 실패를 피하려고 검사를 삭제하지 않는다.
- 새 정책을 기존 승인·동의·권한에 소급 적용하지 않는다. 마이그레이션은 기존 정책값·원문·주체·버전을 명시 보존하고 새 Case의 기본값과 구분한다. 진행 중 Case의 정책 변경은 영향과 권한을 검토하고 종료 기록은 다시 쓰지 않는다.
- 구현하지 않은 모드는 비활성/미지원으로 표시한다. R1에 모델이 생겼다는 이유로 R4 자동 실행이나 P5 게시를 지원한다고 표시하지 않는다. R2가 쓰기 허용을 강제한다고 게시 허용까지 강제되는 것으로 표시하지 않는다.
- 파일/필드 이름과 수치 기본값 등은 상세 설계 제안이며, 사용자 확정 정책과 구별한다. 스키마 번호는 실제 마이그레이션을 만들 때 정한다.

| Plan | 상태·근거 |
|---|---|
| [P4-PLAN-03](plans/P4-PLAN-03.md) | 완료, AC-1~21. [실행 결과](p4/evidence/P4-03-results.md) — Profile 정의 v2 완료 계약, 목적 의무·충족 방식 필수, 결론 요구, 혼합 목적, 실험 근거·정리, v15. Profile 재분류는 범위 밖(3.8절) |
| [P4-PLAN-02](plans/P4-PLAN-02.md) | 완료, AC-1~22. [실행 결과](p4/evidence/P4-02-results.md) — 설정 변경 예약, 부분 재검증, 늦은 결과, 배정 직전 재확인, v14 |
| [P4-PLAN-01](plans/P4-PLAN-01.md) | 완료, AC-1~20. [실행 결과](p4/evidence/P4-01-results.md) — QG-01~07, 조건부 독립 검토, 기본 2회 repair, v13 |
| [P3-PLAN-04](plans/P3-PLAN-04.md) | 완료(제한 있음). AC-1~22 중 **AC-17 만 라이브 미수행**(사용자 지시로 스킵). [실행 결과](p3/evidence/P3-04-results.md) — 경로 A·B 확인, AC-19 확인 |
| [P3-PLAN-R4](plans/P3-PLAN-R4.md) | 완료, AC-1~32. [실행 결과](p3/evidence/P3-R4-results.md) |
| [P3-PLAN-R3](plans/P3-PLAN-R3.md) | 완료, AC-1~22. [실행 결과](p3/evidence/P3-R3-results.md) |
| [P3-PLAN-R2](plans/P3-PLAN-R2.md) | 완료, AC-1~19. [실행 결과](p3/evidence/P3-R2-results.md) |
| [P3-PLAN-R1](plans/P3-PLAN-R1.md) | 완료, AC-1~16. [실행 결과](p3/evidence/P3-R1-results.md) |
| [P3-PLAN-03](plans/P3-PLAN-03.md) | 완료, AC-1~15. [실행 결과](p3/evidence/P3-03-results.md) |
| [P3-PLAN-02](plans/P3-PLAN-02.md) | 완료, AC-1~16. [실행 결과](p3/evidence/P3-02-results.md) |
| [P3-PLAN-01](plans/P3-PLAN-01.md) | 완료, AC-1~15. [실행 결과](p3/evidence/P3-01-results.md) |
| P1·P2 plan | [보존본의 plan 목록](development-history-v0.6.md) 및 `plans/` 원문 유지 |

## 4. 단계 진행표

| 단계 | 상태 | 다음 작업 | 완료 근거/변경 사항 |
|---|---|---|---|
| P1 CLI 계약·실증 | ACCEPTED_WITH_LIMITATIONS | — | [환경](p1-environment-contract.md), [CLI 실증](p1/evidence/P1-02-results.md), [중지 실증](p1/evidence/P1-03-results.md). 제한은 아래 이월표 유지 |
| P2 최소 흐름 | DONE (v0.6) | — | [P2-04 결과](p2/evidence/P2-04-results.md). 당시 승인 중심 동작의 사실 보존 |
| P3 실행 기반·새 정책·통합 | DONE_WITH_LIMITATIONS | — | P3-01~03·R1~R4·P3-04 완료. 라이브는 경로 A·B 확인, **경로 C 는 사용자 지시로 스킵**(AC-17 라이브 증거 없음) |
| P4 품질·여섯 업무·지식 | IN_PROGRESS | **P4-04** | [P4-01](p4/evidence/P4-01-results.md): QG-01~07 정책·별도 의미 검토·repair·v13. [P4-02](p4/evidence/P4-02-results.md): 설정 변경 예약·부분 재검증·늦은 결과·v14. [P4-03](p4/evidence/P4-03-results.md): Profile 정의 v2 완료 계약·목적 의무·결론·혼합 목적·실험 정리·v15. 다음은 컨텍스트·재개이며 자동 착수하지 않는다 |
| P5 GitHub·외부 완료 | PENDING | P5-01 | 범위 사전 허용과 controlled 게시 전 확인으로 개정 |
| P6 원격·운영·전체 수용 | PENDING | P6-01 | 기존 데이터/단절/운영 요구 유지, 새 계약도 포함 |

순서는 `P3-03(완료) → R1(완료) → R2(완료) → R3(완료) → R4(완료) → P3-04(완료)`다. 번호 R1~R4는 새 정책 반영 작업이며 기존 P3-01~03의 재개·재번호가 아니다.

## 5. P3의 남은 작업

| 하위 작업 | 범위 | 성공 기준과 검증 |
|---|---|---|
| ~~P3-R1 정책·Profile·영속 모델~~ | **완료(2026-09-21).** 위임 근거/정책 버전, WorkDepth와 분리된 Autonomy, Profile별 intent 표현, 선택·허용 저장소 및 기록 저장소 모델, 예산 설정 계약, v7→v8 이행 | 달성 근거는 [P3-R1 결과](p3/evidence/P3-R1-results.md). 기록은 있고 **강제는 없다** — 각 축의 담당은 조회의 `enforcement` 에 있다 |
| ~~P3-R2 Repository·작업공간~~ | **완료(2026-09-21).** Case×Repo 작업공간, 선택·쓰기허용의 강제, 허용 내 자동 추가와 세 경계, 실행의 대상 저장소, 코드 조합과 통합 미검증 표시, v8→v9 이행 | 달성 근거는 [P3-R2 결과](p3/evidence/P3-R2-results.md). **선택·쓰기 허용은 강제되고 게시 허용은 여전히 기록뿐이다**(P5) |
| ~~P3-R3 예산 예약·집계·정지~~ | **완료(2026-09-22).** 원자적 예약·정산·Case 단일 누적, 지표별 예약 계약과 세 보장 범위, hard 도달 시 새 실행·재배정 차단, v9→v10 이행 | 달성 근거는 [P3-R3 결과](p3/evidence/P3-R3-results.md). **예산은 강제되고 Autonomy·확인 지점은 여전히 기록뿐이다**(R4). 시간 지표의 hard 는 `no_absolute_cap` 이며 절대 상한이 아니다 |
| ~~P3-R4 자동 실행·진입·완료 경로~~ | **완료(2026-09-22).** Autonomy 기반 진입·기본 자동 완료, Fast Lane 결합 기록, 조건부 독립 의미 검토, 누적 material delta, 피드백 영향 자동 연결, controlled 초기/결과 확인, 로컬 실험, 무변경 충족과 미재현의 구분, v10→v11 이행 | 달성 근거는 [P3-R4 결과](p3/evidence/P3-R4-results.md). **Autonomy·확인 지점이 강제로 바뀌었고 게시 허용만 P5로 남았다.** `autonomy = NULL` Case는 controlled로 **취급**하되 값은 미기록 그대로다 |
| ~~P3-04 수직 통합~~ | **완료(2026-09-22, 제한 있음).** R1~R4 위에서 실제 작은 기능 개발, 같은 Runner 2 Repo 연동, controlled 대표 경로, Task×Repository(v12) | **경로 A·B 확인.** A: 두 저장소 기능 전체·`deep + ask-on-decision`·Task×Repo 강제·조합·사람 호출 3건. B: 시작 확인 전 **다섯 목적 차단**·확인 뒤 같은 흐름·강제 종료 뒤 v12 기록 복원(AC-19)·결과 후보 확인 전 종료 거부·후보 변경 시 이전 확인 무효. **남은 것: 경로 C(예산) 라이브 — 사용자 지시로 스킵.** 검증 실행이 시험을 돌리는 것은 **이 호스트의 CLI sandbox 제약으로 불가**하며 기준은 `unverified` 로 남는다 — 경로 A 는 그래서 닫히지 않았고, 경로 B 는 사람이 미검증을 **예외로 수용**해 `closed_with_exceptions` 로 닫혔다. 어느 쪽도 검증된 완료로 적지 않는다. 상세는 [P3-04 결과](p3/evidence/P3-04-results.md) |

R1~R4가 붙인 네 축의 강제는 게시 권한이 아니다. R4는 필수 정합성 확인을 유지하면서 독립 AI 호출만 조건부로 만들었고 정합성 검사 자체를 OFF로 숨기지 않는다 — 방식이 무엇이었고 무엇을 보지 않았는지가 값으로 남는다. **P3-04가 구현 깊이와 인간 개입의 분리를 실제로 확인했다** — `deep + ask-on-decision`(경로 A)과 `deep + controlled`(경로 B) 둘 다 준비의 양은 수준이 정하고 확인 지점은 Autonomy가 정했다. 시험 기능은 생산자→소비자 계약 변경이었다(P3-04 plan 11절).

**P3 완료 조건과 실제:** 새 정책으로 실제 단일/복수 저장소 기능 흐름을 재현하고, controlled·material delta·예산·유효 버전·dirty 보호의 핵심 경계를 검증한다. **예산 경계만 라이브 없이 남았다** — 자동 시험의 예약·정산·정지 시험은 그대로 통과하지만 기능 흐름 안의 도달은 보지 않았다(사용자 지시로 스킵). 여섯 업무 전체·고급 게이트·지식 기능을 P3에서 끝났다고 표시하지 않는다. P3-03의 관측·명령 수집 기반을 재사용하며 종료 코드/변경량만으로 의미적 성공을 판단하지 않는다.

## 6. P4 — 품질·다양한 업무·지식

참조: [품질 게이트](quality-gates.md), [게이트 운영](gate-operations.md), [Profile](case-profiles.md), [문맥](review-context-contract.md), [지식](project-knowledge.md), [완료](completion-lifecycle.md).

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| ~~P4-01 게이트·repair~~ | **완료(2026-09-22).** QG-01 검사 강도, QG-02~07 선택, Case/Task 설정, 조건부 별도 의미 검토, 스키마 v13 | 필수 기준/증거 유지, 초기 실패 후 기본 2회, 재분할/세션교체 초기화 없음, OFF/미적용/통과 구별. [실행 결과](p4/evidence/P4-01-results.md) |
| ~~P4-02 변경·예약~~ | **완료(2026-09-22).** 검증 1회 경계, 설정 변경 예약·적용 시각·취소, 요청/코드/권한 변경의 부분 재검증, 늦은 결과, 배정 직전 재확인, 스키마 v14 | 원래 판정·정책 보존, 예약이 진행 중 검사를 무효화하지 않음, 강도 변경만 재검증, `unknown` 을 재사용으로 적지 않음, 늦은 결과가 repair·배정을 만들지 않음. [실행 결과](p4/evidence/P4-02-results.md) |
| ~~P4-03 여섯 Profile~~ | **완료(2026-09-23).** Profile 정의 v2 완료 계약, 기준의 목적 의무·충족 방식 필수, 원인·조사 결론 요구, 혼합 목적 선언, 실험 근거 제한·정리 상태, 스키마 v15 | 목적별 필수 의무·허용 방식, 생략 우회 차단, 판단 불가 구별, 무변경 성공은 검증 실행 관측, 기준 없는 목적은 예외 불가, v1 Case 소급 없음. **Profile 재분류는 범위 밖.** [실행 결과](p4/evidence/P4-03-results.md) |
| P4-04 컨텍스트·재개 | **UI-01 이후** 고정 원문/결정/입력, 분할 검토·새 세션, 실패·usage 복원. UI-02 제어와 별도 plan으로 연결 | 원문 누락·stale 표시, 필수 조건 유지, 작성자의 완료 요약만으로 검토하지 않음 |
| P4-05 완료·예외·후속 | **UI-03 이후** 기본 자동 완료, controlled 후보 확인, 사람 예외 수용, 종료 후 새 Case와 기존 결과 설명 | 기준별 결과·실행 정리·요청 완료점 확인, 자동 사람 인수 생성 없음, 예외가 권한을 만들지 않음. 설명 전용 진입·같은 Case 예산 누적·종료 snapshot 보존 |
| **P4-06 지식 관리·적용** | 원본/짧은 적용 내용/조건/필수·참고/버전·상태, Runner 보관, 활동+범위 자동 주입·Manifest | 기존 확정 지식 수동/자동 등록 경로로 검증. 규칙의 권위 승계, scope 확대 재적용, 필수 내용 실제 제공, 원문 불가/충돌 의존 작업 보류, 주입과 준수 구분 |
| **P4-07 선택적 지식 추출** | 결정·문제 해결 결과의 후보, 기존 항목 근거 추가/대체, 후보→적용 연결 | AI 새 일반화가 강제규칙이 되지 않음. 아무 후보 없는 Case 정상완료. 추가 호출을 의무화하지 않으며 발생 비용 누적. 다른 Case에서 적용·버전/범위·원문 가용성 확인 |

**P4 완료 조건:** 여섯 Profile 대표·혼합 목적·실패/복구/변경을 재현하고 Case 간 최소 지식 관리·추출·주입을 검증한다. 지식은 첫 버전 범위이며 FR-19/QG-08을 일괄 후속 확장으로 제외하지 않는다. QG-08은 모든 후보의 별도 AI 실행·사람 승인을 의무화하는 절차가 아니다. 범용 검색·외부 지식 시스템과 공동 사용 UI는 범위 밖이다.

## 7. P5 — GitHub·외부 결과

| 하위 작업 | 범위 | 완료조건 |
|---|---|---|
| P5-01 연결·읽기 | 대표 Repo의 Git remote 후보·기존 인증/접근 확인, 선택 연결, 업무 이슈 위치/참여 Repo, 원래 이슈·PR·CI | 읽기 출처/버전 연결, 비밀정보 비기록, 모호한 remote 선택, 연결 유예와 연결≠게시 허용, 읽지 못한 CI 증거 표시 |
| P5-02 기록 게시 | 논의 단계에는 이슈 없음. 업무화 뒤 연결·허용 안에서 지정 Repo의 Case당 새 이슈, 요약 append·Outbox | 참여 Repo별 이슈 복제 없음, 코드/diff/상세 로그 제외, 코드·기록 겸용 가능하되 별도 코드 권한 필요, 응답유실 중복 방지 |
| P5-03 범위 허용 push·PR | Case 시작 Grant, 정확한 대상/payload 직전 검사, 저장소별 외부 결과 | 범위 내 반복승인 없음, 미허용 Repo/ref/행동 차단, 부분 실패·결과 불명 대조, 같은 원격 ref/PR 경합 검증 |
| P5-04 controlled·CI·리뷰 | 결과 확인→자동게시→필수CI→closure, PR 피드백과 stale 검사 | CI 수정으로 후보내용 변경시 재확인. 동일내용 재전송 반복확인 없음. 상대 Repo 준비 대기와 코드결함 구분. 완료후 수정 새 Case, 일반 이슈댓글 승인/명령 해석 금지 |

실제 외부 쓰기 시험은 시험 대상·범위를 별도로 허용받은 경우만 수행한다. 이 제품의 자동 게시 설계를 개발한다는 지시가 이 개발 저장소의 push 권한은 아니다. 실제 시험을 못 하면 계약시험과 미검증을 구분한다. 강제 push·병합·배포·이슈 종료·CI 수동 재실행·GitLab은 범위 밖이다.

## 8. P6 — 원격·운영·전체 수용

| 하위 작업 | 범위·검증 |
|---|---|
| P6-01 원격·인증 | 소유자/Runner 등록·폐기, LAN/사용자 VPN 암호화, 미등록/권한 회수 거부. 한 Case를 여러 PC에 분산하지 않음 |
| P6-02 데이터 경계 | 상세 문서·대화·지식·코드·증거가 소유 Runner에 남고 제어부 DB/로그/큐/캐시에 영구 복제되지 않음. 과거 지식 소유 PC 오프라인 시 가용성 표시 |
| P6-03 단절·복구 | 실제 CLI/모드별 도구 경계 정지·다음 호출 차단, 지연 결과·부분 외부 효과·예산 예약 재조정. 종료불명 중복쓰기 금지 |
| P6-04 백업·설치 | 호스트별 하루 1회·최근 7개·원본 자동삭제 없음, 부분복원/미푸시·미커밋 보존. Python/Git 준비 후 일반 프로세스 설치·업그레이드·빌드UI |
| P6-05 규모·최종 수용 | 10 Project·3 Runner·동시 AI 2·Runner쓰기 기본 1에서 실제 측정. 전체 FR/NFR/[수용 시나리오](review-acceptance-matrix.md)와 증거 연결. 지표 미제공·한도 강제불가·미실증을 정직하게 표시 |

세 Windows 배포 형태를 확인한다. 모의 프로세스와 실제 호스트·외부 서비스 시험을 구분한다. OpenCode 등 미검증 제한은 보존하고 필수 기준을 임의 면제하지 않는다. Linux/WSL·공동 사용·복수Project Case·분산Case·인터넷 직접 공개는 이후 범위다.

## 9. P3-03·P3-R1~R4에서 이어받는 한계와 처리 위치

| 현재 구현/관측의 한계 | 후속 처리 |
|---|---|
| ~~Autonomy·확인 지점은 기록되지만 아무 것도 막지 않는다~~ | **R4에서 해소.** 네 축이 `enforced` 이고 `publish` 만 P5로 남았다. 시험은 이제 반대를 고정한다 — 되돌아가면 강제가 사라진 것이다 |
| **가벼운 확인은 원문의 의미를 보지 않는다** | `conformance_check.unverified_scope` 에 그대로 남는다. **P4-01이 해소한 것은 선택·강제 경계**다 — 독립 검토가 필요한 조건에서는 가벼운 확인으로 통과하지 않고 Case 설정으로 낮출 수 없으며, 별도 reviewer 세션과 고정 참조를 요구한다. 가벼운 확인 자체가 의미 검토로 승격되지는 않는다 |
| **delta 분류가 본문을 읽지 않는다** | 제어부는 구조화된 사실만 보고 동의된 항목의 AI 출처 변경을 전부 `material` 로 본다. **P4-02가 정한 것은 그 다음 질문이다** — 변경이 어느 판정을 내리고 어느 증거를 재사용하는지를 `change_impact` 가 **참조 대조로만** 정하고, 범위를 모르면 `unknown` 으로 재검증한다. 분류기를 의미 판정기로 바꾸지는 않았다 |
| **Fast Lane 판정은 수준 판단에 의존한다** | 수준은 AI가 축별 근거로 제안하고 사람이 조정한다(P3-01). P4-01이 설계·계획 품질 게이트와 조건부 독립 검토를 붙일 수 있게 했어도 수준 판단 자체의 진위를 자동 판정하지 않는다. **P4-03에서 본 것:** 조사 Profile(RCA·research)의 대표 흐름은 분석·실험 실행만 쓰고 준비 산출물을 요구하지 않으므로 Fast Lane 판정이 끼어들 자리가 없다. Fast Lane은 제품 변경 Profile의 준비 양을 정하는 판정이며 P4-03은 그것을 바꾸지 않았다 |
| **실험 표시는 배정 목적에서 나온다** | `run.is_experiment` 는 요청이 주장하지 못하고 목적에서 도출된다. **P4-03이 두 가지를 더했다** — 실험 실행은 조사 결론(`investigated`)의 근거로만 쓰이고 제품 의무의 근거가 되지 않으며, 실험이 작업공간에 **임시 변경을 남겼는지**를 실행 전후 트리 지문으로 도출한다(`left_changes` 등). 제품 목적 Case의 잔여는 자동 완료를 막는다. **관측은 여전히 격리가 아니다** — "되돌렸다"는 관측한 트리가 같다는 뜻이고, 작업공간 밖 변경은 감지만 한다(D-44) |
| **시간 예산의 hard 한도는 절대 상한이 아니다** | 돌고 있는 CLI 를 초 단위로 끊을 수 없다(P1-03 `cancel_confirmed = unknown`). `enforced_no_absolute_cap` 으로 표시하고 실제 초과를 값·원인과 함께 기록한다. **UI-02**에서 필요한 중단 능력을 먼저 실증하고 **P6-03**에서 전체 복구를 검증한다. D-88 지표 선택도 이 한계를 없애지 않는다 |
| **검증의 취소 요청과 실제 종료는 여전히 다른 값이다** | P4-02는 `stop_requested_at` 만 적고 상태를 `completed` 로 바꾸지 않는다. 요청 뒤 도착한 결과는 원래 실행에 저장되고 `late_result` 로 남는다. **실제로 언제 멈췄는지는 모르는 그대로**이며 그 능력은 P1-03 경계 안에 있다. **UI-02에서 실제 종료·잔류 활동을 실증한 뒤 중단 버튼 연결**, P6-03 전체 복구 수용 유지 |
| **권한 철회 기계 자체는 만들지 않았다** | P4-02는 권한 변경을 변경 사건으로 받아 영향받는 증거만 재검증 대상으로 만들고, 그것이 게이트 예약과 달리 검증 종료를 기다리지 않으며 기존 판정·외부 효과를 되돌리지 않는다는 경계를 시험으로 고정했다. 실제 철회·중지 제어의 실증은 **P6-03**, 외부 효과 대조는 **P5** |
| **토큰·비용은 어댑터가 줄 때만 안다** | 미제공은 `unavailable` 이며 0 이 아니다. codex 는 비용을 주지 않아 `estimated_cost.complete = false` 다. 실제 청구액 대조는 범위 밖이며 추정 지표의 hard 한도는 계속 설정을 받지 않는다 |
| **CLI 내부 모델 호출 수는 관측 범위 밖이다** | `run_count` 는 **시스템이 시작한 호출**이다. 그 계약을 `reservation_contract` 에 값으로 남겼고 다른 것으로 표시하지 않는다 |
| **예약은 한 제어부 DB 의 트랜잭션으로만 보장된다** | 여러 제어부·원격 환경의 예약 조정은 **P6-03**. 지금 보장 범위를 분산 환경으로 넓혀 표시하지 않는다 |
| **게시 허용(`publish_allowed`)은 여전히 기록뿐이다** | 네 축이 강제로 바뀌었다고 게시까지 강제되는 것으로 읽지 않는다. controlled 의 결과 후보 확인도 push·게시 권한을 만들지 않는다(D-65). 실제 push·PR·기록 이슈와 외부 CI 대기는 P5다 |
| **한 실행은 작업공간 하나에서 돈다** | **P3-04에서 실제로 돌려 봤고 바꾸지 않았다.** 계획이 Task 마다 저장소를 정하고(v12) 각 실행이 그 worktree 에서 돌면 두 저장소 기능이 실제로 완성된다 — 계약을 계획 산출물이 고정하기 때문이다. 한 실행이 여러 작업공간을 보게 하는 것은 **필요하지 않았다.** 다만 이 시험의 계약이 단순했다는 것도 사실이며, 드러나지 않은 것을 "문제없음"으로 적지 않는다 |
| **검증 실행이 이 호스트에서 시험을 돌리지 못한다** | CLI 안에서 `python` 도 `py` 도 찾지 못한다(P3-03·P3-04 가 같은 것을 관측했다). 그래서 `met` 판정의 라이브 증거가 없고 기준이 `unverified` 로 남는다. **자동 완료 Case 는 그 상태에서 닫히지 않았고**(경로 A), **controlled Case 는 사람이 예외로 수용해 `closed_with_exceptions` 로 닫혔다**(경로 B) — 둘 다 맞는 동작이며 어느 쪽도 검증된 완료가 아니다. **절대 경로 인터프리터는 시도하지 않았다** — "불가"의 범위는 관측한 만큼이다. 실제 시험 실행은 환경을 갖춘 뒤에 본다 |
| **근거의 진위는 여전히 시스템이 판정하지 않는다** | P4-01은 요청/기준/대상/증거 **참조**와 독립 reviewer를 요구하고 작성자의 완료 주장만으로 일반 게이트를 통과시키지 않는다. 그러나 참조된 명령·종료 코드가 실제인지까지 검증하지는 않는다. 앞선 거짓 `met` 기록은 소급 승격하지 않으며 이 한계를 숨기지 않는다 |
| **조합의 변화는 관측으로만 안다** | 제어부는 git 을 보지 않는다. 실행이 없는 저장소의 항목은 `snapshot_incomplete` 이며 그것은 "모른다"이지 "안 바뀌었다"가 아니다 |
| **`run.repository_id = NULL` 인 옛 실행** | 작업공간이 하나뿐이면 그것으로 해석하고, 저장소가 늘어나면 어느 항목에도 붙지 않는다. 화면이 `unattributed_run_effects` 로 드러내며 지어내지 않는다 |
| ~~`autonomy = NULL`(R1 이전 Case)을 어떻게 진행할지 미정~~ | **사용자 결정(2026-09-22)으로 controlled 취급으로 확정.** 저장된 값은 `NULL`·`migrated_unknown` 그대로이고 **도출만 바뀐다** — 없는 사람의 선택을 적지 않는다. 단계 검토 기본값은 v0.6(사람 검토)을 유지한다. 사람이 값을 기록하면 그때부터 그 설정을 따른다 |
| Project·Task 단위 정책 조정 없음, Profile 재분류 경로 없음 | Project 기본값과 설정 출처는 UI 작업에서 보완한다. **D-86은 미완료 같은 문제를 같은 Case에서 의도/Profile 개정한다는 제품 방향을 확정했다.** UI-04에서 과거 항목·기준·증거·소비·repair 보존과 새 필드 대응을 구현한다. P4-03이 미룬 이유는 다음 의도의 항목 집합 변경으로 기존 기준의 `relates_to`가 대상을 잃기 때문이다. 기존 혼합 목적 완료 계약만으로 이행·쓰기 허용이 구현된 것은 아니다 |
| `project.repo_path` 와 `project_repository` 에 같은 값이 두 곳에 있음 | **R2에서 정본을 `project_repository` 로 옮겼다.** 컬럼은 지우지 않았고 조회가 불일치를 드러낸다(`legacy_repo_path_matches_registry`). 컬럼 제거는 이후 범위 |
| ~~여섯 칸 feature 초안·일괄 동의·별도 QG-01·설계/계획·기본 사람 인수~~ | **R1이 모델을, R4가 실행·화면·진입 조건을 만들었다.** 기본 완료는 조건 충족 자동, 단계 검토 기본값은 자동 진행, QG-01은 방식이 둘이다. v0.6 과거 증거는 그대로이며 기존 Case에는 소급하지 않는다 |
| ~~Case당 작업공간 하나와 단일 저장소 가정~~ | **R2에서 해소.** Case×Repo 작업공간, 저장소별 시작 기준·dirty 관측, 코드 조합과 통합 미검증 표시 |
| 실제 변경이 없으면 구현 실행 실패 | **R4가 기준 판정 쪽을 나눴다** — `satisfaction` 으로 무변경 충족(`already_satisfied`, 검증 실행 증거 필요)과 미재현(`not_reproduced`, `met` 불가)을 구분한다. **실행 자체의 판정은 그대로다** — 변경도 명령도 없는 구현 실행은 여전히 실패다. **P4-03이 정한 것:** 무변경 완료는 구현 실행의 성공이 아니라 **검증 실행의 관측**(`already_satisfied`·`preserved` 는 검증 실행만 근거)이고, 조사 Profile의 무코드 완료는 분석·실험 실행의 근거(`investigated`)다. 그래서 구현 실행의 판정을 바꿀 필요가 없었다 |
| 명령·종료 코드가 AI 자기보고 | **P4-01에서도 남는다.** 일반 게이트는 증거 참조와 reviewer 분리를 강제하지만 외부 실행 사실을 새로 증명하지 않는다. 자기보고를 검증된 실행으로 소급 승격하지 않으며 실제 실행 확인 경로 없음을 판정과 함께 보인다 |
| Task·기준 연결 적합성, 질문 blocks 자유문자열/불명 범위 전체차단 | **P4-01이 부분 해소했고 P4-02가 변경 뒤 유효성을 붙였다.** required 발견은 전달된 `criteria_refs` 에 연결돼야 하고, 연결되지 않은 의견은 advisory다. 요청·코드·권한이 바뀌면 그 참조를 가진 검사만 `needs_recheck` 가 되고 나머지는 이유·참조 버전과 함께 재사용된다. 다만 **참조 관계의 의미 진위**까지 제어부가 읽지는 않으며 불명 관계는 계속 안전한 쪽으로 막는다 |
| 작업공간 밖 변경은 감지만 하고 막지 못함, 원래 Repo 밖은 미관측 | 기존 capability/한계 보존. worktree 는 파일 배치의 분리이며 OS 격리가 아니다(D-44). **R4의 로컬 실험도 같다** — 쓰기를 여는 조건은 늘렸지만 통제 능력은 그대로이며 격리 보장으로 표시하지 않는다 |
| 기준 커밋 기록이 없을 때 P3-03은 merge-base/HEAD 복구 | 복구 경로는 그대로다. **R2의 조합이 `snapshot_incomplete` 로** "기준 커밋만으로는 실제 입력을 설명할 수 없다"를 표시한다. 현재 HEAD를 원래 검증 기준으로 조용히 승격하지 않음 |
| **예산 도달의 기능 흐름 라이브가 없다** | R3 의 예약·정산·정지는 자동 시험으로 고정돼 있고 hard 도달의 거부도 그 시험이 확인한다. **기능이 진행되는 도중에 걸리는 모습**은 P3-04 의 경로 C 이며 **사용자 지시로 스킵**했다(2026-09-22). 예산 라이브가 필요해지는 작업에서 `p3/live/path_bc.py --path c` 로 다시 본다 |
| **사람의 예외 수용이 종료를 연다** | 미검증 기준이 남아도 사람이 그것을 수용하면 Case 가 닫힌다(`closed_with_exceptions`). 원래 판정은 보존되고 자동 완료는 스스로 수용하지 못한다(`auto_policy_cannot_accept_exception`). 예외의 **적절성** 판정은 없다 — 무엇을 수용했는지가 값으로 남는 것이 지금 가진 전부이며 그 게이트는 **P4-05** 다 |
| residual_activity unknown, 안전중지 제품경로 미연결 | **R3에서 예산 쪽을 연결했다** — 시간 지표의 정산이 `unresolved` 로 남고 hard 는 `no_absolute_cap` 이다. UI-02에서 중단·잔류 활동을 검증하고 P6-03 전체 복구로 이어간다. **R4의 자동 완료도 미정리 실행이 남으면 종료를 확정하지 않는다** |
| 종료 Case의 모든 Run 거부 | D-87을 위한 **P4-05 설명 전용 진입**이 필요하다. 공통 closed guard를 통째로 제거하지 않고 기존 결과·근거의 설명만 허용한다. 원래 종료 판정·당시 사용량을 보존하고 이후 설명·예산 변경을 별도 이력으로 같은 Case에 누적한다 |
| 시간 계측과 UI 기본 지표의 차이 | 현재 실행시간은 `assigned_at`~`finished_at`, 경과시간은 `Case.created_at`~조회 시각이다. D-88 구현 시 Run 내부 사람 대기를 실행시간에서 제외할 계측과 종료 시 소비 snapshot이 필요하다. 기존 지표·한도·미확정 정산을 조용히 바꾸지 않는다 |
| 열람 요청 만료·크기 한도 미구현, 원문 PC 경계 | P4-04/P6-02에서 입력범위·문맥크기·원문가용성 검증 |

나머지 미검증·제한은 [P3-03 원문](p3/evidence/P3-03-results.md)·[P3-R1 원문](p3/evidence/P3-R1-results.md)·[P3-R2 원문](p3/evidence/P3-R2-results.md)·[P3-R3 원문](p3/evidence/P3-R3-results.md)·[P3-R4 원문](p3/evidence/P3-R4-results.md)의 미검증·위험 절에서 확인한다. 이 표는 위험을 없앴다는 기록이 아니라 배정 위치다.

## 10. 검증·인계 규칙

- 활성 plan의 성공 기준에 실제 근거를 연결하고 UI/API 우회·핵심 실패·재시작·데이터 보존을 필요한 범위에서 검증한다. 코드가 있거나 CLI가 정상 종료했다고 완료하지 않는다.
- 결과·판정·사람 확인·권한·외부 전달 상태를 구분한다. 제품 기대값을 구현 코드에서 그대로 복제한 시험으로 대체하지 않는다.
- 구현 후 diff와 정책 우회·비밀정보·원문 보관·변경 범위를 검토한다. 문서만 수정한 세션은 문서 링크·식별자·내용 일관성을 확인하고 실행하지 않은 제품 시험을 통과로 적지 않는다.
- 종료 시 현재 상태·진행표·plan·결과·다음 작업을 함께 갱신한다. 미충족은 완료로 표시하지 않고 `WAITING_USER`/`BLOCKED`/미완료 이유와 독립 진행 가능 범위를 기록한다.
- 실제 브랜치/HEAD/미커밋 소유·남은 프로세스·자원·실행 명령을 남긴다. 사용자 변경을 섞거나 임의 삭제하지 않는다. 외부 push·PR 허용은 실제 범위를 확인한다.

### 최신 설계 인계 — 2026-09-23 / UI 개편 2차 문서 반영·개발 인계

- **사용자 요청:** 1차 이후 확정한 8개 결정과 개발 순서를 2차 반영하고 다른 개발 세션의 인계를 정리한다. 주요 제품 선택은 마무리됐고 화면 세부·기술 상세는 구현에서 검토한다. 시안이나 실제 UI 사용성 검증을 수행한 것은 아니다.
- **기준과 보존:** `main` / `416283b`, P4-03 완료·스키마 v15·Profile v2. 2차 시작 시 1차의 루트 문서 19개 수정과 `ui-conversation-design.md` 신규 파일이 미커밋으로 존재했다. 이를 이어 수정하며 제품 코드·완료 plan/evidence·과거 데이터는 변경하지 않는다. 아래 S-019와 1차 인계는 당시 기록이다.
- **추가 반영:** D-83~90의 브라우저 초안, 보관 포함 대화 검색, 결과물/열람 버전 유지, 미완료 목적·유형 변경, 종료 후 설명 소비, 실행시간 합계 기본, 외부 편집기, 개발 순서를 UI·관련 계약·요구·수용 시나리오에 반영한다.
- **다음 행동:** 실제 Git·환경 확인 → UI 설계와 이 문서 1.1~1.2절·P4-03 근거 대조 → **새 UI-PLAN-01 기록·공유 → UI-01 구현·검증·인계**. 이 문서 반영 세션에서는 plan이나 코드를 만들지 않았다. UI-01 이후도 작업별 범위를 지키며 P4-04·UI-02, UI-03 순으로 진행한다.
- **핵심 기술 경계:** Runner 동기 루프와 제어 수신 분리, 원문 PC 접수/멱등, 요청별 잠금, 활성 Profile의 항목/기준 매핑, 설명 전용 closed 진입, 종료 snapshot/같은 Case 예산, 실행시간의 사람 대기 공제가 필요하다. 이 사실을 제품 지원 완료로 표시하지 않는다.
- **새 질문이 필요한 때:** 확정 정책을 변경하거나 보존할 수 없는 제품 동작이 발견되면 근거·영향·권고안을 제시한다. 필드명·기술·시험 설계와 이미 확정한 선택을 매번 사용자에게 다시 묻지 않는다. 이미지·모바일/태블릿·내부 전체 파일 편집기는 후속 검토다.
- **환경 참고:** 1차 문서 검사 때 `.venv\Scripts\python.exe`의 기존 Python 3.12 경로 실행 실패가 관측됐다. 제품 시험 실패로 기록하지 않았으며 문서 검사는 PowerShell로 수행했다. 다음 구현 세션에서 저장소 bootstrap/시험 경로의 실제 가용성을 확인한다.
- **문서 검증:** 변경 문서 20개의 로컬 링크 256개, D-68~90의 단일 정의, FR 34·SC 25·NFR 12의 식별자 중복, AC-01~55의 누락/중복, 코드 블록 경계·문자 깨짐과 `git diff --check`를 확인했다. UI 정본·결정·개발 인계·정책·요구·수용 기준을 교차검토했다. 신규 수용 항목은 향후 기대 동작이며 실행한 제품 시험이 아니다.
- **문서 반영 종료 당시 작업공간:** 제품 코드·스키마·테스트·완료 plan/evidence 변경 없음, HEAD `416283b` 유지. 제품 시험·build·CLI 실행·커밋·push는 수행하지 않았다. 1차부터 이어진 루트 문서 19개 수정과 신규 UI 문서 1개가 미커밋 상태였다. 이후 사용자에게 이번 문서의 커밋·push 지시를 받았으며 실제 Git 상태는 이 문서 상단의 후속 지시와 함께 확인한다.

### 이전 설계 인계 — 2026-09-23 / UI 개편 1차 문서 반영 (당시 논의 계속)

- **사용자 요청:** 대화에서 확정한 UI 설계를 1차 문서 반영한다. 이 논의가 끝나면 다른 세션에서 개발을 이어갈 예정이므로 현재 결정·미정·구현 차이·재개 지점을 구분한다. 문서 반영은 허용됐지만 이번 작업은 코드 구현이나 외부 반영 요청이 아니다.
- **시작 기준:** `main` / `416283b`, 문서 반영 직전 clean. P4-03 구현 `bf86590`과 그 인계가 이미 반영된 상태다. S-019의 P4-03 완료·v15·Profile v2·기존 검증 수치는 그대로 보존한다.
- **반영:** [대화 중심 UI](ui-conversation-design.md) 신설, D-68~82 추가, FR-11/SC-07의 일반 전송 시점과 관련 NFR, 기록 repo 겸용·이슈 시점, 논의/업무화·보관·중단·완료, 작업 기준·로컬 완료·권위 있는 결정 활용을 관련 계약에 반영한다. README/전체 설계의 오래된 P3-03 개발 안내를 현재 상태로 정리한다.
- **구현 상태:** 제품 코드·스키마·테스트·실행 설정 변경 없음. 완료 plan/evidence 변경 없음. 대화 UI와 새로운 상태·제어 경로는 미구현 목표다. 새 개발 plan도 이번에 만들지 않는다.
- **문서 검증:** 변경 문서 20개의 로컬 링크 249개, D-68~82 및 요구/시나리오 ID 중복, 코드 블록 경계, 정책 일관성과 `git diff --check`를 확인했다. 제품 시험·build·CLI 라이브는 이번에 실행하지 않았다. 제품 코드·완료 plan/evidence 변경이 없음을 변경 파일 목록으로 확인했다.
- **이어갈 논의:** UI 설계 14절의 화면별 흐름과 미정 상세를 검토한다. 이미지·휴대폰/태블릿 최적화는 후속 검토다. 아직 논의 전체 완료나 UI 착수 순서 확정으로 읽지 않는다.
- **다른 개발 세션:** 논의 종료 후 갱신된 이 인계와 UI 설계를 다시 읽는다. 기존 다음 항목은 P4-04이며, 현재 하위 작업의 새 plan으로 범위·검증을 정한다. UI 전체 개편을 자동 포함하지 않고 P4-03의 완료 의미·v1 보존·repair/usage를 유지한다. 커밋·push는 이번에 하지 않았으며 과거 허용을 승계하지 않는다.

### 직전 제품 개발 인계 — S-019 / 2026-09-23 / P4-03 여섯 Profile (**완료**)

- **수행:** 시작 상태(`main`/`f6145f4`, clean, `origin/main` 과 같음)와 기준선(`scripts\run-tests.ps1` → pytest **451** + unittest **18**)을 직접 확인한 뒤 [P4-PLAN-03](plans/P4-PLAN-03.md)을 **구현 전에 기록·공유**하고 구현·검증했다. 상세 근거는 [P4-03 결과](p4/evidence/P4-03-results.md)다.
- **완료 계약:** Profile 정의 **v2** 를 더하고 새 Case 의 정의판을 "2" 로 올렸다. v2 는 목적별 필수 의무를 갖는다(refactoring 은 개선+보존, maintenance 는 목표 상태+보존 조건이 적혔을 때의 보존). 기준마다 목적 의무가 붙고(보고값 또는 연결 항목에서 도출·출처 구분), 요구된 의무에 기준이 없으면 `objective_without_criteria` 로 자동·사람 인수 모두 거부된다 — 예외 대상이 아니다. **v1 Case 는 그대로다**(계약 없음, 방식 없는 `met` 허용, 후보 해시 불변).
- **결과 기록 규칙:** v2 기준의 `met` 은 충족 방식 필수(**생략 우회 차단**), 의무별 허용 방식, 원인·조사 기준은 결론 필수와 결론 요구(미기록 = 확정 필수), `already_satisfied`·`preserved` 는 검증 실행만, 실험 실행은 `investigated` 근거로만. 규칙은 순수 모듈 `domain/completion_meaning.py` 한 곳에 있다.
- **혼합 목적·실험:** 의도 문서 v5 의 `objectives` 가 요청이 명시한 목적을 선언하고 목적마다 기준이 필요하다. 실험의 정리 상태를 실행 전후 트리 지문으로 도출하고, 제품 목적 Case 의 잔여는 자동 완료만 막는다. 실험 지시문에 "되돌린다"를 더했다.
- **저장·이행:** 스키마 **v15** — 기준에 의무·출처·결론 요구, 결과에 결론, 의도 버전에 목적 목록, 후보에 목적별 현황. 전부 열거형 `CHECK`, 본문 없음. v14→v15 는 컬럼만 더하고 **의무를 지어내지 않으며** Profile 정의판을 올리지 않는다. 반복 이행이 멱등이다.
- **기존 시험의 의미 검토:** v2 새 Case 에서 방식 없이 `met` 을 적던 시험 10건을 **의미를 검토해** 방식을 명시하도록 고쳤다(기능 Case 는 `changed_and_verified`, `kind=analysis` Case 는 `investigated`+`determined`). 그중 `test_progression` 의 두 곳은 거부된 `met` 을 모른 채 지나가 **다른 상황을 시험하고 있었다** — 응답을 단정하도록 고쳤다. v1 동작은 새 호환 시험으로 따로 고정했다. 검사를 지우지 않았다.
- **화면:** 결과 기록 폼에 **충족 방식 선택이 아예 없었다**(P3-R4가 API 에만 넣었다). v2 에서 방식이 필수가 되면 화면으로는 새 Case 의 `met` 을 적을 수 없으므로 의무별 방식·결론 선택을 넣었다. 목적별 완료 의미·실험 정리·후보의 누락 목적/잔여를 보인다. 사람이 입력하는 의도 초안 화면에는 목적 선언 입력을 넣지 않았다(API 는 받는다).
- **검증:** P4-03 집중 **29 통과**, 이행 집중 **10 통과**(v14→v15 신규 1건), 전체 **pytest 481 + P1 계약 unittest 18 통과**, `npm run build` 성공. 파일 DB 재개로 의무·결론·목적·후보 meaning 복원 확인. 알려진 deprecation warning 2건 외 실패 없음.
- **실제 CLI:** **수행.** 새 AI 출력 계약(의도 문서 v5 의 목적·의무·결론 요구)이므로 `p4/live/profile_intents.py` 로 실제 `codex-cli 0.154.0` 의도 작성 **3회**를 돌렸다(합성 저장소). defect-fix+원인 확정 요청은 `objectives: ["cause"]`·원인 기준 `definitive_required`, research+"판단 불가도 충분"은 조사 기준 전부 `bounded_report_allowed`, refactoring 은 보존 기준 **둘**을 개선 기준과 따로 적었다. 세 건 모두 지시대로였고 의무는 전부 원문이 명시했다(`reported`). 관찰: codex 가 요구되지 않은 보존 기준을 스스로 더했다 — 완료 조건을 늘리는 쪽이며 느슨해지는 일탈은 없었다. 표본은 세 건뿐이다. 근거는 [P4-03 결과](p4/evidence/P4-03-results.md) 3절
- **경계:** **강제 축은 넷 그대로다**(`publish` 만 P5). 조사 Profile 의 제품 수정 차단(D-66)은 목적 선언으로 풀리지 않는다. P4-04 컨텍스트·재개, P4-05 완료·예외 전반, P4-06~07 지식, P5 push·PR 은 구현하지 않았다. **Profile 재분류 경로도 만들지 않았다**(plan 3.8절, 9절 표).
- **사용자 지시로 한 일:** 작업을 마친 뒤 사용자 지시로 P4-03 변경을 **`bf86590` 으로 커밋해 `origin/main` 에 push** 했고, 이 인계 기록을 이어서 커밋·push 했다. 커밋 뒤 `tests/test_migration.py` 를 다시 돌려 `_v14_schema()` 가 커밋 이력에서 v14 스키마를 찾는 것을 확인했다(10건 통과, 건너뜀 없음). 허용은 이 변경에만 적용된다. PR 은 만들지 않았다.
- **다음 행동:** **P4-04(컨텍스트·재개)** 를 새 plan 으로 시작한다. 완료된 P4-PLAN-01~03 을 다시 열지 않고, P4-05 이후로 자동 확대하지 않는다.
- **사람에게 물어야 할 것:** 없다. P4-04 는 문서에 확정된 범위로 plan 을 쓸 수 있다. 새 제품 판단이 생길 때만 질문한다.
- **작업공간:** 시작 `main`/`f6145f4`, clean·`origin/main` 과 같음. 이 세션이 그 위에 P4-03 커밋 `bf86590` 과 인계 기록 커밋을 올렸다 — 새 파일 `domain/completion_meaning.py`, `tests/test_profile_completion.py`, `plans/P4-PLAN-03.md`, `p4/evidence/P4-03-*`, `p4/live/profile_intents.py` 와 수정 파일 `controller/{api,db,repository,schema.sql}`, `domain/{intent_doc,models,profiles,progression}.py`, `runner/{agent,prompts}.py`, `tests/{conftest,test_completion,test_data_boundary,test_migration,test_progression,test_restart_recovery,test_results}.py`, `web/src/{api.ts,ResultPanel.tsx}`, `DEVELOPMENT.md`. 실제 커밋은 `git log --oneline f6145f4..HEAD`, 원격 반영은 로컬/원격 ref 로, 작업 트리는 `git status --short` 로 재확인한다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다(`Get-CimInstance Win32_Process` 로 uvicorn/runner.agent 가 남지 않음을 확인). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p4-03-live\130017994` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p4/evidence/P4-03-live*` 에 있다.
- **다음 세션이 이어서 할 때:** `tests/test_migration.py` 의 `_v14_schema()` 는 `스키마 v15` 표식이 **없는** 가장 최근 커밋 스키마를 찾는다 — P4-03 을 커밋하기 전에도 뒤에도 `f6145f4` 의 스키마를 고른다. 실제 v14 DB 에 있는 `case.profile*` 컬럼은 `db.py` 가 붙이던 것이라 시험이 직접 더한다. 그리고 **v2 기준에 `met` 을 적는 시험은 방식을 함께 적는다** — 도우미 `met_satisfaction_for()`(conftest)가 의무별 기본값을 준다.

### 이전 인계 — S-018 / 2026-09-22 / P4-02 변경·예약 (**완료**)

- **수행:** 먼저 P4-01의 상태를 **직접 재확인**했다(이전 세션이 시험 도중 멈춰 인계 수치의 진위를 믿지 않았다) — `scripts\run-tests.ps1` 로 pytest 430 + unittest 18, `npm run build`, 스키마 v13 이 인계와 일치했다. 그 뒤 [P4-PLAN-02](plans/P4-PLAN-02.md)를 구현 전에 기록·공유하고 검증 1회 경계, 설정 변경 예약·적용 시각·취소, 부분 재검증, 늦은 결과, 배정 직전 재확인, API·화면·v14 이행을 구현했다. 상세 근거는 [P4-02 결과](p4/evidence/P4-02-results.md)다.
- **사용자 지시로 한 일:** 이 세션은 **두 번 커밋·push 했다.** 먼저 P4-01 변경을 `eaae2d9` 로, 작업이 끝난 뒤 P4-02 변경을 따로 커밋해 `origin/main` 에 올렸다. 각 허용은 그 커밋에만 적용되며 다음 변경에는 확대되지 않는다. P4-01 커밋 전에는 이 세션의 P4-02 착수분을 작업 트리에서 **분리**하고 순수 P4-01 상태에서 전체 시험을 다시 통과시킨 뒤 커밋했으며, 커밋 후 P4-02 작업을 복원했다. PR 은 만들지 않았다.
- **예약 경계:** 그 범위에 진행 중 검증이 있으면 변경이 `pending` 으로 예약되고 없으면 즉시 적용된다. 예약 행은 유효 정책에 섞이지 않고 진행 중 검사를 멈추지도, 그 결과를 무효화하지도 않는다. Case 전체 변경은 그 게이트의 어느 Task 검증이든 기다리고 Task 범위 변경은 그 Task 만 기다린다. 종료 시 `현재 결과 보존 → 예약 적용 → 효과 재평가` 를 한 트랜잭션에서 지키며 **판정이 실패여도 반영한다.**
- **좁힌 것:** `needs_recheck` 를 만드는 것을 **검사 강도 변경 하나**로 좁혔다. P4-01은 `setting` 이 바뀌기만 해도 내렸는데 ON/OFF 토글은 이미 끝난 검사가 본 것을 바꾸지 않는다. 구현 전에 plan 4절에 이유를 적고 AC-8을 고쳤다.
- **OFF 의 효과:** 반영된 OFF 는 그 게이트의 새 `product_repair` 차수 배정만 거부한다. 주기·사용량·발견·실패 판정은 남고 다시 ON 해도 초기화되지 않는다. 환경 복구는 막지 않는다.
- **변경 영향:** 요청·코드·권한 변경을 `quality_change_event` 로 받아 실행마다 `revalidate | reuse | unknown` 을 정하고 `quality_revalidation` 에 이유·참조 버전과 함께 남긴다. **재사용도 판단이라 기록한다.** 범위를 모르면 `unknown` 이며 재검증 대상이다. 진행 중 검증은 무효화 대신 **취소를 요청**한다.
- **늦은 결과·배정:** 취소 요청 뒤·OFF 뒤·시작 리비전 불일치·더 새로운 판정 존재가 늦은 결과다. 원래 실행에 `historical`·`late_result = 1` 로 저장되고 repair 를 만들지 않는다. 배정 요청이 `expected_gate_policy` 를 적었는데 바뀌었으면 새 사유 `quality_gate_policy_changed` 로 거부하며, **예약만 있는 상태는 이 사유가 아니다.**
- **저장·이행:** 스키마 **v14**. 정책에 요청/적용 시각·예약 대상 실행·적용 경계·취소 삼종, 실행에 시작 리비전·late·취소 요청 삼종을 더하고 `quality_change_event`·`quality_revalidation` 을 추가했다. v13→v14는 적용 시각만 채우고 예약·취소·재검증을 만들지 않으며 `started_policy_revision` 은 **NULL(미기록)** 이다 — 0으로 적지 않는다. 반복 이행이 멱등이다.
- **검토 중 수정:** 종료된 Case 에 변경 사건이 열려 있던 것을 `guard_open_case` 로 막았고(닫힌 Case 의 판정이 나중에 바뀌면 종료 기록이 조용히 달라진다), `changed_refs_json` 에 DB `CHECK` 를 더했으며, 시작만으로 이전 통과를 `historical` 로 내리던 초안을 고쳐 대체를 종료 시점으로 옮겼다.
- **검증:** P4-02 집중 **20 통과**, 이행 집중 **9 통과**(v13→v14 신규 1건), 전체 **pytest 451 + P1 계약 unittest 18 통과**, `npm run build` 성공. 파일 DB 재개로 예약·재검증·늦은 결과 복원을 확인했다. 알려진 deprecation warning 2건 외 실패 없음.
- **실제 CLI:** **수행하지 않았다.** P4-02는 새 AI 출력 계약·프롬프트를 만들지 않아 확인할 새 계약이 없다. P4-01의 `codex-cli 0.154.0` 실행이 검토 계약의 근거로 남는다. 하지 않은 시험을 통과로 적지 않는다.
- **경계:** P4-03의 여섯 Profile, P4-04의 컨텍스트·재개, P4-05의 완료·예외, P4-06~07의 지식/QG-08, P5의 push·PR은 구현하지 않았다. P3 경로 C 스킵도 그대로다. **강제 축은 넷 그대로이며** P4-02가 더한 것은 그 위의 시간축이다.
- **다음 행동:** **P4-03(여섯 Profile)** 을 새 plan으로 시작한다. 완료된 P4-PLAN-01·02를 다시 열지 않고, 현재 정책 리비전·예약·재검증 판정·repair 누적을 입력으로 삼는다. P4-04 이후로 자동 확대하지 않는다.
- **사람에게 물어야 할 것:** 없다. P4-03은 문서에 확정된 범위로 plan을 쓸 수 있다.
- **작업공간:** 시작은 `main`/`8f50bcb` 였고 이 세션이 그 위에 **두 커밋**을 올렸다(P4-01 `eaae2d9`, 그리고 P4-02). 작업 트리는 커밋 뒤 clean 이며 `origin/main` 과 같다. 별도 라이브 서비스나 장기 실행 프로세스는 남기지 않았다.

### 이전 인계 — S-017 / 2026-09-22 / P4-01 게이트·repair (**완료**)

- **수행:** [P4-PLAN-01](plans/P4-PLAN-01.md)을 구현 전에 기록·공유하고 QG-01~07의 유효 정책, Case/Task override, 조건부 별도 의미 검토, 일반 게이트 판정, 기본 2회 repair, API·화면·v13 이행을 구현했다. 상세 근거는 [P4-01 결과](p4/evidence/P4-01-results.md)다.
- **정책 경계:** QG-01은 기존 `gate_result`·`conformance_check`를 연결할 뿐 일반 표로 복제하지 않는다. QG-01 OFF와 요구 검사 강도 하향은 거부된다. QG-02~07은 추천과 명시 설정을 구분하며 Task 명시가 Case 명시보다 우선한다. `off`·`not_applicable`·`not_run`·`pass`·마지막 실패는 합치지 않는다.
- **검토 경계:** 일반 독립 검토는 reviewer 역할의 `quality_gate_review`, 작성자와 다른 session, 요청/기준/대상/증거 참조가 있어야 채택된다. 판정은 발견에서 계산하며 AI가 기존 기준에 연결하지 못한 의견은 필수 요구가 아니라 advisory다. 실제 `codex-cli 0.154.0` read-only 실행 1회가 계약 JSON을 반환했고 저장소를 바꾸지 않았다.
- **repair:** 최초 실패는 사용 0회로 주기를 만들고 제품 repair 시작이 차수를 예약한다. 기본 한도 2, 세 번째는 거부된다. 환경 복구는 소비하지 않고 Task 재분할·세션 교체·게이트 OFF/ON은 누적을 초기화하지 않는다. 사람의 추가 허용은 한도 상향 리비전이며 이미 쓴 횟수를 보존한다.
- **저장·이행:** 스키마 **v13**에 `quality_gate_policy`·`quality_gate_run`·`quality_gate_finding`·`remediation_cycle`·`remediation_attempt`를 추가했다. 본문 컬럼은 없고 참조/요약 경계를 런타임과 DB 제약으로 검사한다. v12→v13은 기존 QG-01 실패를 보존하고 새 통과·repair를 만들지 않으며 반복 이행이 멱등이다.
- **검토 중 수정:** 명시 게이트 차단 조회가 현재 그래프에 없는 Task를 404로 바꾸던 순서를 고쳐 기존 구조화된 진입 거부를 보존했다. 발견의 선택적 증거 참조와 repair 세션 참조도 내부 호출에서 본문형 값을 넣을 수 없게 보강했다.
- **검증:** P4-01+이행 집중 **19 통과**, 전체 **pytest 430 + P1 계약 unittest 18 통과**, `npm run build` 성공. 파일 DB 재개로 정책·실패·repair 사용량 복원을 확인했다. 알려진 deprecation warning 2건 외 실패 없음.
- **경계:** P4-02의 실행 중 설정 변경 예약·부분 재검증, P4-03의 여섯 Profile, P4-06~07의 지식/QG-08, P5의 push·PR은 구현하지 않았다. P3 경로 C 스킵도 그대로다. 기존 네 `enforcement` 축과 게시 미강제 경계는 유지된다.
- **다음 행동:** **P4-02(변경·예약)** 를 새 plan으로 시작한다. 완료된 P4-PLAN-01을 다시 열지 않고, 현재 정책/판정 리비전과 repair 누적을 입력으로 삼는다. P4-03 이후로 자동 확대하지 않는다.
- **사람에게 물어야 할 것:** 없다. P4-02는 문서에 확정된 범위로 plan을 쓸 수 있다. 새 제품 판단이 생길 때만 질문한다.
- **작업공간:** 시작과 현재 HEAD는 `main`/`8f50bcb`, `origin/main`과 같다. P4-01 변경은 작업 트리에 **미커밋**으로 남아 있다. commit·push·PR·외부 게시 허용은 받지 않았고 수행하지 않았다. 별도 라이브 서비스나 장기 실행 프로세스는 남기지 않았다.

### 그 이전 인계 — S-016 / 2026-09-22 / P3-04 마무리 (**완료, 제한 있음**)

- **수행:** 사용자 지시로 범위를 좁혀 **경로 B(controlled) 라이브와 AC-19(강제 종료 복원)** 를 수행했다. **경로 C(예산 도달)와 환경 제약으로 불가한 검증 실행은 지시대로 스킵**했다. 구현 전에 [P3-PLAN-04](plans/P3-PLAN-04.md) 12절에 범위 축소·AC-19 위치 변경·경로 B 종료 절차 수정을 기록했다.
- **사람에게 확인받은 것:** 없다. 범위 축소가 **사용자 지시**였고 새 제품 판단은 없었다.
- **경계:** **강제 축은 늘지 않았다.** `enforcement` 는 여전히 넷이 `enforced` 이고 `publish` 만 P5 다. 제품 코드에서 바뀐 것은 **지시문 한 곳**(빈 항목 규칙)이며 정책·스키마·진입 검사는 그대로다. 스키마는 **v12** 그대로다.
- **경로 B 가 확인한 것:** 시작 확인 전 **다섯 목적 전부**가 `controlled_start_not_confirmed` 로 막히고 조사는 열려 있다. 확인 뒤 **그 사유만** 사라지고 다른 사유는 남는다. 준비의 양은 `deep` 이 정하고(설계+계획·독립 검토) 단계 검토는 controlled 에서도 `auto_proceed` 다 — **controlled 가 더하는 것은 확인 둘뿐이다.** 일곱 Task 가 두 저장소를 나눠 갖고 전부 실행됐고 대상이 틀린 실행은 `run_task_repository_mismatch` **단독**으로 막혔다. 조합이 두 저장소를 `run_effect` 로 담았다. 두 저장소의 사용자 미커밋 변경은 전 과정 뒤에도 동일하다.
- **AC-19:** 조합·기준 기록 뒤 제어부를 **kill** 하고 다시 띄우자 Task 저장소(v12)·조합·기준 판정·예약 **67건**이 전부 그대로였고, **그 뒤 종료까지 흐름이 계속됐다.** 시간 지표는 조회 시점 계산이라 대조에서 뺐다.
- **가장 중요한 결과:** **같은 미검증 상태에서 두 경로가 다르게 끝났다.** 경로 A(자동 완료)는 열리지 않았고, 경로 B(controlled)는 확인 없는 인수가 `controlled_result_not_confirmed`·`unresolved_criteria` 로 거부된 뒤 **사람이 미검증 4건을 예외로 수용해** `closed_with_exceptions` 로 닫혔다. **기준은 종료 뒤에도 `unverified` 다** — 예외는 "알고 받아들인다"이지 충족이 아니며, 이 Case 를 검증된 완료로 읽으면 안 된다. 그리고 확인한 후보가 바뀌자(늦은 의견) 이전 확인이 `controlled_confirmation_stale` 로 쓰이지 않았고, 같은 내용으로 돌아오자 **재확인 없이** 열렸다.
- **라이브가 찾은 제품 결함 하나:** 계획 실행이 `open_questions` 에 "없다"를 적고 origin 을 `none` 으로 둬서 문서 생성이 거부되고 실행 전체가 실패했다. **거부가 맞고 지시문이 틀렸다** — 비우는 방법을 말하지 않으면 AI 가 빈칸을 문장으로 채운다. `EMPTY_SECTION_RULE` 을 설계·계획·결합 기록 지시문에 넣고 짝 시험 둘을 더했다(지시문이 말하는지 + 그래도 `compose` 가 막는지). **추측으로 고치지 않았다** — origin 을 바꾸면 없는 출처를 만들고 본문을 지우면 내용을 버린다.
- **하네스가 찾은 것:** 확인 지점 ≠ 최종 인수(`path_bc.py` 가 한 동작으로 적고 있었다), QG-01 재작성의 피드백은 **사람이** 반영 판단으로 닫아야 종료가 열린다(막은 것이 맞다), 작업공간 없는 라이브는 Runner 등록을 기다려야 한다, 로그 실패가 프로세스 정리를 건너뛰지 않게 `finally` 를 나눴다, 종료 기록의 필드는 `closure_kind` 다.
- **검증:** **pytest 418 + P1 계약 unittest 18 통과**(경로 A 보고의 415 → 관측 416 → +2), `npm run build` 성공. 경로 B 는 **네 번** 돌렸고 마지막 회차가 한 줄로 끝까지 갔다(앞 세 회차의 실패는 인코딩·하네스 키·피드백 미처리·지시문이었고 제품 정책 위반은 없었다).
- **다음 행동:** **P4-01(게이트·repair)** 을 새 plan 으로 시작한다. P3 로 되돌아갈 것은 경로 C 라이브 하나이며 사용자가 스킵을 지시했다. P4-02 이후로 자동 확대하지 않는다.
- **사람에게 물어야 할 것:** 없다. **커밋·push 는 사용자 지시로 이미 했다**(그 허용은 이 변경에만 적용된다). P4-01 은 새 제품 판단 없이 시작할 수 있고, plan 은 먼저 기록·공유한다.
- **작업공간:** 시작 `main`/`905d6c0`, clean·`origin/main` 과 같음. 아래를 **사용자 지시로 커밋하고 `origin/main` 에 push했다** — 새 파일 `p3/live/controlled_purposes.py`, `p3/evidence/P3-04-B-*`(로그·JSON·의도 원문 14건), 수정 파일 `runner/prompts.py`(빈 항목 규칙), `tests/test_ai_draft.py`(+2), `p3/live/{driver,path_a,path_bc}.py`(하네스), `plans/P3-PLAN-04.md`, `p3/evidence/P3-04-results.md`, `DEVELOPMENT.md`. 실제 커밋은 `git log --oneline 905d6c0..HEAD`, 원격 반영은 로컬/원격 ref로, 작업 트리는 `git status --short` 로 재확인한다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 모두 종료했다(`Get-CimInstance Win32_Process` 로 확인 가능). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-04-live\{path-a,path-b,controlled-purposes}` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-04-*` 에 있다.
- **다음 세션이 이어서 할 때:** 라이브 하네스를 다시 쓸 때 셋을 기억한다 — 출력은 **UTF-8 강제**로 돌린다(`PYTHONUTF8=1`, 아니면 로그 한 줄이 인코딩으로 죽는다), 재작성이 있으면 **피드백을 사람이 닫는다**, 실행 id 는 회차마다 달라야 한다. 그리고 `tests/test_migration.py` 의 옛 스키마 조회는 여전히 커밋 이력에서 찾는다.

### 이전 인계 — S-015 / 2026-09-22 / P3-04 수직 통합 (경로 A)

- **수행:** [P3-PLAN-04](plans/P3-PLAN-04.md)를 기록·공유하고 **시험 기능을 사용자에게 확인받은 뒤**(안 A — 생산자→소비자 계약 변경) 구현·검증했다. 방법은 계획대로 **먼저 끝까지 돌려 보고 막히는 자리를 입력으로 삼는** 순서였다. 스키마 **v12**(`task.repository_id`·`task.repository_ref` 컬럼 둘, 새 표 없음).
- **사람에게 확인받은 것:** **P3-04의 실제 시험 기능**. 세 안(계약 변경 / 결함 수정 / 유틸 추출) 중 첫째다. 두 저장소가 서로 맞아야 끝나고 수준이 `deep` 으로 나오기 쉬워 `deep + ask-on-decision` 을 함께 볼 수 있기 때문이다.
- **경계:** **강제 축은 늘지 않았다.** `enforcement` 는 여전히 넷이 `enforced` 이고 `publish` 만 P5 다. Task×Repo 는 새 축이 아니라 `repository_selection` 이 이미 강제하던 것의 **대조 대상**을 정확히 한 것이다. 다중 Repo 통합 증거의 **강제**는 만들지 않았다(P4-01).
- **라이브가 찾은 것 여섯.** 전부 실제 흐름에서만 나오는 것들이다. (1) **질문에 답해도 그 답이 다시 쓰는 실행에 닿지 않았다** — 답은 기록돼 있었지만 어떤 실행의 입력도 아니었다. (2) **사람이 답한 것을 AI 가정으로 적었다** — origin 규칙에 반대 방향이 없었다. (3) **QG-01 이 요청과 의도를 대조한다면서 요청을 받지 못했다** — 의미 검토의 고정 컨텍스트가 비어 있었다. (4) **공유 연결의 동시 쓰기가 요청 하나를 500 으로 죽였다** — R3 의 `BEGIN IMMEDIATE` 와 스레드풀이 만나는 자리이며 **사람이 볼 수 있는 사유가 없는 실패**다. (5) **재작성 버전의 검토에 후속 지시를 요청으로 줬다.** (6) **코드를 바꾸는 실행이 고정 컨텍스트를 하나도 받지 못했다** — 호출자가 붙인 문자열이 전부였다. 상세는 [P3-04 결과](p3/evidence/P3-04-results.md) 6절.
- **확인된 것:** `deep` 이 준비를 늘리고 `ask_on_decision` 이 확인 지점을 정하는 **조합이 실제로 돌았다**(설계+계획 두 건·독립 검토 필수, 그러나 사람 검토 없음·자동 완료 모드). 일곱 Task 가 저장소를 나눠 갖고 **계획이 정한 순서대로** 전부 실행됐고, 대상이 틀린 실행은 `run_task_repository_mismatch` **단독 사유**로 막혔다. 조합이 두 저장소를 `run_effect` 로 담았다. **사람 결정은 셋**(의도 동의 2, 누적 변경 확인 1)이고 설계·계획·구현·검증·완료에는 없다. 두 저장소의 사용자 미커밋 변경은 전 과정 뒤에도 동일하다.
- **가장 중요한 결과:** **검증이 돌지 않자 Case 가 닫히지 않았다.** 이 호스트의 CLI sandbox 가 Python 에 닿지 못해 네 검증 실행이 모두 명령을 실행하고 실패했고, 기준은 `unverified` 로 남았으며 결과 후보도 종료 기록도 만들어지지 않았다. R4 의 "여는 쪽이 더 위험하다"의 반대편 증거이며 `auto_on_conditions` 가 **조건을 지난다**는 뜻이다. **같은 세션의 앞선 시도는 정확히 반대로 끝났다** — 하네스가 거짓 근거를 먹이자 Case 가 자동 완료됐다. 근거의 진위는 여전히 시스템이 판정하지 않는다(P4-01).
- **검증:** **pytest 415 + P1 계약 unittest 18 통과**(R4 시점 395 → +20), `npm run build` 성공, 커밋된 v11 스키마로 만든 실제 DB 의 v12 이행. 동시성 시험은 **잠금을 지우면 같은 예외로 실패하는 것을 확인하고** 넣었다.
- **다음 행동:** **P3-04 를 이어서 한다.** 남은 것은 (1) **경로 B — controlled** `\.venv\Scripts\python.exe p3\live\path_bc.py --path b`, (2) **경로 C — 예산 도달** `--path c`, (3) 제어부 강제 종료 후 v12 기록 복원. 스크립트는 이미 있고 경로 A 와 같은 흐름을 탄다. P4 를 먼저 시작하지 않는다.
- **사람에게 물어야 할 것:** 없다. **커밋·push 는 사용자 지시로 이미 했다**(그 허용은 이 변경에만 적용된다). 남은 경로 B·C 는 새 제품 판단 없이 진행할 수 있다.
- **작업공간:** 시작 `main`/`ae52816`, clean 관측. 아래를 **사용자 지시로 커밋하고 `origin/main`에 push했다** — 새 파일 `plans/P3-PLAN-04.md`, `tests/test_task_repository.py`, `p3/live/{driver,fixtures,path_a,path_bc}.py`, `p3/evidence/P3-04-*` 와 수정 파일들(`controller/{admission,api,db,repository,schema.sql}`, `domain/{models,prep_doc}.py`, `runner/{agent,prompts}.py`, `tests/{conftest,test_data_boundary,test_migration,test_preparation,test_repositories}.py`, `web/src/{api.ts,WorkGraphPanel.tsx}`). 실제 커밋은 `git log --oneline ae52816..HEAD`, 원격 반영은 로컬/원격 ref로 확인한다. 인계 소비 시 실제 상태를 재확인한다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다(스크립트가 `finally` 에서 정리한다). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-04-live` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-04-*` 에 있다. R1~R4 의 라이브 데이터도 그대로 있다.
- **다음 세션이 이어서 할 때:** 작업 트리는 clean 이고 `origin/main` 과 같다. `tests/test_migration.py` 의 옛 스키마 조회는 **커밋 이력**에서 찾는다(`_v11_schema()` 는 `스키마 v12` 표식이 없는 가장 최근 `schema.sql` 을 고른다) — **push 뒤 재실행해 확인했다**(7건 통과). 그리고 라이브 하네스를 다시 쓸 때 셋을 기억한다: 검토의 지시 원문은 **의도 문서 자체**여야 하고, 실행은 **그래프가 정한 순서**로 요청해야 하며, 실행 id 는 **회차마다 달라야 한다**(멱등 함정).

### 이전 인계 — S-014 / 2026-09-22 / P3-R4 자동 실행·진입·완료 경로

- **수행:** [P3-PLAN-R4](plans/P3-PLAN-R4.md)를 기록·공유하고 **사람에게 물어야 할 것 하나를 확인받은 뒤** 구현·검증했다. 판정을 `domain/progression.py` 한 곳에 두고(유효 Autonomy·Fast Lane·정합성 방식·delta 분류), 진입 검사·완료 검사·화면이 같은 함수를 본다. 스키마 **v11**(`material_delta`·`conformance_check` 두 표, 새 컬럼 다섯).
- **사람에게 확인받은 것:** **`autonomy = NULL`(R1 이전) Case 를 controlled 로 취급한다**(사용자 결정 2026-09-22). 세 안(v0.6 유지 / 진행 차단 / controlled 취급) 중 셋째다. **구현은 적용과 기록을 나눴다** — 저장된 값은 `NULL`·`migrated_unknown` 그대로이고 도출된 `effective_autonomy` 만 `controlled` 이며 출처가 `migrated_unknown_treated_as_controlled` 다. R1 의 "미기록을 기본값으로 읽지 않는다" 시험은 **갱신 없이 그대로 통과한다.**
- **경계:** **이 단계에서 강제로 바뀐 것은 `autonomy` 와 `controlled_checkpoint` 둘이다.** 이제 `enforcement` 에서 `enforced` 가 넷이고 **`publish` 만 P5 로 남았다.** 네 축이 강제된다고 게시 권한이 생기지 않으며, controlled 의 결과 후보 확인도 push·게시 권한을 만들지 않는다(D-65). 지식 적용은 P4-06, 추가 게이트는 P4-01, 외부 CI 는 P5 다.
- **방향이 둘이라는 것:** R1~R3 의 강제는 전부 "막는" 방향이었다. R4 는 `controlled` 를 막고 기본 `ask-on-decision` 을 **연다**. **여는 쪽이 더 위험하다** — 조건을 갖추지 않았는데 열리면 아무도 보지 않은 결과가 완료가 되고, 조건을 갖췄는데 사람 인수 기록을 함께 만들면 있지도 않은 확인이 근거로 남는다. 자동 완료는 미해결 기준·질문·피드백·미정리 실행을 전부 지나야 하고 인수는 `mode = auto_policy`·actor = 정책 식별자다.
- **보존:** **v0.6 기본값 둘이 뒤집혔지만 기존 Case 에는 소급하지 않는다.** 완료(사람 인수 → 조건 충족 자동)와 단계 검토(사람 검토 → 자동 진행)이며, 경계는 **명시 설정 행**(v11 이행이 `migrated_explicit` 로 적었다)과 **Autonomy 미기록 Case**(단계 검토는 v0.6 그대로)다. 옛 게이트 통과는 `conformance_check` 에 `independent` 로 옮겼다 — 지어내는 것이 아니라 R4 이전의 `pass` 가 AI 검토 없이는 나올 수 없던 값이었기 때문이다. `criterion_result.satisfaction` 은 **비워 둔다** — 옛 판정이 어떻게 충족됐는지는 그때 묻지 않았다.
- **검증:** **pytest 395 + P1 계약 unittest 18 통과**(R3 시점 341 → +54), `npm run build` 성공, 제어부 **실제 강제 종료** 후 확인 지점·정합성 방식·완료 도출 복원, 커밋된 v10 스키마로 만든 실제 DB 의 v11 이행. 라이브는 실제 제어부·Runner 프로세스와 **실제 codex**로 controlled 차단·해제, 조건부 독립 검토, Fast Lane 의 가벼운 확인, 기본 자동 완료를 확인했다. 근거는 [P3-R4 결과](p3/evidence/P3-R4-results.md).
- **라이브가 찾은 결함:** 결합 기록의 지시문에 **출력 JSON 형식을 빠뜨렸다.** "출력 형태는 개발계획과 같다"는 한 줄로 끝냈더니 실제 codex 가 자기 나름의 형식(`doc_type: hads.combined-record`, `sections` 를 목록으로)을 내고 구조 보고가 실패했다. 자동 시험은 `FakeCliExecutor` 가 올바른 형식을 내므로 이 구멍을 볼 수 없다. 설계·계획과 같은 JSON 템플릿을 넣고 `sections` 가 객체여야 함을 명시했다. 상세는 결과 문서 6절.
- **구현 중 찾은 결함 셋:** (1) `record_exception_decision()` 이 완료 모드가 자동이면 **사람의** 예외 수용까지 거부했다 — 자동이 기본이 되면 "기본 Case 에서는 사람이 예외를 수용할 수 없다"가 되어 D-32 와 어긋난다. 막아야 하는 것은 자동 정책이 **스스로** 수용하는 것 하나이며 그 검사는 그대로 있다. (2) 자동 진행을 조건 충족 시점에 기록하자 **그 뒤의 사람 검토가 조용히 삼켜졌다** — 사람 검토가 더 강한 사실이므로 그쪽이 남게 했다. (3) `create_intent_version()` 이 새 버전마다 **모든** 판정을 `needs_recheck` 로 내려, "영향받은 것만"을 붙일 입력 자체가 지워지고 있었다 — 판단을 구조 보고 뒤로 옮겼다.
- **다음 행동:** 실제 Git 상태 확인 → P3-04 범위와 [완료](completion-lifecycle.md)·[Profile](case-profiles.md)·P3-R4 결과 확인 → **P3-PLAN-04 작성·공유 → P3-04 구현·검증**. R1~R4 의 모델을 재설계하지 않고 그 위에서 **실제 작은 기능**을 만든다 — 같은 Runner 2 저장소 연동과 `deep + ask-on-decision` 조합, controlled·예산 대기 대표 경로를 실제로 돌린다. 외부 push·PR·CI 는 P5 이며 통과로 가장하지 않는다.
- **사람에게 물어야 할 것:** **P3-04 의 실제 시험 기능이 무엇인가.** 그 선택은 P3-04 plan 에서 정한다(인계가 그렇게 지정했다).
- **작업공간:** 시작 `main`/`b45fc60`, clean 관측. 새 파일 `domain/progression.py`, `tests/test_progression.py`, `plans/P3-PLAN-R4.md`, `p3/evidence/P3-R4-*` 와 수정 파일들(`controller/{admission,api,db,repository,schema.sql}`, `domain/{models,prep_doc}.py`, `runner/{agent,prompts}.py`, `tests/{conftest,test_budget,test_completion,test_data_boundary,test_migration,test_policy,test_preparation,test_restart_recovery,test_results,test_work_graph}.py`, `web/src/{api.ts,PolicyPanel.tsx,ResultPanel.tsx}`)을 **사용자 지시로 커밋하고 `origin/main`에 push했다**(그 허용은 이 변경에만 적용된다). 실제 커밋은 `git log --oneline b45fc60..HEAD`, 원격 반영은 로컬/원격 ref로 확인한다. 인계 소비 시 실제 상태를 재확인한다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다(스크립트가 `finally` 에서 정리한다). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-r4-live` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-R4-*` 에 있다. R1~R3 의 라이브 데이터도 그대로 있다.
- **다음 세션이 이어서 할 때:** 작업 트리는 clean 이고 `origin/main` 과 같다. `tests/test_migration.py` 의 옛 스키마 조회는 작업 트리가 아니라 **커밋 이력**에서 `git show` 로 찾는다(`_v7_schema`·`_v8_schema`·`_v9_schema`·`_v10_schema`). 새로 더한 `_v10_schema()` 는 `material_delta` 가 없는 가장 최근 커밋을 고르므로 v11 을 커밋한 뒤에는 `41252ab` 의 스키마를 찾는다 — **push 뒤 재실행해 확인했다**(6건 통과). 그리고 **`FakeCliExecutor` 가 올바른 형식을 내는 것이 지시문의 정확성을 보장하지 않는다**(위 라이브 결함). 새 지시문을 만들면 실제 CLI 로 한 번 돌려 본다.

### 그 이전 인계 — S-013 / 2026-09-22 / P3-R3 예산 예약·집계·정지

- **수행:** [P3-PLAN-R3](plans/P3-PLAN-R3.md)을 기록·공유한 뒤 구현·검증했다. 예약 계약(`domain/budget.py`)을 새로 두고, Run 생성 트랜잭션 안에서 원자적으로 검사·예약하며, 종료 후 지표별로 정산한다. 스키마 **v10**(`budget_reservation` 한 표). hard 도달은 그 한도를 소비하는 새 실행과 **재배정**을 막고, 정지는 저장하지 않고 도출한다. 화면에 노출·미확정·정지 사유와 **보장 범위**를 보인다.
- **경계:** **이 단계에서 강제로 바뀐 것은 `budget` 하나다.** Autonomy·확인 지점은 R4, **게시 허용과 실제 push·PR은 P5**다. 그리고 "강제한다"가 지표마다 같은 약속이 아니다 — 실행 수·검토 수·컨텍스트 크기만 `enforced_absolute` 이고, 시간은 `enforced_no_absolute_cap`(새 배정만 차단), 토큰·비용의 hard 는 여전히 **설정 거부**다. `tests/test_policy.py` 가 남은 두 축의 "강제 없음"을 계속 고정한다 — R4가 강제를 붙일 때 그 시험을 갱신해야 한다.
- **보존:** 한도를 설정하지 않은 Case 는 그대로 무제한이고 예산으로 아무 것도 막히지 않는다. v10 이행은 **R3 이전에 실제로 돈 실행에도 예약 행을 만든다**(`source = migrated_from_run`) — 그러지 않으면 스키마를 올리는 것만으로 소비가 0 이 된다. 토큰을 보고하지 않은 옛 실행은 `NULL`·`unavailable` 이며 **0 이 아니다**. R1 시절 설정 행의 `recorded_not_enforced` 는 고쳐 쓰지 않고 조회가 현재 판정을 따로 얹는다. 멱등성이 예산보다 먼저다 — 같은 `run_id` 의 재전송은 예산이 소진돼도 기존 실행을 돌려준다.
- **검증:** **pytest 341 + P1 계약 unittest 18 통과**(R2 시점 313 → +28), `npm run build` 성공, 제어부 **실제 강제 종료** 후 한도·소비·예약 복원, 커밋된 v9 스키마로 만든 실제 DB의 v10 이행. 두 연결·두 스레드로 **마지막 한 칸의 경쟁**을 확인했다(하나만 생성). 라이브는 실제 제어부·Runner 프로세스와 **실제 codex 1회**(codex-cli 0.154.0)로 사용량 집계·정산, hard 도달 거부, 한도 상향 후 재개를 확인했다. 근거는 [P3-R3 결과](p3/evidence/P3-R3-results.md).
- **라이브가 찾은 결함:** codex 가 `input_tokens: 30600` 을 보고했는데 **집계의 확정값이 0 이었다.** 정산의 확정 조건을 실행 단위로 잡고 `residual_activity == "none"` 을 요구했는데, 실제 어댑터는 그 값을 **항상** `unknown` 으로 보고한다(P1-03). 판단을 **지표 단위**로 옮겼다 — 잔류 프로세스가 더 늘릴 수 있는 것(실행 시간)과 이미 보고된 것(토큰)은 다르다. 회귀 시험은 실제 어댑터와 같은 보고를 쓴다. 상세는 결과 문서 6절.
- **다음 행동:** 실제 Git 상태 확인 → P3-R4 범위와 [자동화·위임](autonomy-budget-policy.md)·[Profile](case-profiles.md)·[완료](completion-lifecycle.md)·P3-R3 결과 확인 → **P3-PLAN-R4 작성·공유 → R4 구현·검증**. R1~R3의 모델을 재설계하지 않고 그 위에 실행 경로를 붙인다 — 새 자동 실행 경로도 **같은 예약을 지나야 한다**(우회 경로를 만들면 예산이 무의미해진다). P3-04를 먼저 시작하지 않는다.
- **사람에게 물어야 할 것:** **`autonomy = NULL`(R1 이전) Case 를 어떻게 진행할지.** 미기록을 기본값으로 올려 자동 진행 대상으로 삼지 않는다 — R4 plan에서 확인한다.
- **작업공간:** 시작 `main`/`25f1e95`, clean 관측. 새 파일 `domain/budget.py`, `tests/test_budget.py`, `plans/P3-PLAN-R3.md`, `p3/evidence/P3-R3-*` 와 수정 파일들(`controller/{admission,db,repository,schema.sql}`, `domain/models.py`, `tests/{conftest,test_migration,test_policy,test_restart_recovery}.py`, `web/src/{api.ts,PolicyPanel.tsx}`)을 **사용자 지시로 커밋하고 `origin/main`에 push했다**(그 허용은 이 변경에만 적용된다). 실제 커밋은 `git log --oneline 25f1e95..HEAD`, 원격 반영은 로컬/원격 ref로 확인한다. 인계 소비 시 실제 상태를 재확인한다.
- **다음 세션이 이어서 할 때:** 작업 트리는 clean 이고 `origin/main` 과 같다. `tests/test_migration.py` 의 옛 스키마 조회는 작업 트리가 아니라 **커밋 이력**에서 `git show` 로 찾는다(`_v7_schema`·`_v8_schema`·`_v9_schema`). 새로 더한 `_v9_schema()` 는 `budget_reservation` 이 없는 가장 최근 커밋을 고르므로 v10 을 커밋한 뒤에는 `25f1e95` 의 스키마를 찾는다 — **push 뒤 재실행해 확인했다**(5건 통과).
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다(스크립트가 `finally` 에서 정리하고 로그로 확인). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-r3-live` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-R3-*` 에 있다. R1·R2의 라이브 데이터(`hads-p3-r1-live`·`hads-p3-r2-live`)도 그대로 있다.

### 더 이전 인계 — S-012 / 2026-09-21 / P3-R2 Repository·작업공간

- **수행:** [P3-PLAN-R2](plans/P3-PLAN-R2.md)를 기록·공유한 뒤 구현·검증했다. `case_workspace` 를 `(case_id, repository_id)` 키로 재구성하고(스키마 **v9**), 저장소 선택·코드 쓰기 허용이 작업공간을 만들 수 있는지를 **실제로 정하게** 했다. 허용 내 자동 추가(`auto_in_allowance`)와 세 경계의 거절, 실행의 대상 저장소(`run.repository_id`)와 그 worktree 배정, 코드 조합(`Repo ID → 스냅샷 벡터`)과 근거 연결, 통합 미검증 표시, 저장소별 worktree 경로와 실행 효과의 트리 지문을 만들었다. 화면은 저장소별 작업공간·허용 출처·부분 준비·조합을 보인다.
- **경계:** **이 단계에서 강제로 바뀐 것은 `repository_selection` 하나다.** Autonomy·확인 지점은 R4, 예산은 R3, **게시 허용과 실제 push·PR은 P5**다. 쓰기 허용이 게시 허용으로 번지지 않으며, `tests/test_policy.py` 가 남은 세 축의 "강제 없음"을 계속 고정한다 — R3·R4가 강제를 붙일 때 그 시험을 갱신해야 한다.
- **보존:** 기존 단일 저장소 Case 는 그대로 동작한다. 선택 기록이 없는 Case 는 거부하지 않고 허용하되 `allowance_source = implicit_single_repository` 로 **허용의 출처를 남긴다** — 없던 선택을 만들어 적지 않는다. v9 이행은 기존 작업공간의 브랜치·기준 커밋·**worktree 경로**·사용자 트리 관측을 그대로 옮기고, 어느 저장소인지 확인할 수 없으면 이행을 멈춘다. `run.repository_id` 와 조합 참조는 옛 행에서 NULL(미기록)이다.
- **검증:** **pytest 313 + P1 계약 unittest 18 통과**(R1 시점 286 → +27), `npm run build` 성공, 제어부 강제 종료 후 저장소별 작업공간·조합 복원, 커밋된 v8 스키마로 만든 실제 DB의 v9 이행. 라이브는 **실제 git 저장소 두 개**와 실제 Runner 프로세스로 두 worktree 준비·두 dirty tree 보존·네 가지 거부·자동 추가·조합을 확인했고, **실제 codex 1회**가 지정한 저장소의 worktree 안에서 돌았다(그 CLI가 UI 저장소의 `view.py` 를 보고했다). 근거는 [P3-R2 결과](p3/evidence/P3-R2-results.md).
- **구현 중 찾은 결함 셋:** (1) R1이 "R2가 갱신해야 한다"고 남긴 강제 표시 시험이 **갱신 없이 통과했다** — `enforced_by` 만 보고 `state` 를 보지 않았다. 네 축 모두 `state` 를 함께 보도록 고쳤다. (2) 대상 저장소 검사를 쓰기에만 걸었더니 작업공간이 둘인 Case 의 **읽기 전용 실행이 조용히 사용자의 원래 저장소로 떨어졌다** — 검토가 이 Case 의 작업이 아니라 사용자의 다른 작업을 읽게 된다. 권한과 무관하게 적용하도록 고치고 시험을 더했다. (3) 코드 대상이 늘어도 **옛 조합이 `integration_verified = true` 로 남았다** — 같음의 기준이 항목 지문뿐이어서, 작업공간이 늘지 않고 선택만 늘면 같은 조합이 돌아왔다. 통합 범위를 같음의 기준에 넣었다. 상세는 결과 문서 9절.
- **다음 행동:** 실제 Git 상태 확인 → P3-R3 범위와 [자동화·예산](autonomy-budget-policy.md)·P3-R2 결과 확인 → **P3-PLAN-R3 작성·공유 → R3 구현·검증**. R1·R2의 모델을 재설계하지 않고 그 위에 예산 강제를 붙인다. R4·P3-04를 먼저 시작하지 않는다.
- **작업공간:** 시작 `main`/`9c074f3`, clean 관측. 새 파일 `plans/P3-PLAN-R2.md`, `tests/test_repositories.py`, `p3/evidence/P3-R2-*` 와 수정 파일들을 **사용자 지시로 커밋하고 `origin/main`에 push했다**(그 허용은 이 변경에만 적용된다). 실제 커밋은 `git log --oneline 9c074f3..HEAD`, 원격 반영은 로컬/원격 ref로 확인한다. 인계 소비 시 실제 상태를 재확인한다.
- **다음 세션이 이어서 할 때:** 작업 트리는 clean 이고 `origin/main` 과 같다. `tests/test_migration.py` 의 옛 스키마 조회는 **커밋 이력**에서 찾으므로(`_v7_schema`·`_v8_schema`) 이 커밋 이후에도 동작한다 — push 뒤 재실행해 확인했다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다(확인함). 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-r2-live` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-R2-*` 에 있다. R1의 라이브 데이터(`hads-p3-r1-live`)도 그대로 있다.

### 그보다 이전 인계 — S-011 / 2026-09-21 / P3-R1 정책·Profile·영속 모델

- **수행:** [P3-PLAN-R1](plans/P3-PLAN-R1.md)을 기록·공유한 뒤 구현·검증했다. 여섯 Case Profile과 목적별 의도 항목(정의판 v1, 고정), WorkDepth와 분리된 `Autonomy`(기본 ask-on-decision)와 정책 이력, 위임 근거, controlled 확인 지점, 예산 설정 계약(강제 불가 hard 한도는 설정 거부), Project 등록 저장소와 Case의 선택·쓰기허용·게시허용 분리, 기록 저장소 지정, 스키마 **v8** 과 v7→v8 이행. 화면에 정책 패널을 더했다.
- **경계:** **기록은 있고 강제는 없다.** 목적별 진입 조건·자동 실행은 R4, 예산 예약·정지는 R3, Case×Repo 작업공간과 허용 내 자동 추가는 R2다. 조회 응답의 `enforcement` 가 각 축의 담당을 말하고, "강제가 아직 없다"는 사실을 `tests/test_policy.py` 가 시험으로 고정한다 — R2~R4가 강제를 붙일 때 그 시험을 갱신해야 한다.
- **보존:** 기존 Case에 새 기본값을 소급하지 않는다. R1 이전 Case의 Autonomy는 `NULL`·출처 `migrated_unknown`·정책 버전 `0.6` 으로 남고 어디서도 `ask_on_decision` 으로 표시되지 않는다. Profile도 `kind` 에서 유도해 채우지 않는다. 기존 동의·결정·완료 정책·단계 검토 기록과 P1~P3-03의 plan·증거는 수정하지 않았다.
- **검증:** **pytest 286 + P1 계약 unittest 18 통과**(P3-03 시점 242 → +44), `npm run build` 성공, 제어부 강제 종료 후 복원, 커밋된 v7 스키마로 만든 실제 DB의 v8 이행, 라이브 **실제 codex 1회**(refactoring Profile 초안 11항목·기준 3건·질문 1건, 필수 항목 누락 0). 근거는 [P3-R1 결과](p3/evidence/P3-R1-results.md). v0.6의 "여섯 항목" 단정 5건은 의미를 검토해 "그 Case의 Profile이 요구하는 항목"으로 바꾸고 Profile 없는 Case의 여섯 항목 유지 시험을 새로 더했다 — 검사를 지우지 않았다.
- **라이브가 찾은 결함:** 성공 기준이 Profile 의미 항목을 가리키면 구조 보고가 500으로 죽어 초안 전체가 사라졌다. 저장 계층·입력 계약을 함께 고치고 회귀 시험을 더했다. 상세는 결과 문서 10절.
- **다음 행동:** 실제 Git 상태 확인 → P3-R2 범위와 [실행·작업공간](execution-workspace-review.md)·P3-R1 결과 확인 → **P3-PLAN-R2 작성·공유 → R2 구현·검증**. R1의 모델을 재설계하지 않고 그 위에 실행을 붙인다. R3·R4·P3-04를 먼저 시작하지 않는다.
- **작업공간:** 시작 `main`/`3ca144b`, clean 관측. 새 파일 `domain/profiles.py`, `tests/test_policy.py`, `web/src/PolicyPanel.tsx`, `plans/P3-PLAN-R1.md`, `p3/evidence/P3-R1-*` 와 수정 파일 18개를 **사용자 지시로 커밋하고 `origin/main`에 push했다**(그 허용은 이 변경에만 적용된다). 실제 커밋은 `git log --oneline 3ca144b..HEAD`, 원격 반영은 로컬/원격 ref로 확인한다. 인계 소비 시 실제 상태를 재확인한다.
- **다음 세션이 이어서 할 때:** 작업 트리는 clean 이고 `origin/main` 과 같다. `tests/test_migration.py` 의 v7 스키마 조회는 **커밋 이력**에서 옛 스키마를 찾으므로(`_v7_schema`) 이 커밋 이후에도 동작한다 — push 뒤 재실행해 확인했다.
- **남은 자원:** 라이브 제어부·Runner 프로세스는 종료했다. 라이브 데이터는 `%LOCALAPPDATA%\Temp\hads-p3-r1-live` 에 남아 있고 저장소 `var\` 는 건드리지 않았다. 증거 사본은 `p3/evidence/P3-R1-*` 에 있다.

## 11. 유지할 경계

기본 ask-on-decision과 controlled 선택, 역할·정책 출처, 원래 위임과 누적 material delta, 필수 기준·권한·예산의 독립성, 같은 Runner의 Case×Repo, 원문 소유 PC, 범위 허용 외부 반영, 선택적 지식 추출·필수 내용 주입을 함께 지킨다. 새 기능을 편하게 만들기 위해 기존 이력을 지우거나 예산/게이트 실패를 성공으로 바꾸지 않는다. 새로운 제품 판단이 생기면 실제 사례·영향·권고안을 제시해 질문한다.
