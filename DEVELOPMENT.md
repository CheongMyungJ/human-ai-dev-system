# 개발 진행 안내서 — 새 세션의 시작점

이 문서는 `human-ai-dev-system` 개발을 여러 세션에 걸쳐 이어가기 위한 실행 안내서다. 새 세션은 이 문서에서 현재 단계와 인계를 읽고, 해당 단계에 필요한 설계 자료만 추가로 확인한다. **코드·테스트·실행 스크립트·빌드 설정을 변경하기 전에 반드시 plan을 작성하고 기록한다.**

사용자는 새 세션에서 다음과 같이 요청하면 된다.

> C:\git\human-ai-dev-system-design\DEVELOPMENT.md를 읽고 지금 진행할 단계를 수행해줘. 문서의 plan·검증·인계 절차를 지켜줘.

기본 수행 범위는 **현재 단계 하나**다. 그 안의 하위 작업은 계획에 따라 이어서 수행하되, 단계가 끝나면 다음 단계 구현까지 자동으로 확대하지 않는다. 한 세션에서 끝내지 못하면 현재 단계와 미완료 하위 작업을 그대로 인계하고 다음 세션에서 이어간다. 사용자 지시가 있으면 그 범위가 우선한다.

## 1. 현재 상태 — 매 세션 시작·종료 시 갱신

| 항목 | 현재 값 |
|---|---|
| 저장소 | https://github.com/CheongMyungJ/human-ai-dev-system — 비공개 |
| 기본 작업 디렉터리 | `C:\git\human-ai-dev-system-design` |
| 설계 기준 | v0.6, 사용자 결정 D-01~58 |
| 전체 개발 상태 | **제품 코드 P3-01까지 구현(P2 완료, P3-01 완료)**. 제어부(FastAPI·SQLite)·로컬 Runner·React/TS 화면이 연결되고 **실제 코딩 CLI(Codex·Claude Code)가 제품 경로로 연결됐다.** AI가 의도 초안과 **일곱 축의 작업 수준 판단**을 쓰고, 작성과 **별도 세션**이 QG-01로 검토하며, 사람이 동의한 뒤 **설계안·개발계획이 각각 만들어지고 각각 검토된다.** 결과가 기준별 판정과 근거로 보이고 사람이 최종 인수하거나 예외를 수용해 종료한다. **준비가 갖춰지면 기능 구현 배정이 열린다** — 다만 쓰기 권한은 아직 열지 않았다. 시험 169건이 통과한다. 작업 그래프(P3-02)·작업공간과 실제 코드 변경(P3-03), 나머지 게이트(P4), GitHub·원격은 아직이다 |
| 현재 단계 | **P3 — 기능 개발 흐름** (P1은 제한 수용으로 종료, P2는 완료, P3-01 완료) |
| 현재 단계 상태 | `IN_PROGRESS` — P3-01이 완료됐고 P3-02 구현은 아직 시작하지 않았다 |
| 다음 하위 작업 | **P3-02 — 작업 그래프·질문.** 요구·성공 기준과 구현/검증 Task 연결, 순서·의존 관계, 설계/계획으로 이월한 질문을 Task 에 잇는다. P3-01이 만든 수준·설계·계획과 이월 질문 기록 위에 올린다 |
| 활성 plan | 없음. `P3-PLAN-01`은 성공 기준 AC-1~15를 모두 통과해 완료로 종료했고 원문은 [plans/P3-PLAN-01.md](plans/P3-PLAN-01.md)에 있다(수정 기록 1건). 다음 세션이 `P3-PLAN-02`를 작성해야 함. `P2-PLAN-04`는 AC-1~14를 모두 통과해 완료로 종료했고 원문은 [plans/P2-PLAN-04.md](plans/P2-PLAN-04.md)에 있다 |
| 마지막 구현 검증 | 2026-09-21 — P3-01: **실제 codex·claude 를 7회 실행**해 의도 초안·수준 판단·QG-01 검토·설계안·개발계획을 만들고, 준비가 갖춰지자 **`feature_implementation` 이 처음으로 허용됐다**(쓰기 권한은 계속 거부). **codex 가 제안한 `simple` 을 축에서 도출한 `standard` 가 덮었고**(두 번 반복) 별도 세션의 claude 가 같은 문제를 독립적으로 지적했다. 제어부 강제 종료 후 수준·조정 이력·검토 모드·검토 기록이 복원됐고, 바이트 검사로 본문 비보관을 확인했다. **라이브에서 결함 3건을 찾아 고쳤다.** pytest 169 + P1 계약 unittest 18 통과([P3-01 결과](p3/evidence/P3-01-results.md)). 이전 P2-04 결과는 [P2-04 결과](p2/evidence/P2-04-results.md)에 있다 |
| 알려진 제약 | OpenCode는 조사 범위에서 설치 흔적 없음(2026-09-20 확인). 어댑터가 없어 능력을 보고하지 않으며 그 도구를 요청하면 `tool_not_available` 로 거부된다. Codex·Claude는 자동 업데이트가 켜져 있어 실증 결과에 관측 버전을 함께 남겨야 함. **코드를 바꾸는 쓰기 권한(`workspace_write`)은 여전히 열지 않았다** — 설계·계획 검토를 마쳐도 `permission_not_allowed_in_stage` 로 거부한다. Case별 branch/worktree 와 기준 커밋 없이 쓰기를 열면 사용자의 미커밋 변경을 보호할 수단이 없어서이며(FR-08·FR-26) **P3-03에서 작업공간과 함께 연다.** 그래서 **배정이 허용된 `feature_implementation` 실행은 CLI를 부르지 않고 `failed` 로 보고된다** — 진입 조건과 실행 경로는 다른 것이다. **시스템은 산출물의 내용이 충분한지 판정하지 않는다** — 수준이 요구하는 항목이 미정인지까지만 본다(P4의 QG-02·QG-03). **이월 질문의 `blocks` 는 자유 문자열**이며 Task 로 잇는 것은 P3-02. **`residual_activity` 는 항상 `unknown`** — 자식 프로세스 잔류 확인 수단이 없다(P1-03 이월). **안전 중지(다음 호출 차단)는 제품 경로에 연결하지 않았다** — capability로 보고만 한다. **시스템은 근거가 판정을 뒷받침하는지까지는 강제하지 못한다** — 그것은 사람의 판단이다([P2-04 결과](p2/evidence/P2-04-results.md) 9.2절) |
| 필요한 사용자 결정 | **이번 세션(P3-01) 커밋의 origin/main push 허용 여부.** 이전 세션의 push 허용은 그 커밋들에만 적용되며 이후 push 로 확대되지 않는다(원격 main = `e2e4b50`) |
| 다음 세션 첫 행동 | 시작 절차 → `scripts\bootstrap.ps1` · `scripts\run-tests.ps1` 로 현재 코드가 실제로 도는지 먼저 확인(**169 + 18이 기준선**) → [P3-01 결과](p3/evidence/P3-01-results.md)와 FR-07/13, [작업 수준·UX](sizing-and-review-ux.md) 읽기 → `P3-PLAN-02` 작성·공유 → P3-02 구현 |

위 표는 시작 시점의 기록이다. **이 표만 믿지 말고 실제 Git 상태·코드·검증 증거와 대조한다.** 코드가 이미 있는데 구현 미착수로 표시되거나, 이전 세션이 끝나지 않은 상태면 중복 구현하지 말고 먼저 상태를 복구한다.

## 2. 새 세션의 시작 절차

1. 작업 디렉터리와 이 문서가 같은 저장소인지 확인한다. `git status --short --branch`, 최근 커밋, 변경 파일을 읽고 기존 사용자 변경과 이전 세션 작업을 구별한다. 원격 연결은 읽기로 확인하며 자동 reset·clean·stash·강제 checkout·rebase는 하지 않는다.
2. 현재 상태, 단계 진행표, 최신 세션 인계, 활성 plan을 읽는다. `IN_PROGRESS`는 이전 작업을 이어가는 상태이지 처음부터 재작성하라는 뜻이 아니다.
3. [사용자 결정](decisions.md), [전체 설계](design-draft.md)의 범위·통제 조건을 확인하고 해당 단계의 필수 참조만 추가로 읽는다. 설계 제안과 확정한 사용자 결정을 구별한다.
4. 현재 코드에서 이미 충족한 기준과 남은 차이를 확인한다. 기존 테스트 결과는 실행 시점·대상·환경이 현재에도 유효한지 확인한다.
5. 현재 단계의 목표·이번 세션 범위·제외사항·성공 기준을 짧게 사용자에게 알린다. 필요한 읽기 조사를 한 뒤 **plan을 이 문서에 저장하고 구현 전에 공유한다.**
6. 계획한 순서로 구현·검증하고 상태를 갱신한다. 제품 판단이 필요한 부분은 질문하되, 답변에 의존하지 않는 허용 작업은 진행할 수 있다.

현재 단계가 `BLOCKED`/`WAITING_USER`이면 먼저 막힌 조건의 해소 여부를 확인한다. 시간이 지났다는 이유로 승인이나 해결을 가정하지 않는다. 단계가 모두 완료되면 새로운 기능을 임의로 찾아 개발하지 않고 완료와 검증 근거를 보고한다.

기본은 이 디렉터리의 현재 checkout을 이어서 사용한다. 다른 브랜치·worktree가 필요하면 plan에 이유와 정확한 경로를 기록하고 인계에서 다음 세션이 같은 작업을 찾을 수 있게 한다. 다른 디렉터리의 오래된 안내서를 최신 상태로 취급하지 않는다. 동시에 여러 세션이 같은 작업공간을 쓰지 않는다.

## 3. plan 필수 규칙

읽기 조사와 계획 작성은 구현 전 수행할 수 있다. 코드·테스트·프로토타입·실행 스크립트·스키마·의존성 설정을 처음 변경하기 전에 아래 plan을 작성해야 한다. 사용하는 도구에 별도 plan 모드가 있든 없든 **문서로 남긴 계획**이 필요하다.

- plan에는 작업 목록뿐 아니라 성공 기준과 구체 검증 방법을 포함한다.
- 이미 유효한 계획은 새 세션에서 재검토하고 이어 쓴다. 범위·환경·접근법이 달라지면 코드보다 계획을 먼저 수정한다.
- plan을 작성했다고 의도·권한의 미정 사항이 승인되는 것은 아니다. 중요한 제품 동작·범위·성공 기준 변경, 새로운 외부 반영·설치 등이 기존 허용 범위 밖이면 질문한다.
- 이미 합의된 단계 안의 일반 구현 선택은 근거를 기록하고 진행한다. 같은 결정을 반복 확인하지 않는다. 별도 사용자 검토가 지정돼 있다면 그 결과를 기다린다.
- 이 절은 **이 저장소를 개발하는 절차**다. 개발할 제품 자체의 의도 동의·설계/계획 사람 검토 정책을 변경하는 설정이 아니다.

### 활성 plan

현재 활성 plan은 없다. `P3-PLAN-01`이 완료되어 P3-01이 끝났다. 완료된 plan 원문은 근거 보존을 위해 `plans/` 에 그대로 둔다(수정 기록은 문서 끝에 덧붙인다).

| Plan ID | 대상 | 상태 | 원문 |
|---|---|---|---|
| `P3-PLAN-01` | P3-01 | **완료 (2026-09-21)** — 성공 기준 AC-1~15 모두 통과, [결과](p3/evidence/P3-01-results.md) | [plans/P3-PLAN-01.md](plans/P3-PLAN-01.md) |
| `P1-PLAN-01` | P1-01~04 | 완료 (2026-09-20) | [plans/P1-PLAN-01.md](plans/P1-PLAN-01.md) |
| `P2-PLAN-01` | P2-01 | 완료 (2026-09-20) — 성공 기준 7개 모두 통과, [결과](p2/evidence/P2-01-results.md) | [plans/P2-PLAN-01.md](plans/P2-PLAN-01.md) |
| `P2-PLAN-02` | P2-02 | 완료 (2026-09-20) — 성공 기준 AC-1~10 모두 통과, [결과](p2/evidence/P2-02-results.md) | [plans/P2-PLAN-02.md](plans/P2-PLAN-02.md) |
| `P2-PLAN-03` | P2-03 | **완료 (2026-09-20)** — 성공 기준 AC-1~14 모두 통과, [결과](p2/evidence/P2-03-results.md) | [plans/P2-PLAN-03.md](plans/P2-PLAN-03.md) |
| `P2-PLAN-04` | P2-04 | **완료 (2026-09-21)** — 성공 기준 AC-1~14 모두 통과, [결과](p2/evidence/P2-04-results.md) | [plans/P2-PLAN-04.md](plans/P2-PLAN-04.md) |

`P2-PLAN-03`은 P2-02가 넘긴 이월 항목(AI가 의도 초안을 작성하는 역방향 경로)을 범위에 넣어
완료했다. QG-01 게이트·FR-29 진입 조건 검사·실제 코딩 CLI 연결과 한 묶음이어야 순환(의도
초안을 쓰는 실행에 의도 동의를 요구하는 상태)이 생기지 않기 때문이다. 계획을 고치지 않고
끝냈으며 수정 기록이 없다. **다만 계획이 예상하지 못한 일이 하나 있었다** — 라이브 검증에서
QG-01이 실제로 두 번 실패했다. 게이트를 느슨하게 만드는 대신 제품이 설계한 복구 경로
(피드백 → 새 버전 → 재검토)를 그대로 따라 통과시켰다.

`P2-PLAN-04`는 P2-03이 남긴 화면 미확인 경로(게이트 의미 검토 실행·원문 열람·동의·제한
작업을 화면에서 누르는 경로)를 범위에 넣어 닫았다. 계획을 고치지 않고 끝냈다.
**다만 계획이 예상하지 못한 일이 둘 있었다.** (가) 화면이 대체된 의도 버전을 검토하는
결함이 라이브에서 드러나 화면과 서버를 함께 고쳤다. (나) AI 재작성 경로가 이전 버전을
보지 못해 초안이 퇴화했고, 이것은 고치지 않고 P3·P4로 넘겼다(근거는
[P2-04 결과](p2/evidence/P2-04-results.md) 9절).

`P3-PLAN-01`은 P2-04가 남긴 위험 1(**AI 재작성 경로가 이전 버전을 보지 못한다**)을 범위에
넣어 닫았다. 설계·계획 작성이 같은 작성 경로를 쓰기 때문이며, 고치지 않으면 설계 재작성도
같은 방식으로 퇴화해 기능 구현의 선행 조건을 망가뜨린다. 라이브에서 재작성한 의도 v2가
**퇴화하지 않고 기준이 4→5건으로 늘었다**(P2-04에서는 목표 미정·기준 0건으로 퇴화했다).

계획을 한 번 고쳤다. **`plan_not_for_current_design` 사유를 구현하지 않고 지웠다** —
설계를 새로 만들면 그 위의 계획이 곧바로 대체되므로 그 검사는 닿을 수 없는 분기였다.
수정 기록은 plan 문서 끝에 있다. **다만 계획이 예상하지 못한 일이 둘 있었다.**
(가) 배정이 처음 허용된 순간 Runner 에 실행 경로가 없어 루프가 죽었다.
(나) 수준을 올려도 이미 있는 산출물이 부족해지지 않았다 — 조정이 겉치레가 되는 결함이다.
둘 다 라이브에서 드러나 고쳤고 근거는 [P3-01 결과](p3/evidence/P3-01-results.md) 10절에 있다.

`P2-PLAN-02`가 이전 세션에서 넘어온 미결 항목 하나를 정하고 구현했다. **원문 일시중계 읽기 경로를 P2-04로 미루지 않고 P2-02에 넣었다.** 근거는 읽을 수 없는 초안에 대한 동의를 기록하지 않기 위해서다(data-boundary-review 3절, intent-artifacts 3·5절). 구현 중 계획을 두 번 고쳤고 그 기록은 plan 끝의 수정 기록에 있다.



새 계획으로 대체할 때 이전 plan을 삭제하지 말고 이 문서의 세션 기록에 완료·대체 이유와 결과를 남긴다. 기록이 커지면 상세 로그만 저장소의 별도 문서로 옮길 수 있지만 이 문서에는 현재 plan·진행 상태·요약·정확한 참조를 유지한다. 사용자가 다음 파일을 찾아다니게 하지 않는다.

## 4. 단계 진행표

상태는 `PENDING` → `READY` → `IN_PROGRESS` → `DONE`이다. 대기가 필요하면 `WAITING_USER` 또는 `BLOCKED`를 사용한다. 미완료를 DONE으로 기록하지 않는다. 사용자가 미검증 범위를 명시적으로 수용한 경우에만 `ACCEPTED_WITH_LIMITATIONS`와 제한·결정 근거를 기록한다. 이는 시험 통과가 아니다.

| 단계 | 상태 | 다음 하위 작업 | 완료 증거·인계 |
|---|---|---|---|
| P1 CLI 연결 검증 | ACCEPTED_WITH_LIMITATIONS | — | P1-01~04 완료 — [계약](p1-environment-contract.md), [P1-02 결과](p1/evidence/P1-02-results.md), [P1-03 결과](p1/evidence/P1-03-results.md), [OpenCode 계약](p1/opencode/contract.md). **제한: 중복 호출 방지 미검증(2026-09-20 사용자 수용, P2-01 이월) → [P2-01에서 검증해 닫음](p2/evidence/P2-01-results.md)** |
| P2 최소 업무 흐름 | **DONE** | — | P2-01~04 완료 — [P2-01](p2/evidence/P2-01-results.md), [P2-02](p2/evidence/P2-02-results.md), [P2-03](p2/evidence/P2-03-results.md), [P2-04](p2/evidence/P2-04-results.md) 결과와 [`P2-PLAN-01`](plans/P2-PLAN-01.md)·[`02`](plans/P2-PLAN-02.md)·[`03`](plans/P2-PLAN-03.md)·[`04`](plans/P2-PLAN-04.md). 제어부·Runner·화면 연결, 강제 종료 복원, 중복 실행 방지, 원문 비보관, 여섯 항목 초안·열람·피드백·명시 동의, 실제 CLI 연결·QG-01·FR-29 진입 검사·AI 작성 경로, **성공 기준·기준별 결과와 근거·사람 최종 인수·예외 수용 종료·연결된 새 Case** |
| P3 기능 개발 흐름 | **IN_PROGRESS** | P3-02 | P3-01 완료 — [P3-01 결과](p3/evidence/P3-01-results.md)와 [`P3-PLAN-01`](plans/P3-PLAN-01.md). 일곱 축 수준 판단·사람 조정, 설계안·개발계획의 각각 조회·검토, 단계별 검토 모드, FR-29 선행 조건 10가지, 작성 실행의 고정 컨텍스트. **`feature_implementation` 배정이 처음 열렸고 쓰기 권한은 계속 거부된다** |
| P4 품질·다양한 업무 | PENDING | P4-01 | 없음 |
| P5 GitHub 연동 | PENDING | P5-01 | 없음 |
| P6 원격·운영·전체 수용시험 | PENDING | P6-01 | 없음 |

다음 단계의 선행 조건은 앞 단계의 성공 기준 충족과 증거 확인이다. 예외 수용만으로 모든 후속 작업이 실행 가능해지지는 않는다. 후속 작업의 필수 능력·입력·권한이 부족하면 그 작업은 계속 보류한다. 단계 번호를 건너뛰거나 범위를 줄여 완료로 처리하지 않는다.

## 5. P1 — CLI 연결 검증

**목표:** 실제 Windows 환경에서 코딩 CLI를 시스템이 어느 범위까지 제어할 수 있는지 확인하고, 어댑터 구현의 근거를 만든다.

**필수 참조:** [CLI 안전 중지 조사](cli-pause-feasibility.md), [실행·작업공간](execution-workspace-review.md), [기술 확인](review-tech-findings.md), [P1-01 환경 실측·계약 초안](p1-environment-contract.md), FR-28, NFR-07. 기존 조사 문서는 실증 결과가 아니며, 설치 버전에 맞는 공식 인터페이스를 다시 확인한다.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P1-01 환경·계약 — **DONE** | Python·Git·CLI의 실제 위치/버전/사용 가능 여부 조사, 공통 실행 입력·이벤트·결과·능력표 초안 | PATH 조회 실패와 미설치를 구분. OS/버전/확인 방법 기록. 인증 비밀값 없이 실행 가능 여부 표시. 아래 실증의 plan 작성 → 결과: [p1-environment-contract.md](p1-environment-contract.md), `P1-PLAN-01` |
| P1-02 Codex·Claude 실증 — **DONE** | 기존 설치·인증을 사용한 최소 실행 harness, 임시 시험 저장소에서 읽기·작은 변경·구조화 결과·별도 세션 확인 | 두 도구 각각 실제 실행 증거. 성공/실패/권한 질문/세션 식별을 구별하고 작성·검토 세션이 분리됨. 명령 반환 코드만으로 성공 판정하지 않음 → 결과: [P1-02 실증 결과](p1/evidence/P1-02-results.md), harness [`p1/harness/run_cli.py`](p1/harness/run_cli.py) |
| P1-03 중지·복구 실증 — **DONE(부분)** | 도구 A 실행 중 단절 감지 → A 종료 → B 호출 보류 → 연결 회복 후 대조. 취소·프로세스 종료·자식 활동도 확인 | 지원 모드에서 B가 시작되지 않는 증거. 미지원 경로는 명확히 표시. 지연 이벤트·응답 유실에서 중복 호출하지 않음. 강제 종료와 안전 중지를 혼동하지 않음 → 결과: [P1-03 결과](p1/evidence/P1-03-results.md). **차단·강제 종료 구분은 확인했고 재연결 대조·병렬 호출·훅 적용 공백은 미검증**이다 |
| P1-04 OpenCode 문서 계약·결론 — **DONE** | 일관된 공식 버전의 어댑터 계약·응답 fixture/계약 시험, 세 도구 capability 표와 P2에서 사용할 연결 방식 | OpenCode를 설치/실행한 것으로 표시하지 않음. 문서 계약 검증과 실증 미완료 분리. 필수 능력의 공백·대안·후속 작업 명시 |

**제외:** 제품 전체 UI·업무 엔진, GitHub 쓰기, 실제 사용자 프로젝트 변경, 계정/도구의 무단 설치·로그인 설정 변경. OpenCode 설치는 현재 범위에 넣지 않는다.

**단계 완료 조건:** Codex·Claude의 최소 실행과 필수 제어 능력에 대한 실제 결과가 있고, 공통 어댑터 계약과 실패 동작을 설명할 수 있다. 미지원 결과 자체도 유효한 조사 결과지만 필수 요구의 우회 허용을 뜻하지 않는다. 필요한 제어가 불가능하면 구체 근거·대안을 제시해 결정받고 후속 실행 가능 범위를 명시한다. 설치·인증 부족으로 실증을 못 한 경우 전체 완료로 표시하지 않는다. OpenCode의 실증 미완료는 사전 합의된 제한으로 유지한다.

**P1 완료 점검 (2026-09-20, P1-04 16-e):**

| 완료 조건 | 결과 |
|---|---|
| Codex·Claude 최소 실행의 실제 결과 | 충족 — [P1-02 결과](p1/evidence/P1-02-results.md) |
| 필수 제어 능력의 실제 결과 | 충족(조건부) — 다음 호출 차단을 두 CLI에서 부수효과로 확인. 조건은 [P1-03 결과](p1/evidence/P1-03-results.md) 2절 |
| 공통 어댑터 계약과 실패 동작 설명 | 충족 — [계약](p1-environment-contract.md) 6~8절. 실패 동작: 훅 미발화, fail-open, 종료 코드 불충분, 강제 종료 시 프로세스 잔류 |
| OpenCode 실증 미완료를 사전 합의 제한으로 유지 | 충족 — [OpenCode 계약](p1/opencode/contract.md). `verified` 없음, 안전 중지는 지원 보류 |
| **지연 이벤트·응답 유실에서 중복 호출하지 않음**(P1-03 성공 기준) | **미충족** — 재배정·멱등 검사는 제어부가 있어야 확인할 수 있는데 P1 범위에 제어부가 없다 |

**제한 수용 기록 (2026-09-20):** 사용자가 (가)를 선택해 P1을 `ACCEPTED_WITH_LIMITATIONS`로 닫았다.

| 항목 | 내용 |
|---|---|
| 수용한 제한 | P1-03 성공 기준 중 '지연 이벤트·응답 유실에서 중복 호출하지 않음'이 미검증이다 |
| 결정 근거 | `run_id` 멱등성과 배정 세대는 제어부 설계의 일부이며 P2-01 범위와 겹친다. P1에서 최소 제어부를 앞당겨 만드는 것보다 P2-01에서 함께 하는 편이 중복이 적다 |
| 이월 대상 | **P2-01**. 같은 요청 재전송이 중복 실행을 만들지 않는지 실제로 확인한다 |
| 이월 결과 (2026-09-20) | **해소됨.** P2-01에서 제어 API 재전송과 '결과 보고 유실 후 재배정' 두 경우 모두 실행 부수효과가 1회임을 확인했다 → [P2-01 실행 결과](p2/evidence/P2-01-results.md) 4절, 6절 P2의 P2-01 완료 점검 |
| 범위 제한 | 이 수용은 **이 한 항목에만** 적용된다. P1의 다른 기준이나 P2 이후의 기준을 면제하지 않는다 |
| 시험 통과가 아님 | 이는 미검증 상태를 사용자가 감수한 것이지 검증에 통과한 것이 아니다 |

P1-03 결과 7절의 나머지 미검증 항목(재연결 대조, 병렬 호출 경계, 훅 적용 공백, 훅 timeout,
프로세스 트리 종료, 훅 신뢰 영속화)은 **P2-01의 해소 범위가 아니며** 그대로 남아 있다.
P2 이후 해당 기능을 구현할 때 확인한다.
확인되지 않은 능력에 의존하는 기능은 그때까지 지원을 보류한다.

## 6. P2 — 최소 업무 흐름

**목표:** 웹 요청부터 의도 초안·피드백·최신 동의·제한된 실행·결과 확인까지 실제로 연결되는 가장 작은 흐름을 만든다.

**필수 참조:** [의도 산출물](intent-artifacts.md), [전체 설계](design-draft.md), [데이터 경계](data-boundary-review.md), FR-03/04/13/15/16/23/29, NFR-01/03/09/12.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P2-01 최소 골격·영속 상태 — **DONE** | Python/FastAPI·SQLite·React/TS 골격, Project/Case/의도 버전/결정/Run의 최소 모델, 로컬 Runner 계약. **P1에서 이월: 같은 요청 재전송이 중복 실행을 만들지 않는지 실증** | 실제 API·DB·화면 연결과 재시작 복원. 제어 상태·요약과 Runner 원문 저장을 논리적으로 처음부터 분리. 기본 실행/시험 명령 문서화. **`run_id` 멱등성을 실제 재전송으로 확인하고 P1 이월 항목을 닫는다** → 결과: [P2-01 실행 결과](p2/evidence/P2-01-results.md), 코드 `controller/` `runner/` `domain/` `web/`, 시험 `tests/` 19건 |
| P2-02 의도·피드백 — **DONE** | 여섯 필드 초안, 원문/가정/질문, 피드백 반영과 버전 차이, 명시 동의. **원문 일시중계 읽기 경로를 여기서 만들었다** | 작은 기능도 초안·피드백·동의 흐름을 거침. 무응답/조회/단순 질문이 동의가 되지 않음. 오래된 의도 동의가 최신 버전에 적용되지 않음 → 결과: [P2-02 실행 결과](p2/evidence/P2-02-results.md), 코드 `domain/intent_doc.py` `controller/` `runner/agent.py` `web/src/IntentPanel.tsx`, 시험 `tests/` 39건 |
| P2-03 진입 제어·제한 실행 — **DONE** | QG-01의 최소 규칙·별도 의미 검토, 서버 측 실행 조건, 실제 코딩 CLI 연결로 읽기·결과 작성 수준의 제한 작업. P2-02에서 이월한 AI 의도 초안 작성 경로를 함께 만들었다 | UI 버튼 우회 API 호출도 미동의·필수 질문·게이트 미완료면 거부. 실제 실행 ID/결과 저장. 같은 요청 재전송은 중복 실행하지 않음 → 결과: [P2-03 실행 결과](p2/evidence/P2-03-results.md), 코드 `controller/gate.py` `controller/admission.py` `runner/cli_adapter.py` `runner/cli_events.py` `runner/prompts.py`, 시험 `tests/` 77건 |
| P2-04 결과·재시작 | 기준별 결과와 근거 열람, 기본 사람 최종 확인의 최소 흐름, 제어부/Runner 재시작 | 입력·동의·Run·원문 참조가 보존되고 실행 불명 상태가 성공이 되지 않음. 화면에서 처음부터 끝까지 재현 가능한 시연 절차 제공 |

**제외:** 일반적인 기능 구현 자동화 전체, 설계·계획 정책 생략, 원격 배포, GitHub 쓰기. 아직 구현되지 않은 선행 조건을 임시로 항상 통과 처리하지 않는다. 기능 코드 변경 실행은 P3 선행 조건이 준비될 때 연결한다.

**P2-01 완료 점검 (2026-09-20):**

| P2-01 성공 기준 | 결과 |
|---|---|
| 실제 API·DB·화면 연결 | 충족 — 브라우저 클릭으로 Case·원문·Run을 만들고 SQLite를 직접 열어 대조 |
| 재시작 복원 | 충족 — uvicorn 자식 프로세스를 `taskkill /F /T` 로 죽인 뒤 재기동해 확인 |
| 제어 상태·요약과 Runner 원문 저장의 논리적 분리 | 충족 — 제어부 스키마에 본문 컬럼 없음. 표식 문자열이 제어부 DB·로그에 없고 Runner 저장소에 있음을 파일 바이트로 확인 |
| 기본 실행/시험 명령 문서화 | 충족 — `scripts/` 4개와 [README 실행 방법](README.md) |
| **`run_id` 멱등성을 실제 재전송으로 확인** (P1 이월) | **충족** — 제어 API 재전송과 '결과 보고 유실 후 재배정' 두 경우 모두 실행 부수효과가 1회 |

**P2-02 완료 점검 (2026-09-20):**

| P2-02 성공 기준 | 결과 |
|---|---|
| 작은 기능도 초안·피드백·동의 흐름을 거침 | 충족 — 한 항목짜리 요청도 여섯 항목이 모두 기록되고 같은 흐름을 통과해야 동의가 된다 |
| 무응답·조회·단순 질문이 동의가 되지 않음 | 충족 — 열람·피드백·질문 답변을 모두 한 뒤에도 동의 상태는 `동의 없음`이고 decision 행이 0개다 |
| 오래된 의도 동의가 최신 버전에 적용되지 않음 | 충족 — v1 동의 뒤 v2가 생기면 `stale_agreement` 이며 v1 동의 기록은 보존된다. v1 재동의는 거절 |
| (추가) 여섯 항목과 미정 유지 | 충족 — 비운 항목은 `undecided`/`none` 으로 남고 시스템이 채우지 않는다 |
| (추가) 원문 본문 비보관 | 충족 — 의도·질문·피드백·답변·열람 본문이 제어부 DB·WAL·SHM·로그 바이트에 없고 Runner에만 있다 |

**의도 동의를 만드는 경로는 하나뿐이다.** 일반 결정 경로 `POST /api/cases/{id}/decisions` 는
`intent_agreement` 를 409로 거절한다. 화면에서 버튼을 없애는 것만으로는 API 직접 호출을
막지 못하기 때문이다. 거절 사유 다섯 가지와 검사 순서는
[P2-02 결과](p2/evidence/P2-02-results.md) 6절에 있다.

**이 거절들은 FR-29 진입 조건 검사가 아니다.** 동의를 기록할 수 있는지의 조건일 뿐이다.
실행 배정을 여는 조건은 P2-03에서 `controller/admission.py` 로 따로 만들었고, 두 검사는
지금도 별개 함수다. 동의가 있어도 게이트를 통과하지 못하면 실행은 거부된다.

**P2-03 완료 점검 (2026-09-20):**

| P2-03 성공 기준 | 결과 |
|---|---|
| UI 버튼 우회 API 호출도 미동의·필수 질문·게이트 미완료면 거부 | 충족 — 라이브 14건이 전부 직접 HTTP 호출이고 8건이 사유 코드와 함께 거부됐다 |
| 실제 실행 ID/결과 저장 | 충족 — codex·claude 실행 6회의 실제 `session_ref`·정규화 이벤트·결과 원문이 남았다 |
| 같은 요청 재전송은 중복 실행하지 않음 | 충족 — 실제 CLI 경로에서 부수효과가 1회만 늘었고 재전송에는 검사 기록도 생기지 않았다 |
| (추가) QG-01이 실제로 판정한다 | 충족 — **AI가 쓴 초안이 두 번 연속 실패했다.** 모순·확인 불가능한 성공 기준·빠진 질문을 별도 세션이 찾았다 |
| (추가) 동의와 실행 권한의 분리 | 충족 — 사람이 동의한 뒤에도 게이트 미통과면 거부된다 |
| (추가) AI 작성 경로(P2-02 이월) | 충족 — 초안이 Runner에서 태어나 의도 버전이 되고 작성 주체가 버전마다 기록된다 |

**미구현 선행 조건을 통과로 처리하지 않았다.** 설계·계획 검토가 없으므로
`feature_implementation` 목적과 `workspace_write` 권한은 **거부**된다. 이 거부가
P2 제외 범위("아직 구현되지 않은 선행 조건을 임시로 항상 통과 처리하지 않는다")의
구체적 구현이며, 기능 코드 변경 실행은 P3에서 선행 조건과 함께 연결한다.

**P2-02 이월 항목 종료 (2026-09-20):** AI가 의도 초안을 작성하는 역할 분리를
P2-03에서 실제로 만들고 검증했다. 초안이 Runner에서 태어나는 역방향 경로
(`POST /api/runner/intent-versions`)를 만들었고, 거짓이 된 `authoring_note` 와 화면
문구를 함께 고쳤으며, 사람 입력 경로는 그대로 남겼다. 근거는
[P2-03 결과](p2/evidence/P2-03-results.md) 4·5절과 `tests/test_ai_draft.py` 다.

**P1 이월 항목 종료 (2026-09-20):** P1에서 `ACCEPTED_WITH_LIMITATIONS` 로 남긴
'지연 이벤트·응답 유실에서 중복 호출하지 않음'을 P2-01에서 실제로 검증해 닫았다.
근거는 [P2-01 실행 결과](p2/evidence/P2-01-results.md) 4절과
`tests/test_idempotency.py::test_lost_result_response_does_not_cause_re_execution` 이다.
**P1의 다른 미검증 항목(재연결 대조, 병렬 호출 경계, 훅 적용 공백, 훅 timeout,
프로세스 트리 종료, 훅 신뢰 영속화)은 그대로 남아 있다.** 이 종료는 그 한 항목에만 적용된다.

**P2-02 이월 항목 → P2-03: 종료 (2026-09-20):**

P2-02는 의도 초안을 사람이 화면에서 입력하게 만들었고, FR-03의 **역할 분리**(AI가 제시하고
사람이 검토한다)를 검증하지 못한 채 P2-03으로 넘겼다. P2-03에서 그 경로를 만들고 확인했다.

| 항목 | 결과 |
|---|---|
| 이월한 것 | FR-03의 역할 분리 — AI가 초안을 작성해 제시하고 사람은 열람·질문·피드백·동의를 한다 |
| 지금 상태 | **해소됨.** 실제 codex 가 여섯 항목 초안을 썼고 사람은 읽고 동의만 했다 |
| 만든 경로 | `POST /api/runner/intent-versions` — 초안이 Runner에서 태어나므로 방향이 반대다. `POST /api/runner/artifacts` → 이 경로 → `POST /api/runner/intent-structure` 순서로 이어진다 |
| 거짓이 된 문구 | `domain/intent_doc.py` 의 `authoring_note` 와 `web/src/IntentPanel.tsx` 문구를 함께 고쳤다. 지금은 작성 주체(`authoring_mode`)가 버전마다 기록되고 화면에 표시된다 |
| 사람 입력 경로 | 그대로 남아 있다. 라이브에서 v1·v2는 AI가, v3은 사람이 썼고 세 버전이 같은 문서 형식·같은 표를 쓴다 |
| 뜻밖의 결과 | **AI가 쓴 초안이 QG-01에 두 번 연속 걸렸다.** 여섯 항목이 채워진 것과 검토를 요청할 만한 것은 다르다는 사실이 실제로 드러났다 |

**단계 완료 조건:** mock만이 아닌 실제 UI/API/DB와 적어도 한 실제 CLI 경로가 연결된다. 나머지 CLI도 공통 계약과 capability 제한을 유지하고 특정 CLI 전용 구조로 고정하지 않는다. 의도 진입 검사는 정상·거절·재시작 사례로 검증한다.

**P2 완료 조건 점검 (2026-09-20, P2-03 기준):**

| 완료 조건 | 결과 |
|---|---|
| 실제 UI/API/DB 연결 | 충족 — API·DB·화면을 모두 실제 프로세스와 브라우저로 확인. 화면에서 누른 초안 요청이 실제 Claude Code를 실행해 의도 버전을 만들었다 |
| 적어도 한 실제 CLI 경로 | 충족 — codex·claude **두 경로**를 실제 실행 |
| 특정 CLI 전용 구조로 고정하지 않음 | 충족 — 제어부는 `tool_id`·추상 권한·정규화 이벤트만 다루고 CLI 인자는 `runner/cli_adapter.py` 에만 있다 |
| 의도 진입 검사를 정상·거절·재시작으로 검증 | 충족 — 허용 6건·거부 8건·재시작 후 재확인 |

**P2-04 완료 점검 (2026-09-21):**

| P2-04 성공 기준 | 결과 |
|---|---|
| 기준별 결과와 근거 열람 | 충족 — 기준마다 판정·근거 종류·근거 원문 참조가 따로 남고, 근거 원문을 화면에서 일시중계로 받아 봤다. 총점을 만들지 않는다 |
| 기본 사람 최종 확인의 최소 흐름 | 충족 — 결과 후보 → 명시적 인수 → 종료. 모호한 문구는 거절되고, 미충족이 남으면 예외 수용 뒤에야 닫힌다 |
| 제어부/Runner 재시작 | 충족 — 둘 다 `taskkill /F /T` 로 죽인 뒤 재기동해 의도·동의·Run·원문 참조·기준·판정·인수가 모두 복원됐다 |
| **입력·동의·Run·원문 참조가 보존됨** | 충족 — 재기동 전후 대조([P2-04 결과](p2/evidence/P2-04-results.md) 6절) |
| **실행 불명 상태가 성공이 되지 않음** | 충족 — `unknown`·`failed`·`cancelled`·진행 중인 실행을 근거로 한 `met` 은 기록 자체가 거부된다. 미정리 실행이 남으면 인수는 기록되고 **종료는 확정되지 않는다** |
| 화면에서 처음부터 끝까지 재현 가능한 시연 절차 | 충족 — [README 시연 절차](README.md)와 브라우저 두 바퀴. P2-03이 남긴 화면 미확인 경로가 여기서 닫혔다 |

**P2-04에서 발견해 고친 결함 다섯 건과, 고치지 않고 드러낸 한계 두 건**은
[P2-04 결과](p2/evidence/P2-04-results.md) 9절에 있다. 특히 **AI 재작성 경로가 이전
버전을 보지 못하는 것**은 P3·P4로 넘겼고, **근거가 판정을 뒷받침하는지는 시스템이
강제하지 못한다**는 경계를 라이브에서 실제로 확인해 기록했다.

**P2 단계 완료 (2026-09-21).** 네 하위 작업이 모두 끝났고 단계 완료 조건도 위 표에서
충족했다. 다음 단계는 P3이며 구현은 다음 세션에서 시작한다.

## 7. P3 — 기능 개발 흐름

**목표:** 작고 분명한 기능 하나를 의도 → 설계 → 개발계획 → 구현 → 테스트 → 결과 확인까지 처리한다.

**필수 참조:** [작업 수준·UX](sizing-and-review-ux.md), [의도 산출물](intent-artifacts.md), [실행·작업공간](execution-workspace-review.md), FR-05/06/07/08/23/26/29, NFR-03/05/11.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P3-01 수준·설계·계획 — **DONE** | 간소/표준/심층 추천·조정, 별도 조회 가능한 설계·개발계획, 두 단계의 독립 사람 검토 옵션 | 기본 둘 다 사람 검토. 자동 모드도 원문 산출물을 보존. 필요한 산출물/사람 검토가 빠지면 기능 구현 배정 불가 → 결과: [P3-01 결과](p3/evidence/P3-01-results.md), [`P3-PLAN-01`](plans/P3-PLAN-01.md). AC-1~15 모두 통과 |
| P3-02 작업 그래프·질문 | 요구·성공 기준과 구현/검증 Task 연결, 순서·의존 관계, 설계/계획으로 이월한 질문 | 필요한 사람 결정 전에 해당 의존 작업이 실행되지 않음. 단계 자동 진행이 질문을 자동 해결하지 않음. 이미 유효한 승인 반복 요구 없음 |
| P3-03 작업공간·실행 | Case별 branch/worktree, 기준 커밋, CLI 실제 코드 수정, 빌드·테스트 실행·증거 | 사용자 원래 dirty tree 보호. 같은 Case 쓰기 경합 방지. 허용되지 않은 경로/행동은 지원 경계에 맞게 처리하고 OS 완전 격리라고 주장하지 않음 |
| P3-04 작은 기능 통합 | 합의한 시험용 작은 기능을 실제로 개발하는 수직 시나리오 | 최소 1개 요구의 정상·오류/경계·관련 회귀 결과와 실제 코드 diff 확인. 테스트 실패나 미검증을 완료로 표시하지 않음 |

**제외:** 연구·모든 예외 경로의 완성, GitHub push/PR, 원격 Runner. 한 기능 시연을 위해 모든 다른 기능의 상태를 완료로 꾸미지 않는다.

**단계 완료 조건:** 실제 시험 저장소에서 기능 개발 한 건을 끝까지 재현하고, 설계/계획 검토 모드 조합 및 오래된 승인·미해결 질문의 차단을 자동 시험으로 확인한다. 다양한 업무·게이트 상세·예외 종료는 P4에서 확장한다.

## 8. P4 — 품질·다양한 업무·긴 컨텍스트

**목표:** 구현이 잘될 때뿐 아니라 실패·요구 변경·불확실성·세션 교체에서도 올바르게 진행한다.

**필수 참조:** [품질 게이트](quality-gates.md), [게이트 실행](gate-operations.md), [검토 컨텍스트](review-context-contract.md), [완료·예외](completion-lifecycle.md), FR-09~20/25, NFR-03/06/09/10. FR-19와 QG-08은 후속 확장 범위다.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P4-01 게이트·재검증 | QG-01 필수 잠금, QG-02~07 조건별 적용, Case/Task 설정, 별도 의미 검토, 수정 주기 | 최초 실패 후 기본 2회 수정·재검증. 세션 교체로 횟수 초기화 없음. OFF는 통과가 아니며 성공 기준·사람 검토·권한을 해제하지 않음 |
| P4-02 예약 설정·변경 | 검증 중 ON/OFF 예약, 요구 변경 영향, 진행 중 질문/수정 | 현재 검증 1회 결과 저장 후 예약 적용. 다음 수정·재검증은 새 설정 사용. 실제 코드/기준 변경의 영향은 별도로 재검토 |
| P4-03 버그·연구·실험 | 명확한 비기능 요청의 직접 시작, 실험 범위 확인, 가설·반증·판단 불가 | 기능 개발 단계 일괄 강제 없음. 미허용 실험은 확인 전 실행하지 않음. ‘재현 안 됨’을 ‘수정 완료’로 취급하지 않음 |
| P4-04 컨텍스트·중단 | 영속 원문·결정·입력 명세, 작업/검토 분할, 새 CLI 세션 복원 | 의도·기준·필수 질문·실패·수정 차수를 복원. 작성자의 완료 요약만으로 검토하지 않음. 원문 누락·오래된 근거 표시 |
| P4-05 완료·예외·후속 | 사람 최종 인수/Case별 자동 완료, 명시적 예외 수용, 완료 후 새 Case | 자동 완료가 사람 인수나 예외를 조작하지 않음. 실패/미검증 판정 보존. 예외 종료로 추가 실행 권한 생성 금지. 완료 후 수정은 연결된 새 Case |

**제외:** 지식 후보 자동 추출·채택 화면·자동 재사용, 실제 공동 사용. 출처와 결정 근거는 보존한다.

**단계 완료 조건:** 기능·버그·연구 대표 사례와 게이트 실패·설정 변경·기준 변경·세션 교체·예외 종료를 재현한다. 업무 상태, 사람의 결정, 품질 판정, 외부 권한의 독립성이 유지된다.

## 9. P5 — GitHub 연동

**목표:** 시스템 원본과 외부 기록을 구분하면서 GitHub에 요약을 남기고 확인받은 코드·PR 반영을 수행한다.

**필수 참조:** [GitHub 기록](github-journal.md), [실행·작업공간](execution-workspace-review.md), FR-21/22/23/26/30, NFR-02/12.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P5-01 연결·읽기 | 저장소·기존 이슈 출처·PR·CI 상태와 근거 조회, 자격증명 참조 | 원문/상태/코드 버전의 출처 연결. 비밀값 로그 금지. 읽을 수 없는 외부 CI 로그를 읽었다고 표시하지 않음 |
| P5-02 요약 게시 | Case별 새 기록 이슈, 시스템 상단 표식, 요약·참조 append, 사전 게시 허용·영속 Outbox | 기존 이슈는 출처만 연결. 코드/diff/상세 로그 제외. 같은 허용 범위 반복 승인 없음. 과거 본문/댓글 편집 없음 |
| P5-03 승인된 쓰기 | 실제 대상·SHA·PR 내용 준비와 확인, push·PR 생성/수정 | 승인 후 대상/내용 변경 차단. 응답 유실 시 실제 결과 대조 후 판단. 사전 요약 게시 허용을 코드 쓰기 승인으로 재사용하지 않음 |
| P5-04 리뷰·장애 | PR 리뷰 피드백, 오래된 리뷰 대조, 게시 지연/중복/재시작 | 이슈 댓글이 승인/실행 명령이 되지 않음. 게시 실패에도 내부 진행. 완료 후 리뷰 수정은 새 Case. 누적 실패 알림과 전달 상태 보존 |

**제외:** 병합·이슈 종료·CI 수동 재실행·배포·GitLab. 이 저장소가 만들어졌다는 사실은 미래 시험용 이슈/댓글/PR/코드 push를 무제한 허용한 것이 아니다.

**단계 완료 조건:** 계약 시험과 장애 주입 시험을 수행하고, 실제 외부 쓰기 시험은 준비한 대상·내용과 허용 범위를 사용자에게 제시하여 허용받은 시험 저장소에서 실행한다. 허용이나 자원이 없어 실시험을 못 했다면 mock 통과와 실제 미검증을 분리해 인계한다.

## 10. P6 — 원격·운영·전체 수용시험

**목표:** 개인이 Windows 로컬 또는 서버에 설치해 여러 프로젝트에 사용할 수 있는 첫 버전을 완성한다.

**필수 참조:** [데이터 경계](data-boundary-review.md), [구현·운영 기준](implementation-baseline.md), [수용 시나리오](review-acceptance-matrix.md), FR-16/24~28, NFR-01~12.

| 하위 작업 | 범위 | 성공 기준·검증 |
|---|---|---|
| P6-01 원격·인증 | 소유자 인증, Runner 등록/회수, LAN/사용자 VPN 암호화 연결, 경로 매핑 | 미등록·권한 회수·잘못된 대상 요청 거부. PC가 서버 연결을 시작. 인터넷 공개·다중 사용자 기능으로 확대하지 않음 |
| P6-02 원문 일시중계 | 서버 상태/요약/참조와 PC 상세 원문 분리, 원문 조회·입력 저장 계약 | 코드/diff/로그뿐 아니라 상세 의도/설계/계획/대화가 서버 DB·파일·로그·큐·캐시에 남지 않음. PC 오프라인 입력을 저장 완료로 표시하지 않음 |
| P6-03 단절·복구 | 실제 연결 단절, 현재 도구 종료 후 정지, 미확인 실행 대조·재연결 | 정지/종료 확인 전 동일 쓰기 재배정 없음. 다음 호출 보류를 정확한 CLI/모드에서 확인. 오래된 결과가 최신 상태를 덮어쓰지 않음 |
| P6-04 백업·설치 | 각 호스트 일일 백업·최근 7개, 부분 복원, 설치/실행/업그레이드 스크립트와 빌드 UI | 원본·승인·증거 자동 삭제 없음. PC 원문 서버 복제 없음. 꺼진 호스트의 백업 누락 표시. Python·Git 준비 후 일반 사용자 프로세스로 설치 재현 |
| P6-05 규모·최종 수용 | 10개 프로젝트·3 Runner·동시 AI 2개, 동일 Runner 쓰기 기본 1개. FR/NFR·전체 대표 시나리오 연결 | 실제 측정과 미측정을 구분. 기능·오류·복원·데이터 경계 증거. 사용량 미제공을 0으로 표시하지 않음. 총 자동 실행 시간의 기본 한도 없음 |

**제외:** Linux/WSL, Windows 서비스·런타임 포함 설치 프로그램, 다중 계정/공동 편집, 복수 프로젝트 업무, 지식 자동화, 인터넷 직접 공개.

**단계 완료 조건:** 세 배포 형태의 검증, 대표 기능·버그·연구, 실패/복구 시험과 설치 안내를 갖춘다. 여러 프로세스의 모의시험과 실제 호스트 간 시험을 구별한다. 필요한 환경이 없으면 증거와 남은 검증을 명시하고 사람에게 요청한다. OpenCode 실증 미완료 등 사전 합의된 제한을 지원 현황에 그대로 표시한다. 그 밖의 필수 기준 미충족을 임의로 면제하지 않는다.

## 11. 공통 완료·검증 규칙

- 코드가 작성되거나 빌드가 성공했다는 이유만으로 완료하지 않는다. 활성 plan의 각 성공 기준에 시험·관찰·증거를 연결한다.
- 정상 경로뿐 아니라 해당 변경의 핵심 거절·실패·재시작 경로를 확인한다. 기대값은 구현 코드의 복제가 아니라 합의한 요구에서 도출한다.
- 실제 CLI·외부 서비스 시험, fixture/가짜 실행기 시험, 문서 검토를 구분한다. 미실행은 미실행으로 기록한다.
- 구현 뒤 변경 diff를 검토하고 누락·범위 이탈·비밀정보·승인 우회·데이터 보관 위반을 확인한다. 독립 검토 수단이 사용 가능한 경우 작성 입력과 분리해 검토하며, 수행하지 않은 독립 검토를 했다고 쓰지 않는다.
- 기존 기준을 완화해서 통과시키지 않는다. 중요한 의도·범위·권한 변경은 사용자 결정과 설계 문서에 반영한다.
- 검증에 필요한 명령을 기록한다. 아직 코드가 없는 지금은 가상의 테스트 명령이 실행 가능한 것처럼 적지 않는다. P1/P2에서 실제 구조가 정해지면 재현 명령을 추가한다.
- 검증한 변경은 허용 범위에서 로컬 커밋으로 구분해 남길 수 있다. 사용자의 기존 변경을 섞지 않는다. push·PR·병합 등 외부 반영은 해당 대상·범위의 별도 허용을 확인한다. 저장소 초기 연결 허용을 모든 미래 쓰기로 확대하지 않는다.

## 12. 세션 종료와 다음 세션 인계

종료 전에 반드시 다음을 수행한다.

1. 활성 plan의 완료/미완료/실패 항목과 실제 변경을 일치시킨다.
2. 해당 단계의 성공 기준별 결과와 검증 명령·대상·증거를 기록한다. 기준이 남으면 단계 완료로 표시하지 않는다.
3. 단계 진행표와 맨 위 현재 상태를 함께 갱신한다. 단계 완료 시 다음 단계는 READY로 만들고 현재 단계 포인터를 넘긴다. 구현은 다음 세션에서 시작한다.
4. 실제 브랜치·HEAD·미커밋 파일·남은 프로세스/시험 자원을 기록한다. 임의로 사용자 작업을 삭제하거나 자동 병합하지 않는다.
5. 아래 인계 양식을 채운다. 같은 대화를 기억한다는 전제 없이 다음 세션이 재현할 수 있어야 한다.

### 최신 세션 인계

```text
세션 기록 ID / 수행 시점: S-007 / 2026-09-21
작업 단계·하위 작업 / 사용한 Plan ID: P3 — 기능 개발 흐름 / P3-01 수준·설계·계획 / P3-PLAN-01
실제 작업 디렉터리·브랜치·최종 코드 커밋:
  C:\git\human-ai-dev-system-design, main. 시작 HEAD e2e4b50(작업 트리 clean, origin/main과 동기).
  이 세션의 커밋 목록은 `git log --oneline e2e4b50..HEAD` 로 본다(문서 커밋 포함 네 건).
  **push 하지 않았다.** 원격 main 은 e2e4b50 이며 로컬이 앞서 있다.
미커밋 변경과 소유 관계: 없음. 시작 시 clean이었고 이 세션 변경은 모두 커밋함.
  var\, .venv\, web\node_modules\, web\dist\ 는 .gitignore 대상이며 커밋하지 않았다.
  web\dist 는 빌드 산출물이므로 다음 세션에서 bootstrap 또는 npm run build 로 다시 만든다
구현·변경 내용:
  **"이 정도 준비가 필요한 이유"가 먼저 보이고, 그 준비가 실제로 갖춰진 뒤에만 기능
  구현이 배정된다.** P2까지 늘 거부됐던 `feature_implementation` 이 처음으로 허용된다.
  controller/sizing.py 신규 — 일곱 축에서 수준을 도출한다. **평균을 내지 않고**
    근거 부족을 낮음으로 읽지 않는다. `max(제안, 도출)` 이 AI의 조용한 하향을 막는다
  domain/intent_doc.py — **의도 문서 v3.** 축별 `근거 / 판단 / 미확인 / 영향`이 문서
    안으로 들어왔다. v1·v2 문서의 축은 0건이고 그 Case 는 **수준 미결정**이다
  domain/prep_doc.py 신규 — 설계·계획의 정규 문서와 **수준별 필수 항목 표**
  controller/schema.sql — **스키마 v5.** sizing_assessment / sizing_axis /
    preparation_artifact / preparation_section / stage_review_setting / stage_review /
    run_context_ref 신규. **전부 본문 컬럼 없음**
  controller/repository.py — 수준 반영·사람 조정, 준비 산출물 등록·구조 보고·단계별
    조회, 검토 모드와 검토 기록, 고정 컨텍스트 구성.
    `effective_required_sections()` 가 **현재 수준으로** 필수 항목을 판단하는 지점이다
  controller/admission.py — `prerequisite_not_implemented` 한 줄 거부를 **실제 선행
    조건 검사 10가지**로 바꿨다. 갖춰지면 허용한다. `workspace_write` 는 계속 거부
  controller/api.py — 준비 상태 종합 조회, 수준 조정, 단계별 산출물 조회·검토 모드·
    사람 검토·자동 진행 기록, 실행의 고정 컨텍스트 조회, Runner 산출물 등록·구조 보고
  runner/prompts.py, agent.py — design_authoring·plan_authoring 지시문과 산출물,
    **고정 컨텍스트 블록**(읽지 못한 참조는 "읽지 못했다"로 적는다),
    실행 경로 없는 목적은 CLI를 부르지 않고 실패로 보고
  web/src/PreparationPanel.tsx 신규 + App/api — 축별 판단 표·수준 조정(이유·남는 위험
    필수)·두 단계의 산출물·원문 열람·검토 모드·검토 기록. **`자동 조건 충족 (사람 승인
    아님)` 과 `사람이 검토함` 을 다른 문구로** 보인다
  tests/ — 123 → 169건. test_sizing.py(12)·test_preparation.py(30) 신규,
    데이터 경계·마이그레이션·재시작에 추가. 기존 시험 3개는 새 동작으로 옮김
  p3/evidence/P3-01-* 신규 23개, plans/P3-PLAN-01.md 신규, README 절차, DEVELOPMENT 갱신
성공 기준별 결과: 통과 / 실패 / 미검증 / 사용자 수용 제한
  **AC-1~15 전부 통과.** 자세한 근거는 p3/evidence/P3-01-results.md 8절.
  요약하면: 두 단계 기본값이 사람 검토이고 설정 행 없음을 자동 진행으로 읽지 않으며(AC-1),
  축별 판단이 의도 버전에 묶이고 합산되지 않으며(AC-2),
  **AI가 수준을 조용히 낮추지 못하고**(AC-3), 사람 조정에 이유·남는 위험이 필수이며
  다른 조건을 해제하지 않고(AC-4), 설계·계획을 각각 조회하며(AC-5),
  자동 진행이 산출물을 보존하고 사람 승인이 아니며(AC-6),
  산출물·항목·검토가 빠지면 사유별로 배정이 막히고(AC-7·8),
  오래된 승인이 재사용되지 않으며(AC-9), 이월 질문이 막고(AC-10),
  **갖추면 허용되지만 쓰기 권한은 생기지 않으며**(AC-11),
  **재작성이 이전 버전을 보고 퇴화하지 않고**(AC-12),
  데이터 경계가 유지되며(AC-13), 강제 종료 후 복원되고(AC-14),
  화면으로 확인했다(AC-15).
  **라이브로 하지 않은 것**은 결과 9절에 있다 — 자동 진행만으로 배정까지 가는 한 바퀴,
  설계 쪽 `design_incomplete_for_level`, 사람이 직접 입력한 설계·계획, 준비 산출물
  열람 중 재시작. 넷 다 자동 시험으로만 확인했다
실행한 검증 명령·환경·결과·증거 위치:
  PowerShell 7 / Windows 11 Home 10.0.26200 / Python 3.12.10 / Node v22.15.1 / Edge
  scripts\run-tests.ps1   → pytest 169 passed (46s) + P1 unittest Ran 18, OK
  web: npm run build      → tsc -b 통과, vite 빌드 성공
  라이브: uvicorn(127.0.0.1:8769)과 runner.agent 를 별도 프로세스로 띄우고
    **실제 codex-cli 0.154.0 을 3회, Claude Code 2.1.278 을 4회 실행**했다(합계 7회).
    브라우저는 이 PC에 이미 있는 Edge 를 Playwright 로 몰았고 **내려받지 않았다.**
    작업공간은 저장소 밖 임시 git 저장소(%LOCALAPPDATA%\Temp\hads-p3-01-live\workspace,
    reader.py 와 sample.log)이며 이 설계 저장소를 CLI에 노출하지 않았다
  증거: p3/evidence/P3-01-results.md 와 같은 디렉터리의 P3-01-live.log(전 구간), P3-01-ui.log, P3-01-ui-auto.log, P3-01-boundary.log,
    P3-01-ui-01~06.png, P3-01-case/preparation/admission-checks/gate-results/runners.json,
    P3-01-design/plan-artifacts.json, P3-01-intent/intent-v2/design/plan-original.txt
  **자동 시험은 실제 CLI를 부르지 않는다** — conftest 의 FakeCliExecutor 를 쓴다
변경 검토 결과 / 남은 위험:
  변경 diff 검토함. 비밀값·토큰·계정 식별자 없음. .venv/node_modules/dist/var 가 커밋에
  들어가지 않음을 확인. 시스템은 두 CLI의 자격증명을 읽지도 주입하지도 않는다.
  **라이브에서 찾아 고친 결함 3건과 스스로 검토하다 찾은 2건**은 결과 10절에 있다.
  그중 둘이 중요하다 — (가) **배정이 처음 허용된 순간 Runner 에 실행 경로가 없어 루프가
  죽었다.** 실행이 `assigned` 로 멈춰 사람은 왜 아무 일도 없는지 알 수 없었다. 지금은
  CLI를 부르지 않고 `failed` 로 보고한다. (나) **수준을 올려도 이미 있는 산출물이
  부족해지지 않았다** — 필수 항목을 작성 시점 수준으로 판단해 조정이 겉치레였다.
  지금은 Case 의 현재 수준으로 판단한다.
  **가장 중요한 라이브 결과:** codex 가 축 둘을 `insufficient_evidence` 로 적고도
  `simple` 을 제안했고, 축에서 도출한 `standard` 가 적용됐다(두 초안에서 반복).
  그리고 **별도 세션의 claude 가 같은 문제를 독립적으로 지적했다.**
  남은 위험 (1) **구현 실행 경로가 없다** — 배정만 열렸고 실행하면 `failed` 다.
      작업공간·브랜치·기준 커밋과 쓰기 권한은 P3-03이다
  (2) **시스템은 산출물의 내용이 충분한지 판정하지 않는다** — 필수 항목이 미정인지까지만
      본다. 한 줄만 써도 통과하며 그 판단은 사람 검토와 P4의 QG-02·QG-03의 몫이다
  (3) **이월 질문의 `blocks` 가 자유 문자열이다** — Task 로 잇는 것은 P3-02
  (4) **AI가 낳는 질문이 많다** — 라이브에서 설계·계획이 7건을 새로 올려 이월 질문이
      8건이 됐다. 전부 해결을 요구하므로 큰 기능에서는 사람의 부담이 커질 수 있다.
      질문의 중요도 구분은 이번 범위에 없다
  (5) **수준 도출 규칙은 구현 제안이다** — 실측으로 조정한 값이 아니다
  (6) P1·P2에서 넘어온 위험(residual_activity unknown, 안전 중지 미연결, 열람 요청의
      만료·크기 한도 없음)은 그대로다
새 사용자 결정·연결한 설계 변경:
  제품 설계 결정은 없다(D-01~58 변경 없음). 합의 범위 안의 구현 선택 5건을 근거와 함께 진행했다.
  (가) **효과 수준 = max(AI 제안, 축에서 도출한 수준)** — "낮은 항목들의 평균으로 중요한
      영향을 상쇄하지 않는다"와 "AI가 수준을 묵시적으로 낮추지 않는다"의 구체적 구현
  (나) **필수 항목은 Case 의 현재 수준으로 판단한다** — 산출물 작성 시점 수준으로 하면
      사람의 상향 조정이 겉치레가 된다. 항목 표에 `지금 필수`와 `보고 시점 필수`를 함께 둔다
  (다) **계획은 검토를 마친 설계 위에만 만든다** — 의존 관계가 `의도 → 설계 → 계획 →
      구현`이므로 설계 검토 전의 계획은 무엇을 구현할 계획인지 말할 수 없다
  (라) **자동 진행은 `decision` 행을 만들지 않는다** — 한 표에 섞으면 그것이 곧
      "사람 승인 기록 생성"이다. 주체도 사람 이름이 아니라 정책 식별자다
  (마) **진입 조건과 실행 경로를 분리해 다룬다** — 한쪽만 먼저 열릴 수 있고, 그때 배정된
      실행은 CLI를 부르지 않고 실패로 보고한다. 조건이 갖춰진 것과 실행할 수 있는 것은
      다른 사실이며 둘을 합치면 어느 쪽도 정확히 말할 수 없다
남은 프로세스·실험 저장소·외부 게시 상태:
  남은 제어부·Runner 프로세스 0개. 라이브가 쓴 포트 8769는 닫혀 있다.
  이 세션이 띄운 CLI 자식 프로세스도 0개다
  (claude 2개와 codex 1개가 이 PC에 떠 있으나 셋 다 라이브 시작 전부터 있던 것이며
  시작 시각이 2026-09-20 22:56 / 2026-09-21 00:17 / 2026-09-15 19:20 이다).
  GitHub 이슈·PR 등 외부 게시는 없다. **push 하지 않았다.**
  **외부 AI 전송은 있었다** — 사용자 계정의 codex 3회·claude 4회를 실행했고 각 CLI의
  기존 사용자 설정을 따랐다.
  라이브 시험 데이터 %LOCALAPPDATA%\Temp\hads-p3-01-live 는 지워도 된다(저장소 밖).
  이전 세션의 hads-p2-0*-live, hads-playwright, hads-p1\testrepo 도 그대로 두었다
단계 완료 여부와 이유:
  **P3-01 = DONE. P3 는 계속 IN_PROGRESS.** AC-1~15를 실제 실행으로 확인했고 수용
  제한을 새로 만들지 않았다. P3 단계 완료 조건(기능 개발 한 건을 끝까지 재현)은
  아직이다 — 코드를 바꾸는 실행이 P3-03에 있기 때문이다.
  **쓰기 권한은 여전히 열지 않았다** — 거부되는 것이 이번 단계의 올바른 동작이다.
  다음 하위 작업은 P3-02이며 구현은 다음 세션에서 시작한다.
다음 단계·하위 작업 / 다음 세션 첫 명령 또는 읽을 위치:
  P3-02 작업 그래프·질문. 먼저 scripts\bootstrap.ps1 과 scripts\run-tests.ps1 로 현재
  코드가 실제로 도는지 확인한다(**169 + 18 통과가 기준선**. bootstrap 이 web\dist 를
  다시 만든다). 그다음 [P3-01 결과](p3/evidence/P3-01-results.md)와 FR-07/13,
  sizing-and-review-ux.md 를 읽고 `P3-PLAN-02` 를 작성해 공유한 뒤 구현한다.
  **P3-02에서 유의할 것**:
  - **이월 질문 기록이 이미 있다.** `intent_question` 에 `decide_at`·`raised_in_stage`·
    `preparation_id` 가 있고 `blocks` 는 산출물 원문 안의 자유 문자열이다. P3-02는
    그 `blocks` 를 Task ID 로 잇는 것이고, 질문 표를 새로 만들 필요는 없다
  - **`deferred_questions_unresolved` 는 지금 Case 수준으로 막는다.** P3-02는 그것을
    "그 결정에 의존하는 Task 만 막는다"로 좁히는 일이다. 좁히는 것이 느슨해지는 것이
    되지 않도록 주의한다 — 의존 관계가 비어 있으면 여전히 전부 막아야 한다
  - **성공 기준과 Task 를 잇는 자리가 비어 있다.** `success_criterion` 은 의도 버전에
    묶여 있고 `criterion_result` 는 근거 Run 을 가리킨다. P3-02는 요구·기준과 구현/검증
    Task 를 잇는다(FR-07)
  - **AI가 낳는 질문이 많다**(위험 4). 라이브에서 8건이 됐다. Task 의존으로 좁히면
    이 부담이 실제로 줄어드는지 확인할 좋은 기회다
  - 게이트 ON/OFF 설정과 수정 2회 한도는 P4다. P3에서 앞당기지 않는다
  - 쓰기 권한·작업공간은 P3-03이다. P3-02에서 열지 않는다
막힌 조건과 필요한 사용자 답변:
  **origin/main push 허용 여부를 받아야 한다.** 이전 세션의 허용은 그 커밋들에만
  적용되며 이후 push 로 확대되지 않는다. 허용이 없으면 로컬 커밋만 유지하고
  다음 세션이 이 checkout 을 그대로 이어서 쓴다
```

- **주의:** OpenCode는 조사 범위에서 설치 흔적이 없어 문서 계약만 다룬다(P1-04). 기존 CLI 로그인 정보·비밀값을 원문 로그로 기록하지 않는다.

다음 구현 세션도 위 형식으로 인계를 갱신하고 이전 결과는 세션 이력에 요약해 남긴다.

```text
세션 기록 ID / 수행 시점:
작업 단계·하위 작업 / 사용한 Plan ID:
실제 작업 디렉터리·브랜치·최종 코드 커밋:
미커밋 변경과 소유 관계:
구현·변경 내용:
성공 기준별 결과: 통과 / 실패 / 미검증 / 사용자 수용 제한
실행한 검증 명령·환경·결과·증거 위치:
변경 검토 결과 / 남은 위험:
새 사용자 결정·연결한 설계 변경:
남은 프로세스·실험 저장소·외부 게시 상태:
단계 완료 여부와 이유:
다음 단계·하위 작업 / 다음 세션 첫 명령 또는 읽을 위치:
막힌 조건과 필요한 사용자 답변:
```

### 세션 이력

| 세션 | 시점 | 단계·하위 작업 | 결과 요약 |
|---|---|---|---|
| S-001 | 2026-09-20 | 설계 정리 | 설계 v0.6 문서와 이 안내서 준비. 제품 코드·실증 없음 |
| S-002 | 2026-09-20 | P1-01~04 | 환경·CLI·인증 실측 확정, 공통 실행 계약 초안 v0와 `P1-PLAN-01` 기록. harness로 두 CLI의 읽기·쓰기·권한 거절·세션 분리를 실제 실행. 이어서 PreToolUse 훅으로 단절 주입 시험을 해 **다음 도구 호출이 실제로 시작되지 않음**을 부수효과로 확인하고, 강제 종료가 안전 중지가 아님을 프로세스 잔류로 확인. 이어서 OpenCode V2 문서 계약·재구성 fixture·계약 시험 18개를 만들고 세 도구 capability 표를 채움. P1은 '중복 호출 방지' 미검증 1건을 사용자 수용으로 제한 종료(ACCEPTED_WITH_LIMITATIONS)하고 P2-01로 이월. P2 READY |
| S-003 | 2026-09-20 | P2-01 | **제품 코드 첫 골격.** Python 3.12 고정, FastAPI 제어부·SQLite·로컬 Runner·React/TS 화면을 만들고 실제 프로세스와 브라우저로 한 줄 연결을 확인. 강제 종료 후 복원, 같은 (run_id, seq) 이벤트·같은 run_id 요청의 중복 방지, 결과 보고 유실 후 재배정에서도 실행 1회를 부수효과로 확인해 **P1 이월 항목을 닫음**. 원문이 제어부 DB·로그에 없고 Runner에만 있음을 파일 바이트로 확인. pytest 19 + P1 계약 unittest 18 통과. P2-02~04 남음 |
| S-004 | 2026-09-20 | P2-02 | **의도·피드백 흐름.** 여섯 항목 초안·원문 열람·피드백·미정 질문·명시 동의를 만들고 실제 프로세스와 브라우저로 한 바퀴 확인. 원문 일시중계 읽기 경로를 P2-02에 넣기로 정하고 구현(이전 세션 이월 항목). 조회·피드백·질문 답변·무응답이 동의가 되지 않고, 오래된 동의가 최신 버전에 적용되지 않음을 실증. 동의 거절 5조건을 사유로 구별. 의도·질문·피드백·답변·열람 본문이 제어부 파일 바이트에 없음을 확인. 시험 중 발견해 고친 것 3건(질문 요약이 본문 발췌였던 것, 원문을 읽지 않고 동의 가능했던 것, P2-01의 옛 동의 버튼이 검사를 우회하던 것). pytest 39 + P1 계약 unittest 18 통과. P2-03·04 남음 |
| S-005 | 2026-09-20 | P2-03 | **실제 코딩 CLI 연결 · QG-01 · FR-29 진입 검사.** 실제 codex·claude 를 6회 실행해 AI가 의도 초안을 쓰고, 작성과 별도 세션이 QG-01로 검토하고, 동의된 의도에 대해 읽기 전용 제한 작업을 수행하는 한 바퀴를 확인. **QG-01이 AI 초안을 두 번 연속 실패시켰다**(모순·확인 불가능한 성공 기준·빠진 질문) — 게이트를 느슨하게 만들지 않고 피드백→새 버전→재검토로 통과시킴. 진입 조건 검사를 서버에 두어 직접 API 호출 8건을 사유 코드로 거부하고 6건을 허용. 동의가 있어도 게이트 미통과면 거부됨을 실증. 코드 변경 실행은 선행 조건이 없어 `prerequisite_not_implemented` 로 **거부**(임시 통과 처리 없음). P2-02 이월 항목(AI 작성 역할 분리) 종료. 화면도 브라우저로 확인했고 그 과정에서 결함 하나(초안 요청이 골격 실행기로 배정되던 것)를 찾아 화면과 서버 양쪽에서 고침. pytest 79 + P1 계약 unittest 18 통과. P2-04 남음 |
| S-006 | 2026-09-20~21 | P2-04 | **결과·근거와 사람 최종 확인.** 성공 기준을 의도 문서 안에 넣고, 기준마다 판정·근거·판정 주체를 남기고, 사람이 최종 인수하거나 표시된 미충족을 예외로 수용해 종료하는 흐름을 만들었다. **실제 codex·claude 13회**와 **브라우저 두 바퀴**로 처음부터 끝까지 확인했다(정상 종료 1건, 예외 수용 종료 1건). QG-01이 여섯 번 판정해 **다섯 번 막았고** 그중 둘은 사람이 쓴 초안의 실제 모순이었다. 제어부·Runner 강제 종료 후 전부 복원. 라이브에서 결함 3건(대체된 버전을 검토하던 화면, AI 재작성이 피드백을 닫지 못하던 것, 종료 거부가 진입 기록에 없던 것)과 검토로 2건을 찾아 고쳤다. **고치지 않고 드러낸 것 둘** — AI 재작성이 이전 버전을 보지 못하는 것(P3·P4로 이월)과, 근거가 판정을 뒷받침하는지는 시스템이 강제하지 못한다는 경계. pytest 123 + P1 계약 unittest 18 통과. **P2 완료**, P3 READY |
| S-007 | 2026-09-21 | P3-01 | **수준·설계·계획과 준비 선행 조건.** 일곱 축의 근거와 함께 AI가 수준을 제안하고 사람이 이유·남는 위험을 적어 조정한다. 설계안과 개발계획이 각각의 버전·항목·검토 상태로 만들어지고 두 단계의 사람 검토가 서로 독립이다. **`prerequisite_not_implemented` 한 줄 거부를 실제 선행 조건 검사 10가지로 바꿨고, 갖춰지자 `feature_implementation` 이 처음으로 허용됐다**(쓰기 권한은 계속 거부). P2-04가 남긴 위험 1(재작성이 이전 버전을 보지 못함)을 고정 컨텍스트로 닫았다 — 라이브 재작성이 퇴화하지 않고 기준이 4→5건으로 늘었다. **실제 codex·claude 7회**와 브라우저로 확인했다. codex 가 축 둘을 근거 부족으로 적고도 `simple` 을 제안했고 도출값 `standard` 가 적용됐으며, 별도 세션의 claude 가 같은 문제를 독립적으로 지적했다. 라이브에서 결함 3건을 찾아 고쳤다(배정 허용 직후 Runner 루프 사망, 수준 상향이 기존 산출물에 반영되지 않음, 원문 열람 단발 조회). pytest 169 + P1 계약 unittest 18 통과. **P3-01 완료**, P3-02 남음 |

안내서·계약 문서 작성 자체를 P1 완료로 기록하지 않는다.

## 13. 진행 중에도 유지할 제품 경계

핵심 사용자 결정의 빠른 확인 목록이다. 세부 근거는 [D-01~58](decisions.md)을 따른다.

| 주제 | 변경하면 안 되는 기준 |
|---|---|
| 의도 | 기능 개발의 여섯 필드 초안·피드백·최신 명시 동의, QG-01 필수 잠금 |
| 준비 | 간소/표준/심층, 필요한 설계·계획 선행. 기본 사람 검토, 업무별 독립 자동 옵션 |
| 품질 | 별도 AI 의미 검토 세션, 기본 수정 2회, 게이트 OFF와 합의 기준·승인 분리 |
| 설정 경계 | 게이트 설정은 현재 검증 1회 종료 후 적용. Runner 단절은 현재 도구 호출 종료 후 정지 |
| 업무 다양성 | 명확한 버그·연구는 목표·종료조건으로 시작. 미명시 실험은 한 번 범위 확인 |
| 종료 | 기본 사람 인수·업무별 자동 완료, 명시적 예외 종료는 원 판정 보존, 완료 후 수정은 새 Case |
| 실행 | Windows 네이티브, 한 사용자, 프로젝트당 저장소 하나, Case branch/worktree, 신뢰 모드의 격리 한계 표시 |
| 데이터 | 서버+PC에서는 서버 상태/요약/참조, PC의 모든 상세 원문, 인증된 일시중계 |
| CLI | 프로젝트 기본과 역할별 설정, 다른 CLI 장애 대체 전 확인, 외부 AI 전송은 기존 CLI 설정 |
| GitHub | Case마다 새 기록 이슈, 상단 표시·요약 append·코드/diff/상세 로그 제외, PR 리뷰만 피드백 |
| 외부 쓰기 | 확인받은 push·PR 생성/수정만 v1. 병합·이슈 종료·CI 수동 재실행·배포는 확장 |
| 운영 | 총 자동 실행 기본 시간 한도 없음, 사용량 표시, 호스트별 하루 백업·최근 7개·원본 자동 삭제 없음, LAN/VPN |
| 확장 | 공동 사용·복수 프로젝트·지식 자동화·Linux/WSL·인터넷 공개는 초기 구현 범위에 넣지 않음 |

현재 단계가 다른 제품 정책을 필요로 하는 것처럼 보이면 해당 의존 관계와 근거를 제시한다. 작업 편의를 위해 위 기준을 조용히 바꾸지 않는다.
