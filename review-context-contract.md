# 별도 AI 검토의 입력·출력 계약 기준안 v0.7

작성일: 2026-09-21. 상태: D-01~67의 관련 결정을 통합한 기준안이며 구현·호환성·운영 검증 완료를 의미하지 않는다. **독립 AI 의미 검토가 필요한 경우 작성과 별도 세션에서 수행한다.** Fast Lane에서 요청이 명확하고 저위험이며 중요한 의미 변경이 없으면 별도 검토 실행을 의무화하지 않는다. 규칙·실제 테스트만으로 판정할 항목에도 AI 실행을 추가하지 않는다. 프로젝트 기본 CLI·모델과 업무·역할별 설정을 사용하며 사전 허용되지 않은 다른 CLI로 장애 대체하기 전에는 사람에게 확인한다. 같은 CLI·모델도 가능하며 별도 세션 자체가 정확성을 보장하지는 않는다. 적용 정책은 [자율 진행·예산](autonomy-budget-policy.md)을 따른다.

## 1. 목적과 책임 경계

별도 검토 실행은 작성자의 결론을 재승인하는 과정이 아니라, 지정된 산출물을 현재 기준과 원문 근거에 대조하여 관찰·누락·불확실성을 반환하는 작업이다. 작성자의 자체 점검은 별도로 남길 수 있지만 실제 사용자 요구나 실행 증거를 대신하지 않는다.

검토 AI에는 읽기·분석 역할을 배정하고 구조화된 발견을 반환하게 한다. 코드·문서·기준·게이트 설정 변경이나 사용자 승인 생성은 그 역할에 포함하지 않는다. CLI 권한 설정과 시스템 승인으로 통제하지만 신뢰하는 개인 프로젝트용 Windows native 첫 버전에서 완전한 OS 격리를 보장하지 않는다. 테스트·빌드·재현이 더 필요하면 실행 제안을 반환하고 시스템이 기존 허용 범위와 선행 조건을 확인하여 Runner에 별도 검증 작업을 배정한다.

기본은 ask-on-decision이다. 원래 요청을 목표·범위·성공 기준으로 정리하고 중요한 의미 변경(material delta)과 미해결 사람 판단을 확인한다. 명확한 저위험 요청은 기능 개발이어도 동일 의도에 대한 재승인 없이 필요한 구현·검증을 진행한다. 새로운 제품 동작 선택, 중요한 범위·기준 변경, 권한 확대·예산 한도 변경·예외 수용은 해당 결정 전에 확인한다. controlled는 최초 의도 확인과 최종 결과 확인을 요구하며 별도 의미 검토·품질 판정과 구별한다. 연구·분석의 복구 가능한 로컬 실험은 기존 권한·예산 안에서 자동 수행하고, 그 경계를 넘는 활동은 확인한다.

역할을 다음처럼 분리하는 안을 제안한다.

| 역할 | 책임 |
|---|---|
| 컨텍스트 구성기 | 해당 검토에 필요한 최신 기준·원문·산출물·적용 지식의 버전을 고정하고, 필수 내용과 조건·예외, 필요한 범위와 추가 조회 경로를 제공 |
| 검토 AI | 제공·조회한 근거에서 발견 사항, 확인한 범위, 미확인 항목과 추가 검증 제안을 반환 |
| 결과 검증기 | 출력 형식, 참조 존재·버전, 실제 도구 실행 기록과의 일치, 입력 누락·검토 범위 확인 |
| 게이트 평가 서비스 | 유효한 검사·발견을 기준별 평가 상태로 정리. 의견과 증명된 위반을 구분 |
| 정책·스케줄러 | 현재 업무 Profile·자율성·게이트 정책, 입력 유효성·필요한 사람 판단·실행 권한·설정된 예산을 확인해 후속 진행 가능 여부 결정 |

검토 AI가 출력한 `통과 권고`만으로 게이트를 통과시키거나 실행을 시작하지 않는다. 반대로 검토자의 새 아이디어를 사용자 동의 없이 새로운 필수 요구로 추가하지 않는다. 어떤 위반 판정에 사람 확인이 필요한지와 재검토 절차는 운영 정책에 연결한다.

## 2. 검토 입력 계약

입력은 전체 대화 복사본 대신 **필수 핵심 정보 + 버전이 고정된 참조 목록 + 추가 조회 경로**로 구성한다. 입력 요약은 파생 자료다. 서버+개인 PC Runner 구성에서는 서버의 상태·요약·참조와 PC의 상세 원문을 합쳐 논리적 시스템 기록으로 관리한다. 상세 코드·diff·로그·의도·설계·계획·대화·검토 입력과 출력은 PC에 보관하고, 원문을 필요로 하는 AI 검토도 해당 Runner에서 실행한다.

서버는 원문 ID·버전·해시와 상태·요약만 영구 보존한다. 웹의 상세 열람·검토는 PC가 연결된 동안 원문을 일시중계하며 서버 DB·로그·캐시·추적 기록에 영구 복사하지 않는다. PC가 끊기면 원문 접근 불가를 표시하며 서버 요약을 원문 검토의 대체물로 삼지 않는다. 단일 호스트 구성에서는 이 논리 경계가 같은 PC에 배치된다. 외부 AI 전송은 기존 CLI 사용자 설정을 따르고 자격증명은 Runner에 유지하므로 이 구조를 오프라인 모델 처리라고 설명하지 않는다.

| 입력 묶음 | 포함할 내용 | 누락·불일치 시 처리 |
|---|---|---|
| 대상과 범위 | Case·Task·GateRun 식별자, 게이트 정의 버전, 검사 목적, 검토할 영역과 제외 영역 | 검사 대상이 불명확하면 검토 불가로 반환 |
| 목표·의도·사람 피드백 | 원문 요청·피드백과 반영 관계, 현재 목표·범위·종료조건, Profile·자율성, material delta와 필요한 결정의 상태·근거 | 최초 요청의 권한과 별도 동의를 구분. controlled의 최초 확인 또는 중요한 변경의 확인 대기는 그대로 표시하고 자동 진행을 사람 승인으로 기록하지 않음 |
| 요구의 성격·질문 시점 | 사용자 확인, 프로젝트 규칙, 사실, AI 가정·제안, 질문·출처, 결정할 단계와 의존 작업 | 의도 단계에 필요한 결정을 임의로 미루지 않음. 설계·계획으로 이월한 사람 결정은 의존 작업 전 확인. 단계 자동 진행이 이를 대신하지 않음 |
| 현재 기준 | 성공 기준과 검토 기준의 ID·버전·필수/권고 구분·적용 범위, 확인 방법, 기준 변경 이력 | 없는 수치·정책·제외사항을 검토자가 임의 보충하지 않음 |
| 정확한 대상 상태 | 문서 버전·내용 해시, 대상 Repository별 기준 커밋과 검토 커밋 또는 보존한 변경 스냅샷, 관련 설정·의존성 버전과 통합 대상 조합 | 작업공간 경로와 브랜치 이름만으로 대상 고정 불가. 실행 중 바뀐 상태나 다른 Repository의 증거를 같은 대상으로 섞지 않음 |
| 실행 증거 | 실제 Run ID, 입력 코드 상태, 실행 환경·도구·명령 참조, 종료 상태, 원시 결과 위치, 기준별 연결 | 작성자의 ‘테스트 성공’ 문장만 있으면 미확인. 문서 게이트 등 불필요한 환경 항목은 명시적으로 해당 없음 처리 |
| 관련 설계·계획·지식 | 필요한 인터페이스, 설계 결정·근거, 적용되는 지식의 ID·버전·필수/참고·권위·조건·예외 | 필수 내용은 링크만으로 대체하지 않음. 후보·AI 제안을 필수 규칙으로 승격하지 않고 가용성·적용 불확실성을 표시. 주입은 준수 증거가 아님 |
| 이전 검토 | 기존 발견의 안정 ID, 당시 근거·대상 버전, 수정 내역, 재확인할 범위 | 같은 발견의 재발과 해결 여부를 추적. 앞선 검토자의 평가도 재확인할 자료이며 사실로 강제하지 않음 |
| 입력 목록과 조회 계약 | 포함·제외 자료 목록, 자료별 원본 ID·버전, 조회 가능한 허용 범위, 현재 조회 불가 항목 | 필요한 자료가 없으면 부족한 항목을 명시. ‘찾지 못함’을 ‘문제 없음’으로 바꾸지 않음 |
| 종료 후보와 결정 | QG-06이면 후보 버전, 기준별 결과, 남은 범위·영향, 인수·예외 기록 참조, 실제 실행 정리 상태 | 기준·검증 충족 후 기본 자동 완료와 controlled의 최종 확인을 구분. controlled의 확인은 코드 push·PR 반영 전에 수행. 예외가 있어도 원래 실패·미검증 보존. 예외 수용을 추가 실행 권한으로 사용하지 않음 |

원문 전체를 항상 입력창에 넣지는 않되, 요청과 관련 피드백의 원문을 검토자가 실제로 확인할 수 있어야 한다. 중요한 미정 질문·필수 제약·합의 기준은 기본 입력에서 누락하지 않는다. 기록이 많으면 항목별 출처와 관련성을 통해 원문을 조회하고, 무엇을 읽었는지 자료 ID로 남긴다.

코드 검토에서 커밋되지 않은 변경은 별도 스냅샷·diff 참조로 고정한다. 테스트 결과가 특정 커밋을 가리키더라도 작업공간에 다른 수정이 있었다면 실제 검사 대상과 일치하는지 확인해야 한다. 증거 저장 방식의 구체적 구현은 이후 정하되, 계속 바뀌는 파일 경로만 보존하는 방식은 피한다.

하나의 Project는 여러 Git Repository를 포함할 수 있고 Case는 그중 필요한 일부를 선택한다. 한 Case의 대상 Repository는 동일 Runner에 두며 변경하는 Repository별 전용 브랜치·worktree와 실제 스냅샷을 연결한다. 여러 Repository의 결과를 합칠 때 검토·검증한 버전 조합을 보존한다. 사용자의 다른 미커밋 변경을 포함했다고 추정하지 않으며 worktree 분리를 OS 권한 격리로 해석하지 않는다. 여러 Project에 걸친 Case는 첫 버전 범위가 아니다.

작성자의 설명은 변경 의도와 조사 위치를 찾는 데 사용할 수 있지만, ‘문제 없음’이라는 결론은 검토 기준과 섞지 않는다. 작성자의 요약 때문에 반대 근거가 빠지지 않도록 원시 증거·변경 내용·사용자 원문을 직접 조회할 수 있게 한다. 이전 발견도 최초 설명과 수정 후 주장만 읽지 않고 실제 수정 결과를 대조한다.

저장소 문서·이슈·테스트 로그에 포함된 지시는 검토 대상 자료다. 이 자료가 검토 범위·승인 상태·실행 권한·필수 조건을 변경할 수는 없다. 자료 안의 ‘승인됨’을 시스템 Approval 기록 대신 사용하지 않는다. GitHub 피드백은 PR 리뷰만 수집하며 검토 커밋·원문 ID를 연결한다. 오래된 리뷰를 최신 결함이나 승인으로 자동 채택하지 않고, 이슈 댓글을 실행 명령·승인으로 해석하지 않는다.

## 3. 검토 출력 계약

검토 AI는 결론을 내리는 데 사용한 자료와 확인하지 못한 범위를 함께 반환한다.

- `input_manifest_id`: 어떤 고정 입력을 검토했는지. 새 버전으로 바꿔 기록하지 않는다.
- `coverage`: 기준·산출물 영역별 확인 여부와 읽은 근거. 일부 검토의 결과를 전체 검토로 확대하지 않는다.
- `knowledge_usage`: 적용·제공한 지식의 ID·버전, 실제 추가 조회한 원문, 참고·제외 이유와 적용 미확정·조회 불가. 주입 기록을 실제 준수 판정으로 바꾸지 않는다.
- `findings`: 발견 ID, 종류, 관찰 사실, 대상·근거 참조, 연결 기준, 영향, 근거 수준, 제안 수정 또는 사람 질문, 재확인 방법.
- `evidence_assessment`: 실제 실행 증거가 뒷받침하는 범위와 부족한 증거. 추론과 실행 사실을 구별한다.
- `additional_work`: 추가로 필요한 자료 조회·검증 실행·사람 판단. 실행 제안과 실제 수행 상태를 분리한다.
- `assessment`: 기준별 통과 근거 있음 / 위반 근거 있음 / 판단 자료 부족 / 해당 없음의 제안. 후속 진행의 최종 결정은 포함하지 않는다.

CLI가 출력한 일반 문장을 구조화된 결과로 변환하더라도 원문과 변환 기록을 연결한다. 필수 필드나 근거 참조가 없으면 결과를 보완 요청 또는 실행 오류로 처리하고 성공으로 해석하지 않는다. 구조화 출력 자체도 내용의 진실성을 보장하지 않는다.

‘관련 코드를 읽어보니 기대 동작을 할 것 같다’는 정적 관찰이며 ‘테스트가 통과했다’는 실행 증거가 아니다. 검토 AI가 테스트 결과를 인용하려면 실제 검증 Run과 결과를 연결해야 한다. 미실행 명령은 실행 제안으로만 기록한다. 종료 코드 0만으로 모든 성공 기준을 충족했다고 판정하지 않는다.

다음은 하나의 의도 초안을 검토하는 간단한 입출력 형태 예시다. ID·해시 표기는 설명용이며 실제 자료를 나타내지 않는다.

```json
{
  "review_input": {
    "manifest_id": "CTX-17",
    "gate": {"id": "QG-01", "definition_revision": 2},
    "target": {"artifact_id": "INTENT-7", "revision": 3, "content_hash": "illustrative-hash"},
    "intent": {"approval_state": "pending", "source_refs": ["MSG-1", "FEEDBACK-4"]},
    "criteria_refs": ["INTENT-QUALITY@2"],
    "open_question_refs": ["QUESTION-ROWS@1"],
    "evidence_refs": [],
    "retrieval_scope": ["CASE-7", "PROJECT-2:read"],
    "source_location": {"runner_id": "RUNNER-PC-1", "server_persists_original": false},
    "role_policy": {"read": true, "modify": false, "execute_project_commands": false}
  },
  "review_output": {
    "input_manifest_id": "CTX-17",
    "coverage": {"reviewed": ["INTENT-7@3", "MSG-1", "FEEDBACK-4"], "unreviewed": []},
    "findings": [{
      "id": "FINDING-9",
      "kind": "unsupported_assumption",
      "criterion_ref": "INTENT-QUALITY@2:source-and-status",
      "target_ref": "INTENT-7@3:scope",
      "evidence_refs": ["MSG-1", "QUESTION-ROWS@1"],
      "observation": "행 범위는 미정인데 현재 페이지 50행이 확정 범위로 작성됨",
      "proposed_action": "미정 상태로 정정하고 행 범위를 사람에게 확인"
    }],
    "assessment": "revision_needed",
    "additional_work": [{"kind": "human_question", "ref": "QUESTION-ROWS@1"}],
    "execution_claims": []
  }
}
```

이 예시에서는 실제 제품 선택이 남아 있어 초안을 별도 검토한 경우를 보여준다. 검토 AI가 답을 대신 선택하지 않으며 QG-01 평가와 필요한 사람 결정은 별개다. 모든 요청에 같은 검토 실행이나 의도 동의를 요구하는 예시가 아니다. `role_policy`는 시스템이 요청한 역할 범위이며 OS 수준 강제 격리를 검증했다는 뜻이 아니다. 실제 상세 입력·출력은 해당 PC에서 보존하고 서버에는 요약·참조만 남긴다.

## 4. 컨텍스트 초과·분할·재시작

컨텍스트가 커지면 과거 대화를 계속 압축해 이어가는 방식에만 의존하지 않는다. 실행마다 입력 크기를 점검하고 필수 핵심을 유지하며 관련 자료를 단계적으로 조회한다. 어느 자료를 보지 못했는지 명시해야 하므로 모델이 내용을 모두 기억했다고 가정하지 않는다.

1. **핵심 보존:** Profile에 맞는 목표·종료조건, 의도·material delta·필요한 결정 상태, 필수 제약의 실제 내용·조건·예외, 기준, 미정 질문의 결정 단계·의존 작업, 검토 범위·버전을 먼저 제공한다. 설정된 입력 예산 때문에 조용히 삭제하지 않고 분할하거나 해당 의존 작업을 보류한다.
2. **필요 자료 조회:** 파일·문서 전체 대신 관련 절·diff·원시 결과를 읽되, 참조 경계나 반대 근거를 확인할 수 있는 조회 경로를 유지한다. 인덱스·검색 결과만으로 전체 검토 완료를 주장하지 않는다.
3. **검토 분할:** 영역별로 검토할 수 있으면 동일한 기준 버전을 공유하는 하위 검토로 나눈다. 경계·공유 데이터·통합 효과를 최종 대조하는 검토를 남긴다. 모든 하위 검토가 통과했다는 사실만으로 통합 검토를 통과시키지 않는다.
4. **중간 기록:** 검토한 기준·자료, 발견, 아직 확인하지 않은 항목과 다음 조회 위치를 저장한다. 체크포인트는 완료 판정이 아니다.
5. **재시작:** 새 세션은 고정 입력 목록·체크포인트·원본 참조에서 복원한다. 새 세션이라는 이유로 실패 이력·시도 횟수·누적 예산 사용량·남은 질문을 초기화하지 않는다. 원본 조회에 실패하면 미확인 상태를 유지한다.
6. **최신성 재검사:** 결과 도착 시 기준·대상·실제 적용 정책을 대조한다. 오래된 결과는 당시 이력으로 보존하며 현재 통과로 쓰지 않는다. 영향 없는 결과의 재사용은 입력·의존 관계로 판단한다. 실행 중 예약 설정만으로 현재 검토를 무효화하지 않고 **현재 검증 1회 종료 후** 예약 정책을 적용하여 다음 수정·재검증을 판단한다.
7. **Runner 단절:** 현재 도구 호출 종료의 안전 경계에서 멈추고 재연결 후 실행·원문·최신 정책을 대조하여 재개한다. 도구 호출 종료는 검증 1회 전체의 종료와 다르며 단절만으로 예약 정책을 적용하거나 다른 Runner에 중복 배정하지 않는다. 실제 경계 제어는 CLI·버전·모드별 실증 대상이다.

지속 보존 대상은 원문 요청·피드백·결정, 산출물 버전, 입력 목록과 조회 자료 참조, 검사 증거, 발견, 재검토 관계와 체크포인트다. 상세 원문은 PC, 상태·요약·참조는 서버라는 확정 경계를 따른다. 모든 대화·원시 로그를 매 세션에 복제하거나 모델 내부 사고 과정을 보존할 필요는 없다. 각 호스트의 지정 경로에 하루 1회 자동 백업하고 최근 7개 백업을 유지한다. PC 원문을 서버 백업으로 복제하지 않으며 이 순환 규칙으로 원본·승인·증거를 자동 삭제하지 않는다.

GitHub에는 Project의 지정 기록 Repository에 매 Case 새 기록 이슈 하나를 만들고 의도·설계·계획·검증의 요약과 참조만 누적한다. 코드·diff·상세 로그는 제외하고 기존 이슈는 출처로 연결한다. 게시 전 내용 점검은 수행하지만 완벽한 비밀정보 필터를 주장하지 않는다. 게시 장애 중에도 내부 검토를 계속한다. 코드 push·PR 생성·수정은 준비된 실제 대상·내용이 유효한 사전 허용 범위에 드는지 확인하며, 허용이 없거나 범위를 벗어나면 사람에게 확인한다. controlled의 최종 결과 확인은 코드 push·PR 반영 전에 수행한다. 첫 버전에서 병합·이슈 종료·CI 수동 재실행·배포를 수행하지 않는다.

### 지식 적용과 가용성

[프로젝트 지식 계약](project-knowledge.md)에 따라 컨텍스트 구성기가 작업 범위·활동에 맞는 내용을 제공한다. 단계와 지식 종류를 일대일로 고정하지 않으며 별도 설계 단계 없는 Fast Lane에도 관련 필수 내용을 포함한다. 범위·관련 지식 변경 시 재평가하고 유효한 입력은 재사용한다. ContextManifest는 선택·제공·제외·적용 미확정·조회 불가를 구분한다. 등록된 규칙의 포함을 모든 관련 지식 발견이나 AI 준수의 보장으로 표현하지 않는다.

한 Case의 Repository가 같은 Runner에 있다는 사실은 과거 지식 원문까지 그 Runner에 있다는 뜻이 아니다. 원본 소유·버전·가용성을 확인하고 필수 내용을 확인할 수 없으면 해당 의존 작업만 보류한다. 참고 자료 접근 실패는 한계로 표시한다. 원문 조회 실패·범위 불확실을 규칙의 무효나 미적용으로 바꾸지 않는다. 다른 Case의 지식 갱신도 기존 Run의 고정 입력을 덮어쓰지 않으며 실제 요구 변경이면 material delta로 처리한다.

## 5. 수용 시나리오

| 시나리오 | 기대 결과 |
|---|---|
| 작성 AI가 CSV 행 범위를 임의 확정 | 검토자가 사용자 원문·미정 질문과 초안을 대조하여 근거 없는 확정을 지적한다. 이를 미정 질문으로 복원하며 검토 AI가 임의 정책을 대신 선택하지 않는다. |
| 테스트 미실행인데 작성 요약에 성공이라고 기록 | 연결된 Runner 검증 기록이 없으면 실행 증거 없음으로 남긴다. 코드 정적 관찰만으로 테스트 통과를 생성하지 않고, 필요한 실행은 별도 작업으로 제안한다. |
| 검토 시작 후 코드 또는 요구 변경 | 결과를 시작 당시 입력 버전에 귀속시킨다. 변경의 영향을 받는 검토는 최신 상태의 진행 근거로 사용하지 않으며, 사용자 승인·게이트 설정을 이전 값으로 덮어쓰지 않는다. |
| 장기 검토 중 입력 한계 또는 세션 종료 | 원문·필수 제약·질문을 잃지 않고 분할 또는 체크포인트 복원한다. 읽지 못한 범위가 남으면 전체 완료를 주장하지 않고, 실제 읽은 자료와 부분 결과를 추적할 수 있다. |
| 큰 작업을 여러 검토로 분할 | 각 영역 검토 결과뿐 아니라 영역 간 계약·공유 데이터·동일 기준 버전을 확인한다. 검토 범위가 겹치거나 빠진 항목을 드러내며 전체 검토 누락을 개별 통과 수로 감추지 않는다. |
| PC 연결 없이 상세 검토 요청 | 서버의 상태·요약은 보되 원문 열람 불가를 표시하고 원문이 필요한 판정을 보류한다. 요약으로 원문을 본 것처럼 기록하지 않으며 서버에 상세 원문을 영구 복사하지 않는다. |
| 설계로 이월한 사람 질문 | 결정할 단계·의존 작업이 입력에 남고 해당 작업 전에 사람 결정을 받는다. 자동 설계·계획 옵션이나 게이트 OFF로 이를 지우지 않는다. |
| 자동 완료 모드에 남은 미검증 | 사람에게 범위·영향을 제시하고 예외 수용을 요청한다. 수용 후에도 판정은 미검증이고 추가 실행 권한을 만들지 않는다. |
| 완료한 업무의 수정 요청 | 원래 검토·종료 기록을 보존하고 새 Case로 연결한다. 새 기능 개발은 새 의도·검토 조건을 적용하며 과거 인수를 새 범위의 동의로 사용하지 않는다. |

## 6. 확정 범위와 남은 검증

독립 의미 검토가 필요한 경우 별도 세션을 사용하고 프로젝트 기본 CLI·모델 및 업무·역할별 설정을 적용한다. 장애·비용 때문에 권한이나 공급자 범위를 몰래 바꾸지 않는다. 추가 검증은 합의 범위·기존 허용 안에서 수행하고 자동 수정·재검증은 최초 실패 후 기본 최대 2회이며 업무별 조정한다. 예산은 기본 무제한이지만 설정된 한도는 검사·강제하고 사용량·경과 시간·미제공 값을 구분한다. 합의 범위·필수 결정·품질 수정 횟수는 계속 적용한다. 개별 도구의 장시간 미응답·환경 장애 재시도와 Case 전체 예산은 구분하여 구현·검증한다.

기본은 기준·필요 검증 충족 후 자동 완료이며 controlled는 최종 사람 확인을 요구한다. 예외 종료는 미충족·미검증 모두 대상으로 사람의 범위·영향 확인이 필요하며 원래 결과를 보존한다. 첫 버전의 지식 관리·선택적 추출·자동 제공은 별도 계약을 따른다. QG-08을 모든 후보의 추가 AI 실행·사람 승인·Case 완료 조건으로 만들지 않는다. 첫 버전은 한 사용자용이며 다중 계정·역할 UI를 이 계약에서 추가하지 않는다.

Python/FastAPI·SQLite, React/TypeScript 웹 UI와 일반 사용자 프로세스 배포를 채택한다. 초기 수용시험의 10개 프로젝트·3개 Runner·동시 AI 2개, 같은 Runner 쓰기 기본 1개는 시험 규모이며 등록 상한·실측 보장이 아니다. 필요한 원문이 있는 Runner에서 검토한다는 배정 경계는 동시성 설정과 무관하게 유지한다.

입력·출력 형식과 세부 필드 이름은 구현 계약이며 실제 CLI 구조화 출력·누락 탐지·원문 조회·중계 기록을 시험해야 한다. 원문이 서버 오류 로그·캐시·백업에 남지 않는지와 Windows CLI의 도구 경계·권한 제어 한계를 검증한다. LAN 또는 사용자 VPN의 원격 열람에는 소유자 인증·암호화 연결을 적용하고 인터넷 직접 공개는 첫 버전 범위에 넣지 않는다. 훅·권한 설정의 실제 강제 범위, 백업 복원과 원문 참조 대조, 이견 처리와 누락 탐지 정확도는 구현 후 검증 대상으로 남는다.
