# 서버·개인 PC 간 데이터 경계 기준안 v0.8 2차 반영

갱신일: 2026-09-23. D-01~90과 [대화 중심 UI 설계](ui-conversation-design.md)의 관련 2차 확정분·개발 인계를 반영한다. 개발 기준은 DEVELOPMENT.md의 P4-03 완료·schema v15·Profile v2이며 아래 새 대화·요청 제어와 보관 검증을 완료했다는 뜻은 아니다. 첫 버전은 Windows 네이티브·한 사용자용이며 서버 제어 + 개인 PC Runner에서 서버는 상태·요약·참조를 보관하고 코드·diff·상세 로그·상세 의도/설계/계획/대화·지식 원문은 PC에 유지한다. 접수 전 브라우저 초안과 접수된 원문 정본을 구분하며 GitHub 자동 요약 게시는 요약과 참조에 한정한다. 다중 Repository, 사전 허용 범위의 외부 반영, 지식 자동 제공도 이 보관 경계를 바꾸지 않는다.

## 1. 시스템 원본과 저장 위치의 구분

‘시스템이 원본을 관리’한다는 뜻을 ‘서버가 모든 파일을 보관’한다는 뜻으로 사용하지 않는다. 제어 서버의 상태 DB와 등록된 Runner의 산출물 저장소를 하나의 논리적 시스템으로 관리한다. GitHub는 읽기용 누적 기록이며 원본이나 승인 기록을 대신하지 않는다.

| 위치 | 보관할 내용 제안 |
|---|---|
| 제어 서버 | 논의 준비/업무 단계와 요청 처리·작업·실행 상태, 목표와 진행 요약, 질문·결정의 요약, 동의·권한·설정·예산 사용 기록, 기준·산출물·증거·지식의 ID와 버전·해시·소유 Runner 및 허용된 적용 메타데이터, 검증 판정 요약, 게시 상태와 GitHub ID |
| 개인 PC | Repository별 코드·worktree·diff·상세 로그·실험 원시 결과, 상세 검토 입력과 산출물 원본, 대화 본문과 본문 검색 인덱스, 상세 지식·발췌·적용 내용, CLI 세션 자료, 전송 전 실행 이벤트와 산출물 목록 |
| 브라우저 | 화면에 요청한 상세 내용의 일시적 표현과 접수 전 사용자 초안. 현재 브라우저에 대화별 텍스트·붙인 자료 참조를 자동 저장하여 대화 전환·새로고침·종료 후 복구한다. 기기 간 동기화는 하지 않으며 구체 저장 기술은 후속 구현 대상이다. |

상태 필드에 코드나 전체 로그를 넣어 저장 제한을 우회하지 않는다. 예를 들어 발견 요약에는 기준 ID와 ‘권한 검사 누락’을 기록하고 실제 코드 발췌는 PC의 증거를 참조한다. 요약에도 비밀정보가 포함될 수 있으므로 허용 필드와 내용 검사가 필요하다.

**D-51에 따라 의도·설계·계획·원문 요청과 피드백의 상세 본문도 PC에 보관한다.** 서버에는 판단·진행에 필요한 구조화 상태, 요약과 원본 참조를 남긴다. 의미를 축약한 요약을 사용자가 동의한 원문으로 대체하지 않는다. PC 단절 중에는 이미 불러온 내용은 유지하지만 추가 원문 조회·새 검토 결정 전송에 연결이 필요함을 표시한다. 이 경계는 서버 제어부와 개인 PC를 분리한 구성에 적용한다. 로컬 완결형·서버 완결형은 제어부와 Runner가 같은 호스트에 놓일 수 있으며 Runner 역할이 원문을 보관한다.

산출물 참조는 PC의 임의 절대 경로 대신 `artifact_id, revision, content_hash, owner_runner_id, availability`로 관리한다. 실제 경로는 Runner가 허용 저장소 안에서 해석한다. 저장 완료 응답은 원본이 필요한 기록의 경우 PC 영속 저장과 서버 참조 등록이 확인된 시점에 제공한다. 둘 중 하나만 성공하면 동기화 대기로 남기고 요청 ID로 재조정한다.

대표 대화 하나는 준비 단계부터 같은 Case에 연결한다. 논의를 업무화해도 상세 대화·결정·제약·누적 사용량의 원본과 참조를 유지한다. 보관은 목록 정리이며 원문 삭제나 완료 판정이 아니다. 보낸 메시지의 수정은 원문을 보존한 연결된 새 정정 요청이고 기존 실행·외부 반영 기록을 지우지 않는다. 정확한 요청·메시지·보관 상태의 필드와 기존 데이터 이행은 추가 설계 대상이다.

브라우저 초안은 접수 전 사용자 자료이며 접수된 대화·문서 원문 정본의 보관 위치를 바꾸지 않는다. 전송 실패·접수 확인 전에는 유지하고 PC 원문 저장과 서버 참조 등록이 모두 확인된 전송분만 삭제한다. 늦은 접수 확인이 그 뒤 편집한 초안을 지우지 않아야 한다. 브라우저 저장을 시스템 접수로 표시하거나 서버 원문 대기열·기기 간 동기화의 허용으로 확대하지 않는다. 복원·재연결 후에도 사용자가 직접 전송한다.

프로젝트 파일·코드·산출물 참조는 자료 ID·고정 버전/해시·위치를 전송한 의견과 연결한다. 상세 인용·피드백 본문은 PC 원문이며 서버의 참조 메타데이터에 본문을 넣지 않는다. 첫 버전은 텍스트와 프로젝트 자료 참조이고 이미지 첨부·분석은 후속 검토다. 대화 검색은 현재 프로젝트의 보관 대화까지 제목·요약·본문·결정 사항을 대상으로 한다. 본문과 본문 검색 인덱스는 PC에 두며 서버에 복제하지 않는다. PC 미연결 시 서버의 제목·요약·결정 요약만 검색하고 빠진 범위를 표시한다. 일치 부분과 해당 메시지·결정으로의 이동은 원문 가용성을 따르며 검색용 발췌를 서버 영구 저장의 예외로 삼지 않는다. 범용 코드·파일 검색은 범위가 아니다.

Project는 여러 Repository를 등록하고 Case는 필요한 일부를 선택한다. 한 Case의 대상 Repository는 같은 Runner에서 다루며 원본·증거·경로에는 Repository 식별자를 연결한다. 같은 Runner 조건이 과거 Case의 지식 원문까지 그 Runner에 존재한다는 보장은 아니다. 원본 소유와 가용성은 계속 개별 참조로 확인한다. 여러 Project에 걸친 Case는 첫 버전 범위가 아니다.

## 2. AI 실행과 컨텍스트 구성

이 배포 모드에서 상세 자료가 필요한 의도 작성·설계·구현·AI 품질 검토는 해당 PC Runner의 CLI에서 수행하는 안을 제안한다. 서버는 목표·기준 참조·정책·작업 범위를 전달하고, PC의 컨텍스트 구성기가 원문과 현재 코드·증거를 조회한다. 서버 요약만 보고 상세 품질 검토를 완료했다고 처리하지 않는다.

독립 의미 검토가 필요할 때 별도 검토 세션도 같은 PC의 원본을 읽는다. 입력 목록·기준·Repository별 산출물과 지식 버전을 고정하고 상세 결과는 PC에, 판정과 발견 요약·증거 참조는 서버에 기록한다. 읽기 권한 검토자가 추가 실행을 요구하면 기존 권한·선행 조건·설정된 예산을 확인하여 검증 작업을 배정한다. 별도 세션 사용이 로컬 자료를 다른 서버 Runner로 복제할 권한은 아니다. 프로젝트 기본 CLI·모델과 Case/역할별 설정을 적용하며 장애를 이유로 다른 CLI로 조용히 바꾸지 않는다.

사용자에게 하나의 대화와 진행 요약을 보여주더라도 내부 작성·검토 세션, 실제 입력·판정·산출물 증거의 출처를 합치지 않는다. 자동 정리한 업무 결정과 프로젝트 공통 규칙은 원래 발언·권위·적용 범위·버전·대체 관계를 참조한다. AI 제안이나 요약 생성이 새 규칙·권한을 만들지 않는다.

미완료인 같은 문제의 목적 확장·유형 변경은 같은 Case에 남고 기준·의도 항목의 `relates_to` 연결·과거 판단·실행·소비·repair를 보존한다. 새 목적의 의무·권한·확인 조건을 적용하며 목적 추가로 Profile 쓰기 제한을 우회하지 않는다. 종료 후 단순 설명은 기존 결과 원문을 근거로 같은 대화에서 답하고 종료 기록을 바꾸지 않는다. 이후 설명 실행의 사용량은 같은 Case 예산에 누적하되 종료 당시 사용과 분리한다. 추가 실험·수정은 새 Case다.

시간 한도 선택 시 기본은 논의·업무·검토·재시도·종료 후 설명의 실제 AI 실행시간 합계다. 사람 대기는 제외하고 병렬 실행은 각 시간을 합산한다. 경과시간은 별도 옵션이며 기존 Case의 한도 의미는 소급 변경하지 않는다. 전체 예산 무제한 기본은 유지하고 실행시간 관측·예약·정산과 종료 이후 설명의 귀속을 원본 참조·상태 기록으로 대조한다.

[프로젝트 지식](project-knowledge.md)의 자동 제공도 PC의 컨텍스트 구성기에서 원본·핵심 내용·적용 조건을 확인하여 수행한다. 필수 내용은 실제 컨텍스트에 포함하고 서버의 축약 요약이나 원문 링크만으로 제공 완료를 기록하지 않는다. 상세 지식을 Registry·검색 자료라는 이름으로 서버에 영구 복제하지 않는다. 원본 조회 불가·적용 미확정·충돌이 필수 판단을 막으면 해당 의존 작업만 보류하며, 참고 자료의 가용성 부족은 한계로 표시한다. 지식 원문을 다른 Runner에서 자동 복제하는 권한을 추가하지 않는다.

**2026-09-24 결정(P4-06b, 미구현):** 사용자 결정으로 **지식(적용 내용·조건·예외와 자동 등록 규칙의 권위 사용자 메시지 한 건)은 이 경계의 예외**이며 서버 DB 에 저장한다(D-67 보충). 다른 PC·소유 PC 미연결에서도 주입하기 위해서다. 해시 대조·영수증은 유지하고 서버 로그에는 본문을 남기지 않는다. 대화·의도·설계·계획·코드·로그의 경계는 바뀌지 않는다.

여기서 ‘PC에서 CLI 실행’은 ‘외부 AI 공급자에게 자료가 전송되지 않음’을 뜻하지 않는다. D-52에 따라 첫 버전은 기존 CLI의 사용자 전송 설정을 그대로 따른다. 별도 프로젝트별 전송 정책은 이후 확장이다. CLI 로그인 파일은 업무 기록과 함께 서버로 복사하지 않는다.

## 3. 웹 상세 열람과 임시 중계

제안 흐름은 `브라우저의 원본 ID 요청 → 서버의 접근·대상 확인 → 연결된 Runner의 고정 버전 읽기 → 브라우저 표시`다. 원본 조회는 읽기 전용이며 임의 파일 다운로드 API로 만들지 않는다. 화면에는 소유 PC·원본 버전·연결 상태를 표시하고 다른 버전이 도착하면 현재 검토 대상을 바꿔치기하지 않는다. D-44에 따라 신뢰하는 개인 프로젝트와 CLI 권한·시스템 승인을 전제로 하며 이 구조가 운영체제 전체 격리를 보장한다고 설명하지 않는다.

상단 결과물 목록에는 실제 산출물을 나열하고 일반 진입은 최신 버전, 과거 링크는 해당 버전을 연다. 읽던 버전은 새 버전이 생겨도 유지하며 새 버전·변경 보기를 안내한 뒤 사용자 선택으로 전환한다. 과거 버전의 참조 피드백은 그대로 고정하고 최신이 아님을 표시한다. 앱 안은 읽기·참조 피드백이며 편집은 외부 도구에서 한다. 작업 PC 이름·경로 복사를 제공하되 열기는 해당 작업 PC에서 지원·검증된 수단에 한정한다. 외부 편집은 현재 파일과 고정 참조를 대조·보존하고 기존 검토 원본을 덮어쓰지 않는다. 전체 편집기는 후속 검토다.

서버를 경유한다면 **임시 메모리 중계와 영구 보관을 분리**한다. 중계 데이터가 DB·파일·작업 큐·요청 본문 로그·오류 보고·프록시 캐시에 저장되지 않도록 구현·검증한다. 응답 캐시와 서비스워커 저장도 제한하는 안을 제안한다. 메모리 중계는 서버 프로세스가 내용을 취급한다는 뜻이므로 ‘서버로 전송되지 않음’이라고 설명하면 안 된다. 운영체제의 메모리 덤프 등까지 포함한 보장 범위도 별도로 확인해야 한다.

임시 중계는 D-51에서 허용한 방식이다. 서버가 원문을 잠시 취급하지만 영구 보관하지 않는다는 구분을 유지하고, LAN 또는 사용자가 마련한 VPN에서 인증·암호화된 연결로 구현한다.

PC가 오프라인이면 서버에 남은 상태·요약·판정 이력과 마지막 확인 시각을 표시하고 이미 불러온 상세 내용·초안을 유지한다. 초안 편집은 허용하되 일반 메시지와 질문/확인 카드 전송을 비활성화한다. 추가 원문 열람에는 `PC 연결 필요`, 실제 실행이 불명확하면 `실행 상태 확인 필요`를 표시하고 원문 부재를 빈 문서·삭제로 가장하지 않는다. 초안의 브라우저 자동 저장은 시스템 접수·원문 저장 완료가 아니며 서버 영구 원문 대기열로 우회하지 않는다. 브라우저 종료 후 초안 복구는 지원하고 기술은 후속 구현에서 정한다. 재연결 뒤 원본·실행·정책 상태를 확인하고 사용자가 직접 전송하며 초안을 자동 발송하지 않는다.

온라인에서도 일반 전송은 현재 요청 처리 중 잠그고 초안만 편집한다. 범위가 정해진 카드 답변과 중단 요청은 별도 경로로 받는다. 서버에는 요청 처리 상태·대상 참조·중복 방지 메타데이터를 두되 이를 상세 원문 보관의 예외로 사용하지 않는다. 긴 CLI 실행 중에도 Runner가 원문 저장·상태/제어 수신을 처리해야 한다. 요청 종료·실제 중단 확인과 원문 저장 완료는 서로 다른 사실이다.

controlled의 최초·최종 확인, material delta의 결정, 선택된 설계 검토·외부 반영 확인처럼 정확한 원본과 현재 버전이 필요한 행위는 확인 가능한 자료가 없으면 대기한다. 자동 진행도 원문이 필요한 판정을 서버 요약으로 대체하지 않는다. 과거 유효한 동의·검증을 PC 오프라인이라는 이유만으로 삭제하거나 실패로 바꾸지 않는다. 중지 요청이나 설정 변경은 서버에 기록하되 ‘요청 저장’과 ‘Runner 반영’을 구별한다. 실행 재개·외부 반영 전에 재연결된 원본·정책 버전을 재검사한다.

## 4. GitHub 기록과 외부 반영

논의 준비 단계에는 이슈를 만들지 않는다. 명확한 업무로 전환하면 Project의 업무 이슈 위치에 Case당 새 이슈 하나를 유효한 게시 허용 안에서 만들고 관련 선행 논의의 결정을 요약한다. 대상 Repository가 여러 개여도 이슈를 각각 만들지 않는다. 대표 Repository가 코드와 업무 이슈를 겸할 수 있고 별도 위치도 선택할 수 있다. 기록 위치 지정만으로 코드 대상·권한을 추가하지 않으며 Git 원격·인증 자동 감지는 게시 허용이 아니다.

상단 시스템 표시, 이전 글을 수정하지 않는 append 방식, 업무 시작의 게시 허용, 게시 장애와 내부 진행 분리는 유지한다. 게시 본문은 의도·설계·계획·검증 요약과 참조이며 코드·diff·상세 로그는 제외한다. 게시 상태·식별자·해시는 서버가 관리한다. 상세 참조는 시스템의 접근 확인을 거치는 조회 주소로 표현하고, PC가 오프라인이면 상세를 제공할 수 없음을 표시한다. 업무 이슈 위치는 요약 위치일 뿐 지식의 원본·권위·적용 범위를 바꾸지 않는다.

PC에서 요약을 생성·검사하고 게시 정책을 충족하는 고정 payload만 서버 outbox에 보존하는 안을 제안한다. 제목·요약 필드 안의 코드나 로그 발췌도 제외한다. 서버는 이 고정 요약을 PC 오프라인 중에도 재전송할 수 있지만, 새 상세 산출물에 대한 요약은 원문 연결 없이 재생성하지 않는다. 응답 유실 후 GitHub 결과를 조회해 중복 생성을 막는다. 게시 본문이 원문 전체를 대체하거나 PC 원본의 백업이 된다고 간주하지 않는다.

서버 저장 제한은 GitHub 공개 허용과 같은 정책이 아니다. 반대로 GitHub 게시 허용이 서버에 상세 자료를 보관하는 권한도 아니다. push·PR 생성/수정은 유효한 사전 허용의 Repository·대상 ref/PR·행동·범위와 실제 준비한 변경을 대조하여 그 안에서 실행한다. 허용이 없거나 범위를 벗어나면 준비된 대상·내용으로 확인받는다. 요약 게시 허용은 코드 공개 권한이 아니며 controlled의 최종 결과 확인은 코드 push·PR 반영보다 앞선다. 코드 push는 코드가 있는 Runner에서 수행하고 각 Repository의 외부 결과·부분 실패를 별도로 기록한다. 여러 원격 반영을 하나의 원자적 동작이라고 가정하지 않는다.

## 5. 백업·복구·Runner 변경

서버 백업은 업무 상태·승인·참조를, PC 백업은 원본 산출물·지식·Repository별 미커밋 변경·증거를 복구한다. 한쪽 백업만으로 전체 업무를 복원했다고 표시하지 않는다. Git 원격 저장소도 미푸시 코드·실험 로그·의도·지식 원문 전체의 백업은 아니다. 두 저장소의 ID·버전·해시 목록을 대조하고 원본 누락과 오래된 복원 상태를 드러낸다. D-57에 따라 각 호스트의 지정 경로에 하루 한 번 자동 백업하고 최근 7개를 보관한다. 원본·승인·증거는 자동 삭제하지 않으며 PC 원문을 서버로 복제하지 않는다.

PC 원본이 소실되면 서버의 과거 승인·판정 이력은 보존하되 근거 조회 불가를 표시한다. 서버 요약이나 GitHub 사본을 조용히 원본으로 승격하지 않는다. 복구한 파일은 버전·해시를 확인하고, 증거를 복구하지 못하면 필요한 재검증과 사람 판단으로 연결한다.

다른 Runner로 이동할 때 서버가 새 PC에 전체 자료를 재구성해 줄 수 있다고 가정하지 않는다. 원본·Git 상태·산출물·실행 기록을 기존 PC 또는 백업에서 가져오고 식별자·해시·환경을 확인한 뒤 소유 참조를 바꾸는 관리 절차를 제안한다. 자격증명은 자동 이관하지 않는다. 기존 실행의 종료·안전 정지 여부가 불명확하면 새 PC에 동일 쓰기를 배정하지 않는다. D-40에 따라 단절 시 현재 도구 호출 종료의 안전 경계에서 멈추고, 재연결 후 실제 실행 상태를 대조한다.

중단과 재시도는 같은 Case의 유효한 원본·결과·사용량·수정 이력을 보존한다. 중단 요청 뒤 후속 실행을 막고 실제 정지를 확인하며, 정지 불명 상태에서는 동일 쓰기를 재개하지 않는다. 완료한 부분·남은 변경·미검증을 요약하고 원래 파일 상태와 대조한 후 이어간다. 로컬 구현·검증 완료는 전용 작업공간의 결과이며 원래 폴더·기본 브랜치·GitHub에 반영한 상태와 구분한다. 미커밋 변경 포함과 후속 업무의 이전 결과 기준은 원래 폴더를 보존하고 정확한 스냅샷 참조로 연결한다.

수용 검증은 PC 연결/오프라인의 상세 조회·전송 차이, 초안의 브라우저 복구·접수 전 보존·늦은 확인 대조·자동 전송 금지, 요청 잠금·카드 범위·중단 확인, 서버 로그·캐시에 상세 원문·지식·본문 인덱스가 남지 않는지, 서버 또는 PC 한쪽만 복원한 경우, 여러 Repository의 원본·증거 연결, 과거 지식 소유 Runner의 조회 불가, 원본 손실, Runner 이관과 늦은 결과 수신, GitHub 응답 유실과 부분 반영을 포함한다. 준비 단계 업무화·보관, 활성 유형 변경의 연결 보존, 종료 후 설명의 누적 구분, 부분 검색 표시, 정정 요청·읽는 버전·외부 편집 대조도 확인한다. 제품 정책 D-01~90과 상세 구현 제안을 구분하며 실제 보관·복구 검증은 구현 후 수행한다.

개발 인계는 D-90의 대화·요청 기반 → P4-04와 연결한 문맥·복구·제어 → 새 UI 기본 흐름 → 나머지 P4·추가 UI → P5/P6를 따른다. 원문 보관·초안·검색·요청 제어의 접점은 각 하위 작업 plan에서 구체화하며 P4-04 하나에 UI 전체를 포함하지 않는다. P4-03 완료 상태와 과거 증거는 유지한다.
