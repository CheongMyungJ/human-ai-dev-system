# 품질 게이트 실행·복구 기준안 v0.7

작성일: 2026-09-21. D-01~67의 관련 결정을 반영한 실행·재검증·설정 변경·완료 계약이다. 구현·CLI 호환성·성능 실증 결과는 아니다. 공통 위임·예산·권한 계약은 [자동진행·위임·예산 정책](autonomy-budget-policy.md), 종료 계약은 [완료 정책](completion-lifecycle.md)을 따른다.

요청 정합성은 항상 확인하되 명확한 저위험 요청에는 가벼운 확인을 사용한다. 위험·불확실성·정책에 따라 독립 AI 의미 검토가 선택되면 작성과 별도 세션에서 수행하며 같은 CLI·모델도 가능하다. 모든 게이트에 별도 AI 실행을 강제하지 않는다. 최초 품질 실패 뒤 repair는 기본 최대 2회이고 전체 예산과 별도로 관리한다. 전체 예산은 기본 무제한이며 설정 한도는 실행 전 예약과 실제 사용량·제어 능력으로 강제한다. 게이트 설정 변경은 현재 검증 1회 종료 뒤 적용하고 현재 결과는 시작 당시 입력·정책에 연결해 보존한다.

## 1. 검토자와 진행 판정의 역할

작성 실행은 산출물과 변경 설명을 만든다. 선택된 독립 검토 실행은 고정된 대상·기준을 읽고 발견·근거·판정 제안을 반환한다. 제어 서비스는 필요한 검사 완료, 기준의 필수성, 현재 입력과의 일치, 위임·정책·권한·예산을 검사해 후속 실행 가능 여부를 결정한다. 사람에게 버전 유효성·증거 연결·결과 후보 구성을 대신 판단하게 하지 않는다.

| 주체 | 하는 일 | 직접 바꾸지 않는 것 |
|---|---|---|
| 작성 실행 | 허용 범위의 문서·코드 작성, 발견 사항에 대한 수정 | 사용자 동의·검토 기준·게이트 설정 |
| 별도 AI 검토 세션 | 요청 원문·유효 의도·기준·산출물·증거를 대조해 발견 사항 반환 | 대상 코드·산출물, 사람 승인, 스케줄러의 실행 허용 상태 |
| 규칙·검증 실행기 | 필드·참조·버전 검사, 허용된 테스트·빌드·측정, 원시 결과 보존 | 검증이 확인하지 않은 동작에 대한 성공 주장 |
| 게이트 평가 서비스 | 필수 검사 결과와 발견 사항을 정책에 따라 집계 | 새로운 제품 요구를 임의로 필수 조건에 추가 |
| 스케줄러·정책 서비스 | 최신 유효 결과·필요한 사람 확인·실행 권한·예산 예약을 검사하고 배정 | 과거 결과를 새 입력에 무조건 승계하거나 AI 추천만으로 필수 기준 완화 |

별도 세션에는 작성자가 낸 ‘완료’ 주장만 주지 않는다. 원래 요청, 사용자 피드백, 확정 결정, 기준, 대상 산출물, 실제 증거를 포함한다. 작성 이유가 검토에 필요하면 그 출처를 구분해 제공한다. 같은 모델을 쓴 별도 세션은 오류가 서로 독립이라는 보장이 없으므로 실제 테스트와 근거 대조를 계속 사용한다.

독립 AI 검토에는 읽기·검토 역할을 배정하고 CLI 권한·시스템 위임 검사로 실행 범위를 통제한다. 첫 버전은 신뢰하는 개인 프로젝트의 Windows native 실행이며 OS 완전 격리를 보장하지 않는다. 검토자가 코드를 수정할 필요가 있으면 별도 작성 작업에 배정하고 필요한 독립 재검토는 별도 세션에서 수행한다. 가벼운 작성자 점검을 독립 검토 완료로 표시하지 않는다.

현재 실행 위임은 최초 요청·후속 명시 결정·정책으로 판단한다. 명확한 요청에 동일 내용의 초안 승인을 다시 요구하지 않는다. 미위임 제품 결정이나 중요한 변경은 해당 의존 작업 전에 확인하고 독립적인 조사·검증은 계속할 수 있다. 분석·연구에는 기존 권한·예산 안의 복구 가능한 로컬 재현·임시 계측·테스트를 포함한다. 실험을 명시하지 않았다는 이유만으로 묻지 않으며 제품 수정·외부 영향·권한 확대로 목적과 범위를 넓히지 않는다.

프로젝트 기본 CLI·모델을 두고 업무·역할별 변경을 지원한다. 장애 시 사전 허용되지 않은 다른 CLI로 변경하기 전에는 사람에게 확인하며 새 세션이나 도구 변경으로 누적 수정 횟수·기준을 초기화하지 않는다. 첫 버전은 한 사용자용이지만 실행·변경·결정의 주체를 기록한다.

## 2. 입력 스냅샷과 기록 모델

현재 문서나 현재 브랜치라는 이름만으로 검토 대상을 정하지 않는다. 실행마다 실제 평가한 버전을 고정하고, 결과를 채택할 때도 그 대상이 여전히 유효한지 확인한다.

| 기록 | 최소 정보와 목적 |
|---|---|
| `GateSubject` | Project·Case·Task, 게이트 유형, 채택 경계. ‘이 구현 묶음을 통합에 사용해도 되는가’처럼 논리적 검토 대상을 식별 |
| `GateInputSnapshot` | 원문 요청·위임·기준·Profile 및 산출물 버전, 참여 저장소별 커밋·미커밋 변경, 환경·도구·검사 범위·지식/증거 참조·정책 버전 |
| `GateRun` | 대상 스냅샷, 실행 주체·검토 세션, 시작·종료·중단, 실행 상태, 원시 결과, 이전 실행·수정 주기 연결 |
| `GateEvaluation` | 사용한 검사 결과, 기준별 판정, 차단·권고 발견, 실행 결과의 유효성, 재검토 사유. 판단을 내린 정책 버전 |
| `GateFinding` | 안정적인 발견 ID, 관련 기준·대상·근거·영향, 최초 발견, 변경 이력, 해결·재발·기각 근거 |
| `RemediationCycle` | 같은 채택 경계에서 해결 중인 실패 묶음, 누적 수정·재검증 차수, 허용 한도, 실행 비용·시간, 사람에게 요청한 이유 |
| `GatePolicyChange` | 변경 주체·관찰·권고·실제 적용 출처, Case/Task, 이전·새 값, 요청 시각·정책 버전·효력 시각, 진행 실행 처리 결과 |
| `CompletionCandidate / FinalAcceptance` | 요청한 완료 지점·결과·기준·저장소별 코드·게시 대상·예정 외부 검증, 실제 사람 확인 또는 자동 정책의 적용 기록 |
| `ExceptionDecision / ClosureRecord` | 미충족·미검증 범위·영향·주체·원 판정, 완료/예외 종료/취소 구분. 추가 실행 권한과 분리 |
| `BudgetLedger / Reservation` | Case별 지표·역할·Run 소비와 진행 중 예약, 측정 범위·exact/estimated/unavailable, 한도·능력·정산·실제 중단 상태 |

완전한 파일 사본을 매번 대화에 넣을 필요는 없다. 내용이 고정된 참조와 필요한 부분을 전달하되, 누락된 중요 근거는 조회할 수 있게 한다. 외부 서비스 상태나 통제되지 않은 환경처럼 고정할 수 없는 입력은 관찰 시점과 한계를 기록한다. 단순 해시 일치가 관찰하지 않은 환경까지 같음을 보장하지 않는다.

서버+개인 PC Runner 구성에서 코드·diff·상세 로그와 상세 의도·설계·계획·대화·검토 원문은 PC에 보관한다. 서버는 상태·요약·원문 ID/버전/해시를 보존하며 원문을 영구 저장하지 않고 일시중계한다. 상세 `GateFinding`, 검사 로그·입력 스냅샷 내용과 체크포인트 원문도 이 경계를 따른다. 원문이 필요한 작성·분석·검토는 해당 Runner에서 수행하고 PC가 연결돼야 상세 열람·검토한다. 서버의 참조와 PC 원문을 함께 논리적 시스템 기록으로 관리하며 PC 단절을 원문 없음이나 검토 통과로 바꾸지 않는다.

외부 AI 전송은 기존 CLI의 사용자 설정을 따른다. Runner에 남는 로그인 자격증명을 서버 업무 기록에 복사하지 않고, PC에서 CLI를 실행한다는 이유로 오프라인 모델 처리라고 설명하지 않는다.

상태는 다음 네 축으로 나눈다.

- 적용 설정: 필수 확인 / 명시 ON / 명시 OFF / 상위 정책 따름·자동 조건부 적용. 자동 생략과 업무상 미적용도 근거를 남김.
- 실행 상태: 대기 / 실행 중 / 취소 요청됨 / 종료 / 실행 실패 / 실행 여부 확인 필요.
- 품질 판정: 통과 / 실패 / 사람 판단 필요 / 실행 불가 / 미검증 / 업무상 미적용.
- 현재 유효성: 현재 입력에 유효 / 변경으로 재검토 필요 / 과거 입력의 기록.

예를 들어 ‘OFF, 마지막 검사는 실패, 현재 입력에는 재검토 필요’가 동시에 가능하다. 화면과 API는 이를 하나의 녹색 성공 표시로 압축하지 않는다. 게이트가 꺼져 있어도 해당 성공 기준이 미충족이라는 사실은 별도로 남는다.

## 3. 검사부터 채택까지

1. Profile·대상·현재 위임·정책을 계산한다. 요청 정합성 확인을 유지하며 독립 AI 검토 선택 이유를 기록한다. 기능 개발용 문서·질문지를 다른 업무에 일괄 강제하지 않는다. 지식 채택은 필요 후보에 적용하고 매 Case의 별도 AI 실행으로 만들지 않는다.
2. 입력 스냅샷과 필요한 검사 목록을 고정한다. 필수 기준과 검사 연결, 기존 지식의 적용 범위를 확인하고 관련 없는 형식 검사를 추가하지 않는다.
3. 유효한 기존 증거를 재사용하고 부족한 규칙·실행·선택된 AI 검토를 배정한다. 실행 전 전체 예산을 예약하고 선택된 독립 AI 검토는 작성과 별도 세션에서 수행한다.
4. 개별 결과가 빠지거나 검토 출력이 계약을 만족하지 못하면 그 검사는 미검증 또는 실행 불가로 남긴다. 비어 있는 발견 목록을 자동 통과로 해석하지 않는다.
5. 평가 서비스가 필수 검사, 발견 사항, 사람 판단 항목을 집계한다. 검토자의 심각도 표현만으로 필수 성공 기준을 완화하지 않는다.
6. 결과 저장 시 입력과 실제 적용 정책의 버전을 대조한다. 입력·유효 조건이 바뀌면 옛 결과를 보존하고 영향받는 조건을 재평가한다. 적용 전의 예약 설정만 있다는 이유로 현재 검증을 무효화하지 않는다.
7. 후속 배정 시 현재 위임·권한·의존 조건·필요한 사람 확인·예산을 다시 검사한다. 저장과 배정 사이의 변경을 반영하고 늦은 결과로 옛 권한의 실행을 시작하지 않는다.

제어 서비스의 결과 채택·배정은 기대 상태 버전을 명시한다. 실행 요청은 입력·정책 버전·배정 토큰·예산 예약을 연결하고 Runner가 시작 직전 검사한다. 외부 행동은 호출 직전 권한도 재확인한다. 연결 단절 시 현재 도구 호출 종료의 안전 경계에서 멈추고 재연결 뒤 상태·입력·위임·예약·실제 사용량을 대조한다. 단절이나 lease 만료만으로 같은 쓰기를 다른 Runner에 중복 배정하지 않는다. CLI 내부 경계·중단 능력은 버전·모드별 검증 대상으로 남기며 서버 상태 변경을 실제 프로세스 종료로 표시하지 않는다.

Project는 여러 Git 저장소를 포함하고 Case는 필요한 저장소를 선택한다. 선택 목록과 사전 허용 pool을 구분하며 허용 범위 안의 추가 선택은 자동 수행하고 범위 밖 추가는 확인한다. 한 Case의 참여 저장소는 같은 Runner에서 실행하고 저장소별 전용 브랜치·worktree를 연결한다. Runner 간 분산 Case는 이후 확장이다. 같은 Case와 Runner의 쓰기는 기본 직렬화하며 읽기 검토라도 빌드·테스트가 공유 자원을 바꾸면 경합 대상으로 취급한다. 초기 수용시험의 프로젝트 10개·Runner 3개·동시 AI 2개는 등록 상한이나 실측 성능 보장이 아니다.

변경 영향이 없는 검사를 모두 재실행할 필요는 없다. 예를 들어 UI 설명 수정이 데이터 변환 테스트의 입력·기준·환경을 바꾸지 않았다면 해당 증거를 재사용할 수 있다. 반면 승인 대상, 권한 요구, 필수 검사 계약이 바뀌면 관련 결과를 재검토한다. 재사용 판단에도 이유와 참조 버전을 남긴다.

## 4. 수정·재검증 누적 단위

최초 품질 실패 뒤 자동 수정·재검증은 **기본 최대 2회**로 하고 업무별로 조정한다. 최초 평가가 실패하면 자동 수정 1회차를 수행하고, 여전히 실패하면 2회차까지 수행한다. 이후 미해결이면 현재 근거와 필요한 결정을 사람에게 제시한다. 제품 판단·권한 변경이 필요하면 횟수를 채우기 전에도 질문한다. 새 세션이나 Task 재분할로 한도를 초기화하지 않도록 다음 누적 단위를 사용한다.

전체 예산은 기본 무제한이나 사용자가 설정한 한도는 별도로 강제한다. 작성·검토·이견 해소·환경 복구·재시도·지식 추출과 모든 참여 저장소의 실행을 Case에 누적한다. 여러 동시 실행은 `사용량 + 예약 + 새 예약 ≤ 한도`를 배정 전에 검사하고 완료 뒤 정산한다. repair 여유가 남아도 hard 한도에서는 새 소비 실행을 배정하지 않는다. 공급자 사용량의 실측·추정·미제공을 구분하고 미제공을 0으로 처리하지 않는다. 총시간 무제한은 개별 도구 미응답이나 환경 복구의 무한 반복을 허용하지 않는다.

한도 소진은 부분 결과·미검증·누적 소비를 보존한 일시 중지다. 필요한 정리는 예산 안에서 예약하며 소진 뒤 새 유료 AI 작업을 정리 명목으로 시작하지 않는다. 이미 진행 중인 결과의 기록·대조는 수행한다. 관측·중단 능력상 정확한 상한을 보장할 수 없는 실행에는 그 보장을 표시하지 않고 해당 설정의 배정을 보류한다. 정확 한도와 추정 기준 한도, 취소 요청과 실제 종료·초과 노출을 구분한다. 상세는 공통 예산 정책을 따른다.

**최초 실패를 만든 평가와 그 이후의 자동 수정·재검증 차수를 구분한다.** 하나의 차수는 실패에 대응하는 수정 작업을 시작하고, 수정 결과를 관련 검사로 다시 평가하는 묶음이다. 여러 발견을 한 수정으로 해결하면 한 차수로 기록한다. 실제 차수 사용·예약과 CLI 호출 수는 별도로 기록한다.

repair 누적 범위는 `같은 논리적 채택 경계의 RemediationCycle`이다. 의도·산출물 버전, CLI 세션, Profile·경로·Task ID만 바꿔 초기화하지 않는다. 사용자가 목표·범위를 실질적으로 바꿔 새 주기가 필요하면 이전 주기와 관계·사유·새 한도를 명시한다. 기존 실패와 전체 예산 소비는 남는다.

| 사건 | 수정·재검증 차수 처리 제안 |
|---|---|
| 최초 게이트 평가가 품질 실패 | 수정 주기를 개설. 최초 평가와 첫 번째 자동 수정 차수를 구분 |
| AI가 결함을 수정하고 관련 검사 수행 | 해당 차수에 작성·검토·도구 실행을 연결 |
| 같은 결함이 다시 발견됨 | 동일 발견 ID를 이어 쓰고 누적 차수 증가. 새 이름으로 초기화하지 않음 |
| 수정 후 다른 결함을 발견함 | 새 발견 ID를 추가하되 진행 중 주기의 누적 한도 유지 |
| 여러 발견을 함께 수정함 | 하나의 차수에 여러 발견을 연결. 발견 수만큼 차수를 중복 계산하지 않음 |
| 네트워크 응답만 유실됨 | 먼저 같은 실행의 완료 여부를 대조. 새 수정 차수로 바로 시작하지 않음 |
| 테스트 환경을 준비하지 못함 | 환경 복구로 분류. 제품 결함 수정 차수와 분리하되 시간·비용과 장애 재시도는 기록 |
| 코드 수정 후 재검증이 환경 장애로 실패함 | 수행한 수정 차수는 그대로 누적. 같은 코드의 검증만 다시 실행하는 환경 복구는 추가 수정 차수와 구분 |
| 작성 실행이 중간에 종료됨 | 같은 실행·차수의 복구 여부를 확인. 새 세션에서 이어도 같은 차수이며 이미 사용한 비용을 삭제하지 않음 |
| 사용자 답변이 있어야 수정 가능함 | 자동 반복하지 않고 질문 대기. 답변 전 추가 수정 차수를 만들지 않음 |
| 사용자가 추가 시도를 허용함 | 해당 한도를 늘린 새 정책 기록. 사용한 차수·전체 자원 소비를 0으로 되돌리지 않음 |
| 리뷰 이견을 증거로 재검토함 | 코드 수정이 없다면 새 repair 차수로 만들지 않되 실제 AI·도구 실행은 전체 예산에 누적. 수정하면 해당 주기에 연결 |
| Profile·Fast Lane 여부가 바뀜 | 기존 실패·repair 차수·사용량 유지. 기준 자체의 변경은 별도 material delta로 처리 |

CLI 호출이 실제 시작되었는지 불분명한 경우에는 ‘예약됨/시작 확인/결과 확인 필요’를 구분해 기록한다. 같은 실행 복구가 중복 차수로 계산되지 않게 실행 ID를 사용한다. 실행이 발생했을 가능성이 있으면 사용량을 확인하기 전 0으로 단정하지 않는다.

`GateFinding` 매칭은 기준 ID, 대상 위치·인터페이스, 재현 조건과 원인을 참고한다. 메시지 문구나 파일 행 번호만으로 동일성을 판단하지 않는다. AI는 기존 발견과 같은지 제안할 수 있으나 근거를 남긴다. 동일성 판단이 불확실해도 주기 전체 누적 한도는 유지하므로 발견 이름 바꾸기로 한도를 우회할 수 없다.

횟수가 남아 있어도 미위임 제품 정책·기준 변경·권한 확대·예외 수용이 필요하면 해당 작업 전에 사람에게 요청한다. 기술적 불확실성은 먼저 허용된 조사·증거 대조로 좁히고 위험 발견 자체를 자동 질문으로 바꾸지 않는다. 질문은 시점·의존 작업에 연결하며 단계 자동 진행이 사람 결정을 대신하지 않는다. 같은 실패를 반복해 유효한 다음 조치가 없거나 한도가 소진되면 근거·남은 선택을 제시한다.

## 5. 게이트 ON/OFF 변경과 실행 중 검사

설정은 원래 판정과 별도 기록으로 저장한다. 사용자 명시 OFF, 정책에 따른 자동 조건부 생략, 업무상 미적용을 구분한다. OFF는 해당 게이트의 검사·진행 차단을 사용하지 않는 것이며 관찰한 결함이 사라졌다는 뜻은 아니다. 현재 위임·선택한 사람 검토·권한·예산·성공 기준은 유지한다.

진행 중 설정 변경은 **예약하고 현재 검증 1회가 끝나면 반영**한다. 이후 자동 수정·재검증부터 새 설정을 사용한다. 현재 검증은 시작 당시 정책을 유지하며 예약 요청만으로 그 결과를 무효화하거나 검사를 중단하지 않는다. 다음 정보와 제어가 필요하다.

- 요청한 설정과 실제 적용된 설정, 두 시각을 구분한다.
- 실행 중인 검사·수정 차수에 대한 처리 결과를 기록한다.
- CLI·도구의 취소 지원 능력을 확인하고 `취소 요청`과 `실제 종료 확인`을 구분한다.
- 취소 요청 뒤 늦게 도착한 결과도 원래 실행에 저장한다. 그 결과만으로 다시 ON 처리하거나 후속 수정·배정을 시작하지 않는다.
- 중단된 실행이 만든 부분 변경과 로그를 보존한다. 게이트를 끈다는 이유로 코드 변경을 자동 롤백하지 않는다.
- 게이트 전용 검사와 별도 성공 기준의 검증 작업을 구분한다. 하나의 테스트가 두 목적을 가진다면 게이트 OFF만으로 그 검증 의무까지 취소하지 않는다.
- 다른 Task나 Case 전체 게이트에는 설정 변경을 전파하지 않는다. 명시적인 상위 설정 상속 관계는 표시한다.

검증 종료를 확인하면 `현재 결과 보존 → 예약 게이트 정책 적용 → 남은 기준·위임·권한·사람 검토·repair·예산 확인 → 다음 배정` 순서로 처리한다. 검증 한 번 종료가 Task·Case 전체 성공은 아니다. 결과가 실패여도 예약 설정을 반영할 수 있으므로 게이트 통과만 기다리는 순환은 만들지 않는다.

예약 OFF가 반영되면 그 게이트만을 위한 자동 수정·재검증은 새로 배정하지 않는다. 이미 발견된 결함이 별도의 합의된 성공 기준도 위반한다면 그 미충족과 해결 작업은 남는다. 같은 결함 수정 주기의 누적 한도도 OFF/ON 토글로 초기화하지 않는다. 종료 여부가 불명확하면 예약 상태를 유지하고 실제 실행 상태를 확인한다.

게이트 정책의 적용 경계는 **현재 검증 1회 종료**다. Runner 단절의 정지 경계인 **현재 도구 호출 종료**와 혼동하지 않는다. 검증 한 번이 여러 호출로 구성되면 단절 중간에 안전 정지할 수 있으며, 그 사실만으로 검증이 완료되거나 예약 게이트 정책이 적용됐다고 표시하지 않는다.

게이트 설정 예약은 취소 명령이 아니다. 별도 중지 요청·권한 철회·hard budget·실행 장애에는 해당 실행 제어와 실제 종료 확인을 적용하며 게이트 검증 종료까지 무조건 기다리지는 않는다. 이미 외부 요청이 전송됐다면 철회를 미반영으로 표시하지 않는다. 결과를 대조하고 중복 호출을 막은 뒤 허용 범위·예산 안에서 제한 재시도한다. 이미 반영한 결과를 자동 롤백하지 않는다.

ON으로 바꾸면 현재 입력에 맞는 기존 결과가 있는지 먼저 확인한다. 검사의 내용·범위가 그대로라면 ON/OFF 토글만으로 실제 테스트를 다시 실행할 이유는 없다. 기준이나 검토 계약이 달라졌으면 필요한 부분을 재실행한다. 이미 진행된 후속 작업은 자동으로 과거로 되돌리지 않고 영향받는 결과와 앞으로의 배정을 재평가한다.

검사가 OFF로 바뀌는 동시에 옛 통과 결과가 도착하더라도 결과는 옛 정책의 기록이다. 현재 정책과 필수 조건을 다시 평가해야 후속 배정을 할 수 있다. 반대로 ON 변경이 완료된 뒤 옛 OFF 요청을 담은 실행자가 다음 작업을 시작하지 않도록 배정 버전·유효성 확인을 사용한다.

## 6. 상태 전이표

아래 ‘후속 진행’은 해당 결과에 의존하는 작업을 뜻한다. 영향 없는 읽기·정리·다른 작업까지 업무 전체를 일괄 중지하지 않는다.

| 현재 상태·사건 | 다음 상태 또는 기록 | 후속 진행 |
|---|---|---|
| ON·미검증, 선행 입력 확보 | 입력 고정 → 실행 대기 → 실행 중 | 해당 결과 의존 작업 대기 |
| 현재 입력의 필수 검사 완료·충족 | 통과·현재 유효 | 다른 필수 조건까지 충족하면 배정 가능 |
| 수정 가능한 기준 위반 발견 | 실패 → 수정 주기 → 재검증 | 해당 결과 의존 작업 보류 |
| 자동 한도 소진 또는 반복으로 진전 없음 | 실패 이력 유지·사람 판단 대기 | 추가 시도·범위 조정 등 결정까지 보류 |
| 필요한 제품 선택이 미정 | 사람 판단 필요·질문 생성 | 결정 의존 작업 보류, 사람 열람·응답 허용 |
| 인프라 장애·검토 출력 파손 | 실행 불가 또는 미검증·복구 대기 | 통과로 처리하지 않음 |
| 실행 중 입력·기준 변경 | 원래 실행 보존·변경으로 재검토 필요 | 최신 입력에 유효한 평가 필요 |
| 통과 저장 후 배정 직전 정책 변경 | 배정 버전 불일치·현재 조건 재평가 | 옛 배정 요청만으로 시작하지 않음 |
| 실행 중 OFF 요청 수신 | 새 정책 요청 기록·현재 검증 1회 종료까지 적용 예약 | 현재 검증은 기존 정책 유지. 종료 후 다음 자동 수정·재검증 배정 전에 적용 |
| OFF 적용 완료 | 명시 OFF 또는 자동 생략의 적용 출처·이전 판정 보존 | 해당 게이트 차단만 제외, 독립 조건 재평가 |
| 취소 요청 뒤 검사 완료 이벤트 도착 | 원래 실행의 종료·결과 보존 | 현재 정책의 배정 조건을 다시 확인 |
| ON으로 변경 | 유효 결과 재사용 또는 검사 대기 | 필요한 조건 충족 전 의존 배정 대기 |
| 제어 서비스 재시작·Runner 재연결 | 실행 ID별 현 상태 대조, 불명확하면 확인 필요 | 같은 검사를 즉시 중복 배정하지 않음 |
| 검증 중 Runner 단절 | 현재 도구 호출 종료 후 안전 정지, 검증 진행 위치 보존 | 재연결·상태 대조 전 다음 도구 호출과 중복 배정 보류 |
| 유효한 종료 후보가 기준 충족 | 기본 ask-on-decision 자동 완료. controlled는 변경 결과 확인 뒤 허용 게시·필요 CI를 거쳐 완료 | 요청한 완료 지점·인수·외부 권한을 구분 |
| 사람이 남은 범위·영향을 보고 예외 수용 | 원래 실패·미검증 유지, 예외 수용 종료 기록 | 미충족을 통과로 바꾸거나 추가 실행 권한을 생성하지 않음 |
| 완료 후 수정 요청 | 기존 종료 보존, 연결된 새 Case 생성 | 새 요청의 위임·Profile·검토·검증·외부 반영 조건 적용 |
| 전체 hard budget 도달 | 소비·예약·부분 결과 보존, 예산으로 일시 중지 | 새 소비 실행 차단·진행 결과 대조. 완료·예외 수용으로 바꾸지 않음 |
| 권한 철회와 외부 실행 응답 유실 | 새 관련 배정 차단·반영 여부 확인 필요 | 이미 전송한 요청 대조 전 중복 쓰기 금지, 자동 롤백 없음 |
| controlled 게시 뒤 CI 수정으로 후보 변경 | 새 후보·증거 연결, 결과 재확인 대기 | 변경 내용 확인 후 허용 게시. 동일 내용 재전송에는 재확인 없음 |

## 7. 실제 사례로 보는 동작

### 사례 1. CSV 범위가 미정인 의도 초안

요청은 ‘관리 화면을 CSV로 내보내기’이고 AI가 현재 페이지만 내보낸다고 단정했다. 정합성 확인에서 불확실성을 식별하고 선택된 별도 검토 세션이 원문에 없는 결정을 발견한다. 작성 실행은 ‘현재 페이지 / 필터에 맞는 전체 결과’를 질문으로 바꾸고 출처를 정리한다. 질문을 드러낸 기록은 검토 가능하지만 그 결정에 의존하는 구현은 아직 허용되지 않는다. 답변 뒤 위임이 명확해지면 기본 모드에서 전체 초안을 다시 승인받지 않는다. 대기 중 같은 답을 추측하며 repair를 소모하지 않는다.

### 사례 2. 권한 검사의 같은 결함이 다시 발견됨

UI에서는 관리자만 버튼을 보지만 API는 일반 사용자에게도 파일을 반환한다. 발견 F-17을 만들고 권한 성공 기준과 연결한다. 첫 수정 후에도 직접 API 요청이 성공하면 F-17의 미해결로 기록한다. 다른 세션이 ‘서버 인가 누락’이라는 새 문구를 사용해도 실패와 누적 차수가 초기화되지 않는다. 수정으로 다른 역할의 접근이 깨지면 별도 발견 F-18을 추가하되 같은 수정 주기의 한도를 사용한다.

### 사례 3. 테스트를 시작하기 전에 의존 서비스가 내려감

통합 테스트 환경의 DB 연결이 안 되어 사용자 시나리오를 실행하지 못했다. 환경 장애로 기록하고 필요한 복구 작업을 만든다. 제품 버그를 가정해 코드를 수정하지 않는다. 자동 수정 횟수와 환경 재시도는 분리하지만 실제 시간·비용은 보존한다. 환경이 복구되어 수행한 테스트만 품질 증거로 채택한다.

### 사례 4. 구현 게이트 실패 후 사용자가 OFF

QG-04가 실패한 뒤 사용자가 해당 Task의 게이트를 끈다. 실행 중이면 현재 검증 1회 종료 후 적용하며 UI에는 ‘꺼짐 / 마지막 결과 실패’가 보인다. 결함과 성공 기준은 남는다. ‘관리자만 다운로드’가 필수 기준이었다면 OFF만으로 정상 완료하지 않는다. 사람이 남은 문제·미검증 범위·영향을 확인해 수용하면 예외 종료하며, 원래 기준 변경은 별도 의도·기준 변경 흐름으로 처리한다. 어느 쪽도 추가 실행 권한을 자동 생성하지 않는다.

### 사례 5. 검토 중 코드 변경과 늦은 통과

검토 세션은 코드 A를 보고 있는데 사람이 B로 수정한다. A의 통과 결과가 도착하면 A에 대한 기록으로 보존하고 B에 대한 현재 통과로 사용하지 않는다. 변경이 없는 검사 결과는 범위가 맞으면 재사용하고 B가 영향을 준 검사만 수행한다. 결과 저장 직후 OFF·ON 정책이 바뀌었을 때에도 후속 배정은 현재 정책을 다시 확인한다.

### 사례 6. 취소 요청과 Runner 재연결이 겹침

서버에서 OFF를 예약했지만 PC Runner가 끊겨 현재 검사 종료 여부를 알 수 없다. Runner는 현재 도구 호출 종료 후 안전 정지하고 서버는 ‘설정 예약됨, 실행 상태 확인 필요’를 표시한다. 같은 검사를 다른 Runner에 맡기지 않는다. 재연결 뒤 실행 ID로 종료·일시 중지·취소 상태를 대조한다. 검증 1회가 끝났다면 결과를 보존하고 예약 정책을 적용하며, 중간 정지라면 그 진행 위치부터 대조해 재개한다. 단절이나 OFF 예약 자체를 프로세스 취소로 표시하지 않는다.

## 8. 설계 검증 항목

1. 별도 검토 세션에 작성자의 완료 주장만 전달되는 경우를 차단하고 원래 요청·기준·대상 버전이 포함되는지 확인한다.
2. 규칙 검사로 충분한 항목에 의무 AI 실행을 추가하지 않는다. 의미 검토가 필요한 항목은 작성 세션을 그대로 재사용하지 않는다.
3. 문구·행 번호 변경, 세션 교체, Task 재분할, 새로운 발견 추가로 수정 주기 한도가 초기화되지 않는다.
4. 최초 실패 평가, 자동 수정 차수, 환경 장애 재시도, 실제 호출 비용이 구분된다.
5. 사용자 답이 필요한 의도 질문이 자동 수정 무한 반복을 일으키지 않는다.
6. 게이트 OFF 뒤 이전 실패·합의 기준·사람 검토 조건이 남고 후속 배정은 독립 조건을 계속 검사한다.
7. 취소 요청과 종료 확인을 구분하며 늦은 결과·중복 결과 수신이 중복 수정이나 배정을 만들지 않는다.
8. 평가 중 입력 변경, 평가 저장 후 정책 변경, 배정 직전 정책 변경에서 오래된 통과로 시작하지 않는다.
9. 재시작 뒤 진행 중 실행의 상태를 대조하고 확인되지 않은 실행을 성공 또는 취소로 단정하지 않는다.
10. 미검증과 환경 장애를 품질 통과로 바꾸지 않고 유효한 동일 증거는 중복 실행 없이 재사용한다.
11. ask-on-decision과 controlled에서 같은 성공 기준을 사용한다. controlled는 결과 확인 뒤 게시·필요 CI를 거쳐 종료하고 내용이 달라진 CI 수정만 재확인한다. 예외는 사람이 수용하며 원 판정이 남는다.
12. 설계·계획으로 미룬 필수 사람 질문이 의존 작업 직전 해소되고 자동 진행 옵션으로 우회되지 않는다.
13. PC 원문이 서버의 영구 로그·캐시·DB에 복제되지 않으며, PC 연결 없이는 원문을 봤다고 주장하지 않는다.
14. 단절의 도구 경계 정지, 게이트 설정의 검증 종료 적용, 권한 철회·hard budget의 실행 제어를 구분하고 CLI·모드별 능력을 검증한다.
15. 작성·리뷰·이견 해소·복구·재시도·지식 추출의 동시 예약이 Case 한도를 넘지 않으며 세션·Profile·Task 분할로 소비를 초기화하지 않는다.
16. 명시 OFF·자동 조건부 생략·미적용·통과와 관찰·추천·적용의 출처를 구분한다.
17. Fast Lane은 최소 논리 기록과 가벼운 정합성 확인으로 진행할 수 있고 위험 발견 시 일반 자동 경로로 전환한다. 전환만으로 사람 승인을 강제하지 않는다.
18. 동일 Runner의 여러 저장소별 버전·증거·게시 상태를 연결하고 부분 게시 실패·응답 유실을 전체 성공이나 자동 롤백으로 바꾸지 않는다.

## 9. 완료·게시와 남은 검증

기본 ask-on-decision은 요청한 완료 지점과 유효 기준을 충족하면 자동 완료한다. controlled는 변경 결과를 확인받고 허용된 게시·필요 외부 검증 뒤 완료한다. `FinalAcceptance`는 실제 사람의 후보 확인에만, 자동 완료는 적용 정책에 연결한다. 예외는 `ExceptionDecision`으로 별도 보존하고 원래 미충족·미검증·게이트 실패를 바꾸지 않는다. 실행이 불명확하거나 확인한 내용이 달라졌으면 과거 인수로 현재 종료를 확정하지 않는다. 완료 후 설명 질문은 응답할 수 있고 수정은 연결된 새 Case로 처리한다.

GitHub에는 Project 지정 기록 저장소의 Case별 새 이슈 하나에 요약·참조만 누적한다. 코드·diff·상세 로그는 제외하고 기존 이슈는 출처로 연결한다. 요약 게시 실패 중 내부 진행은 계속하며 대조·제한 재시도·알림을 관리한다. PR 리뷰만 피드백으로 수집한다. push·PR 생성·수정은 Case 시작에 허용한 범위에서 자동 수행하고 controlled에서는 결과 확인을 먼저 한다. 병합·이슈 종료·CI 수동 재실행·배포는 후속이다. 요약 게시 상태가 품질 판정·인수를 바꾸지 않지만 요청한 완료 지점의 PR·CI 미충족은 완료 조건으로 남는다.

Python/FastAPI·SQLite와 React/TypeScript 웹 UI를 채택한다. Python·Git을 준비한 환경에서 설치·실행 스크립트와 빌드된 UI를 제공하고 일반 사용자 프로세스로 실행한다. CLI는 기존 설치·로그인을 사용한다. PostgreSQL·별도 대형 워크플로 엔진은 이후 확장이다. 이 선택은 CLI 도구 경계·원문 중계·저장 복구의 실제 검증을 대신하지 않는다.

각 호스트의 지정 경로에 하루 1회 자동 백업하고 최근 7개를 보존한다. PC 원문은 PC 측 백업에 남기고 서버로 복제하지 않는다. 백업 순환을 원본·승인·증거 자동 삭제 정책으로 확대하지 않는다. 원격 접근은 LAN 또는 사용자 VPN에서 소유자 인증·암호화 연결을 사용하고 인터넷 직접 공개는 이후 확장이다.

남은 검증은 CLI별 실제 제어·관측·예약 한도 강제 능력, 출력·동일 결함 매칭·증거 재사용, 개별 미응답·환경 재시도·게시 알림 수치, PC/서버 백업 복원이다. 전체 예산 기본 무제한과 설정 한도 강제, repair·환경 복구 한도의 독립성을 검증한다. 최소 지식 관리·추출·주입은 포함하되 매 Case의 별도 채택 리뷰나 공동 작업 역할 UI를 강제하지 않는다.
