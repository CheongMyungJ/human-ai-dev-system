# 프로젝트 지식의 관리·추출·활용 기준안 v0.7

작성일: 2026-09-21. 사용자 결정 D-67과 관련 D-59~66을 반영한다. 구현 완료를 뜻하지 않는다. 첫 버전은 기존 업무 기록에서 재사용 가치가 있는 지식을 관리하고, 작업 범위와 활동에 맞는 내용을 자동 제공한다. 외부 RRF·MCP·범용 지식 검색은 이 시스템의 범위에 포함하지 않는다.

## 1. 목적과 최소 범위

목적은 반복 판단과 실수를 줄이고 해당 작업에 이미 적용되는 결정·제약을 놓치지 않도록 하는 것이다. 모든 중간 산출물을 별도 지식으로 복제하거나 매 Case에 지식 문서를 요구하지 않는다. 기존 Decision·ArtifactRevision·프로젝트 문서를 원본으로 참조하며 짧은 적용 내용을 연결한다.

초기 대상은 다음과 같다. 분류는 설명을 돕는 표식이며 고정 절차나 자동 권위를 만들지 않는다.

| 대상 | 남길 내용 | 주의할 점 |
|---|---|---|
| 설계 결정·ADR | 무엇을 왜 선택했는지, 조건·대안·트레이드오프 중 재사용할 이유 | 결정의 효력과 코드 반영 상태는 별도 |
| Constraint | 지켜야 하는 조건과 예외, 적용 범위, 권위 있는 출처 | AI가 새로 추론한 제안을 의무로 승격하지 않음 |
| Known Problem | 반복하거나 재발 가능한 문제, 발생 조건, 진단·검증 방법 | 당시 관찰·가설·해결 여부를 구분 |
| 필요한 구조·운영 지식 | 코드 일부만으로 파악하기 어려운 구조, 비자명한 빌드·테스트·환경 조건 | 설명 대상과 코드·도구·환경의 적용 조건 보존 |

등록 가치가 있으려면 출처가 있고, 적용 범위를 정할 수 있으며, 다른 업무에서 다시 사용할 가능성이 있어야 한다. 비자명한 판단 근거, 반복 조사 비용 절감, 여러 위치에 걸친 구조 설명, 중요한 오류 예방 중 하나 이상이 등록 이유가 될 수 있다. 모든 가치 기준을 동시에 만족하도록 요구하지 않는다. 단순 실행 로그·일시적 가정·Case 진행 요약 자체는 지식으로 승격하지 않는다.

## 2. 관리: 원본·권위·상태

`Project Knowledge Registry`는 기존 원본과 적용 관계를 관리하는 논리적 등록부다. 별도 중앙 원문 저장소를 뜻하지 않는다. 초기에는 다음 정보만 필요하며, 모든 항목에 복잡한 메타데이터를 채우도록 강제하지 않는다.

| 정보 | 최소 계약 |
|---|---|
| 식별·버전 | 안정된 ID, 변경 이력과 원본 버전 참조 |
| 적용 내용 | 작업에 제공할 짧은 핵심 내용, 필요한 조건·예외 |
| 출처·근거 | 기존 결정·문서·Case·실행 증거의 참조와 권위의 근거 |
| 범위·활동 | Project, 필요한 Repository와 경로·모듈·API, 관련 활동·환경 조건 |
| 효력·상태 | 필수/참고, 후보/활성/대체/무효, 변경 이유와 대체 관계 |

경로는 Repository 식별자와 함께 해석한다. 한 Repository에서 얻은 사실을 같은 Project 전체에 자동 일반화하지 않는다. Project 공통 규칙은 그 범위가 확인된 경우에만 공통으로 등록한다. 전체 심볼 그래프나 모든 API의 관계 모델은 초기 요구가 아니다.

**출처의 권위, 지식의 유효성, 코드의 구현 상태는 다른 정보다.** 이미 확정된 사용자 결정·프로젝트 규칙을 충실히 등록할 때 기존 권위를 승계하며 같은 내용을 다시 승인받지 않는다. 관찰·운영 팁은 근거와 조건을 확인하여 참고 지식으로 자동 활성화할 수 있다. AI가 도출한 새 의무·금지·예외는 참고 후보이며 사용자 결정이나 명시적 위임 없이 필수 규칙이 되지 않는다. 등록·활성화 자체는 실행 권한을 생성하지 않는다.

승인된 ADR·Constraint가 아직 구현되지 않았거나 코드와 다르면 미반영·위반 가능성을 표시한다. 코드 변경만으로 규칙을 무효화하지 않는다. 반대로 Case 브랜치에서 검증한 사실은 해당 코드·환경에 한정되며 기본 브랜치에 적용됐다고 표시하지 않는다. 병합 여부 하나만으로 모든 사실의 유효성을 결정하지 않고 실제 코드·환경과의 관계를 확인한다.

### 수명과 갱신

- **후보:** 재사용할 가능성이 있으나 근거·범위·충돌 등을 더 확인해야 하는 상태. 조사 단서로 제시할 수 있지만 필수 지침으로 적용하지 않는다.
- **활성:** 지정 범위에서 제공할 수 있는 상태. 활성 참고 지식과 활성 필수 규칙을 구분한다.
- **대체:** 후속 버전·결정이 이전 내용을 대체한 상태. 이전 원본과 당시 사용 기록은 보존한다.
- **무효:** 철회·반증 등 근거에 따라 더 이상 적용하지 않는 상태. 단순 조회 실패나 적용 여부 미확정을 무효로 기록하지 않는다.

변경은 새 버전·이유·대체 관계로 남기며 과거 내용을 덮어쓰지 않는다. 재확인이 필요한 이유와 원문 가용성은 별도로 표시한다. 대체·무효 항목은 기본 주입에서 제외하되 이력을 자동 삭제하지 않는다.

재확인 조건은 지식의 성격을 따른다. 설계 결정·제약은 권위 있는 변경·철회·예외와 관련 계약을, 구조 설명은 설명 대상의 변경을, 운영 지식은 도구·환경 변경을 확인한다. 알려진 문제는 수정·재발 근거를 갱신하되 해결 후에도 진단 지식으로 남길 수 있다. 모든 커밋 변경으로 모든 지식을 만료시키지 않는다.

충돌 시 출처의 권위·적용 조건·명시적 대체 관계를 먼저 확인한다. 최신 날짜·좁은 경로·AI 다수 의견만으로 우선순위를 만들지 않는다. 관찰 충돌은 코드·환경 차이를 조사하고, 규칙과 코드가 다르면 규칙 폐기 전에 구현 위반인지 확인한다. 근거만으로 해소하지 못한 중요한 선택은 사람에게 요청하며 해당 지식에 의존하는 작업만 보류한다. Case 한정 예외를 Project 규칙 변경으로 확대하지 않는다.

## 3. 추출: 의미 있는 사건에서 선택적으로 수행

추출 계기는 중요한 결정 확정, 비자명한 문제 해결, 반복 문제 발견, 구조·계약 변경, 기존 지식의 반증·수정이다. 기존 작성·검토·완료 실행에서 해당 근거를 이미 다루었다면 그 결과에 후보를 함께 남길 수 있다. 후보가 없다는 이유로 별도 AI 실행을 만들거나 모든 Case에 지식 산출물을 요구하지 않는다.

자동 추출은 내용을 새로 발명하는 과정이 아니다. 실제 결정과 관찰을 원본에 연결하고, 관찰·가설·제안의 성격을 유지하며 적용 범위를 좁힌다. 참고 후보의 자동 활성화에는 원문·증거와의 일치, 적용 조건, 기존 지식과의 충돌 확인이 필요하다. 확인되지 않으면 후보로 남긴다. 기존 권위의 승계와 새로운 필수 규칙 채택은 구분한다.

QG-08의 출처·범위·충돌 확인 원칙을 사용하되 모든 후보에 독립 AI 검토·사람 승인·별도 게이트 실행을 의무화하지 않는다. 필요한 검사 수준은 기존 정책과 위험을 따른다. 지식 정리·채택은 Case 완료와 독립이며 선택적 추출 대기가 완료를 막지 않는다.

추출·중복 후보 정리·재확인도 실행과 사용량을 기록한다. 별도 실행이 필요하면 [예산 정책](autonomy-budget-policy.md)의 적용 대상이다. 완료 후 작업이라는 이유로 예산을 우회하지 않으며 설정된 한도 내에서 수행할 수 없으면 미처리 상태로 남긴다. 같은 출처·내용의 중복 후보는 연결·정리하고 원본 기록을 삭제하지 않는다.

## 4. 활용: 적용 범위와 활동에 따른 자동 제공

컨텍스트 구성기는 현재 작업의 범위와 활동을 등록된 적용 관계에 대조한다. 설계에 ADR, 구현에 Constraint, 검증에 Known Problem을 우선 제공할 수 있지만 배타적인 단계 매핑으로 사용하지 않는다. 제약은 설계 선택 전에도 필요하고 알려진 문제는 재현·계획·구현에도 필요하다. Fast Lane처럼 별도 단계가 생략돼도 해당 활동에 필요한 내용은 제공한다.

적용 관계는 첫 관련 작업 전, 작업 범위·중요 판단 대상이 바뀔 때, 관련 지식·원본이 변경됐을 때 재평가한다. Repository·경로·API 등의 범위가 넓어지면 추가 적용 항목을 확인한다. 아직 범위를 모른다는 사실을 미적용으로 간주하지 않는다. 우선 공통 필수 제약을 제공하고 조사로 범위가 드러나면 보완한다. 실행 중 범위 확대를 관측하고 관련 행동 전에 지식을 갱신할 제어 경계가 필요하다. 해당 CLI 실행 방식에서 이를 지원하지 못하면 작업을 분할하거나 지원되는 경계에서 멈춰 입력을 재구성하며, 실행 중 동적 주입을 보장한다고 표시하지 않는다.

범위와 관련 입력이 변하지 않으면 유효한 제공 결과를 재사용하며 활동 이름만 바뀌었다고 원문을 다시 읽거나 AI를 재실행하지 않는다.

| 구분 | 제공 계약 |
|---|---|
| 필수 | 실제 핵심 내용과 적용 조건·예외를 컨텍스트에 포함. 원문 참조만으로 전달 완료 처리하지 않음 |
| 참고 | 관련성이 높은 짧은 내용·출처를 제공하고 필요하면 원문 조회. 참고 자료 부족을 필수 규칙 위반으로 바꾸지 않음 |
| 후보 | 불확실성과 후보 상태를 유지하여 필요한 조사 단서로 제공. 확정 규칙으로 표현하지 않음 |

입력 예산이 부족해도 필수 내용을 조용히 제외하지 않는다. 필요한 조건을 유지한 채 작업·입력을 나누거나 해당 의존 작업을 보류한다. 필수 적용 내용 확인 불가·중요한 적용 범위 불확실·미해결 충돌은 필요한 조사나 사람 판단으로 연결한다. 참고 자료의 접근 실패는 한계를 표시하며 무관한 업무까지 중지하지 않는다.

보장 범위는 **등록된 적용 관계 중 현재 범위에 해당하는 필수 항목을 제공하고 누락·불확실성을 드러내는 것**이다. 모든 관련 지식을 발견했다는 보장이 아니다. 전체 자료를 무조건 입력하거나 광범위한 검색 시스템을 요구하지 않는다.

### ContextManifest와 결과 검증

각 실행의 ContextManifest에는 적용 범위·활동, 선택한 지식 ID·버전, 실제 제공한 내용 참조, 제외 이유, 적용 미확정·접근 불가를 남긴다. 원문을 추가로 읽었으면 읽은 원본 버전도 기록한다. AI의 ‘읽었다’는 선언만으로 실제 원문 조회 기록을 만들지 않는다. 사후 발견한 누락은 당시 입력과 연결하며 처음부터 누락을 모두 알고 있었다고 표현하지 않는다.

주입은 전달 기록이며 이해·준수의 증거가 아니다. 규칙 검사·실행 증거·필요한 독립 검토가 준수 여부를 따로 확인한다. 검토 AI가 새 요구를 만들거나 필수 지식을 제공했다는 이유로 실제 검증을 생략하지 않는다. 세부 입력·출력 계약은 [별도 검토 컨텍스트](review-context-contract.md)를 따른다.

진행 중 다른 Case에서 관련 지식이 갱신되어도 기존 Run의 입력 기록을 바꾸지 않는다. 다음 배정·결과 채택에서 영향을 확인하고 영향을 받는 결과만 재평가한다. 지식 갱신이 기존 Case의 합의 기준을 자동으로 다시 쓰지는 않는다. 실제 요구·범위 변경이면 material delta로 처리하고, 무관한 지식 변경으로 모든 Case를 무효화하지 않는다.

## 5. 보관·Repository·Runner 경계

상세 지식·발췌·원문은 소유 Runner에 보관하고 서버에는 허용된 상태·요약·참조만 둔다. 등록부라는 이름으로 상세 지식이나 코드에서 파생한 자료를 서버에 모으지 않는다. 웹 원문 조회는 기존 인증된 일시중계 계약을 따른다. [데이터 경계](data-boundary-review.md)의 원문 비보관·백업·가용성 원칙을 그대로 적용한다.

한 Case가 다루는 여러 Repository는 같은 Runner에 놓이지만, 과거 Case의 모든 지식 원문까지 그 Runner에 존재한다는 뜻은 아니다. 소유 Runner와 원본 가용성을 확인하고 필요한 자료가 없으면 앞의 필수/참고 처리 원칙을 적용한다. 자동 주입이 다른 Runner의 상세 원문을 무단 복제할 권한을 만들지 않는다.

Project의 지정 기록 Repository에 만드는 Case 이슈 하나는 요약 기록 위치다. 지식의 원본·권위·적용 범위를 그 Repository로 바꾸지 않는다. 복수 Project에 걸친 업무·자동 지식 일반화는 첫 버전 범위가 아니다.

## 6. 수용 시나리오

| 시나리오 | 확인할 결과 |
|---|---|
| 기존 확정 Constraint 등록 | 원래 권위를 승계하고 같은 내용의 재승인 없음. AI가 새 규칙을 덧붙이지 않음 |
| AI의 반복 관찰에서 규칙 제안 | 근거·범위를 가진 참고 후보로 남고 자동 의무화되지 않음 |
| Fast Lane에서 바로 수정 | 별도 설계 단계 없이도 관련 필수 내용이 제공되고 실제 준수는 별도 검증 |
| Repository·경로 범위 확장 | 새 적용 항목을 확인하고 다른 Repository의 동명 경로를 혼동하지 않음 |
| 필수 본문 조회 불가·입력 한도 부족 | 링크만으로 제공 완료 처리하거나 조용히 누락하지 않고 해당 의존 작업을 처리 |
| Case 브랜치의 운영 사실 추출 | 당시 코드·환경에 연결하고 기본 브랜치의 사실로 일반화하지 않음 |
| 다른 Case의 규칙 갱신 | 기존 Manifest 보존, 관련 결과의 영향 확인, 필요 시 material delta 처리 |
| 후보 없음·추출 예산 부족 | 불필요한 추가 실행·최종 완료 차단 없이 후보 없음 또는 미처리 상태를 기록 |
| 과거 지식 소유 Runner 미연결 | 원문 가용성 표시, 서버 요약의 원본 승격·상세 원문 자동 복제 없음 |

시험은 이러한 계약의 동작과 실제 제공 기록을 확인한다. 등록된 규칙의 제공 성공을 완벽한 기억·지식 발견·AI 준수로 확대하지 않는다.
