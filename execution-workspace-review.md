# 실행·작업공간과 GitHub 반영 상세 검토안 — v0.8

작성일: 2026-09-21. 1차·2차 개정: 2026-09-23. 상태: D-01~90 중 실행·외부 반영 관련 결정, 특히 D-70·73~79·85·86·89를 반영한 설계 계약. CLI별 동작·운영체제·권한 격리를 실제 검증한 결과는 아니다. 화면과 대화 흐름은 [대화 중심 UI 설계](ui-conversation-design.md)를 따른다.

구현 기준은 P4-03 완료·스키마 v15다. 이번에 확정한 요청 처리 단위의 전송 잠금·중단, 기록 Repo의 코드 작업 겸용, 미커밋 변경을 보존 복사하는 시작 경로, 목적·유형 변경의 실행 진입과 외부 편집기 연결은 기존 구현의 추가·변경과 실제 검증이 필요하다. 아래 목표 동작을 현재 지원 능력으로 표시하지 않는다.

## 1. 확정 범위와 실행 경계

하나의 Project에 여러 Repository를 연결하고 Case는 필요한 일부를 선택한다. Case의 코드 작업은 필요한 Repo·원문에 접근할 수 있는 동일 Runner에서 수행한다. 여러 Runner에 분산하는 Case와 여러 Project를 가로지르는 업무는 후속 범위다. 복수 Repo 등록을 원문·자격증명의 자동 이관 허용으로 해석하지 않는다.

GitHub 개발 쓰기는 Case 시작 시 구체적으로 허용한 범위의 push·PR 생성/수정을 지원한다. 범위 안에서는 매번 확인하지 않되 controlled의 결과 후보 확인을 먼저 적용한다. 병합·이슈 종료·CI 수동 재실행·배포·강제 push를 이 허용에 포함하지 않는다. 기록용 이슈·요약 게시는 별도 허용을 따르며 논의·준비 단계에는 이슈를 만들지 않는다. 업무화 뒤 연결·게시 허용이 갖춰지면 Project 지정 위치에 Case당 새 이슈 하나를 만든다. 대표 저장소를 기본 이슈 위치로 하고 같은 Repo가 코드 작업과 기록을 겸할 수 있다. 기록 Repo 지정만으로 코드 변경 집합에 자동 추가하거나 코드 권한을 부여하지 않으며, 별도로 유효한 코드 작업 허용이 있으면 겸용을 이유로 거부하지 않는다. PR 리뷰는 피드백 자료이며 일반 이슈 댓글은 실행 입력·승인이 아니다.

Runner 연결이 끊기면 현재 도구 호출이 끝나는 안전 경계에서 멈추고 재연결 후 재개한다. 이는 긴 CLI 세션 전체가 끝날 때까지 계속 실행한다는 뜻이 아니다. 제어부의 배정, Runner의 도구 실행, 외부 반영을 구분하여 같은 정책을 검사해야 한다.

## 2. Case 작업공간 생성·사용

Project의 Repo 목록, Case의 기존 허용 범위, 현재 선택한 Repo 집합을 구분한다. AI는 원 요청의 목표·권한 안에서 관련 Repo를 추가 선택하고 이유·범위 버전을 기록할 수 있다. Project에 속한다는 사실만으로 쓰기·외부 반영 권한을 얻지는 않는다. 명시 제외 Repo, 새 권한, 중요한 제품·보안·데이터 영향 또는 목적·성공 기준 확대가 생기면 사람에게 확인한다. 선택 집합 변화에 영향받는 계획·지식 입력·증거를 재평가한다.

Case의 허용 Repo pool은 원 요청·프로젝트 정책·명시 제외로 정해지는 접근 범위다. 현재 선택은 그 안에서 이번 작업에 관련된 대상이며, Repo별 읽기·쓰기 허용과 push·PR 사전 허용은 각각 대조한다. 선택했다고 쓰기까지 허용되거나 쓰기 허용이 외부 반영 허용으로 확대되지는 않는다.

코드 변경 작업공간은 `Case × Repository`별로 식별한다. 각 Repo의 실제 위치, 시작 기준 커밋, 전용 브랜치와 worktree 경로를 기록하고 Task·Run이 사용하는 Repo와 작업공간을 명시한다. 읽기 전용 Repo를 변경 대상으로 간주하지 않는다. 브랜치·경로 충돌이 있으면 기존 내용을 덮어쓰지 않고 소유 관계를 대조한다. 사용자 원래 작업공간과 시스템이 만든 공간을 구분하고 Windows 경로는 실제 해석된 경로와 허용 범위로 확인한다.

일반적인 새 코드 업무는 등록된 원래 작업공간의 현재 체크아웃 브랜치 마지막 커밋을 기본 시작 기준으로 제안한다. 사용자는 작업 기준에서 다른 브랜치·커밋을 선택할 수 있고 실행 시작 전 실제 커밋으로 고정한다. 미커밋·미추적 변경이 있으면 변경 목록과 `포함해서 시작 / 커밋된 코드에서 시작`을 제시한다. 포함 선택은 원래 폴더를 보존하면서 별도 작업공간에 반영하고 포함한 변경의 스냅샷·출처를 기록해야 한다. ~~이 복사 경로는 현재 구현 완료 기능이 아니다.~~ **UI-04d(2026-09-24)가 구현했다** — 포함은 복사가 아니라 **스냅샷 커밋**이다: 작업 PC 가 임시 인덱스로 사용자 트리를 HEAD 위의 커밋으로 만들어 그 커밋에서 worktree 를 펴며(`base_commit` = 스냅샷, `committed_base` = HEAD, 포함 수·지문 기록) 원래 트리·인덱스·브랜치·HEAD 는 그대로다. 더러운 트리는 사람이 고르기 전에는 만들지 않고, 사람이 본 목록과 다른 것은 포함하지 않는다([UI-04d 결과](ui/evidence/UI-04d-results.md)). 완료된 업무의 후속 변경은 이전 결과를 시작 기준으로 제안하고 실제 코드 상태·권한을 다시 확인한다 — 이 제안 경로는 아직 없다(후속 대화도 HEAD 기준).

사용자의 원래 작업공간에 미커밋·미추적 파일이 있어도 이를 자동 커밋·stash·reset·삭제하지 않는다. 별도 worktree 생성은 원래 작업공간을 깨끗하게 만들기 위한 작업이 아니다. 선택되지 않은 변경을 몰래 복사하거나 폐기하지 않는다. 시작 기준이 불분명하면 Case가 어떤 코드를 기반으로 할지 먼저 확인한다.

같은 Case에서 작업공간을 변경하는 실행은 한 번에 하나만 배정한다. 복수 Repo 지원이 동시 쓰기 확대를 의미하지 않는다. 파일을 수정하는 구현뿐 아니라 빌드·테스트가 같은 디렉터리에 생성물을 쓰는 경우도 고려한다. 읽기 전용 검토는 고정된 산출물·코드 스냅샷으로 병렬 수행할 수 있다. 공유 저장소 메타데이터를 바꾸는 작업에는 별도 짧은 조정 구간을 두고, 서로 다른 Case의 포트·테스트 DB·임시 디렉터리 충돌도 확인한다.

사용자가 Case worktree를 직접 편집하면 시스템 쓰기와 경합할 수 있다. 실행 전후 HEAD·인덱스·미커밋 변경을 비교하여 예상하지 못한 변경을 보존하고 영향받는 작업을 조정한다. 이 감지는 동시 쓰기를 완벽히 예방하는 권한 격리가 아니다. worktree 역시 파일 배치의 분리 수단이며 다른 경로·공유 자격증명 접근을 막는 sandbox라고 설명하지 않는다. 첫 버전은 D-42·44에 따라 한 사용자의 신뢰하는 프로젝트와 CLI 권한 설정·시스템 승인을 전제로 하며 OS 완전 격리를 보장하지 않는다.

앱에서는 문서·diff·산출물 버전·검증 근거를 읽고 선택한 내용을 대화에 참조하여 피드백한다. 직접 파일을 편집할 때는 외부 편집기를 사용하며 앱 안의 전체 편집기는 후속 범위로 둔다. 작업 PC 이름·실제 경로와 경로 복사를 제공하고, `열기`는 지원이 검증된 수단이 있을 때 해당 작업 PC를 대상으로 실행한다. 다른 PC의 브라우저에서 누른 동작을 사용자의 현재 PC에 있는 같은 경로로 임의 대체하거나 원격 파일 열기를 지원한다고 추정하지 않는다.

외부 편집 이후 다음 AI 실행 전에 실제 파일·HEAD·인덱스 상태를 다시 확인하고 외부 변경을 보존한다. 당시 검토·검증한 버전은 유지하며 외부 편집한 최신 파일을 기존 산출물 버전이나 통과 증거에 덮어씌우지 않는다. 변경 영향에 따라 입력·결과 후보·검증의 유효성을 재평가한다.

기존 권한·예산 안에서 복구 가능한 로컬 재현·계측·테스트는 분석·연구 목적에 포함하여 자동 수행할 수 있다. 임시 코드 작성만으로 기능 개발의 모든 문서·승인을 강제하지 않는다. 다만 원인 분석 요청을 제품 수정으로 확대하거나 외부 효과·추가 권한을 임시 실험이라는 이름으로 허용하지 않는다.

같은 문제의 미완료 Case에서 사용자가 수정을 추가 요청하면 [Profile 변경 계약](case-profiles.md)에 따라 같은 Case의 의도·Profile 개정으로 연결할 수 있다. 목적 추가만으로 RCA·research의 쓰기 가드를 해제하지 않는다. 수정 목적·Profile·기준과 Repo별 코드 권한·실행 진입을 다시 대조하고 controlled 확인과 새 권한 경계를 유지한다. 이전 실행·검증·실패·repair·사용량은 보존한다.

### 2.1 코드 조합과 검증 증거

복수 Repo의 실행·검토·완료 후보는 `Repo ID → 정확한 스냅샷 참조`의 코드 조합으로 대상을 고정한다. 기준 커밋만으로 실제 입력을 설명할 수 없으면 HEAD·인덱스·미커밋 변경의 보존 스냅샷을 함께 연결한다. 움직이는 브랜치 이름이나 파일 경로만으로 검증 버전을 고정하지 않는다.

증거마다 실제 검증한 Repo·의존 Repo의 코드 조합과 환경·계약 버전을 남긴다. 개별 Repo 검사가 모두 통과해도 공유 API·데이터·호환성의 통합 조건을 충족했다고 자동 추정하지 않는다. 필요한 통합 환경·근거가 없으면 미검증 또는 환경 대기로 남긴다. 무관한 Repo 변경으로 모든 증거를 폐기하지 않고 실제 의존성이 바뀐 증거를 재평가한다.

이미 목표 상태임을 유효한 증거로 확인했다면 변경 없는 결과도 가능하다. 변경 파일 수·CLI 완료 문장·종료 코드만으로 Case 완료를 결정하지 않으며, 미재현을 문제 해결로 확대하지 않는다. 업무별 완료 의미는 [Case Profile](case-profiles.md)을 따른다.

## 3. 배정 중복과 lease·fencing

배정 기록에는 Case·Task·Run·Runner, 선택 범위 버전과 Repo별 작업공간·스냅샷, 입력·정책 버전, 실행 ID, 배정 세대 번호를 연결한다. 같은 요청을 다시 전달해도 Runner는 실행 ID를 조회하여 기존 실행에 연결한다. 이벤트에는 실행 ID와 순번을 사용하고 로컬에 보존한 뒤 전송하여 재접속 때 중복·누락을 대조한다.

lease는 일정 기간 배정 주체를 정하고 갱신하는 수단이다. 유효기간 수치는 아직 정하지 않는다. 만료는 서버가 Runner의 상태를 확신하지 못한다는 신호이며, 이전 프로세스가 실제로 멈췄다는 증거가 아니다. lease 만료만으로 다른 Runner에 같은 Case 쓰기를 맡기지 않는다.

fencing은 배정 세대를 증가시켜 오래된 실행자의 상태 갱신·새 명령·외부 반영을 거부하는 장치다. 이를 검사하는 제어 API·반영 서비스에서는 효과가 있지만, 일반 로컬 파일시스템에 이미 접근하는 CLI를 번호 하나로 멈출 수는 없다. CLI가 직접 쓰기 권한과 자격증명을 갖고 있다면 그 경로에도 실제 통제가 없이는 보장을 확대할 수 없다.

따라서 재배정 전에 기존 실행의 종료·일시 중지와 작업공간 상태를 확인한다. 확인할 수 없으면 `실행 상태 확인 필요`를 유지한다. 호스트를 잃은 경우의 강제 인계·접근 회수 절차는 별도 운영 설계가 필요하며, 현재 설계는 연결 단절을 자동 장애 조치의 근거로 삼지 않는다.

## 4. 연결 단절에서 재개까지

실행 PC와 연결이 끊기면 화면에는 마지막 확인 상태·시각과 `연결 끊김 · 실행 상태 확인 필요`를 표시한다. 이미 불러온 내용과 입력 초안은 유지하고 편집할 수 있으나 일반 메시지와 질문 답변의 전송은 중지한다. 재연결 뒤 실제 상태를 대조한 다음 전송 가능 여부를 갱신하며 입력 초안을 자동 전송하지 않는다. 화면의 연결 상태를 실제 실행 종료나 Case 완료로 바꾸지 않는다. GitHub 게시만 실패한 경우에는 내부 업무를 계속하고 게시만 재시도한다.

| 시점 | Runner와 제어부의 동작 |
|---|---|
| 정상 연결 | 배정·정책을 확인하고 허용된 도구 호출 시작. 호출 전후 상태 기록 |
| 단절 감지 | 다음 호출을 시작하지 않도록 로컬 제어를 예약. 이미 시작한 호출은 상태를 추적 |
| 현재 호출 종료 | 결과·부분 변경을 보존하고 안전 경계에 멈춤. 성공·실패 모두 현재 결과로 기록 |
| 호출이 계속 실행 중 | `단절·현재 호출 종료 대기` 표시. 즉시 정지됐다고 말하지 않음 |
| 재연결 | 실행·이벤트·파일 상태, 최신 의도·정책·승인·배정 세대를 대조 |
| 재개 | 현재 조건이 유효하면 다음 호출부터 계속. 변경·충돌이 있으면 필요한 조정 후 재개 |

단절은 감지 지연이 있으므로 실제 연결이 끊긴 순간에 이미 다음 호출이 시작되지 않았다고 보장하지 않는다. 감지 방법과 지연 목표는 운영 요구로 검증한다. 호출이 병렬로 열려 있다면 이미 시작한 호출들의 상태를 모두 확인하고 새로운 호출을 막아야 한다. 오래 실행되는 호출을 강제 종료할지, 얼마 뒤 알릴지는 이번 결정에 포함되지 않는다.

‘도구 호출 종료’도 실제 부수 작업의 종료와 같아야 한다. 셸 명령이 백그라운드 자식을 남기거나 원격 작업을 시작하고 즉시 반환하면 활동이 남아 있을 수 있다. 이런 동작은 자식·외부 실행 상태를 추적하거나 안전 경계 보장 범위 밖이라고 표시한다. 현재 도구가 이미 수행한 외부 효과를 단절 뒤 되돌릴 수 있다고 가정하지 않는다.

CLI가 내부 도구 시작·종료를 알려주는 것만으로 충분하지 않다. 다음 호출을 실제로 보류하는 제어 능력도 필요하다. Runner가 CLI 프로세스를 한 번 실행한 뒤 내부 활동을 관찰·제어할 수 없다면 선택한 안전 중지 정책을 보장할 수 없다. 프롬프트에 ‘끊기면 멈춰라’고 쓰거나 프로세스 종료 신호만 보내는 것을 같은 지원 수준으로 표시하지 않는다.

### 4.1 사용자의 현재 요청 중단과 재시도

대화의 `중단`은 현재 사용자 요청에 따른 실행을 멈추고 후속 실행 시작을 막는 제어다. Case 전체의 취소·완료·보관과 구분한다. `중단 요청 중`과 실제 종료 확인을 별도로 기록하고, 해당 요청의 실행·자식·잔여 활동을 확인하기 전에는 일반 메시지 전송 잠금을 풀지 않는다. 종료를 확인하지 못하면 `실행 상태 확인 필요`를 유지하며 같은 작업을 중복 실행하지 않는다.

이미 작성한 문서·코드·대화와 누적 사용량은 보존한다. 실제 중단이 확인되면 완료한 부분·남아 있는 변경·미완료·미검증을 요약하고 같은 대화에서 다음 요청을 받을 수 있다. 사용자가 재개나 방향 변경을 요청하면 실제 파일·실행·정책 상태를 다시 확인한 뒤 유효한 결과를 재사용한다. 중단 확인 자체가 새 권한이나 필수 검증 면제를 만들지 않는다.

복구 가능한 일시 오류는 기존 권한·예산·재시도 한도 안에서 처리한다. 해결되지 않거나 로그인·설정·사용자 판단이 필요하면 문제·완료한 부분·필요한 조치를 제시한다. `다시 시도`는 같은 Case의 유효 결과·시도·비용을 이어받고, 성공 여부가 불명확한 실행·외부 쓰기는 실제 결과를 먼저 대조한다. 품질 수정 차수와 환경 복구 시도는 별도로 관리한다.

## 5. 어댑터 능력과 검증

각 CLI·버전·실행 모드별로 다음 능력을 기록한다. 문서상 가능, 어댑터 구현됨, 실제 검증됨, 미지원 상태를 구분하며 특정 제품이 지원한다고 이 문서에서 단정하지 않는다. OpenCode는 설치 전 실환경 검증 미완료로 유지한다.

| 능력 | 확인할 내용 |
|---|---|
| 호출 경계 관찰 | 도구 시작·종료와 실행 ID를 구별할 수 있는가 |
| 다음 호출 차단 | 단절 감지 후 CLI가 다음 도구를 실행하기 전에 실제로 막을 수 있는가 |
| 자식·병렬 실행 추적 | 호출 반환 뒤 남은 활동을 확인하는가 |
| 취소·종료 확인 | 요청 수신과 프로세스·도구 종료 확인을 구분하는가 |
| 재개·결과 대조 | 재연결·프로세스 교체 뒤 이미 한 작업을 중복 수행하지 않는가 |

시험은 호출 중 연결 차단, 호출 사이 차단, 결과 전송만 유실, 장시간 호출, 백그라운드 자식, CLI 종료와 Runner 종료를 각각 재현한다. 하나의 실행 모드에서 통과했다고 다른 모드나 버전까지 검증된 것으로 표시하지 않는다. 요구 능력이 없는 조합은 해당 기능의 지원을 보류한다. 사전 허용되지 않은 다른 CLI로 대체할 때는 D-45에 따라 사람에게 확인하며, 확인이 필요한 실제 호환성 실패와 아직 발생하지 않은 가정을 구분한다.

## 6. 허용 범위와 실제 push·PR의 대조

Case 시작 시 허용하는 행동·대상 Repo·원격 ref 또는 대상 PR·조건을 명확히 한다. 실제 커밋이 만들어지기 전의 범위 허용과 준비된 결과 후보의 내용 확인은 다른 기록이다. 구현 중 생성되는 매 SHA마다 사전 허용을 다시 받지는 않지만, 그 SHA가 기존 목표·대상·허용 조건 안에 있는지는 실행 직전에 검사한다. 허용하지 않은 Repo·ref·PR 또는 행동을 계획 변경만으로 추가하지 않는다.

push 준비 결과에는 저장소의 실제 식별자, 목적 원격 ref, 전송할 정확한 커밋 SHA, 검토 기준 원격 상태, diff와 검증 결과를 포함한다. PR 생성·수정에는 대상 저장소·PR, base/head, 관련 SHA, 변경할 제목·본문을 연결한다. 움직일 수 있는 로컬 브랜치 이름만으로 대상을 확인하지 않는다. 기존 허용에 포함되지 않은 행동이나 material delta는 준비된 실제 내용으로 확인받는다.

`controlled`의 순서는 `시작 범위 확인 → 작업·로컬 검증 → 결과 후보 확인 → 허용된 push/PR → 필요한 외부 CI·결과 확인 → 완료`다. 결과 후보에는 Repo별 코드 조합·내용과 예정된 외부 반영을 포함한다. 후보 확인 뒤 코드·의미 있는 내용이 바뀌면 변경된 결과를 다시 확인하고 반영한다. 외부 CI가 동일 내용에 대한 증거만 보탠 경우에는 후보 내용을 다시 승인받지 않는다. CI 수정으로 내용이 달라졌다면 재확인이 필요하다. 기본 ask-on-decision은 기존 허용과 기준을 충족하면 자동 반영·완료할 수 있다.

원격 브랜치가 바뀌면 그 영향을 대조하고 필요한 통합 증거·권한 유효성을 재평가한다. 같은 Repo의 서로 다른 Case worktree는 사용할 수 있지만 같은 원격 ref·PR을 갱신하는 행동은 직렬화한다. 로컬 확인과 원격 반영 사이의 경쟁은 서버 내부 잠금만으로 막을 수 없으므로 사용 수단의 조건부 갱신·충돌 탐지 능력을 검증한다. 충돌을 자동 강제 push로 해결하지 않는다. 응답 유실 시 해당 SHA·내용이 목적 ref·PR에 반영됐는지 먼저 조회하고 결과가 불분명하면 즉시 같은 쓰기를 반복하지 않는다.

### 6.1 부분 반영과 요청별 종료

여러 Repo의 push·PR을 원자적 트랜잭션으로 취급하지 않는다. 일부 반영 후 실패하면 Repo별 실제 상태를 보존하고, 아직 수행되지 않았음이 확인된 행동만 기존 허용·예산 안에서 재시도한다. 결과가 불명확하면 먼저 원격과 대조한다. 이미 성공한 반영을 무조건 반복하거나 자동 rollback하지 않는다.

다른 Repo의 필요한 변경이 아직 반영되지 않아 발생한 CI 실패를 즉시 코드 결함으로 분류하지 않는다. 해당 검증의 전제·코드 조합을 확인하고, 선행 조건이 부족하면 그 상태를 남긴다. 부분 반영·draft PR 생성만으로 원래 요청이 요구한 전체 반영·CI를 충족했다고 표시하지 않는다.

로컬 결과, push, PR 생성 또는 준비 상태, 외부 CI 중 무엇이 완료에 필요한지는 Case의 원 요청·성공 기준으로 정한다. 코드 변경 업무의 프로젝트 기본 완료 지점은 전용 작업공간의 로컬 구현·검증이다. 원래 폴더에 적용·기본 브랜치에 병합됐다는 뜻이 아니며 결과 위치·실제 반영 상태를 표시한다. 요청에 PR·CI가 포함되면 해당 완료 지점과 허용 범위를 적용한다. PR을 요청하지 않은 연구·로컬 수정에 PR을 강제하지 않으며, 필수 외부 결과가 남아 있으면 Case 전체 완료를 보류한다. 요약 기록 게시 대기는 결과를 바꾸는 외부 개발 행동과 별개다. 상세 완료 계약은 [completion-lifecycle.md](completion-lifecycle.md)를 따른다.

## 7. PR 리뷰의 현재 유효성

리뷰·리뷰 댓글은 외부 ID, 작성자, 작성·갱신 시점, 검토 커밋, 파일·위치, 원문을 보존하고 중복 이벤트를 대조한다. 오래된 커밋의 의견을 최신 코드의 확정 결함으로 표시하지 않는다. 코드가 바뀌었어도 여전히 적용되는 의견은 현재 범위와 대조하여 후속 작업 후보로 연결한다.

리뷰 내용은 피드백 자료이며 시스템의 의도 승인·실행 권한 변경 명령이 아니다. 기존 범위의 수정과 중요한 의도·범위 변경을 구분한다. 완료된 Case에 도착한 리뷰의 수정은 기존 Case 재개가 아니라 연결된 새 Case로 처리한다. 새 Case도 변경하는 Repo마다 전용 브랜치·worktree를 사용한다. 새 PR 또는 수정할 기존 PR의 대상과 코드 관계를 새 Case의 허용 범위에 연결한다. 이전 Case의 승인을 새 범위에 묵시적으로 재사용하지 않으며 controlled의 새 결과 확인도 적용한다.

## 8. 초기 운영과 기술 검증

D-55의 시험 규모는 프로젝트 10개·Runner 3대·동시 AI 실행 2개이며 같은 Runner 쓰기는 기본 1개다. 기본 예산은 무제한이지만 설정된 시간·실행·토큰 등 한도는 D-61과 [예산 정책](autonomy-budget-policy.md)에 따라 강제한다. 무제한 기본값도 합의 범위·품질 수정 횟수·실행 권한을 없애지 않는다. D-57의 백업은 각 호스트에 하루 한 번·최근 7개 보관이며 원본을 자동 정리하지 않는다. D-58의 원격 연결은 인증·암호화를 적용한 LAN/사용자 VPN 범위다.

정책상 안전 중지와 CLI의 실제 능력 일치 여부는 [공식 문서 조사](cli-pause-feasibility.md)와 설치 후 계약 시험으로 확인한다. 미지원 조합의 구체 대안은 시험 결과가 나온 뒤 제시하며, 현재는 검증이 필요하다는 상태를 유지한다. 이는 사용자 결정 미정과 다르다.

## 9. 추가 수용 시나리오

1. 한 Project에서 API·UI Repo를 선택하면 동일 Runner에 Repo별 작업공간·기준 상태가 연결되고, 기록 Repo는 코드 변경 대상에 자동 포함되지 않는다.
2. 기존 목표·권한 안의 관련 Repo 추가는 자동 기록하고 필요한 컨텍스트·증거를 갱신한다. 명시 제외 Repo·추가 권한·중요한 영향 확대는 질문한다.
3. 미커밋 변경이 포함된 두 Repo의 통합 검증은 실제 조합을 고정한다. 한 Repo가 바뀌면 영향받는 통합 증거를 현재 통과로 재사용하지 않는다.
4. 한 Repo push 성공 후 다른 Repo의 응답이 유실되면 실제 원격 상태부터 대조한다. 전체 성공으로 표시하거나 이미 성공한 Repo를 자동 rollback하지 않는다.
5. 두 Case가 같은 원격 ref·PR을 변경하려 하면 갱신을 직렬화하고 예상 상태와 대조한다. 서로 다른 worktree라는 이유로 원격 충돌을 무시하지 않는다.
6. controlled 후보 확인 뒤 외부 CI에서 수정이 필요하면 변경 결과를 다시 확인한다. 같은 내용에 대한 CI 증거 추가만으로 같은 후보를 반복 확인하지 않는다.
7. 대표 저장소를 이슈 위치와 코드 대상으로 함께 선택해도 별도로 허용된 코드 작업은 가능하다. 이슈 위치만 지정한 경우에는 코드 작업 권한이 생기지 않는다.
8. 미커밋 변경이 있으면 포함 여부를 선택하고 원래 폴더를 보존한다. 화면의 브랜치 이름뿐 아니라 실제 시작 코드와 포함한 변경을 추적할 수 있다.
9. 중단 버튼의 응답만으로 종료를 표시하지 않는다. 잔여 활동이 불명확하면 전송 잠금을 유지하고, 확인 뒤 같은 Case의 결과·비용을 보존하며 계속한다.
10. 외부 편집기에서 작업공간을 수정하면 다음 AI 실행 전에 변경을 확인·보존하고 영향받는 입력·증거만 재평가한다. 문서의 과거 버전과 당시 검증 근거는 최신 파일로 바뀌지 않는다.
