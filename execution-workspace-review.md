# 실행·작업공간과 GitHub 반영 상세 검토안

작성일: 2026-09-20. 상태: D-36~40을 반영한 설계 초안. CLI별 동작·운영체제·권한 격리를 실제 검증한 결과는 아니다.

## 1. 확정 범위와 실행 경계

첫 버전은 프로젝트당 저장소 하나, Case당 전용 브랜치와 worktree 하나를 사용한다. GitHub 개발 쓰기는 사람이 확인한 코드 push와 PR 생성·수정을 지원하며 병합·이슈 종료·CI 재실행·배포는 후속 범위다. 기록용 이슈·산출물 누적은 이미 합의한 별도 게시 허용을 따른다. 외부 피드백은 PR 리뷰를 수집하며 일반 이슈 댓글을 실행 입력으로 받지 않는다.

Runner 연결이 끊기면 현재 도구 호출이 끝나는 안전 경계에서 멈추고 재연결 후 재개한다. 이는 긴 CLI 세션 전체가 끝날 때까지 계속 실행한다는 뜻이 아니다. 제어부의 배정, Runner의 도구 실행, 외부 반영을 구분하여 같은 정책을 검사해야 한다.

## 2. Case 작업공간 생성·사용

Case 시작 시 프로젝트 저장소 식별자, 실제 저장소 위치, 시작 기준 커밋, 전용 브랜치와 worktree 경로를 기록한다. 브랜치·경로 충돌이 있으면 기존 내용을 덮어쓰지 않고 소유 관계를 대조한다. 사용자 원래 작업공간과 시스템이 만든 공간을 구분하고 Windows 경로는 실제 해석된 경로와 허용 범위로 확인한다.

사용자의 원래 작업공간에 미커밋·미추적 파일이 있어도 이를 자동 커밋·stash·reset·삭제하지 않는다. 별도 worktree 생성은 원래 작업공간을 깨끗하게 만들기 위한 작업이 아니다. 시작 기준에 사용자의 미커밋 변경을 포함할지는 별도 입력이며, 선택되지 않은 변경을 몰래 복사하거나 폐기하지 않는다. 시작 기준이 불분명하면 Case가 어떤 코드를 기반으로 할지 먼저 확인한다.

같은 Case에서 작업공간을 변경하는 실행은 한 번에 하나만 배정하는 안을 제안한다. 파일을 수정하는 구현뿐 아니라 빌드·테스트가 같은 디렉터리에 생성물을 쓰는 경우도 고려한다. 읽기 전용 검토는 고정된 산출물·코드 스냅샷으로 병렬 수행할 수 있다. 공유 저장소 메타데이터를 바꾸는 작업에는 별도 짧은 조정 구간을 두고, 서로 다른 Case의 포트·테스트 DB·임시 디렉터리 충돌도 확인한다.

사용자가 Case worktree를 직접 편집하면 시스템 쓰기와 경합할 수 있다. 실행 전후 HEAD·인덱스·미커밋 변경을 비교하여 예상하지 못한 변경을 보존하고 영향받는 작업을 조정한다. 이 감지는 동시 쓰기를 완벽히 예방하는 권한 격리가 아니다. worktree 역시 파일 배치의 분리 수단이며 다른 경로·공유 자격증명 접근을 막는 sandbox라고 설명하지 않는다. 첫 버전은 D-42·44에 따라 한 사용자의 신뢰하는 프로젝트와 CLI 권한 설정·시스템 승인을 전제로 하며 OS 완전 격리를 보장하지 않는다.

## 3. 배정 중복과 lease·fencing

배정 기록에는 Case·Run·Runner·worktree, 입력·정책 버전, 실행 ID, 배정 세대 번호를 연결한다. 같은 요청을 다시 전달해도 Runner는 실행 ID를 조회하여 기존 실행에 연결한다. 이벤트에는 실행 ID와 순번을 사용하고 로컬에 보존한 뒤 전송하여 재접속 때 중복·누락을 대조한다.

lease는 일정 기간 배정 주체를 정하고 갱신하는 수단이다. 유효기간 수치는 아직 정하지 않는다. 만료는 서버가 Runner의 상태를 확신하지 못한다는 신호이며, 이전 프로세스가 실제로 멈췄다는 증거가 아니다. lease 만료만으로 다른 Runner에 같은 Case 쓰기를 맡기지 않는다.

fencing은 배정 세대를 증가시켜 오래된 실행자의 상태 갱신·새 명령·외부 반영을 거부하는 장치다. 이를 검사하는 제어 API·반영 서비스에서는 효과가 있지만, 일반 로컬 파일시스템에 이미 접근하는 CLI를 번호 하나로 멈출 수는 없다. CLI가 직접 쓰기 권한과 자격증명을 갖고 있다면 그 경로에도 실제 통제가 없이는 보장을 확대할 수 없다.

따라서 재배정 전에 기존 실행의 종료·일시 중지와 작업공간 상태를 확인한다. 확인할 수 없으면 `실행 상태 확인 필요`를 유지한다. 호스트를 잃은 경우의 강제 인계·접근 회수 절차는 별도 운영 설계가 필요하며, 현재 설계는 연결 단절을 자동 장애 조치의 근거로 삼지 않는다.

## 4. 연결 단절에서 재개까지

| 시점 | Runner와 제어부의 동작 |
|---|---|
| 정상 연결 | 배정·정책을 확인하고 허용된 도구 호출 시작. 호출 전후 상태 기록 |
| 단절 감지 | 다음 호출을 시작하지 않도록 로컬 제어를 예약. 이미 시작한 호출은 상태를 추적 |
| 현재 호출 종료 | 결과·부분 변경을 보존하고 안전 경계에 멈춤. 성공·실패 모두 현재 결과로 기록 |
| 호출이 계속 실행 중 | `단절·현재 호출 종료 대기` 표시. 즉시 정지됐다고 말하지 않음 |
| 재연결 | 실행·이벤트·파일 상태, 최신 의도·정책·승인·배정 세대를 대조 |
| 재개 | 현재 조건이 유효하면 다음 호출부터 계속. 변경·충돌이 있으면 필요한 조정 후 재개 |

단절은 감지 지연이 있으므로 실제 연결이 끊긴 순간에 이미 다음 호출이 시작되지 않았다고 보장하지 않는다. 감지 방법과 지연 목표는 운영 요구로 검증한다. 호출이 병렬로 열려 있다면 이미 시작한 호출들의 상태를 모두 확인하고 새로운 호출을 막아야 한다. 오래 실행되는 호출을 강제 종료할지, 얼마 뒤 알릴지는 이번 결정에 포함되지 않는다.

‘도구 호출 종료’도 실제 부수 작업의 종료와 같아야 한다. 셸 명령이 백그라운드 자식을 남기거나 원격 작업을 시작하고 즉시 반환하면 활동이 남아 있을 수 있다. 이런 동작은 자식·외부 실행 상태를 추적하거나 안전 경계 보장 범위 밖이라고 표시한다. 현재 도구가 이미 수행한 외부 효과를 단절 뒤 되돌릴 수 있다고 가정하지 않는다.

CLI가 내부 도구 시작·종료를 알려주는 것만으로 충분하지 않다. 다음 호출을 실제로 보류하는 제어 능력도 필요하다. Runner가 CLI 프로세스를 한 번 실행한 뒤 내부 활동을 관찰·제어할 수 없다면 선택한 안전 중지 정책을 보장할 수 없다. 프롬프트에 ‘끊기면 멈춰라’고 쓰거나 프로세스 종료 신호만 보내는 것을 같은 지원 수준으로 표시하지 않는다.

## 5. 어댑터 능력과 검증

각 CLI·버전·실행 모드별로 다음 능력을 기록한다. 문서상 가능, 어댑터 구현됨, 실제 검증됨, 미지원 상태를 구분하며 특정 제품이 지원한다고 이 문서에서 단정하지 않는다. OpenCode는 설치 전 실환경 검증 미완료로 유지한다.

| 능력 | 확인할 내용 |
|---|---|
| 호출 경계 관찰 | 도구 시작·종료와 실행 ID를 구별할 수 있는가 |
| 다음 호출 차단 | 단절 감지 후 CLI가 다음 도구를 실행하기 전에 실제로 막을 수 있는가 |
| 자식·병렬 실행 추적 | 호출 반환 뒤 남은 활동을 확인하는가 |
| 취소·종료 확인 | 요청 수신과 프로세스·도구 종료 확인을 구분하는가 |
| 재개·결과 대조 | 재연결·프로세스 교체 뒤 이미 한 작업을 중복 수행하지 않는가 |

시험은 호출 중 연결 차단, 호출 사이 차단, 결과 전송만 유실, 장시간 호출, 백그라운드 자식, CLI 종료와 Runner 종료를 각각 재현한다. 하나의 실행 모드에서 통과했다고 다른 모드나 버전까지 검증된 것으로 표시하지 않는다. 요구 능력이 없는 조합은 해당 기능의 지원을 보류한다. 다른 CLI로 대체할 때는 D-45에 따라 사람에게 확인하며, 확인이 필요한 실제 호환성 실패와 아직 발생하지 않은 가정을 구분한다.

## 6. 승인된 push·PR에 대한 구속

push 준비 결과에는 저장소의 실제 식별자, 목적 원격 ref, 전송할 정확한 커밋 SHA, 검토 기준 원격 상태, diff와 검증 결과를 포함한다. 움직일 수 있는 로컬 브랜치 이름만 승인 대상으로 삼지 않는다. 실행 직전 현재 입력·권한과 대조하고, 다른 SHA로 바뀌었으면 기존 확인을 재사용하지 않는다.

원격 브랜치가 바뀌면 그 영향을 대조한다. 로컬 확인과 원격 반영 사이의 경쟁은 서버 내부 잠금만으로 막을 수 없으므로 사용 수단의 조건부 갱신·충돌 탐지 능력을 검증한다. 충돌을 자동 강제 push로 해결하지 않는다. 응답 유실 시 승인 SHA가 목적 ref에 반영됐는지 먼저 조회하고 결과가 불분명하면 즉시 같은 쓰기를 반복하지 않는다.

PR 생성·수정은 대상 저장소·PR, base/head, 관련 SHA, 변경할 제목·본문 등 실제 내용을 고정해 확인받는다. PR 수정 요청과 코드 push는 각자의 변경 대상을 기록한다. 두 행동을 한 화면에서 함께 확인받을 수 있지만 한쪽 확인이 임의의 후속 변경까지 허용하지 않는다. 병합·이슈 종료·CI 재실행·배포는 첫 버전 실행 메뉴와 배정 대상에 포함하지 않는다.

## 7. PR 리뷰의 현재 유효성

리뷰·리뷰 댓글은 외부 ID, 작성자, 작성·갱신 시점, 검토 커밋, 파일·위치, 원문을 보존하고 중복 이벤트를 대조한다. 오래된 커밋의 의견을 최신 코드의 확정 결함으로 표시하지 않는다. 코드가 바뀌었어도 여전히 적용되는 의견은 현재 범위와 대조하여 후속 작업 후보로 연결한다.

리뷰 내용은 피드백 자료이며 시스템의 의도 승인·실행 권한 변경 명령이 아니다. 기존 범위의 수정과 중요한 의도·범위 변경을 구분한다. 완료된 Case에 도착한 리뷰의 수정은 기존 Case 재개가 아니라 연결된 새 Case로 처리한다. 새 Case도 전용 브랜치·worktree를 사용한다. 실제 외부 반영을 준비할 때 새 PR 또는 수정할 기존 PR의 대상과 코드 관계를 제시하고 확인받는다. 어떤 PR을 변경하는지 모르는 상태로 이전 승인을 재사용하지 않는다.

## 8. 초기 운영과 기술 검증

D-55의 시험 규모는 프로젝트 10개·Runner 3대·동시 AI 실행 2개이며 같은 Runner 쓰기는 기본 1개다. D-56에 따라 Case 전체 자동 실행 시간에 기본 한도를 두지 않고 사용량·경과 시간을 표시한다. D-57의 백업은 각 호스트에 하루 한 번·최근 7개 보관이며 원본을 자동 정리하지 않는다. D-58의 원격 연결은 인증·암호화를 적용한 LAN/사용자 VPN 범위다.

정책상 안전 중지와 CLI의 실제 능력 일치 여부는 [공식 문서 조사](cli-pause-feasibility.md)와 설치 후 계약 시험으로 확인한다. 미지원 조합의 구체 대안은 시험 결과가 나온 뒤 제시하며, 현재는 검증이 필요하다는 상태를 유지한다. 이는 사용자 결정 미정과 다르다.
