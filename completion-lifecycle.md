# 업무 완료·예외 종료와 후속 업무 설계안 — v0.8

작성일: 2026-09-21. 1차·2차 개정: 2026-09-23. D-01~90 중 관련 정책, 특히 D-31~35·59~67·69·72·74·76·78·79·85~88을 반영한 상세 설계안이다. [자동진행·위임·예산 정책](autonomy-budget-policy.md), [Case Profile](case-profiles.md), [프로젝트 지식](project-knowledge.md)을 함께 적용한다. 대화·검토 화면은 [대화 중심 UI 설계](ui-conversation-design.md)를 따른다.

구현 기준은 P4-03 완료·스키마 v15다. Profile 정의 v2의 목적별 완료 계약·목적 의무·결론 요구·혼합 목적·실험 정리를 유지하고 기존 v1 Case에 소급 적용하지 않는다. 이번에 추가한 논의·보관, 요청 중단, 미완료 Case 목적·유형 변경, 완료 후 설명의 실행·예산 처리와 후속 Case 이동은 설계 확정 사항이며 구현 완료를 뜻하지 않는다.

## 1. 확인된 정책

| 결정 | 동작 |
|---|---|
| D-31·59·65 완료와 확인 | 기본 ask-on-decision은 요청한 완료 지점 충족 시 자동 완료. controlled는 시작·변경 결과 확인 뒤 허용된 게시와 필요한 외부 검증을 거쳐 완료 |
| D-32 예외 종료 | 사람이 남은 문제·미검증 범위와 영향을 확인하고 예외를 수용하여 종료 가능. 미충족·미검증 모두 대상이며 원래 판정을 통과로 바꾸지 않음 |
| D-33·78 완료 후 수정 | 기존 Case를 재개하지 않고 연결된 새 Case로 처리하고 해당 대화로 이동. 질문만 들어오면 기존 결과에 대해 설명 가능 |
| D-34·63·74 기록 이슈 | 논의·준비 단계에는 생성하지 않는다. 업무화 뒤 연결·게시 허용이 갖춰지면 Project의 지정 위치에 Case마다 새 이슈 하나 생성. 참여 저장소가 여럿이어도 하나이며 기존 이슈는 출처로 연결 |
| D-35 게시 장애 | GitHub 게시 장애 중에도 내부 업무 계속. 게시를 대기·재시도하고 누적 실패를 알림 |
| D-69 보관 | 대화를 목록에서 정리하는 가시성 상태. 완료·예외 종료·취소가 아니며 복원하면 같은 Case에서 계속 |
| D-76 현재 요청 중단 | 현재 요청의 실행과 후속 시작을 멈추고 실제 종료를 확인. 부분 결과·누적 소비를 보존하며 같은 Case에서 계속 가능 |
| D-78 기본 완료 지점 | 코드 변경 업무는 전용 작업공간의 로컬 구현·검증까지. 요청에 PR·CI가 있으면 해당 지점과 별도 허용 범위를 적용 |
| D-86 미완료 목적·유형 변경 | 같은 문제의 미완료 Case는 의도·Profile 개정으로 이어가며 기존 기준·결정·증거 대응과 소비·실패·repair 누적을 보존 |
| D-87 완료 후 설명 | 같은 Case의 기존 결과·근거를 설명할 수 있으나 완료 판정은 불변. 설명 실행도 같은 Case 사용량·예산에 포함하며 추가 실험·수정은 새 연결 Case |

## 2. 상태와 판단의 분리

요청에 따른 실행 위임, 선택한 사람 검토, 품질 게이트, 성공 기준, 결과 인수, 예외 수용, 외부 반영 권한은 각각 관리한다. 사람의 결과 확인이 검증 실패를 없애거나 외부 실행 권한을 자동 부여하지 않는다. QG-06 업무 종료 게이트를 꺼도 이 완료 정책과 합의한 성공 기준은 유지한다.

요청마다 완료 지점을 식별한다. 로컬 변경·검증, PR 준비, 허용된 push·PR 생성, 특정 외부 CI 충족 등 필요한 결과를 구분한다. 게시 권한이 있다는 이유만으로 요청에 불필요한 외부 행동을 추가하지 않는다. Profile은 결과와 증거의 기본 의미를 제공하며 실제 완료 계약은 현재 Case의 목표·기준·완료 지점에 따른다.

코드 변경 업무의 프로젝트 기본값은 로컬 구현·검증까지다. 결과는 전용 작업공간에 있으며 원래 폴더에 적용되거나 기본 브랜치에 병합됐다는 뜻이 아니다. 결과 요약에 변경·검증·남은 문제와 작업공간 위치·실제 반영 상태를 표시한다. 프로젝트 기본값은 명시한 요청·기존 Case의 완료 계약을 조용히 바꾸지 않으며, Profile v2의 목적별 필수 기준도 축소하지 않는다.

| 표시 상태 | 의미와 이후 동작 |
|---|---|
| 최종 확인 대기 | controlled의 변경 결과 후보 또는 별도로 선택한 인수를 기다림. 필요한 외부 CI가 게시 후 실행될 예정임을 표시할 수 있음 |
| 완료 | 현재 합의된 종료 조건을 충족하고 필요한 인수 또는 자동 완료 정책을 적용함 |
| 예외 수용으로 종료 | 지정된 미충족·미검증을 사람이 알고 수용함. 기준별 원래 결과와 예외를 함께 표시 |
| 일시 중지 | 중지 요청·예산 소진 등으로 실행을 보류. 같은 Case의 부분 결과·남은 작업·누적 소비를 유지 |
| 취소 | 업무 수행을 중단하는 결정. 성공이나 예외 인수로 표시하지 않음 |

완료 전 같은 문제의 수정·목적 확장·유형 변경 요청은 진행 중인 같은 Case의 의도·Profile 개정으로 반영하고 기존 기준·결정·증거 대응 및 누적 사용량·실패·repair를 보존한다. controlled의 결과 확인 이후라도 게시·필요 CI가 아직 끝나지 않았다면 종료 전이다. 완료 또는 예외 종료 후의 수정 요청은 새 Case로 연결한다. 취소는 성공 인수가 아니며 실행 중단 요청과 실제 중단 확인을 구분해 기록한다. 실제 공동 작업의 역할·권한 기능은 이후 확장이다.

논의·준비 단계의 대화는 별도 `논의 완료` 판정 없이 보관할 수 있다. 보관은 실행 상태와 별개이며 보관·복원만으로 실행을 중단·재개하거나 종료 조건을 충족시키지 않는다. 현재 요청의 중단 뒤에도 같은 Case를 유지하지만, 이미 완료·예외 종료된 Case의 수정에는 새 Case 원칙을 적용한다.

## 3. 결과 후보와 최종 인수

AI와 시스템은 요청한 완료 지점에 맞춰 `결과 후보`를 구성하고 다음 내용을 버전으로 연결한다. 후보 구성 자체를 사람에게 맡기지 않는다.

- 현재 요청·위임·결정·종료 조건과 결과 산출물, 참여 저장소별 코드 버전·변경 식별.
- 성공 기준별 결과와 실제 증거, 적용 게이트의 상태.
- 남은 문제·미검증 범위·알려진 영향과 제안 후속 조치.
- 업무 범위의 게시 대상·PR·CI 결과, 아직 예정된 외부 검증과 진행 중 실행 상태.

최종 확인 화면에는 결과 요약·변경·기준별 근거와 예정된 게시·외부 검증을 보여주고 `이 결과 확인`, `수정 요청`, 해당할 때 `선택한 예외를 수용하고 종료`를 구분한다. 대화 안의 확인 카드에서 의미가 분명한 버튼으로 결정할 수 있으며 정형 동의 문구를 직접 입력하게 하지 않는다. 필요한 예외 사유·대상 범위는 함께 수집한다. 확인은 사용자가 본 후보의 코드·내용·기준·버전 또는 해시와 결정 주체·시각에 연결한다. 코드나 검토 내용이 달라지면 이전 확인을 새 후보에 사용하지 않는다. 같은 내용의 재전송, 관련 없는 대화, 예정대로 도착한 외부 검증 증거만으로 다시 확인하지 않는다.

기본 ask-on-decision에서는 유효한 필수 기준·검증·권한 조건과 요청한 외부 결과까지 충족하면 결과를 제시하고 자동 완료한다. 기록에는 적용 정책을 남기며 사람이 인수했다고 표시하지 않는다.

controlled의 순서는 `초기 목표·범위·기준·허용 행동 확인 → 작업·로컬 검증 → 변경 결과 확인 → 허용된 push·PR → 필요한 외부 CI → 완료`다. 설계·계획의 사람 검토는 별도 선택이며 같은 내용의 확인을 반복하지 않는다. 이미 해당 대상·정책의 유효한 명시적 시작 확인이 있으면 반복하지 않는다. 상세한 최초 요청만으로 controlled 시작 확인을 자동 생성하지 않는다. 로컬 결과까지가 요청한 완료 지점이면 불필요한 게시를 끼워 넣지 않는다.

게시 전 결과 확인과 Case 완료 판정을 분리한다. 게시 후에만 실행할 수 있는 CI는 예정된 조건으로 남기며 아직 통과했다고 표시하지 않는다. 결과 확인 뒤 같은 후보의 외부 조건이 충족되면 추가 인수 없이 완료한다. QG-06은 후보 준비 상태와 최종 종료 조건을 구분해 평가하고 유효한 검사를 재사용한다. 단계 이름 때문에 별도 AI 검토를 중복 실행하지 않는다. 여기서 게시 전 결과 확인은 코드 push·PR에 적용하며 이미 허용된 기록 이슈·요약 게시까지 지연시키지는 않는다.

CI 실패를 수정하면서 확인한 내용이 달라졌다면 새 후보를 확인받은 뒤 다시 게시한다. 동일 코드의 환경 복구·상태 조회·허용된 재전송만으로 재확인하지 않는다. 복구·수정은 기존 권한·repair·전체 예산을 함께 적용하며 CI 수동 재실행 등 미지원 권한을 자동 생성하지 않는다.

어느 Autonomy에서도 미충족·미검증을 AI가 예외 수용하지 않는다. 예외가 필요하면 대상과 영향을 제시하여 사람의 명시적인 결정을 받는다. 무응답·시간 경과·AI의 낙관적 판단은 인수나 예외 수용이 아니다. 단, 합의한 연구 종료조건이 근거·한계 보고인 경우 판단 불가 결론은 예외 없이 정상 완료일 수 있다. 확정 결론이 필수였으면 미충족으로 남는다.

## 4. 예외 수용의 범위

예외 요청에는 어떤 기준·검사·결과가 미충족 또는 미검증인지, 무엇을 확인했고 무엇을 확인하지 못했는지, 관찰된 영향과 아직 알 수 없는 영향을 구분해 적는다. 사용자는 제시된 예외를 선택하여 수용하거나 수정을 요청할 수 있다.

Profile v2에서 요구된 목적 의무에 성공 기준 자체가 없는 `objective_without_criteria`는 기준의 미충족·미검증과 다르다. 예외 수용 대상이 아니며 자동 완료와 사람 인수 모두 거부한다. 해당 목적의 기준을 먼저 갖춰야 한다. 이 경계는 [P4-03 구현 결과](p4/evidence/P4-03-results.md)를 유지한다.

한 화면에서 최종 인수와 표시된 예외를 함께 결정할 수 있다. 내부적으로는 `최종 인수 기록`과 `예외 결정 기록`을 분리해 보존한다. 예외가 적용되는 대상과 종료에 미치는 효과를 명시하며, 다른 기준이나 향후 업무까지 포괄적으로 면제하지 않는다.

예외 수용은 기존 기준을 삭제하는 요구 변경과 다르다. 예를 들어 ‘10만 행 지원’ 기준이 미충족인데 이번 결과를 수용하면 기준과 실패는 남는다. 요구 자체를 ‘1만 행 지원’으로 바꾸려는 경우에는 의도·기준의 변경 절차로 처리한다. 어느 경우도 AI가 완료하기 쉽게 기준을 몰래 낮추는 방식으로 구현하지 않는다.

예외 수용 또는 최종 인수만으로 push·PR·병합 권한을 생성하지 않는다. 외부 행동은 Case 시작에 허용한 저장소·브랜치·대상·행동 범위와 현재 후보의 조건을 검사한다. 허용 범위 안의 push·PR에는 별도 재승인을 요구하지 않되 controlled의 결과 확인 순서를 지킨다. 범위 밖 행동은 해당 변경을 확인받는다. 종료 기준에 어떤 외부 결과가 포함되는지는 각 업무의 합의에 따른다.

## 5. 실행 정리와 결과 유효성

최종 확인은 실행 프로세스의 종료 명령이 아니다. 종료 후보를 만들 때 진행 중 실행과 결과를 확인할 수 없는 실행을 표시하고, 결과에 영향을 주는 변경이 계속되는지 확인한다. 사용자가 인수하거나 예외를 수용했다고 해당 실행을 성공·취소·종료 확인으로 바꾸지 않는다.

중단이 필요하면 별도 실행 제어를 요청하고 실제 종료 또는 정리 상태를 확인한다. Runner 연결이 끊겼다면 `실행 상태 확인 필요`를 유지한다. 결과 후보의 유효성을 확정할 수 없는 실행이 남은 동안은 인수 결정을 보존하더라도 업무 종료 확정을 보류하고 해당 실행을 대조한다. 이때 같은 작업을 다른 Runner에서 즉시 중복 실행하지 않는다.

늦게 도착한 결과는 원래 실행·버전에 연결한다. 대상 코드나 기준이 달라졌으면 현재 결과를 덮어쓰지 않고 영향받는 조건만 재평가한다. 게시 재시도처럼 결과 내용을 바꾸지 않는 후속 처리와 결과를 바꾸는 개발 실행을 구분한다.

예산 소진이나 권한 철회 도중 외부 요청이 이미 전송됐을 수 있다. 결과를 대조하기 전 중복 쓰기·다른 Runner 재배정을 하지 않는다. 일부 저장소의 게시가 완료되고 다른 저장소가 실패했다면 각각의 실제 상태를 보존하고 허용된 범위·예산 안에서 제한 재시도한다. 부분 성공을 전체 완료로 만들거나 이미 반영한 결과를 자동 롤백하지 않는다.

정리를 위해 필요한 소비는 예산 안에서 예약한다. 한도 소진 뒤 새 AI 요약·수정 실행을 무조건 시작하지 않는다. 기존 결과의 기록·실행 대조는 계속할 수 있으며, 추가 작업 없이 종료 조건이 확인됐다는 사실과 새 자원을 소비해 작업을 재개하는 것을 구분한다.

## 6. 종료 후 새 Case와 GitHub 기록

완료 후 질문은 기존 결과·증거를 설명하는 대화로 처리할 수 있다. 실제 수정·추가 개발 요청이면 기존 Case를 참조하는 새 Case를 만들고 해당 대화로 이동한다. 이전 결과·관련 결정·제약·수용한 예외·새 요청을 출처로 연결하고 상단에서 이전 업무로 돌아갈 수 있게 한다. 명확한 후속 요청에 대해 새 업무를 만들지 다시 묻는 절차를 강제하지 않는다. 기존 완료 기록은 유지하며 새 Case의 진행 상태와 섞지 않는다.

완료 후 설명은 같은 Case의 기존 결과·근거를 읽어 답하는 범위다. 원래 완료 판정·종료 시점 후보·기준·증거를 변경하거나 새 제품 검증을 수행하지 않는다. 추가 실험·수정이 필요하면 연결된 새 Case로 처리한다. 설명에 사용한 AI 실행·토큰·시간 등도 같은 Case의 총사용량·기존 예산에 누적하며 화면에는 종료 시점 사용량과 종료 후 설명 사용량을 나눠 보여준다. 잔여 예산이 부족하면 한도 조정이 필요하고 별도 무제한 설명 실행으로 우회하지 않는다.

현재 구현은 닫힌 Case의 모든 새 Run과 정책 변경을 차단한다. 따라서 이 설명 흐름은 완료 결과를 다시 여는 기능과 구분된 실행 진입(admission)·예산/정책 변경 이력의 추가 설계·구현이 필요하다. 종료 후 한도 변경은 설명에 적용한 값·주체·시점·이유를 남기고 종료 당시 정책·완료 근거는 유지한다. 완료 결과물과 버전도 과거 상태로 조회할 수 있도록 보존하며 후속 설명이나 새 Case의 결과로 덮어쓰지 않는다.

새 Case는 원래 결과를 참고하되 새 요청에 맞는 Profile·목표·범위·기준·권한과 실제 적용 Project·Case 정책을 평가한다. 적용 Autonomy가 ask-on-decision이고 요청이 명확하면 자동 진행하며 기능 개발이라는 이유로 별도 초안 승인을 일괄 요구하지 않는다. controlled가 적용되면 해당 시작·결과 확인을 유지한다. 이전 인수나 위임을 새 범위의 포괄 허용으로 사용하지 않는다. 요청 정합성 확인과 선택한 사람 검토·필수 검증은 적용한다.

논의·준비 단계의 Case에는 GitHub 이슈를 만들지 않는다. 업무화 뒤 연결·게시 허용이 갖춰지면 새 Case마다 Project의 지정 위치에 새 GitHub 이슈 하나를 만들고 원래 업무·기존 이슈는 출처 링크로 연결한다. 미설정·미허용이면 실제 게시하지 않고 내부 작업은 계속한다. 기존 이슈가 요청의 시작점이어도 기록 이슈를 재사용하지 않는다. 참여 저장소별 PR·코드 버전을 연결하되 Case 이슈는 하나다. 해당 Case의 게시 허용 범위에서 시스템 표시가 있는 요약·참조를 누적하며 코드·diff·상세 로그는 제외한다.

GitHub 요약 게시 실패는 내부 산출물·결정의 저장 실패와 구분한다. 내부 작업은 계속하고 게시 항목을 대기열에 보존하여 재시도한다. 최종 결과도 같은 방식으로 누적하며 `완료 · GitHub 요약 게시 대기`처럼 두 상태를 함께 표시할 수 있다. 반면 요청한 완료 지점에 포함된 코드 push·PR·CI가 미충족이면 정상 완료로 표시하지 않는다. 누적 실패 알림의 구체 횟수·시간·채널은 운영 정책에서 정한다. 응답만 유실된 경우에는 외부 게시 여부를 먼저 대조해 중복 생성을 줄인다.

의미 있는 작업 결과 중 재사용 가치가 있는 결정·제약·비자명한 사실을 선택적으로 지식 후보로 만들고 출처·적용 범위·유효성을 연결한다. 지식 후보 채택은 Case 완료와 독립이며 모든 Case에 별도 QG-08 리뷰를 강제하지 않는다. 선택된 지식 추출·검토 실행도 해당 Case의 예산에 귀속하고 부족하면 후보·추출 대기 상태를 보존한다. 미채택 지식을 완료나 미래 작업의 권한으로 사용하지 않는다. 후보가 없거나 추출 가치가 낮으면 생략하고 모든 Case에 지식 산출물·추출 실행을 의무화하지 않는다.

## 7. 최소 데이터 확장

| 기록 | 최소 정보 |
|---|---|
| `CompletionPolicy` | 요청한 완료 지점, Autonomy와 선택한 사람 검토, 정책 버전·변경 주체 |
| `CompletionCandidate` | Case revision, 결과·위임·기준 버전, 저장소별 코드·게시 대상, 증거·게이트·외부 결과·예정 조건, 미해결 항목, 실행 정리 상태 |
| `FinalAcceptance` | 확인한 후보의 내용·기준·코드 식별, 실제 사람 결정·주체·시각, 유효성·수정 요청. 자동 정책 적용과 구별 |
| `ExceptionDecision` | 후보와 대상 기준·발견, 원래 상태, 남은 범위·영향, 수용/거절, 주체·시각, 허용 효과 |
| `ClosureRecord` | 완료/예외 종료/취소 구분, 후보·실제 인수 또는 자동 정책·예외·외부 결과 참조, 확정 시각 |
| `CaseRelation` | 이전 Case·새 Case, 후속 수정 등 연결 사유, 새 요청 출처 |

기존 게시 기록에는 대상 Case·이슈·산출물 버전과 전송 상태를 연결한다. 첫 버전은 한 소유자를 식별하고 결정 주체를 기록한다. 예외는 대상 후보·기준과 해당 종료에만 적용하며 미래 업무나 추가 실행으로 확대하지 않는다. D-57에 따라 원본·승인·증거는 자동 삭제하지 않고 각 호스트에서 하루 한 번 백업해 최근 7개를 보관한다.

## 8. 구체 사례

1. **작은 CSV 기능:** 요청한 행 범위·인코딩·권한 검증과 완료 지점을 충족했다. ask-on-decision은 자동 완료하고, controlled는 변경 결과 확인 뒤 필요한 게시·CI를 마치면 완료한다. 같은 기준을 사용하되 사람 확인 기록은 실제 확인한 경우에만 남긴다.
2. **대용량 일부 미충족:** 1만 행은 통과했으나 합의한 10만 행에서 실패했다. 사람이 해당 제한과 영향을 확인해 수용하면 예외 종료한다. ‘10만 행 통과’로 표시하지 않는다.
3. **연구의 판단 불가:** 정한 비교 실험과 자료 검토를 마쳤지만 우열을 확정하지 못했다. 합의한 종료 조건이 근거·한계 보고를 요구했다면 정상 완료할 수 있다. 확정 결론 자체가 필수 기준이었다면 충족으로 표시하지 않는다.
4. **완료 후 조건 추가:** 완료된 CSV 업무에 ‘선택한 열만 내보내기’가 추가된다. 새 Case를 만들고 이전 결과를 연결하며, GitHub 연결·게시 허용이 갖춰지면 새 기록 이슈를 만든다. 명확한 새 요청은 자동 정리·검증하여 진행하며 GitHub 요약 게시 장애만으로 내부 작업을 멈추지 않는다.
5. **원격 CI 뒤 수정:** controlled에서 A를 확인받아 게시했지만 CI가 결함을 발견했다. B로 수정·검증한 변경 결과를 다시 확인받고 게시한다. A와 내용이 같은 요청의 재전송에는 재확인을 요구하지 않는다.
6. **결함이 이미 해결됨:** 대상 버전에서 요구 동작이 충족됨을 증거로 확인하면 코드 변경 없이 완료할 수 있다. 단순 미재현을 해결로 표시하지 않는다.

## 9. 수용 시나리오

1. 같은 후보를 ask-on-decision과 controlled에서 평가하면 성공 기준 판정은 같다. controlled는 결과 확인을 게시 전에 수행하며 필요한 외부 검증 이후에 종료한다.
2. 어느 모드에서든 필수 미충족·미검증이 남으면 AI가 예외를 수용하지 않는다. 사람이 수용한 뒤에도 원래 결과와 적용 범위가 보인다.
3. 최종 확인 도중 코드 변경이나 실행 상태 불명이 발생하면 오래된 후보로 종료를 확정하지 않는다. 인수와 실제 중단 확인을 구분한다.
4. 완료 후 질문은 설명으로 처리하고 수정 요청은 연결된 새 Case로 처리한다. 새 기록 이슈는 업무화·연결·게시 허용 조건에 따른다. 과거 위임을 새 범위로 확대하지 않으며 명확한 새 요청의 승인을 반복 요구하지 않는다.
5. 요약 게시 장애 중에도 내부 산출물·인수·종료 기록이 보존된다. 요청한 완료 지점에 포함된 PR·CI 실패는 요약 게시 장애와 구분하며, 일부 게시·응답 유실을 대조한 뒤 제한 재시도한다.
6. 예산 소진·권한 철회 뒤 늦게 온 결과를 기록하되 새 무허가 실행·중복 게시·자동 롤백을 만들지 않는다.
7. 지식 후보의 미채택이 기준을 충족한 Case 완료를 막지 않으며 추출 비용이 전체 예산에서 누락되지 않는다.
8. 완료된 결과에 대한 설명 실행은 같은 Case의 총예산을 사용하고 종료 후 소비로 구분된다. 한도가 부족하면 설명 실행을 우회하지 않으며 한도 변경 뒤에도 종료 당시 완료 판정·기준·정책 기록은 그대로다.
