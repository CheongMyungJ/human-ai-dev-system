# 업무 완료·예외 종료와 후속 업무 설계안

작성일: 2026-09-20. 상태: 사용자 결정 D-31~58을 반영한 v0.6 상세 설계안. 구현·운영 검증 결과는 아니다.

## 1. 확인된 정책

| 결정 | 동작 |
|---|---|
| D-31 최종 확인 | 기본은 사람이 최종 결과를 확인한 뒤 완료. Case별로 자동 완료 선택 가능 |
| D-32 예외 종료 | 사람이 남은 문제·미검증 범위와 영향을 확인하고 예외를 수용하여 종료 가능. 미충족·미검증 모두 대상이며 원래 판정을 통과로 바꾸지 않음 |
| D-33 완료 후 수정 | 기존 Case를 재개하지 않고 연결된 새 Case로 처리. 질문만 들어오면 기존 결과에 대해 설명 가능 |
| D-34 기록 이슈 | 매 Case마다 새 GitHub 기록용 이슈 생성. 기존 이슈는 출처로 연결 |
| D-35 게시 장애 | GitHub 게시 장애 중에도 내부 업무 계속. 게시를 대기·재시도하고 누적 실패를 알림 |

## 2. 상태와 판단의 분리

의도 동의, 품질 게이트, 성공 기준, 최종 인수, 예외 수용, 외부 반영 권한은 각각 관리한다. 사람의 최종 확인이 검증 실패를 없애거나 외부 실행 권한을 자동 부여하지 않는다. QG-06 업무 종료 게이트를 꺼도 이 완료 정책과 합의한 성공 기준은 유지한다.

| 표시 상태 | 의미와 이후 동작 |
|---|---|
| 최종 확인 대기 | 종료 후보가 준비되어 사람의 인수를 기다림. 수정 요청 가능 |
| 완료 | 현재 합의된 종료 조건을 충족하고 필요한 인수 또는 자동 완료 정책을 적용함 |
| 예외 수용으로 종료 | 지정된 미충족·미검증을 사람이 알고 수용함. 기준별 원래 결과와 예외를 함께 표시 |
| 일시 중지 | 실행을 잠시 멈춤. 같은 Case의 진행 상태와 재개할 작업을 유지 |
| 취소 | 업무 수행을 중단하는 결정. 성공이나 예외 인수로 표시하지 않음 |

최종 확인 전의 수정 요청은 진행 중인 같은 Case에 반영한다. 완료 또는 예외 종료 후의 수정 요청은 새 Case로 연결한다. 취소는 성공 인수가 아니며 실행 중단 요청과 실제 중단 확인을 구분해 기록한다. 실제 공동 작업의 역할·권한 기능은 이후 확장이다.

## 3. 결과 후보와 최종 인수

완료를 요청할 때 `최종 결과 후보`를 만들고 다음 내용을 하나의 버전으로 연결한다.

- 현재 의도·결정·종료 조건과 결과 산출물·코드 버전.
- 성공 기준별 결과와 실제 증거, 적용 게이트의 상태.
- 남은 문제·미검증 범위·알려진 영향과 제안 후속 조치.
- 업무 범위에 포함된 PR·CI 등 외부 결과 및 진행 중 실행 상태.

최종 확인 화면에는 결과 요약과 기준별 근거를 먼저 보여주고 `이 결과 인수`, `수정 요청`, 해당할 때 `표시된 예외를 수용하여 종료`를 구분한다. 인수는 사용자가 본 후보 버전에 연결한다. 후보가 의미 있게 바뀌면 이전 확인을 새 결과의 인수로 사용하지 않는다. 관련 없는 대화나 GitHub 게시 상태만 바뀐 경우까지 다시 확인하지 않는다.

기본 모드에서는 준비된 후보를 보여주고 사람 확인을 기다린다. Case의 자동 완료 모드에서는 유효한 필수 기준·적용 조건과 필요한 외부 결과가 충족되면 정책에 따라 완료한다. 기록에는 `자동 완료`와 적용 정책을 남기며 사람이 인수했다고 표시하지 않는다.

자동 완료 모드도 미충족·미검증을 스스로 예외 수용하지 않는다. 예외가 필요하면 대상과 영향을 제시하여 사람의 명시적인 결정을 받는다. 무응답·시간 경과·AI의 낙관적 판단은 인수나 예외 수용이 아니다.

## 4. 예외 수용의 범위

예외 요청에는 어떤 기준·검사·결과가 미충족 또는 미검증인지, 무엇을 확인했고 무엇을 확인하지 못했는지, 관찰된 영향과 아직 알 수 없는 영향을 구분해 적는다. 사용자는 제시된 예외를 선택하여 수용하거나 수정을 요청할 수 있다.

한 화면에서 최종 인수와 표시된 예외를 함께 결정할 수 있다. 내부적으로는 `최종 인수 기록`과 `예외 결정 기록`을 분리해 보존한다. 예외가 적용되는 대상과 종료에 미치는 효과를 명시하며, 다른 기준이나 향후 업무까지 포괄적으로 면제하지 않는다.

예외 수용은 기존 기준을 삭제하는 요구 변경과 다르다. 예를 들어 ‘10만 행 지원’ 기준이 미충족인데 이번 결과를 수용하면 기준과 실패는 남는다. 요구 자체를 ‘1만 행 지원’으로 바꾸려는 경우에는 의도·기준의 변경 절차로 처리한다. 어느 경우도 AI가 완료하기 쉽게 기준을 몰래 낮추는 방식으로 구현하지 않는다.

예외 수용 또는 최종 인수만으로 push·PR·병합을 실행하지 않는다. 외부 행동은 기존 정책에 따라 실제 대상·내용·버전에 대한 허용을 확인한다. 하나의 화면에서 함께 확인받도록 구성할 수 있지만 각각의 결정 대상을 분명히 하고 별도 기록으로 연결한다. 종료 기준에 어떤 외부 결과가 포함되는지는 각 업무의 합의에 따른다.

## 5. 실행 정리와 결과 유효성

최종 확인은 실행 프로세스의 종료 명령이 아니다. 종료 후보를 만들 때 진행 중 실행과 결과를 확인할 수 없는 실행을 표시하고, 결과에 영향을 주는 변경이 계속되는지 확인한다. 사용자가 인수하거나 예외를 수용했다고 해당 실행을 성공·취소·종료 확인으로 바꾸지 않는다.

중단이 필요하면 별도 실행 제어를 요청하고 실제 종료 또는 정리 상태를 확인한다. Runner 연결이 끊겼다면 `실행 상태 확인 필요`를 유지한다. 결과 후보의 유효성을 확정할 수 없는 실행이 남은 동안은 인수 결정을 보존하더라도 업무 종료 확정을 보류하고 해당 실행을 대조한다. 이때 같은 작업을 다른 Runner에서 즉시 중복 실행하지 않는다.

늦게 도착한 결과는 원래 실행·버전에 연결한다. 대상 코드나 기준이 달라졌으면 현재 결과를 덮어쓰지 않고 영향받는 조건만 재평가한다. 게시 재시도처럼 결과 내용을 바꾸지 않는 후속 처리와 결과를 바꾸는 개발 실행을 구분한다.

## 6. 종료 후 새 Case와 GitHub 기록

완료 후 질문은 기존 결과·증거를 설명하는 대화로 처리할 수 있다. 실제 수정·추가 개발 요청이면 기존 Case를 참조하는 새 Case를 만들고, 이전 결과·수용한 예외·새 요청을 출처로 연결한다. 기존 완료 기록은 유지하며 새 Case의 진행 상태와 섞지 않는다.

새 Case가 기능 개발이면 이전 의도를 참고해 새 초안을 작성하되, 새 업무의 최신 의도에 대한 사람 동의와 필수 의도 품질 게이트를 거친다. 이전 인수나 의도 동의를 새 기능 개발의 포괄 허용으로 사용하지 않는다. 순수 연구·설명 업무에는 기능 개발 전체 단계를 일괄 강제하지 않는다.

새 Case마다 새 기록용 GitHub 이슈를 만들고 원래 업무·기존 이슈는 출처 링크로 연결한다. 기존 이슈가 사용자 요청의 시작점이어도 기록 이슈를 재사용하지 않는다. 해당 Case의 게시 허용 범위에서 시스템 표시가 있는 요약·참조를 누적하며 코드·diff·상세 로그는 제외한다.

GitHub 게시 실패는 내부 산출물·결정의 저장 실패와 구분한다. 내부 작업은 계속하고 게시 항목을 대기열에 보존하여 재시도한다. 최종 결과도 같은 방식으로 누적하며 `완료 · GitHub 게시 대기`처럼 두 상태를 함께 표시할 수 있다. 누적 실패 알림의 구체 횟수·시간·채널은 운영 정책에서 정한다. 응답만 유실된 경우에는 외부 게시 여부를 먼저 대조해 중복 생성을 줄인다.

## 7. 최소 데이터 확장

| 기록 | 최소 정보 |
|---|---|
| `CompletionPolicy` | 프로젝트 기본값, Case별 선택, 정책 버전·변경 주체 |
| `CompletionCandidate` | Case revision, 결과·의도·기준 버전, 증거·게이트·외부 결과 참조, 미해결 항목, 실행 정리 상태 |
| `FinalAcceptance` | 후보 ID, 사람 확인/자동 정책, 결정·주체·시각, 수정 요청 |
| `ExceptionDecision` | 후보와 대상 기준·발견, 원래 상태, 남은 범위·영향, 수용/거절, 주체·시각, 허용 효과 |
| `ClosureRecord` | 완료/예외 종료/취소 구분, 후보·인수·예외 참조, 확정 시각 |
| `CaseRelation` | 이전 Case·새 Case, 후속 수정 등 연결 사유, 새 요청 출처 |

기존 게시 기록에는 대상 Case·이슈·산출물 버전과 전송 상태를 연결한다. 첫 버전은 한 소유자를 식별하고 결정 주체를 기록한다. 예외는 대상 후보·기준과 해당 종료에만 적용하며 미래 업무나 추가 실행으로 확대하지 않는다. D-57에 따라 원본·승인·증거는 자동 삭제하지 않고 각 호스트에서 하루 한 번 백업해 최근 7개를 보관한다.

## 8. 구체 사례

1. **작은 CSV 기능:** 합의한 행 범위·인코딩·권한 검증이 모두 끝났다. 기본 모드에서는 사람이 최종 결과를 인수하면 완료한다. 자동 모드면 같은 조건에서 자동 완료하고 정책을 기록한다.
2. **대용량 일부 미충족:** 1만 행은 통과했으나 합의한 10만 행에서 실패했다. 사람이 해당 제한과 영향을 확인해 수용하면 예외 종료한다. ‘10만 행 통과’로 표시하지 않는다.
3. **연구의 판단 불가:** 정한 비교 실험과 자료 검토를 마쳤지만 우열을 확정하지 못했다. 합의한 종료 조건이 근거·한계 보고를 요구했다면 정상 완료할 수 있다. 확정 결론 자체가 필수 기준이었다면 충족으로 표시하지 않는다.
4. **완료 후 조건 추가:** 완료된 CSV 업무에 ‘선택한 열만 내보내기’가 추가된다. 새 Case·새 기록 이슈를 만들고 이전 결과를 연결한다. GitHub 장애가 있어도 새 업무의 내부 의도 검토는 진행하고 게시만 대기한다.

## 9. 수용 시나리오

1. 같은 후보를 기본 모드와 자동 모드에서 평가하면 사람 확인 필요 여부만 달라지고 기준 충족 판정은 같아야 한다.
2. 자동 모드에서 미충족·미검증이 남으면 AI가 예외를 수용하지 않는다. 사람이 수용한 뒤에도 원래 결과와 적용 범위가 보인다.
3. 최종 확인 도중 코드 변경이나 실행 상태 불명이 발생하면 오래된 후보로 종료를 확정하지 않는다. 인수와 실제 중단 확인을 구분한다.
4. 완료 후 질문은 설명으로 처리하고 수정 요청은 연결된 새 Case·새 기록 이슈로 처리한다. 새 기능 의도 동의를 이전 업무로 우회하지 않는다.
5. 게시 장애 중에도 내부 산출물·인수·종료 기록이 보존되고 업무는 진행된다. 재연결 뒤 대기 게시를 처리하며 중복·응답 유실과 누적 실패를 구분한다.
